# Muad'Dib handover XXIV — 2026-09-23, Phase 3 The Meeting Loop, one story from close

Read with `docs/internal/TWO-BRAINS.md`, `pm/roadmap/holdspeak-philo/phase-3-the-meeting-loop/current-phase-status.md`, `docs/internal/philo/graph/COUNCIL.md`.

## Where the tree is (main 5de5d0c3)

- Philo Phase 2 The Graph: CLOSED (final-summary.md). Phase 3 The Meeting Loop: RATIFIED by the owner (D1 synthetic material; D2 the lane checks the install; D3 four stories; D4 dark ember).
- MERGED: A1 #613 (decisions recorded; New Decision opens its window; failures named; the 393 receipt is a full-width row per the owner; Chair shrinks under it — five Astra rounds), A3 #615 (BRIEF: READING… / DID NOT LOAD · HTTP n + Retry / No brief yet only on null / date caption; the producer-clock seam `MondayBriefService(clock=)`; the next-day brief carries the decision), A4 #614 (the Thought foot: KEPT · time / SAVING… / DID NOT SAVE · cause + Retry / CHANGED ELSEWHERE; library receipt lines to 12 px).
- OPEN: A2 PR #616 (Astra's lane, worktree `../wt-philo-3-02`, branch `feat/philo-3-02-summary`). Astra is paying Muad'Dib's counsel (`checks/story-02-built-muaddib.md`, RATIFY-WITH-CONDITIONS): C1 the failure cause in plain words (not the queue wrapper); the stale `1 QUEUED` chip beside FAILED; the refresh blanking `setMeetingDetails({})`; the transcript well COLLAPSED by default (the owner's ruling; summary open); the two raw-id strings. Astra session `01a0ccae-e5fe-7411-9f78-be398b85575a`, lane output `.tmp/astra-lane-3-02-r4.out`.

## Next session, in order

1. Read Astra's r4 lane report; run a second Muad'Dib counsel (Opus 5.5 worker, read-only, check shape) on #616; append to `checks/story-02-built-muaddib.md`; commit on Astra's branch (`dw contract new --story PHILO-3-02`); merge on a lifted verdict (reconcile with main first); remove `../wt-philo-3-02`.
2. The closure chain: worktree `../wt-philo-3-close` off main; an Opus 5.5 worker runs end to end on the rig at 1440 and 393: import the synthetic meeting (`tests/fixtures/philo3_architect_meeting.*`) → summary on the Arrival with the actual LAN host (`http://192.168.1.43:8080`, Qwen3.6-35B) → restart → the same summary → record a decision through the face → advance the producer day (rig `clock` adapter `producer-clock`) → the dated brief with the decision. Retained observations; technical completion and usefulness SEPARATE (usefulness was PARTIAL in A2: two of three planted decisions corrupted or lost by ASR/summary).
3. `phase-3-the-meeting-loop/final-summary.md` (jobs and mornings; both brains' verdicts; what he still cannot do); flip the exits; commit/PR/merge.
4. The owner's sitting: SITTING-07 steps on his own desk (his DB was wiped 2026-09-21 and is fresh); the ember look stays.

## Laws learned this phase (add to briefs)

- Restore only ` M` tracked shots after rig runs (`git show HEAD:path`), never `??` — a loop truncated 17 untracked shots once.
- `scripts/check_web_baseline.py` matches failing test NAMES: a new failing path under a baselined name reads as "zero branch-new". Run the specific fence directly.
- Astra's `claude -p` fails from an isolated HOME ("Not logged in"); Muad'Dib checks Astra's designs from his own session and records them in the lane's checks/.
- Every evidence capture: `set -o pipefail`, no `| tail` on the command under capture.
- Philo `api-reference.json` drifts whenever a test file that calls routes is added: regenerate before pushing.
- The atlas/graph schemas must admit what the rig runs (check steps in setup, http_fault boundaries, phase-3 jobs) — widen both consistently.
- Face changes are designed on the canvas and the owner ratifies; repairs (opening an existing window, library Buttons) are not design; placement is.

## Open ledger (deferred by the council, not lost)

First-value recovery destinations; receipt targets and restore; Recall's recent-mode refresh; Rhythm/Settings local time; the arrival's Record feedback; Concierge download/cloud Check; Thread Keep-as-note; raw controls outside the library; `.surface-footer-readiness` at 10 px; the aftercare panel covering verbs; the supersede route shadowed with no change callbacks; ASR instability on the fixture; the four Constitution amendments still unruled.
