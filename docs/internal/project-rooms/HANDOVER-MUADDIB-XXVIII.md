# Muad'Dib handover XXVIII (r2 after Astra's check, RATIFY-WITH-CONDITIONS paid; the counter-proposal adopted) — 2026-09-24 night: the method, and the two roads the owner asked for

Read with `docs/internal/TWO-BRAINS.md`, XXVII (Phase 5 closed), `pm/roadmap/holdspeak-philo/phase-5-the-one-service-layer/final-summary.md`, `pm/roadmap/holdspeak/BACKLOG.md` (PHILO-5 follow-ups), and `pm/roadmap/holdspeak-philo/PHASE-6-7-CHARTER-DRAFTS.md` (this session's proposals, unratified).

## The owner's ask (verbatim, 2026-09-24)

"Let's write a handover to work the same way on the morning path repairs? And... rolling more and more capabilities of our platform through this native mcp way? So we can, one day, drive it fully via MCP? (aka, new slice of mcp)"

## The method ("the same way") — what Phases 3–5 settled

1. **Charter from evidence, not taste.** A phase opens from a ledger the previous phase measured (a closure run, a census, a counsel's MISSED list). Every story names its Problem with code lines, its edges/states, its acceptance boxes, its rig case at 1440 and 393, and its effort as a council-style estimate.
2. **Both brains, every artefact.** Muad'Dib authors → Astra checks (or the reverse); nothing authored is acted on unchecked; checks are recorded under `checks/` verbatim; a BOUNCE is paid in a round, a RATIFY-WITH-CONDITIONS is paid in the same commit; the orchestrating session alone merges. The other brain checks (Astra's `claude -p` sessions ARE Muad'Dib's channel when Astra launches them, TWO-BRAINS.md:114); the lane owner merges after counsel on built; the orchestrating session's own counsel is recorded beside, never above.
3. **Design before build where a verb or a word moves.** Canvas from the real library species, both widths, exact ASD-STE100 strings, fixtures from the REAL producer (`_compose`), the owner ratifies on the canvas (AskUserQuestion is the fastest way to get his rulings). Repairs (an existing state drawn honestly, a library species bug) need no canvas.
4. **Fences red first.** Every repaired seam has a fence that failed on a `git archive origin/main` copy with the branch's tests overlaid; structural invariants get deliberate mutations; "Unknown tool" / missing-symbol failures are never counted as red; every migrated operation ships a three-state compatibility table (base, round one, built); a declared result shape runs its real producer.
5. **The rig proves durable state; the browser proves the face.** `op` cases (headless, ~2.7 s, registry inside the owning hub via `/api/mcp`) for outcomes; browser cases at both widths for what he sees; `readable_text` (visible, in viewport, unobscured) not text containment; face verdicts never claimed by `op`.
6. **Lane law.** Scoped tests, isolated HOME, never a serial full suite (CI runs it); shots from glass tests only with `HOLDSPEAK_EVIDENCE_WRITE=1`; the rig takes `--out`; `git status` clean of `pm/roadmap/holdspeak/` after runs; never symlink `node_modules`; a worktree per lane, removed after merge; `git log -1` after every gated commit (the gate can refuse silently under a grep).
7. **Merge law (the owner's ruling).** Merge on the other brain's lifted verdict + green scoped fences + the fast CI jobs; the heavy jobs (~1 h) are not waited on, but their triage is assigned afterwards and failure NAMES are compared against the relevant base — the 44 at `497d90f3` is historical evidence, not a permanent allowance. Carried with it: words and the ACTIVE atlas fences change together; receipts survive every rendered branch; same-hub object identity ≠ restart durability; palette refusals are exercised through dispatch; generated references stay current; real/replayed provenance stays separate.
8. **Records.** The phase status is the canon; `final-summary.md` closes with both brains' verdicts per story, the pre-fix reds per story, and the ledger of what he still cannot do; his sitting is "rehearsed, owner-reviewed shots" unless he sits; memory + a handover at every session end.

## Road A — the morning path repairs (PROPOSED Phase 6 "The Honest Morning", bounded)

From the PHILO-5 follow-ups, the items ON HIS MORNING PATH, each canvas-free (an existing idiom drawn honestly) with a red-pre-fix fence and a rig/browser case at both widths:
1. A failed import shows SAVED (`intelBadge.ts:26`; `ChairHome.tsx:265` never passes `import_failed`; AND the wire adapter drops transcription status, `web/src/desk/api.ts:463` — the helper alone cannot repair the face) → the existing FAILED idiom; fence producer → wire adaptation → rendered badge.
2. The brief head vs receipt count (5 vs 6; `briefEgress.tsx:43-52` vs `ChairHome.tsx:875-886`) and the two time formats (`GENERATED SEP 25 18:19` vs `6:19 PM`; `routes/monday_brief.py:47-58`) → ONE time format (an existing idiom, canvas-free); the counts have DIFFERENT lawful meanings (generated items vs currently unhandled; THIS WEEK excluded from the Arrival) — preserve both meanings and the historical snapshot; if a label or meaning changes, a SMALL CANVAS AMENDMENT first.
3. The `MEETING READY · N open · 0 decided` toast: no zero token at render (UX-CANON A.8; canvas-free) — and its PLACEMENT (fixed, hard-coded bottom offset, covers the summary and capture bar at 393) needs a small CANVAS showing the unobstructed placement at both widths before build (`AmbientLayer.tsx:175`, `intelligenceAttention.ts:95-96`).
4. The raw pipeline item counted in the brief (`monday_brief_service.py:500`) AND the same raw wording in breakage titles (`:597`) → human producer wording that names the recorded action accurately (a summary REQUEST is not a completion), consistent count.
5. Two broke rows for one missing decision read (`routes/decisions.py:58-61`; retained observer rows 67 and 68 vs op row 6) → one cause, one row (fixes the op/browser parity FAIL 2:1); preserve legacy decision reads; never obtain parity by suppressing failures globally.
6. The S4 ~1 s empty decision body after Done at 393 (the save/exit seam is `DecisionPullout.tsx:66`; `:69-71` is status cycling; `dataSlice.ts:297` already patches text optimistically, so "await the refresh" is NOT yet a diagnosis) → find the cause; fence the initial transition and failed-save behaviour, not eventual text.
Out: the Info-window rename (`name` ignored — needs a decision on the field, small but a contract question), the shelf enum drift (Road B), the Article XI ruling (his). Estimate: 3–4 engineering days PROVISIONAL (the toast canvas, the S4 diagnosis, counsel rounds and the closing runs are inside it; two lanes do not halve it). Lanes: Muad'Dib 1/2/4 owning `ChairHome.tsx` exclusively; Astra 3/5/6 (the toast's capture-clearance seam at `ChairHome.tsx:685` is handed over by patch, not co-edited); shared atlas/generated files have one shipping owner per commit. One closing chain on the rig (the Phase 4 exit-1 case + the Phase 5 rehearsal re-run) — his review of the shots.

## Road B — the next MCP slice (PROPOSED Phase 7 "The Desk on the Contract")

**The census at main `189a6b52` (residual-set.json, 320 identities: 256 MCP, 64 HTTP; reproduced by Astra):** MCP — desk 46 · project 42 · people 17 · thought 16 · provider 13 · cadence 11 · workbench 10 · model_library 7 · seven families at 5 (concierge, decision_record, inference_assignment, meeting, reaction, scheduled_recording, watch) · five at 4 (ask, heartbeat, interview, plugin_job, recipe) · six at 3 (coder, follow_through, kb, practice_recipe, settings, zone) · seven at 2 · seven at 1. HTTP — settings 5 (mostly heartbeat constructions, not five settings capabilities) · projects 4 · automations 3 · monday_brief 3 · eight at 2 · 33 at 1. Counting both transports: eight families at 5, eight at 3. **Arithmetic:** 320 − 46 − 42 = 232, not "below ~180"; further payments must be enumerated per phase. The "desk 46" are 42 primitive identities (incl. compatibility aliases) + `desk.snapshot`, `desk.needs_you` and two workbench verbs; the kinds are notes, decisions, kbs, directories, workflows, chains — NOT artifacts; zone membership is 3 more, knowledge membership 3, `decision.supersede` 1.

**Recommendation (Astra's counter-proposal, ADOPTED): Road A first; then a BOUNDED desk slice — notes, zones/directories, knowledge membership, and the remaining decision operations: 33 MCP residual identities + 3 relevant HTTP constructor identities (not a guaranteed 36-entry reduction while fallbacks remain). Workflows/chains, the workbench aliases and the aggregate reads (`desk.snapshot`, `desk.needs_you`) are PARKED with their owning slices.** Why: it completes a concrete filing/retrieval JOB for him (file a note, find it, put it in a zone); it finishes what Phase 5 started on decisions (delete/status/supersede). Not "uniform": Thought-owned notes carry revision behaviour (`primitive_service.py:91`) and workbench execution differs — a finite explicit descriptor TABLE (each row naming its real callable, schema, result, refusals, exposure, completion) is lawful under the settled Declaration position; a discovery framework or universal CRUD semantics is not. "Owner-only where a verb writes" is a POLICY CHANGE, not a consequence: preserve existing authority behaviour or name and ratify the change; owner-only is not kernel admission. Then **projects (42)** — the Room, the connectors, the steward — his second job. People (17) after: its own store and custody (keychain) deserve their own boundary story. Provider/settings move first only if setup blocks the chosen job.

**Shape (four sequential stories):** 01 explicit primitive contracts (notes, directories, kbs) + DISCOVERY — the tool descriptions name the jobs (a client without the repo finds them) + the shelf-enum alignment repair (assigned here); 02 memberships (zone, knowledge) + decision completion (delete/status/supersede) under the NAMED authority settlement (his Article XI ruling decides admission + receipts for filing; migration-only work may proceed carrying the debt, never inventing an exemption); 03 NEW actual-atlas browser cases (the atlas has note cases but none for directories, kbs, zone membership, supersede) plus their `.op` siblings and the equivalence run; 04 the owner asks in ordinary words for a filing/retrieval job from a client WITHOUT repository access (an acceptance criterion this time), a DURABLE relationship for "attach to a meeting" (defined with its readback, or the job changed — a Markdown link never satisfies it), both-width shots, rehearsed and owner-reviewed. Estimate: NOT yet grounded — re-estimate before build authorization once the new atlas cases and the admission scope are known (Phase 5's 11–14 d is the only precedent). **Toward "drive it fully via MCP" — the definition of done:** every in-scope server capability discoverable and executable through MCP, reaching the declared operation and the bound service, with demonstrated authority, outcomes, refusals and readback; a reasoned exceptions inventory for what stays hand-written by design (multipart parsing, file custody and cleanup, response/envelope mapping, authentication, URI handling; rig setup/restart/clock/observation; window and layout verbs). An empty residual census is neither necessary nor sufficient (it counts constructors and declared exposure, not traversal).

**His decisions (AskUserQuestion, in Astra's words):** D1 "Which job next: file and find desk notes, work in a project room, or prepare work for your reports?"; D2 "Complete the morning repairs first, or run the MCP work alongside them?" (recommended: repairs first); D3 "Which named local edits require kernel admission and receipts?" — his gesture already approves the act; any exemption needs an explicit constitutional basis or amendment; D4 Codex stays the rehearsal client unless he wants another; the discovery-without-repo and the already-open-Desk-freshness promises are stated because they set scope.

## Laws learned this week (carried)

See XXVII §"Laws learned"; plus: a charter's baseline is a census pinned to a commit with stated counting rules, never a grep; a lane may start from main before a records PR lands — verify at counsel.
