# PHILO-3-02 — full-suite classification

The quiet, serial Python run completed with **15 failed, 11,530 passed, 116 skipped, 4 xfailed, 2 warnings and 11 errors in 5687.30 seconds**. [Complete raw output](full-python.raw.log) is retained, including skip/xfail reasons. It ran through Delivery Workbench capture, with a fresh HOME, Node 22, the shared browser/npm caches, and `--ignore=tests/e2e/test_metal.py`. No worker edited during the full run and no second hub ran alongside it. This is **not a full-suite green claim**.

`ORCHESTRATION.md` requires (a) old posture to be corrected when introduced by this lane, (b) real lane regressions to be fixed, and (c) flake claims to have two serial green runs. It separately requires reproduction on main before inherited debt is ledgered. No failure below is labelled (c).

## Lane fallout

| Full-run failures | Class | Cause and correction | Proof |
| --- | --- | --- | --- |
| Eleven `test_graph_walk_calibration.py` setup errors | b — calibration adapter regression | The calibration adapter lacked the real Hub's `url` property used by trigger-response capture. Align the adapter, preserving calibration assertions. | Full log 165–2021; calibration focused collection/run beside this file |
| `test_the_real_j4_import_case_fires_the_upload_and_binds_the_meeting_id` | b — calibration transport/completion regression; a — stale note assertion | The adapter lacked multipart `form` and the now-required completed-import read. Model the separate 202/200 calibration steps explicitly. Read the named ID from the observed row, not an old predicate-note wording. Calibration is not real-import proof; the actual atlas runs remain the latter. | Full log 3564–3682; first correction exposes the stale note assertion in `calibration-fallout.fixed.raw.log` |
| Three summary/restart glass cases in `test_hs201_lane_a_glass.py` and `test_hs201_summary_face_glass.py` | a — old single-surface selector | Arrival now intentionally displays the same transcript/summary as Meetings. Scope the existing Meetings assertions to the Meetings surface; preserve their content, receipt, restart and action checks. | Full log 2970–3563; original strict-mode failures name both rendered surfaces |
| `test_private_backend_route_and_recovery_decisions_are_classified` | a — new projection classification omitted | Classify `_is_scheduled_retry` as a durable lineage projection; it neither selects a route nor dispatches. Do not bypass the census. | Full log 3684–3701; isolated red repeated in `unit-fallout.first.raw.log` |
| `test_primary_copy_has_no_prohibited_operational_drift` | b — new read-failure facts incomplete | Retain SUMMARY · READ FAILED, show its actual cause and MEETING KEPT, with the existing row action. Fence the rendered failure/retention/action together. Keep the copy parser unchanged. | Full log 3724–3741; isolated red repeated in `unit-fallout.first.raw.log` |

Post-correction calibration + rig + actual-atlas checks: **144 collected, 144 passed in 100.06 seconds**. [Collection](calibration-fallout.collect.raw.log), [final run](calibration-fallout.final.raw.log). The full run above remains unchanged. The copy/census and all three affected summary/restart glass cases pass together: **19 collected, 19 passed in 45.92 seconds**, through DW capture ([collection](summary-fallout.collect.raw.log), [run](summary-fallout.fixed.raw.log)). The final rendered read-error fence fails against the pre-fix component at the missing retention fact ([red](read-facts.final-pre-fix.raw.log)); all eleven Arrival completion tests pass after correction ([green](read-facts.post-fix.raw.log)). Thus the six lane failures and eleven calibration errors are corrected in focused verification. The nine inherited failures below remain; no second full Python green is claimed.

## Inherited debt — reproduced on main

Astra made a fresh `git archive` of **`7b2c2b2afa7c7dd02b56ce97f269d6f1585042c0`**, outside the main checkout, at `.tmp/philo3-full-main-ks2lbj0a`. The baseline imported its own `holdspeak/__init__.py` and built its own web assets using Node 22. Baseline index SHA-256: **`e964afc36cae543835714be3e771679d333050e23ae15b412f40bf5322d5b605`**. The same environment and isolated HOME reproduced **9 failures, 4 passes** in 74.55 seconds. [Baseline output](main-baseline-fallout.raw.log). Four glass cases also failed in a fresh focused lane run ([output](glass-fallout.first.raw.log), 33.90 seconds). No inherited failure is silently excluded from the full count.

| Exact test / parameterization | Class | Observed cause | Home and follow-up |
| --- | --- | --- | --- |
| `test_hs170_meetings_glass.py::TestMeetingsGlass::test_meetings_face` | a, inherited | Fixture seeds actuator proposals; facets now require open action items. Missing `meetings-facets` on main too. | Existing HS-170 glass fixture; update its producer setup under that test's owner. |
| `test_hs200_attention_glass.py::TestAttentionGlass::test_long_row_393` | a, inherited | Test scrolls `window`; `.chair` owns scrolling. Main's last section bottom 972.296875 is below bar top 821. | HS-200 attention glass; drive the actual scroll container and retain hit proof. |
| `test_hs200_preparation_brief_glass.py::test_running_then_kept_brief_with_claims_and_not_read[393]` | b, inherited | NOT INCLUDED click does not expose `prepare-not-included` within five seconds, on main and lane. | Existing preparation face; council follow-up to Disclosure/PreparePosture. No A2 redesign. |
| `test_hs200_task_resume_glass.py::test_saved_ask_draws_the_ratified_unfinished_row[393]` | b, inherited | Unfinished row opens behind composer: `covered=1`, `verbClear=-101`, on main and lane. | Existing task-resume face; council follow-up to ProjectRoomCore/composer geometry. |
| `test_hs201_12_thought_note_glass.py::test_thought_note_is_one_clean_note[1440]` and `[393]` | a, inherited | Broad receipt selector now matches both KEPT and filing lines from A4. | A4/Thought glass; scope filing assertions and preserve separate kept-receipt checks. |
| `test_hs201_12_thought_note_glass.py::test_thought_note_long_context_and_open_well_never_clip[1440]` and `[393]` | a, inherited | Same two-line strict-mode mismatch before geometry checks. | A4/Thought glass; same selector correction. |
| `test_phase200_continuity.py::test_the_last_known_observation_survives_a_restart` | a, inherited | Hard-coded 2026-09-07 observation is beyond the intentional 14-day retention horizon on 2026-09-23. | Existing continuity fixture; pin an explicit test clock or use a recent observation. Keep product TTL. |

These nine pre-existing failures are retained debt, not new lane regressions and not flake claims. The two existing phone defects remain real owner costs. Their repair would widen the council's A2 scope; the evidence is available to the next owning lane.

## Web suite

The integrated full `npm run check` passed **306 files / 2,806 tests**, including token/architecture checks, typecheck, production build and bundle budget. [Web record](web-check.md). After the read-failure correction, the full check passes again: **306 files / 2,806 tests**, 89.60 seconds for vitest; typecheck, production build and bundle gate pass. [Final complete log](full-web-final.raw.log), DW capture `2026-09-23T19:08:27Z`. Desk JS 1,320,404 B; CSS 319,048 B; source maps 0. The first final attempt caught a test-only `getByRole` option type error; its [raw output](full-web-fallout-first.raw.log) is retained. Anchored name matches correct that option without changing the assertions or product.

Astra reviewed and restored **511 explicit tracked ` M` evidence paths** rewritten by the full/focused suites, using successful `git show HEAD:path` reads and preserving their test output under `.tmp/philo3-full-rewrites/`. [Path/hash receipt](restored-tracked-evidence.json). All 135 then-untracked files were verified byte-for-byte unchanged. Nine newly generated unrelated test shots were then parked by explicit path, with hashes retained in [parked-test-shots.json](parked-test-shots.json). No restore touched an untracked file or any A2 observation.
