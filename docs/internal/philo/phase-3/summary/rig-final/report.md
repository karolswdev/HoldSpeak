# PHILO-3-02 — actual-atlas proof repairs

Astra completed the rig repairs after the worker handoff. The current atlas uses the retained 34.67525-second synthetic architect meeting. Import waits for the real worker to report `transcription_status=complete`, positive duration, and transcript segments. The Models window must close before Arrival is observed. Each J6/J7 recipe has one Run; queued admission reads the trigger response, running reads the real `claimed` wire state, and ready reads `succeeded`.

The retained provider failure is substituted only at the named provider boundary; producer admission, route binding, receipts, queue settlement, persistence, and browser refresh remain real. The conductor captures retry settings at startup, so all three failure recipes restart after PUT settings and import, before Run. Terminal failure requires `FAILED\nLAST ATTEMPT`; LAST ERROR alone also matches retrying and was insufficient. The pre-correction run is retained and excluded from terminal-failure proof. Manual retry proves only admission; the observed successor is RESERVED, not completed recovery.

Restart records old/new process IDs, one DB path, complete before/after meeting payloads, summary equality, receipt equality and meeting identity. J5 uses real host text and nine-point ownership of Run; phone requires 44 px height, desktop 24 px. Astra observed 24 px on the actual phone before the CSS fix, then 44 px after it.

The no-engine absence is checked inside the existing Meetings section. Checking inside a missing Run selector earned BLOCKED and proved nothing; that original run and the red/green fence are retained. Microphone/record-only cases remain lawfully blocked; stop requires a genuinely active recording; no-transcript refusal requires a real empty transcript. No fixture row is injected to manufacture those states.

Pre-fix source is HEAD `aac7544e`, copied with git show into an isolated proof directory. The final tests, retained WAV and schemas are test inputs only. `pre-fix.raw.log`:24 failures, 3 passes, 47 deselected before the two later recipe fences. `failure-recipe.pre-fix.raw.log`:2 failures. `no-engine.pre-fix.raw.log`:1 failure; its post-fix run passes. Final focused collection/run and the actual observation index are recorded separately. Missing helper APIs are tooling pre-fix failures, not claimed rendered product failures.

All observations are immutable, including blocked runs and the failure recipe's earlier false positive. The final closure index will identify the current run for each case/width. No owner sitting or complete planted-fact recovery is inferred from technical completion.

The reload recipe originally reloaded while the job was still running. Its corrected fence first waits for a rendered summary and retains the durable detail's exact text, then compares it after reload. `reload.pre-fix.raw.log` retains two failing fences. Because completed actions can move the Meetings section below the fold, an optional `frame_selector` records a separately labelled centered scroll and `framed.png` after the raw observation. Raw before/after shots, predicate, completion time and verdict remain unchanged. `framing.pre-fix.raw.log` fails before the helper exists; the focused fence ensures a failed framing operation cannot erase the raw result or invent a shot. Actual atlas runs verify the resulting glass capture.

The integrated actual ready run exposed a further tooling error: `/api/intel/jobs` is the active queue, so succeeded jobs disappear. The ready and actual-host recipes now read durable meeting detail; two pre-fix recipe failures and a real-producer success check retain this boundary. The interrupted run, including its original incomplete observation, is named in `terminal-read-interruption.md`; it is excluded from completion evidence. Corrected actual ready runs pass at both widths.
