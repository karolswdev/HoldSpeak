# PHILO-3-02 — technical completion

The imported-meeting chain completes at 1440 and 393 on the integrated A1/A2/A4 build. This is a technical verdict. [Usefulness](usefulness.md) is partial: the planted facts are not all recovered, and the owner has not sat with the finished chain.

## Retained observations

[The index](observations-index.md) links all **60 observations**, their before/after shots, separately labelled framing shots, revisions, build hashes and full real-engine output. Its [JSON](observations-index.json) selects the latest complete `final/` observation for each of the **20 actual atlas cases at two widths**. There are no missing pairs or parse errors: **30 pass; 10 blocked**. Blocked is not a pass. The native recording cases cannot run without the prohibited microphone/active recording, and the transcript-absent case cannot be manufactured from this speech fixture. The interrupted ready run remains incomplete in the index.

Every invocation used `scripts/graph_walk.py run --atlas docs/internal/philo/graph/atlas.json`, one case and one hub at a time, with an isolated HOME and the retained synthetic WAV. `atlas-phase3.json`'s `extends` field is metadata, not implemented execution inheritance; these runs explicitly select the base atlas containing J4–J7.

The integrated build used for the complete matrix has index SHA-256 `f7df17ed5696e8a501abac65ba23f861455e1a0c4506142eeeb259d8c6087642`, asset `index-EPIMRzZ0.js`, at revision `0ad59c0488ba100b516e3b1763d93a9a81f486ac` with the retained recipe/reference edits dirty. It includes main `7b2c2b2a` (A1) and `8c078662` (A4). Earlier runs remain explicitly attributed to their own revision/build.

## Acceptance reads

All run directories below are under `pm/roadmap/holdspeak-philo/phase-3-the-meeting-loop/assets/story-02-shots/rig/final/`; the index supplies direct links.

| Result | 1440 observation | 393 observation | Reading |
| --- | --- | --- | --- |
| Completed import | `20260923T165203Z-case.j4.meetings_import.imported-astra-1440` | `20260923T165230Z-case.j4.meetings_import.imported-astra-393` | Actual import worker complete; duration 34.67525 s; nonempty segments. Browser row proof follows in J5/J6. |
| Planned host and clear Run | `20260923T165407Z-case.j5.meeting_open.planned_host_disclosed-astra-1440` | `20260923T165448Z-case.j5.meeting_open.planned_host_disclosed-astra-393` | LAN host before Run; all nine hit samples owned; phone Run height 44 px. |
| One Run, summary and actual host arrive | `20260923T191042Z-case.j6.run_summary.summary_text-astra-1440` | `20260923T191150Z-case.j6.run_summary.summary_text-astra-393` | First summary at 6.691 s / 6.223 s; no manual refresh; actual receipt host `192.168.1.43`. Length, word count and transcript well retained. |
| Failed job and cause | `20260923T170507Z-case.j6.run_summary.intel_failed-astra-1440` | `20260923T162747Z-case.j6.run_summary.intel_failed-astra-393` | FAILED, LAST ATTEMPT and LAST ERROR; actual receipt remains; headline counts work needing attention. Provider reply replayed at its named boundary. |
| Scheduled retry and cause | `20260923T170618Z-case.j6.run_summary.retrying-astra-1440` | `20260923T170648Z-case.j6.run_summary.retrying-astra-393` | RETRYING with durable predecessor cause and receipt. No invented UI attempt limit. |
| Exact text after reload | `20260923T170959Z-case.j7.arrival_load.reload_persisted-astra-1440` | `20260923T163802Z-case.j7.arrival_load.reload_persisted-astra-393` | Recipe waits for a summary before reload and compares its complete text afterwards. |
| Exact summary/receipt/meeting after restart | `20260923T171417Z-case.j7.hub_restart.intel_retained-astra-1440` | `20260923T171510Z-case.j7.hub_restart.intel_retained-astra-393` | PID 15536 → 15798, meeting `3511adbe`; PID 16026 → 16275, meeting `6d66aa30`. One DB path per run; all three equality flags true. |

The actual endpoint identifies itself as **Qwen3.6-35B-A3B-UD-Q5_K_XL.gguf**, owned by **llamacpp**, at `http://192.168.1.43:8080`. Each real-engine observation retains its `/v1/models` identity and each completed execution its actual route receipt. ASR uses `mlx-community/whisper-base-mlx`; its errors remain in the transcript. The fixture is generated speech, not the owner's recording.

## Limits and ledger

- At 393, the existing aftercare panel covers the last summary line in the raw J6 arrival capture. One normal scroll reveals it fully; `framed.png` and its nine owned hit samples are separate from the raw timing/verdict. Open remains clear. This is a retained face cost, not a hidden successful raw hit test.
- Manual retry proves a successful retry request and a RESERVED successor (`awaiting_parent_terminal`), not completed recovery. Automatic retry and terminal failure use the retained failure reply; they are not claims about a real LAN outage.
- Some protocol `observe_at` reads repeat POST without the trigger body and retain a 400 payload. The status predicate correctly reads `trigger_response`; reviewers must distinguish the two. This tooling debt is ledgered rather than treating the second payload as the actual admission reply.
- Sealed passes are unchanged. The graph census retains five inherited miscited anchors and fourteen subtype conflicts, with no new/stale/unread source drift after regeneration. These remain PHILO-2-07 council/tooling work.
- The rig cannot establish native recording, the owner's sitting, or recovery of all planted facts. The phase exit remains open.

The red/green seam records are [install](install-d2/report.md), [queue](queue/report.md), [detail](detail/report.md), [Arrival](arrival/final-proof.md), [copy fences](doc-fences/report.md), and [actual-atlas repairs](rig-final/report.md). Full-suite classification and counsel on built are separate closure records.

After the full-suite read-failure fact correction, final build `index-CHOl_t-i.js` (index SHA-256 `3d79edd5157680ad696e3c312393b9e83feebf375da5e85181ac53c3d85496a6`) passed fresh actual J6 runs at both widths, as named in the table. The final phone summary is shorter (72 px) and owns all nine raw hit samples; the earlier 90 px summary overlap remains retained as a content-length cost. The final build changes the read-error well only; restart/product execution code remains the code used in the retained J7 identity runs.
