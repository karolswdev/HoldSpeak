# PHILO-4-02 — Astra lane report

## LANE

`feat/philo-4-02-rows`, worktree `../wt-philo-4-02`, base `566495b0`.
Two Luna lanes at the owner's `gpt-5.6-luna` / `xhigh` ruling implemented
the producer/Arrival and the readable-row predicate. Astra checked the
real producers, the negative controls, the scoped runs and the shots.

## OUTCOME

Built and verified locally; Muad'Dib's closing conditions are paid. Decisions lead the Arrival, newest first
by the source record's persisted `created_at`. Other sections keep their
order. The cap is three, with the existing headline/fold counting rule.
The PR will remain open for Muad'Dib; no merge is authorized.

Acceptance, in story order:

1. Cap provenance and owner ratification: settled by the charter check,
   Phase 4 status and story 01 canvas round six ask 2. The cap is retained.
2. Ordering: producer timestamps survive persisted reload; first paint
   and Generate replacement use the record chronology. Red/green below.
3. Readable at both widths: final 1440 and 393 pass with all nine real hit
   points owned. The captured 393 red exposes covered bottom padding;
   a longer-row fence also exposed covered content. Both seams are paid.
4. Rendered fence: several older decisions, older non-decision rows and
   one new decision; both new vitest tests fail archived main and pass
   here. The three visible rows, seven-item head and four-item fold agree.
5. Actual as-written closure: final 1440 and 393 PASS, with one next-day
   Generate and no Ack/Defer/fold. Earlier failures remain on disk. The
   independent breakage variant also passes at both widths.

## PROOF

All logs below live next to this report. DW command/output captures ship
in `pm/roadmap/holdspeak-philo/phase-4-the-morning/evidence-story-02.md`.

| Run | Verbatim result |
| --- | --- |
| archived main, producer | `3 failed in 1.29s` |
| archived main, rendered | `Test Files  1 failed (1)` / `Tests  2 failed (2)` |
| archived main, readable predicate | `3 failed, 5 passed in 5.13s` |
| scoped Python first run | `1 failed, 209 passed in 106.98s (0:01:46)` |
| repaired Python/atlas/wording fences | `90 passed in 3.35s` |
| all ChairHome rendered tests | `Test Files  14 passed (14)` / `Tests  81 passed (81)` |
| final Python/atlas/wording/breakage check | `92 passed in 3.90s` |
| final ChairHome rendered check | `Test Files  14 passed (14)` / `Tests  81 passed (81)` |
| direct covered transition, archived main | `1 failed in 18.05s` |
| direct covered transition, initial native scroll | `1 failed in 5.68s` |
| direct covered transition, final CSS | `1 passed in 10.23s` |

The sole scoped Python failure was source-reference drift. Three anchors
also fail on archived main; seventeen moved with this lane. All twenty
active references now name their actual symbols. The repair is in the
same commit; sealed observations remain unchanged.

Actual case: `case.closure.chain.s5_next_day_brief_has_it`, real LAN engine
`http://192.168.1.43:8080`, fresh HOME per invocation. Shots/observations:
`pm/roadmap/holdspeak-philo/phase-4-the-morning/assets/story-02-shots/`.

| Run ID | Width | Verdict | First decision row |
| --- | --- | --- | --- |
| `20260924T065401Z-case.closure.chain.s5_next_day_brief_has_it-astra-1440` | 1440 | PASS | `(256,359,928,44)`; all nine hit points owned |
| `20260924T065829Z-case.closure.chain.s5_next_day_brief_has_it-astra-393` | 393 | FAIL | `(12,470,369,114)`; capture bar owns the three bottom hit points |

The final build is `index-Y37FyLq1.js`. All four final walks have their
before and after shots inspected by Astra. The decision is row **1** in
every run, wholly inside the viewport, with all nine hit points owned:

| Final run ID | Width | Verdict | First decision row |
| --- | --- | --- | --- |
| `20260924T072614Z-case.closure.chain.s5_next_day_brief_has_it-astra-1440` | 1440 | PASS | `(256,359,928,44)` |
| `20260924T072514Z-case.closure.chain.s5_next_day_brief_has_it-astra-393` | 393 | PASS | `(12,356,369,114)` |
| `20260924T072814Z-case.closure.chain.s5_next_day_brief_with_breakage-astra-1440` | 1440 | PASS | `(256,404,928,44)` |
| `20260924T072722Z-case.closure.chain.s5_next_day_brief_with_breakage-astra-393` | 393 | PASS | `(12,356,369,114)` |

`closure-proof.json` contains the verified new and old brief ids/dates,
row timestamps, geometry, retained DB comparisons and breakage ids. The
as-written cases contain no breakage; only the named variants pay that
obligation. Both variants use a real failed read through the service and
observer, and record the process-only evening timezone (`Etc/GMT+11`).

The 393 bar measures 138 px. Native scroll padding is 227 px, including
the existing 89 px dock band; end clearance is 138 px. A separate real
browser fence positions the old row before the click and proves the
longer result becomes unobscured with no test scroll after Generate.
Its API replies are labelled browser fixtures, not claimed as producer
evidence. The real-producer tests and actual atlas walks supply that proof.

`verify_closure.py` checks the new brief id and next producer date, exact
source decision timestamp, position one, positive viewport geometry,
nine owned `elementFromPoint` samples, old durable brief/items/shelf
unchanged, the one Generate response and retained summary/receipt after
the hub restart. It reads retained observations and does not edit them.

## LEDGER

- **(a) Tooling:** active atlas source drift repaired; real-browser
  obstruction predicate added with negative controls. The old brief's
  DB rows are retained read-only before the rig removes its HOME.
- **(b) Product:** missing decision recency and decisions-last ordering
  repaired. The 393 success-result obstruction is repaired by measured
  CSS clearance and native nearest scrolling. No global layout redesign.
- **(c) Environment:** default Homebrew Node cannot load libllhttp;
  command-scoped installed Node 22.21.0 works. LAN engine reachable.
- Phase 3 ASR/summary fidelity, owners/dates and the owner's sitting stay
  explicit costs. This lane cannot certify the summary's usefulness.
- The face repeats the producer sort; cross-zone naive/UTC ordering and
  missing-timestamp ties remain untested. The producer is the ordering rule.
  Generate's existing focus loss while disabled remains a PHILO-4-01 cost.

## AMENDMENTS

No acceptance criterion weakened. The predicate is stronger than text
containment. The named breakage variant pays story 03's existing
scope amendment separately from the mandatory as-written case.

Canvas round-six item 7: **c1 belongs to THIS WEEK on both canvas dates**.
The real meeting/artifact/accept/commit writers and full producer prove
it in `this-week.log`. The producer suppresses its duplicate `Commitment
due` row; a still-recorded source may independently yield a review row.
Existing routing is unchanged. Muad'Dib withdrew his requested edit to the
pinned September 19 schema inventory after reading the generator: its
successful regeneration/check leaves the historical inventory unchanged.
The live boundary census is regenerated in this commit. See
`census-response.md`, `census-pinned.log`, and the phase's built check.

## UNKNOWN

The owner's sitting and summary usefulness remain unverified. Full serial
suites are assigned to PR CI by the lane brief; only scoped suites run
locally. Muad'Dib's built verdict is RATIFY-WITH-CONDITIONS, all paid;
his C2 reply is RATIFY. The final PR remains open for the invoking brain.
No merge is authorized.

## PR delivery follow-up — 2026-09-24

PR [626](https://github.com/karolswdev/HoldSpeak/pull/626) is open on
`feat/philo-4-02-rows`. The implementation commit is `44cc8521`; its gate
passed all seven rules and history verification passed. The working tree
was clean after the push. The PR remains unmerged.

The first [CI Documentation Navigation job](https://github.com/karolswdev/HoldSpeak/actions/runs/35971254436/job/107541264055) found `API reference drift:
docs/generated/api-reference.json`. Regeneration adds exactly four literal
route test candidates for `test_philo4_02_generated_clearance.py`; it does
not change a transport field or the pinned schema inventory. The complete
documentation job command chain passes locally (`ci-documentation.log`,
paired DW capture `2026-09-24T07:48:14Z`; commands match the Documentation
Navigation job in `.github/workflows/test.yml`), including nine navigation tests,
all generated-reference checks, architecture and coverage. The follow-up
commit ships this generated-file correction and its evidence. Product code
and the four final glass runs are unchanged. Broader PR CI remains pending.

The API reference check was absent from the earlier local check chain. The
complete doc-CI script now runs before every further commit in this lane.

## Check — Muad’Dib, CI correction, 2026-09-24

Claude session `ca18f694-e6e1-45f1-83e5-0ba438442be0`.

**VERDICT: RATIFY**

**FINDINGS**

1. **The drift is exactly what Astra says it is.** Comparing the committed reference against the regenerated one, four routes gained one test candidate each, all naming the new e2e fence: `/api/brief/generate` POST, `/api/brief/latest` GET, `/api/desk/needs-you` GET, `/api/inference/assignments` GET. No API field, snapshot, or schema census changed. The generator reads test candidates from the current working tree (`scripts/philo_api_reference.py:58`, `rglob('test_*.py')`) and labels them "literal route mentions, not inspected assertion or passing-test evidence" (`:71`). The e2e fence's route stubs are those literal mentions.

2. **The root cause was a missing check in the shipped commit's chain, not a wrong artifact.** `final-census.log` shows only the boundary census and the graph check; the API reference check that CI runs (`.github/workflows/test.yml:31`) was not part of the pre-commit verification. The lane added a test file that mentions routes, so the reference moved. Bookkeeping, not product.

3. **The captured verification is the real CI chain.** `.tmp/philo402-doc-ci.sh` is line for line the Documentation Navigation job (`test.yml:26-38`), run under an isolated HOME with exit 0, and captured through the evidence file (run `2026-09-24T07:48:14Z`, index tree `29420dac`). Every step reports checked or verified, including the API reference and the pinned census, which stayed at five outputs verified.

4. **A second gated commit under PHILO-4-02 is the right shape.** The story is already done (`story-02…md:5`); the commit flips nothing, touches one generated file and the story's evidence file, and evidence neither appears nor disappears orphaned. That is the "bookkeeping commit that flips nothing" TWO-BRAINS exempts from a full check, yet Astra asked anyway. Consistent with the PHILO-4-01 pattern where the generated reference was regenerated in a follow-up commit (`5fcf6409`).

**CONDITIONS**

- Stage exactly `docs/generated/api-reference.json`, the evidence file, `ci-documentation.log`, and the lane-report addendum; `dw contract new --story PHILO-4-02 --tests-capture <evidence path>#2026-09-24T07:48:14Z`; certify the boxes on the captured run's output, not on memory.
- The lane-report addendum names the cause in one line: the API reference check was absent from the local chain; the doc-CI script now runs before every commit in this lane.
- No other file in the commit. The `.tmp` script stays untracked.

**MISSED**

1. Low: the same omission will recur on any lane that adds a test file mentioning a literal route. Worth one line in the lane brief template, not this commit.
2. Nil otherwise.

**TUESDAY:** Unchanged. This commit touches nothing the owner sees.

**UNKNOWN:** I did not run the doc-CI script myself; I read its captured output and matched it against the workflow. I did not preflight `dw gate` on the unstaged tree since staging is the author's act.

Astra disposition: all conditions paid. Exactly the four named files ship;
the script stays in `.tmp`. The full captured output was read before
certifying the follow-up contract. No story flips in this commit.
