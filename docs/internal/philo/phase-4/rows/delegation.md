# PHILO-4-02 worker record

Astra owns `../wt-philo-4-02`, branch `feat/philo-4-02-rows`.
Both workers were invoked with `spawn_agent`, model `gpt-5.6-luna`,
reasoning `xhigh`, fresh context. They were told to read TWO-BRAINS first,
AGENTS, ORCHESTRATION §3, the Constitution and UX canon. No worker may
stage, capture DW evidence, flip a story, make a contract or commit.

## decision_rows

Story: `pm/roadmap/holdspeak-philo/phase-4-the-morning/story-02-decision-in-the-visible-rows.md`.
Settled design: story 01 canvas boards 1/4/5 and rounds five/six;
three-row cap kept; decisions first, newest first by record `created_at`;
other section order and count rules kept. Verify anchors against main
`566495b0`: `monday_brief_service.py:761,1168`, `ChairHome.tsx:801`.
Own producer, minimal type/schema seam, ChairHome ordering, and the new
Python/React fences. Parent owns roadmap, evidence and actual walks;
readable_rig owns the rig and atlas files (do not touch).

Write fences first; hold for baseline RED and explicit PROCEED. Mint
through real production writers, never injected finished decision rows.
Test several older decisions with different timestamps, timestamp
preservation, reload order and the rendered first three/count/fold.
Scoped tests: new decision tests, Monday Brief unit tests and relevant
ChairHome tests only. Every pytest in isolated HOME; never metal/full suite.
Report exact collection and run outputs, touched files and limits; hold
for SHIP. The orchestrator performs all shipping operations.

Review round: initial draft SQL fixtures were rejected before baseline
execution. Required real desk writer and meeting-artifact reconciliation;
separate timestamp/order fences; unordered rendered input so recency is
actually exercised. Commitment de-duplication must preserve any independent
Review decision row. No design criterion changed.

## readable_rig

Same story/design/worktree. Own `scripts/graph_walk.py`, actual as-written
`atlas-phase3.json` closure step 5, shared atlas/graph predicate enums and
focused readable-row tests. Parent owns generated graph and runs real cases
serially; decision_rows owns product code (do not touch).

Reuse real snapshot `elementFromPoint` hit tests: expected text, visible
positive geometry, in viewport and all hit points owned by the row. A real
browser probe must reject a covering dock despite in-viewport geometry.
Keep the closure interaction as written: no Ack, Defer, fold or additional
Generate after the producer-day advance. Retain restart proof, identity,
date and isolated DB reads for old-triage preservation before rig cleanup.

Write fences first and hold for RED. Baseline execution recorded
`3 failed, 5 passed in 5.13s`; PROCEED then sent. Scoped tests: new rig fence,
atlas/schema and existing graph calibration tests only, isolated HOME.
Never execute a hub concurrently with the parent's walk. Hold for SHIP.

## Shared scars

Change wording and its fences together. A lying producer double proves
nothing. Receipts must survive the rendered branch transition. Never use
stash/reset/checkout/restore/clean/switch or blanket restore loops; never
touch untracked evidence. The actual atlas cases, not sample calibration,
close the face claim. The Tuesday job is one Generate, yesterday untouched,
new decision readable. No full suite in this lane (PR CI owns it).

## Observed scroll seam

The actual 393 atlas run exposed bottom padding under the capture bar.
Astra asked decision_rows for a read-only diagnosis. Muad'Dib checked
`seam-plan.md` before product work; his conditional ratification is in the
phase's `checks/rows-seam-muaddib.md`. The worker then owned only
ChairHome/chair.css: measured capture height, native instant nearest
scroll once after a successful Generate, unchanged cap and canvas.
Root owns the actual atlas runs, the breakage variant, source anchors and
all shipping. The focused follow-up browser fence starts the old row near
the bar before its longer replacement; no post-click test scroll is allowed.

Root session: `01a0d21a-e795-7861-9995-cf31f0eed09b`.
Worker sessions: decision_rows `01a0d21c-29ab-77b0-b9e6-f9b57e59c52f`;
readable_rig `01a0d21d-110f-72a3-b4aa-459edb100ec1`.
