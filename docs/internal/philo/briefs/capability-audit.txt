# HoldSpeak: Capability Audit, System Integration Map, and Documentation-Agent Specification

## Executive reconstruction

The most important conclusion is that **HoldSpeak is no longer merely a dictation application**. The repository currently describes a substantially broader local-first system whose unifying purpose is to turn human speech, meetings, project context, and agent activity into **reviewable work objects, durable knowledge, and explicitly approved actions**. Its original “hold a key, speak, release, type” loop still exists, but it now sits beside meeting intelligence, project memory, model routing, persistent agent conversations, coding-agent supervision, companion-device access, approval-gated external actions, a spatial “Desk” UI, and a runtime/process journal. citeturn1search0turn1search1

That explains why the product has become difficult even for its creator to hold in one mental model: **the codebase appears to have crossed the boundary from “application with features” into “platform with several interacting subsystems.”** The public documentation itself now names Meetings, Notes, Artifacts, Projects, Agents, Workflows, Integrations, Coder sessions, attention, Receipts, Dictation, Studio, Settings, companions, model destinations, plugins, connectors, and approval mechanisms. citeturn1search0turn1search1

There is even evidence of this identity transition in the package metadata. `pyproject.toml` still describes HoldSpeak primarily as “Voice typing for macOS and Linux,” while the README describes a system encompassing dictation, meetings, knowledge, agents, remote execution, approval-gated actions, coding-agent workflows, and companions. The package is currently version `0.4.0`, requires Python 3.10+, and is classified as Beta, while the README characterizes the project as “0.x, early but real,” with mature features but still-changing APIs, configuration, and defaults. citeturn1search0turn1search2

I would therefore describe the current product this way:

> **HoldSpeak is a local-first voice-work hub that converts spoken activity into typed output, structured meeting knowledge, grounded conversations, agent work, and human-approved external actions, while preserving provenance and making execution boundaries visible.**

That framing reduces the system to four conceptual loops:

| Loop | Human intent | HoldSpeak does | Durable result |
|---|---|---|---|
| **Speak → Type** | “Turn what I am saying into useful text.” | Transcribe, clean, route, contextualize, optionally rewrite, preview, inject. | Typed text + dictation journal + learning signal. |
| **Meet → Understand** | “Turn this conversation into what matters next.” | Capture/import, transcribe, identify intent, run intelligence plugins, extract decisions/actions/risks/etc. | Meeting + transcript + typed artifacts + aftercare state. |
| **Context → Think** | “Reason about the material I have accumulated.” | Ground a model on selected meetings, artifacts, notes, projects, or other records. | Cited answer/conversation + reusable Artifact. |
| **Propose → Approve → Act** | “Let software help me do something without surrendering control.” | Generate a proposal, expose boundary and exact payload, require approval, execute, record a receipt. | Auditable external effect or denied action. |

Those four loops are much easier to communicate than the complete feature list, while still encompassing most of what the present product does. The README strongly supports this interpretation: it repeatedly emphasizes local operation, typed artifacts rather than recordings alone, retained decisions, explicit approval before side effects, provenance, and visible execution destinations. citeturn1search0

A second important conclusion is that **the Desk has become the product's organizing surface**. The canonical frontend architecture describes HoldSpeak Web as a single typed Vite/React 19 application whose principal surface is the Desk; Pixi v8 renders the world layer, Zustand supplies Desk state, DOM windows are composited above it, and FastAPI serves the SPA. Only `/`, `/welcome`, and `/presence` remain true routes; older product paths effectively deep-link into windows on the Desk. citeturn1search8

That means the current product architecture is best understood as:

**HoldSpeak Hub + execution kernel + durable store + work engines + policy/approval layer + Desk/companions**

rather than as:

**dictation app + many unrelated features**.

---

## Capability inventory

The following is the most concrete inventory I can derive from the repository's current canonical/public documentation. I distinguish between **shipped/documented**, **optional**, **built but unreleased**, **platform-constrained**, and **deliberately prohibited** behavior rather than treating everything mentioned in the repository as equally mature.

### Core runtime and product shell

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Local HoldSpeak hub** | A single runtime serves both dictation and meeting-oriented functionality rather than maintaining two unrelated applications. | Core/shipped. citeturn1search0 |
| **FastAPI application host** | The browser SPA is served through FastAPI, which also forms part of HoldSpeak's HTTP application boundary. | Core/shipped. citeturn1search8 |
| **Desk** | Principal spatial workspace containing meetings, notes, knowledge, agents, artifacts and other work objects. | Canonical UI. citeturn1search0turn1search8 |
| **Desk objects** | Meetings, Notes, Knowledge, Agents, Artifacts, Projects, Workflows, Integrations, Coder sessions, attention and Receipts are represented through the Desk system. | Documented product surface. citeturn1search0turn1search1 |
| **Spatial interactions** | Select/open objects; contextual Info; drag objects onto agents/knowledge objects; lasso sets of objects; file objects into Zones. | Shipped/documented. citeturn1search0 |
| **Specialized workrooms/windows** | Dictation, Meetings, Studio and Settings remain focused operational surfaces even though Desk is the front door. | Documented. citeturn1search1 |
| **WebGL world layer** | Pixi v8 scene graph renders the Desk world. | Canonical frontend architecture. citeturn1search8 |
| **DOM window layer** | Conventional application windows are composited above the world scene. | Canonical frontend architecture. citeturn1search8 |
| **Zustand Desk state** | Frontend Desk state is maintained in a Zustand store. | Canonical frontend architecture. citeturn1search8 |
| **SQLite persistence** | The complete HoldSpeak database is described as a single SQLite file. | Core/shipped. citeturn1search0 |
| **Configuration store** | Main configuration is stored under `~/.config/holdspeak/config.json`, with user-facing settings exposed through the Desk. | Core/shipped. citeturn1search0 |

### Dictation and voice interaction

The dictation pipeline is considerably more than speech-to-text. Its documented flow is approximately `speech → Whisper transcript → punctuation cleanup → dictation pipeline → typed text`, with project context, Coder-session context, routing and optional LLM rewriting inserted into the intelligent portion of that pipeline. citeturn1search6

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Hold-to-talk dictation** | Hold configured hotkey, speak, release, transcribe and insert into active application. | Foundational/shipped. citeturn1search0 |
| **Local Whisper transcription** | Speech recognition can run locally instead of requiring cloud transcription. | Foundational/shipped. citeturn1search0turn1search2 |
| **Cross-application text injection** | Final text is inserted into the currently active application. | Shipped; Wayland constrained. citeturn1search0 |
| **Punctuation cleanup** | Raw transcription can be cleaned before higher-level dictation processing. | Pipeline stage. citeturn1search6 |
| **Intent routing** | Speech can be routed according to what the utterance appears intended to accomplish. | Shipped/documented. citeturn1search0turn1search6 |
| **Application/target-aware rewriting** | Output can be rewritten differently for Codex, Claude, a terminal, browser, editor, or another target. | Shipped/documented. citeturn1search0 |
| **Project-grounded dictation** | Relevant project context can be incorporated before rewriting the utterance. | Shipped/documented. citeturn1search0turn1search6 |
| **Coder-session context** | Active coding-agent/session information can participate in the dictation pipeline. | Documented. citeturn1search6 |
| **Optional LLM rewrite** | User-controlled model can transform rough speech into target-appropriate text. | Optional. citeturn1search0turn1search6 |
| **Preview-before-type** | Final transformed text can be shown before insertion; user explicitly chooses Type or Discard. | Optional. citeturn1search0turn1search6 |
| **Wake-phrase mode** | Hands-free listening can be triggered by a wake phrase; the result is previewed rather than silently typed. | Optional/documented. citeturn1search0 |
| **Language pinning** | Spoken-language setting can explicitly select any of Whisper's supported language choices described by HoldSpeak. | Shipped/documented. citeturn1search0 |
| **Spoken-symbol dictionary** | Custom spoken phrases can be mapped to literal symbols, such as “double colon” → `::`. | Shipped/documented. citeturn1search0 |
| **Voice commands** | Spoken keywords can trigger configured actions such as opening a URL, starting an application or running a command. | Shipped/documented. citeturn1search0 |
| **Dictation journal** | Each run records utterance/output/routing/timing information. | Core learning/provenance mechanism. citeturn1search0 |
| **Correction memory** | User can correct a bad result and retain that correction as learning context. | Shipped/documented. citeturn1search0 |
| **Replay** | An older utterance can be re-run through the changed pipeline so routing/output changes can be observed. | Shipped/documented. citeturn1search0 |
| **Learning digest** | Reports the actual number of similar corrections affecting behavior, including an honest zero. | Shipped/documented. citeturn1search0 |
| **Activity pre-briefing** | Recently touched records can be suggested as cited context for a forthcoming dictation. | Documented. citeturn1search0 |
| **Remote companion dictation** | An iPad can speak into the hub's full dictation path and cause approved text/actions on the desktop side. | Built/tested companion; iPad app not released. citeturn1search0 |

The important architectural distinction is that **transcription and dictation transformation are separate responsibilities**. Whisper tells HoldSpeak approximately what was spoken; the dictation pipeline decides how that speech should become useful work. That separation is one of HoldSpeak's strongest product concepts. citeturn1search6

### Meeting capture, intelligence, and aftercare

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Live microphone capture** | Record meeting speech live. | Shipped/documented. citeturn1search0 |
| **System-audio capture** | Capture system audio in addition to microphone audio. | Shipped with platform-specific backends. citeturn1search0 |
| **Speaker labeling** | Live meeting results can include speaker distinctions. | Shipped/documented. citeturn1search0 |
| **Audio import** | Existing recordings can be imported into meeting processing. | Shipped/documented. citeturn1search0 |
| **WAV ingestion** | WAV import is available directly. | Shipped/documented. citeturn1search0 |
| **Compressed-audio ingestion** | Other audio formats can be processed through `ffmpeg`. | Optional dependency/tooling path. citeturn1search0 |
| **VTT import** | Existing caption transcript can become a HoldSpeak meeting while preserving source information such as timestamps/speaker data where available. | Shipped/documented. citeturn1search0 |
| **SRT import** | Same general transcript-import flow for SRT. | Shipped/documented. citeturn1search0 |
| **Plain-text transcript import** | Existing textual transcript can enter the meeting system. | Shipped/documented. citeturn1search0 |
| **Meeting intent scoring** | Transcript is classified/scored across areas including architecture, delivery, product, incident and communications. | Documented. citeturn1search0 |
| **Plugin intelligence pipeline** | A sequence of meeting-intelligence plugins runs against transcript material. | Core meeting-intelligence mechanism. citeturn1search0 |
| **Built-in intelligence plugins** | Repository documentation says HoldSpeak ships 14 built-in LLM-backed plugins. | Shipped/documented. citeturn1search0 |
| **Decision extraction** | Convert meeting evidence into typed decision records. | Shipped/documented. citeturn1search0 |
| **Action-item extraction** | Extract actionable commitments and ownership. | Shipped/documented. citeturn1search0 |
| **ADR generation** | Produce architecture decision record-like artifacts. | Shipped/documented. citeturn1search0 |
| **Risk register generation** | Derive structured risks from meeting context. | Shipped/documented. citeturn1search0 |
| **Incident timeline generation** | Create typed timeline artifacts for incident-oriented meetings. | Shipped/documented. citeturn1search0 |
| **Typed artifacts** | Intelligence results are represented as structured artifact types rather than opaque prose only. | Core product concept. citeturn1search0 |
| **Evidence/source linkage** | Artifacts can be associated with the transcript moments supporting them. | Shipped/documented. citeturn1search0 |
| **Decision lifecycle** | Decisions can be accepted or superseded and durable decisions can be promoted to other records. | Documented. citeturn1search0 |
| **Meeting aftercare** | Post-meeting view summarizes what remains open, what was decided and what changed since an earlier meeting. | Shipped/documented. citeturn1search0 |
| **Open actions by owner** | Follow-up state can be organized around responsible people. | Documented. citeturn1search0 |
| **Follow-up drafting** | Generate a follow-up draft based on meeting outcomes. | Shipped/documented. citeturn1search0 |
| **Issue proposal** | Accepted action can become a proposed GitHub issue. | Approval-gated integration. citeturn1search0 |
| **Slack delivery proposal** | Digest or follow-up can be proposed for Slack transmission. | Approval-gated integration. citeturn1search0 |
| **Meeting archive** | Saved meetings are retained and revisitable. | Core. citeturn1search0 |
| **Archive search/filter** | Filter/search by date, speaker, tag and open actions. | Shipped/documented. citeturn1search0 |

This subsystem has a particularly strong product philosophy: **a meeting is not “done” when recording ends; it is done when the resulting knowledge and commitments are resolved into durable records and explicit next actions.** That philosophy is visible throughout the README's distinction between transcript storage and “closing the loop.” citeturn1search0

### Knowledge, grounding, conversations, and artifacts

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Project Memory** | Find material retained from earlier local project work. | Documented. citeturn1search0 |
| **Ask this project** | Ask model-backed questions from local project material with cited sources. | Documented. citeturn1search0 |
| **Match-count disclosure** | Project question-answering reports how much matching material fit the prompt instead of implying exhaustive inclusion. | Documented honesty/provenance feature. citeturn1search0 |
| **Object grounding** | User can explicitly attach meetings and related records/artifacts to an AI request. | Shipped/documented. citeturn1search0 |
| **Meeting expansion during attach** | A meeting can be expanded into its digest, transcript or generated artifacts for grounding. | Documented. citeturn1search0 |
| **Context-window gauge** | UI measures selected grounding material against model context capacity. | Documented. citeturn1search0 |
| **Lasso-grounded Ask AI** | Multiple Desk objects can be selected spatially and treated as one grounding set. | Documented. citeturn1search0 |
| **Persisted answer cards** | An answer can become a retained Desk object rather than being transient chat output. | Documented. citeturn1search0 |
| **Artifact provenance** | Retained answer records which objects it read and the user's instruction. | Documented. citeturn1search0 |
| **Persistent Agent conversations** | Agent chat is multi-turn and survives page reload rather than being a one-shot prompt. | Shipped/documented. citeturn1search0 |
| **Agent grounding** | Grounding attachments work in persistent Agent conversations. | Shipped/documented. citeturn1search0 |
| **Save Agent reply as Artifact** | Individual model replies can be promoted into durable Desk artifacts. | Shipped/documented. citeturn1search0 |
| **Model-pinned chat** | A user can open a conversation specifically tied to an available model. | Shipped/documented. citeturn1search0 |
| **Interview composition** | The architecture describes Interview as composing existing Thread conversation, model routing and MCP services rather than introducing another model runtime. | Architecture-level capability. citeturn1search3 |
| **Thread tool loop** | The Thread service is responsible for model conversation and its tool loop; the Thread UI/store carries messages, streamed replies, references and tool activity. | Architecture-level capability. citeturn1search3 |

That last point is architecturally important: the repository explicitly says Interview **does not become another universal automation engine**. Instead, it composes established conversation, routing, and MCP services. That is the right kind of reuse boundary for a product that otherwise risks accumulating parallel orchestration systems. citeturn1search3

### Models, execution destinations, and distributed inference

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Model inventory** | Models can be added/downloaded/connected and then become available to the hub. | Documented. citeturn1search4 |
| **Job assignments** | Available models are separately assigned to concrete HoldSpeak jobs. | Documented. citeturn1search4 |
| **Capability validation** | Server checks that a model supports the capability required by a proposed assignment. | Documented invariant. citeturn1search4 |
| **Readiness validation** | Unavailable or inadequately configured model cannot silently become a working assignment. | Documented invariant. citeturn1search4 |
| **Boundary validation** | Saved execution boundary participates in assignment validation. | Documented invariant. citeturn1search4 |
| **Named repair state** | Missing key/unsupported/unavailable state remains visible with a specific repair rather than being hidden. | Documented. citeturn1search4 |
| **No silent model substitution** | HoldSpeak explicitly refuses to silently choose a different model when the selected one cannot satisfy an assignment. | Documented invariant. citeturn1search4 |
| **Immutable execution plan** | Model assignment is frozen when a run starts; changing settings affects later work rather than mutating an active run. | Documented invariant. citeturn1search4 |
| **Attempt/fallback receipts** | Receipt preserves selected primary, attempts, fallback reason, actual model/boundary and terminal result. | Documented. citeturn1search4 |
| **On-device destination** | Work can execute on the current machine. | Core. citeturn1search0 |
| **Paired-device destination** | Work can execute on another paired HoldSpeak node. | Documented. citeturn1search0 |
| **Private endpoint destination** | Model/inference work can target a private endpoint. | Documented. citeturn1search0 |
| **External OpenAI-compatible endpoint** | User can deliberately point intelligence at an external compatible service. | Optional egress path. citeturn1search0 |
| **Reusable Runs on destinations** | Execution destinations are modeled as reusable named configuration entities. | Documented. citeturn1search0 |
| **Agent-specific destination** | A destination can be assigned to an Agent. | Documented. citeturn1search0 |
| **Mesh execution** | `holdspeak mesh serve` can expose another user's machine as an execution node. | Documented. citeturn1search0 |
| **Per-device API keys** | Destination definition can synchronize while API secrets remain local to the machine holding them. | Documented trust-boundary behavior. citeturn1search0turn1search4 |
| **Secret subresource** | Keys are written through an owner-only secret path and ordinary model/settings reads expose presence rather than secret value. | Documented security behavior. citeturn1search4 |
| **Secret exclusion from receipts/database/sync** | The model-routing documentation explicitly says keys do not appear in ordinary responses, receipts or synchronization material. | Documented invariant. citeturn1search4 |

This is sophisticated enough that I would treat **model execution routing as a first-class subsystem**, not merely “LLM settings.”

### Human authority, security, and auditability

HoldSpeak's security documentation explicitly presents itself as a living threat model defining stored data, trust boundaries, what can leave the machine and the intended at-rest posture. citeturn1search5

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Visible execution boundary** | Operational work identifies whether execution occurs on this device, a paired device, private endpoint or external service. | Core security UX. citeturn1search0turn1search5 |
| **Propose → approve → execute** | External side effects are represented as proposals rather than being silently executed. | Core authority model. citeturn1search0 |
| **Actuators off by default** | Side-effect-capable integrations begin disabled. | Security invariant/documented. citeturn1search0 |
| **Exact-action approval** | Approval applies to the concrete proposed action/payload rather than granting an indefinite capability. | Documented. citeturn1search0 |
| **Audit receipts** | Actions and important execution facts leave provenance-bearing receipts. | Core concept. citeturn1search0 |
| **Agent Gate** | A risky Claude Code Bash call can be held pending user approval in the Desk. | Optional/documented. citeturn1search0 |
| **Two-step Gate arming** | Gate is off by default and deliberately armed. | Security-oriented UX. citeturn1search0 |
| **Redacted action preview** | Held risky operation is shown through a redacted preview before authorization. | Documented. citeturn1search0 |
| **Allow / deny with reason** | User can approve or reject the held tool invocation; denial reason goes back to the agent. | Documented. citeturn1search0 |
| **Fail-closed Gate** | Once armed, inability to reach the hub causes denial rather than bypassing approval. | Explicit documented invariant. citeturn1search0 |
| **Control modes** | Documentation names Secure, Normal and YOLO modes while stating that review/authority/execution remain conceptually separate and certain invariants cannot be relaxed. | Documented control surface; exact semantics should remain a dedicated specification. citeturn1search1 |
| **No account / no telemetry design** | Main README states the ordinary local setup does not require a hosted HoldSpeak account or telemetry service. | Product posture. citeturn1search0 |

This approval architecture may eventually become **the most strategically valuable part of HoldSpeak** because it provides a reusable answer to a rapidly growing problem: how a human supervises increasingly autonomous local coding and work agents without approving every harmless operation or surrendering control over consequential ones.

### Coder, GitHub, Slack, companions, and ambient presence

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **Claude Code session presence** | Running Claude Code sessions can appear as Desk objects through hooks. | Documented. citeturn1search0 |
| **Codex session presence** | Codex sessions can likewise appear in the Desk workflow. | Documented. citeturn1search0 |
| **Session status/question visibility** | Companion/Desk can expose when a session is active or waiting for input. | Documented. citeturn1search0 |
| **Human response to Coder session** | User can answer a waiting coding agent through typed or spoken input. | Documented. citeturn1search0 |
| **Grounded response to Coder session** | Meeting/Note context can be attached to a response. | Documented. citeturn1search0 |
| **AI-drafted response** | HoldSpeak can prepare a reply which remains editable before explicit transmission to the coding agent. | Documented. citeturn1search0 |
| **PR observation** | Registered repositories' pull requests appear with state, CI conclusion, observation time and match provenance. | Documented. citeturn1search0 |
| **Coder worktree launch** | Coder session can be directed into the worktree corresponding to matched PR work. | Documented. citeturn1search0 |
| **Model-generated PR review Artifact** | HoldSpeak can retain an AI review as an Artifact. | Documented. citeturn1search0 |
| **Proposed GitHub comment** | Complete proposed comment can await human approval before posting. | Documented. citeturn1search0 |
| **No PR merge/close/force-push** | README explicitly places these operations outside HoldSpeak's available PR control surface. | Deliberate prohibition. citeturn1search0 |
| **Slack send** | Meeting digest/follow-up can be transmitted after explicit approval. | Approval-gated. citeturn1search0 |
| **Qlippy** | Optional desktop-presence mascot reflects runtime state and surfaces a small subset of approval/learning/follow-up moments. | Optional, off by default. citeturn1search0 |
| **Non-focus-stealing presence panel** | Native panel is designed to accept pointer input when visible without stealing keyboard focus. | Documented. citeturn1search0 |
| **Boundary badge in Qlippy** | Presence cards identify egress/execution posture. | Documented. citeturn1search0 |
| **iPad Desk client** | First-class companion uses the hub's existing APIs for both dictation and meeting workflows. | Built and tested but explicitly unreleased. citeturn1search0 |
| **iPad meeting review** | Companion can consume meeting artifacts, confidence/source information and aftercare. | Built/documented, unreleased client. citeturn1search0 |
| **iPad proposal approval** | Pending proposals can be reviewed on the companion while preserving the separate approval step. | Built/documented, unreleased client. citeturn1search0 |
| **iPad import** | Audio/transcript files can be pushed into hub intelligence processing. | Built/documented, unreleased client. citeturn1search0 |
| **AIPI-Lite companion** | ESPHome-based portable device provides meeting controls/status and can participate in responding to waiting coding sessions. | Optional hardware companion. citeturn1search0 |
| **LAN/Tailscale companion topology** | Companion traffic can use the user's own network rather than a HoldSpeak-hosted relay. | Documented network model. citeturn1search0 |

### Platform and operational capabilities

| Capability | Functional meaning | Apparent status |
|---|---|---|
| **macOS 14+ Apple Silicon** | Primary macOS environment in current support matrix. | Supported. citeturn1search0 |
| **Linux X11** | Voice typing, global hotkeys and injection supported. | Supported. citeturn1search0 |
| **Linux Wayland** | Core operation exists but global hooks/synthetic typing are constrained. | Best effort for those facilities. citeturn1search0 |
| **Clipboard fallback** | Wayland can fall back to clipboard paste when direct synthetic typing is blocked. | Documented fallback. citeturn1search0 |
| **BlackHole system-audio path** | macOS meeting audio capture can use BlackHole. | Platform integration. citeturn1search0 |
| **Pulse/PipeWire capture** | Linux meeting system audio uses normal Linux audio facilities. | Platform integration. citeturn1search0 |
| **PyPI packaging** | Installable with `pip install holdspeak`. | Shipped. citeturn1search0 |
| **Feature extras** | Package extras split meeting and various dictation-model backends. | Shipped packaging model. citeturn1search0 |
| **`holdspeak doctor`** | Diagnoses permissions/backends and reports configuration/schema conditions rather than merely checking installation. | Operational tool. citeturn1search0 |
| **Backup** | Explicit database snapshot command. | Shipped. citeturn1search0 |
| **Restore** | Restore an earlier HoldSpeak database snapshot. | Shipped. citeturn1search0 |
| **Pre-migration backup** | Older database is backed up before an upgrade modifies it. | Documented safety invariant. citeturn1search0 |
| **Forward-schema refusal** | Older HoldSpeak refuses to open a database produced by a newer build instead of risking destructive interpretation. | Documented safety invariant. citeturn1search0 |
| **Plugin authoring** | Meeting intelligence/actuation architecture has an extension path for user-written plugins. | Developer feature. citeturn1search0turn1search1 |
| **Connector development** | Repository exposes a separate extension route for integrations/connectors. | Developer feature. citeturn1search0 |
| **Documentation render testing** | Architecture documentation says an E2E test verifies Mermaid diagrams continue to render. | Documentation engineering capability. citeturn1search3 |

The overwhelming takeaway from this inventory is that HoldSpeak currently contains **at least nine meaningful systems**:

**voice runtime, meeting engine, knowledge system, conversation/agent runtime, model-execution router, human-authority layer, integration/actuator framework, multi-device runtime, and Desk UI shell.**

That is why it feels substantially larger than its origin.

---

## Functional architecture and the kernel

The term **kernel** deserves special treatment because it can easily become architecturally vague.

The main README explicitly describes a read-only **Process** window that displays what the **kernel journal** says is “running, waiting, unknown, or finished,” while emphasizing that Process observes rather than controls work. citeturn1search0

From the public material I could verify, that is the strongest direct evidence for the kernel's role. I therefore would **not** describe it as an operating-system-like kernel, nor assume undocumented class boundaries. The defensible interpretation is:

> **The HoldSpeak kernel is the runtime coordination/accounting layer that represents ongoing work and records its lifecycle into a journal which other UI surfaces can observe.**

That appears consistent with the product's broader architecture of immutable execution plans, durable receipts, waiting approvals, model attempts, Coder states, and observable process status, although the exact mapping between those subsystems and the kernel implementation should be verified against source code before being made canonical. citeturn1search0turn1search4

I would formalize the architecture into six layers:

| Layer | Responsibility |
|---|---|
| **Experience layer** | Desk, specialized windows, Qlippy, browser presence, iPad, AIPI-Lite. |
| **Ingress layer** | Hotkey/audio, browser/API calls, imports, companion calls, Coder hooks, connector events. |
| **Work engines** | Dictation pipeline, meeting intelligence, Thread/Interview, search/knowledge, plugin processing. |
| **Execution kernel** | Work identity, state transitions, waiting/running/completed/unknown state, journal, correlation/provenance. |
| **Authority and routing layer** | Model assignment, destination choice, trust boundary, approval gates, actuators, control mode. |
| **Persistence/integration layer** | SQLite, config/secrets, project memory, receipts, external services, remote nodes. |

The kernel should ideally own **work lifecycle**, not business-specific semantics. For example:

```text
Created
   |
   v
Planned
   |
   +---------------------+
   |                     |
   v                     v
Ready                 Waiting
   |                 for user /
   |                 resource
   |                     |
   +----------<----------+
   |
   v
Running
   |
   +----------+-------------+
   |          |             |
   v          v             v
Succeeded   Failed       Unknown
   |
   v
Recorded / Receipt finalized
```

The meeting engine should know what an `ActionItemArtifact` means. The model router should know what a model assignment means. The actuator layer should know what a Slack proposal means. **The kernel should only need to know that a unit of work has an identity, plan, state, dependencies, boundary, provenance and terminal outcome.**

That separation would give HoldSpeak a very powerful common runtime:

```text
Dictation run ───────┐
Meeting import ──────┤
Plugin execution ────┤
Model generation ────┤
Agent turn ──────────┤
Coder request ───────┼──> Kernel work model / journal
Gate approval ───────┤
GitHub proposal ─────┤
Slack proposal ──────┤
Mesh inference ──────┘
```

The current documentation is already moving in this direction through receipts, explicit destinations and frozen model execution plans. For model-backed work specifically, HoldSpeak freezes the assignment at run start and records the primary choice, attempts, fallback reasons, actual model, actual boundary and terminal result; that is exactly the sort of information a generalized kernel work record should correlate with. citeturn1search4

I would make the following kernel invariants explicit in the future architecture specification:

| Proposed invariant | Why it matters |
|---|---|
| Every meaningful asynchronous operation receives a stable `work_id`. | Enables end-to-end debugging and provenance. |
| Parent/child relationships are explicit. | A meeting → plugin → model call → proposed action can be traced as one tree. |
| State transitions are append-only events. | Journal remains trustworthy. |
| Current state is derived from or reconcilable with the journal. | Prevents Process from disagreeing with reality. |
| Boundary is recorded before execution. | Security decision is reconstructable. |
| Approval and execution are different events. | Avoids accidental conflation of authority and work. |
| Request payload hash / execution payload hash can be compared. | Proves “what you approved is what ran.” |
| Terminal receipts are immutable. | Preserves auditability. |
| Unknown is a valid state. | Better than lying about distributed or interrupted work. |
| Recovery after restart is specified per work type. | Critical once the hub coordinates remote/model/agent processes. |

This is not a claim that all those invariants already exist. It is the architectural contract I would recommend extracting from HoldSpeak's existing philosophy of honest state, receipts, immutable plans and explicit approvals. Those principles are already strongly represented in the current documentation. citeturn1search0turn1search4

---

## System integration map

At the highest level, the system appears to be structured like this. The central-hub, Desk, model-destination, companion and approval relationships are directly reflected in current repository documentation; the explicit placement of the **kernel** below is a normalized architectural interpretation of the documented kernel journal and process model and should be verified against implementation before being declared canonical. citeturn1search0turn1search3turn1search4turn1search8

```mermaid
flowchart TB

    subgraph Humans["Human interaction"]
        HOTKEY["Global hotkey / microphone"]
        BROWSER["Browser / Desk"]
        IPAD["iPad companion"]
        AIPI["AIPI-Lite"]
        IMPORT["Audio / VTT / SRT / TXT import"]
    end

    subgraph Coding["Coding-agent world"]
        CLAUDE["Claude Code"]
        CODEX["Codex"]
        REPOS["Registered repositories / PRs"]
    end

    subgraph Hub["HoldSpeak Hub"]
        API["FastAPI / local HTTP API"]

        subgraph UI["Experience layer"]
            DESK["Desk — React / Pixi / Zustand"]
            WINDOWS["Meetings / Dictation / Studio / Settings"]
            PRESENCE["Qlippy / Presence"]
            PROCESS["Process — read-only runtime view"]
        end

        subgraph Engines["Work engines"]
            DICT["Dictation pipeline"]
            MEET["Meeting intelligence"]
            THREAD["Thread / Interview"]
            MEMORY["Project Memory / grounded Ask"]
            PLUGINS["Plugin runtime"]
        end

        subgraph Control["Execution and authority"]
            KERNEL["Kernel / work journal"]
            ROUTER["Model assignment + destination router"]
            GATE["Gate / approval system"]
            ACTUATORS["Actuator proposals"]
            RECEIPTS["Receipts / provenance"]
        end

        subgraph State["Durable state"]
            DB[("SQLite")]
            CONFIG["Configuration"]
            SECRETS["Local secrets"]
            ARTIFACTS["Meetings / Notes / Artifacts / decisions / actions"]
        end
    end

    subgraph Inference["Inference destinations"]
        LOCAL["This device"]
        PAIRED["Paired HoldSpeak node"]
        PRIVATE["Private endpoint"]
        EXTERNAL["External OpenAI-compatible service"]
    end

    subgraph External["Approved external effects"]
        GITHUB["GitHub"]
        SLACK["Slack"]
        OS["OS / URL / app / command"]
    end

    HOTKEY --> API
    BROWSER --> API
    IPAD --> API
    AIPI --> API
    IMPORT --> API

    CLAUDE --> API
    CODEX --> API
    REPOS --> API

    API --> DICT
    API --> MEET
    API --> THREAD
    API --> MEMORY

    MEET --> PLUGINS

    DICT --> KERNEL
    MEET --> KERNEL
    THREAD --> KERNEL
    MEMORY --> KERNEL
    PLUGINS --> KERNEL

    KERNEL --> ROUTER
    KERNEL --> GATE
    KERNEL --> RECEIPTS
    PROCESS --> KERNEL

    ROUTER --> LOCAL
    ROUTER --> PAIRED
    ROUTER --> PRIVATE
    ROUTER --> EXTERNAL

    GATE --> ACTUATORS
    ACTUATORS --> GITHUB
    ACTUATORS --> SLACK
    DICT --> OS

    DICT --> DB
    MEET --> DB
    THREAD --> DB
    MEMORY --> DB
    RECEIPTS --> DB
    DB --> ARTIFACTS

    CONFIG --> ROUTER
    SECRETS --> ROUTER

    API --> DESK
    API --> WINDOWS
    API --> PRESENCE
```

There are really **two orthogonal axes** in this architecture.

The first is the **work axis**:

```text
Input
  ↓
Normalize
  ↓
Understand / transform
  ↓
Produce durable object
  ↓
Optionally propose effect
  ↓
Human authority
  ↓
Execute
  ↓
Receipt
```

The second is the **boundary axis**:

```text
                 WHERE MAY THIS WORK RUN?

     This device
         │
     Paired device
         │
    Private endpoint
         │
  External service

Every operation should be able to answer:
- what data was sent?
- why?
- where?
- under which assignment?
- under whose authority?
- what actually ran?
- what came back?
```

That boundary model is already visible in HoldSpeak's destination documentation and security posture. Models are not supposed to silently shift destinations; keys are treated separately from ordinary configuration; receipts record actual execution information; user-facing surfaces expose where work ran. citeturn1search0turn1search4turn1search5

The complete dictation path can consequently be represented as:

```mermaid
sequenceDiagram
    actor U as User
    participant H as Hotkey/Mic
    participant STT as Whisper
    participant DP as Dictation Pipeline
    participant CTX as Context / Corrections
    participant MR as Model Router
    participant PV as Preview Gate
    participant APP as Active App
    participant J as Journal

    U->>H: Hold and speak
    U->>H: Release
    H->>STT: Audio
    STT->>DP: Transcript
    DP->>DP: Cleanup + intent routing
    DP->>CTX: Fetch project/session/learning context
    CTX-->>DP: Relevant context
    opt LLM rewrite enabled
        DP->>MR: Grounded rewrite request
        MR-->>DP: Rewritten text
    end
    opt Preview enabled
        DP->>PV: Final candidate
        PV-->>U: Type it / Discard
    end
    DP->>APP: Inject approved text
    DP->>J: Record route/output/timing
```

That sequence follows the current documented pipeline, including transcription, cleanup, intelligent routing, project/Coder context, optional rewriting, preview and journaling. citeturn1search0turn1search6

Meeting processing is structurally different:

```mermaid
sequenceDiagram
    actor U as User
    participant C as Capture / Import
    participant T as Transcript
    participant I as Intent Analysis
    participant P as Intelligence Plugins
    participant M as Model Router
    participant A as Artifact Store
    participant AC as Aftercare
    participant G as Approval Gate
    participant X as External Actuator

    U->>C: Record meeting / import file
    C->>T: Audio or supplied transcript
    T->>I: Normalized transcript
    I->>P: Intent + meeting material
    loop Applicable plugins
        P->>M: Typed extraction request
        M-->>P: Structured result
        P->>A: Artifact + provenance
    end
    A->>AC: Decisions/actions/risks/etc.
    AC-->>U: Open / decided / changed
    opt Side effect requested
        AC->>G: Proposed action
        G-->>U: Exact action preview
        U->>G: Approve / deny
        G->>X: Execute only if approved
    end
```

That is the essential meeting value proposition: **capture is only ingress; structured aftercare is the output.** citeturn1search0

---

## Primary workflows and use cases

The feature inventory becomes much more comprehensible when expressed as a finite set of user jobs.

**Developer dictation.** A developer holds the configured key and says something loosely structured such as “tell Claude to update the caching service so stale entries are evicted before the database lookup.” HoldSpeak transcribes it, identifies the target/context, incorporates project or Coder-session information, optionally rewrites it into something better suited to the destination and types the final text. The run enters the journal so the user can later see what was heard, routed and typed. citeturn1search0turn1search6

**Learn from a bad dictation.** The resulting text is wrong. Instead of simply deleting it, the user corrects the result. That correction participates in the correction-memory mechanism; the learning digest reports actual similar reach, and an old utterance can subsequently be replayed through the updated pipeline to demonstrate whether routing or transformation improved. citeturn1search0

**Vocabulary-heavy technical dictation.** A developer selects the spoken language and establishes spoken-symbol vocabulary so domain-specific utterances produce programming syntax or organization-specific terms more reliably. citeturn1search0

**Command by voice.** A recognized keyword can map to a configured OS-level action: opening something, launching an application, executing a command or otherwise triggering one of the supported voice-command operations. citeturn1search0

**Hands-free but controlled dictation.** Wake-phrase mode can listen without holding the conventional hotkey, while keeping the resulting text behind a confirmation experience rather than blindly committing it to an application. citeturn1search0

**Meeting capture.** The user records microphone and system audio. HoldSpeak turns the meeting into a transcript with speaker information and passes the result into intelligence processing. citeturn1search0

**Meeting import.** An existing audio recording, VTT, SRT or text transcript enters the same retained-meeting workflow rather than remaining a second-class file. Structured timestamp and speaker information from suitable transcript formats is retained where available. citeturn1search0

**Architecture meeting → durable decisions.** Meeting intelligence identifies architecture-oriented material, plugins extract decisions, ADR-oriented content, actions and risks, and the user can inspect supporting transcript moments. Decisions can be accepted, superseded and ultimately promoted into enduring project documentation. citeturn1search0

**Recurring project meeting.** Aftercare can answer a more useful question than “what was said?”: what remains open, who owns it, what was decided, and what changed relative to previous work. citeturn1search0

**Meeting action → GitHub.** An accepted action becomes a proposed GitHub issue. The external effect remains distinct from extraction: HoldSpeak presents an approval-gated action rather than treating “AI thinks this is an action” as permission to create a ticket. citeturn1search0

**Meeting result → Slack.** HoldSpeak can prepare the digest/follow-up and send it through its external-action mechanism only after the user approves the concrete proposal. citeturn1search0

**Research the project's institutional memory.** Project Memory and Ask this project expose retained material to model-backed questioning while maintaining source citation and disclosing how much matching material fit within the prompt. citeturn1search0

**Ad-hoc grounded reasoning.** The user lassos several Desk objects and asks a question specifically about that set. A valuable answer can be retained as a new Artifact whose provenance states the input objects and instruction that produced it. citeturn1search0

**Persistent specialist agent.** Rather than invoking a one-off generation, a user opens an Agent, accumulates turns in a durable conversation, attaches relevant HoldSpeak records, and promotes useful replies into persistent artifacts. citeturn1search0

**Choose exactly where AI runs.** A user can assign work to the local machine, another HoldSpeak node, a private endpoint or an explicitly configured external model service. The assignment is validated before being accepted and frozen into an execution plan when work starts. citeturn1search0turn1search4

**Distributed private compute.** A more powerful machine can run `holdspeak mesh serve`; another HoldSpeak installation uses it as a named execution destination, allowing the local UX and data workflow to be separated from where model inference actually occurs. citeturn1search0

**Monitor coding agents.** Claude Code or Codex hooks expose active sessions and questions into HoldSpeak. A human can respond by typing or voice, potentially grounded on a Note or Meeting rather than mentally reconstructing the context. citeturn1search0

**Supervise a risky agent operation.** With Gate deliberately enabled, an agent's risky Bash call can pause, surface an appropriately redacted preview and require approval. If HoldSpeak cannot obtain that approval because the hub is unavailable, the armed gate fails closed. citeturn1search0

**Follow pull-request work.** HoldSpeak observes a matched PR and its CI state, can enter the matching worktree with a Coder session, retain a model-generated review, and prepare an exact GitHub comment for approval. It deliberately does not expose merge, close or force-push operations as part of this workflow. citeturn1search0

**Use HoldSpeak away from the workstation.** The iPad companion is described as a true client of the hub's APIs, not simply a remote control. It can participate in dictation, meeting review, proposals, imports, learning information, pre-briefing and Coder interaction, although the repository explicitly says the app itself has not yet been released. citeturn1search0

**Ambient awareness without ambient autonomy.** Qlippy presents selected attention-worthy states such as approval requests, meaningful learning events or open meeting follow-up without independently acting. The current documentation stresses that dismissing these notifications does not itself authorize an action. citeturn1search0

This gives HoldSpeak a much cleaner functional decomposition:

```text
                         HOLDSPEAK

      Capture              Understand             Act
        │                      │                   │
        ▼                      ▼                   ▼
   Speech / Audio       Context / Models       Proposals
   Meetings             Plugins                Approval
   Imports              Memory                 Execution
   Agent events         Threads                Receipts
        │                      │                   │
        └─────────────── Durable Work ────────────┘
                               │
                               ▼
                             Desk
```

---

## Improvement opportunities

The biggest opportunities are no longer primarily about adding functionality. **HoldSpeak already has more functionality than its current conceptual model comfortably communicates.** The next improvement phase should therefore optimize for coherence, contracts, discoverability and reduction of accidental complexity.

| Priority | Opportunity | Concrete recommendation |
|---|---|---|
| **Critical** | Product identity has drifted beyond package description. | Rewrite the canonical product definition around “voice work → durable context → human-approved action.” Update package metadata, README synopsis, onboarding and screenshots from the same vocabulary. |
| **Critical** | No human can reliably remember this capability surface. | Create a machine-readable capability registry and generate documentation/coverage views from it. |
| **Critical** | “Kernel” can become an architectural catch-all. | Give the kernel a precise contract: entities, state machine, event/journal schema, APIs, invariants, crash recovery and explicit list of things the kernel does **not** own. |
| **Critical** | Many workflows overlap through slightly different concepts. | Standardize Work, Artifact, Proposal, Approval, Execution, Receipt, Destination, Agent, Model, Project and Source as canonical domain terms. |
| **High** | Advanced functionality overwhelms first-time experience. | Introduce progressive disclosure: initial product has Speak and Meet; Desk intelligence, Agents, Integrations, Mesh, Gate and companion features appear as users activate them. |
| **High** | Feature implementation status is difficult to infer. | Give every capability one of: Stable, Beta, Experimental, Platform-limited, Built-unreleased, Deprecated, Internal. |
| **High** | Model execution has mature semantics but risks living only in prose. | Define execution-plan and receipt schemas formally and generate diagrams/reference docs from them. |
| **High** | Security concepts cross nearly every subsystem. | Produce one authoritative trust-boundary model covering browser, hub, paired node, endpoint, external service, connector, actuator and Coder hooks. |
| **High** | Meetings, dictation and agents all create provenance differently. | Normalize source/provenance representation and let every Artifact expose “why does this exist?” |
| **High** | Integrations can become an uncontrolled collection of special cases. | Require every integration to implement the same Connector / Proposal / Approval / Actuator / Receipt contract wherever possible. |
| **High** | Distributed execution complicates debugging. | Give every run a correlation ID visible in UI, logs, kernel journal, model receipt and integration receipt. |
| **High** | Frontend object vocabulary is expansive. | Establish an explicit Desk object ontology with allowed verbs and lifecycle states per object type. |
| **Medium** | Internal planning documents coexist with canonical docs. | Generate an explicit docs-status index identifying canonical/current, historical, RFC, superseded and generated documents. |
| **Medium** | Operational recovery becomes increasingly important. | Document restart semantics for active transcription, meeting processing, model calls, actuator approval and mesh work. |
| **Medium** | Cross-platform behavior is necessarily uneven. | Generate a platform capability matrix from automated capability probes/E2E tests rather than maintaining it manually. |
| **Medium** | The project already guards Mermaid rendering. | Extend documentation CI to link checking, capability-to-test traceability, API/schema freshness and stale-file detection. |

The package-description mismatch is especially telling. The Python manifest still positions HoldSpeak as voice typing while the README presents an integrated voice/meeting/agent workspace. citeturn1search0turn1search2 Fixing that is not marketing polish—it forces an architectural decision about what the product **is**.

My strongest recommendation is to establish a **Capability Registry** such as:

```yaml
capabilities:
  - id: dictation.push_to_talk
    name: Push-to-talk dictation
    domain: dictation
    status: stable
    surfaces:
      - desktop
      - ipad
    platforms:
      macos: supported
      linux_x11: supported
      linux_wayland: best_effort
    inputs:
      - audio
    outputs:
      - typed_text
      - dictation_run
    boundaries:
      default: this_device
      external_allowed: false
    depends_on:
      - transcription.whisper
      - text_injection
    api:
      - /api/...
    config:
      - dictation....
    implementation:
      - holdspeak/...
    tests:
      - tests/...
    docs:
      - docs/DICTATION_PIPELINE_GUIDE.md

  - id: meeting.github_issue_proposal
    name: Propose GitHub issue from action item
    domain: meetings
    status: beta
    side_effecting: true
    authority:
      proposal_required: true
      explicit_approval_required: true
    boundaries:
      default: external_service
    depends_on:
      - meeting.aftercare
      - integration.github
      - authority.actuators
```

Once that exists, HoldSpeak can automatically answer:

```text
What can HoldSpeak do?
What is experimental?
What works on Wayland?
What can send data externally?
Which features require a model?
Which features can cause side effects?
Which ones require explicit approval?
Which APIs support them?
Which tests prove them?
Which docs explain them?
What changed between releases?
```

That single registry would substantially reduce the confusion you are experiencing today.

I would similarly define the core conceptual ontology as:

```text
Source
  Something HoldSpeak can read or hear.

Work
  A tracked computation or operation.

Artifact
  A durable result of Work.

Destination
  Where computation occurs.

Proposal
  A requested side effect that has not been authorized.

Approval
  A human authority decision over one concrete Proposal.

Execution
  The actual performance of an approved side effect.

Receipt
  Immutable evidence describing what actually occurred.

Agent
  A durable conversational identity with an assigned model/destination.

Project
  A scope for context, memory, repositories and related objects.

Kernel
  The runtime mechanism tracking Work lifecycle and journal state.
```

Every feature should fit this vocabulary. When it cannot, that is a useful architecture signal.

The simplicity goal should therefore **not** be “remove sophisticated capabilities.” It should be:

> **Make sophisticated capabilities collapse into a very small number of reusable concepts.**

That is how HoldSpeak can remain simultaneously simple and powerful.

---

## Ultimate documentation-agent prompt

The following is the deliverable I would give directly to a strong coding/repository agent. It intentionally tells the agent **not to trust the existing documentation—including this report—and to prove every claim from the repository, tests, runtime contracts and history.**

```text
You are the principal technical archivist, systems architect, product analyst,
developer-experience engineer, security-documentation engineer, and AI-agent
tooling designer for the HoldSpeak project.

Repository:
  https://github.com/karolswdev/HoldSpeak

MISSION
=======

Your task is not merely to "improve the README."

Your task is to reconstruct HoldSpeak from first principles and leave behind the
most complete, accurate, maintainable documentation and agent-knowledge system
possible.

HoldSpeak has grown from local voice typing into a significantly larger system.
The repository appears to encompass at least:

- push-to-talk dictation;
- local speech recognition;
- an intelligent dictation pipeline;
- correction/learning/replay;
- meeting capture and import;
- meeting-intelligence plugins;
- structured decisions/actions/ADRs/risks/etc.;
- meeting aftercare;
- a spatial Desk UI;
- Notes, Meetings, Artifacts, Projects, Agents, Workflows, Integrations,
  Coder sessions, attention and Receipts;
- model registration and job assignments;
- local / paired-device / private-endpoint / external model execution;
- persistent model conversations;
- grounding and project memory;
- human approval and actuator semantics;
- a kernel/process journal;
- Claude Code/Codex integrations;
- GitHub and Slack actions;
- Qlippy/presence;
- an iPad companion;
- AIPI-Lite;
- plugins/connectors;
- backup/migration/doctor/recovery behavior.

THIS LIST IS A RESEARCH HYPOTHESIS, NOT SOURCE OF TRUTH.

You MUST verify it against the current repository.

The final result must let a new engineer answer, without archaeology:

1. What exactly is HoldSpeak?
2. What can it do today?
3. What cannot it do?
4. Which capabilities are stable, experimental, partial, platform-limited,
   built-but-unreleased, deprecated, historical, or merely planned?
5. Which user-facing surface exposes each capability?
6. Which backend components implement it?
7. Which API routes participate?
8. Which database entities persist it?
9. Which configuration values affect it?
10. Which models/services/devices does it depend on?
11. Which data may leave the machine?
12. Which actions can cause side effects?
13. Where does human approval occur?
14. What does the kernel actually own?
15. What happens when something crashes, disconnects, times out, or is denied?
16. Which tests prove each claim?
17. Which documents are authoritative?
18. How should another AI coding agent safely modify this subsystem?
19. How can the project keep all of the above accurate automatically?

NON-NEGOTIABLE PRINCIPLES
=========================

ACCURACY OVER NARRATIVE.

Do not document functionality because README text says it exists.

For every meaningful capability, inspect implementation, routes, schemas,
configuration, tests and where useful git history.

Existing docs are evidence, not authority.

When docs and code disagree:
- record the disagreement;
- determine which behavior is actually executable;
- inspect tests and recent changes;
- classify the doc as stale if appropriate;
- do not silently choose whichever version sounds nicer.

Never transform a planned feature into a shipped feature.

Never infer "working" merely because a class or route exists.

Never infer "unused" merely because something is not called from one obvious
entry point.

Never call a capability local/private if an optional branch can transmit data
externally.

Never call an action approval-gated unless the actual execution path verifies
that approval.

Never claim a test proves behavior unless you inspect the test and its
assertions.

Every architectural assertion must be traceable to repository evidence.

RESEARCH SNAPSHOT
=================

At the beginning:

- record repository URL;
- record branch;
- record exact commit SHA;
- record commit timestamp;
- record HoldSpeak package/version information;
- record relevant platform/runtime versions;
- record whether the working tree was clean;
- record documentation-generation date.

Put this in:

  docs/generated/REPOSITORY_SNAPSHOT.md

No generated documentation may pretend to describe a newer commit.

PHASE: REPOSITORY ARCHAEOLOGY
=============================

Inventory the entire repository.

Produce a machine-readable tree classification identifying:

- Python packages;
- frontend packages;
- Swift/iPad sources;
- ESPHome/AIPI sources;
- scripts;
- tests;
- API schemas;
- migrations;
- configuration;
- packaging;
- GitHub Actions;
- docs;
- internal plans;
- PM/roadmap artifacts;
- fixtures;
- generated files;
- static assets;
- native/platform glue;
- plugin code;
- connector code;
- model backends;
- coding-agent hooks.

Inspect every project manifest and dependency file.

For each dependency that materially affects architecture, determine:
- why it exists;
- which subsystem consumes it;
- whether it is required or optional;
- which platform it applies to;
- whether it creates an external/network boundary.

Do not produce a useless package-by-package encyclopedia.
Focus on architectural dependencies.

Inspect git history strategically.

Use history to answer:
- Why was the Desk introduced?
- When did the kernel appear?
- When did model destinations/assignments appear?
- When did meeting intelligence become a plugin system?
- When did Coder/Gate/Receipts appear?
- Which older architecture documents have been superseded?
- Which terminology changed?

Do NOT turn git history into a chronological dump.

Produce:
  docs/generated/REPOSITORY_MAP.md
  docs/generated/ARCHITECTURE_HISTORY.md

PHASE: CAPABILITY RECONSTRUCTION
================================

Create:

  docs/CAPABILITIES.md
  docs/generated/capabilities.yaml

This is one of the most important deliverables.

For EVERY user-visible or developer-visible capability, assign:

- stable ID;
- canonical name;
- concise purpose;
- domain;
- status;
- user-facing surfaces;
- entry points;
- supported platforms;
- input types;
- output types;
- persistent entities created/modified;
- APIs used;
- configuration keys;
- dependencies;
- model requirements;
- network requirements;
- trust boundary;
- whether external egress is possible;
- whether it can cause a side effect;
- approval requirements;
- kernel/work types involved;
- source-code implementation paths;
- test evidence;
- canonical docs;
- known limitations;
- known failure states;
- deprecation/replacement information.

Use a controlled status vocabulary:

  stable
  beta
  experimental
  partial
  platform_limited
  built_unreleased
  planned
  deprecated
  internal
  historical

Use a controlled exposure vocabulary:

  user
  operator
  developer
  integration
  internal

Use a controlled side-effect vocabulary:

  none
  local_reversible
  local_irreversible
  external_reversible
  external_irreversible

Explicitly find NEGATIVE capabilities.

Examples:
- operations intentionally not exposed;
- unsupported platforms;
- actions integrations refuse to perform;
- model fallbacks that are intentionally forbidden;
- approval modes that cannot bypass hard invariants.

"What HoldSpeak refuses to do" is first-class product documentation.

Build a traceability matrix:

  Capability
      -> UI surface
      -> API
      -> service/domain implementation
      -> persistence
      -> integration
      -> test
      -> documentation

No capability should be classified Stable without meaningful evidence.

PHASE: DOMAIN MODEL
===================

Reverse-engineer the actual domain vocabulary.

Investigate at least:

- Meeting
- Transcript
- TranscriptSegment / source moment
- Decision
- ActionItem
- Artifact
- Note
- Project
- Knowledge
- Agent
- Thread
- Interview
- Workflow
- Integration
- Connector
- Plugin
- Actuator
- Proposal
- Approval
- Receipt
- CoderSession
- Model
- Assignment
- Destination / Runs-on destination
- Process / Work
- Kernel journal entry
- Attention
- Zone
- Desk object
- correction-memory entities
- dictation run/journal entities.

Determine which are:
- database entities;
- API representations;
- frontend-only projections;
- derived read models;
- domain services;
- conceptual documentation terms only.

DO NOT conflate these categories.

Produce:

  docs/DOMAIN_MODEL.md
  docs/generated/domain-model.yaml

Include:
- glossary;
- entity relationship diagram;
- ownership;
- lifecycle;
- state transitions;
- references/foreign relationships;
- source-of-truth location.

Identify aliases and competing terminology.
Recommend ONE canonical term where the repository uses several.

PHASE: KERNEL RECONSTRUCTION
============================

This is a mandatory deep dive.

Search all source, schemas, migrations, tests and documentation for:

  kernel
  journal
  process
  work
  run
  task
  job
  state
  receipt
  waiting
  running
  unknown
  completed
  recovery
  resume
  cancellation
  parent
  child
  dependency
  correlation

Determine WHAT THE KERNEL ACTUALLY IS.

Do not accept a marketing interpretation.

Answer:

- Is "kernel" a concrete module/package?
- Is it a service?
- Is it an event journal?
- Is it a scheduler?
- Is it a workflow engine?
- Is it primarily an accounting layer?
- Is it persisted?
- Does it launch work?
- Does it merely observe work?
- Does it recover work?
- Does it model parent/child operations?
- How does an operation acquire identity?
- How does state change?
- Who may change state?
- What makes something Waiting?
- What makes something Unknown?
- What constitutes completion?
- How are crashes represented?
- What happens across HoldSpeak restarts?
- What happens when a remote node disappears?
- What happens when a model call times out?
- What happens while waiting for human approval?
- How does Process consume kernel information?
- How do receipts relate to journal entries?
- Do model plans participate directly?
- Do dictation/meeting/plugin/agent/actuator runs share the same mechanism?

Write:

  docs/KERNEL.md

The document MUST contain:

1. Definition.
2. Responsibilities.
3. Explicit non-responsibilities.
4. Component diagram.
5. Data structures.
6. State machine.
7. Event/journal semantics.
8. Work identity/correlation.
9. Parent-child relationships.
10. Persistence.
11. Restart/recovery.
12. Cancellation.
13. Failure semantics.
14. Unknown-state semantics.
15. Approval interaction.
16. Destination interaction.
17. Receipt relationship.
18. Public/internal APIs.
19. Security implications.
20. Extension guidance.
21. Tests proving each invariant.

If the current kernel is not internally coherent enough to document this way,
say so and create:

  docs/architecture/PROPOSED_KERNEL_CONTRACT.md

Clearly mark proposal versus reality.

PHASE: END-TO-END SYSTEM ARCHITECTURE
=====================================

Rewrite or supplement the architecture documentation so a contributor can see
the entire product, not merely a selected flow.

Create:

  docs/SYSTEM_ARCHITECTURE.md
  docs/SYSTEM_INTEGRATION_MAP.md
  docs/generated/components.yaml
  docs/generated/integrations.yaml

At minimum map:

INGRESS
- desktop microphone
- global hotkey
- system audio
- imported recordings
- imported transcripts
- browser microphone if implemented
- iPad requests
- AIPI-Lite
- Claude/Codex hooks
- GitHub observations
- connector inputs

SURFACES
- Desk
- focused windows/workrooms
- Settings
- presence/Qlippy
- Process
- companions

ENGINES
- transcription
- dictation transformation/routing
- meeting pipeline
- meeting-intelligence plugin runtime
- project memory/search
- Thread
- Interview
- Agent conversations
- model routing
- Coder integration

CONTROL
- kernel
- assignments
- immutable execution plans
- destinations
- Gate
- proposal/approval
- actuators
- control modes
- receipts

STATE
- SQLite
- migrations
- config
- secret store
- local file/material stores
- indices/search stores if applicable
- companion state

EXTERNAL
- local model runtime(s)
- paired HoldSpeak nodes
- private endpoints
- OpenAI-compatible endpoints
- GitHub
- Slack
- operating-system actions
- MCP services
- any other connector found in code

Use Mermaid diagrams.

Every diagram must have accompanying prose so information is accessible even if
the diagram does not render.

Required diagrams:

- complete component map;
- deployment topology;
- trust boundaries;
- dictation sequence;
- meeting sequence;
- meeting-to-actuator sequence;
- persistent Agent conversation sequence;
- Coder/Gate sequence;
- model-assignment execution sequence;
- mesh execution sequence;
- restart/recovery flow;
- database/domain ERD;
- Desk/frontend composition;
- plugin lifecycle;
- connector lifecycle.

PHASE: DICTATION DOCUMENTATION
==============================

Produce a canonical document:

  docs/DICTATION_ARCHITECTURE.md

Reconstruct the complete flow from key-down to final insertion.

Identify:

- hotkey implementation;
- microphone lifecycle;
- buffering;
- silence/end detection;
- Whisper backend selection;
- supported transcription engines;
- language pinning;
- text normalization;
- punctuation;
- symbol replacement;
- intent classification;
- application/target detection;
- project context;
- Coder context;
- correction memory;
- LLM rewriting;
- preview;
- text injection;
- clipboard fallback;
- Wayland behavior;
- wake mode;
- voice commands;
- journaling;
- replay;
- learning digest;
- activity pre-briefing;
- remote dictation;
- browser-based speech ingress if present.

For each stage show:

  INPUT -> TRANSFORMATION -> OUTPUT -> FAILURE -> FALLBACK

Make clear which stages are mandatory and which are optional.

Document exactly when audio or transcript data can leave the device.

PHASE: MEETING DOCUMENTATION
============================

Produce:

  docs/MEETING_ARCHITECTURE.md
  docs/MEETING_INTELLIGENCE.md
  docs/MEETING_AFTERCARE.md

Trace:

live capture
  -> audio channels
  -> transcription
  -> diarization/speaker labels
  -> normalized meeting
  -> intent analysis
  -> plugin selection
  -> model requests
  -> typed artifacts
  -> source/provenance
  -> aftercare
  -> proposals
  -> approval
  -> actuators
  -> receipts.

Inventory ALL built-in meeting plugins from source.

For each plugin include:

- plugin ID;
- purpose;
- activation/selection;
- expected input;
- model interaction;
- schema/output artifact;
- source attribution;
- failure behavior;
- side effects, if any;
- actuator proposals;
- tests.

Do not repeat an unverified claim that there are "14" if current source differs.

Describe decision lifecycle:
- proposed/extracted;
- accepted;
- superseded;
- promoted/exported;
- whatever states source code actually supports.

Describe action-item lifecycle similarly.

PHASE: MODEL AND INFERENCE DOCUMENTATION
========================================

Produce:

  docs/MODEL_RUNTIME.md
  docs/EXECUTION_DESTINATIONS.md

Reverse-engineer:

- model registry;
- discovery;
- local models;
- downloads if present;
- capabilities;
- assignments;
- readiness;
- key/secret state;
- destination definitions;
- model choice validation;
- execution-plan freezing;
- attempts;
- retry;
- fallback;
- explicit non-fallback behavior;
- timeout;
- cancellation;
- token/context accounting;
- result provenance;
- receipts.

Document all destination categories discovered in source:

  this device
  paired device
  private endpoint
  external service

Do not assume these names if source has changed.

For each destination answer:

- What leaves the originating machine?
- What remains local?
- Where is authentication material stored?
- What syncs?
- What does not sync?
- How is availability determined?
- How does failure surface?
- What receipt proves what actually happened?

PHASE: SECURITY, AUTHORITY, AND TRUST
====================================

Read the existing security document, then prove it against implementation.

Produce:

  docs/SECURITY_MODEL.md
  docs/AUTHORITY_MODEL.md
  docs/generated/trust-boundaries.yaml

Model these separately:

  DATA BOUNDARY
  Where information may flow.

  COMPUTE BOUNDARY
  Where work executes.

  AUTHORITY BOUNDARY
  Who can authorize a side effect.

  SECRET BOUNDARY
  Where credentials exist.

  AUDIT BOUNDARY
  What evidence is retained.

Inventory every path capable of:

- outbound HTTP;
- WebSocket;
- launching a process;
- executing a shell command;
- opening a URL;
- manipulating an application;
- typing/pasting;
- touching GitHub;
- touching Slack;
- sending to an LLM endpoint;
- communicating with paired nodes;
- using MCP;
- receiving remote commands.

For every side-effect path prove:

  REQUEST
      ->
  PROPOSAL, if required
      ->
  REVIEW
      ->
  AUTHORITY CHECK
      ->
  EXECUTION
      ->
  RECEIPT

Where code does not follow this structure, highlight it.

Document Secure / Normal / YOLO or whatever control modes currently exist.

State precisely:
- what each mode changes;
- what each mode cannot change;
- which invariants remain hard;
- whether mode affects review, authority, execution, or all three.

Create a table named:

  "What can leave your machine?"

with one row per data class and operation.

PHASE: DESK AND FRONTEND
========================

Produce:

  docs/DESK_ARCHITECTURE.md
  docs/DESK_OBJECT_MODEL.md

Reconstruct the canonical web architecture.

Verify:
- Vite;
- React version;
- React Router;
- Pixi;
- Zustand;
- DOM/window composition;
- actual canonical routes;
- demoted/deep-link routes;
- API client organization;
- event/state flow.

Inventory every Desk object type.

For each object:
- icon/visual identity;
- backing domain entity;
- current state indicators;
- single click;
- double click;
- right click;
- drag;
- drop targets;
- lasso behavior;
- available verbs;
- boundary badge;
- attention state;
- persistence;
- deep-link/window.

Create a verb matrix:

                   Create Open Ask File Attach Run Approve Delete ...
  Meeting
  Note
  Artifact
  Agent
  Knowledge
  Project
  Coder session
  Receipt
  ...

Only mark a verb present if code proves it.

This matrix should expose accidental inconsistencies in Desk interaction design.

PHASE: AGENTS, THREADS, INTERVIEW, AND MCP
=========================================

Produce:

  docs/AGENTS_AND_THREADS.md

Clarify the difference between:

- Model
- Agent
- Thread
- Conversation
- Interview
- Coder session
- Workflow
- MCP server/tool
- Artifact

Explain which object owns:
- model assignment;
- destination;
- conversation history;
- attached grounding;
- tool permissions;
- artifacts;
- receipts.

Verify the architectural claim that Interview composes Thread, routing and MCP
rather than introducing a parallel model runtime.

Document the tool loop precisely.

Show how references and tool activity become visible to the user.

PHASE: CODER / CLAUDE / CODEX
=============================

Produce:

  docs/CODER_INTEGRATION.md
  docs/GATE.md

Inventory every installed hook and its lifecycle.

Show:

Claude/Codex
  -> hook
  -> HoldSpeak session representation
  -> Desk object
  -> question/waiting state
  -> user response
  -> delivery back to session.

For Gate show:

agent requests operation
  -> hook interception
  -> risk/policy decision
  -> redacted preview
  -> HoldSpeak approval request
  -> allow / deny
  -> reason propagation
  -> agent continuation.

Prove fail-closed semantics through tests/source.

Document:
- timeout;
- hub offline;
- malformed request;
- duplicate response;
- stale approval;
- hub restart;
- concurrent approvals.

Clearly separate:
- coding-session observation;
- human response;
- AI-drafted response;
- risky-operation authorization.

PHASE: EXTERNAL INTEGRATIONS
============================

Produce:

  docs/INTEGRATIONS.md
  docs/CONNECTOR_DEVELOPMENT.md
  docs/ACTUATOR_DEVELOPMENT.md
  docs/generated/integrations.yaml

For each integration:

- purpose;
- direction;
- read/write;
- auth;
- secrets;
- data boundary;
- triggers;
- API endpoints;
- proposals;
- approvals;
- execution;
- receipts;
- error handling;
- retries;
- rate limits;
- idempotency;
- tests.

Pay particular attention to:
- GitHub;
- Slack;
- OS voice actions;
- MCP;
- paired HoldSpeak nodes;
- external model endpoints.

For GitHub explicitly document supported AND deliberately unsupported actions.

Do not imply merge/close/force-push support unless implementation proves it.

PHASE: COMPANIONS
=================

Produce:

  docs/COMPANIONS_ARCHITECTURE.md
  docs/IPAD.md
  docs/AIPI_LITE.md

For iPad:
- determine exact release/readiness status;
- map every typed client;
- map every API consumed;
- map its local store;
- migrations;
- secrets;
- trust badges;
- paired-hub identity;
- failure when desktop disappears;
- dictation flow;
- meeting flow;
- proposal approval;
- Coder flow;
- file import;
- learning/pre-briefing.

Clearly separate:
  implemented and tested
from:
  publicly released.

For AIPI-Lite:
- firmware;
- bridge;
- transport;
- remote audio protocol;
- status/control messages;
- authentication;
- Coder reply behavior;
- failure modes.

PHASE: DATA MODEL AND STORAGE
=============================

Produce:

  docs/DATA_MODEL.md
  docs/STORAGE_AND_MIGRATIONS.md

Reverse-engineer the actual SQLite schema and migration path.

Create a real ERD.

Document:
- tables/entities;
- ownership;
- important indexes;
- retention;
- cascades;
- artifact payloads;
- transcript storage;
- dictation journal;
- corrections;
- meetings;
- decisions/actions;
- model/destination configuration;
- receipts;
- kernel/process state;
- conversations;
- attachments;
- projects;
- integration state.

Answer:
- What is NOT in SQLite?
- Where are large/audio artifacts stored?
- Where are secrets stored?
- Where are search indexes stored?
- What is ephemeral?

Document version compatibility.

Prove:
- backup-before-migration;
- refusal of newer schema;
- restore behavior;
- integrity checks.

PHASE: OPERATIONS
=================

Produce:

  docs/OPERATIONS.md
  docs/TROUBLESHOOTING.md

Treat `holdspeak doctor` as a product.

Inventory every check it performs.

For each check:
- condition;
- success;
- degraded state;
- failure;
- recommended repair;
- platform applicability.

Document:
- installation;
- upgrade;
- rollback;
- backup;
- restore;
- logs;
- diagnostic collection;
- model troubleshooting;
- microphone troubleshooting;
- system-audio troubleshooting;
- hotkey troubleshooting;
- injection troubleshooting;
- Wayland;
- database corruption;
- schema mismatch;
- mesh failure;
- companion pairing;
- external integration errors.

Write troubleshooting as decision trees, not generic prose.

PHASE: PLATFORM MATRIX
======================

Produce:

  docs/PLATFORMS.md
  docs/generated/platform-matrix.yaml

For every capability record:

              macOS Apple Silicon | Linux X11 | Linux Wayland | iPad | AIPI

Classify:

  supported
  supported_with_setup
  degraded
  best_effort
  unsupported
  not_applicable
  unknown

Where possible, derive this automatically from capability metadata/tests.

Call out platform-specific dependencies:
- MLX;
- BlackHole;
- Pulse/PipeWire;
- synthetic keyboard restrictions;
- clipboard fallback;
- permissions;
- native presence implementation.

PHASE: USER MENTAL MODEL
========================

Produce:

  docs/WHAT_IS_HOLDSPEAK.md
  docs/USE_CASES.md

The product must be understandable in five minutes.

Try this high-level model, but change it if repository evidence suggests a
better one:

  SPEAK -> TYPE
  MEET -> UNDERSTAND
  CONTEXT -> THINK
  PROPOSE -> APPROVE -> ACT

Explain HoldSpeak first through user outcomes, not architectural nouns.

Then explain the deeper architecture.

Create progressive onboarding levels:

LEVEL: START
- local dictation;
- first meeting.

LEVEL: ENHANCE
- dictation pipeline;
- corrections;
- meeting intelligence;
- project context.

LEVEL: ORGANIZE
- Desk;
- Notes;
- Artifacts;
- Project Memory.

LEVEL: DELEGATE
- Agents;
- models;
- destinations;
- Coder.

LEVEL: CONNECT
- GitHub;
- Slack;
- companions;
- mesh.

LEVEL: GOVERN
- Gate;
- control modes;
- approval;
- receipts.

Do not expose everything to a beginner simultaneously.

PHASE: MACHINE-READABLE ARCHITECTURE
====================================

The documentation must not become stale immediately.

Create a directory:

  architecture/

or an appropriate repository-native equivalent containing:

  capabilities.yaml
  components.yaml
  integrations.yaml
  trust-boundaries.yaml
  domain-model.yaml
  platform-matrix.yaml

Design JSON Schema or another validation schema for each.

Implement a lightweight validation tool such as:

  scripts/validate_architecture.py

It should verify where practical:

- IDs unique;
- referenced capability IDs exist;
- referenced component IDs exist;
- referenced integration IDs exist;
- docs paths exist;
- source paths exist;
- test paths exist;
- status uses valid enumeration;
- external side effect has declared authority model;
- external boundary has declared egress information;
- stable capability has test evidence or an explicit documented exemption.

Do not introduce a huge framework for this.
Keep it boring, inspectable and reliable.

PHASE: DOCUMENTATION COVERAGE
=============================

Generate:

  docs/generated/DOC_COVERAGE.md
  docs/generated/doc-coverage.json

For each capability calculate whether it has:

- implementation evidence;
- test evidence;
- user docs;
- architecture docs;
- operational docs, where required;
- security docs, where required;
- platform status;
- API documentation, where required.

Use this to find undocumented capabilities and unsupported documentation claims.

PHASE: AGENT SKILLS
===================

Create agent-oriented skills appropriate to the repository's existing agent
skill format. If no format exists, create a simple and well-documented
`agent/skills/` structure.

At minimum provide:

  agent/skills/holdspeak-repo-navigator/
  agent/skills/holdspeak-capability-verifier/
  agent/skills/holdspeak-kernel/
  agent/skills/holdspeak-dictation/
  agent/skills/holdspeak-meetings/
  agent/skills/holdspeak-model-routing/
  agent/skills/holdspeak-desk/
  agent/skills/holdspeak-security-review/
  agent/skills/holdspeak-plugin-author/
  agent/skills/holdspeak-connector-author/
  agent/skills/holdspeak-api-client/
  agent/skills/holdspeak-troubleshooter/
  agent/skills/holdspeak-release-auditor/
  agent/skills/holdspeak-doc-maintainer/

Each skill MUST tell an agent:

1. When this skill applies.
2. Canonical files to inspect first.
3. Domain vocabulary.
4. Architectural invariants.
5. Typical change paths.
6. Tests that must be run.
7. Documentation that must be updated.
8. Security/privacy traps.
9. Platform-specific traps.
10. Anti-patterns.
11. How to prove completion.

Example:

A model-routing skill must warn:
- never silently substitute a different model unless current policy explicitly
  allows and records it;
- preserve frozen execution-plan semantics;
- never expose keys in ordinary APIs;
- preserve actual-destination receipts.

A meeting-actuator skill must warn:
- extraction is not authorization;
- a model-generated action item is not permission to call GitHub/Slack;
- preserve exact proposal -> approval -> execution correspondence.

A kernel skill must warn:
- never invent parallel lifecycle state outside the kernel if the kernel owns
  that state;
- preserve journal/event semantics;
- preserve Unknown rather than fabricating terminal success/failure.

PHASE: API REFERENCE
====================

Generate or write:

  docs/API_REFERENCE.md

Inventory every backend endpoint.

Group endpoints by domain.

For each:
- method;
- route;
- auth/trust assumptions;
- request schema;
- response schema;
- error responses;
- side effects;
- idempotency;
- approval requirements;
- boundary;
- primary clients;
- implementation path;
- tests.

Distinguish:
- stable API;
- internal frontend API;
- companion API;
- integration webhook/hook API;
- experimental/internal endpoints.

If OpenAPI exists, compare docs against OpenAPI and preferably generate the
mechanical portions.

PHASE: DECISION RECORDS
=======================

Do not silently redesign architecture while documenting it.

Where you find a real unresolved architectural issue, create a PROPOSED ADR.

Potential topics may include:

- canonical definition of Kernel;
- canonical Work model;
- Artifact ontology;
- Proposal/Approval/Execution/Receipt contract;
- Connector versus Actuator distinction;
- model Destination taxonomy;
- Desk object verb consistency;
- capability registry as source of truth.

Put proposals somewhere clearly separated from accepted architecture.

Every proposed ADR must include:
- context;
- current reality;
- problem;
- options;
- recommendation;
- migration impact;
- compatibility;
- security impact;
- alternatives rejected;
- decision status = PROPOSED.

PHASE: DOCUMENTATION INFORMATION ARCHITECTURE
============================================

Refactor documentation navigation so there are clear audiences:

START
  What is HoldSpeak?
  Getting Started
  Core workflows

USE
  Dictation
  Meetings
  Desk
  Memory
  Agents
  Coder
  Companions

UNDERSTAND
  System Architecture
  Kernel
  Domain Model
  Data Model
  Model Runtime
  Security / Authority

EXTEND
  Plugins
  Connectors
  Actuators
  API
  Companion development

OPERATE
  Doctor
  Models
  Destinations
  Backup / Restore
  Troubleshooting
  Platform behavior

CONTRIBUTE
  Repository map
  Architecture rules
  Testing
  Docs maintenance
  Release process

REFERENCE
  Capabilities
  Configuration
  API
  Schemas
  Glossary
  Platform matrix

HISTORY / INTERNAL
  RFCs
  superseded architecture
  roadmaps
  historical plans

A new contributor should never accidentally treat an old Phase plan as current
system documentation.

PHASE: DOCUMENTATION STYLE
==========================

Use plain language.

Prefer:
  "The hub stores this in SQLite."

over:
  "Persistence semantics are facilitated by the storage abstraction."

Prefer:
  "Approving the proposal sends the Slack message."

over:
  "The approval mechanism provides an execution pathway."

Use canonical domain vocabulary consistently.

For every advanced feature include:

  What it is
  Why it exists
  What the user sees
  How it works
  What it stores
  What can leave the machine
  What can go wrong
  How to troubleshoot it
  Where the implementation lives

Avoid duplicate truth.

When one document owns a contract, other documents should link to it rather
than rewriting it differently.

PHASE: DOC TESTING
==================

Extend the existing documentation quality approach.

At minimum ensure CI can detect:

- broken internal links;
- missing files referenced by generated registries;
- invalid YAML/JSON schemas;
- broken Mermaid;
- stale generated capability index;
- references to deleted API routes;
- invalid source/test paths;
- conflicting capability IDs.

Where feasible add executable examples.

Do not make CI dependent on external cloud APIs merely to verify docs.

FINAL DELIVERABLE TREE
======================

The exact repository layout may justify modifications, but the finished package
should contain equivalents of:

docs/
  README.md
  WHAT_IS_HOLDSPEAK.md
  GETTING_STARTED.md
  CAPABILITIES.md
  USE_CASES.md
  SYSTEM_ARCHITECTURE.md
  SYSTEM_INTEGRATION_MAP.md
  DOMAIN_MODEL.md
  KERNEL.md
  DATA_MODEL.md
  STORAGE_AND_MIGRATIONS.md
  DICTATION_ARCHITECTURE.md
  MEETING_ARCHITECTURE.md
  MEETING_INTELLIGENCE.md
  MEETING_AFTERCARE.md
  DESK_ARCHITECTURE.md
  DESK_OBJECT_MODEL.md
  AGENTS_AND_THREADS.md
  CODER_INTEGRATION.md
  GATE.md
  MODEL_RUNTIME.md
  EXECUTION_DESTINATIONS.md
  SECURITY_MODEL.md
  AUTHORITY_MODEL.md
  INTEGRATIONS.md
  COMPANIONS_ARCHITECTURE.md
  IPAD.md
  AIPI_LITE.md
  API_REFERENCE.md
  PLATFORMS.md
  OPERATIONS.md
  TROUBLESHOOTING.md
  CONNECTOR_DEVELOPMENT.md
  ACTUATOR_DEVELOPMENT.md
  GLOSSARY.md

docs/generated/
  REPOSITORY_SNAPSHOT.md
  REPOSITORY_MAP.md
  ARCHITECTURE_HISTORY.md
  DOC_COVERAGE.md
  capabilities.yaml
  components.yaml
  integrations.yaml
  trust-boundaries.yaml
  domain-model.yaml
  platform-matrix.yaml
  doc-coverage.json

agent/skills/
  holdspeak-repo-navigator/
  holdspeak-capability-verifier/
  holdspeak-kernel/
  holdspeak-dictation/
  holdspeak-meetings/
  holdspeak-model-routing/
  holdspeak-desk/
  holdspeak-security-review/
  holdspeak-plugin-author/
  holdspeak-connector-author/
  holdspeak-api-client/
  holdspeak-troubleshooter/
  holdspeak-release-auditor/
  holdspeak-doc-maintainer/

architecture/ or equivalent:
  schema definitions for the machine-readable registries

scripts/
  validate_architecture.py
  generate_capability_docs.py
  check_doc_coverage.py

FINAL AUDIT
===========

Before finishing, perform a hostile accuracy review.

Search the final documentation for absolute claims such as:

  always
  never
  local
  private
  secure
  supported
  stable
  automatic
  encrypted
  approved
  immutable
  exactly
  all
  no data leaves

For every such word:
- prove the claim;
- qualify it;
- or remove it.

Search for every named product capability in UI labels, routes and tests.

Find:
- UI capability absent from docs;
- docs capability absent from code;
- API absent from docs;
- side effect absent from security model;
- secret absent from threat model;
- platform conditional absent from platform matrix;
- model request absent from boundary documentation;
- background process absent from kernel/process documentation.

Run all documentation validation.

Run the relevant test suite.

Do not declare success with failing tests unless the failure demonstrably
predates your changes; if so, document it explicitly.

FINAL EXECUTIVE REPORT
======================

Produce:

  docs/generated/DOCUMENTATION_AUDIT_REPORT.md

It must contain:

A. What HoldSpeak actually is now.
B. Complete subsystem list.
C. Complete capability count by domain and status.
D. Architecture summary.
E. Kernel conclusion.
F. Trust-boundary summary.
G. Top architectural ambiguities discovered.
H. Stale/incorrect documentation corrected.
I. Undocumented functionality discovered.
J. Planned functionality previously mistaken for shipped behavior.
K. Missing tests discovered.
L. Security-sensitive gaps discovered.
M. Platform inconsistencies discovered.
N. Documentation files created/changed.
O. Agent skills created.
P. Machine-readable registries created.
Q. Validation tooling added.
R. Remaining unknowns.
S. Recommended next engineering actions, prioritized.

QUALITY BAR
===========

A successful result should make all of these possible:

A new USER can understand HoldSpeak without understanding its architecture.

A new CONTRIBUTOR can identify where a feature lives without repository
archaeology.

A maintainer can answer "does HoldSpeak support X?" from a generated capability
registry.

A security reviewer can identify every intentional data-egress path.

An agent can modify dictation without accidentally violating meeting or model
routing invariants.

An agent can modify actuators without accidentally bypassing human authority.

A developer can understand the kernel without reading every implementation
file.

A release can state exactly which capabilities are Stable, Beta, Experimental,
Platform-limited, or Unreleased.

Documentation drift becomes detectable rather than dependent on memory.

Most importantly:

THE DOCUMENTATION MUST MAKE HOLDSPEAK FEEL SMALLER THAN THE CODEBASE.

The system may be sophisticated.

The mental model must not be.

Do not achieve simplicity by omitting capabilities.

Achieve simplicity by discovering and documenting the small number of
consistent concepts from which those capabilities are built.
```

The agent prompt deliberately makes **verification and documentation tooling part of the same job**. That is crucial. A one-time heroic documentation pass would reproduce the same problem a year from now. The goal should instead be to make architecture, capability status and documentation coverage **machine-checkable properties of the repository**.

---

## Open questions and limitations

There are several areas I would consider **especially important to resolve during the source-level audit**.

**The exact kernel implementation remains the largest architectural unknown.** The indexed current README definitely names a kernel journal and a read-only Process view of running/waiting/unknown/finished state, but the material available in this research pass did not expose enough kernel source to responsibly identify its concrete classes, journal schema, ownership of execution, restart behavior or relationship to receipts. Any more specific claim here would risk manufacturing architecture from terminology. citeturn1search0

**The exact inventory of the 14 meeting plugins should be generated from implementation rather than copied from marketing documentation.** The current README explicitly claims 14 built-in, LLM-backed plugins and gives representative outputs such as decisions, actions, ADRs, risks and incident timelines, but a definitive documentation package should name and trace every plugin from registration through its schema and tests. citeturn1search0

**Control-mode semantics need a dedicated implementation audit.** The documentation index confirms Secure, Normal and YOLO modes and emphasizes that review, authority and execution are distinct and that hard invariants exist, but those invariants should be enumerated from the policy/authorization code rather than inferred from the index page. citeturn1search1

**The relationship between Workflows, Agents, Interview, Thread, MCP and the kernel deserves normalization.** The architecture page makes one very helpful boundary explicit—Interview composes Thread, routing and MCP rather than becoming a separate runtime—but the complete cross-system orchestration model should be established from source. citeturn1search3

**iPad needs explicit “implementation maturity versus release status” documentation.** The README is unusually clear that the screens and typed-client layer are built and tested while the application itself has not yet been released. That distinction should become a formal capability status so future docs never turn “implemented” into “available to users.” citeturn1search0

**The public identity of HoldSpeak now lags its architecture.** `pyproject.toml` still frames it as a voice-typing application while the canonical README describes a much larger voice-work, meeting, knowledge, agent and governance environment. Resolving this is the highest-level product-design decision exposed by the audit. citeturn1search0turn1search2

The key strategic insight is therefore not that HoldSpeak has accumulated “too many features.” It is that those features have **not yet been compressed into a sufficiently explicit architecture and vocabulary**.

The architecture already contains the beginnings of that compression:

```text
Sources produce Work.
Work produces Artifacts.
Artifacts provide Context.
Models execute at Destinations.
Potential side effects become Proposals.
Humans grant Approval.
Approved work may Execute.
Reality becomes a Receipt.
The Kernel records what the system is doing.
The Desk makes all of it visible.
```

If that model survives the source-level verification requested in the agent prompt, it can become the conceptual backbone of HoldSpeak.

And that would make the product's ambition much easier to understand:

> **HoldSpeak is the place where your voice becomes work—and where AI can help carry that work forward without obscuring the context, destination, authority, or evidence behind what happened.**

--

