ROLE: check. Read-only counsel as Muad'Dib on Astra PHILO-6-05 design BEFORE product edits in /Users/karol/dev/tools/wt-philo-6-b. No edits, staging, agents, hubs, commits or broad suites. Return exact VERDICT/FINDINGS/CONDITIONS/MISSED/TUESDAY/UNKNOWN, path:line or raw observation evidence, Tenet for findings.

Read TWO-BRAINS §3, the phase story-05 and docs/internal/philo/phase-6/body/design.md + diagnosis.md + current test web/src/desk/__tests__/philo605DecisionBodyDiagnosis.test.tsx. Product seam web/src/desk/store/dataSlice.ts refresh162-191/updatePrimitive289-369; api.ts loadAll626+; DeskApp live items80 and derived object133+. No product code edited yet for05.

Actual baseline S4 proof from `git archive origin/main` copy: docs/internal/philo/phase-6/body/red-s4-393-continuous/20260925T025111Z-case.closure.chain.s4_saved_content-astra-393/observation.json and corresponding red-s4-1440-continuous directory. first postDone paint is already readable (39310.8ms,144010.1ms), then old whole loadAll overwrites body (393159.1ms,144077.3ms), then later read restores. decision_traffic shows prePUT GET with blank body, successful PUT, postPUT GET with saved text. First held-prop hypothesis was rejected and corrected; production parent is subscribed. First-frame-only rig prototype PASSED even while initial .959s was empty; retained earlier red-s4-393 is explicitly not a fix proof. Continuous frame predicate now retains a sticky unreadable frame and fails the real atlas at both widths. Scripts graph_walk.py's probe is armed BEFORE actual Done click and tracks frames through terminal observation. Raw observations unchanged; archive source/import manifest under rows/baseline.json.

Design proposes a store-local per-item {version,pending} map, snapshots at refresh start, retain optimistic/current items touched after or pending at start (even if absent in returned bucket), synchronous current-version rollback on rejection, existing failure receipt, ignore stale response effects over newer write, then existing refresh. Protect failure rollback when follow-up read is unavailable. No pullout shadow, new verbs or changed flow. Please challenge scope: preserve unreachable data for ALL kinds vs only protected written items may be broader than necessary; Tenet1 applies. Tests planned on real store + producer mapping controlled transport, real Pullout/footer rendered receipt, archive red and green; failed save slow/failed refresh, during-write refresh, older rejection versus newer write.

A criterion clarification is required: initial frame was already correct on main, so we cannot honestly claim initial-frame-only red. Proposed story wording: first read frame remains readable through the save/refresh transition, with pre-click observer and every frame retained; first-frame assertion stays, continuous-no-empty fence supplies the pre-fix red. Acceptance strengthened, no eventual-only closure. Owner may overrule at sitting. What did we miss; would you ratify the minimal repair and visible clarification before a Luna builds? No final built verdict asked.
