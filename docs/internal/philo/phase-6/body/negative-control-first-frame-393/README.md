# PASS negative control — first frame only

This archived S4 run passed the first-frame-only predicate before the fix. It is not red proof. The continuous red runs are in `../red-s4-393-continuous/` and `../red-s4-1440-continuous/`. The raw observation keeps its original run/output paths for provenance.
