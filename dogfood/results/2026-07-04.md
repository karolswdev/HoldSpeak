# Dogfood run — 2026-07-04

- **Environment:** macOS (Darwin 25.2.0, arm64) · repo main @ f7f8283 (+ this run's harness fixes) · sandbox `dogfood/_home` · Whisper MLX · intel + dictation runtime on the REAL `.43` endpoint (Qwythos-9B-Claude-Mythos-5-1M-Q6_K, grammar-free vision server)
- **Tier:** both (tier-1 swap for the no-LLM checks, tier-2 for everything else) + the Cadence tier
- **Driver:** Claude (headless; checks needing a mic, native HUD glass, or a human hand are honest SKIPs)
- **Score:** 40 PASS · 14 PARTIAL · 1 FAIL · 8 SKIP (of 63 checks)
- **Top findings:** F-05 (imported meetings never get typed plugin artifacts — the run's headline), F-10 (the rewriter fabricates from empty input), F-01 (theaterorb.png 404s everywhere), plus 3 harness gaps fixed in-run (blocks fixtures, the missing project-rewriter stage, the stale intel model default).

# HoldSpeak Dogfood Protocol

The end-to-end exercise. You drive a real, isolated HoldSpeak against believable
data — three mock repos with `.hs/` context and completed-stage history, plus
meetings and dictation rendered through the macOS `say` voices — and you check
off what works. The point is to feel every surface the way a user does, on
real metal, and to keep a fillable record you can re-run each release.

**How to use this file**

1. Run the preflight (section 0).
2. Copy this file to a dated run: `cp dogfood/PROTOCOL.md dogfood/results/$(date +%F).md`
   (the `results/` copies are gitignored — fill those in, leave this master clean).
3. Work top to bottom. For each check, set **Result** to `PASS` / `FAIL` /
   `PARTIAL` / `SKIP` and jot a **Note** (what you saw, screenshot path, the bug).
4. Roll the run up in `dogfood/RESULTS-TEMPLATE.md`'s header (env, tier, score,
   top failures) at the top of your dated copy.

**Two tiers** (you chose both):

- **Tier 1 — Plumbing.** No LLM, no microphone. Fast, deterministic, run it
  every iteration. Catches dead routes, broken imports, config drift.
- **Tier 2 — Real metal.** Real `say` → Whisper → intel on `.43`. The truth
  tier: proves the LLM-shaped features actually produce output, not just plumb.

Legend: `hs` = the isolated launcher (`dogfood/hs`, or `hs` after `source
dogfood/env.sh`). Web routes are on the URL `hs web` prints. "Expect" is the
pass bar; if reality disagrees, that's a `FAIL` and a finding.

---

## 0 · Preflight

- [x] **P-01 · Build the sandbox.** `dogfood/setup.sh` (tier-2 config, intel → `.43`).
  - Expect: writes `_home/.config/holdspeak/config.json`; links HF cache + Models.
  - Result: **PASS** · Note: setup.sh --force wrote the tier-2 config (intel -> .43, model overridden to the live Qwythos-9B via DOGFOOD_INTEL_MODEL; the env.sh default still names the retired Qwen3.5 — F-06); HF cache + Models symlinked.

- [x] **P-02 · Isolated doctor is green.** `dogfood/hs doctor`
  - Expect: runtime deps OK; config loads; DB at the sandbox path (not your real `~`).
  - Result: **PASS** · Note: 22 passed / 1 warn (cloud egress, intentional tier-2) / 0 failed; DB + config at the sandbox paths; .43 preflight PASS with the model available.

- [x] **P-03 · `.43` reachable.** `curl -s http://192.168.1.43:8080/v1/models` (use `dangerouslyDisableSandbox` if scripting).
  - Expect: a model list comes back. If not, Tier 2 is blocked — do Tier 1 only.
  - Result: **PASS** · Note: GET /v1/models returned Qwythos-9B-Claude-Mythos-5-1M-Q6_K.gguf (grammar-free vision server).

- [x] **P-04 · Render fixtures.** `python dogfood/make_fixtures.py` (macOS `say`).
  - Expect: `_audio/*.wav` for 6 meetings + 6 dictation sets; `MANIFEST.json`; `*.script.txt` ground truth.
  - Result: **PASS** · Note: 38 files: 6 meeting wavs + 5 dictation sets + german + spoken-symbols + voice-macros, MANIFEST.json + .script.txt ground truths.

- [x] **P-05 · Plumbing e2e green.** `HOLDSPEAK_DOGFOOD=1 uv run pytest -q tests/e2e/test_dogfood_plumbing_e2e.py`
  - Expect: all pass (scenarios well-formed, repos load, transcripts parse).
  - Result: **PASS** · Note: 20 passed in 0.12s.

---

## 1 · Tier 1 — Plumbing (no LLM, no mic)

> Optionally swap to the lean config first: `dogfood/setup.sh --tier1 --force`.
> Everything here must work with intel and the dictation pipeline off.

### 1.1 CLI surface

- [x] **T1-01 · doctor variants.** `hs doctor --strict` then `hs doctor --connectors`
  - Expect: `--strict` exits non-zero only on real warnings; `--connectors` lists packs + discovery errors.
  - Result: **PASS** · Note: --strict exits 1 on the one real (intentional) cloud-egress warn; --connectors lists 5 first-party packs, no discovery errors.

- [x] **T1-02 · Help for every subcommand.** `hs --help`, then `--help` on `web meeting history actions import doctor backup restore dictation agent-hook device-psk intel`.
  - Expect: each prints usage, no traceback.
  - Result: **PASS** · Note: All 13 subcommand --help variants print usage, no tracebacks (cadence too).

- [x] **T1-03 · Backup / restore round-trip.** `hs backup`; note the `.bak`; `hs restore` (list); `hs restore <file> --yes`.
  - Expect: timestamped backup created; restore confirms then swaps; DB intact.
  - Result: **PASS** · Note: Fresh sandbox first: honest 'no database yet' error. After imports: timestamped .bak created; restore --yes swapped AND banked the pre-restore DB as its own .bak; history intact (5 rows) after.

- [x] **T1-04 · device-psk.** `hs device-psk show` then `hs device-psk rotate` then `show`.
  - Expect: a PSK shows; rotate changes it; persisted in sandbox config.
  - Result: **PASS** · Note: PSK shows; rotate changes it; persisted in the sandbox config.

### 1.2 Import — transcript path (deterministic, no Whisper)

- [x] **T1-05 · Import all three committed transcripts.**
  `hs import dogfood/transcripts/pylon-incident.vtt --title "Pylon cert outage" --tag incident`
  then the `.srt` and `.txt`.
  - Expect: three meetings created fast (no model load); segments carry speakers (Jordan/Wei/Priya; Priya/Mara/Sam; Dana/Ravi/Meaghan).
  - Result: **PASS** · Note: All three transcripts imported fast, no model load; speakers from the files feed the meetings (Jordan/Wei/Priya; Priya/Mara/Sam; Dana/Ravi/Meaghan).

- [x] **T1-06 · History CLI.** `hs history`; `hs history -s cert`; `hs history --from 2026-01-01 --to 2026-12-31`; `hs history <id>`; `hs history <id> --export markdown` (also `json`, `txt`).
  - Expect: list, search hit, date filter, detail, and all three export formats render.
  - Result: **PASS** · Note: list/search(cert)/date-filter/detail all correct; all three export formats render real files (json 2.4K, md/txt ~950B) under the SANDBOX Documents dir.

- [x] **T1-07 · Actions CLI.** `hs actions --all`; `hs actions --owner Priya`; `hs actions --done <id>`; `hs actions --dismiss <id>`.
  - Expect: cross-meeting action items list; owner filter works; done/dismiss change state.
  - Result: **PASS** · Note: Checked after intel: cross-meeting list, --owner filter, --done/--dismiss change state (see the T2 section run).

### 1.3 Web surface — every page loads

- [x] **T1-08 · Launch web isolated.** `hs web` (note the URL/port).
  - Expect: server binds 127.0.0.1; `/health` returns OK.
  - Result: **PASS** · Note: Binds 127.0.0.1 (ephemeral port); /health ok. Note: `hs web` has no --port flag (protocol says 'note the URL/port' - fine).

- [x] **T1-09 · Walk every page.** Open `/`, `/welcome`, `/setup`, `/history`, `/settings`, `/activity`, `/dictation`, `/commands`, `/companion`, `/presence`, `/docs/dictation-runtime`.
  - Expect: each renders with content (no blank tab, no console error, no 500). Note any that look unstyled or empty.
  - Result: **PASS** · Note: All 11 pages 200 with real content, zero page-level console errors - EXCEPT one systemic 404'd asset /theater/theaterorb.png on every theater-bearing page (F-01). / correctly fronts /welcome on the fresh sandbox.

- [x] **T1-10 · History UI + facets.** On `/history`: the 3 imported meetings show; filter by speaker facet, by tag, by date.
  - Expect: faceted search narrows the list server-side.
  - Result: **PASS** · Note: Server-side narrowing exact: speaker=Priya->2, tag=incident->1, search=cert->1, speaker+tag combo->1; /api/meetings/facets serves the 8 speakers + 3 tags.

### 1.4 Config cockpit

- [x] **T1-11 · Read + write settings.** On `/settings`: change the hotkey display, the model name, the transcription language, toggle presence. Save.
  - Expect: changes persist to `_home/.config/holdspeak/config.json` (verify by reading the file); reload shows them.
  - Result: **PASS** · Note: PUT /api/settings persisted model.language=de to the sandbox config.json and reverted clean; presence toggle too.

- [x] **T1-12 · Settings is sectioned + searchable.** Search the settings page for "wake", "macro", "intel".
  - Expect: the search surfaces the right sections; every knob from `config.json` is reachable.
  - Result: **PASS** · Note: Search 'wake' filters to the wake-word section (screenshot); sectioned sidebar; 'macro'/'intel' also surface their sections.

### 1.5 Dictation plumbing

- [x] **T1-13 · dry-run with pipeline off.** `hs dictation dry-run "claude add idempotency to the charge endpoint"`
  - Expect: prints the pipeline trace + final text; works with no LLM backend (stages skipped honestly).
  - Result: **PASS** · Note: Tier-1 config: trace prints all stages, intent-router skipped honestly (empty_blockset), final_text = input, 0.01ms.

- [x] **T1-14 · blocks + runtime.** From inside `dogfood/repos/ledgerline`: `hs dictation blocks ls`; `hs dictation blocks show agent_task_buildout`; `hs dictation blocks validate`; `hs dictation runtime status`.
  - Expect: the repo's `blocks.yaml` resolves; the block shows; validate passes; runtime status reports the resolved backend.
  - Result: **PARTIAL** · Note: The mock repos shipped WITHOUT .holdspeak/blocks.yaml (F-02, fixed in this run: all 3 repos now carry believable blocks). And blocks ls/show DON'T cwd-detect - they need --project . (F-03; dry-run DOES auto-detect: 'project: ledgerline', 2 blocks resolved). With --project: ls/show/validate all green; runtime status reports resolved backend mlx + missing sandbox model honestly.

- [x] **T1-15 · Journal records even without typing.** After a couple of dry-runs, confirm rows in the journal (DB `dictation_journal`, or the journal view).
  - Expect: raw + final text + warnings stored; retention cap honored.
  - Result: **FAIL** · Note: 0 journal rows after CLI dry-runs AND a web /api/dictation/dry-run under tier-1 (F-07). Re-tested under tier-2: see T2-17 note.

- [x] **T1-16 · Spoken-symbol mapping (text path).** `hs dictation dry-run "email me at sign ledgerline dot dev"` with the tier-config symbol table.
  - Expect: "at sign" → `@`; check `hash`/`dash`/`percent sign` and that attach modes control spacing.
  - Result: **PARTIAL** · Note: Spoken symbols do NOT apply on the dry-run text path - by design they live on the capture paths (dictation_capture/wake_glue/device_glue, post-Whisper). Protocol drift; the real check is T2-23 on real audio.

- [x] **T1-17 · Voice macros dry-fire.** On `/commands`: the 4 macros (open inbox / launch editor / list files / paste quote) list; use the test-fire button (`/api/commands/test`) on each.
  - Expect: keyword match is whole-utterance + normalized; each action kind (open_url/launch_app/shell/type_text) reports a correct dry-fire; nothing fires on a non-match.
  - Result: **PASS** · Note: /api/commands/test fires ACTIONS (kind+payload), not keywords. shell dry-fired bounded (real ls, rc 0); type_text returns preview-not-typed; unknown kind 400s naming the vocabulary. open_url/launch_app not fired (they execute for real on the host - same bounded connector as shell); keyword matching is dispatcher-side (unit-covered).

### 1.6 Audio import plumbing (Whisper, intel off)

- [x] **T1-18 · Import a rendered meeting with intel off.** `hs import dogfood/_audio/meeting-questline-balanced-sync.wav --title "Balanced sync"`
  - Expect: meeting goes `importing` → transcribes via Whisper → persists; transcript is recognizable against `meeting-questline-balanced-sync.script.txt`. No artifacts (intel off) — that's correct.
  - Result: **PASS** · Note: 3 windows transcribed via Whisper(MLX); transcript recognizably matches the script ('M1' misheard as 'and one' - normal say-voice level); intel honestly 'disabled in config'; no artifacts.

- [x] **T1-19 · Bad-format import is honest.** `hs import dogfood/PROTOCOL.md`
  - Expect: a clear "unsupported format" error, no crash, no empty meeting.
  - Result: **PASS** · Note: Clear unsupported-format error naming all supported formats, no crash, no empty meeting. Wrinkle: exit code 0 on the error (F-04).

---

## 2 · Tier 2 — Real metal (`say` → Whisper → `.43` intel)

> Use the tier-2 config (`dogfood/setup.sh` default). Each meeting import below
> runs the full intel pipeline. Compare artifacts against the scenario's
> `description` and the repo's completed-stage docs. Intel is non-deterministic —
> judge on substance (did it surface the decision, the owners, the risk?), not
> exact wording.

### 2.1 Meetings — one per MIR profile

- [x] **T2-01 · Architect review (ledgerline).** `hs import dogfood/_audio/meeting-ledgerline-arch-review.wav --title "Scaling the write path"` with `mir_profile=architect`.
  - Expect artifacts: an **ADR draft** (event-sourced write log), a **mermaid** component diagram, a captured **decision** (adopt event sourcing + hourly snapshots, defer sharding), a **dependency map** (snapshot ← append log; reconcile ← snapshot+tail), **action items** with owners (Alex=ADR, Mara=property test, Priya=migration).
  - Result: **PARTIAL** · Note: Base intel EXCELLENT on real .43: the event-sourcing decision + hourly snapshots + deferred sharding + the property-test requirement all surfaced; owners Alex/Mura(=Mara)/Me. But ZERO typed artifacts (no ADR/mermaid/dependency-map artifacts): imported meetings never run the plugin host (F-05).

- [x] **T2-02 · Incident retro (ledgerline).** Import `meeting-ledgerline-incident-retro.wav`, `mir_profile=incident`.
  - Expect: an **incident timeline** (14:02 retry storm → 14:11 23 double-posts → 16:40 made whole), a **runbook delta** (over-billing remediation + duplicate-key alert), a **risk** (no concurrency/load test), a **decision** (don't close LL-118 until both tests green), owners (Mara/Priya/Sam).
  - Result: **PARTIAL** · Note: Substance: idempotency-key race named as cause, retry storm, append-only reversal, slow detection called out; actions = concurrency regression test + runbook step + duplicate-key alert (owners Whisper-garbled: Marones/Prior). Typed timeline/runbook-delta artifacts missing (F-05).

- [x] **T2-03 · Product review (questline).** Import `meeting-questline-product-review.wav`, `mir_profile=product`.
  - Expect: **requirements** for guilds v1 (create/invite-link/shared quest/leaderboard; non-goal no public directory), **customer signals** (accountability-with-friends demand), a **risk** (moderation/abuse surface; activation leak), a **decision** (defer guilds, fix activation, run a flagged shared-quest experiment to measure WAQC).
  - Result: **PARTIAL** · Note: Substance: defer Guilds V1, fix activation step 3, flagged shared-quest experiment, WAQC metric named; 3 actions. Requirements/customer-signal artifacts missing (F-05).

- [x] **T2-04 · Delivery standup (questline).** Import `meeting-questline-delivery-standup.wav`, `mir_profile=delivery`.
  - Expect: a **milestone plan** (M1 onboarding fix → M2 streak tz → M3 shared-quest experiment; guilds v1 → Q4), a **scope-guard** flag (onboarding revamp must not grow into a full redesign), a **decision** + owners.
  - Result: **PARTIAL** · Note: Substance: M1 onboarding -> M2 streak tz -> M3 experiment sequencing + Guilds to Q4 + the scope guard ON the onboarding revamp. Milestone-plan artifact missing (F-05).

- [x] **T2-05 · Incident war-room (pylon).** Import `meeting-pylon-incident-warroom.wav`, `mir_profile=incident`.
  - Expect: a **timeline** (09:14 page → 09:18 TLS errors → 09:52 mitigated, ~38m SEV-2), root-cause chain (PI-198 net-policy → ACME solver blocked → silent renewal fail), a **runbook delta** (break-glass reissue; ACME-path validation), a **risk** (no synthetic ACME test), a **decision** (14-day headroom alert + synthetic ACME CI test), owners.
  - Result: **PARTIAL** · Note: Substance: the full root-cause chain (net-policy -> ACME solver blocked -> silent renewal fail), the alert-fired-too-late insight, the 14-day headroom alert + synthetic ACME CI test decisions, 4 actions. Timeline/runbook-delta artifacts missing (F-05).

- [x] **T2-06 · Balanced sync (questline).** Import `meeting-questline-balanced-sync.wav`, `mir_profile=balanced`.
  - Expect: the buried **decision** (monthly opt-in product-update email) is captured despite the small talk; **action items** with owners (Meaghan=draft email, Tom=watch staging, Dana=tell marketing + leadership note); maybe a **stakeholder update** draft. Brief intents are not dropped.
  - Result: **PARTIAL** · Note: The BURIED decision captured despite small talk (monthly opt-in product email), all 3 owners right (Meaghan-as-Me/Tom/Dana). The brief-intents-not-dropped bar met at intel level; stakeholder-update artifact missing (F-05).

### 2.2 Routing controls

- [x] **T2-07 · Profile changes the artifacts.** Re-route T2-01 under `delivery` then `incident`: `hs intel --route-dry-run <id> --profile delivery` and `--reroute <id> --profile architect`. Or via `/api/intents/profile`.
  - Expect: the plugin chain (and artifacts) differ by profile; dry-run shows the plan without persisting; reroute persists.
  - Result: **PASS** · Note: Route level: architect plan = project_detector/requirements/mermaid/adr_drafter; delivery plan adds action_owner_enforcer/milestone_planner/dependency_mapper. Dry-run shows the plan without persisting; --reroute persists the window. Artifact-level diff unobservable (F-05: reroute records the route but executes no chain).

- [x] **T2-08 · Manual intent override.** `hs intel --reroute <id> --override-intents incident,architecture --threshold 0.5`; or `/api/intents/override`; or `/api/intents/preview` on a snippet.
  - Expect: the override forces those intents; preview simulates a route for arbitrary text.
  - Result: **PASS** · Note: --override-intents incident,architecture --threshold 0.5 persisted with override_intents recorded on the window.

- [x] **T2-09 · Segment probe surfaces buried intents.** With `intent_segment_probe_enabled` off, import the balanced sync; then on, re-import. Compare.
  - Expect: with the probe on, the brief/buried decision in T2-06 is less likely to be dropped.
  - Result: **SKIP** · Note: The probe flag exists and was on for all imports; its artifact-level effect is unobservable while imports produce no artifacts (F-05).

- [x] **T2-10 · Plugin disabling.** Set `disabled_plugins=["mermaid_architecture"]`, re-run T2-01.
  - Expect: the diagram plugin is **skipped** (visible in plugin-runs), not failed; other artifacts unaffected.
  - Result: **SKIP** · Note: Plugin skipping unobservable: no plugin chain executes on imported meetings at all (F-05); plugin-runs is empty.

### 2.3 Deferred intel queue

- [x] **T2-11 · Queue + process + retry.** With `.43` briefly unreachable, import a meeting (job queues); `hs intel` (list), `hs intel --process`, `hs intel --retry <id>`, `hs intel --retry-failed`; `/api/intel/summary`.
  - Expect: jobs queue when realtime is down, process on demand, retry with backoff; summary reports failed % / backoff.
  - Result: **PASS** · Note: Intel pointed at a dead endpoint: job queued with the honest failure + Next-retry backoff; endpoint restored -> --retry --retry-mode retry-now + --process succeeded; /api/intel/summary clean.

### 2.4 Aftercare

- [x] **T2-12 · Aftercare digest + jump + draft.** On a finished meeting: `/api/meetings/<id>/aftercare`, `/followup-draft`; in the UI use the transcript-moment jump.
  - Expect: an open/decided/changed digest; jumping lands on the right transcript moment; a local follow-up email draft reads sensibly. Read-only — nothing executes without approval.
  - Result: **PARTIAL** · Note: Open items by owner + since-last diff + a sensible markdown follow-up draft; T1-07's done/dismiss flowed into the digest (1 open left). decisions[] empty - the digest reads DECISION ARTIFACTS which imports never get (F-05).

### 2.5 Actuators — propose → approve → execute

- [x] **T2-13 · Off by default.** With `allow_actuators=false`, open a meeting's aftercare.
  - Expect: proposals may be listed but **no execute buttons** are active.
  - Result: **PARTIAL** · Note: allow_actuators=false: propose lands, and API-approving EXECUTED - which is the RATIFIED Phase-61 design (aftercare slack: the approval IS the gate; allow_actuators=True by design at that call site, memory feedback_gate: don't fix). The protocol's expectation is about UI execute buttons (unverified headless). Protocol wording drift filed (F-11).

- [x] **T2-14 · GitHub issue actuator.** Set `allow_actuators=true`, `allowed_actuators=["github_issue_actuator"]`. From an accepted action item, propose → approve → `/aftercare/file-issue`.
  - Expect: a proposal with target + preview; approval gates execution; the result carries an **egress badge** (cloud + target). (Use a throwaway repo or a dry path.)
  - Result: **SKIP** · Note: Not fired (a real gh issue-create against a throwaway repo left for the owner's glass run); the propose->approve gate shape verified on the slack/webhook paths.

- [x] **T2-15 · Slack send.** Set `slack_webhook_url` (a real incoming webhook, or observe the refusal when empty). `POST /api/meetings/<id>/export/slack`.
  - Expect: with a URL set, the digest/draft posts to Slack via propose→approve→execute; the egress badge shows `cloud` + the Slack target; the URL never appears in any stored payload.
  - Result: **PASS** · Note: Against a LOCAL receiver: propose (preview = exact message) -> approve -> the receiver got ONE POST byte-equal to the preview; the webhook URL appears in NO stored payload (credential joined at execute time).

- [x] **T2-16 · Webhook host allowlist.** Enable `webhook_post_actuator`, set `webhook_allowed_hosts=["example.com"]`, try a proposal targeting a non-listed host.
  - Expect: the unlisted host is **refused**; a listed host is allowed.
  - Result: **PASS** · Note: webhook_allowed_hosts=[example.com]: propose lands, approval ends status=failed (execution refused for the unlisted 127.0.0.1 host); the receiver got nothing.

### 2.6 Dictation — real voice, grounded rewriting

> Render the dictation clips (P-04). Feed each clip through the real pipeline
> (hotkey capture, or the dictation runtime against the rendered audio). The
> rewriter must ground in the repo's `.hs/` + KB. Run from inside each repo dir
> so project detection fires.

- [x] **T2-17 · ledgerline grounding.** Speak/feed `dictation-ledgerline` from `dogfood/repos/ledgerline`.
  - Expect: rewrite names concrete files (e.g. `src/ledgerline/api/charges.py`, `db/idempotency.py`), adds an **acceptance-criteria checklist**, and calls out the **append-only / double-entry / idempotency** invariants from `.hs/memory.md`. Not a paraphrase.
  - Result: **PASS** · Note: THE product thesis on real metal: rambling speech -> a precise brief citing LL-118, tests/test_ledger.py named, an acceptance-criteria checklist, and ALL FOUR .hs/memory.md invariants (append-only, sum-to-zero double-entry, integer minor units, idempotency replay). Intent matched agent_task_buildout at 0.95 via .43. Required fixing F-09 first (setup.sh omitted project-rewriter from stages).

- [x] **T2-18 · questline grounding.** From `dogfood/repos/questline`, feed `dictation-questline`.
  - Expect: rewrite requires a **feature flag** + a **tracked event**, references the north-star **WAQC**, names files (`src/server/streaks.ts`, etc.).
  - Result: **PASS** · Note: Feature flag (flag.sharedQuestsV1 in src/lib/flags.ts) + tracked event (shared_quest_invited via src/lib/analytics.ts) + WAQC named. Block match missed (phrasing outside examples) - the rewriter grounded via .hs regardless.

- [x] **T2-19 · pylon grounding.** From `dogfood/repos/pylon-infra`, feed `dictation-pylon`.
  - Expect: rewrite requires a **rollback plan**, honors **progressive rollout** + **no manual kubectl**, names files (`terraform/cluster.tf`, runbooks).
  - Result: **PASS** · Note: Matched infra_change_buildout 0.95; rollback plan + progressive rollout + no-manual-kubectl invariants + terraform/main.tf, cluster.tf, runbooks/cert-renewal.md named; acceptance checklist.

- [x] **T2-20 · Multi-pass + latency budget.** With `rewrite_passes=2`: dry-run a ledgerline task and watch timing.
  - Expect: the 2-pass output is tighter than 1-pass; the latency budget is respected (stages abort if over `max_total_latency_ms`).
  - Result: **PASS** · Note: rewrite_passes=2 with the 8000ms budget: pass 1 ran 4.5s, pass 2/2 honestly SKIPPED ('would exceed 8000ms budget') and surfaced as a pipeline warning.

- [x] **T2-21 · LLM target detection.** With `target_detect_llm_enabled=true`, dictate an ambiguous target.
  - Expect: low-confidence heuristic targets get re-classified by the LLM; final target is sensible.
  - Result: **PARTIAL** · Note: target_detect_llm_enabled=true: the stage ran but target_profile came back id=unknown source=none - no candidate target profiles exist in the sandbox to classify into; ambiguous-target reclassification unobserved.

### 2.7 Languages

- [x] **T2-22 · German dictation.** Set `model.language="de"`, feed `dictation-german` (voice Anna).
  - Expect: clean German transcription. Then set `model.language="auto"` and confirm it still works (the one-knob behavior). Compare quality.
  - Result: **PASS** · Note: language=de: near-perfect German ('Edempotenzschluessel' the only wobble). language=auto on a second German clip: still correctly German (the one-knob behavior). Ground truths matched.

- [x] **T2-23 · Spoken symbols in real speech.** Feed `dictation-spoken-symbols` (real audio).
  - Expect: after transcription, "at sign"→`@`, "hash"→`#`, "dash"→`-`, "percent sign"→`%`, with correct spacing per attach mode.
  - Result: **SKIP** · Note: Spoken symbols live on the CAPTURE paths (post-Whisper dictation_capture/wake_glue/device_glue) by design - meeting import is verbatim (the Phase-59 rule) and the dry-run is text-in. Needs the live mic path: the owner's glass.

### 2.8 The learning loop

- [x] **T2-24 · Correct in the moment → it learns.** Dictate a ledgerline task, correct the result in the UI (e.g. fix a term), then dictate a similar task.
  - Expect: the correction persists (`dictation_corrections`); the later, similar dictation shows a **"learned from N similar"** chip; the learning digest reflects it. Honest at N=0.
  - Result: **PASS** · Note: Journal entry corrected (kind=intent, value=agent_task_buildout) -> taught=true similar=3; a similar utterance's dry-run returned learning.matched=true 'learned from 3 similar'; the digest flipped (corrections_made=1, similar_nudged=4). The whole visible loop, live.

### 2.9 Wake word

- [x] **T2-25 · Arms, doesn't type.** Enable `wake_word` (preview), say the wake word then a phrase.
  - Expect: it **arms** an 8s window and shows a preview (does not auto-type); one-shot "Type it" types it once and won't re-type. Ordinary speech does not false-accept.
  - Result: **SKIP** · Note: Wake word needs the live mic loop (openWakeWord on stream) - the owner's glass. The wake settings section verified in T1-12's screenshot.

### 2.10 Activity intelligence + pre-briefing

- [x] **T2-26 · Connectors + nudges.** On `/activity`: configure a connector (GitHub/JIRA preview/run), generate nudges.
  - Expect: source-cited nudge cards appear; dismiss + select work; `/api/activity/briefing` returns nudges + artifacts before a meeting.
  - Result: **PARTIAL** · Note: Privacy gate honored (enabled=true required), 2 real ledger records seeded, /api/activity/nudges + /briefing live - but zero nudges computed from the minimal seed (scoring thresholds want richer/near-term activity). Connector configure UI untested headless.

- [x] **T2-27 · Meeting candidates.** Preview candidates, add one manually, start a meeting from it.
  - Expect: candidate → live meeting; the pre-briefing "dictate with this" loop grounds the meeting in the record.
  - Result: **SKIP** · Note: Meeting candidates + start-from-candidate need the candidate-inference connector's live inputs; deferred to glass.

### 2.11 Presence + Qlippy

- [x] **T2-28 · Presence HUD + Qlippy.** Enable `presence.enabled` (and `presence.mascot`) in `/settings`; open `/presence`; run a meeting/dictation.
  - Expect: the native HUD appears (focus-safe, off by default); cards update live (decision/result/learned/aftercare); Qlippy mascot toggles; a card **Approve** equals the dashboard action (audit parity).
  - Result: **SKIP** · Note: The native HUD would spawn a panel over the owner's active session - not fired headless. /presence page renders (T1-09); the HUD + Qlippy were device-proven in Phases 41/56.

### 2.12 Speakers / diarization

- [x] **T2-29 · Diarization + speaker management.** With `diarization_enabled=true`, import a multi-voice meeting; on `/history` open speakers.
  - Expect: distinct speakers separated; merge/rename works; `cross_meeting_recognition` links the same voice across meetings.
  - Result: **SKIP** · Note: Import does NOT diarize by design (meeting_import.py: live meetings only get speaker labels; imports take --speaker or file speakers). Live-capture diarization is the owner's glass; file-speaker facets proven in T1-10.

---

## 3 · Cross-cutting

- [x] **X-01 · Egress / quiet-trust badges.** Across the run, confirm the badge scope is correct: `local` (no LLM), `local+cloud` (intel on `.43`), `cloud + target` (Slack/GitHub). No privacy prose paragraphs — just the badge.
  - Result: **PASS** · Note: Observed across the run: the settings header chip read 'Configured endpoint' under cloud intel (screenshot); the cadence audit egress scope=local; the slack proposal preview carried the send text with the URL never stored. No privacy prose anywhere.

- [x] **X-02 · Export formats everywhere.** Export a meeting as `txt`, `markdown`, `json`, `srt` (CLI + UI).
  - Expect: all four are well-formed and contain transcript + artifacts.
  - Result: **PARTIAL** · Note: CLI exports json/markdown/txt well-formed with transcript (+facets). srt is NOT an --export choice (F-12); UI export not driven.

- [x] **X-03 · Schema / version safety.** `hs doctor` reports `config_version`; `hs backup` before any migration; `hs restore` recovers. Try opening the sandbox DB note the refuse-newer behavior if you bump the version.
  - Result: **PASS** · Note: doctor reports config schema state; backup/restore round-trip proven in T1-03 (restore even banks the pre-restore DB); the desktop refuse-newer matrix is test-locked (Phase 50) - not re-provoked here.

- [x] **X-04 · First-run journey (fresh sandbox).** `rm -rf dogfood/_home && dogfood/setup.sh`, then `hs web`: the `/welcome` wizard fronts the dashboard; walk it; complete a first dictation.
  - Expect: guided fresh-clone → verified first dictation with zero file edits; the `first_run` milestone flips and `/welcome` stops fronting.
  - Result: **PARTIAL** · Note: Fresh sandbox: / fronts /welcome with the wizard ('Hold a key. Speak. Watch it type.'); FIRST_DICTATION_SUCCESS flip releases / to the desk. The actual spoken first dictation is the owner's mic moment.

- [x] **X-05 · It stayed isolated.** Confirm your real `~/.config/holdspeak` and `~/.local/share/holdspeak` were never touched during the run.
  - Expect: all writes landed under `dogfood/_home`.
  - Result: **PASS** · Note: Real ~/.config/holdspeak untouched (mtime Jun 20); real DB's only write (23:12:59) predates this run and belongs to the owner's own launchd instance; every run write landed under dogfood/_home.

---

## Tier C · The Cadence Engine (CAD-8)

> Off by default. These run under the isolated sandbox `hs` (HOME=`dogfood/_home`).
> No external side effect should ever occur without an approval.

- [x] **C-01 · Projection.** Import a meeting (or run a `say`-pipeline scenario), then
  `hs cadence run-now`.
  - Expect: open loops are projected from the meeting's action items + any pending
    proposals, scored, ordered by staleness; re-running does not duplicate them.
  - Result: **PASS** · Note: run-now projected 28 open loops from the run's meetings' action items; the second tick did not duplicate (28 stable).

- [x] **C-02 · Brief + closeout.** `hs cadence brief` then `hs cadence closeout`.
  - Expect: the brief leads with the single highest-leverage move; the closeout lists
    every open loop with a recommended close/file/snooze/kill/delegate.
  - Result: **PASS** · Note: brief: 'Nothing pressing - loops clear' (0 due now, honest) with 28 open; closeout lists all 28 with recommended review decisions.

- [x] **C-03 · Decisions stick.** Kill a loop, then `hs cadence run-now` again.
  - Expect: the killed loop does NOT reappear.
  - Result: **PASS** · Note: Killed a loop via /api/cadence/closeout/apply (applied=1, egress local); after run-now the killed loop stays gone (open 27) and is absent from the list.

- [x] **C-04 · No egress.** `hs cadence audit --out /tmp/audit.json`.
  - Expect: `egress.scope = "local"`; the audit JSON is complete (loops + nudges +
    policies) and nothing left the machine during C-01..C-03.
  - Result: **PASS** · Note: audit --out: egress.scope=local ('Local audit'), complete JSON (loops+nudges+policies); nothing left the machine during C-01..03.

- [x] **C-05 · Master off-switch.** With `cadence.enabled = false` (default), start
  `hs web`.
  - Expect: no cadence thread runs; the runtime is byte-identical to a build without
    cadence; the read commands still work on demand.
  - Result: **PASS** · Note: cadence.enabled stayed default-false the whole run; no cadence thread in the web runtime; every read command worked on demand.

---

## Findings

Log every `FAIL` / `PARTIAL` here as you go, so the run produces a worklist:

| # | Check | Severity | What happened | Repro | Filed? |
|---|-------|----------|---------------|-------|--------|
| F-01 | T1-09 | LOW | `/theater/theaterorb.png` 404s on every theater-bearing page (one systemic broken sprite path in the shipped bundle) | open any page, watch the console | finding logged (this run) |
| F-02 | T1-14 | HARNESS | the mock repos shipped without `.holdspeak/blocks.yaml`, so the blocks checks could never pass | — | **fixed in this run** (all 3 repos gained believable blocks) |
| F-03 | T1-14 | LOW | `dictation blocks ls/show` don't cwd-detect the project (dry-run does); they need `--project .` | `cd dogfood/repos/ledgerline && hs dictation blocks ls` | finding logged |
| F-04 | T1-19 | LOW | `hs import` exits 0 after an unsupported-format error | `hs import dogfood/PROTOCOL.md; echo $?` | finding logged |
| F-05 | T2-01..06, T2-09/10/12 | **MED-HIGH** | imported meetings never receive typed plugin artifacts: import + deferred intel run base analyze only; `--reroute` records the route but executes no chain; the plugin host runs only on live windows. Half of Tier 2's expectations rest on a capability the import path doesn't have | import any meeting, check `/artifacts` + `/plugin-runs` (both empty) | finding logged — candidate follow-on phase |
| F-06 | P-01 | HARNESS | `env.sh`/`setup.sh` default intel model names the retired `Qwen3.5-9B-UD-Q6_K_XL.gguf`; `.43` now serves Qwythos | — | noted (override via `DOGFOOD_INTEL_MODEL` works; default left for the owner's call) |
| F-07 | T1-15 | LOW | with `pipeline.enabled=false` (tier 1), dry-runs never journal even though `journal_enabled=true`; tier-2 journals fine | tier-1 config, dry-run, check `dictation_journal` | finding logged |
| F-08 | T2-17 | LOW (docs) | the canonical blocks example pairs `mode: append` with a `{raw_text}` template, which doubles the utterance (append = original + rendered); `replace` is the right mode for such templates | author a block per the test example, dry-run | finding logged |
| F-09 | T2-17 | HARNESS | `setup.sh` omitted `project-rewriter` from the pipeline stages — NO grounded rewriting could ever run in the harness | — | **fixed in this run** |
| F-10 | T2-18 (accidental) | MED | the project-rewriter, handed an EMPTY utterance, fabricated a complete plausible task from `.hs` context alone (questline); no empty-input guard | `hs dictation dry-run ""` from a repo with `.hs/` | finding logged |
| F-11 | T2-13 | PROTOCOL | the check expects `allow_actuators=false` to block execution, but the aftercare Slack decision route executes on approval BY RATIFIED DESIGN (Phase 61: the approval is the gate); the check should test the UI button posture | — | protocol wording to fix on the next revision |
| F-12 | X-02 | LOW | `history --export` offers json/txt/markdown; the protocol also expects `srt` | `hs history <id> --export srt` | finding logged |
|   |       |          |               |       |        |

---

## Addendum — 2026-07-04, same day: F-05 fixed (Phase 80) and re-proven

Phase 80 (`holdspeak/meeting_plugins.py`) landed the persisted-meeting plugin-run
seam the same day this run filed F-05. Re-driven on the same sandbox, same real
`.43` endpoint:

- **T2-01 re-scored PASS.** A fresh import of the arch review under
  `mir_profile=architect` produced **4 typed artifacts**: an accepted **ADR**
  ("Adopt Event Sourced Write Log with Hourly Balance Snapshots", real context +
  options weighed), a **mermaid** flowchart of the discussed system, extracted
  **requirements**, and a project association — with all plugin runs `success`
  and the honest status detail ("…4 artifact(s) from the routed plugin chain").
- **T2-07 re-scored PASS (the artifact half).** `hs intel --reroute <id>
  --profile delivery` now EXECUTES: `executed: true`, the delivery chain
  (action_owner_enforcer / milestone_planner / dependency_mapper / …) all
  `success`, artifacts grew to 10 — the profile visibly changes the artifact set.
- **T2-02..06, T2-09, T2-10, T2-12's decisions lane:** unblocked by the same
  mechanism (one representative re-driven above; not individually re-scored here).
- The router-off path stays byte-identical (test-locked), and unchanged reruns
  dedup instead of stacking (DB-backed idempotency on the window's
  transcript hash).

---

## Addendum — 2026-07-04, same day: the small-findings sweep (Phase 81)

Phase 81 closed the remaining code findings and the docs/harness/protocol rows,
each fix landed with tests where the finding was filed:

- **F-10 CLOSED (was MED).** `ProjectRewriter.run` now no-ops
  (`reason="empty_input"`) before any model call when the latest text is empty
  or whitespace — an empty utterance can no longer be dressed up as a task from
  `.hs` context alone. Tests pin that the runtime is never invoked.
- **F-07 CLOSED — T1-15 re-scored PASS (web + live paths).** The journal now
  follows `journal_enabled`, not the pipeline gate: a pipeline-off dictation or
  web dry-run records a passthrough row (no stages, final = transcript, warning
  "dictation pipeline disabled") and the dry-run returns its `journal_id`. The
  CLI dry-run remains a stateless debugger by design (it has no DB wiring under
  ANY tier) — the protocol's journal checks should drive the web dry-run.
- **F-01 CLOSED.** The theater orb was referenced root-relative while the Astro
  base is `/_built`; fixed and live-proven (200, 6,350B; the old path 404s). No
  other un-prefixed asset reference exists in `web/src`.
- **F-03 CLOSED.** `blocks ls/show` cwd-detect the project via the same
  `detect_project_for_cwd()` dry-run uses; `--project` still wins.
- **F-04 re-scored: NOT REPRODUCIBLE.** The exact recorded repro
  (`dogfood/hs import dogfood/PROTOCOL.md; echo $?`) exits **1** at survey; the
  exit path (`main.py` `raise SystemExit(run_import_command(args))`) predates
  this run. Locked with a test so a real regression is caught.
- **F-12 CLOSED.** `history --export srt` ships; `Speaker: text` cue bodies
  round-trip through the real transcript parser (speakers + timestamps intact),
  proven on this sandbox's own imported meeting.
- **F-08 CLOSED (docs).** The DIR-01 plan's canonical blocks examples paired
  `mode: append` with templates starting `{raw_text}` (append re-emits the
  original first — the utterance doubled). Both examples now use `replace`,
  with the mode semantics stated inline. The shipped starter templates were
  already correct.
- **F-06 CLOSED (harness).** `dogfood/env.sh` + `setup.sh` default intel model
  is now the live `Qwythos-9B-Claude-Mythos-5-1M-Q6_K.gguf`.
- **F-11 CLOSED (protocol).** T2-13 reworded to the ratified aftercare design:
  approval IS the execution gate on that route; the check now targets un-gated
  execute affordances, not approval-executes.

Ledger state after Phases 80 + 81: **every row closed or re-scored.** F-02 and
F-09's harness gaps were already fixed in-run.
