# Terms

- **posting** — writing the two balanced rows for one money movement.
- **reversal** — a new entry that negates a prior one (we never delete).
- **idempotency key** — client-supplied id that makes a write replay-safe.
- **reconcile** — match ledger entries against the processor settlement file.
- **minor units** — integer cents; the only money representation in the code.
