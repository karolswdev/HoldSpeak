// HS-135-04 — L10 sparse-surface rule: below this item count, filter
// chrome hides, zero-value metric tiles collapse, verbs REMAIN, the
// empty well speaks.  JS constant (conditional rendering, not styling).
export const SPARSE_THRESHOLD = 5;
