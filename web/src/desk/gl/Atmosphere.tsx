/** HS-110-05 — the quiet workbench. The animated spotlight pulse is gone;
 * the base gradient stays as a settled dark surface. */
export function Atmosphere() {
  return (
    <div className="desk-stage" aria-hidden="true" />
  );
}
