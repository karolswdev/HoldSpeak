title: Cloud egress selected — named endpoint, execution still closed
description: >
  The explicit cloud-treatment posture for UAT. A recipe assigns a real, named
  OpenAI-compatible summary route, but no model call is made while staging this
  deck. The actuator master switch stays closed, and the recipe creates only an
  unapproved proposal, so applying this deck cannot egress.
requires: []
config:
  config_version: 1
  model:
    name: base
    warm_on_start: false
  meeting:
    intel_enabled: false
    allow_actuators: false
