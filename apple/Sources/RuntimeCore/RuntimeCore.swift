import Foundation
import Contracts

/// Layer 2 — the meeting/artifact/MIR engines, persistence, settings, sync.
/// No UI. Placeholder for Phase 1; the engines land in Phases 5–7/10.
public enum RuntimeCore {
    public static let layer = "runtime-core"
}
