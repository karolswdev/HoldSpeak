# Demo — GitHub write-connector loop (HS-38-06)

Generated by `demo_write_loop.py` — an injected `gh` runner (no real GitHub call).

The actuator proposes a GitHub issue for the first **unowned** action item; the
connector runs `gh issue create` (and only that) behind `shell:exec` + the manifest.

## 1. Propose (the actuator never acts)

- status: **proposed**
- preview: Open a GitHub issue in acme/app: “Follow up: Wire the staging smoke test”
- payload: `{'body': 'Raised in **Release planning** with no owner assigned.\n\n- **Task:** Wire the staging smoke test\n- **Owner:** _unassigned_\n- **Due:** _not stated_', 'repo': 'acme/app', 'title': 'Follow up: Wire the staging smoke test'}`
- `gh` calls so far: **0**

## 2. Execute BEFORE approval → refused, no egress

- raised `ActuatorExecutionError`: proposal c01b11797f2841e1850cdaa975e38259 is 'proposed', not 'approved' — refusing to execute
- `gh` calls: **0** (unchanged)

## 3. A non-allow-listed op (`gh repo delete`) → refused BEFORE egress

- raised `ConnectorOperationRefused`: connector 'github_issue_writer' refused operation [subprocess: gh repo delete acme/app]: operation is not on the connector's permission manifest
- runner calls: **0** (the gate was never reached)

## 4. Approve → execute → executed + result

- the one `gh` argv: `['gh', 'issue', 'create', '--repo', 'acme/app', '--title', 'Follow up: Wire the staging smoke test', '--body', 'Raised in **Release planning** with no owner assigned.\n\n- **Task:** Wire the staging smoke test\n- **Owner:** _unassigned_\n- **Due:** _not stated_']`
- status: **executed**
- result: `{'issue': 128, 'url': 'https://github.com/acme/app/issues/128'}`

## 5. The audit trail (every transition recorded)

| # | → status | actor | detail |
|---|---|---|---|
| 1 | proposed | system | proposal recorded |
| 2 | approved | karol |  |
| 3 | executed | executor | executed via connector; payload 17f9e6dcad49 |

**Lifecycle:** proposed → approved → executed
**Total real `gh` calls in this demo:** 1 (exactly one — the executed issue).
