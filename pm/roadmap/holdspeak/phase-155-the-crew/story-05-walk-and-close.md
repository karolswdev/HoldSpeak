# HS-155-05 - The walk and the close — the arc closes

- **Project:** holdspeak
- **Phase:** 155
- **Status:** backlog
- **Depends on:** HS-155-01, HS-155-02, HS-155-03, HS-155-04
- **Unblocks:** -
- **Owner:** unassigned

## Problem

The Crew — and with it the whole Desk Chat port (DC-01..05) — is
claimed only when walked on real metal and glass, docs touched, counsel
heard (settled design D5).

## Scope

- **In:** metal `assets/story-05-metal.py` on `.43` (the default-grammar
  law applies): the parent delegates a real evidence-read task
  (`chat.subthread` under safe mode → pending box → allow), the child
  runs on the conductor, the notification returns, the parent's next
  turn uses it; the fence leg (a People-touching child under a cloud
  parent override → withheld). Glass
  `tests/e2e/test_hs155_crew_glass.py` at 1440 + 393; shots →
  `assets/story-05-shots/`; the ARC exhibit (151..155, five rooms).
  Docs: README / USER_GUIDE / MCP_SIDECAR (tool counts — `len(TOOLS)`),
  the Desk Chat plan §6.9 marked shipped and the plan's status ledger
  closed out. Close counsel; honest sweep (name-diff vs main; web
  baseline); the warpdrv grep unchanged (plan + phase records only).
- **Out:** anything beyond DC-05.

## Acceptance criteria

- [ ] Metal legs PASS on `.43`, payloads kept; the fence leg proves no laundering.
- [ ] Glass both widths, zero overflow; the arc exhibit link in the evidence.
- [ ] Counsel zero open must-fix; docs + the plan closed out; sweep name-diff clean.

## Test plan

- **Unit:** the full scoped 155 set + the 153/154 regression sets.
- **Integration:** the glass file; the metal script.
- **Manual / device:** the owner's attended leg holds the merge word for the arc.

## Notes / open questions

- This story closes the port RFC — say so in the plan's §17-style ledger.
