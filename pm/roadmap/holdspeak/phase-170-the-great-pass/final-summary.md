# Phase 170 — The Great Pass: final summary (DRAFT — closes on his word)

## What shipped

- **The face canon (01):** docs/internal/UX-CANON.md — the owner's
  thirteen rulings, the species list, the type steps, the grammar, the
  review protocol — linked from CLAUDE.md as source canon. The census:
  17 surfaces × 2 widths shot; 671 canon violations located, ranked by
  Tuesday weight (assets/census/); re-shot after the sweep.
- **The species sweep (02):** three Fable lanes by file ownership paid
  the tree from 671 to 151 real hits — raw `<button>` 147 → 4
  (allowlisted with reasons), accent rails 8 → 0, unnamed egress 1 → 0,
  emoji 112 → 21; `countToken`/`countLabel` in the library; the retired
  setup wizard parked. **The ratchet** (`tests/unit/test_ux_canon_ratchet.py`
  + `tests/ux_canon_ceiling.json`): per rule AND per face, hard zeros on
  rails and egress, lowering only by a deliberate command; the
  scanner's own false positives fixed first (property access, JSX
  comments, TypeScript, boolean `.length`).
- **The Concierge (03):** Settings → Models is ONE window titled
  `Models` on the Door's grammar — `N engines found` at the display step,
  FOUND rows (emblem · name by served model · latency/size/runtime/key
  tokens · host chip · state; cloud rows carry an explicit `Check` with
  `1 TOKEN · $`; presets `Download` → a progress token), THE SET's seven
  groups with an in-place picker of ChoiceCards (Speech recognition =
  local Whisper only; `OFF` is a card), `Adjust` unfolding the
  capability table UNDER the set with a host per row, the portaled
  footer `7 GROUPS · 4 ENGINES · 1 WAITING · Cancel · Use these` — `Use
  these` disabled while any group WAITS. The wire:
  `/api/concierge/detect|propose|probe|apply|download` + MCP family;
  detect never generates on a cloud key; apply refuses 409 beside
  WAITING; per-group CAS documented (atomic write backlogged). Six
  boards, counsel RATIFY-W-C paid; the legacy Models/Assignments modules
  parked.
- **The four Tuesday faces (04):** the arrival (one headline `N need
  you across M projects` / `Nothing needs you`; NEXT; NEEDS YOU ·
  THOUGHTS · BRIEF · MEETINGS as sections absent at zero; the capture
  bar at the foot; the Chair hero and lanes PARKED), the Settings hub
  (seven ledger rows with state tokens from `/api/settings/hub`; the
  posture said once; `No default model` in the warning tone), Speak
  (transport · well with its mic · LANDS IN · RESULT with the teach row
  in place · ENGINE by served model with its real host · Details fold;
  the register strip under the fold), Meetings (the list as a stream —
  the state carries its verb, `Run intelligence` only with words, `NO
  TRANSCRIPT` never `0 SEG`, dead captures `INTERRUPTED`; the detail
  split with NEEDS YOU rows absent at zero and speaker tokens). Twelve
  boards, counsel RATIFY-W-C paid. The wire: `POST /api/meetings/{id}/
  intelligence/run` (+ `transcriptWords`), `GET /api/desk/needs-you`,
  `GET /api/settings/hub`; MCP 197 tools / 35 families.
- **The docs (06):** README, USER_GUIDE, POSITIONING's canonical names
  (the Concierge, the arrival, Speak, Meetings, needs you, Run
  intelligence), UX-CANON's ratchet paragraph, the sidecar prose —
  written to the ratified designs, then every build-dependent sentence
  re-verified against the built faces.
- **The walk (05):** `tests/e2e/live170_walk.py` pre-walked the owner's
  real desk twice (ten shots, a facts ledger, a Defects section). His
  own walk and his word: OWED (below).

## Found live, paid in the phase

- The arrival's BRIEF flooded with 1837 kernel-operation names as rows
  (`PrimitiveService.delete_directory` · Ack · Defer) — the Monday
  brief's untriaged ledger. Human items only, capped at 3 + `N more`;
  the source fix backlogged to 171-06.
- Four recordings dead since AUG 11 still wore `REC` + `Retry` —
  `INTERRUPTED` now; a live capture (ended_at null, started within 6 h)
  keeps `REC`.
- The dictation engine named `Migrated intel endpoint` (a 143 migration
  label) with a `THIS DEVICE`, then `CLOUD`, chip on a LAN host —
  detect names engines by served model and classifies private hosts as
  LAN; Speak's ENGINE row uses the same resolver.
- Parking the Chair board left the 144 Door's action items (`/api/door`:
  overdue · now · waiting · unassigned) with no face — found by the old
  144 rig failing, not by the new rigs passing. They join the arrival's
  NEEDS YOU by addendum. The lost-doors audit of every parked component
  then found four more (Schedule recording; the armed countdown's
  Cancel; the Door items' inline lawful verbs; Connect calendar) — all
  re-homed on the arrival by addendum; three demotions named (inline
  compose → Speak; lane pagination → the Intelligence brief; the setup
  wizard's JQL editor → post-creation Watch configuration).
- Fourteen glass rigs never rebuilt the web bundle (the 163 law reached
  19 of 33) — `tests/e2e/conftest.py` makes build-first universal.
- The Room rig's history seeds landed on yesterday between 00:00 and
  05:00 (a midnight flake); the Room rig's unscoped ledger-row locator
  picked the arrival's rows once the arrival rendered rows first.
- The second `MeetingSummary` class (db/models/__init__.py) missed
  `transcript_words`; the archive's watch pause moved into the
  sanctioned repo module inside the same transaction.
- The Phase 143 censuses learn the wire; the tool classes learn the
  eight new MCP tools; the api-surface consumers regenerate after the
  parkings.

## Counsel on the built phase (2026-09-05 03:20): RATIFY-W-C

M-1 raw buttons in the picker · M-2 the BRIEF's `Generate` door · M-3 the
AGENTS section when sessions exist · S-1 partial apply on the face ·
S-2/S-3 the host on `Run intelligence` (Article III) · S-4 sleep waits in
rigs · S-5 `Decide`/`Dismiss` per the board · N-1/N-2/N-3 code-to-board
fidelity and two dead-end navigations · the Concierge's Cancel closing
the wrong window. **Status: being paid** (this section flips to PAID
with the commit list before the PR).

## The suite

- Full suite, CI shape (-n auto): PENDING the conditions' last commit —
  the last run before the faces: 9392 passed / 9 failed = 6 inherited or
  environment-bound (zero diff vs main in every file involved) + 3
  xdist-only (green serially).
- vitest 2018 green after the parkings; web baseline zero branch-new;
  the ratchet + 57 guards green; the five hs170 rigs + the 169 rigs +
  the census rig green serially.

## What the owner owes the phase

1. His word on the canvas
   (https://claude.ai/code/artifact/3fc26e25-1d5f-4796-b2e9-0d4bae9bff20):
   the Concierge page, the four-faces page and the N.
2. His walk on his desk of the five built faces (the runner's shots
   under assets/story-05-shots/ are the pre-walk).
3. His word on the merge (the decision of 02:00: faces built under the
   standing goal; the word moved from before-build to before-merge).
4. His attended walk of the 169 Door and Room — still owed.

## After 170

Phases 171–180 are chartered as PLANNED (his questions per phase in
docs/internal/project-rooms/HANDOVER-MUADDIB.md §1.4). 171 The Heartbeat
activates when this phase merges.
