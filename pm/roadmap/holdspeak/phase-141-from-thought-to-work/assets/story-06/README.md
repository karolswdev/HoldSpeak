# HS-141-06 owner-glass walk

These frames came from a bare `MeetingWebServer` against a fresh temporary HOME
and database. The walk used no seed or populate fixture. It typed through the
ordinary first-value surface, kept the Note, adopted it with **Develop this
thought**, changed the working body, and pressed the then-current completion
label inside the
450 ms save-debounce window.

The walk asserted the final accepted body before accepting Done. It then stopped
the hub, reopened the same database under a new server instance, opened a fresh
browser context, found the completed Note through the real Floor List, resumed
it, and completed it again. Both 1440×900 and 393×900 asserted exact viewport
widths. The final run produced zero console or page errors.

- `good-enough-done-1440.png` — immediate Done with the final body;
- `completed-reload-1440.png` — completed Note found after restart;
- `resume-refining-1440.png` — explicit lifecycle resume;
- `completed-reopen-settled-393.png` — settled phone sheet, one primary;
- `working-resumed-settled-393.png` — resumed phone editor, one primary.
