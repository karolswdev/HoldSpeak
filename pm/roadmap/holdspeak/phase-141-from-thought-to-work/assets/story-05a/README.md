# HS-141-05A owner glass — default AI context

Fresh isolated HOME/database walks used the real browser, HTTP routes, shared
application service, local default-policy ledger, Thought attachment ledger,
restart-safe projections, and MCP Thought family. The labelled no-dispatch
engine remained at zero calls: configuring, applying, removing, stopping, and
repairing default context never invoked a model.

| State | 1440×900 | 393×900 |
|---|---|---|
| Existing picker: complete current/future sets before **Use these by default** | [set default](./hs-141-05a-set-default-1440.png) | [set default](./hs-141-05a-set-default-393.png) |
| Later local Thought: authoritative Everyday context + Project launch attached at birth, both marked **Default** | [born with default](./hs-141-05a-born-default-1440.png) | [born with default](./hs-141-05a-born-default-393.png) |
| Existing default-born Thought after source edit: named stale explanation and **Update context** as the sole state primary | [stale repair](./hs-141-05a-stale-1440.png) | [stale repair](./hs-141-05a-stale-393.png) |
| Invalid later birth: `AI context None`, named whole-set skip, unavailable last-known selection, and **Stop using by default** | [not applied](./hs-141-05a-not-applied-1440.png) | [not applied](./hs-141-05a-not-applied-393.png) |

The walk also completed the successful Update after each stale screenshot,
proved per-Thought removal left the future set unchanged, stopped the complete
future set without changing the open Thought, replaced it again through MCP,
and captured only refs/revisions in default-policy browser commands. Both widths
had zero page/console errors, zero horizontal overflow, and at most one visible
primary action. At 393px search did not steal focus.

## Rulings and test ledger

- **Technical counsel — RATIFY.** Full policy/action/application ledger
  validation, atomic create/adopt, independent hash-bound failure attribution,
  strict HTTP/MCP schemas, restart/replay, race, privacy, authority, and no-sync
  invariants passed after the final blockers were closed.
- **Owner-glass counsel — RATIFY.** The current/future scope distinction,
  persistent Default marker, future-only effect, named whole-set fail-open, and
  stale repair hierarchy remained legible at both widths without another
  settings surface.

```text
focused context + schema: 55 passed
broad refinement/MCP/HTTP/sync + schema: 138 passed
tests/e2e/test_hs14105a_default_context_glass.py: 2 passed
```

The executable source of the eight screenshots is
[`tests/e2e/test_hs14105a_default_context_glass.py`](../../../../../../tests/e2e/test_hs14105a_default_context_glass.py).
