# Phase 108 - The Locked Room - owner sitting

## One machine command

```bash
uv run --extra test python scripts/phase108_closeout_beats.py
```

It prints eight PASS/FAIL beats. B2 briefly opens a blank TextEdit
document, types a random `HS108_CONFINED_DESKTOP_*` marker through the
spawned warrant executor, verifies both receipts, and closes without
saving.

Latest machine preflight: **8/8** in one session; 4,382 Python tests,
373 web tests, live bus 3/3, and production typecheck/build passed with
no deselection. The real desktop operation was
`op_b73ba81d0fdc41c98a0ed092d4c74681`.

## What to inspect

| Beat | Owner-level question |
|---|---|
| B1 | Do forgery, replay, payload swap, expiry, policy drift, malformed shape, and focus change all stop before raw input—and does a timeout permanently close the desynchronized pipe? |
| B2 | Does a real desktop effect still land through the new child and leave both receipts? |
| B3 | Do both text and key steering enter `process.input@1`, including named refusal? |
| B4 | Can an unauthenticated caller reach `gh`, `jira`, or `dw`? It must not. |
| B5 | Does never-claimed end refused, while claimed silence ends indeterminate and never retries? |
| B6 | Does the real built web runtime replay the browser bus with Chromium, with zero skips? |
| B7 | Is the debt register truly empty while every former statement remains pinned? |
| B8 | Are the complete Python and web sweeps green, or are any unrelated failures named honestly? |

## The visible hand-check

1. Watch B2 open and close TextEdit. The marker should appear once.
2. In a disposable tmux pane, use the Desk to send ordinary text and one
   harmless key such as Left. Confirm each act appears once and its process
   row ends.
3. Move focus away during one desktop attempt if you want the crown case:
   it must refuse `desktop_focus_generation_changed`, spend the warrant,
   and never type after focus returns.
4. Read `holdspeak/kernel/effect_ledger.json`: `sites` is empty. Read the
   fence output: former sites are still asserted rather than forgotten.

## Held questions

None. Clause 6's wording already ratified its own automatic sunset when
the register reached empty. The sitting judges whether the delivered
implementation earns closeout, not whether an agent may amend that clause.

## Verdict

Recorded verbatim on 2026-07-30:

> 's all's good, moyt.

No blocking defect was raised. See `evidence-story-07.md`.
