# Phase 165 - The MCP Family (P6): final summary

**Exit, verbatim:** a local MCP client can drive the same scenario,
and the product is ready for Gate B partners. **MET:** one real
stdio subprocess client drove §15's ten acceptance points end to
end, x2 deterministic, every number measured into the transcript
artifact. THE OWNER'S VERDICT: "PASS" — Gate B ready, one round.

## The seven stories

1. **The family skeleton** - project.* reads with EQUALITY parity
   against the service returns; five §11.2 resources; MCP-006
   BUILT, not found (the FAMILIES registry crashed whole on one bad
   import; per-family importlib + DEGRADED_FAMILIES now isolates,
   proven through the real assembly).
2. **The command tools** - fourteen thin drivers over the Web
   routes' exact seams; command_id replay proven; the NullCollector
   premise overturned (collect_all is DB-only — the real collector
   composes; open_review true parity); copied-glue register kept.
3. **The driver tools** - steward runs under the immediate-id law
   (MCP-003 slow-phase proof), the durable setup interview,
   provider discovery, the watch graduation with typed
   legacy_watch_boundary refusals (Direction A; the legacy-side
   guard ledgered after counsel's catch); serializers DELEGATE to
   the route's own.
4. **The palette** - the thread palette's allow-list species applied
   at the MCP layer (dispatch_for_palette, typed refusals); MCP-006
   widened to every family; the 57-proof error sweep found zero
   prose errors.
5. **The walk** - the client was built (none existed): subprocess
   stdio JSON-RPC, one fixture seam (the snapshot fetcher file,
   verified by the orchestrator's read). 39 calls/run; 2
   transitions, 5 proposals, 11 receipted steps, room revision
   2->3, same-command replay, no_op re-evaluations. Owner: PASS.
6. **The docs** - MCP_SIDECAR 407->620 lines: the §15 worked
   example from REAL transcript excerpts; the honesty notes stated
   (fetcher seam, one-directional boundary, V0 refusals).
7. **The close** - this document.

## Gates (real numbers)

- Full CI-style suite: **21 failed / 8963 passed / 61 skipped in
  26:50** -> sweep: 8 = main's recorded baseline; 2 proven flakes
  (x2 green, untouched); 1 local-environment known-fail (hs152
  hands glass: Playwright executable resolution, untouched by the
  branch, absent from the CI baseline — ledgered); **10 real, paid
  in-round**: the fifth Mode seed's pin family (six thread_modes +
  two desk-seed sets, honestly updated — the manifest still seeds
  4; seed_modes ensures the 5th), the 157-era characterization test
  rewritten to today's truth, and the tool-gate census — whose
  demand EXPOSED A REAL DEFECT: MCP-only tools leaking into the
  derived Desk/Chase thread palettes; the _thread_side law now bars
  project.*/provider.* at the derivation, with the 55/61 size pins
  holding unchanged as the seal-proof. Zero unexplained.
- Web: baseline **2275 passed, zero branch-new**.
- Counsel: **RATIFY-WITH-CONDITIONS with ZERO M**; four S paid
  in-round (the boundary-docs honesty catch, delta empty-state
  coverage parity, the provider-list import, honest checkboxes);
  eight N ledgered.

## Debts

- New, carried: the legacy-side watch guard (reactions family
  unguarded against graduated rows — wasteful, not destructive);
  the sidecar fetcher seam (_watch_service composes no
  snapshot_fetcher — real-sidecar watch evaluation needs live gh
  auth); no sidecar recover_on_startup (documented design);
  MCP-triggered runs render Manual on the face (honest, noted);
  dispatch_for_palette's production consumer beyond the walk is
  future agent sessions; hs152's local-environment failure; plus
  counsel's remaining N (TOOLS.extend assembly gap, two-step
  command recording, inline resource error shape, item-8 scoping
  note, empty-tools Mode prompt, absent github_adapter in the MCP
  setup service).
- Carried from before (re-listed): 164 N-1..N-5 + the
  scheduled-path trigger wire + per-watch cadence editing; 163
  S-4/N-1/N-3; 160 N-5/N-1/N-2; 158 S-1/N-1/N-3; 159 seeding
  walls; 161 N-1.

## For P7 (The Jira Parity — RATIFIED by the owner)

The owner's word, verbatim: "Yes, I will want Jira parity." §14 P7:
a real Jira provider adapter for site/Project/type/status discovery
and issue search; compile/test/baseline/poll semantic issue Watches;
exit = Jira readiness backed by live discovery/search and the same
no-duplicate Delta/action behavior, never pushed fixtures alone.
Natural riders: the sidecar fetcher-seam fix (a provider-injection
shape serves both gh and Jira), the legacy-side watch guard, the
scheduled-path trigger wire.
