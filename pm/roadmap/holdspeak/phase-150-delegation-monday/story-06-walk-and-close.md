# HS-150-06 — The walk and the close

- **Project:** holdspeak
- **Phase:** 150
- **Status:** ready
- **Depends on:** HS-150-01, HS-150-02, HS-150-03, HS-150-04, HS-150-05
- **Unblocks:** —
- **Owner:** unassigned

## Problem

The exit story: the manager's Monday proven cold, the sweep with
BOTH baselines (pytest + the new web one), the counsel, the
exhibit.

## Scope

### In

- The phase rig (clone the 149 patterns + every rig law): seed
  three reports, map an alias ON A REAL CARD, filter by chip,
  read staleness, generate the brief, assert person_sections in
  the RESPONSE and their ABSENCE in the tables, "Add to 1:1
  agenda" round-trip, the BriefLane first-load act, both widths,
  ×2 + the stamped capture; zero keychain calls by construction.
- The DELEGATION and MONDAY probes re-run verbatim from the audit
  — both must flip PAINFUL/PERSON-BLIND → one-gesture answers,
  with before/after pairs.
- Full sweep DETACHED; pytest baseline + the story-04 web baseline
  both consulted; readable/capture pairing; asset restore
  (dirs 141–150).
- final-summary; close counsel (the persisted-boundary attack is
  its standing question); the exhibit to the owner; the branch
  HOLDS for the shot verdict + merge word.

### Out

- The merge (the owner's word).

## Acceptance criteria

1. The rig green ×2 + capture; the probes flipped with frames.
2. Sweep verdict in BOTH vocabularies, zero unresolved branch-new.
3. Counsel verdict recorded; concerns fixed or ledgered by name.

## Test plan

The rig ×2 + dw capture; the detached sweep; both baselines; the
pair frames.
