# HS-166-07 - The close: gates, debts, final summary

- **Project:** holdspeak
- **Phase:** 166
- **Status:** done
- **Depends on:** HS-166-01..06
- **Unblocks:** post-arc (Gate B partner feedback, MCP-008, the debt ledger, the parked backlog)
- **Owner:** unassigned

## Problem

The eleventh close — and the arc's V0 COMPLETE if it holds. The
liturgy is law; the scars ride.

## Scope

- **In:** full suite CI-style isolated HOME -n auto, totals READ;
  sweep vs the recorded main baseline (27 names @ run 33459107466 —
  three phases merged since; if candidates smell like drift,
  refresh from a fresh main run); candidates proven at root cause;
  web check + baseline; counsel read-only adversarial (the hunt
  named in the charter: a Jira read without the status read-back,
  a derived label dressed as enumerated, a Jira fork of the
  evaluator) with M in-round; churn restored BEFORE staging;
  final-summary with the arc's V0 verdict; PR with the basis; merge
  per the standing posture; debts ledgered.
- **Out:** post-arc work.

## Acceptance criteria

- [x] Full suite READ; sweep zero unexplained; web gates green; counsel zero open must-fix.
- [x] Debt ledger updated; final-summary; COMPLETE 7/7; README true; PR merged.

## Test plan

- **Everything:** this story IS the gate run.

## Trace record (orchestrator round, 2026-09-03)

- Full CI-style suite (isolated HOME, -n auto, two halves): unit
  13 failed / 7879 passed / 6 skipped in 12:53; the rest 6 failed /
  1322 passed / 55 skipped in 8:03 → **19 failed / 9201 passed / 61
  skipped**. Sweep vs main's baseline AT THE BRANCH BASE (23 names
  from run 33697563134 @ 493253d8 — refreshed this session; the old
  27-name list died with the previous scratchpad): 8 = baseline;
  11 branch-new → 9 real, paid in-round; 1 proven flake (the
  inference-runner deadline test, x2 green); 2 = the live walk under
  an isolated HOME (acli unauthorized) — now an honest, COLLECTABLE
  skipif (a module-level skip broke the sweep's collection by node
  id — learned here). Zero unexplained.
- The nine real, paid: the effect census (the acli read-subprocess
  site classified like gh's — the tombstone law); the thread tool
  gate census (the seven new MCP tools classified; provider.* stays
  out of the thread palettes); the command-tool pin (+ the clarify
  twin); the activity connector set (+ the acli pack); the API
  surface manifest + markdown REGENERATED by the generator (06's
  hand-written route list replaced by canon); and the 163 steward
  glass ×2 — the walk's route-level watermark dedup BROKE §9.3's
  same-watermark law (a second manual run IS created and
  RECONCILES; Gate 4's existing-run dedup belongs to the conductor
  drain) — reverted, the helper kept for the drain, the walk now
  measures the 163 contract (replay_new_run + replay_reconciled,
  zero new door items).
- Counsel RATIFY-WITH-CONDITIONS: one M (the palette pin 37→44,
  renamed honestly) + three S paid in-round (the automations read
  model calling a working Jira adapter "unavailable" on the route
  AND its web twin; the auth-status read-back filling a missing
  Site/Email from expectations); seven N ledgered.
- Close capture (the settled tree): candidates 9 passed + 2 honest
  skips; the LIVE walk 2 passed (a 1-of-2 live flake seen once
  between captures — Jira Cloud eventual consistency — passes x2 on
  re-run, ledgered); web baseline zero branch-new (one vitest flake
  seen once, clean on re-run); the flake proof x2.
- The PR is opened on the local gates per the standing posture; the
  MERGE waits for the owner's word on the two galleries (the face
  on the rig; the live walk on his site) — OWNER SEES SHOTS BEFORE
  MERGE.
