# HS-149-06 — The walk and the close

- **Project:** holdspeak
- **Phase:** 149
- **Status:** ready
- **Depends on:** HS-149-01, HS-149-02, HS-149-03, HS-149-04, HS-149-05
- **Unblocks:** —
- **Owner:** unassigned

## Problem

The exit story — and a first: the populated-People walk, fully
headless, through the story-01 seam. The Tuesday probes must flip
from DOES NOT EXIST to EXISTS on glass.

## Scope

### In

- Extend the cold walk (a tenth leg or the audit rig graduated):
  through the 01 seam — set up People headless, create the
  relationship, LINK the series via the real picker, assert the
  rail person chip, PREP → the Prep lens populated, Record this
  beside it (the Tuesday pair on one row), the L2 honesty state,
  zero keychain calls asserted for the whole walk (the spy), both
  widths, run twice + the stamped capture.
- Before/after pairs against audit-walk-shots (the probes'
  DOES-NOT-EXIST shots get their EXISTS afters).
- Full sweep DETACHED; triage vs the pytest baseline AND the three
  known web-unit inherited names (the 148 discovery — this phase's
  web tests must not hide behind them; stash-compare if any of the
  three families are touched).
- final-summary.md; close counsel (fresh opus; the privacy
  boundary is the standing question); the exhibit to the owner.

### Out

- The merge (the owner's word; no pre-given word).

## Acceptance criteria

1. The headless populated walk green twice — zero keychain calls,
   zero GUI dialogs possible by construction.
2. Tuesday probes (a) and (b) EXIST on glass with shots.
3. Sweep verdict recorded with the readable-log pairing;
   baseline-honest.
4. Counsel verdict recorded; concerns fixed or ledgered by name.

## Test plan

The extended walk ×2 + dw capture; the detached sweep; the pair
manifest; the keyring-spy assertion over the walk hub.
