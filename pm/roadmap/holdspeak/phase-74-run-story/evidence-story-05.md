# Evidence — HS-74-05 — Closeout: the run walk

- **Shipped:** 2026-07-02
- **Commit:** this commit (branch `phase-74-run-story`)

## The walk (REAL metal, one session)

Against the real hub + the `.43` endpoint, `location.pathname` asserted
`/` after every beat:

1. **arrive** — the desk, the rail, the zone.
2. **ask → frames → materialize** — "Say the word 'walked' and nothing
   else." → the page hook captured exactly `["running", "ready"]`
   (scope run) → the artifact appeared wearing `is-new`.
3. **open + via lineage** — the body is the model's words; the chip
   reads via Owl.
4. **file into the zone** — Move to… → Answers → the live membership row
   in the DB + "1 item" on the tray (`05-answer-filed.png`).

Zero page errors. The phase ledger ships as
[final-summary.md](./final-summary.md).

## Acceptance criteria — re-checked

- [x] The full loop in one session; the pathname assertion every beat.
- [x] A real-model beat inside the walk.
- [x] The landed artifact files like any primitive.
- [x] final-summary.md; the phase closes 5/5; the PR merges on a
      CONCLUSION-CHECKED green.
