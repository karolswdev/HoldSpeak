# HS-74-05 — Closeout: the run walk

- **Status:** todo
- **Severity:** HIGH
- **Depends on:** HS-74-01..04

## What

One continuous session on `/`: ask from the rail → the theater plays →
the artifact materializes → open it → `via` lineage → file it into a
zone. Pathname never leaves `/`. final-summary.md; the phase closes;
PR to main merged on a CONCLUSION-CHECKED green (the Phase-73 incident's
lesson applied mechanically: check conclusions, never chain merge after
a checks watch).
