# HS-148-06 — The walk and the close

- **Project:** holdspeak
- **Phase:** 148
- **Status:** ready
- **Depends on:** HS-148-02, HS-148-03, HS-148-04, HS-148-05
- **Unblocks:** —
- **Owner:** unassigned

## Problem

The exit story: prove the reforged menus cold on the real product,
sweep, and close honestly.

## Scope

### In

- Extend the cold walk's menu coverage: the existing Go legs
  (door_walk_hs144.py:705-710, 1018-1029) get grammar assertions
  (glyph lane present under the shipped variant, keycap well
  rendered, ghosted item stippled-not-hidden, ArrowDown enters the
  panel) and a head-menu leg (right-click a window head → registry
  entries with keycaps). Both widths; fresh contexts; run twice.
- Full sweep the CI way (DETACHED — nohup+disown+done-file per the
  147 lesson), triaged against the inherited baseline; readable log
  + dw capture paired; `git checkout --` the phase asset dirs after
  glass.
- The after-shot exhibit: before/after pairs against
  `assets/audit-menu-shots/` (the eight defects each get their
  after), cross-read, orchestrator-eyeballed FIRST, delivered to
  the owner. The beauty-pass question asked explicitly: does this
  operate with joy?
- final-summary.md; close counsel (fresh opus, pointers, the
  Tuesday + joy questions); ledger consolidated.

### Out

- The merge itself (the owner's word + shot verdict).

## Acceptance criteria

1. Extended walk green twice; the D3 repair proven on the walk (not
   just unit).
2. Sweep verdict: baseline-subset/exact, zero branch-new (or every
   name dispositioned).
3. The exhibit pairs delivered; a flinch is a redo.
4. Counsel verdict recorded; concerns fixed or ledgered by name.

## Test plan

The extended walk ×2 via dw capture pairing; the detached full
sweep; the pair manifest.
