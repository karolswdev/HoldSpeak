# Evidence - HS-148-02

- **Story:** HS-148-02 - The content sweep (glyphs, groups, casing, …)
- **Status:** done
- **Date:** 2026-08-29

## Proof

### Captured run — 2026-08-29T16:37:17Z

- **Command:** `bash -c (cd web && npx vitest run src/desk/__tests__/menuGlyphs.test.tsx src/desk/__tests__/floorMenu.test.ts src/desk/components/DeskMenuBar.test.tsx src/desk/__tests__/verbRegistry.test.ts src/desk/__tests__/workMenu.test.tsx)`
- **Cwd:** .
- **Exit code:** 0
- **Index-tree:** 3d5a4411dd540a830ef0b7bf8b86a1e11a12daca

```text

 RUN  v4.1.9 /Users/karol/dev/tools/HoldSpeak/web


 Test Files  5 passed (5)
      Tests  52 passed (52)
   Start at  10:37:17
   Duration  759ms (transform 683ms, setup 264ms, import 1.35s, tests 253ms, environment 1.17s)
```

## Orchestrator triage note (2026-08-29)

Verified beyond the builder's word: 52 focused re-run and read; the
glyph picks are a coherent mono family (seven kind glyphs argued one
line each; the Ask family deliberately shares its star); the
Desk-menu context ruling (verb panel whose New group hides under C,
shows under all) recorded for the exhibit; the bound-key set proven
byte-identical twice over; the counsel's discrimination sentence
read as satisfied (panel declaration + two centralized gating
rules, not scattered selectors).

**The "pre-existing failures" claim was INTERROGATED per the
standing lesson — and this time verified TRUE**: chat.test /
containerQueryLaw / writeReceiptGuard fail identically on the dirty
tree and HEAD, and every implicated surface is byte-identical to
main (BriefLane's swallowed write dates to HS-135-07). LEDGERED AS
A DISCOVERY: the web vitest suite carries three standing inherited
failures the pytest-based sweep baseline NEVER SEES — the protocol
has a web-unit blind spot; a web baseline (or three fixes) is a
named follow-up for the close counsel to weigh.
