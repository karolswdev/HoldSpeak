# HS-160-07 - The walk: the same window twice, and a source that honestly fails

- **Project:** holdspeak
- **Phase:** 160
- **Status:** backlog
- **Depends on:** HS-160-05 (rig may build against the wire; face legs after 06)
- **Unblocks:** HS-160-08
- **Owner:** unassigned

## Problem

The §14 P2 exit is a sentence: one real Project produces REPEATABLE
evidence-linked Delta with HONEST partial coverage. On glass,
deterministically, twice.

## Scope

- **In:** `tests/e2e/test_hs160_delta_glass.py` (the house rig; build
  first): (1) THE LOOP — a Project via the 159 interview (the
  arc compounds: finalize with a native watch), seed real changes
  (a new meeting, a decision transition, an overdue item), open
  review → the queue renders grouped, evidence chips open sources →
  decide by KEYBOARD (J/K/A/L/X per the face) → accept → /room shows
  the advanced cursor + zeroed pending → RE-OPEN with no new facts →
  the honest no-new-delta state (no duplicates — PV-J02's law).
  (2) THE REPEAT — open the same frozen window twice server-side,
  byte-identical (via the API). (3) THE DEGRADED LEG — one source
  forced failing (the hs143 pre-boot monkeypatch pattern) → the
  review carries degraded coverage visibly, others intact
  (WEB-SCN-002's spirit). Shots of the queue (wide), the one-card
  (393), the degraded state, the completion summary → assets/
  story-07-shots/ (feeding 06's sheet). Overflow asserted; zero
  false events asserted.
- **Out:** provider sources, Steward, updates.

## Acceptance criteria

- [ ] All legs deterministic ×2; the re-open-no-facts leg produces zero duplicate proposals; the repeat leg byte-identical.
- [ ] The degraded leg shows named degraded coverage while intact sources deliver (never "no change").
- [ ] Keyboard-driven decisions on glass (not click-theater); shots saved >20KB; overflow zero.

## Test plan

- **E2E:** the three legs; playwright-env scratch script; build-first law.

## Notes / open questions

- Seeding gaps stand (159's walls) — DB-layer where no route exists, named per the law.
