# Phase 160 — Project Rooms: The Delta (P2) — Final summary

**Closed:** 2026-09-01. **Counsel:** RATIFY-W-C — zero must-fix; the
S-class split honestly: the hollow-suppression catch and the window
atomicity FIXED in-round, the rest backlogged under counsel's own
conditions. **The owner's verdict:** the review face PASSED on its
FIRST sheet — the arc's banked taste laws became the art direction.
8/8 stories done with evidence.

## What the phase proved

The §14 P2 exit, on glass twice: *one real Project produces
repeatable evidence-linked Delta with honest partial coverage.*

- **Schema v69** (real-DB proven): observations with the
  deterministic-ID-as-PK ruling, evidence links, proposals with the
  dismissal-basis law, frozen review windows.
- **Five collectors, one contract:** seven grounded observation
  kinds, retry no-ops, failure isolation, the watch adapter provably
  fetch-free over STORED snapshots.
- **The frozen twelve-step review:** §7.2 one-to-one, golden
  byte-identical windows, MATERIALITY v1 pinned at a canonical
  0.795, one-open-review, conflicts keep both sources, step 11 an
  identity hook.
- **The decisions:** the closed handler map (risk_attention becomes
  a REAL item through the 158 command; record-only kinds honest;
  conflict refuses accept), TRUE cross-window suppression (made
  true in-round — see below), deferred returns flagged, superseded
  ruled, one-transaction accept advancing the cursor exactly once.
- **The wire + the face:** 5 routes (api-surface 604 after the
  consumer regen), /room's review section real, and the review
  POSTURE — grouped human queue, materiality temperatures, the
  typed comparison ledger, four verbs judged from J/K/A/E/L/X.

## The close's honesty ledger

1. Counsel caught story 04's DEL-003 claim hollow — the basis hash
   made cross-window suppression IMPOSSIBLE. Fixed in-round
   (eca6cde6): source_version rides patch_json, both hash sites
   agree, HARD suppression proven; story 04's record carries the
   correction by name.
2. The api-surface fence caught the manifest missing the face as a
   consumer of its own review routes — the second time this exact
   lesson fired (159's was /project-setups). Root-caused by regen,
   proven deterministic.
3. The orchestrator's own aware-UTC hardening (04) and counsel's
   N-3 (storage) closed the timezone seam end to end.
4. One untracked stray glass PNG parked (never deleted) per the
   standing rule.

## Gates

- Authoritative suite: 12F/8335P in 22:52 vs main's 27-name
  baseline — 2 candidates: api-surface REAL (root-caused + isolated
  5-green), one_path_census proven churn (untouched file, isolated
  green). Zero unexplained branch-new.
- Web: check green (bundle gate); baseline 2060/1 — the
  THRICE-proven ThoughtDocumentPane churn (untouched, isolated 1/1).
- Counsel RATIFY-W-C; in-round: S-1, S-3, N-3. 198 churned PNGs
  restored.

## Backlog carried (named, per counsel's conditions)

- **P2a:** ~~S-2 decide→create_item split transaction~~ PAID by
  HS-161-07 (conn-threading, fault-proven); N-5 widen the no-fetch
  spy; 158's S-1/N-1/N-3; 159's seeding walls + evidence read path +
  finalize name override.
- **P3:** S-4 source chips open their source (with Draft-update);
  N-1 Space preview; N-2 a server-side undismiss verb.

## The road from here

P2a "The GitHub Watch" — the proving V0 slice under the owner's
delivery=GitHub ruling, the stopwatch-bar phase — charters after the
merge, anchors re-verified.
