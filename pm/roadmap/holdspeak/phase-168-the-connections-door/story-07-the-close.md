# HS-168-07 - The close: gates, riders, debts, final summary

- **Project:** holdspeak
- **Phase:** 168
- **Status:** backlog
- **Depends on:** HS-168-05, HS-168-06
- **Unblocks:** -
- **Owner:** unassigned

## Problem

The phase closes only on the full picture: the suite, the sweep,
counsel, the debts named, the owner's word.

## Scope

- **In:** the full suite in two halves (`-n auto`, isolated HOME,
  the metal exclusion) with the sweep against the branch BASE's CI
  failure names (`comm -13`; candidates = branch-NEW names only,
  re-run on the settled tree); the web suite + the inherited
  baseline zero branch-new; every glass rig (158..168) green; counsel
  (opus-worker) reads the diff against the charter's hunts (a second
  readiness derivation; a connect card that loses the session; a
  scope applied silently; a chip the wire lacks; a broken deep link;
  a walk step that shot the desk) — M+S paid in-round, N ledgered;
  the churn (older phases' shot PNGs the rigs rewrite) restored
  BEFORE staging; final-summary.md with the debt ledger (167's
  carried + this phase's); the phase status COMPLETE 7/7; the
  project README; the PR from `feat/connections-door` → main on the
  local gates with the basis recorded; merge on the owner's word.
- **Out:** new work.

## Acceptance criteria

- [ ] Full suite totals read from the saved halves; the sweep zero unexplained; web zero branch-new; rigs green.
- [ ] Counsel's verdict recorded; M+S paid; N in the ledger.
- [ ] final-summary.md written; PR opened with the basis; merge on his word.

## Test plan

- **Close:** the two-halves close wrapper (the 166/167 pattern) + the candidates re-run; `.githooks/dw check holdspeak`; `.githooks/dw gate --porcelain`.
