# Phase 143 — The Intelligence Router: final summary

**Closed:** 2026-08-27. **Stories:** 15/15 done. **Verdict:** every
production inference call in HoldSpeak rides one server-owned,
capability-versioned router with frozen route plans, controller-owned
fallback, and honest receipts — and the owner has two joyful surfaces
to command it.

## What shipped

- **The spine (01–06):** capability registry; immutable model-profile
  authority with hub-local bindings; the sparse assignment store with
  whole-chain precedence; frozen route/operation plans; the durable
  fallback controller and failure law.
- **Adoption (07–10):** Thoughts/Ask/writing, Meetings/speech/
  background, and Agents/Workbenches/Recipes/sequences/workflows all
  migrated onto the canonical resolver and the `InferenceRunner` waist;
  mutable `inference.run` late routing retired; fake fallback labels
  replaced with truthful vocabulary; qualified Recipe chat became the
  first production ToolTurn adopter over the Story-09 foundation.
- **The owner glass (12–13):** the Model Library (availability-only —
  adding a model provably changes zero assignments, byte-checked) and
  the Assignments experience (seven honest rows, one shared editor,
  ABA-safe Use-default, Next-run receipts), each screenshot-walked at
  1440/393/200% with keyboard/screen-reader/reduced-motion proof and
  owner-reviewed shots before merge.
- **Transport parity (11):** twelve MCP twins over the same application
  services, a reciprocal golden-parity harness, a compound hostile-sync
  proof, and the retirement of every side door (destination/profile CRUD
  tools, acquisition aliases, stale target-override fields).
- **Docs (15, owner-ruled mid-phase):** `docs/internal/
  ARCHITECTURE_INTELLIGENCE_ROUTER.md` with the verified Recipe-run
  trace; six entry points defer; retired vocabulary gone.
- **Closeout (14):** the one-restart cross-product chaos proof, the
  393-glass flake fixed with a real ready-barrier, the retired-
  vocabulary doc guard, and the write-once
  [`closeout ledger`](assets/phase-143-closeout-ledger.md) — twelve
  kill criteria and nine exit criteria with verified production-path
  citations.

## The honest residue

- 11 inherited test failures predate the phase and carry named
  non-router ownership (ledger §4).
- Seven Swift physical leaves are HELD by owner ruling (web is the
  spec); the admitted-attempt bridge is frozen compile-green on
  `hold/hs143-10-slice5-swift-bridge` as the recreation seed.
- Documented carries live in ledger §5; a carry is not a waiver.

## How it ran

Six stories closed in this arc (10–15) across ~2 days: Terra build
rounds paired with opus gate audits (eight audits, seven with zero
product bugs), orchestrator verification of every worker claim, full
sweeps triaged against the inherited baseline (closing capture: 6680
passed / the 11-node baseline exactly), and owner rulings recorded
where they landed — the Swift freeze, the docs charter, and the
ceremony bar throughout.
