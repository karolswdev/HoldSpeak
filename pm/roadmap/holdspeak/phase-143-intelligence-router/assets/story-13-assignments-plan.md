# HS-143-13 — Capability Assignments Experience

**Status:** pre-implementation plan. **Planning base:** `feat/hs143-12-model-library`. This is the second Settings job: Model Library answers what is available; Assignments answers which compatible ordered chain each HoldSpeak job uses. They must remain separate (`assets/owner-experience.md:5-25`; `assets/architecture-contract.md:405-428`). No vendor SDK, browser-authored compatibility decision, or second route resolver is in scope.

## 1. Obligation register

| ID | Binding obligation | Slice | Production-object proof |
| --- | --- | --- | --- |
| AC-1 | The default glass is the fixed seven-row roster, therefore a twentieth owner-visible capability cannot increase its page height (`story-13-capability-assignments-experience.md:22-30`; `assets/owner-experience.md:67-92`; `holdspeak/services/inference_assignment_service.py:149-243`). | S2 | Seed 100 registry capabilities plus the canonical seven groups. Assert exactly seven 48–56px summary rows, no per-row select, and 1440/393 DOM geometry/overflow evidence. |
| AC-2 | One editor owns the entire ordered chain. It has one revision-checked atomic save, drag plus accessible Move up/down, no slot autosave, unique 1–4 legs (`story-13-capability-assignments-experience.md:24-25`; `assets/owner-experience.md:93-122`; `holdspeak/services/inference_assignment_service.py:256-394`, `:1387-1473`). | S1, S3 | Construct real `Database`, profiles/bindings, registry, `InferenceAssignmentService`, and route HTTP requests. Assert reordered payload/ordinal is one committed revision, stale expected revision conflicts, replay is stable, and keyboard announcement says the new ordinal. |
| AC-3 | Every inherited/default summary names the resolved chain, never opaque `Default`/`Automatic`; a leaf previews its *exact* effective capability chain/policy before its override is cleared (`story-13-capability-assignments-experience.md:26-27`, `:32`; `assets/owner-experience.md:124-141`; `holdspeak/services/inference_assignment_service.py:396-423`, `:1762-1819`). | S1, S3, S4 | Seed divergent invocation, subject, exact, group, and global rows. Assert preview excludes precisely the candidate key, clearing returns the actual winner, and UI renders `Uses … · name → name`, then a `Next run` receipt while the already-admitted route hash remains unchanged. |
| AC-4 | Broken configured entries stay in order, issues sort first and offer one Fix. Group/global rows receive the server's member compatibility/policy intersection, including registry-growth problems; the browser never guesses it (`story-13-capability-assignments-experience.md:26-27`, `:32`; `assets/architecture-contract.md:169-181`, `:212-226`; `holdspeak/services/inference_assignment_service.py:1592-1696`, `:1843-1900`). | S1, S2, S5 | Drift a binding/readiness/profile capability after save and add an incompatible member capability. Assert the saved entry remains, a projected issue has one repair, issue rows lead, and no client-derived compatibility branch is exercised. |
| AC-5 | 393 has 44px targets, wrapped names, no horizontal overflow, and a sticky Save inside the in-world sheet; desktop is one side sheet, not a modal or matrix (`story-13-capability-assignments-experience.md:28`; `assets/owner-experience.md:163-185`; `docs/internal/DESIGN_SYSTEM.md:204-250`; `docs/internal/ARCHITECTURE_WEB_FRONTEND.md:56-72`, `:88-99`). | S3, S5 | Real hub screenshots at 1440×900 and 393×900 for populated/empty/error/editor/broken/boundary states. Assert focus return, dock non-overlap, 44px target boxes, sticky Save, and `scrollWidth <= viewport`. |
| AC-6 | Only migrated families become editable. Thoughts ships first; Stories 08–10 light their existing group/leaves through the same atoms, while private target selectors are replaced rather than left beside them (`story-13-capability-assignments-experience.md:29-31`; `assets/owner-experience.md:124-141`; `assets/story-10-placement-adoption-plan.md:499-505`). | S4 | Source census and live controls prove no surviving private selector for Thoughts, dictation/meetings, recipe/agent, or workbench. Existing production admissions freeze before mutation; later admission alone sees the changed chain. |
| OX-1 | The chooser reuses the Model Library row grammar but receives only server-filtered compatible candidates for the exact capability revision. Boundary disclosure names material beside the affected cloud leg, RAW holds technical details, and ordinary glass has no IDs/paths/endpoints/secrets (`assets/owner-experience.md:112-122`; `assets/architecture-contract.md:197-226`; `web/src/pages/cores/ModelLibraryCore.tsx:311-348`). | S1, S3 | Candidate DTO fixture includes compatible, savable-with-repair, and broken saved legs. UI asserts one egress badge at the crossing, specific repair, folded RAW, and absence of private locators. |
| OX-2 | Assignment edits affect the next reservation only. In-flight receipt remains frozen and reports actual fallback truth, not a browser prediction (`assets/owner-experience.md:133-160`; `assets/architecture-contract.md:228-296`, `:364-403`). | S4, S5 | Admit a real Thought/Recipe/Workbench operation, mutate via owner glass, then prove old route/operation/attempt receipt bytes are unchanged and the next production admission resolves the new chain. |

## 2. Current-tree inventory

### 2.1 Assignment authority, projection, consumers, and transport gap

| Seam | Evidence read | Disposition for Story 13 |
| --- | --- | --- |
| Canonical sparse authority | `InferenceAssignmentService` is OWNER-only and explicitly owns ordered-chain CAS/inheritance rather than fallback/provider work (`holdspeak/services/inference_assignment_service.py:90-105`, `:127-133`). `set_assignment`, `clear_assignment`, `preview_use_default`, and `resolve_effective` already supply the mutation/ABA primitives (`:256-394`, `:396-585`). | **KEEP and compose.** Do not add a second table, resolver, browser policy, or one-slot mutation. Add only an owner-safe editor/read projection around this service where existing methods cannot project a compatible candidate set. |
| Seven-row owner projection | `assignment_summary()` emits global plus the six canonical groups, detects unknown owner groups, projects group/global issues, and returns `InferenceAssignmentSummary@1` (`holdspeak/services/inference_assignment_service.py:149-243`; canonical groups at `:52-60`). Unit proof pins seven rows and readiness/disabled drift (`tests/unit/test_phase143_inference_assignments.py:711-741`). | **KEEP as the overview source.** The UI may format labels/chains but may not add permanent leaf rows or infer issue state. |
| Scope/precedence and ABA primitive | Scope admits global, group, exact capability, subject, and invocation (`holdspeak/services/inference_assignment_service.py:1288-1385`). Resolution is invocation → subject → exact → group → global and returns a whole winner chain (`:1762-1819`); preview excludes the row being cleared (`:396-423`). | **Expose through the editor projection and narrow HTTP only.** Use the server preview immediately before the explicit `Use default` clear. Do not render a generic `Automatic` state. |
| Group/global compatibility and saved-leg drift | Group/global policy intersection and registry-growth issue are calculated server-side (`holdspeak/services/inference_assignment_service.py:1543-1590`, `:1843-1900`); entries remain labelled and report boundary/readiness (`:2101-2184`). | **KEEP; surface verbatim safe labels/codes through a closed owner DTO.** A group chooser must be evaluated against all affected capabilities, never against one convenient leaf. |
| Model Library precedent | Library is an availability-only aggregate that guards unchanged assignment heads (`holdspeak/services/model_library_service.py:1-12`, `:223-259`) and its HTTP router checks OWNER before JSON parsing (`holdspeak/web/routes/model_library.py:28-47`). The Settings Models slot is intentionally availability-only (`web/src/pages/cores/settingsModels.tsx:1-11`). | **Mirror the aggregate/router idiom, not its semantics.** Assignment command responses must carry canonical committed/current truth, not an assignment-unchanged receipt. Library remains untouched as the peer job. |
| Recipe subject consumer | Post-marker legacy `profile_id` writes map one recipe ID to exact `recipe.run` and `recipe.chat` subject assignments (`holdspeak/services/recipe_service.py:226-264`). The Story-10 mapping specifies the durable record ID, blank inheritance, and the two capabilities (`assets/story-10-placement-adoption-plan.md:150-175`). | **REPLACE the private Recipe selector with one shared subject `AssignmentSummary`/Editor instance per capability context.** Do not write the legacy field to reach it. |
| Workbench subject consumer | Post-marker `profile_id` maps to `workbench.item` and `resolver_profile_id` maps to `voice.reference_resolve` for that workbench (`holdspeak/services/workbench_service.py:49-78`; mapping at `assets/story-10-placement-adoption-plan.md:150-158`). | **REPLACE both Workbench selectors with shared subject summaries/editors.** Keep the compatibility writer only until every old transport client has been removed and the census proves it is no longer a UI side door. |
| Current HTTP gap | The live web server composes/registers only the library aggregate/router (`holdspeak/web_server.py:705-720`, `:721-759`, `:819-829`); `WebContext` has `model_library_service` but no assignment service (`holdspeak/web/context.py:82-92`); the sole model-library routes are `/api/inference/model-library…` (`holdspeak/web/routes/model_library.py:73-170`). No assignment route was found. | **Add the minimal owner-only HTTP seam in S1.** It is necessary for React to consume server truth, but it is not Story 11's parity/sync/MCP project. |

### 2.2 Every current browser assignment/pointer surface

`tests/unit/test_phase143_surface_fallback_census.py:86-118` is the exact fail-closed browser inventory. It currently names **28** source surfaces; the selector/action subset below is dispositioned individually, and the remaining wire/display entries are retained only while they do not author assignment truth. Any removal/addition updates that exact census *after* its replacement proof, never before.

| Current fragment | Current evidence | Story-13 disposition and reason |
| --- | --- | --- |
| `RunsOnPicker.tsx` | A reusable `CycleGadget` lists raw inference targets and writes an ID (`web/src/desk/components/RunsOnPicker.tsx:15-92`). | **RETIRE.** It is the shared private selector; replace all consumers with the shared assignment atoms, not a renamed target picker. |
| Ask | `AskPanel` mounts that picker (`web/src/desk/components/AskPanel.tsx:499-505`); `ask.ts` sends `profile_id`/`inference_target_id` (`web/src/desk/ask.ts:173-179`). | **REPLACE the glass; RETIRE outgoing pointer fields.** Thoughts is the first active subject/context experience. Its summary names the exact resolved `ask.answer` chain and Change opens the common editor. |
| Editor/Project Thought paths | `EditorAIBar` mounts the picker (`web/src/desk/components/EditorAIBar.tsx:197-203`); `ProjectMemoryCore` does too (`web/src/pages/cores/ProjectMemoryCore.tsx:295-303`). | **REPLACE.** They consume the same Thoughts summary/editor contract rather than create two alternate target controls. |
| Dictation recovery | `UtteranceWell` exposes picker after an alternate-runs-on error (`web/src/pages/cores/dictation/UtteranceWell.tsx:100-108`); `useSpeakDeck` sends transient `profile_id` and describes it as a one-run override (`web/src/pages/cores/dictation/useSpeakDeck.ts:105-109`, `:294-298`). | **RETIRE the alternate target picker/field.** Server controller receipt and the group/leaf assignment editor own fallback and next-run choice. Keep only honest terminal/recovery display, never a client-selected retry. |
| Meetings Settings RAW pointers | Dictation RAW retains `target_profile_override` (`web/src/pages/cores/SettingsCore.tsx:672-729`) and Meetings RAW retains `intel_summary_model` (`:793-870`). | **RETIRE.** Both are private pointer writers. Their migrated group is editable from Assignments; RAW may retain non-routing diagnostics only. |
| Recipe editor and chat/run pullout | `RecipeEditor` saves `profile_id` from `Default runs on` (`web/src/desk/pullouts/editors/RecipeEditor.tsx:104-120`); `PersonaChat` (`web/src/desk/components/PersonaChat.tsx:358-366`) and `CapabilitySection` (`web/src/desk/pullouts/shared/CapabilitySection.tsx:171-181`) mount the picker. | **REPLACE.** Recipe run/chat use their subject assignments. The changed summary labels `Next run`; no old editor write-through is offered beside it. |
| Workbench editor | `WorkbenchWindow` renders `profile_id` and `resolver_profile_id` pickers (`web/src/desk/components/WorkbenchWindow.tsx:375-424`) and writes both fields (`:1204-1223`). | **REPLACE.** Render `workbench.item` and `voice.reference_resolve` subject summaries and open the shared editor; delete target-field mutations from this surface. |
| Workbench creation/template and list display | `WorkbenchTemplatePicker` chooses/sends `profile_id` at creation (`web/src/desk/components/WorkbenchTemplatePicker.tsx:29-102`); home resolves `wb.profile_id` for display (`web/src/pages/cores/WorkbenchesHomeCore.tsx:87-91`); `workbenchTarget.ts` is the inherited target display helper (census: `tests/unit/test_phase143_surface_fallback_census.py:99-101`). | **RETIRE creation selector and helper; REPLACE list display.** New workbenches start inherited. Lists show the server-resolved subject summary, not a hand-joined legacy target. |
| Browser transports tied to selectors | `chat.ts` posts `inference_target_id` (`web/src/desk/chat.ts:114-117`); `dataSlice.ts` maps/sends pointer fields (`web/src/desk/store/dataSlice.ts:295-299`, `:533-536`). | **RETIRE pointer input branches.** Preserve only route-receipt reading after it has been recast as frozen route evidence; no compatibility payload may become an editable side door. |
| Display/wire entries | `desk/api.ts`, `Pullout.tsx`, `detail-types.ts`, `store/types.ts`, `lib/primitives.ts`, and `pages/cores/core-types.ts` are classified display transports; `DeliveryBoard.tsx` and `deliveryFactory.ts` are explicitly unrelated (`tests/unit/test_phase143_surface_fallback_census.py:89-118`). | **KEEP only non-authoritative receipt/history shapes.** Update type names/fixtures only when a retired pointer field disappears. Do not mistake historical route evidence for a selector. |
| Library client/core | `ModelLibraryCore` is the availability core (`web/src/pages/cores/ModelLibraryCore.tsx:83-456`) and `modelLibrary.ts` calls only library endpoints (`web/src/pages/cores/modelLibrary.ts:77-136`). | **KEEP.** It remains the peer Models destination and must never gain assignment edit code. |
| Settings mount/aliases | The seven-tile roster includes Models at `settingsPrefs.tsx:28-44`; `SettingsCore` mounts `ModelsModule` at `:917-921`; `configure-runs-on` still aliases to Models (`web/src/desk/components/SurfaceWindows.tsx:290-301`). | **REPLACE the old pointer destination.** Keep Models as Model Library, add its peer Assignments Settings module, and redirect former `configure-runs-on` entrances to Assignments rather than preserving an old selector room. |

## 3. Ordered slices

Every DB-shaped command, generator, test, and real-hub walk runs with an isolated `HOME`; a generator is not exempt. Read its captured output before advancing. Each slice's last operational step is its named census/anchor regeneration and review, never an early allowlist edit. The gitignored browser bundle is rebuilt from `web/src` and is not committed (`docs/internal/ARCHITECTURE_WEB_FRONTEND.md:122-141`; Story-12 precedent at `assets/story-12-model-library-plan.md:151-155`).

### S1 — Owner assignment editor projection and narrow HTTP seam

**Goal.** Keep `InferenceAssignmentService` as the only mutation/resolution authority, but add a closed owner `AssignmentEditorProjection@1` read operation that supplies: selected scope/capability context, draft base revision, exact current or inherited chain, server-projected group/global issues, filtered compatible candidates for the exact capability revision, configured broken/savable-with-repair legs retained in order, permissible retry-policy facts, and capability-specific boundary/material disclosure. It may use profile/binding facts, but must not return ordinary-glass locators/secrets or make the browser call compatibility code. The DTO must separately identify one selected capability when group/global policy/compatibility needs exact preview; a group/global overview must never pretend one policy fits every leaf (`holdspeak/services/inference_assignment_service.py:1475-1516`, `:1543-1696`, `:1843-1900`).

**HTTP-only fold-in.** Add owner-before-body, recursively closed GET/POST routes beside `model_library.py`: summary, editor projection/read, set whole chain, Use-default preview, and clear. Use one `WebContext`-composed service and the process registry/foundation that production routing uses. Commands require stable `command_id`, expected revision, and the existing receipt/current projection. Error mapping mirrors the library router's safe 400/403/404/409/503 mapper (`holdspeak/web/routes/model_library.py:20-47`). Do **not** add MCP twins, HTTP↔MCP golden fixtures, sync import/export, broad compatibility inventory, events as authority, or route-receipt transport; those remain Story 11 (`story-11-http-mcp-sync-compatibility.md:16-39`).

**Files to change.** `holdspeak/services/inference_assignment_service.py`; add `holdspeak/web/routes/inference_assignments.py`; add the one `WebContext` field and server composition/import/router registration in `holdspeak/web/context.py`, `holdspeak/web_server.py`, and `holdspeak/web/routes/__init__.py`. Reuse the existing model-library router idiom, never Model Library mutations (`holdspeak/services/model_library_service.py:93-107`, `:223-259`).

**Proof.** Extend `tests/unit/test_phase143_inference_assignments.py` with server editor projections, group/global differing-policy intersection, registry growth, broken configured entries, exact preview, 1–4 unique ordered legs, CAS/replay/changed-payload cases. Add `tests/unit/test_web_inference_assignment_routes.py` with OWNER/None/AGENT/MODEL_TURN-before-body matrix, recursively unknown fields, safe DTO sentinel, replay and stale revision conflict. Add a real-object integration proving library → assignment mutation → Thought next admission and the frozen previous route remains unchanged.

**Focused command.** `HOME_REAL="$HOME" HOME="$(mktemp -d)" uv run --python 3.13.11 pytest -q tests/unit/test_phase143_inference_assignments.py tests/unit/test_web_inference_assignment_routes.py tests/unit/test_phase143_inference_route_plans.py tests/integration/test_phase143_placement_adoption_matrix.py --tb=short`

**Last step.** Regenerate/review changed routing-authority and HTTP-route anchors, then rerun `tests/unit/test_phase143_routing_authority_census.py`; do not pre-classify the new route/service.

### S2 — Settings peer destination and bounded overview

**Goal.** Keep the existing Settings `Models` slot as the availability-only Model Library and add **Assignments** as its adjacent peer destination, not a tab inside Library and not a new top-level product route. Implement a host-agnostic `CapabilityAssignmentsCore` plus typed client module. Its initial face is exactly the server seven-row projection: group/global effective named chains (first two plus `+N`), inheritance source, at most one status/one Fix, issues first, and one `Show task overrides` disclosure whose default is Overrides & issues. All tasks is grouped under the six server labels. Nothing in the leaf registry adds permanent overview height (`assets/owner-experience.md:67-92`; `web/src/pages/cores/settingsPrefs.tsx:28-44`; `web/src/pages/cores/SettingsCore.tsx:991-1034`).

Use the existing surface kit and Model Library's compact selected-row vocabulary, but a summary row is a command to one editor, never a select. `SurfaceState` keeps empty/error in flow. Make error/empty truth explicit: no global chain says **No default model** with one `Choose default`; a broken assignment stays named with one `Fix` (`holdspeak/services/inference_assignment_service.py:157-167`, `:223-237`). Use container reflow/tokens, no modal role, no card grid, no raw controls, and no browser compatibility algorithm (`docs/internal/DESIGN_SYSTEM.md:204-250`; `docs/internal/ARCHITECTURE_WEB_FRONTEND.md:37-86`).

**Files to change.** Add `web/src/pages/cores/CapabilityAssignmentsCore.tsx` and `assignmentExperience.ts`; add a thin Settings adapter; amend `web/src/pages/cores/settingsPrefs.tsx`, `SettingsCore.tsx`, `web/src/desk/components/SurfaceWindows.tsx`, and `web/src/desk/surface/surface.css`. Keep `settingsModels.tsx`/`ModelLibraryCore.tsx` separated. Redirect `configure-runs-on` to `scope: "assignments"` only after the old selector is gone.

**Proof and screenshots.** Add `web/src/pages/cores/__tests__/CapabilityAssignmentsCore.test.tsx` for exact seven rows, issue-first ordering, 100-capability bounded height, named inheritance, empty/error/Fix, override filters, no matrix/select-per-row, and one primary. Add `tests/e2e/test_hs143_assignments_glass.py` using production server composition and isolated DB. Capture real-hub 1440×900 and 393×900 **populated, empty, and error overview** shots in `assets/story-13-shots/`; assert overflow, row height, dock intersection, and no page errors.

**Focused commands.** `(cd /Users/karol/dev/tools/HoldSpeak/web && npm run test:web -- src/pages/cores/__tests__/CapabilityAssignmentsCore.test.tsx)`; then `(cd /Users/karol/dev/tools/HoldSpeak/web && npm run build)`; then `HOME_REAL="$HOME" HOME="$(mktemp -d)" PLAYWRIGHT_BROWSERS_PATH="$HOME_REAL/Library/Caches/ms-playwright" uv run --python 3.13.11 pytest -q tests/e2e/test_hs143_assignments_glass.py --tb=short`.

**Last step.** Regenerate/review the Settings/SURFACES/architecture anchors and update the exact surface census only after the old entry has actually disappeared; rerun `tests/unit/test_phase143_surface_fallback_census.py` and the Settings core tests.

### S3 — One assignment editor, chooser, ordering, and ABA-safe Use default

**Goal.** Clicking a summary or allowed leaf opens one in-world side sheet on desktop and full-width sheet on narrow glass. It contains `Use default` with the server-projected exact effective chain, a Custom draft, ordered model legs, boundary warning adjacent to a cloud leg, compact reusable Library-row chooser, drag plus Move up/down, folded Details/RAW fallback policy, Cancel, and one sticky **Save assignment**. All draft operations stay local until Save submits one full chain/CAS body. A server conflict refreshes/reopens in-flow; it never merges individual slots. A saved but now broken leg stays in order with its one repair. Selecting a row/radio never saves or runs it (`assets/owner-experience.md:93-122`, `:176-185`; `docs/internal/DESIGN_SYSTEM.md:40-47`, `:123-148`).

`Use default` is deliberately a two-stage in-sheet action: obtain `preview_use_default` for the exact scope/capability, present its resolved chain/retry/boundary truth, then submit `clear_assignment` with the previewed assignment revision. If the head changed, show the named conflict and require a fresh preview; never blindly clear a newly recreated ABA row. This uses the service's existing excluded-key preview and revision CAS rather than browser comparison (`holdspeak/services/inference_assignment_service.py:396-423`, `:425-565`).

**Files to change.** Add shared `AssignmentSummary`, `AssignmentEditor`, and `AssignmentModelChooser` under `web/src/pages/cores/` or a bounded shared surface module, then compose them from `CapabilityAssignmentsCore`. Extend only `surface.css` with token/container styles and the library's row grammar as a presentational dependency; do not import/mutate Model Library state. The editor has no arbitrary text field; if an incidental filter/search field is needed, it receives the click-to-toggle mic and remains local-only. Cloud egress uses one `EgressChip`, not reassurance prose (`docs/internal/POSITIONING.md:183-202`).

**Proof and screenshots.** Component tests cover roving/radiogroup selection, no action on selection, one primary, draft cancel, keyboard/drag reorder, ordinal live announcement, Escape exact focus return, Mod/Ctrl+Enter, retry/boundary disclosure, broken leg and conflict. Real-hub tests cover global, group, exact leaf, `Use default` preview/clear, and group/global server issue projection. Save 1440 and 393 **editor populated, broken-entry, boundary-warning, and conflict/error** shots in `assets/story-13-shots/`.

**Focused commands.** `(cd /Users/karol/dev/tools/HoldSpeak/web && npm run test:web -- src/pages/cores/__tests__/CapabilityAssignmentsCore.test.tsx)`; `HOME_REAL="$HOME" HOME="$(mktemp -d)" PLAYWRIGHT_BROWSERS_PATH="$HOME_REAL/Library/Caches/ms-playwright" uv run --python 3.13.11 pytest -q tests/e2e/test_hs143_assignments_glass.py tests/unit/test_phase143_inference_assignments.py tests/unit/test_web_inference_assignment_routes.py --tb=short`.

**Last step.** Regenerate/review only editor/component anchors and rerun their focused tests after source change. Do not add a selector exception to the census.

### S4 — Replace pointer glass, with Thoughts first

**Goal.** Replace, never coexist with, the legacy `RunsOnPicker` family. First land Thoughts: `AskPanel`, `EditorAIBar`, and `ProjectMemoryCore` render the common summary and Change/editor for their registered exact capability/subject context; their requests no longer send `profile_id` or `inference_target_id`. A mutation after an admitted Thought visibly says **Next run**, while its current route receipt remains frozen. Then activate migrated Story-07 dictation and Story-08 Meetings through the Settings group/leaf editor, retire `UtteranceWell` alternate-target recovery and both Settings RAW pointer fields, and route `configure-runs-on` to Assignments. No browser retry/fallback picker survives (`web/src/desk/components/RunsOnPicker.tsx:15-92`; `web/src/pages/cores/dictation/UtteranceWell.tsx:100-108`; `web/src/pages/cores/SettingsCore.tsx:672-729`, `:793-870`).

**Files to change.** Remove `RunsOnPicker` imports/calls and target state from `AskPanel.tsx`, `EditorAIBar.tsx`, `ProjectMemoryCore.tsx`, `UtteranceWell.tsx`, `useSpeakDeck.ts`, `desk/ask.ts`, `desk/chat.ts`, and `desk/store/dataSlice.ts`; revise the smallest related wire types/fixtures only after callers stop sending the fields. Delete `RunsOnPicker.tsx` when its final consumer is gone. In `SettingsCore.tsx`, delete routing pointer controls rather than hide them in RAW.

**Proof and screenshots.** Update the existing coupled web tests before the sweep: `web/src/desk/__tests__/ask.test.ts`, `chat.test.ts`, `wireGuard.test.ts`, `web/src/pages/cores/dictation/__tests__/transientRetry.test.tsx`, and `SettingsCore.test.ts` (current coupled suite is discoverable at the source-census test, `tests/unit/test_phase143_surface_fallback_census.py:86-165`). Add production-object integration for Thought mutation-after-admission/next-run-only route hashes and dictation/meeting effective resolution. Save 1440 and 393 **Thought subject, Dictation issue/recovery, and Meetings group** shots in `assets/story-13-shots/`.

**Focused command.** `HOME_REAL="$HOME" HOME="$(mktemp -d)" uv run --python 3.13.11 pytest -q tests/unit/test_phase143_surface_fallback_census.py tests/unit/test_ask_no_retarget.py tests/unit/test_ask_runner_migration.py tests/unit/test_dictation_session_admission.py tests/unit/test_phase143_meeting_live_cutover.py tests/integration/test_phase143_placement_adoption_matrix.py --tb=short`; run the named web tests and `npm run build` separately after reading this output.

**Last step.** Regenerate/review `assets/generated-surface-fallback-census.md` and exact `WEB_ROUTING_SURFACES` only after the private source is removed. Preserve unrelated historical display/receipt rows instead of deleting them to make the count look good.

### S5 — Recipe/Workbench subject replacement and final real-hub walk

**Goal.** Replace Recipe and Workbench controls with the same atoms, not a feature-owned selector: Recipe run/chat subject summaries; Workbench `workbench.item` and `voice.reference_resolve` summaries; and server-resolved Workbench list detail. New template/blank Workbench creation starts inherited, without a target choice. Remove `RecipeEditor`'s legacy `profile_id`, both `WorkbenchWindow` writes, the template picker choice/body, and `workbenchTarget` display helper. The old service write-through remains only as compatibility until the browser census has zero writers, then is reduced/refused in the same change so no side door remains (`holdspeak/services/recipe_service.py:229-264`; `holdspeak/services/workbench_service.py:49-78`; `assets/story-10-placement-adoption-plan.md:499-505`).

**Whole-object final proof.** Perform the complete real-hub walk with production objects and isolated HOME: Library adds/retains models without assignment changes, then Assignments writes global/group/exact/Recipe/Workbench chains; admit a Thought, Recipe, and Workbench parent; mutate each assignment; prove only the later parent uses the change and prior route/attempt/receipt is frozen. Walk broken legs, group/global compatibility issue, 4-leg bound, reorder, conflict, Use-default ABA preview, local→cloud warning, all fallback receipt states available to the projection, and no raw private values on ordinary glass. Capture **every final populated/empty/error/editor/subject/broken/boundary state at 1440 and 393** under `assets/story-13-shots/`.

**Accessibility/device gate.** At 200% zoom, keyboard-only operation, screen-reader names/live ordinal/status, Escape focus restore, 44px narrow targets/sticky Save/no horizontal overflow, dock non-overlap, and `prefers-reduced-motion: reduce`, repeat the editor and one subject summary. This is the final slice because the contract requires interaction truth at 200%, not a component green alone (`assets/owner-experience.md:163-185`; `docs/internal/ARCHITECTURE_WEB_FRONTEND.md:56-82`).

**Tests to update/run up front.** `web/src/desk/__tests__/recipeProfileWriters.test.ts`, `web/src/desk/components/__tests__/workbenchEditing.test.tsx`, `workbenchFrames.test.tsx`, `workbenchOperates.test.tsx`, `writeReceipts.test.tsx`, `tests/unit/test_recipe_precedence.py`, `test_recipe_runner_migration.py`, `test_workbench_runner_migration.py`, `test_phase143_subject_pointer_migration.py`, `test_phase143_surface_fallback_census.py`, `test_interior_canon_guard.py`, plus the S1 route tests and S2/S3 real-hub glass test. This is the Story-12 sweep-tail lesson: enumerate and migrate old-glass E2Es, API manifest/route tests, interior-canon guards, and census classifications before editing the source, not after a late test failure (`tests/e2e/test_hs143_model_library_glass.py:78-185`; `tests/unit/test_phase143_surface_fallback_census.py:189-255`; `docs/internal/ARCHITECTURE_WEB_FRONTEND.md:56-82`).

**Focused commands.** `(cd /Users/karol/dev/tools/HoldSpeak/web && npm run check)`; `HOME_REAL="$HOME" HOME="$(mktemp -d)" PLAYWRIGHT_BROWSERS_PATH="$HOME_REAL/Library/Caches/ms-playwright" uv run --python 3.13.11 pytest -q tests/e2e/test_hs143_assignments_glass.py tests/unit/test_phase143_inference_assignments.py tests/unit/test_web_inference_assignment_routes.py tests/unit/test_recipe_precedence.py tests/unit/test_recipe_runner_migration.py tests/unit/test_workbench_runner_migration.py tests/unit/test_phase143_subject_pointer_migration.py tests/unit/test_phase143_surface_fallback_census.py tests/unit/test_interior_canon_guard.py --tb=short`. Then run the isolated-HOME full-suite command from `CLAUDE.md` with `-n auto`, read the captured output, and capture evidence before any status flip.

**Last step.** Regenerate/review all actually affected census/route/component anchors and `assets/generated-surface-fallback-census.md`, rerun their exact gates, review every `assets/story-13-shots/` artifact, then capture DW evidence. No generated anchor or screenshot substitutes for a production-object route-freeze proof.

## 4. [ORCH-CALL] items

1. **Assignments destination. Recommendation: add Assignments as the peer Settings tile immediately adjacent to the existing Models tile, which remains the Model Library, and retain no standalone page.** This precisely implements the owner contract's two peer Settings jobs (`assets/owner-experience.md:5-19`) while respecting the one Desk/one Settings window law (`docs/internal/POSITIONING.md:80-123`). Former `configure-runs-on` entrances target Assignments after the old selector is gone.
2. **Subject assignments versus seven global rows. Recommendation: the seven-row overview remains global/group only; subject overrides appear only in a contextual `AssignmentSummary` on an open Thought, Recipe, Workbench, Agent, or Project, and its Change opens the same editor pre-scoped to the exact capability/subject.** `Show task overrides` lists global capability leaves, not an unbounded cross-object subject inventory. This preserves bounded default height and the contract's reuse rule (`assets/owner-experience.md:83-92`, `:124-141`) while honoring Recipe/Workbench's exact migration mapping (`assets/story-10-placement-adoption-plan.md:150-158`).
3. **Story-11 transport verdict. Recommendation: fold in only narrow owner-only HTTP now: closed summary/editor/preview/set/clear endpoints, owner-before-body, stable command replay/CAS, and safe DTO/error mapping.** No MCP twin, reciprocal golden parity, sync hostility, broad compatibility transport inventory, or events authority lands here; those are explicitly Story 11 work (`story-11-http-mcp-sync-compatibility.md:16-39`). This follows the accepted Story-12 HTTP-only precedent (`assets/story-12-model-library-plan.md:108-115`).
4. **ABA-safe Use default. Recommendation: render it as an in-sheet preview state, not a blind destructive button: fetch the server's exact excluded-scope preview, show named effective chain/retry/boundary, then clear with the current expected revision; a conflict discards the preview and asks the owner to refresh.** The existing service already supplies excluded-key resolution and clear CAS (`holdspeak/services/inference_assignment_service.py:396-423`, `:481-565`), so a browser-side equality guess is prohibited.
5. **Invocation override UI. Recommendation: do not preserve a general `Runs on` picker as an invocation editor. Retire the browser's raw target overrides; contextual owner edits are durable subject/exact/group/global assignment changes whose receipt says Next run.** Invocation scope remains resolver authority for admitted operations (`holdspeak/services/inference_assignment_service.py:1348-1363`, `:1762-1819`), but it is not a private feature selector or a way to bypass frozen route admission.

## 4b. Orchestrator dispositions on the [ORCH-CALL]s (2026-08-26)

All five recommendations ACCEPTED as written, decided by the
orchestrator as tie-breaker (ratified owner-experience contract; these
are implementation granularity). One boundary clarification on call 5:
the Model Library detail pane's **Runs-on FOLD** (restored by Story 12's
sweep round as a folded server-fact disclosure, guarded by
`test_runs_on_room_stays_folded_into_the_models_module`) is a DISPLAY of
server truth and STAYS; what call 5 retires is the legacy `RunsOnPicker`
SELECTOR and every raw invocation-target override — pickers die,
disclosures live. Receipts on contextual edits say "Next run" per the
Story-10 next-run-only law. Build order: S1+S2 in one round, S3, gate
audit over the server+overview half, S4, S5 (walk + a11y/zoom), sweep,
close. OWNER LAW: shots before merge. The Story-12 lesson is codified in
the risk register's final row — the cross-cutting tests of every
replaced surface are named slice work, not sweep surprises.

## 5. Risk register

| Risk | Containment |
| --- | --- |
| A new editor becomes a second resolver or browser compatibility engine. | Server emits closed summary/editor/candidate/issue facts; test incompatible group/global policies, registry growth, broken saved leg, and assert UI has no compatibility computation (`holdspeak/services/inference_assignment_service.py:1543-1696`). |
| A one-slot reorder/autosave causes a partial chain or ABA clear deletes the wrong state. | Submit one ordered 1–4 leg body with expected revision; use preview then revision-checked clear; test stale save, replay, changed payload, and same-ID conflict (`holdspeak/services/inference_assignment_service.py:256-394`, `:425-565`). |
| A mutation appears to retarget active work. | Production-object mutation-after-admission tests inspect frozen route/operation/attempt/receipt IDs before and after, then admit a second parent (`assets/architecture-contract.md:228-296`). |
| Legacy picker or raw Settings pointer remains beside the shared editor. | Inventory all 28 census rows before S4/S5; delete selector imports/body fields, redirect entry alias, and regenerate the exact fail-closed census only last (`tests/unit/test_phase143_surface_fallback_census.py:86-165`, `:200-212`). |
| A new Assignments room silently merges Library's job. | Separate Settings modules/services/client endpoints. Library commands retain their assignment-head invariance test (`holdspeak/services/model_library_service.py:223-259`); Assignments alone calls set/clear. |
| Mobile sheet overlaps dock or loses Save/keyboard authority. | Real-hub 1440/393 screenshots plus geometry assertions, 200%/reduced-motion/screen-reader/keyboard walkthrough, sticky footer and focus-return checks (`assets/owner-experience.md:163-185`). |
| Built UI is green only because the ignored bundle is stale. | Run `npm run build`/`npm run check` from `web` in every UI slice and review source plus real-hub screenshots; never stage `static/_built` (`docs/internal/ARCHITECTURE_WEB_FRONTEND.md:122-141`). |
| Surface replacement breaks indirect tests late. | Treat old-glass E2Es, API/route manifest tests, interior-canon guards, Settings/core tests, and census classifications as S4/S5 named work, before the sweep, following the Story-12 real-hub suite pattern (`tests/e2e/test_hs143_model_library_glass.py:78-185`). |
