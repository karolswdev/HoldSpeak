# Story 08 · Phase C slice C3 counsel (Sol, capped pass)

Reviewed at `4381a820`, 2026-08-24, under the owner's capped protocol
(one ruling round + at most one fix round; findings must reproduce on an
ordinary product action; fault-injected/adversarial scenarios = recorded
notes). Fresh counsel session, scoped to C3. In parallel, an independent
owner-side code-review stream (opus-worker) audited the same diff; its
four findings are dispositioned below alongside the counsel's.

## Verdict: DO-NOT-RATIFY → fixed in the single permitted fix round

**The one blocker (ordinary-path, reproduced in production):** reserved
Stop-handoff rows were exposed to the recovery glass's generic **Retry**
and **Skip** (`meeting_intel_service.py:52-59`). Reproductions: (a)
Retry before settlement superseded the reservation and queued+executed
fresh work while the original route was still `stopping`, with no
unknown-ledger events (`db/intel.py:2250-2270`); (b) Skip persisted
`skipped`, yet automatic unknown recovery — which did not require the
original to still be reserved (`db/intel.py:683-710`) — fresh-admitted
and executed anyway: the Meeting ended `ready` against the owner's
explicit choice. Counsel's words: "Skip currently means 'run it
anyway'." A control-trust failure on a normal button.

**Everything else PASS under the counsel's own production probes:** the
reserve-inert freeze (only a reserved row + `handoff_reserved` event),
the unique activation witness, unknown terminals never activating,
automatic recovery minting the linked fresh job with both ledger events
while the original stays reserved, the ordinary Stop → restart →
summary/label/title delivery with exactly one activation/claim/
completion and no doubled output, and the post-commit cancellation
signal.

## The fix (same day, single round)

- Shared current-lineage predicate: an intact C3 reservation means
  `status='reserved'` AND `lifecycle_posture='reserved'`, provider
  identity matched, no settlement (`db/intel.py:653-688`).
- Generic Retry and Skip refuse at the repository before any mutation
  (`db/intel.py:2251-2252`, `:2352-2353`); the service maps the state to
  a typed `ConflictError(code="reserved")` and the recovery glass stops
  advertising the controls (`meeting_intel_service.py:39-68`).
- Automatic unknown recovery requires the intact reserved original on
  both fields — a skipped/superseded/settled historical reservation can
  never be recovered later (`db/intel.py:704-749`).
- Counsel's four proofs committed as production-object tests: glass
  projects `{retry:false, skip:false}`; repo/service refusals; no
  descendant while unsettled; unknown terminal → exactly one linked
  fresh descendant, original reserved, both events, only the descendant
  executes. Plus: a known-settled handoff skips normally and is never
  unknown-recovered.

## Audit-stream findings, dispositioned

1. Per-pass reconciliation/recovery `BEGIN IMMEDIATE` scans — **NOTE**
   (bounded by drain cadence, single-user; gating candidate recorded).
2. Fence-fault → `save()` legacy enqueue → active-descriptor uniqueness
   collision → parent OPEN + fence pending — confirmed by the counsel's
   own fault-only probe; **NOTE per the capped fault rule** (requires an
   injected handoff failure; recorded here for the sitting:
   `intel_admission.py:494-502`, `persistence.py:86-103`,
   `db/meetings.py:763-800`).
3. Serial synchronous post-commit cancel loop on Stop — measured
   production-shaped: four 200ms cancels would cost 800ms serially on
   the owner's hero action. **FIXED in the round** (lawful under
   durable-fence-before-best-effort-cancel): cancellation signals moved
   to daemon threads after the authoritative Stop transaction;
   `stop_wall_ms=16.31` measured; the late-live-result cancellation
   proof re-run green.
4. Missing index for the unsettled-handoff scans — **FIXED in the
   round**: `idx_stop_handoffs_unsettled_provider` added additively via
   reconcile-on-open; canonical snapshot regenerated by procedure.

## Tired-Tuesday judgment (counsel's, pre-fix)

The automatic Stop experience is good — Stop returns queued truth,
restart adopts it, aftercare arrives without prompts or a stuck button.
The Retry/Skip exposure was the bar to ratification; it is now fenced
with the counsel's own proofs.

## Orchestrator disposition

Blocker accepted and fixed within the capped protocol; verification
against the counsel's probe recipes is by the committed production-shaped
tests plus the closing sweep (**6418 passed / zero branch-new**; two
known load flakes serial-green). Per the owner's protocol, no second
counsel round is convened. **C3 is CLOSED; Phase C (C1+C2+C3) is
structurally complete.** Remaining sitting notes: items 1 and 2 above.
