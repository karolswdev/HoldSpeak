# Evidence — HS-78-04 — The re-recorded demo + docs + closeout

- **Shipped:** 2026-07-02/03
- **Commit:** this commit (branch `phase-78-talk-to-the-desk`)

## The demo (recorded, 5 beats, real speech + the real .43 model)

1. Arrive: the desk WITH the waiting Claude (a real hook-ingested
   session; every lane seeded — the review's third complaint answered).
2. **Claude's question answered BY VOICE** (the marquee first): held the
   mic, the recording spoke, `Sent` — the remote seam received the words.
3. A note **spoken into existence** (the editor's mic filled the body).
4. Filed by drag: the note **left the stage** (the review's first
   complaint, fixed on camera).
5. The Owl **asked by voice**; the real `.43` model's answer landed as
   an artifact with the beat.

No staged opt-in friction (the preview card does not appear — the
review's second complaint honored; the memory rule recorded). Zero page
errors; `location.pathname` never left `/`. The video ships to the owner
directly (an ephemeral artifact; the committed screenshots cover the
stills).

## Docs

- WEB_DESK.md gains **Talk, don't type** (the mics + Hold to answer);
  GETTING_STARTED's desk paragraph gains the mic line. Guards **85
  passed** first try.

## Acceptance criteria — re-checked

- [x] Every lane seeded incl. the waiting coder; the voice fills on
      camera; docs speak the mic; guards green; the PR merges on a
      conclusion-checked green.
