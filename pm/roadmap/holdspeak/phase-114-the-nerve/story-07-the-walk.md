# HS-114-07 - The walk

- **Project:** holdspeak
- **Phase:** 114
- **Status:** backlog
- **Depends on:** HS-114-01, HS-114-02, HS-114-03, HS-114-04, HS-114-05, HS-114-06
- **Unblocks:** —
- **Owner:** unassigned

## The thesis (the bar)

A screenshot walk at 1440×900 against the real hub with .43 endpoint
live proves every P0 and P1 finding from the applicability study is
resolved. Article IX: intelligence claims require evidence on a
real hub, model, device, and viewport.

## Method

Playwright walk capturing real browser state at 1440×900 + 393×852.
Every shot proves a specific finding.

### Shot sequence

1. **Fresh seed → desk.** The desk shows the seeded Homelab
   destination in Settings > Models, the Architecture reviewer
   agent and Summarize workflow on the floor alongside the six
   drawers. (Proves HS-114-01: seeded nerve.)

2. **Cmd+I opens Ask.** Ask panel opens with no prior selection.
   Empty-context state: composer well, RunsOnPicker, lens selector,
   egress lamp. (Proves HS-114-02: keyboard shortcut.)

3. **Ask result with egress lamp.** A completed Ask showing the
   LampGadget readout (● LAN HOMELAB) in the instrument footer,
   plus the HUB> turn with inline receipt.
   (Proves HS-114-04: egress readout.)

4. **Context verb on a Note.** Right-click a Note > "Ask this"
   visible in the context menu. (Proves HS-114-02: context verb.)

5. **Agent chat with egress lamp.** PersonaChat showing the
   Architecture reviewer with RunsOnPicker set to Homelab and
   LAN lamp in the footer. (Proves HS-114-04: persona egress.)

6. **Per-destination TEST.** Models table with Homelab row showing
   `● READY 42ms` after TEST. (Proves HS-114-03: per-row probe.)

7. **Model discovery dropdown.** The MODEL cell showing a
   CycleGadget populated with models discovered from .43.
   (Proves HS-114-03: model discovery.)

8. **Editor transform proposal.** A Note with selected text, the
   aerogel proposal inset visible with Accept/Reject TransportKeys
   and egress receipt. (Proves HS-114-05: propose, don't replace.)

9. **Meeting intel with egress lamp.** Meeting card showing intel
   state with `● LAN HOMELAB` inline.
   (Proves HS-114-04: meeting egress.)

10. **RunsOnPicker empty state.** A RunsOnPicker with no targets
    showing `NO DESTINATIONS` and the dashed affordance.
    (Proves HS-114-06: honest empty state.)

## Acceptance

- 10 shots captured at 1440×900, all showing real data from the
  real hub with the real .43 endpoint.
- Every P0 and P1 finding from the applicability study has a
  corresponding shot.
- Shots saved under `assets/hs-114-07/`.

## Test plan

- Playwright capture against the real hub.
- Visual inspection of every shot.
