# HS-162-03 - The model drafter: constrained prose over the frozen router

- **Project:** holdspeak
- **Phase:** 162
- **Status:** backlog
- **Depends on:** HS-162-02
- **Unblocks:** HS-162-04
- **Owner:** unassigned

## Problem

The value bar (PV-H04: ≥70% generated content retained) wants prose a
human keeps. UPD-002: unsupported model language MUST be omitted or
visibly marked for owner review.

## Scope

- **In:** generator="model": the frozen intelligence-router
  assignment (the 143 helpers — no new routing doors) drafts over
  THE SAME pinned manifest 02 built; the output is constrained to
  02's claim schema — every model sentence either binds to evidence
  refs from the manifest or is emitted into a visibly MARKED
  "unverified" span (never silently dropped into the body as fact);
  model failure/unavailability falls back to 02 with an honest
  generator field (UPD-003 / the STW-007 spirit); the LLM call is
  admitted the house way and badged at the surface (05). Real-metal
  proof per standing law: one marked live test against the .43
  endpoint.
- **Out:** any Steward execution, scheduling.

## Acceptance criteria

- [ ] Claim discipline under test: a fake model emitting an unsupported sentence lands it MARKED, never as bare fact; supported sentences bind refs.
- [ ] Router-down ⇒ deterministic fallback with generator recorded honestly; no crash, no silent downgrade.
- [ ] One live .43 leg (marked, skip-clean elsewhere) drafts a real room and the claims resolve.

## Test plan

- **Unit:** fake-runner truth tables. **Integration:** live-marked .43 leg.
