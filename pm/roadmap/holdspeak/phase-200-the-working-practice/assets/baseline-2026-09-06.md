# HS-200-01 attested baseline detail — 2026-09-06

Companion to [BASELINE.md](../BASELINE.md). BASELINE.md holds the
disposition and the summary; this file holds the row-level record the
summary points at.

**Attested:** 2026-09-06, America/Denver.
**Repository revision inspected:** `bea4176c2f7e5a5709e3404e517bd0790d2fe7b2` (main, also the tip of `feat/phase-200-g0`).
**Planning baseline it replaces:** `519afd4f` (2026-09-05), which predates PRs 558, 563, 564, 565, 566.
**Method:** read-only. Git and GitHub queries, `sqlite3 file:…?mode=ro` against the owner's live database, authenticated GET against the running hub, local toolchain probes. No write, no product suite run against the owner database, no mutation of any file outside this phase directory.

**Secret handling.** No key, token, or credential value appears in this
file. Where a secret is material the record names its *state* (set /
not set) and its variable or field name only.

---

## 1. Observations, recorded separately from Git state

The story requires backend, frontend, database, model, and source
observations kept apart from Git state, because a checkout does not
attest a running process. Section 1.1 is the repository. Sections
1.2–1.7 are the machine.

### 1.1 Git state (repository only — attests nothing about the running product)

| Fact | Value |
|---|---|
| main HEAD | `bea4176c2f7e5a5709e3404e517bd0790d2fe7b2`, 2026-09-06 11:29:49 -0600 |
| Working branch | `feat/phase-200-g0`, at the same commit as main |
| Package version (pyproject) | `holdspeak` 0.4.0, `requires-python >=3.10` |
| Structural issues (`.githooks/dw check holdspeak`) | 6 in the working tree; 5 on committed main |

Merged since the planning baseline and inside the Tuesday arc:

| PR | Merge commit | What it carries |
|---|---|---|
| 553 | `0e69f3d7` | Phase 170 The Great Pass — face canon, sweep + ratchet, Concierge, four Tuesday faces |
| 554 | `397e3594` | Phase 171 The Heartbeat — sweep on a cadence, needs-you in shade and dock, brief recurring |
| 555 | `0c17425e` | Phase 172 The Loop Closes — production proposal bridge, People joins |
| 556 | `d416e08a` | Phase 173 The Steward's Hand and Voice — drafter, observations, nudges |
| 557 | `8c994305` | Phase 174 Reach — remote transport, runner, scoped credentials |
| 558 | `aa278604` | Phase 175 Calendar and the Clock — wire landed, faces not built, paused |
| 559 | `d4fbfaca` | Handover: 174 merged, 175 parked |
| 560 | `855b34cb` | Web integration quality checks restored; Chair retry receipts |
| 561 | `075d6833` | Repeatable onboarding and Thread conversations (Interview) |
| 562 | `519afd4f` | Product documentation refresh; STE writing policy — **the Phase 200 planning baseline** |
| 563 | `bea4176c` | **The Phase 200 charter itself** |
| 564 | `7d897302` | Phase 175 ruling R1 — event→Room matcher requires the Room's full name |
| 565 | `7a47904e` | Handover Muad'Dib XV → XVI |
| 566 | `8fb56d97` | Phase 176 The Speak Loop — CLOSED 7/8; `text` correction kind, Journal stream, voice law |

BASELINE.md's planning row for PR 558 ("Calendar work exists in an open
PR", observed head `2cde6e62`) is superseded: 558 merged at
`aa278604` on 2026-09-06T02:18:51Z, and its R1 follow-up merged at
`7d897302`.

**One PR remains open: #526** `feat/relationship-aware-memory`,
"integrate relationship-aware memory and specify Continuity". Opened
2026-09-02, last updated 2026-09-02T03:37:31Z, 61 files, +7063/−164,
base `main`, mergeability reported UNKNOWN by GitHub (it predates
fourteen merges). It carries two separable things:

1. **Implemented:** a local relationship-aware retrieval contract —
   deterministic FTS/BM25 seeds plus bounded relationship expansion and
   parent hydration, integrated across Ask/Thought, Thread/chat,
   Recipe/Agent, Sequence/Workflow, Workbench and Coder steering, and
   exposed through Desk/Project Memory, HTTP and MCP with Project
   fences.
2. **Designed only:** the "Continuity" program (CF-0) —
   `docs/internal/CORE_MEMORY_DESIGN.md`,
   `CORE_MEMORY_INTEGRATION_SRS.md`,
   `CONTINUITY_CF0_CONTRACT_SRS.md`, `CORE_MEMORY_COUNCIL.md`. Its own
   PR body states CF-1–CF-5 do not exist.

Phase 200 consequence: #526's *retrieval* half is a direct input to
HS-200-10 (working context) and HS-200-11 (preparation); its
*Continuity* half is a competing program specification that Phase 200
has not adopted. Neither is assumed in the obligation map below;
HS-200-10 owns the reconciliation.

### 1.2 Backend (the machine)

| Fact | Value | Source |
|---|---|---|
| Interpreter | Python 3.13.11 (`.venv/bin/python3`) | local probe; hub agrees |
| Toolchain | uv 0.9.28 (Homebrew) | `uv --version` |
| Product version reported by the running hub | 0.4.0 | `GET /api/setup/status` → `version` |
| Platform reported by the running hub | Darwin 25.2.0 (arm64) | `/api/setup/status` section `runtime` |
| Schema version | **76**, `max(version)` in `schema_version` | read-only DB query |
| Canonical schema snapshot | `tests/fixtures/db_schema_canonical.txt`, enforced by `tests/unit/test_db_schema_policy.py` (`test_fresh_db_is_created_at_current_version`, `test_reconcile_is_noop_on_current_db`) | repository |
| Speech/model libraries in the product venv | `mlx` PRESENT, `mlx_whisper` PRESENT, `sounddevice` PRESENT, `llama_cpp` PRESENT, `openai` PRESENT; `whisper`/`faster_whisper` MISSING | `.venv/bin/python3` import probe |
| Same probe under the system python3 | all MISSING | system probe |

The second row matters for HS-200-03: the CI runner's Unit job fails
with `ModuleNotFoundError: No module named 'mlx_whisper'`, a library
that is present in the owner's venv and absent on the runner. That is
an environment difference, not a product defect.

### 1.3 The running processes — two hubs, one database

This is the single most consequential observation of this story, and it
is exactly the condition C1 forbids ("Two processes cannot silently own
the same scheduled work") and P200-A01 requires detecting.

| PID | Started | Listening | Database open |
|---|---|---|---|
| 63921 | Sat 2026-09-05 17:55:03 | 127.0.0.1:54644 | `~/.local/share/holdspeak/holdspeak.db` |
| 81866 | Sun 2026-09-06 10:36:02 | 127.0.0.1:49353 | `~/.local/share/holdspeak/holdspeak.db` |

Both are `.venv/bin/holdspeak web --no-open` from the same checkout at
`/Users/karol/dev/tools/HoldSpeak`. Both hold the same database file
open. Neither reports the other. `/api/runtime/status` on 49353 reports
`runtime_started_at 2026-09-06T10:36:02.691265` and
`url http://127.0.0.1:49353` — it has no field naming a sibling
instance, no instance ID, and no backend revision.

Because the process loads code at start, the hub on 49353 is running the
tree as it stood at 10:36 — the Phase 176 build. main moved to
`bea4176c` at 11:29. **The running product is not the repository HEAD,
and nothing in the product says so.** HS-200-02 owns this.

### 1.4 Frontend

| Fact | Value | Source |
|---|---|---|
| Node | v22.21.0; npm 11.6.2 | local probe |
| Stack | React 19.2.7 on Vite 7.3.6, package `holdspeak-web` 0.0.1 | `web/package.json` |
| Served bundle | `holdspeak/static/_built/`, built 2026-09-06 10:36:01 | filesystem |
| Bundle entry | `assets/index-CtQrk5zn.js` (with `react-SZyUX699.js`, `desk-Coy-boIO.js`, `desk-BQXI9adu.css`, `index-BliYBPWO.css`) | `_built/index.html` |
| Desk vitest baseline | 2336 passed, 0 failed, 0 skipped | Phase 176 `evidence-story-08.md:22`, via `scripts/check_web_baseline.py --run` |
| Web baseline verdict at 176's close | zero branch-new | same evidence |
| TypeScript | **exactly 3 errors**, all `TS2345` in `web/src/desk/pullouts/IntelligenceTruth.test.tsx` at lines 229, 231 and 233 — a `sections` literal typed `unknown[]` not assignable to `Record<string, BriefItemRef[]>` | `npx tsc --noEmit -p web/tsconfig.json`, reproduced 2026-09-06 |

The bundle mtime (10:36:01) and the hub start (10:36:02) agree: the
served bundle and the running backend are the same Phase 176 build.
There is no mechanism that would have detected disagreement.

The three tsc errors are not merely cosmetic debt: they are the **sole**
cause of the red `DeskOS Web Quality` job in CI (§3.4).

### 1.5 Database (the owner's real desk, read-only, counts only)

Database: `~/.local/share/holdspeak/holdspeak.db`, 81,448,960 bytes,
243 tables, `schema_version` max 76, `PRAGMA user_version` 0 (the
product stamps its own `schema_version` table, not the pragma).

| Table | Count |
|---|---|
| meetings | 6 |
| projects | 10 (**1 active, 9 archived**) |
| dictation_journal | 9 |
| dictation_corrections | **0** |
| decisions | 0 |
| decision_records | 0 |
| decision_commitments | 0 |
| desk_decisions | 0 |
| calendar_events | 0 |
| calendar_event_projects | 0 |
| meeting_projects | 0 |
| connector_watches | 32 (gh 19 — 18 enabled; jira 12 — 11 enabled; native 1) |
| watch_rules | 32 |
| watch_provider_connections | 2 (`github` connected, `jira` connected, both transport `connector_pack`) |
| project_sources | 32 (pull_request 17, issue 12, branch_ci 2, meetings 1 — all enabled) |
| project_updates | 5 |
| notes | 17 |
| threads | 10 |
| thread_messages | 26 |
| interview_sessions | **0** |
| action_items | 1 |
| follow_through_proposals | 0 |
| artifacts | 2 |
| recipes | 6 |
| recipe_results | 1 |
| monday_briefs | 1 |
| steward_runs | 3 |
| cadence_loops | 2 |
| scheduled_recordings | 1 |
| intel_jobs | 4 |
| kernel_operations | 889 |
| kernel_parent_runs | 21 |
| first_value_events | 5 |
| profiles | 5 |
| inference_assignments | 10 |
| model_manifests | 0 |
| speakers | 0 |
| segments | 7 |
| activity_records | 164 |
| directories | 6 |
| directory_memberships | 12 |

People live in a separate protected store,
`~/.local/share/holdspeak/people.v1.sqlite3` (mode 0600, two tables
`meta` and `records`, rows encrypted — `key_id`, `nonce`, `ciphertext`).
Its census, by row kind only:

| Kind | Count |
|---|---|
| agenda_item | 2 |
| commitment | 1 |
| one_on_one | 1 |
| relationship | 1 |
| request | 1 |
| **total records** | **6** |

No record content was read from either store.

The DB has 124 sibling `.bak` files from 2026-08-09 to 2026-09-06,
1.3 MB to 80.3 MB. HS-200-02's restore rehearsal has material to work
with and HS-200-38 has a retention question to answer.

### 1.6 Model and runtime identity

From the running hub (`/api/setup/status`, `/api/dictation/readiness`,
`/api/runtime/status`) and `~/.config/holdspeak/config.json`.

Overall setup verdict: **`needs_attention`**. Twenty-six sections pass;
four warn.

| Concern | Observed |
|---|---|
| Speech-to-text backend | `auto` resolves to **`mlx`**; transcription model `base`, `warm_on_start` true, status `loaded` |
| Microphone | pass — default input "MacBook Pro Microphone (index 2)" |
| System audio | BlackHole 2ch detected; ffmpeg on PATH |
| Dictation pipeline LLM runtime | requested `auto` → resolved **`llama_cpp`**, model `/Users/karol/Models/gguf/gemma-4-E4B/gemma-4-E4B-it-qat-UD-Q4_K_XL.gguf`, `model_exists` true |
| Dictation readiness | `ready: true`, `egress_boundary: cloud`, pipeline enabled, budget 800 ms |
| Meeting intelligence | provider `cloud`, "ready", model `gpt-5-mini`, endpoint `https://api.openai.com/v1`, profile "Migrated intel endpoint" (`legacy-intel`) |
| **API key** | **NOT SET.** Both meeting intel and dictation resolve to profile "Migrated intel endpoint", which "requires a key but `$HOLDSPEAK_PROFILE_LEGACY_INTEL_E2FFCBB52936DBD6_KEY` is unset". The key value is not read, not copied, and not present anywhere in this record. |
| Egress state | `intel_egress.can_transmit_offmachine: true`, "Cloud — transcripts are sent to the configured cloud endpoint" |
| Inference assignments | 10 rows in `inference_assignments`; 5 rows in `profiles`; 0 `model_manifests` |

Runs-on destinations, as the hub reports them:

| Destination | Class | State |
|---|---|---|
| This device | same_device | ready |
| LAN Qwen3.6 35B-A3B (8080) | private_network | ready |
| LAN Qwythos 9B Vision (8081) | private_network | ready |
| Migrated intel endpoint | external_service | **unavailable — needs a key** |
| NEW DESTINATION | external_service | **unavailable — no valid endpoint URL** |
| OpenRouter · Quick Qwen | external_service | ready |

The two `private_network` destinations are the LAN llama.cpp box
(historically 192.168.1.43, ports 8080 and 8081). They are *ready* and
require no key. The immediate consequence for HS-200-04 and the owner's
owed walks: **a working local route already exists; the default route
does not work.** That is a configuration repair inside an existing
Settings surface, not new work.

Four warnings, in the hub's own words:

1. `meeting-intelligence-egress` — provider `cloud`; transcripts leave the machine.
2. `trust-destinations` — enabled external destinations: Meeting intelligence, Dictation intelligence, Custom webhook, GitHub issues, Failure alerts.
3. `runtime-profiles` — the unset key above, for both meeting intel and dictation.
4. `runs-on-destinations` — the two unavailable destinations above.

**One inconsistency observed and not resolved here:** the
`web-runtime-auth` section reads "Auth token is set", while the same
response's `trust.auth_token_set` field reads `false`. Both come from
the same request. One of the two is wrong. Recorded for HS-200-02; not
diagnosed in this story.

### 1.7 Sources (presence only)

| Source | Present | Evidence |
|---|---|---|
| GitHub CLI (`gh`) | yes, `/opt/homebrew/bin/gh`, authenticated to github.com for two accounts (`karolswdev` active, `karoldriven`), scopes `gist, read:org, repo, workflow` | `gh auth status` (token value masked by gh itself; not captured) |
| Atlassian CLI (`acli`) | yes, `/opt/homebrew/bin/acli` 1.3.36-stable | `acli --version` |
| Connector packs registered in the product | first-party: `acli_confluence`, `acli_jira`, `calendar_activity`, `firefox_ext`, `gh`, `jira`, `meeting_context` | `/api/setup/status` section `connector-packs` |
| Provider connections in the database | 2, both `connected`: `github`, `jira` | read-only DB query |
| Calendar sources configured | **0** (`config.calendar.sources` is an empty list; `calendar_events` is 0 rows) | config + DB |
| People keystore | native macOS Keychain | `/api/setup/status` section `people-keystore` |
| Mesh nodes | none has ever served this mesh | `/api/setup/status` section `mesh-edges` |
| Devices | `GET /api/devices/health` → `{"devices": []}` | hub |

Phase 175 shipped the calendar wire; the owner has **no calendar source
configured and zero events**. HS-200-22 must therefore treat calendar as
optional for first value (as its own scope already says) and cannot
assume an available event stream.

### 1.8 The twice-run requirement

The story's test plan requires the identity observation to be reproduced
twice against the actual selected runtime. A single read-only script
(`observe.sh`, kept in the session scratchpad; it performs only
`git rev-parse`, version probes, `sqlite3 …?mode=ro` counts, `lsof`,
and authenticated GETs) was run twice against the hub on 127.0.0.1:49353.

**Result: the two runs are byte-identical (`diff` empty).** Both report
main `bea4176c`, branch `feat/phase-200-g0`, Python 3.13.11, Node
v22.21.0, package 0.4.0, bundle built 2026-09-06T10:36:01 with entry
`assets/index-CtQrk5zn.js`, **two** `holdspeak web` processes (63921 on
:54644, 81866 on :49353), 243 tables, schema 76, the counts of §1.5,
`hub.version` 0.4.0, `hub.overall needs_attention`, and the model rows
of §1.6.

No write of any kind was issued. The owner's database counts are
unchanged by this story.

---

## 2. Inherited proof obligations — one destination each

Historical records are preserved and pointed at, never rewritten. No
earlier story is flipped, closed, or re-stated as complete by this
story.

**Delivery owner:** unassigned for every row below. The Phase 200
status file carries "Delivery owner: unassigned until implementation
starts"; that placeholder is the owner of record until the phase names
one, and each row names the destination story that will carry the
obligation when it does.

### 2.1 The owed attended walks

Every walk from Phase 170 onward was run by a read-only runner on the
owner's desk. None was the owner's own attended walk. The historical
record is `docs/internal/project-rooms/HANDOVER-MUADDIB.md` §4 "Owed to
the owner" and each phase's `current-phase-status.md`.

| Owed | Historical record | Phase 200 destination | Delivery owner |
|---|---|---|---|
| 170 attended walk (Concierge, the four Tuesday faces) | phase-170 status; handover §4 | HS-200-04 (first value and model readiness), evidence leg | unassigned |
| 171 attended walk (Heartbeat, needs-you, notifications) | phase-171 status | HS-200-15 (attention and notifications) | unassigned |
| 172 attended walk (the loop closes; meeting → proposals) | phase-172 status | HS-200-12 (meeting to reviewed outcomes) | unassigned |
| 173 attended walk (Steward's hand and voice) | phase-173 status | HS-200-11 (Project preparation brief) | unassigned |
| 174 attended walk (Reach) | phase-174 status | HS-200-31 (durable execution host) | unassigned |
| 175 attended walk (Calendar and the Clock) | phase-175 status + final-summary | HS-200-22 (calendar dependency integration) | unassigned |
| 176 attended walk — story 06, the only story not closed in 176 | `pm/roadmap/holdspeak/phase-176-the-speak-loop/story-06-the-walk.md`, status CLOSED 7/8 | HS-200-05 (physical voice, correction, custody) | unassigned |

HS-200-16's two-day owner sequence is the gate at which the accumulated
walk debt is discharged as one integrated leg rather than seven
separate re-runs. Phase 176's story 06 stays open in Phase 176; Phase
200 does not close it.

Beat 0 of the 176 walk is already known to be blocked: the Speak face
reads `DICTATION · GPT 5 mini · API.OPENAI.COM · KEY NOT SET`. §1.6
shows two ready LAN destinations that would unblock it without a key.

### 2.2 The 172–174 owner questions

Unanswered. Recorded in `HANDOVER-MUADDIB.md` (Muad'Dib XI–XIII).
Phase 200 does not answer them; it gives each one a story that will put
it back in front of the owner with built behavior beside it.

| Question | Origin | Destination |
|---|---|---|
| The two-prefix vocabulary `Decide:` / `Confirm:` | 172, counsel | HS-200-09 (daily workflow design) |
| The 1:1 card's summary-then-Now shape; `2 PRS WAITING ON ANIA` with the name in caps | 172, counsel | HS-200-14 (People preparation boundary) |
| Jira assignments on the 1:1 card — keep? | 172, counsel | HS-200-14 |
| The nudge's attribution — tool named, no personal name | 173, counsel | HS-200-11 / HS-200-21 (Steward voice in the brief and its cadence) |
| `REVIEW WAIT` in days from `createdAt` — acceptable approximation? | 173, counsel | HS-200-07 (coverage: an approximation is a support state) |
| The 7-day cooldown shown as `NUDGED N D AGO` — keep? | 173, counsel | HS-200-15 (notification transitions) |
| Confluence: blog posts vs page search (the CLI cannot list or search pages) | 174 | HS-200-37 (connector expansion decision, evidence-based) |
| The awake-Mac prerequisite vs a lid-open V0 | 174 | HS-200-31 (durable execution host) |
| Credentials in memory, re-issued after every restart, vs persisted | 174 | HS-200-32 (durable credential lifecycle) |
| The leg from the .43 box itself — when? | 174 | HS-200-31 |
| The remote listener on for his desk once a credential exists | 174 | HS-200-32 |

### 2.3 Phase 176 story 06 and the BACKLOG "AG" remainders

`pm/roadmap/holdspeak/BACKLOG.md` §"AG. Phase 176 remainders" parks
twelve counsel P2s (C5–C13, C15, C16), two walk findings, two re-read
notes and a docs item. Their destinations:

| Remainder | Substance | Destination |
|---|---|---|
| C5 | `APPLIED` names a text rule that changed nothing (case-only rule) | HS-200-06 — a receipt that claims an effect that did not occur is a support-semantics defect |
| C6 | the intent nudge marks `corrected=True` when nothing changed | HS-200-06 |
| C7 | 17 of 24 `MIC_ALLOWLIST` entries suppress nothing | HS-200-03 (fence hygiene) |
| C8 | a pure insertion/deletion becomes a whole-sentence rule, still receipted `TAUGHT` | HS-200-05 |
| C9 | the Learned wing subscribes to `learning_event`, which a `text` teach never emits | HS-200-05 |
| C10 | after `Forget`, an unresolved id renders an empty HEARD/SAID well | HS-200-05 |
| C11 | the footer receipt survives a wing switch and masks `N TODAY` | HS-200-05 |
| C12 | a teach that 500s after the store wrote shows `REFUSED · nothing written` | HS-200-05 — a false-refusal receipt |
| C13 | a purely lexical text rule silently does nothing when the pipeline is disabled or the runtime is unloaded | HS-200-05 |
| C15 | the Journal row shows the transcript while search also matches `final_text` | HS-200-05 |
| C16 | a frame arriving between the initial GET and `setRows` ordering | HS-200-05 |
| walk finding: empty-transcript row renders a blank primary | needs a `NO TEXT` token | HS-200-05 |
| walk finding: `KEY NOT SET` on the Speak face | honest face, unconfigured route | HS-200-04 |
| re-read N2: `browser` vs `dictation` source naming for his voice | one word, one meaning | HS-200-05 |
| re-read C3 note: the widened `_looks_secret` guard drives product-wide redaction | watch for a surprising redaction | HS-200-06 |
| docs: `docs/DICTATION_PIPELINE_GUIDE.md` §12 describes the retired `/dictation` page; two stale code comments | a rewrite is its own story | HS-200-38 (public procedures on the packaged candidate) |

### 2.4 The "CI runner environment" backlog entry

`BACKLOG.md:1087` records, from 2026-09-05, that the GitHub Actions
Unit job fails on a fixed environment set and that "the merge gate is
the local CI-shape suite (6 inherited) + his word; Actions is not read
as a gate until this is paid. A phase of its own."

Phase 200 makes that phase HS-200-03. §3 is its ledger. The backlog
entry itself is preserved; HS-200-03 is its destination.

### 2.5 Repository structural debt

`.githooks/dw check holdspeak` reports **six** issues in the working
tree today, **five** of them on committed main:

| Issue | On committed main? |
|---|---|
| `phase-101-the-native-innards/evidence-story-04.md`: evidence exists but matching story is not done | yes |
| `phase-152-the-hands`: all stories done, `final-summary.md` missing | yes |
| `phase-153-the-practice`: all stories done, `final-summary.md` missing | yes |
| `phase-154-the-call`: all stories done, `final-summary.md` missing | yes |
| `phase-156-the-front-door`: all stories done, `final-summary.md` missing | yes |
| `phase-170-the-great-pass/evidence-story-03.md`: evidence exists but matching story is not done | **no — untracked working-tree file, never committed** |

BASELINE.md's planning text said five. Five is still right for main;
the sixth is an untracked artifact of another lane's work in the shared
tree and is not this story's to move. No summary is fabricated and no
old story is flipped to make the checker green. Destination: HS-200-40
(phase close) reviews whether the four missing summaries are written by
their own phases or explicitly retired.

---

## 3. The current CI failure ledger

Source: GitHub Actions run **34007939416** on main `7a47904e`
(2026-09-06T03:01:19Z) — the most recent *completed* run. The two runs
after it (`8fb56d97` → 34048521091, `bea4176c` → 34048777373) were
still `in_progress` and `queued` at observation time and cannot be
classified.

Job outcomes: Documentation Navigation **success**, Linux Smoke
**success**, Unit Tests **failure**, Integration Tests (macOS)
**failure**, E2E Tests (macOS) **failure**, DeskOS Web Quality
**failure**, All Tests Summary **failure**.

Totals: Unit `30 failed, 8618 passed, 9 skipped in 4529.68s`;
Integration `3 failed, 1026 passed, 27 skipped in 2268.22s`;
E2E `2 failed, 135 passed, 46 skipped, 134 deselected in 1786.17s`.

Reproduction, unless a row says otherwise:
`HOME=$(mktemp -d) uv run pytest -q <nodeid>` from the repository root,
excluding `tests/e2e/test_metal.py`.

### 3.1 Unit Tests — 30 failures, by family

| # | Identity | Observed reason | Class | Repair story |
|---|---|---|---|---|
| 1–2 | `tests/unit/test_ask_grounding_claims.py::test_flags_an_unsupported_claim_and_not_a_supported_one`, `::test_no_grounding_claims_when_no_context_material` | `assert 409 == 200` | inherited (fails identically on main per Phase 176's classification) | **HS-200-06** — it is literally the claim-support seam |
| 3 | `tests/unit/test_ask_runner_migration.py::test_ask_uses_versioned_contract_hash_runner_and_staged_projection` | `ServiceError: model file not found: ~/Models/gguf/Qwen3.5-9B-Instruct-Q6_K.gguf` | runner environment — a local model file the runner does not have | **HS-200-03** (skip marker `requires_local_model`) |
| 4–5 | `tests/unit/test_desk_seed.py::test_fresh_db_seeds_exactly_the_manifest`, `::test_reset_tombstones_clutter_and_reseeds` | seed manifest set mismatch | inherited product/fixture drift | **HS-200-03** |
| 6–7, 8–14 | `tests/unit/test_dictation_session_admission.py` — 9 failures: `test_authorized_pre_session_warm_runs_as_the_preload_service`, `test_mutable_preload_knobs_cannot_change_the_frozen_parentless_source`, `test_pre_session_warm_uses_the_assigned_speech_route_not_the_legacy_knob` (`SpeechSessionRefused: no_assignment`); `test_phase_d_default_cold_wake_runs_its_complete_routed_bundle` (`ModuleNotFoundError: No module named 'mlx_whisper'`); `test_phase_d_couples_speech_and_writing_markers_for_one_complete_pipeline`, `test_phase_d_day_one_standalone_speak_to_fill_uses_the_migrated_route`, `test_phase_d_egress_refuses_a_missing_frozen_transcription_entry`, `test_phase_d_local_transcription_route_reports_local_egress`, `test_phase_d_nonlocal_historical_speech_refuses_before_construction_or_dispatch[mesh]` and `[private_network]`, `test_phase_f_routed_text_entry_uses_provider_route_egress_without_transcription` | no speech engine and no speech assignment on the runner; `mlx_whisper` absent | runner environment | **HS-200-03** (`requires_local_speech` skip) with **HS-200-05** owning any that still fail with a real engine |
| 15–18 | `tests/unit/test_phase143_speech_lifecycle_adoption.py` — 4 failures, all `SpeechSessionRefused: no_assignment` | same cause as above | runner environment | **HS-200-03** |
| 19–21 | `tests/unit/test_hs175_calendar_wire.py::TestHttpsSourceReceipt::test_sweep_receipt_names_https_host`, `TestSweepRunsCalendarRefresh::test_sweep_calls_refresh_and_receipts`, `::test_sweep_receipts_applied_false_when_no_sources` | `assert None == 'calendar.refresh'`; "receipt must carry calendar sub-receipt"; `KeyError: 'calendar'` | **NOT in Phase 176's inherited list** — these appeared with the 175 merge (`aa278604`) and/or the R1 matcher fix (`7d897302`) | **HS-200-22** — a real calendar-wire regression to diagnose, not an environment skip |
| 22–23 | `tests/unit/test_kernel_effect_fence.py::test_kernel_broker_has_zero_driver_specific_conditionals`, `::test_kernel_broker_modules_stay_within_line_budget` | density/architecture fences over budget | inherited | **HS-200-03** (pay or re-baseline the fence, explicitly) |
| 24 | `tests/unit/test_product_copy.py::test_primary_copy_has_no_prohibited_operational_drift` | 29 pre-existing copy offenders | inherited | **HS-200-03** to make it legible; **HS-200-09** owns the copy itself |
| 25–27 | `tests/unit/test_project_mcp_commands.py::test_all_command_tools_discoverable`, `::test_no_sql_in_project_family_module`, `tests/unit/test_project_mcp.py::test_project_tools_are_discoverable_with_versioned_schemas` (`assert 42 == 38`) | the project MCP tool set drifted from its recorded schema census | inherited | **HS-200-03**; the census update belongs with whatever story next changes the project tools |
| 28 | `tests/unit/test_web_runtime.py::test_run_web_runtime_warms_transcriber_on_start` | `assert False` — no transcriber to warm on the runner | runner environment | **HS-200-03** |

The Unit job reports 30 failures and 30 `FAILED tests/…` lines; the
table groups them into 28 rows because two families are collapsed. The
node IDs above are quoted verbatim from the run's failed-log extract.

**Two divergences between Phase 176's local classification and this
Actions run, both recorded honestly rather than reconciled here:**

- `tests/unit/test_hs173_nudge_wire.py` is named in Phase 176's
  twelve inherited failures but does **not** appear anywhere in this
  Actions run's failed log (zero matches). Either it is environment-
  sensitive in the other direction, or the local run and the runner
  disagree. Not diagnosed by this story.
- `tests/unit/test_hs175_calendar_wire.py` (3 failures) and
  `tests/integration/test_web_activity_api.py` (3 failures) appear on
  the runner and are **not** in Phase 176's list at all. They are the
  ledger's most likely genuine product defects and are the reason
  HS-200-03 must re-derive the ledger from a full local run rather than
  inheriting either list.

None of the failures above were reproduced locally by this story — the
brief scoped this worker to documentation checks only. HS-200-03 owns
the reproduction.

### 3.2 Integration Tests (macOS) — 3 failures

| Identity | Observed reason | Class | Repair story |
|---|---|---|---|
| `tests/integration/test_web_activity_api.py::test_briefing_endpoint_returns_latest_briefing_and_run` | `assert None is not None` | not in Phase 176's inherited list | **HS-200-03** to classify; **HS-200-11** if the briefing path is genuinely broken |
| `tests/integration/test_web_activity_api.py::test_extension_events_endpoint_creates_records` | `assert 0 == 1` | as above | **HS-200-03** |
| `tests/integration/test_web_activity_api.py::test_project_briefings_endpoint_empty_when_no_runs` | `assert 404 == 200` | as above | **HS-200-03** |

These three are a single-module cluster and were not covered by Phase
176's classification, which was drawn against a local run. They are the
clearest candidate for a real product defect in the ledger.

### 3.3 E2E Tests (macOS) — 2 failures, and the rotating flaky family

| Identity | Observed reason | Class | Repair story |
|---|---|---|---|
| `tests/e2e/test_hs144_door_glass.py::test_meetings_deep_link_waits_for_registered_surface_x15` | `AssertionError: ['console: Failed to load resource: … 404 (Not Found)']` | inherited — fails identically on a main worktree (Phase 176 serial re-run) | **HS-200-03** |
| `tests/e2e/test_hs153_practice_glass.py::test_guardrail_row_renders_and_deny_focused` | `Expected deny, got 'allow' at 1440` | inherited — fails identically on main | **HS-200-03**, with **HS-200-09** owning the guardrail's face if the product is wrong |

The rotating flaky rig family, from Phase 176's serial re-run at
`5a0a29f5` (`current-phase-status.md:321-343`): `hs154` call, `hs170`
speak-unset, `hs172` settings-meetings 393 and `hs176` loop 393 were
**xdist-only** failures — green serially. `tests/e2e/test_hs175_arrival_glass.py`'s
overflow test flipped (green on main, red once serially, green on
retry), and the same file produced the run's single setup error,
`test_arrival_cancel_idle_event_born`. Destination: **HS-200-03**,
which must decide whether the critical-journey suite runs serially or
whether the rigs are made xdist-safe.

### 3.4 DeskOS Web Quality — 1 failure, fully diagnosed

The job runs `npm run typecheck` → `tsc --noEmit` and exits 2 on
**exactly the three `TS2345` errors of §1.4**, all in
`web/src/desk/pullouts/IntelligenceTruth.test.tsx` (229, 231, 233).
Reproduced locally at `bea4176c`, identical output.

This is the cheapest red job on the board: one test file's fixture
literal needs a type. Destination: **HS-200-03**.

### 3.5 Ledger shape

| Class | Count | Primary destination |
|---|---|---|
| Runner environment (speech engine, `mlx_whisper`, local model file) | 15 | HS-200-03 |
| Inherited fences and censuses (kernel density ×2, product copy, project MCP ×3, desk seed ×2) | 8 | HS-200-03 |
| Claim-support seam (ask grounding ×2) | 2 | HS-200-06 |
| Calendar wire (hs175 ×3) | 3 | HS-200-22 |
| Web activity API (integration ×3) | 3 | HS-200-03 → possibly HS-200-11 |
| E2E inherited (hs144, hs153) | 2 | HS-200-03 |
| TypeScript (web quality) | 3 errors, 1 job | HS-200-03 |
| Rotating flaky rigs | 5 rigs + 1 setup error | HS-200-03 |

**35 failing test identities across four red jobs**, plus 3 tsc errors,
plus the flaky family. Phase 176 classified 12 as inherited against a
main worktree; this ledger is drawn against GitHub Actions and is
larger because the runner lacks the owner's speech and model
environment.

---

## 4. The pilot Project

The Phase 200 charter assumes "one real transformation stream". The
handover records "his desk holds zero Projects". That is **no longer
literally true**, and the correction matters.

The owner's database holds **10 Project rows: 1 active, 9 archived.**

| id | archived | lifecycle | created | revision | sources | watches | updates | observations | proposals | reviews |
|---|---|---|---|---|---|---|---|---|---|---|
| `proj-10b35905777c` | no | active | 2026-09-04 | 2 | 4 (1 issue, 1 meetings, 2 pull_request) | 4 | 2 | 7 | 6 | 1 |
| `proj-102233e71c47` | yes | archived | 2026-09-03 | 4 | 2 | 2 | 3 | — | — | — |
| `proj-4ed6be467d96` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-81fa4a0532a5` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-4db9f394af6f` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-f8a971e19079` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-58aa849b3207` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-e508bb832077` | yes | archived | 2026-09-04 | 2 | 3 | 3 | 0 | — | — | — |
| `proj-22f86af7fb2e` | yes | archived | 2026-09-04 | 2 | 4 | 4 | 0 | — | — | — |
| `proj-fd894f49bbd3` | yes | archived | 2026-09-04 | 2 | 4 | 4 | 0 | — | — | — |

Names, descriptions and any other Project content were not read.

What the shape says, and what it does not:

- All ten were created on 2026-09-03 and 2026-09-04 — the Phase 168/169
  Connections-Door and Room build window. Nine were archived, which is
  the pattern of build-and-clear, not of a stream being worked.
- The one active Project is the only one with observations (7),
  proposals (6), a review (1), and a `meetings` source. It is the only
  candidate on the desk.
- **Its provenance is unknown to this story.** It may be a real stream
  the owner started, or a Room left standing from the 169 walk. Nothing
  read-only distinguishes them, and this story will not read its name or
  content to guess.
- Zero decisions, zero decision records, zero commitments, zero
  calendar events, zero interview sessions. Whatever that Project is, no
  daily loop has run on it.

**Therefore the pilot Project is NOT selected by this story.** The
acceptance criterion "Select one real pilot Project and sources when
available. Keep unavailable employer context explicit" is met by
recording that it is not available and asking, not by inventing one.

### The open question for the owner

> **Which real transformation stream is the Phase 200 pilot, and which
> sources belong to it?**
>
> Your desk holds one active Project (`proj-10b35905777c`, created
> 2026-09-04, 4 sources, 7 observations, 6 proposals, 1 review) and
> nine archived ones from the same two days. We cannot tell from the
> outside whether the active one is a real stream of yours or a Room
> left standing from the Phase 169 build.
>
> Three things are needed before HS-200-11 can produce a brief you would
> keep:
>
> 1. **The stream.** Is `proj-10b35905777c` it? Should it be renamed and
>    re-scoped? Or should a new Project be created for the real work?
> 2. **The sources.** GitHub and Jira are connected (32 watches, 32
>    project sources across the ten Projects). Which repositories,
>    boards or issue scopes belong to the pilot stream? Confluence is
>    available through `acli` but its CLI cannot list or search pages
>    (the open 174 question).
> 3. **The employer boundary.** ACCEPTANCE.md forbids inferring
>    employer sources, organizational decision rights, or financial
>    savings from personal fixtures. If the real stream is employer
>    work that cannot enter this database, say so and the pilot uses a
>    personal stream with the limitation recorded — the sample is then
>    honestly narrower, not silently substituted.
>
> Until this is answered, HS-200-11, HS-200-16 and HS-200-23 cannot
> start their owner-facing legs. Everything else in G0 and most of G1
> proceeds without it.

There is a second, smaller decision attached: the four warnings of §1.6
mean the default model route does not work (no key), while two LAN
destinations are ready. HS-200-04 can repair that through the existing
Settings surface without an owner decision — but *which* route the
pilot's briefs should run on is the owner's call, because it decides
whether transcripts leave the machine.

---

## 5. The obligation map

Disposition vocabulary, as EXECUTION.md defines it: **reuse and prove**
· **integrate** · **repair a demonstrated gap** · **new work** ·
**defer**. Every row is either an existing implementation named by
`file:line`, route, or MCP tool, or an explicit MISSING.

Read this map with one caution: an EXISTS row attests that code is on
disk at `bea4176c`. It does not attest that the path runs, that the
running hub loaded it, or that its output is useful. §1.3 shows those
are different questions.

### 5.1 C1 — Runtime identity and custody

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| Backend build identity | **MISSING** — the only identity is the package version string `holdspeak.__version__` (`holdspeak/__init__.py:52`), surfaced at `holdspeak/setup_status.py:270,275`. A version string does not change per commit, so it cannot distinguish an old process from a new checkout. `holdspeak/web/routes/system/health.py` carries no build identity at all. | new work | HS-200-02 |
| Frontend bundle identity | **MISSING** — `web/vite.config.ts` defines no build id or sha; `web/package.json:3` is a static `0.0.1`; nothing in `web/src` reports a bundle stamp. | new work | HS-200-02 |
| Process start | EXISTS `holdspeak/web_runtime.py:173` (`runtime_started_at`), serialized `holdspeak/runtime/activity.py:126,157-158`, served through `/api/runtime/status` (`holdspeak/web/routes/system/health.py:41-63`). Observed live in §1.3. | reuse and prove | HS-200-02 |
| Opaque database identity | **MISSING** — `instance_id` exists only for delivery nodes (`holdspeak/delivery/node_link.py:907`); `_database_id` (`holdspeak/delivery/direct_gesture_input.py:24`) is a Python `id()` cache key, not durable. | new work | HS-200-02 |
| Schema version exposed | PARTIAL — the reader exists (`holdspeak/db/core.py:48` `read_schema_version`, stamped by `holdspeak/db/reconcile.py:707-711` from `SCHEMA_VERSION` at `holdspeak/db/__init__.py:72`), but `schema_version` appears **nowhere** under `holdspeak/web/`. No route exposes it. | repair a demonstrated gap | HS-200-02 |
| Config revision exposed | **MISSING** — `CONFIG_VERSION = 1` (`holdspeak/config/core.py:76`) is a format version coerced forward at `:100-116`, not a per-edit revision, and no diagnostics route reports it. | new work | HS-200-02 |
| Two runtimes / stale bundle detected | **MISSING, and demonstrated live** — §1.3. No pidfile, no single-instance guard, no DB-ownership lock on the hub; `holdspeak/web_server.py` does not diagnose a bind conflict. The tree's only two-owner detection is the mesh worker DB lock (`holdspeak/db/mesh_worker.py:80`, `fcntl.flock … LOCK_EX|LOCK_NB` → `WORKER_OWNER_LOCKED`), scoped by its own docstring to "one worker database … and nothing else". | new work — **the highest-value G0 repair** | HS-200-02 |
| Backup and restore | EXISTS — `holdspeak/db/core.py:81` `backup_database`, `:96` `restore_database` (phase 50); CLI `holdspeak backup` / `holdspeak restore` at `holdspeak/main.py:424,428-442`, dispatch `:563-571`, implementation `holdspeak/commands/backup.py:24,41`; restore snapshots the current DB first; test `tests/unit/test_backup_restore_cli.py`. **Gap:** covers `DEFAULT_DB_PATH` only — not attachments, not the People sidecar (`people.v1.sqlite3`), not the Keychain. | reuse and prove + repair | HS-200-02, HS-200-38 |

### 5.2 C2 — Claim meaning

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| The overstating parser | EXISTS and is the defect: `holdspeak/services/project_update_service.py:548` `_parse_model_output` sets `verified=True` at **`:590`** and `verified=False` at **`:600`**, gated only by `valid_refs = [r for r in cited if … r in inventory_refs]` at **`:583`** — ref existence in the inventory, nothing more (phase 162, HS-162-03). | repair a demonstrated gap | HS-200-06 |
| Kind / support / acceptance as independent axes | **MISSING** — no `ClaimSupport`, `claim_kind`, or `support_state` anywhere in `holdspeak/`. The only `Claim` is the boolean-verified dataclass at `project_update_service.py:89-116`. The three axes are today collapsed into two unrelated one-dimensional fields: a `verified` bool, and `lifecycle`/`review_state` strings. | new work | HS-200-06 |
| Claims serialization and persistence | EXISTS — `Claim.to_dict()` `project_update_service.py:107-116`, call sites `:819`, `:899`; persisted as opaque `claims_json` in `holdspeak/db/updates.py` (insert, draft-update, supersede) into `project_updates` (`holdspeak/db/schema.py:3932`). **Consequence:** a support migration is a JSON blob rewrite, not a column add. | integrate | HS-200-06 |
| Decision acceptance and supersession | PARTIAL — `DecisionRecordService.supersede` `holdspeak/services/decision_record_service.py:263` writes `lifecycle='superseded'` at `:297` with a field-change receipt at `:301`; `due_for_review` `:338`. **No accept/reject verb on the service.** The nearest acceptance state is the meeting action item's `review_state` (`holdspeak/db/meetings.py:45`, transition `:947-960`, surfaced `holdspeak/services/project_service.py:355`). | repair a demonstrated gap | HS-200-06, HS-200-13 |
| Conservative migration of old `verified` values | **MISSING** | new work | HS-200-06 |

### 5.3 C3 — Evidence manifests and working context

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| Versioned evidence manifest for a brief | **MISSING** — nothing binds a brief to source refs plus observed revisions and times. The best precedent to copy is the Thought refinement frozen attachment: `holdspeak/services/refinement_thought_service.py:373-374,406` (`frozen_aggregate_revision`, `frozen_working_revision`, `frozen_attachment_revision`, `frozen_attachment_sha256`, `admission_sha256`) over `refinement_attachment_revisions` (`holdspeak/db/schema.py:959`) and siblings (phase 141). It freezes *which records* and *a content hash*, not observation times or external source revisions. | new work, modelled on an existing pattern | HS-200-11, HS-200-25 |
| Qualified refs registry | EXISTS `holdspeak/db/relationships.py:23` `qualified_ref`, validated against the hard-coded frozenset at `:17-20` (`meeting, transcript, artifact, decision, note, knowledge, zone, project, persona, workflow, sequence, integration`); `ValueError` on an unknown kind at `:29-31`. Registration = editing that literal. A second, thread-local ref vocabulary exists unvalidated in `thread_refs` (`holdspeak/db/schema.py:3614`, writer `holdspeak/db/threads.py:563`, kinds include `import_hash` at `:636`). | integrate — and reconcile the two vocabularies | HS-200-10, HS-200-25 |
| Interview → Note/Thought/Project promotion | **MISSING** — `InterviewService` (`holdspeak/services/interview_service.py:31`) holds facts and suggestions in reduced thread state only (`_reduce` `:154`, `_fact` `:199`, `_suggestion` `:224`, `_invalidate` `:194`) and has no promote method. Adjacent promotion machinery exists but is not wired to Interview: `RefinementThoughtService.adopt_note` `holdspeak/services/refinement_thought_service.py:184`, `DecisionLifecycleService.promote` `holdspeak/services/decision_lifecycle_service.py:55`. Context attach/detach/refresh by ref exists at `holdspeak/services/refinement_context_service.py:657,665,673`. | new work over existing seams | HS-200-10 |
| Protected People storage boundary | EXISTS `holdspeak/people/store.py:19` (`DEFAULT_PEOPLE_DB_PATH`), sole writer `EncryptedPeopleStore` `:49` ("the only persistence authority for People confidential payloads"), AES-GCM via `holdspeak/people/crypto.py`, policy `holdspeak/people/policy.py:42` `PeoplePolicy.allows` (`:48-58`: leader-private never leaves; only capability-gated MCP read/write of shared intent); MCP boundary `holdspeak/mcp/families/people.py:249,254,462` (phase 138). Census in §1.5: 6 records. | reuse and prove | HS-200-14 |
| Retention / revocation evaluated at disclosure and at new execution | PARTIAL — one real disclosure-time recheck exists, Interview-scoped: `InterviewService._prune_unavailable_facts` `holdspeak/services/interview_service.py:67-81` re-queries the source message and drops facts whose quote is gone, cascading to suggestions at `:81` — but it **drops silently** and names no coverage gap. Activity retention is a blunt `retention_days` sweep (`holdspeak/activity_history.py:236-238`). Grounding hydration returns a `missing` list (`holdspeak/grounding.py:149-153,322-326`) — a hydration failure, not a retention evaluation. **MISSING:** any evaluation at new execution; any named coverage gap. | repair a demonstrated gap | HS-200-03 (the fence), HS-200-10 |

### 5.4 C4 — Coverage and attention

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| The swallowed failed Room read | EXISTS and is the defect: `holdspeak/services/needs_you_aggregate.py:29` `build_aggregate` swallows a failed read at `:54-55` (`except Exception: continue`) and a non-`ok` section at `:57-58`, then asserts `"stale": False` at `:105`; the cache re-asserts it at `:203`. A skipped Room leaves **no trace in the payload** (phase 170/171; see `holdspeak/web/routes/projects.py:378`). | repair a demonstrated gap | HS-200-07 |
| Per-source coverage record (available/stale/failed/forbidden/unavailable, observation time, repair path) | **MISSING.** `NeedsYouCache` exists (`needs_you_aggregate.py:144`, durable dirty marker `mark_needs_you_dirty` `:113`, phase 172). The only source-level state anywhere is the Room section tag `ok \| degraded \| absent` minted at `holdspeak/services/project_service.py:473-480,247` — and `build_aggregate` discards it. | new work over an existing signal | HS-200-07 |
| Five-item cap with an honest remainder | PARTIAL — route `GET /api/desk/needs-you` (`holdspeak/web/routes/projects.py:436`) exists; the face `web/src/desk/chair/ChairHome.tsx:636` → `NeedsYouSection` `:754` renders **all** unmuted items with no cap and no "N more". The only capped section on the arrival is the brief (`BRIEF_CAP = 3` at `:1025`, overflow verb `:1074-1081`) — an existing pattern to copy. | repair a demonstrated gap | HS-200-15 |
| Stable source ids and dedup | **MISSING** — `build_aggregate` sets `"ref": item.get("title", "")` (`:59-60`), and the face concatenates door and Room items with a severity sort only (`ChairHome.tsx:411-420`). Nothing dedups across meeting, Watch and commitment projections. | new work | HS-200-15 |
| Notification transitions, quiet hours, per-Project settings | PARTIAL — `HeartbeatService` `holdspeak/services/heartbeat_service.py:76`; quiet hours `in_quiet_hours` `:260`; the decision `_run_notification_decision` `:537`; the edge persisted `_persist_edge` `:631` (phase 171). **The transition detector is count-only:** `EdgeDetector.should_fire` `holdspeak/desktop_notify.py:46-50` is `count > last_notified_count` — so C4's explicit case, *a changed item set with the same count*, is not considered. **Per-Project settings MISSING:** `get_settings()` `:97` is one global row. | repair a demonstrated gap | HS-200-15 |

### 5.5 C5 — Prepared recipe configuration

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| A versioned recipe catalog | **MISSING as specified.** `RecipeService` `holdspeak/services/recipe_service.py:49` over the `recipes` table (`holdspeak/db/schema.py:1242`) is *user-authored prompt personas* — `name/system_prompt/user_template/tools_json/kb_id/profile_id/kind`, no version, no descriptor, no catalog. MCP `recipe.list/get/run/chat` (`holdspeak/mcp/tools.py:225-228`, dispatch `:693-701`) operates on that table. The owner's desk holds 6 such rows and 1 result (§1.5). **The name is taken; the concept is not built.** | new work — and settle the naming collision in HS-200-17 | HS-200-17 |
| Recipe 1: meeting preparation | EXISTS as a service to compose — `MondayBriefService` `holdspeak/services/monday_brief_service.py:122`, `generate` `:182`, window `:134`, lookahead `:163`, calendar collector `:746`, meeting-watch collector `:855` (phase 175 widened the window). MCP `monday_brief_generate`, `monday_brief_get`. | reuse and prove | HS-200-11, HS-200-17 |
| Recipe 2: decision and commitment review | EXISTS — `FollowThroughService` `holdspeak/services/follow_through_service.py:114` (`board` `:128`, `commit_decision` `:263`, `complete` `:346`); `DecisionRecordService` `holdspeak/services/decision_record_service.py:20`, review interval `due_for_review` `:338`. | reuse and prove | HS-200-13, HS-200-17 |
| Recipe 3: weekly Project update | EXISTS — `ProjectUpdateService` `holdspeak/services/project_update_service.py:703` (`draft_update` `:826`, `draft_update_command` `:1013`, `publish_update` `:1196`); `ProjectStewardService` `holdspeak/services/project_steward_service.py:97` (`run_due` `:127`, `run_once` `:345`) over `steward_runs` (`schema.py:3980`) and `steward_policies` (`:3956`) (phase 163). His desk: 5 updates, 3 steward runs. | reuse and prove | HS-200-17, HS-200-21 |
| A configuration plan record (id/revision, recipe version, target refs + expected revisions, source scope, route policy, trigger, limits, prerequisites, authority, per-step state) | **MISSING.** The nearest durable plan records are inference-side (`inference_route_plan_commands` `schema.py:2697`, `inference_operation_route_request_plan_commands` `:2789`) and steward-side (`steward_policies` `:3956`); none carries the recipe fields or the `proposed → prepared → applying → applied → verified → paused → needs-repair` state. | new work | HS-200-18 |

### 5.6 C6 — Setup transactions and recovery

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| ProjectSetupService and its session lifetime | EXISTS `holdspeak/services/project_setup_service.py:169` (traces INT-001..006, phase 159 HS-159-03): `start_setup` `:206`, `get_setup` `:228`, `answer` `:275`, `suggest` `:328`, `finalize` `:641` (one transaction, ACT-004). Expiry `SESSION_TTL = 24h` at `:54`, enforced `:241-249`. Tables `project_setup_sessions` `schema.py:3701`, `project_setup_answers` `:3715`, `watch_setup_proposals` `:3729`. MCP `project_setup_start/answer/suggest/resume/finalize`. His desk: **0 interview_sessions**. | reuse and prove | HS-200-19 |
| Durable command identity before the first effect | PARTIAL, **and the gap is exactly where C6 needs it.** `project_commands` (`schema.py:1538`, `request_hash / status / result_json / error_code`, CRUD `holdspeak/db/projects.py:772-855`) exists, and replay-or-conflict is implemented in **exactly two places** — `holdspeak/web/routes/steward.py:72-87` and `holdspeak/mcp/families/project.py:1568-1584`. Three writers insert rows without checking replay (`project_service.py:3896`, `project_delta_service.py:1269`, `project_update_service.py:1297`). There is **no unique index on `request_hash`**, and `ProjectSetupService` mints **no command envelope at all**. | repair a demonstrated gap | HS-200-19 |
| Per-step receipts and read-back | EXISTS for the Steward only — `steward_steps` (`schema.py:4004`) carries `idempotency_key`, `expected_state_json`, `observed_state_json`, `receipt_json`; the VERIFY phase is written at `project_steward_service.py:591-609` and documented at `:15`; `steward_commands` `:4027` is the replay substrate. **MISSING for the setup controller:** `finalize` (`project_setup_service.py:641`) is one transaction with no per-step receipts and no read-back. | integrate the Steward's pattern | HS-200-19 |

### 5.7 C7 / C8 — Assignment ownership, lifecycle and failure windows

The architect-assistant contract already specifies almost all of C7 and
C8 in more detail than CONTRACTS.md does — `docs/internal/architect-assistant/CONTRACTS.md`
§3 assignment definition (`:48`, with the field table and illustrative
JSON; the document itself says at `:93` that sending it to a current
endpoint is unsupported), §4 lifecycle (`:94`), §5 result manifest and
verification (`:114`), §6 authority and execution topology (`:122`,
proposing `assignment.run@1`), §7 scheduling and failure (`:132`, with
pilot limits, a fire key, and its own failure-window table). **None of
the assignment-specific sections is implemented.** HS-200-24's job is
reconciling two specifications, not discovering one.

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| AssignmentService owning immutable work definitions | **MISSING** — no such service and no such table among the 213 `CREATE TABLE` statements in `holdspeak/db/schema.py`. The only `*assignment*` code is `holdspeak/services/inference_assignment_service.py` (phase 143), which assigns **models to roles**, not work to workers. The whole C7 definition record is unbuilt. **Note the name collision** with `inference_assignments` (10 rows on his desk). | new work | HS-200-25 |
| Worker launch (the first adapter's dispatch) | EXISTS — `POST /api/delivery/factory/launch` `holdspeak/web/routes/delivery_factory.py:127` (phase 94, HS-94-07) over `LaunchService` `holdspeak/delivery/factory_launch.py:460`, profiles-only (no client argv); receipts `delivery_command_receipts` `schema.py:1922`, repository `holdspeak/db/delivery_receipts.py:179`. Second door: `POST /api/coders/factory/spawn` `holdspeak/web/routes/system/coder_factory_routes.py:14`. | reuse and prove | HS-200-26 |
| Worker steering | EXISTS over HTTP — `holdspeak/web/routes/system/coder_steering_routes.py:34`, `/peek` `:109`, `/arm` `:196`, `/disarm` `:250` over `holdspeak/coder_steering.py:1` (phase 87). **MCP is read-only:** `holdspeak/mcp/families/coder.py:13,25,37` = `coder.list/get/audit`; no MCP steer, send or stop tool exists. C11 requires the same service validation through Web and supported MCP tools. | repair a demonstrated gap | HS-200-28 |
| Adapter capability ledger | EXISTS `holdspeak/agent_capabilities.py:72` (`LEDGER`, phase 104 HS-104-01), adapter tuple `:51`, vocabulary `:35` (TOOL_HOOKS, SESSION_IDENTITY, USAGE_TOKENS, REPO_HEAD, BLOCKING), four consumers `:176`; the doctor line is `holdspeak/commands/doctor.py:1373`. **The four adapters:** `tmux-pane`, `delivery-node`, `mesh-node`, `claude-code-hooks`. **Load-bearing for HS-200-24:** only `claude-code-hooks` declares `BLOCKING: AUTHORITATIVE` (`:100`); every other adapter is `BLOCKING: UNAVAILABLE`, and only it declares authoritative `USAGE_TOKENS`. C7's "a task requiring an unenforceable capability must refuse that adapter" therefore already has a factual basis — and `tmux-pane` / `delivery-node` cannot support a cancellation- or spend-guaranteed recipe. | reuse and prove — **this settles the first-adapter choice on evidence** | HS-200-24, HS-200-26 |
| Kernel parent runs, child operations, receipts | EXISTS — `kernel_parent_runs` `schema.py:3102` (phase 131 HS-131-04), controller `holdspeak/kernel/parent_run.py:56`, child reservation `:147` (`reserve_child`, `BEGIN IMMEDIATE` + epoch guard + child budget); receipts `kernel_receipts` `schema.py:2002` (phase 106 HS-106-04); operations `:1969`; journal writer `holdspeak/kernel/journal.py:138`. **The `kind` CHECK list at `schema.py:3110` is closed and has no `assignment.run`** — adding one is a schema edit, exactly as the architect-assistant §6 proposes. | reuse and prove + integrate | HS-200-25 |
| Refs registered before Web/MCP exposure | EXISTS `holdspeak/refs.py:35` (`CITIZEN_TYPES` frozenset, phase 157 REF-001..004); `refs.py:132` `format()` raises `UnregisteredTypeError`, `:113` `parse()` flags `is_registered`. `assignment:` is **not** a registered type. | integrate | HS-200-25 |
| Mandatory checks / acceptance policy records | **MISSING** — no `acceptance_checks`, `mandatory_check`, `review_policy`, or check-result record anywhere. The only near-hit is `holdspeak/operation_policy.py:240` (`next_state="awaiting_review"`), which is control-posture approval of one kernel operation, not a business acceptance record with verifier identity, evidence ref, and pass/fail/unknown status. | new work | HS-200-27 |
| Artifacts and their domain owner | EXISTS as tables — `artifacts` `schema.py:340` (phase 74), `artifact_sources` `:369` (lineage). **There is no `ArtifactService`**: writes go through `holdspeak/db/plugins.py:744` and four kernel projections (`holdspeak/kernel/recipe_projection.py`, `rails_journal_projection.py`, `meeting_plugin_projection.py`, `sequence_workflow_projection.py`). "Artifacts keep their existing domain owner" therefore means the projection layer, not a service. | integrate | HS-200-27 |
| Leases and generation fences | EXISTS — three mechanisms, **none unified and none assignment-level**: parent lease `holdspeak/kernel/parent_run.py:79` (`lease_process_id`, `lease_heartbeat_at`) with `execution_epoch` as the fence (`:162,196`) and a stale-lease sweeper `:333`, heartbeats `holdspeak/kernel/parent_lease.py:8`; `inference_runtime_leases` `schema.py:3086` with a unique partial index `WHERE state='active'` (`:3104`), driver `holdspeak/kernel/local_runtime_lease.py:37`; `turn_capability_leases` `schema.py:3238` (phase 143 HS-143-09), driver `holdspeak/services/tool_turn_controller.py:451`. **No fire-key uniqueness for a recipe occurrence.** | reuse and prove + new work at the assignment level | HS-200-29, HS-200-34 |
| Run link persisted before physical dispatch | **PARTIAL — and a live defect on the exact adapter HS-200-26 names.** At kernel level the order is right: `holdspeak/kernel/journal.py:138` inserts `kernel_operations` (`UNIQUE(principal_identity, idempotency_key)`, `schema.py:2000`) before claim, and `parent_run.py:147` commits `active_child_invocation_id` in a `BEGIN IMMEDIATE` transaction before `begin_child_dispatch` (`:162-163`). But the delivery-factory launch path documents its own order at `holdspeak/delivery/factory_launch.py:690-700` as worktree.create → **factory.spawn** → target issue → Work attempt → *"5. the launch record persisted"*, and the ledger row (`LaunchLedger`, `factory_launch.py:382`) is written **after** the process is spawned on both the success and the failure branches (`:768`, `:797`, `:826`). If the hub dies between spawn and `self._ledger.record(record)`, a live agent session exists with no durable link. | repair a demonstrated gap — **carry this finding into HS-200-24's review, not just HS-200-25** | HS-200-24, HS-200-25 |
| One terminal winner on cancel | EXISTS — `holdspeak/kernel/parent_terminal.py:21` `cancel_parent` (outcome-state election table `:11`, publication-claim wait loop), and `parent_run.py:273` performs the guarded `state='CANCELLING', execution_epoch=execution_epoch+1 … WHERE state='OPEN' AND execution_epoch=? AND publication_claim_id=''`. MCP: `sequence.cancel` `holdspeak/mcp/families/sequence.py:29`, `workflow.cancel` `:58`, both routing to `cancel_by_operation_id`. `plugin_job.cancel` (`holdspeak/mcp/families/plugin_job.py:55`) is weaker — it refuses running jobs outright at `:56` and is not a terminal-race winner. | reuse and prove | HS-200-29 |
| Attempts bound to an immutable definition and target revision | PARTIAL — `work_attempts` `schema.py:1873` and `work_attempt_events` `:1907` (phase 94 HS-94-04), repository `holdspeak/db/delivery_attempts.py:1`, service `holdspeak/delivery/attempts.py:1`, consumer `holdspeak/services/delivery_service.py:13`; keyed `(source_id, project, story_id, worktree_id)` with a partial unique index pinning one live attempt per session (`schema.py:1902`). **No definition or revision column** — C8's binding needs a schema addition. | integrate | HS-200-25 |

**Phase 155 The Crew, reconciled.**
`pm/roadmap/holdspeak/phase-155-the-crew/` exists, chartered 2026-08-30
from a ratified settled design, **0/5 stories, all `backlog`, nothing
shipped** (`current-phase-status.md:29-35`). Its design: `chat.subthread`
as a receipted MCP tool with a **depth cap of 1**, children run by the
*existing* workbench conductor (30 s inline then background), results
returned as `thread.notification` tool-role messages, a crew row on the
desk with steer/stop — explicitly no second runtime, no crew templates,
no cross-desk crews (`:14-16,44-47`). None of it is in the code: no
`chat.subthread` or `thread.notification` tool exists in
`holdspeak/mcp/families/`. Its three ratified decisions (subthread calls
stay receipted; depth cap 1; no auto-approve) are **directly reusable as
HS-200-24 inputs and do not conflict with C7** — 155 designed a
*conversational* child, C7 designs a *work-definition* child, and 155's
"reuse the conductor" is the same "no second worker queue" constraint
HS-200-24 restates. Disposition: **integrate the decisions, defer the
surface**; BACKLOG.md:1003 keeps DC-05 parked.

**DELIVERY_STATUS.md, reconciled.** The architect-assistant delivery
record claims the **Interview increment only** (DP-00A manual path,
merged PR #561), riding the existing `tool.turn` parent with limits of
10 minutes / 30 children / 10 passes; its verification is mechanical
(backend regressions, 1,481 desk vitest tests, a browser walk at 1440
and 393 against a **scripted fixture model**, two live LAN-model trials
with kernel receipts) and it states explicitly that this is "not owner
acceptance". It honestly retains content-quality gaps, an unbound-session
crash window, guards failing identically to unchanged HEAD, an untested
physical microphone, and an open owner verdict. Its remaining item 5 is
the direct Phase 200 handoff: "Deliver Assignment-based supervised work
and bounded scheduling through DP-03/DP-04/DP-06. **This increment does
not enable them.**" No Phase 200 row assumes otherwise.

### 5.8 C9 — Scheduling and availability

C9 says "Heartbeat, Cadence, Steward, and scheduled recordings retain
their existing owners" and "Phase 200 does not introduce a second
generic scheduler." Both hold — but the survey found **five** scheduling
owners on main, not four. The fifth, the kernel delegated-schedule
path, is the only one with a durable per-firing dedupe row, and is the
pattern HS-200-33 should extend rather than reinvent.

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| Heartbeat | EXISTS `holdspeak/services/heartbeat_service.py:76` (phase 171); the sweep driver is a daemon thread on a 60 s tick at `holdspeak/runtime/heartbeat.py:22`; routes `GET`/`PUT /api/settings/heartbeat` (`holdspeak/web/routes/system/settings.py:294,306`), `POST /api/settings/heartbeat/run-now` `:321`; MCP family `holdspeak/mcp/families/heartbeat.py:14`. | reuse and prove | HS-200-21, HS-200-33 |
| Cadence | EXISTS — `cadence_loops` `holdspeak/db/schema.py:1724`; **two** services, engine `holdspeak/cadence/service.py:33` and API-facing `holdspeak/services/cadence_service.py:20`; scheduler `holdspeak/cadence/scheduler.py:25`; 11 MCP tools at `holdspeak/mcp/families/cadence.py:16-141`. His desk: 2 loops, `cadence.enabled=false`. | reuse and prove | HS-200-21, HS-200-33 |
| Steward | EXISTS — `steward_policies` `schema.py:3956`, `steward_runs` `:3980` with the partial unique index `uq_steward_runs_one_active_per_project` `:3998`; service `holdspeak/services/project_steward_service.py:97` (phase 163); MCP `project.run_steward` `holdspeak/mcp/families/project.py:381` (+ `configure_steward` `:348`, `stop_steward` `:400`, `get_steward_run` `:413`, `steward.trigger` `:426`); HTTP `holdspeak/web/routes/steward.py:62,151,169,190,207,224,403`. | reuse and prove | HS-200-21, HS-200-33 |
| Scheduled recordings | EXISTS — `scheduled_recordings` `schema.py:3470` (`cron_expr` + tz + revision + `next_fire_at` + a `missed` state); `ScheduledRecordingService` `holdspeak/services/scheduled_recording_service.py:143` (phases 136/147), calendar-linked in 175 at `holdspeak/calendar_ingest_conductor.py:199`. | reuse and prove | HS-200-22, HS-200-33 |
| The fifth owner: kernel delegated schedule | EXISTS — `kernel_schedule_delegations` `schema.py:1661`, `kernel_schedule_ticks` `:1676` (**PK `(workbench_id, due_minute)`** — the one durable per-firing dedupe in the tree); writers `holdspeak/services/schedule_delegation.py:47-121`, the `INSERT OR IGNORE` tick claim `holdspeak/kernel/schedule_delegated.py:84`; consumer `holdspeak/services/workbench_runner.py:211`. Scope: Workbench scheduled inference only. | integrate — **the pattern to generalise** | HS-200-33 |
| Durable occurrence identity per logical firing | **MISSING as a general contract.** Partial precedents only: `kernel_schedule_ticks` `(workbench_id, due_minute)` (minute-grain, Workbench-only, no tz, no revision); the scheduled-recording tombstone key `(calendar_source_id, calendar_uid, calendar_starts_at)` `schema.py:3492-3501`; `steward_runs.watermark` `:3989`. **Heartbeat and Cadence have no per-firing identity at all.** | new work | HS-200-33 |
| Leases and generation fences at the scheduler seams | **MISSING.** Adjacent machinery exists elsewhere: intel executor leases `holdspeak/intel_queue.py:240` (`_BoundExecutorLease`, `renew_bound_executor_lease`, `lease_lost`) and the executor fence `holdspeak/kernel/projection_stager.py:141,275`. The Steward gets in-DB mutual exclusion (not a lease) from `uq_steward_runs_one_active_per_project`. **Heartbeat, Cadence and scheduled recordings hold no lease and no fence, and the Heartbeat loop is a bare in-process thread (`holdspeak/runtime/heartbeat.py:31-38`) — so the two hub processes observed in §1.3 would both sweep.** | new work — **and §1.3 is the live demonstration** | HS-200-34 |
| Per-binding overlap / retry / missed-run / quiet-hour / expiry policy | Split five ways: **quiet hours EXIST** (Heartbeat `heartbeat_service.py:260`, held at `:288`; Cadence `cadence/scheduler.py:25-30,66`); **retry EXISTS for the Steward only** (`steward_policies.max_retries` `schema.py:3966`, `max_actions_per_run` `:3967`, `cooldown_seconds` `:3968`); **missed-run PARTIAL** (a terminal `missed` state `schema.py:3485`; no coalescing, no gap record); **expiry PARTIAL** (`kernel_schedule_delegations.expires_at` `:1666`, credential TTL `holdspeak/principals.py:140,150`); **overlap MISSING** (only the Steward's one-active index); **per-binding declaration MISSING — there is no binding record at all.** | new work over four existing owners | HS-200-33, HS-200-34 |

### 5.9 C10 — Credentials and remote deployment

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| Reach transport and runner | EXISTS (phase 174) — transport is the Streamable-HTTP MCP route `POST /api/mcp` `holdspeak/web/routes/mcp_http.py:60`; settings `GET`/`PUT /api/settings/remote` `:161,211`, `POST /api/settings/remote/credentials` `:233`, `DELETE …/{id}` `:278`. The runner is a stdlib client script `scripts/reach_runner.py`, documented in `docs/REACH_RUNNER.md`. **There is no `holdspeak/reach*` module** — Reach is those routes plus that script. | reuse and prove | HS-200-31 |
| Credential durability across a hub restart | **CONFIRMED LOST.** `holdspeak/principals.py:103-110`: `class AgentCredentialStore` — *"In-memory, revocable credentials minted once per supervised process. … The store is wiped on process restart (persistence deferred; see D4 H6)."* Backing state is plain dicts (`:118`); module singleton `agent_credentials = AgentCredentialStore()` at `:272`. `docs/REACH_RUNNER.md:41` says the same to the user. | repair a demonstrated gap | HS-200-32 |
| Credential stores | EXISTS — **three, sharing no code path.** (a) `AgentCredentialStore` `holdspeak/principals.py:103` — Reach only, in-memory, sha256 at rest `:98`, TTL capped at 30 days `:95`, `revoke`/`revoke_by_id` `:218,232`, tool-palette scope `:89`. (b) `CredentialService` `holdspeak/services/credential_service.py:86` — integration secrets in config, redacted-only reads; routes `holdspeak/web/routes/system/settings_secrets.py:33,47,57`. (c) `NativeKeyStore` `holdspeak/people/keys.py:53` — macOS Keychain / Secret Service, no file or env fallback, **People keys only**. **Nothing binds (a) to (c).** | integrate — HS-200-32's three named seams are three unrelated stores | HS-200-32 |
| Machine enrollment, separate from transient agent credentials | **MISSING** — no enrollment record. The only adjacent pieces are `PrincipalKind.NODE` / `PrincipalRight.NODE_LINK` (`holdspeak/principals.py:23,42,55`), a right with no durable table, and mDNS advertising `holdspeak/mesh.py:134`. Every Reach identity today is a transient agent credential. | new work | HS-200-32 |
| Operator runbook (startup, sleep, backup, restore, credential replacement, availability, pause) | PARTIAL. Exists: credential replacement, troubleshooting and the external-cron pattern `docs/REACH_RUNNER.md:28-41,95-115`; backup/restore/upgrade `docs/RELEASING.md:57-73`; sleep stated once at `docs/USER_GUIDE.md:867` ("The hub does not prevent sleep"); execution paths `docs/AUTOMATION.md`. **MISSING:** any always-on-hub runbook — **no launchd or systemd service install anywhere in `docs/`** (grep returns zero), no availability-status procedure, no "pause all scheduled work" procedure, no rotation-without-downtime path (rotation today is "issue a new one after restart"), no restore rehearsal against a copy. | new work | HS-200-31, HS-200-38 |
| The hub host choice | Observed, not decided: the hub runs on the owner's Mac (§1.3), the two ready inference destinations are LAN endpoints that are **not** application hubs (§1.6) — C10's "a reachable inference endpoint is not automatically the application hub" is already the live situation. | defer to the story, with the 174 questions attached (§2.2) | HS-200-31 |

### 5.10 C11 — Shared experience and interfaces

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| The shared UI library | EXISTS `web/src/desk/surface/` (`Surface.tsx`, `gadgets.tsx`, `SurfaceFooter.tsx`, `Material.tsx`, `patterns/`, `controls/`, species CSS `surface.css`), contract `web/src/desk/surface/contract.md`; canon `docs/internal/UX-CANON.md:14` — "Every verb is the library Button." | reuse | HS-200-09 and every face story |
| The voice law | EXISTS and is **at ceiling 0** (phase 176). Scanner rule `mic` `scripts/ux_canon_scan.py:100`, law comment `:129-138`, `MIC_SPECIES = ("StringGadget","PadGadget","EditInPlace")` `:154`, per-element `scan_mic` `:314`, 24-entry `MIC_ALLOWLIST` `:164-215`. Ratchet `tests/unit/test_ux_canon_ratchet.py:55` against `tests/ux_canon_ceiling.json` — `"mic": 0`; allowlist size pinned at 24 by `tests/unit/test_ux_canon_scan.py:526`. Component `web/src/desk/components/MicButton.tsx`, re-exported at `web/src/desk/surface/controls/MicButton.tsx:3`. Other ceilings: `A1:4, A3-prose:11, A3-sentence:1, A4:0, A8:25, A9:0, B:32, C:4, DS6:0, emoji:21, raw-ids:16`. | reuse and hold the ceiling | every face story; BACKLOG C7 (allowlist trim) → HS-200-03 |
| The Actions disclosure | EXISTS `web/src/desk/pullouts/ThreadPullout.tsx:1123-1130` — a native `<details data-testid="tool-activity">` summarised by `countLabel("Actions ·", routineTools.length)`; routine tools fold, `attentionTools` render unfolded at `:1131`. Pattern component `web/src/desk/surface/patterns/ActionNotice.tsx`. | reuse | HS-200-28 |
| Same service validation through Web and MCP | PARTIAL — see C7: steering is HTTP-only, MCP `coder.*` is read-only. | repair a demonstrated gap | HS-200-28 |

### 5.11 C12 — Release and observation

| Obligation | Today | Disposition | Story |
|---|---|---|---|
| Packaging and release | EXISTS — `pyproject.toml:224` (`[project.scripts]` → `holdspeak`, `holdspeak-mcp`), sdist include `:172`, wheel packages + `holdspeak/static/_built/**` `:191-195`, build hook `:201` → `hatch_build.py:1` (builds the Vite bundle into the wheel); `.github/workflows/release.yml:1` (PyPI OIDC trusted publishing on `v*` tags or manual dispatch); `scripts/release_gate.py:1`; `scripts/install.sh`; `scripts/linux_smoke.sh`; contract `docs/RELEASING.md:1`. | reuse and prove | HS-200-38, HS-200-39 |
| API surface guard | EXISTS — generator `scripts/gen_api_surface.py:1` (enumerates the real assembled FastAPI app and extracts consumers from `apple/` and `web/src`); guard `tests/unit/test_api_surface.py:51,58,65,75`. **Current count: 668 routes** (`docs/api-surface.json`, rendered to `docs/API_SURFACE.md`). | reuse | HS-200-38 |
| MCP registry and generated reference | EXISTS — registry `TOOLS` `holdspeak/mcp/tools.py:38` (extended `:216,253`, aggregating 21 family modules under `holdspeak/mcp/families/`); generator `scripts/gen_mcp_sidecar_doc.py:1`; generated `docs/MCP_SIDECAR.md:3` — **222 tools across 40 families**, 34 non-owner / 37 owner discovery resources; drift guards `tests/unit/test_mcp_sidecar_doc_drift.py`, `tests/unit/test_doc_drift_guard.py`. | reuse | HS-200-38 |
| Public procedures a release must update | EXISTS — index `docs/README.md:1`; the user-facing set is `README.md` plus `docs/`: `GETTING_STARTED`, `USER_GUIDE`, `WEB_DESK`, `ENVIRONMENTS`, `GLOSSARY`, `INTERVIEW`, `ARCHITECTURE_WORK`, `PROJECT_ROOMS`, `PEOPLE_INTEGRATION`, `PEOPLE_SECURITY`, `DICTATION_PIPELINE_GUIDE`, `DICTATION_COPILOT`, `VOICE_COMMANDS`, `ACTIVITY_PREBRIEFING`, `MEETING_MODE_GUIDE`, `DESK_MEMORY`, `MODELS`, `AUTOMATION`, `CADENCE`, `AUTHORITY`, `AGENT_HOOK_INSTALL`, `REACH_RUNNER`, `MCP_SIDECAR`, `API_SURFACE`, `ARCHITECTURE`, `RELEASING`, `SECURITY`, `INFERENCE_TARGETS`, `PLUGIN_AUTHORING`, `CONNECTOR_DEVELOPMENT`, `FIREFOX_EXTENSION_GUIDE`, `DEVICE_PROTOCOL`. Two are generated (`API_SURFACE.md`, `MCP_SIDECAR.md`); `docs/README.md:52` hardcodes "222 tools across 40 families" by hand and is stale-prone. | reuse; repair the hand-written count | HS-200-38 |
| Web inherited baseline | EXISTS — `scripts/check_web_baseline.py:1` (BASELINE-MATCHED / BRANCH-NEW / HEALED, exit 0 only on zero branch-new), baseline `tests/web-inherited-baseline.txt` (**5 entries**: MicButton retention claim; InlineEditor HS-129-08; containerQueryLaw HS-129-06; writeReceiptGuard HS-132-06; workbenchAutomations STARTS WHEN), doctrine `tests/WEB_BASELINE.md`. | reuse | HS-200-03 |
| The isolated proof driver | EXISTS `docs/internal/architect-assistant/proof/run_tests.py` (18 lines): a `TemporaryDirectory`, `patch.object(Path, "home", …)`, and a custom `os.path.expanduser` shim, then `pytest.main`. **What it does NOT isolate — load-bearing for HS-200-03:** `$HOME` is never set in `os.environ`, so any code reading `os.environ["HOME"]`, any subprocess, and any absolute path computed at import time before the patch escape it; it does not touch `XDG_DATA_HOME`, the Keychain, or model paths. **It is not invoked by any CI workflow** — CI runs plain `uv run pytest` (`.github/workflows/test.yml:67,122,197`). | repair a demonstrated gap | HS-200-03 |
| CI jobs | EXISTS `.github/workflows/test.yml`: `docs-navigation` → Documentation Navigation (`:13-14`), `web-quality` → DeskOS Web Quality (`:28-29`), `unit-tests` → Unit Tests (`:49-50`), `integration-tests` → Integration Tests (macOS) (`:84-85`), E2E Tests (macOS) (`:149`), `linux-smoke` → Linux Smoke (`:213-214`), `all-tests` → All Tests Summary (`:245-246`). | integrate | HS-200-03 |
| Measures naming build / model / hardware / scenario / denominator | **MISSING** — no such record exists. ACCEPTANCE.md §"Evidence record" defines the shape; nothing implements it. | new work (documentary) | HS-200-08, HS-200-16 |

### 5.12 Story-level disposition summary

One disposition per story, derived from the contract rows above.
"Reuse and prove" means the behaviour exists and the story owes fresh
integrated proof — it does **not** license a re-implementation.

| Story | Disposition | The concrete gap on current main |
|---|---|---|
| 01 Baseline and obligation map | **new work (documentary)** — this story | no attested baseline existed since `519afd4f` |
| 02 Runtime identity and restore | **new work** + reuse backup/restore | 5 of 8 C1 identity fields MISSING; two hubs on one DB undetected (§1.3); schema version unexposed; restore covers the main DB only |
| 03 CI isolation and release contract | **repair** | 35 failing identities + 3 tsc errors across 4 red jobs (§3); the proof driver leaks `$HOME` and is not used by CI |
| 04 First value and model readiness | **reuse and prove** + repair | Concierge (`holdspeak/services/concierge_service.py`, phase 170) and first-value attempts (`holdspeak/services/setup_service.py:13`) exist; the default route has no key while two LAN routes are ready (§1.6); return-to-task MISSING |
| 05 Physical voice and custody | **reuse and prove** + repair | the whole path exists incl. 176's `text` kind (`holdspeak/plugins/dictation/corrections.py:34-39,142,194`); never proven on the owner's physical metal; 11 BACKLOG "AG" defects land here (§2.3) |
| 06 Claim support semantics | **repair** | `project_update_service.py:583,590` is the demonstrated overstatement; the three axes MISSING; `claims_json` is an opaque blob |
| 07 Coverage and partial results | **repair** | `needs_you_aggregate.py:54-58,105` swallows failures and asserts `stale: False`; the Room's own `ok/degraded/absent` tag is discarded |
| 08 Semantic evaluation harness | **new work** | no corpus, no live-model runner, no scoring protocol; ACCEPTANCE.md §"Quality corpus" is the spec |
| 09 Daily workflow design | **reuse (design)** | the library and canon exist; the daily path is undesigned; three 172-era owner questions attach here (§2.2) |
| 10 Scoped working context | **new work over existing seams** | Interview has no promotion path (`interview_service.py:31`); two ref vocabularies to reconcile; PR #526's retrieval half is an input |
| 11 Project preparation | **reuse and prove** | `MondayBriefService`, `MemoryService`, `ProjectUpdateService` exist; no evidence manifest (C3); no pilot Project (§4) |
| 12 Meeting to reviewed outcomes | **reuse and prove** | trigger `holdspeak/intel_queue.py:295` → `ProposalBridgeService` `holdspeak/services/proposal_bridge_service.py:28` exists (phase 172); 6 meetings, 0 decisions on his desk |
| 13 Decision and commitment continuity | **reuse and prove** + repair | supersede exists (`decision_record_service.py:263`); no accept/reject verb; 0 decision records on his desk |
| 14 People preparation boundary | **reuse and prove** | `PeopleService` `holdspeak/services/people_service.py:64` with `_require_owner` on every verb; 6 protected records; two 172 questions attach |
| 15 Relevant attention | **repair** | no five-item cap, no dedup, count-only notification edge (`holdspeak/desktop_notify.py:46-50`), no per-Project settings |
| 16 Two-day daily loop | **reuse and prove (owner leg)** | gated on the pilot Project (§4); discharges the accumulated walk debt (§2.1) |
| 17 Recipe catalog and compiler | **new work** | `recipes` is prompt personas, not a versioned catalog — a naming collision to settle |
| 18 Interview prepared configuration | **new work** | no configuration-plan record with revision, trigger, route policy and state |
| 19 Setup apply and reconciliation | **repair** | `project_commands` has no unique index on `request_hash`; three writers skip the replay check; `ProjectSetupService` mints no envelope; the Steward's `steward_steps` pattern is the model to copy |
| 20 Interview revisit and quality | **reuse and prove** + repair | InterviewService exists (PR #561); 0 sessions on his desk; quality unproved on a real model |
| 21 Repeatable brief on an existing cadence | **integrate** | four scheduler owners exist; no binding record at any of them |
| 22 Calendar dependency integration | **integrate** + repair | 175's wire landed (`holdspeak/calendar_ingest.py`, `calendar_ingest_conductor.py`, matcher `:69` after `ff781cbb`, Door `holdspeak/services/door_service.py:272`); **his desk has 0 calendar sources and 0 events**, the faces below are unbuilt, and `tests/unit/test_hs175_calendar_wire.py` has 3 failures on the runner |
| 23 Daily-practice pilot | **new work (owner)** | gated on §4 |
| 24 Assignment design and adapter decision | **new work (design)** | two specifications to reconcile (CONTRACTS C7/C8 vs architect-assistant §3–§7); the capability ledger already decides the adapter on evidence; the factory-launch ordering defect is an input |
| 25 Assignment definitions and run links | **new work** | no AssignmentService, no table, no `assignment.run` kernel kind, no `assignment:` ref type, no revision column on `work_attempts` |
| 26 Supervised worker adapter | **reuse and prove** | the launch and steering paths exist (phases 87/94); `claude-code-hooks` is the only adapter with authoritative BLOCKING |
| 27 Result verification and review | **new work** | no acceptance-check record anywhere; artifacts have tables but no service |
| 28 Assignment desk controls | **repair** + new surface | Actions disclosure exists; MCP steering is read-only |
| 29 Assignment recovery and cancellation | **reuse and prove** + new work | cancel election and epoch fences exist at kernel level; nothing at assignment level |
| 30 Supervised assignment pilot | **new work (owner)** | gated on 23–29 |
| 31 Durable hub deployment | **new work** | no launchd/systemd install documented anywhere; the awake-Mac question is open (§2.2) |
| 32 Durable credential lifecycle | **repair** | credentials confirmed in-memory (`holdspeak/principals.py:103-110`); three unlinked stores |
| 33 Durable trigger bindings | **new work over the kernel tick pattern** | `kernel_schedule_ticks` is the only durable per-firing row; Heartbeat and Cadence have none |
| 34 Leases, retries, missed runs | **new work** | no lease or fence at any of the four scheduler owners; §1.3 shows two hubs that would both sweep |
| 35 Bounded unattended recipes | **integrate** | gated on 17, 23, 30, 32–34 |
| 36 Unattended restart proof | **new work (deployment)** | gated on 31–35 |
| 37 Outcome review and expansion decision | **new work (owner)** | the 174 Confluence question and the 178/179 deferrals resolve here |
| 38 Release package and operator guides | **reuse and prove** + repair | packaging exists; the operator runbook and the People/attachment backup path do not |
| 39 Cold release rehearsal | **reuse and prove** | gated on 38 |
| 40 Release and phase close | **new work (documentary)** | also reviews the four missing final summaries (§2.5) |

### 5.13 The map's arithmetic

Counted from the tables above, not estimated. A row carrying two
dispositions is counted under both, so the tag total exceeds the row
count.

**Contract obligations (§5.1–§5.11): 69 rows.**

| What the row says exists today | Rows |
|---|---|
| EXISTS (in whole or as a named service/table/route) | 36 |
| MISSING outright | 22 |
| PARTIAL | 9 |
| Neither — a confirmed defect (`AgentCredentialStore` is wiped on restart) or an observed-but-undecided condition (the hub host) | 2 |
| **total** | **69** |

| Disposition | Rows tagged (5 rows carry two; 74 tags over 69 rows) |
|---|---|
| reuse and prove | 25 |
| new work | 22 |
| repair a demonstrated gap | 16 |
| integrate | 10 |
| defer | 1 |

**Stories (§5.12): 40 rows, 48 disposition tags, 8 stories carrying two.**

| Disposition | Stories tagged |
|---|---|
| new work | 18 |
| reuse and prove | 14 |
| repair a demonstrated gap | 13 |
| integrate | 3 |
| defer | 0 — every deferral in this phase is routed into HS-200-37's expansion decision rather than attached to a story |

The 22 MISSING obligations concentrate in three places: the whole
Assignment domain (C7/C8), the scheduling binding and occurrence layer
(C9), and runtime identity (C1). Everything C7 calls "the existing
owners" is real and citable; everything it calls "the new part" is
greenfield. The single largest reusable asset the map found is that
**more than half of the contract obligations already exist in code** —
Phase 200's dominant activity is integration and proof, not
construction, everywhere outside those three areas.

---

## 6. What this story did not verify

Recorded so no later story treats an absence as a clearance.

1. **No failing test was reproduced locally.** The ledger in §3 is read
   from GitHub Actions run 34007939416 and from Phase 176's recorded
   classification. HS-200-03 owns the reproduction and will find
   discrepancies (§3 already names two).
2. **The two runs after `7a47904e` were not classified** — they were
   `in_progress` and `queued` at observation time. The ledger therefore
   does not describe main's tip.
3. **The `auth_token_set` contradiction** in `/api/setup/status`
   (§1.6) was observed, not diagnosed.
4. **The active Project's provenance is unknown** (§4). Nothing
   read-only distinguishes a real stream from a Room left standing, and
   its content was not read.
5. **No EXISTS row was executed.** Every `file:line` in §5 attests code
   on disk at `bea4176c`. It does not attest that the path runs, that
   the running hub loaded it, or that its output is useful — and §1.3
   proves those are separate questions.
6. **The Phase 176 walk findings were not re-observed**; they are
   carried from that phase's record.
7. **Nothing was measured.** No performance target in ACCEPTANCE.md
   has a baseline yet; HS-200-16 establishes the first.
8. **PR #526 was read from its GitHub metadata and body only.** Its
   diff was not inspected and its conflicts were not assessed.
