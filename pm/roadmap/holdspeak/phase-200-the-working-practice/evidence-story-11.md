# Evidence - HS-200-11

- **Story:** HS-200-11 - Produce a useful Project preparation brief
- **Status:** done
- **Date:** 2026-09-17

## Proof

### Captured run — 2026-09-18T04:03:43Z

- **Command:** `env HOME=/var/folders/q7/5dzz5g2116b3lq8rhg7hwjrr0000gn/T/tmp.eHeoHghOX5 uv run pytest -q -p no:cacheprovider tests/unit/test_phase200_preparation_brief.py`
- **Cwd:** .
- **Exit code:** 0
- **Index-tree:** 2a9a2164703162d06ad5ee45b4772f83788e2cde

```text
.....................................                                    [100%]
37 passed in 11.96s
```

### Captured run — 2026-09-18T04:03:56Z

- **Command:** `env HOME=/var/folders/q7/5dzz5g2116b3lq8rhg7hwjrr0000gn/T/tmp.6slrmjSw5H PLAYWRIGHT_BROWSERS_PATH=/Users/karol/Library/Caches/ms-playwright uv run pytest -q -p no:cacheprovider tests/e2e/test_hs200_preparation_brief_glass.py tests/e2e/test_hs200_claim_support_glass.py`
- **Cwd:** .
- **Exit code:** 0
- **Index-tree:** 2a9a2164703162d06ad5ee45b4772f83788e2cde

```text
............                                                             [100%]
12 passed in 87.40s (0:01:27)
```

## What was built

A purpose alone prepares a brief (no calendar). The brief binds a SOURCE MANIFEST: every source with its state, observed time and revision; named omissions (`NOT READ n` with reason and repair); current and superseded decisions carried in; `NOT INCLUDED n` for what the bound of 3/3/3 left out; a sha256 recomputed on every read so a tampered manifest cannot be kept. Bounded output (priorities, questions, obligations) from a deterministic drafter that is the shape authority and a model drafter parsed to the same shape; every claim on three independent axes; an unsupported value printed AND typed unsupported; no source → `NO SOURCE` + `Find support` (which opens Desk memory with the sentence prefilled). A ref to a superseded decision is typed `DECISION · SUPERSEDED` with its successor and never enters the outline. Unavailable AI keeps the purpose (persisted draft), names the route state through the real resolver (`KEY NOT SET`, `ENDPOINT UNREACHABLE · host`, `CREDENTIAL EXPIRED · PROVIDER`), draws `Prepare` refused, offers `Set up model` with focus returning to `Prepare`; `Write it myself` is absent by the owner's verdict. `Stop` cancels the kernel op and a stopped run inserts nothing. The species `ClaimAxes` is promoted to the library.

## Counsel-on-built: BOUNCE, then paid

P0: after a request had LEFT and been answered with prose (or an HTTP 500), the refused face said `PURPOSE KEPT · NOTHING SENT` with footer `THIS DEVICE` (Constitution III, VI). Now the refusal carries the invoke receipt and the face reads `SENT · 127.0.0.1 · NOTHING USABLE RECEIVED` (or `· NOT ANSWERED`) with the target's egress chip; `NOTHING SENT` appears only when no invoke op exists. Counsel's probe E, before → after, in the table below. P1: readiness was never consulted on a real desk (the plan leg id did not match the profile row, so READY always and the model label was the engine kind) → the route is read through the planner's leg and the deployment revision; a superseded decision the model cited landed under DECIDE TODAY → typed superseded and ledgered; silent truncation → `NOT INCLUDED n`; `Stop` stopped nothing → kernel cancel plus disconnect watch, no orphan drafts; `Find support` opened Desk memory empty → the `q:` scope prefills and runs. The NAME unknown heuristic (HS-200-06's shared helper) minted a false NAME on 28 of 30 ordinary sentences (`NAME SPRINT · NO SOURCE`); it now never fires on a sentence-initial word, an acronym, a weekday, a hyphen half, or a no-source sentence, treats `hh:mm` as one token, and names a single word only when the desk knows the person; a real name still lands. P2 paid: `CREDENTIAL EXPIRED · PROVIDER` for 401/403; the manifest disclosure no longer redraws the omitted rows; return-to-task focus lands on `Prepare`; the purpose wraps at 393 (PadGadget); `Discard` wired on a draft; source rows matched by watch id. The kept-brief glass legs now dispatch over the real wire to a fake OpenAI server, so the host chip and the kernel ops prove the wire.

| Counsel probe | Before | After |
|---|---|---|
| route on a closed port | `READY · openai compatible · boundary ''` | `READY · QWEN3 35B · private_network`, `invoke.sent False` |
| superseded cited as a priority | `s_priorities_1 … source_linked` | `s_superseded_0 … superseded`, successor linked |
| `04:00` in a question | two NUMBER unknowns + NAME `Who` | one NUMBER, no NAME |
| Stop | one orphan draft row | `[]` |
| Find support | `filledInputs: []` | the sentence prefilled and searched |
| return-to-task | active element INPUT | `prepare-verb` |
| prose answer | `NOTHING SENT` / `THIS DEVICE` | `SENT · 127.0.0.1 · NOTHING USABLE RECEIVED` / host chip |
| 30 sentences | 28 false NAME unknowns | 0 |

## Not verified / carried

The disconnect-watch path without the stop route is exercised only through `Stop`. A frozen route whose profile row was deleted still reads READY and dispatches (reported, unruled). Skipped by ruling to BACKLOG: the resumed `INCOMPLETE · n OF m SOURCES` state; `Open source` on a commitment ref. The story's planned suite name was `phase200_preparation`; the built suite is `phase200_preparation_brief`.
