# Phase 159 — Project Rooms: The Interview (P1a) — Final summary

**Closed:** 2026-08-31. **Counsel:** RATIFY-W-C — one M, two S, all
dead in-round with proofs. **The owner's verdict:** the face PASSED
on round 4, after two verbatim bounces that became the design.
7/7 stories done with evidence.

## What the phase proved

The §14 P1a exit sentence, run on glass twice, to the second:
*setup resumes after reload and opens a non-empty Project Room
without external provider dependency.*

- **The graduation:** `connector_watches` became WatchSpec@1
  additively (v68) with the legacy fleet breathing through 31
  characterization pins green on both sides, the backfill idempotent
  in reconcile, and the real-DB proof run on a copy of the owner's
  actual desk.
- **One façade:** WatchService owns the watch lifecycle (material
  edits stale test+baseline, honest zero-match tests, ledger-silent
  baselines); watch_validation.py holds the closed condition/action
  trees at the package root; the no-third-door fence guards the
  table.
- **The setup engine:** durable resumable sessions (server-side
  rehydration, no cache theater — counsel's word), deterministic
  suggestions traceable to real desk facts, ONE-transaction finalize
  (fault-proven), the Blank hatch, no hollow Projects.
- **The wire:** 20 routes, the 409 law for dead sessions, the full
  happy walk through the real app.
- **The face:** four rounds under two pairs of eyes. The owner's
  "I legit don/t know what I am looking at" became the consequence
  headline, one-fact-once, and the pinned verb; his card bounce
  became library ChoiceCards — and when the shortcut wore library
  classes feature-side, the surface fence caught it and
  ChoiceCardShell joined the library properly (one source of
  material).

## What the fences caught at this close (the system working)

1. The phase's own no-third-door fence caught the phase's own
   finalize (raw connector_watches INSERT, one story after the fence
   was built) — sealed via conn-accepting repo helpers, perimeter
   unchanged.
2. The surface fence caught the ChoiceCard class reuse → the shell
   extraction.
3. The architecture guard caught the orchestrator's own
   document.querySelector → a ref.
4. The api-surface fence caught the manifest missing the face as a
   consumer of its own routes → regenerated.

## Gates

- Full suite CI-style: 15F/8187P in 22:10 vs main's 27-name baseline
  — 5 candidates: 4 proven churn flakes (isolated green, files
  untouched by the branch), 1 real (the api-surface consumer drift,
  fixed at root cause + proven). Zero unexplained branch-new.
- Web: check green (guard + bundle gate); baseline 1984/0,
  baseline-subset, zero branch-new.
- Counsel RATIFY-W-C: M-1 fence seal, S-1 duplicate-project close
  (transaction + honest-replay belt), S-2 evidence pin — all landed;
  N-1 judged the ChoiceCard deviation lawful AND named the shell as
  the right fix before the fence demanded it.

## P2 backlog (carried forward)

- 158's: S-1 legacy-wrapping writes single-transaction; N-1
  empty-patch revision burn; N-3 fields-array labels.
- 159's: create routes for meetings/decisions/Door (the seeding
  walls); the evidence-subject native read path; finalize-time name
  override on the wire (the headline infers client-side today).

## The road from here

P2 "The Delta" charters after the merge, anchors re-verified — the
standing rule.
