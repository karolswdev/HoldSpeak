# HS-105-07 - Closeout — the sitting loop and the spec ledger

- **Project:** holdspeak
- **Phase:** 105
- **Status:** done
- **Depends on:** HS-105-01, HS-105-02, HS-105-03, HS-105-04, HS-105-05, HS-105-06
- **Unblocks:** none (Phase 104 activates after this phase closes)
- **Owner:** unassigned

## The bar

This phase exists because of an owner verdict about FEEL, so it can
only close on the owner's hands (Article IX.4), run as a LOOP in
the Phase-101 discipline: sitting → verdict → hands-first fix round
→ gated commit → re-sit, until the verdict is "it feels like an
OS," not "it looks like one." The machine walk below is the
admission ticket to each sitting, never the close itself.

## The machine walk (live, staged, one continuous session)

Staged dense: the 40-object seed from HS-105-01 plus the 30-member
zone from HS-105-03, real intelligence on the LAN endpoint for the
drop-run beat.

1. **Icons:** the dense world reads at a glance; induced states
   (facts count, open circuit, needs-you, freshness) wear their
   badges; selection shows the true second image. Both viewports.
2. **Drop-onto:** note→agent runs on real metal, lineage names the
   drop; note→kb raises the proposal; groundable→orb opens Speak
   holding context; a refused pair returns quietly. Pointer AND
   touch.
3. **Zone windows:** open dense zone, both views, sort, Clean Up,
   Snapshot; drag out / in / onto a target from both views; reload
   — window, view mode, sort, and member positions all restored
   (the HS-103-01 walk extended).
4. **Info:** open on one of every kind; rename from Identity; two
   tooltype edits persisting through the real update paths; lineage
   walked from a run-born artifact; receipts naming this walk's own
   drop-run.
5. **Menus:** Object menu tracking selection kind; a ghosted verb
   naming its reason; ten verbs proven identical via menu, ⌘K, and
   wire; a consent verb refusing over the wire by name.
6. **Reflex sampling:** the 101 round-9 method — real pointer
   sequences, positions/heights sampled over time (settled_box
   double-reads), a touch context, every screenshot READ.
7. **Sweep:** full pytest (metal exclusion), web suite + build +
   tokens gate, all guards, all censuses — outputs to files, read
   before any flip.

## The sitting loop

The owner drives the same beats hands-on, workstation glass,
unscripted wandering encouraged (the verdicts that matter come from
the moves no walk predicted). Each verdict recorded verbatim in the
phase status; each rejection becomes a hands-first fix round
committed against this story; re-sit until the bar. The bar, in the
owner's own framing: Workbench 2.0 on steroids — an OS, not a
gimmick.

## Bookkeeping

- **The spec ledger** (the standing web-desk-is-the-spec direction)
  recorded in final-summary.md: contract-specified atoms (icon cell
  + badge sources, the drop matrix, zone view/persistence keys, the
  Info schema, the verb registry export) vs behaviors living only
  in TypeScript — spec debt, named per item.
- Parked candidates (public screens/workspaces, nested zones, icon
  editor, the process window, user macros on the registry) written
  into BACKLOG.md with one-line scopes.
- Phase 104's activation unblocked; its charter already records the
  order. Roadmap README pointer + "Last updated" moved; the phase
  flips CLOSED only on the owner's accepting verdict.

## Acceptance

- Every walk beat green with evidence via `dw evidence capture`;
  screenshots under `assets/`.
- The sitting loop's verdicts recorded; the final verdict accepting;
  the spec ledger and backlog entries shipped.

## Chef's notes

- Do not schedule the first sitting until the dense-desk walk is
  green — a sparse desk cannot produce the OS verdict either way
  (five objects feel fine in any design; forty tell the truth).
- Keep fix rounds hands-first: operate, measure the reflex failure,
  fix at cause. The round-9 inversion is why 101 finally landed;
  copy the method, not just the ceremony.
