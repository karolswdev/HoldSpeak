# HoldSpeak — Roadmap

> **New here? Read [`HANDOVER.md`](./HANDOVER.md) first** — current state, where to
> pick up, and the repo conventions that bite (PMO commit gate, write-once
> evidence, no `Co-Authored-By`, metal-test exclusion).

**Last updated:** 2026-06-11 (**Phase 54 — Dictation Frontend Decomposition: in-progress (2/6)** — **HS-54-02 (behavior modules) done:** the 2,907-line `dictation-app.js` carved into a 19-line entry + twelve single-concern ES modules under `scripts/dictation/` (largest 576 lines, all under the ~600 budget), code moved verbatim, the module graph kept acyclic by a section-loader registry in `core.js`. **Zero test files changed**; slice 158 passed; full suite **2540 passed, 17 skipped**; `npm run build` clean. Proven live by an all-tabs Playwright dogfood: nine tabs activate + populate, dry-run → moment-of-truth → journal → correction add/delete all work, **zero uncaught page errors** (16/16 PASS). Prior: **HS-54-01 (the module seam) done:** the `?raw` + `new Function()` loader was a Phase-10 migration shim with nothing depending on the eval; the dictation page now loads its behavior as a real bundled ES module, with the first carved module (`scripts/dictation/discovery-nudge.js`) proven live by a 4/4 Playwright dogfood (show / dismiss-persists / return-on-clear / global-off-persists). Module scope immediately caught a latent duplicate `escapeAttr` declaration the eval had masked via hoisting (dead alias deleted, live implementation kept). Client chunks ship un-minified via a `configEnvironment` hook (Astro 6 hardcodes client `minify: true`) — the shim always shipped full un-minified source anyway. Every page-content assertion byte-identical (one helper learned the carved layout). Full suite **2540 passed, 17 skipped**; `npm run build` clean; 0 `_built/` tracked. Earlier: scaffolded — backlog candidate **D** promoted to its own phase after a post-53 strategic review: the dictation cockpit is now **6,101 coupled lines** (`dictation.astro` 3,134 + `dictation-app.js` 2,967) and the density invariant lost five phases running (P40/P45/P47/P48/P53). Six stories (module seam → behavior modules → section partials → density guard → docs → closeout), behavior-preserving (tests unmodified, screenshot-verified per tab); defines the frontend decomposition pattern (the twin of the 26/31/32/34 backend lineage). The same review refreshed [`BACKLOG.md`](./BACKLOG.md): new candidates **I** (meeting import + faceted history search), **J** (Qlippy presence enhancer, absorbing **G**; RFC at [`proposals/qlippy-presence-enhancer.md`](./proposals/qlippy-presence-enhancer.md)), **K** (languages + spoken-symbol dictionary), parked **L–O**; agreed sequence **54 → I → J → K**, hardware day closes 25/24 then 15 opens. Also fixed the stale Phase-53 index row (was "scaffolded", is CLOSED). Prior: **Phase 53 — Activity Pre-Briefing: CLOSED (7/7)** — closed the loop for real. Chasing a real-metal proof exposed that "Dictate with this" did not actually feed the model: the live dictation path never read the pin and the project-rewriter never consumed activity records. HS-53-07 wired both ends — a process-local one-shot selection pin (`dictation_selection.py`) set by `POST /api/activity/nudges/select` and consumed by `run_dictation_pipeline`, which now passes `selected_record_id` to `build_activity_context`; the project-rewriter names the selected record in both the draft + refine prompts. Proven on the `.43` Qwen3.5-9B-Q6 endpoint: the same generic dictation, run with vs. without a selected `github_issue`, yields a generic control task off the ledger `.hs` context vs. a treatment task "Implement the `--since` flag ... as requested in HoldSpeak#412" — the selection demonstrably changes the model output (`dogfood-real-llm-transcript.txt`, RESULT: PASS). Byte-identical default when no pin. +17 tests; full suite **2540 passed, 17 skipped**; `npm run build` clean; 0 `_built/` tracked. Two dogfoods green (engine math + real metal). See [`final-summary.md`](./phase-53-activity-prebriefing/final-summary.md); PR to `main` merged on green; BACKLOG candidate F flipped to shipped.)
**Current phase:** **Phase 54 — Dictation Frontend Decomposition: in-progress (2/6)** ([brief](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md), [status](./phase-54-dictation-frontend-decomposition/current-phase-status.md)) from [backlog](./BACKLOG.md) candidate **D** (frontend density paydown), promoted to its own phase on user direction (the agreed post-53 sequence **54 → I → J → K**). Decompose the 6,101-line dictation frontend into section partials + behavior modules, behavior-preserving (the existing page-content tests pass unmodified; every tab screenshot-verified), locked by a density guard so it cannot regrow. Six stories (seam → modules → partials → guard → docs → closeout). Read [`AGENT-BRIEF.md`](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md) first. Prior: **Phase 53 — Activity Pre-Briefing: CLOSED (7/7)** ([final-summary](./phase-53-activity-prebriefing/final-summary.md)) from [backlog](./BACKLOG.md) candidate **F** (local activity as pre-briefing fuel), picked by the user as the next phase. HoldSpeak already records source-cited local activity; this turns it into concrete, dismissible pre-briefing nudges on the daily surfaces ("what you touched since your last meeting"; "dictate a reply with this issue as context"), gated by the existing activity privacy toggle, read-only (surfaces and offers, never acts), every nudge cites its source, one action feeds the record into dictation. Six stories (engine -> API -> dictate-with-context -> nudge UI -> docs -> closeout). Read [`AGENT-BRIEF.md`](./phase-53-activity-prebriefing/AGENT-BRIEF.md) first. Prior: **Phase 52 — Voice Command Macros on a carved dispatch seam: CLOSED (7/7)** ([final-summary](./phase-52-voice-macros/final-summary.md)) from [backlog](./BACKLOG.md) candidate **B** (a voice command launcher) with a scoped slice of **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) on a dedicated `/commands` board, and it fires when they speak the keyword while dictating, through the reused actuator guarded execution, on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Off by default and byte-identical when off; the user owns the risk (configuring is consent, no per-fire prompt) and the blast radius is bounded (deterministic match + a per-macro manifest). The board was designed UI-first and screenshot-verified; full suite 2499 passed; PR to `main` merged on green. The dogfood fired a real shell command end to end. **No follow-on required.** Earlier this phase: **HS-52-06 (docs) done:** `docs/VOICE_COMMANDS.md` (linked in the docs index under Dictate) documents the feature honestly in product-tense; the Phase-51 roadmap-vocabulary guard now scans this Phase-52 doc and passes, `humanizer` run, no em/en dashes. **HS-52-05 (the Voice Commands board) done:** the centerpiece. A dedicated `/commands` route with a card-per-command grid (per-kind color edges/badges, a live "what fires" preview per card, the honest shell danger treatment, a per-card Test button via `POST /api/commands/test`, a per-kind adaptive editor with match hint + conflict warning, an inviting empty state). Built UI-first to the design doc and screenshot-verified (4 PNGs; a screenshot pass caught a real `[hidden]`-override CSS bug). **HS-52-04 (dispatch wiring) done:** the feature works end to end (Python side, off by default): `dictation_runner.dispatch_voice_command` sits at the top of the carved seam, matches the whole utterance deterministically, fires the bounded connector and types nothing, surfacing "command: <keyword>" as a runtime activity; `web_runtime` returns early on a fire (byte-identical when off / no match). The non-meeting persistence wrinkle was resolved honestly: the actuator table is meeting-FK'd, so a fire reuses the guarded execution but is audited via the activity broadcast + log, not that table. **HS-52-03 (local action connectors) done:** `plugins/voice_macro_connector.py` reuses the Phase-38 gated-connector framework: `open_url`/`launch_app`/`shell` run bounded subprocesses behind a per-macro manifest (a connector built for one command refuses any other before egress), `type_text` types via an injected writer; `allow_actuators=False` blocks before the connector runs. **HS-52-02 (macro model + config) done:** `VoiceMacro` = keyword + a deterministic action (`open_url` / `launch_app` / `shell` / `type_text` + one payload), with a single-source `preview()` and a normalized whole-utterance `matches()`; `MacrosConfig` (off by default) round-trips config-version-safe through `/api/settings` with a clean 400 on a malformed macro. **HS-52-01 (scoped-E carve) done:** the dictation orchestration carved into `holdspeak/dictation_runner.py`, byte-identical. Full suite 2480 passed. Designed UI-first as a dedicated `/commands` board. from [backlog](./BACKLOG.md) candidate **B** (re-envisioned by the user as a voice command launcher) paired with a scoped slice of candidate **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through the existing actuator guarded executor (Phase 37/38), reused with new local connectors; configured macros are auto-approved (the user owns the risk), off by default, deterministic and bounded (the keyword selects a pre-configured action; the transcriber never composes a command), every fire audited. Landed on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Seven stories (carve -> model+config -> connectors -> dispatch -> editor UI -> docs -> closeout). Read [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md) first. Prior: **Phase 51 — Public-Docs Hygiene: CLOSED (5/5)** ([final-summary](./phase-51-public-docs-hygiene/final-summary.md)) from [backlog](./BACKLOG.md) candidate H, the cheap release-facing follow-on to the Phase-50 gate. The deeper user/operator guides leaked the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README was already clean. All five stories shipped: a classified leak inventory, a scrub of all 6 in-scope guides into product-tense (keeping product nouns `actuator` and named specs `MIR-01`/`DIR-01`), a case-insensitive doc-drift guard scoped to user-facing docs (`README` + non-recursive `docs/*.md`; internal/evidence/assets out), the rule codified in `DOCS_STYLE.md`, and a closeout dogfood proving the guard both ways. Docs-and-test only, behavior-preserving. Full suite green (2454 passed, 17 skipped), PR to `main` merged on green CI. **No follow-on required;** the strongest remaining backlog bet is **B** (voice macros), best paired with a scoped slice of **E** (the routing seam) under one thesis. Prior: **Phase 50 — Release Readiness ("cut a real 0.x"): CLOSED (7/7)** from [backlog](./BACKLOG.md) candidate C (the open-source-ship unlock). HS-50-01 (one true version), HS-50-02 (the safe-by-default schema policy, the heart of the phase), HS-50-03 (backup + restore), and HS-50-04 (doctor + config honesty) are done: `_ensure_schema` does create-fresh / no-op-equal / backup-then-apply-older / refuse-newer, `backup_database` takes a consistent SQLite snapshot before any destructive action, `holdspeak backup` / `holdspeak restore` give the user an on-demand snapshot and a safe reversal, `doctor` (and `/api/setup/status`) now report the real schema state honestly (newer DB flagged FAIL), and `Config` carries a `config_version` that coerces an older shape forward without dropping fields and flags a newer one. (The live schema is additive today, so the real holes closed are a newer DB run by an older build and the lack of a backup before any future migration.) HS-50-05 (verified clean-machine install) is done: a fresh-venv `uv pip install -e .` reaches `holdspeak doctor` exit 0 with only the expected optional warnings, version 0.2.1 resolved from real install metadata. All seven stories are done: one true version, the safe-by-default schema matrix, backup + restore, an honest doctor + config_version, a verified clean install, the upgrade/backup policy doc, and a green dogfood proving the matrix end to end. HoldSpeak is now safe to install, upgrade, and trust (the PyPI publish is a deliberate maintainer step once the gate is green). Make HoldSpeak safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (never silently destroy data on upgrade — refuse-newer, backup-then-apply-older, no-op-equal, create-fresh), a backup the user can run, a `doctor` honest about the DB/config it found, config that can evolve, a verified clean-machine install, and honest docs with an upgrade/backup policy. The next schema bump currently wipes an existing user's DB (`db/core.py:_ensure_schema`) with no backup — that is the bet. Behavior-preserving (no feature/capture/dictation/plugin/synthesis change). Read [`AGENT-BRIEF.md`](./phase-50-release-readiness/AGENT-BRIEF.md) first; HS-50-02 (the safe schema policy) is the heart, HS-50-01 (one true version) the cheapest first win. Prior: Phase 49 (Meeting Aftercare) CLOSED, PR #33.

**Previous phase:** **Phase 49 — Meeting Aftercare ("close the loop"): CLOSED (6/6)**, from [backlog](./BACKLOG.md) candidate A. The meeting result now closes its own loops, read-only + reuse, never acting without approval. The "Your next move" aftercare panel at `/history` shows what's still open (by owner), what was decided, and what changed since the last meeting (`compute_meeting_aftercare` + `GET /api/meetings/{id}/aftercare`, quiet when nothing's open/decided/changed); "show me the moment" jumps to the transcript segment that justifies a result (only on a real timestamp, focus-safe); a "File as issue" affordance turns an accepted action into a GitHub-issue actuator *proposal* through the existing propose -> approve -> execute flow (no new write primitive, off by default, human-approved, audited; an executor test proves an unapproved proposal never egresses); and "Draft follow-up" assembles a local markdown summary, preview + copy only, never sent. Capture/plugins/synthesis untouched; actuators off by default. Closed with before/after, a green dogfood, full suite green (2426/17), and a PR to `main` merged on green. See [final-summary](./phase-49-meeting-aftercare/final-summary.md). Prior: Phase 48 (Visible Learning Loop) CLOSED, PR #30.
**Status:** Plugin rollout complete (Phases 16 → 27 → 28 → 29; 14 real plugins). Remaining open work is largely hardware-gated (Phases 24, 25 HS-25-07).

**Backlog (candidate next phases):** [`BACKLOG.md`](./BACKLOG.md) — the living parking lot so post-Phase-48 ideas (meeting aftercare, voice macros, release-readiness, frontend density, activity pre-briefing, decision-point privacy) are not lost. Shipped as focused phases in sequence, not one mega-phase.

## Vision

HoldSpeak is a local, private, hold-to-talk voice typing tool for macOS
and Linux. The current product is a working voice typer with a web
runtime, meeting mode, transcription via MLX-Whisper / faster-whisper,
deferred meeting intelligence, and an emerging intelligent-typing layer
for project-aware local writing.

This roadmap exists because the project is graduating from "useful
script" to a real product. The current chapter is **local intelligent
typing**: utterances can flow through ordered, user-configurable stages
between Whisper and the keyboard, optionally using target-profile
detection, project context, agent hooks, and OpenAI-compatible local or
LAN LLM endpoints before text is injected.

## Source canon

Phase content must be grounded in these. If a phase disagrees with
canon, canon wins.

- `README.md` — public install + usage surface.
- `docs/SECURITY.md` — security & privacy posture: data classes, trust boundaries, egress points, encryption-at-rest stance (HS-25-03).
- `docs/USER_GUIDE.md` — user-facing product workflows.
- `docs/internal/PLAN_ARCHITECT_PLUGIN_SYSTEM.md` — parent RFC for the plugin system.
- `docs/internal/PLAN_PHASE_MULTI_INTENT_ROUTING.md` — sibling phase: meeting-side multi-intent routing (MIR-01).
- `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md` — DIR-01 spec for the dictation pipeline.
- `docs/internal/PLAN_PHASE_WEB_FLAGSHIP_RUNTIME.md` — web-first runtime migration.
- `docs/internal/PLAN_ACTIVITY_ASSISTED_ENRICHMENT.md` — local activity assisted enrichment design.
- `aipi-lite/` — first-class AIPI-Lite firmware, bridge, docs, tests, and imported device-side roadmap.
- `pyproject.toml` — package contract; the source of truth for installable extras.

## Phase index

| Phase | Goal (one line) | Status | Folder |
|---|---|---|---|
| 0 | Project + roadmap setup; packaging hardening | done | [phase-0-setup](./phase-0-setup/) |
| 1 | DIR-01: Dictation intent routing + transcript enrichment pipeline | done | [phase-1-dictation-intent-routing](./phase-1-dictation-intent-routing/) |
| 2 | MIR-01: Meeting-side multi-intent routing + artifact synthesis | done | [phase-2-multi-intent-routing](./phase-2-multi-intent-routing/) |
| 3 | DIR-01 loop closure: project context, llama_cpp leg, runtime counters, cold-start cap | done | [phase-3-dictation-loop-closure](./phase-3-dictation-loop-closure/) |
| 4 | WFS-01 extended: audit web flagship + interactive config for blocks / project KB / dictation runtime / dry-run preview | done | [phase-4-web-flagship-runtime](./phase-4-web-flagship-runtime/) |
| 5 | Usability powerhouse: reduce setup/dogfood friction and make web workflows faster | done | [phase-5-usability-powerhouse](./phase-5-usability-powerhouse/) |
| 6 | Action follow-through: make meeting action items and artifacts reviewable and traceable | done | [phase-6-action-follow-through](./phase-6-action-follow-through/) |
| 7 | Local handoff exports: make reviewed meeting work portable as local Markdown/JSON outputs | done | [phase-7-local-handoff-exports](./phase-7-local-handoff-exports/) |
| 8 | Local activity intelligence: mine Safari/Firefox history metadata into a private default-on work-context ledger | done | [phase-8-local-activity-intelligence](./phase-8-local-activity-intelligence/) |
| 9 | Assisted activity enrichment: add opt-in local connectors, annotations, and meeting candidates | done | [phase-9-assisted-activity-enrichment](./phase-9-assisted-activity-enrichment/) |
| 10 | Web design system & character pass: tokens, components, identity, route rebuilds, refreshed designer handoff | done | [phase-10-web-design-system](./phase-10-web-design-system/) |
| 11 | Local connector ecosystem: reusable manifests, fixtures, first-party connector packs, and developer docs | done | [phase-11-local-connector-ecosystem](./phase-11-local-connector-ecosystem/) |
| 12 | Workbench voice: replatform tokens + components on a Workbench-evoking palette + pixel font; dashboard polish; designer handoff refresh | done | [phase-12-workbench-voice](./phase-12-workbench-voice/) |
| 13 | Connector runtime + pipelines + meeting context: turn the framework on, chain packs, surface pre-meeting briefings + cross-meeting summaries | done | [phase-13-connector-runtime-and-context](./phase-13-connector-runtime-and-context/) |
| 14 | AIPI-Lite devices: remote audio ingest substrate — `AudioSource` Protocol, `RemoteAudioRecorder`, `/api/devices/audio` WebSocket, per-device speaker labels in transcripts; same-LAN scope (cross-network is phase 15) | done | [phase-14-aipi-lite-devices](./phase-14-aipi-lite-devices/) |
| 15 | Out-and-about: cross-network reach for AIPI-Lite — tunneling (Tailscale / Cloudflare Tunnel / WireGuard candidate evaluation), TLS, per-device PSKs, paired with the AIPI-Lite firmware's portable WiFi (multi-SSID + captive portal + Improv-WiFi) on the device-side roadmap | not-started | [phase-15-out-and-about](./phase-15-out-and-about/) |
| 16 | First real synthesizer: replace `mermaid_architecture`'s `DeterministicPlugin` stub with a real LLM-backed plugin, wire the LLM capability gate, render `mermaid` artifacts as inline SVG in the web view, reality-check the plugin RFC | done | [phase-16-first-real-plugin](./phase-16-first-real-plugin/) |
| 17 | Device Initiative: device → server upstream frames (`device_health` + `query` w/ `last_segment` case) lighting up AIPI-Lite phase 4's `blocked` bridge stories; minimal web UI rendering for device health. Sibling to HS-14, paired with AIPI-4 in the AIPI-Lite roadmap. | done | [phase-17-device-initiative](./phase-17-device-initiative/) |
| 18 | Intelligent Typing Copilot: project-aware local intelligent typing with target profiles, Claude/Codex hooks, optional external-agent summarization, `.hs` context conventions, OpenAI-compatible runtimes, and web cockpit support | done | [phase-18-intelligent-typing-copilot](./phase-18-intelligent-typing-copilot/) |
| 19 | Intelligent Typing Daily-Use Hardening: safe `.hs/.../*.md` project-doc suggestions, telemetry, target-profile overrides, and real endpoint dogfooding | done | [phase-19-intelligent-typing-hardening](./phase-19-intelligent-typing-hardening/) |
| 20 | AIPI Companion: same-LAN physical companion UX for agent-waiting status, voice replies, gestures, and debug visibility | done | [phase-20-aipi-companion](./phase-20-aipi-companion/) |
| 21 | AIPI-Lite First-Class Integration: import firmware and bridge source into HoldSpeak and define the unified developer workflow | done | [phase-21-aipi-lite-first-class](./phase-21-aipi-lite-first-class/) |
| 22 | AI PI Companion UX: state model, gestures, LCD cadence, bridge display wiring, and live hardware dogfood | done | [phase-22-ai-pi-companion-ux](./phase-22-ai-pi-companion-ux/) |
| 23 | AI PI Companion UX Polish: long-prompt display, multi-session identity, preview/browse, and target confidence | done | [phase-23-ai-pi-companion-ux-polish](./phase-23-ai-pi-companion-ux-polish/) |
| 24 | AI PI Companion Productization: web companion overview, stale-session controls, public companion docs/artwork, confidence affordances, and display update cadence | in-progress (3/6) | [phase-24-ai-pi-companion-productization](./phase-24-ai-pi-companion-productization/) |
| 25 | Trust & Hardening: no silent cloud egress, web-runtime auth + bind guard, threat-model/encryption-at-rest doc, LLM-runtime thread-safety, transcription timeout, config-knob audit. Prerequisite to Phase 15. | in-progress | [phase-25-trust-and-hardening](./phase-25-trust-and-hardening/) |
| 26 | Web Runtime Decomposition: break the `web_server.py` monolith into route modules + a shared context, behavior-preserving. Fast-follow to Phase 25. | done | [phase-26-web-runtime-decomposition](./phase-26-web-runtime-decomposition/) |
| 27 | Ubiquitous plugins + spoken-meeting e2e: flip the highest-value stub plugins to real (`action_owner_enforcer`, decisions/open-questions, requirements) re-using the Phase-16 pattern, and add a real `say`→Whisper→MIR→plugins→web spoken-meeting end-to-end harness with screenshots. Continuation of Phase 16. | done | [phase-27-ubiquitous-plugins-and-e2e](./phase-27-ubiquitous-plugins-and-e2e/) |
| 28 | Plugin rollout II: round out the core meeting types — a behavior-preserving synthesis renderer-registry refactor, then flip `adr_drafter` (architecture), `milestone_planner` (delivery), and `risk_heatmap` (risk register) to real, each with a structured web render + spoken-e2e coverage. Continuation of Phase 27. | done | [phase-28-plugin-rollout-ii](./phase-28-plugin-rollout-ii/) |
| 29 | Complete the plugin rollout + public docs: flip the last seven stubs (delivery/product, incident, comms) to real → fourteen real plugins / zero stubs, and document the plugin system on the public README. Continuation of Phase 28. | done | [phase-29-plugin-rollout-iii-and-docs](./phase-29-plugin-rollout-iii-and-docs/) |
| 30 | UI/UX overhaul: retire the Amiga Workbench aesthetic and ship "Signal" — a bold, distinctive, dark-first identity (Space Grotesk / Inter / JetBrains Mono, signature orange, real depth + motion) as a full UX/IA redesign of all five web routes, design system derived with the `ui-ux-pro-max` skill. | done | [phase-30-ui-ux-overhaul](./phase-30-ui-ux-overhaul/) |
| 31 | Database decomposition: break the 5,481-line `db.py` monolith into per-domain repositories behind a thin `Database` container, **delete** the `MeetingDatabase` god-class (greenfield), and **squash** the 18-version migration ladder to one canonical schema. Data-layer twin of Phase 26. | done | [phase-31-database-decomposition](./phase-31-database-decomposition/) |
| 32 | Foundation hardening & doc truth: class-ify `web_runtime.py`, invert the meeting→web-server coupling, **retire the TUI + menubar** (web is the sole interactive runtime), converge audio ownership, add an ungated CI core-path smoke test, extract a route error helper, and reconcile stale non-PMO docs. | done (7/7) | [phase-32-foundation-hardening](./phase-32-foundation-hardening/) |
| 33 | Documentation & open-source readiness: de-prescribe/refresh the model framing (current Qwen3.5 + a `MODELS.md` bring-your-own contract), add an **Apache-2.0 LICENSE** + `pyproject` metadata, reorganize `docs/` (user-facing vs internal/historical) with an index, an OSS-grade README pass + CHANGELOG/CONTRIBUTING, and a refreshed pixel-art asset set. | done | [phase-33-documentation-oss-readiness](./phase-33-documentation-oss-readiness/) |
| 34 | Structural decomposition II: sub-split the four files that now dominate the tree (5,373 lines combined) into navigable, single-responsibility packages — `web/routes/dictation.py` + `web/routes/activity.py` into route sub-packages, and `agent_context.py` + `intel.py` into modules — behavior-preserving (import paths + route table held identical). Twin of Phases 26/31/32; discharges Phase 26's deferred finer-split follow-up. | done | [phase-34-structural-decomposition-ii](./phase-34-structural-decomposition-ii/) |
| 35 | Plugin frontier: make the meeting-intel plugin system extensible by others — a public `docs/PLUGIN_AUTHORING.md`, a plugin-pack manifest + discovery loader (mirroring connector packs; first-party + user packs), per-project enable/disable, and the incident/comms spoken-e2e scenario. Behavior-preserving for the 14 built-ins; actuators deferred to Phase 37. Discharges Phase 29's "next frontier" handoff. | done | [phase-35-plugin-frontier](./phase-35-plugin-frontier/) |
| 36 | Meeting intelligence & experience: make HoldSpeak's meeting intelligence deliver on real, messy meetings end-to-end. **Intelligence** — fix the routing weakness where a brief intent in a digression is diluted below threshold and silently lost: segment the conversation + probe each segment for intent ("fish out" intent per segment), LLM-assisted with a lexical fallback. **Experience** — Signal "elevated" artifact cards (retire the basic chips), copy-as-Markdown, overflow-safe wide artifacts. Tied together by a dynamic/messy spoken-e2e with a before/after capture (7 → 13 artifact types). Artifact data shapes unchanged. On direct user feedback. | done | [phase-36-meeting-artifact-experience](./phase-36-meeting-artifact-experience/) |
| 37 | Actuators: turn on the plugin system's third kind — an actuator proposes an external side effect (file a ticket, post a message) behind a **preview → human approval → execute** flow with an audit trail (the RFC's open question #5; intersects the Phase-25 egress posture). The invariant: no external side effect without an explicit, audited, per-action human approval; executed == previewed. | done | [phase-37-actuators](./phase-37-actuators/) |
| 38 | Actuators II: make the proven-safe actuator mechanism *useful* — **real write connectors** (`gh issue create`, webhook POST) behind a per-connector **permission manifest** routed through the connector `PermissionGate`, and **live in-meeting proposals** (broadcast + a live approve/reject panel). The Phase-37 invariant is unchanged; every connector adds a narrower gate. On user direction. | done | [phase-38-actuators-ii](./phase-38-actuators-ii/) |
| 39 | Dictation Copilot Depth: make the daily-driver intelligent-typing loop **deeper and self-improving** — **multi-pass rewriting** (draft → critique → refine), **session correction memory** (the pipeline learns from corrections within a session), **model-assisted target detection** (LLM fallback below the heuristic threshold), a **project-doc suggestion quality gate** (dedup + recurrence suppression + consolidation), and **depth telemetry**. All opt-in; default byte-identical (the DIR-01 invariant). On user direction. | done | [phase-39-dictation-copilot-depth](./phase-39-dictation-copilot-depth/) |
| 40 | Configuration Cockpit & Persistent Memory: make the whole dictation copilot configurable, observable, and **memorable from the Web UI** — surface the Phase-39 knobs in `/api/settings` + a Signal-styled, readiness-driven **setup cockpit** (no hand-edited JSON/YAML), make correction memory **persistent** (DB-backed) with a curate UI, and render depth telemetry richly. UX + persistence; pipeline behavior unchanged. On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-40-config-cockpit/AGENT-BRIEF.md). | done | [phase-40-config-cockpit](./phase-40-config-cockpit/) |
| 41 | Runtime Presence Indicators: an **opt-in** (`HOLDSPEAK_DESKTOP_PRESENCE=1`), per-platform **native** desktop presence layer so a user dictating into another app sees what the copilot is doing — one `runtime_activity` contract → a websocket broadcast → a Signal `/presence` card, hosted natively (macOS non-activating `NSPanel`+`WKWebView` HUD + `NSStatusItem` glyph; Linux libnotify notification + tray + a GTK-WebKit overlay). Focus-safe; off by default; flag-unset byte-identical. Salvages codex PR #17's bones (rejects its Tk renderer). | done | [phase-41-runtime-presence](./phase-41-runtime-presence/) |
| 42 | First-Run Delight & Daily Confidence: make **arrival** stellar — a user goes from fresh clone to a **verified first dictation**, with visible privacy/trust state and **zero file editing**, in one guided local cockpit (TTFD dogfood: launch→/setup 1.13s). A setup-state contract (an adapter over the doctor) + `first_run` milestone, global settings completion, a welcome/setup route + CLI nudge, a guided real-app first-dictation, an ambient trust chip + panel, a runtime model assistant, and presence onboarding. On user direction. | done | [phase-42-first-run-delight](./phase-42-first-run-delight/) |
| 43 | World-Class Onboarding & First-Run UX: reimagine the first run as a full-screen `/welcome` **wizard** (Welcome · live Permissions · Model picker · a celebratory first-dictation reward · a one-click presence toggle · Done), make desktop presence a **config-backed UI toggle** (kill the env var as the path; live start/stop), and **redesign Settings** (sectioned + searchable + progressive). A world-class UX layer on the Phase-42 plumbing. On user feedback. | done | [phase-43-world-class-onboarding](./phase-43-world-class-onboarding/) |
| 44 | Daily-Surface Polish: carry the Phase-43 wizard's bar to the three daily-driver surfaces (dashboard `/`, dictation cockpit `/dictation`, history `/history`), behavior-preserving — one shared premium language (ambient glow, hero grammar, contained pill navs with a solid-accent active tab, elevated rounded surfaces, reduced-motion-safe depth). Per-surface page-content tests + a live before/after. On user direction. | done | [phase-44-daily-surface-polish](./phase-44-daily-surface-polish/) |
| 45 | Dictation Memory & the Moment of Truth: give the daily-driver dictation loop the memory the meeting side already has — a persistent, private, reviewable **dictation journal** (said → routed → typed → latency), an **in-the-moment "was that right? fix it here"** correction loop that teaches without leaving your flow, and **replay** (re-run a past utterance through the tuned pipeline so "it learned" becomes tangible). Local-first; journaling is a side-channel (typed output byte-identical); focus-safe; provable remotely via dry-run. On user direction. | done | [phase-45-dictation-memory](./phase-45-dictation-memory/) |
| 46 | Documentation Excellence & the 10-Second Hook: the product outgrew its docs (13 phases since the Phase-33 doc pass). Give the documentation the lift the product got — a **doc truth audit** (claims vs live code) → a **bold README reimagining** (10-second hook + cool-facts strip, every graphic kept, repetition cut, depth linked out, materially shorter) → a **docs voice & structure system** + an elevated journey-grouped index → a **visual lift** (real UI screenshots via the Playwright scripts) → a **coverage** sweep (feature → doc matrix; nothing shipped is undiscoverable; nothing overstated) → closeout. Honest not hype; graphics stay; docs-only. On user direction (README too low on cool facts, too large/repetitive; graphics stay; other docs need a lift). | done | [phase-46-documentation-lift](./phase-46-documentation-lift/) |
| 47 | Project Knowledge: Legible & Inviting: make the "teach the copilot about this project" capability understandable. Today `/dictation` shows two adjacent jargon-named tabs — **"Project KB"** (the `.holdspeak/project.yaml` `kb:` map → `{project.kb.*}` placeholders, default `kb-enricher` stage) and **"Project Context"** (the *separate* `.hs/` Markdown files, optional `project-rewriter` LLM stage) — with no explanation of what either is or how they relate (the Phase-46 docs pass even conflated them). Settle the concept + names → in-app explainers + inviting empty states → a guided setup flow (fresh repo → working, no file editing) → a dismissible discovery nudge → docs alignment → closeout. Behavior-preserving (presentation/onboarding, not a pipeline redesign); UX bar via `ui-ux-pro-max`. On user direction ("I struggle to understand the project KB; it's UI/UX too"). | CLOSED ✅ (6/6) | [phase-47-project-kb-legibility](./phase-47-project-kb-legibility/) |
| 48 | The Visible Learning Loop ("What HoldSpeak learned"): HoldSpeak already records every dictation (the journal), learns from corrections (the Jaccard correction memory), and can replay to prove it improved — but the learning is buried in two raw tabs. Make the loop **visible** (a windowed "What HoldSpeak learned" digest with honest counts), **trustworthy** (inline "learned from N similar" signals from the same matcher that nudges routing, quiet at N=0), and a **normal ritual** (one-tap right/wrong on a result, reusing the existing correct path) → docs telling the loop end to end as the open-source differentiator → closeout. Behavior-preserving (read-only aggregation; correcting reuses the existing write path); honesty over hype; focus-safe. On user direction ("I like the 'what I learned this week' view; let's make this open-source thing happen"). | CLOSED (5/5) | [phase-48-visible-learning-loop](./phase-48-visible-learning-loop/) |
| 49 | Meeting Aftercare ("close the loop"): the meeting side has 14 plugins, artifacts, proposals, and action review, but the result is display, not follow-through. Surface what is still open / decided / changed since last meeting, jump to the transcript moment that justifies a result, turn accepted actions into issues through the existing human-approved actuator flow, and draft a copyable follow-up. Read-only aggregation + reuse of the actuator system; never acts without approval. | CLOSED (6/6) | [phase-49-meeting-aftercare](./phase-49-meeting-aftercare/) |
| 50 | Release Readiness ("cut a real 0.x"): 49 phases of features, never cut as a real release. The DB is SCHEMA_VERSION=1 and the next bump silently wipes a user's data; the in-code version disagrees with the package version; doctor never checks the DB; the install pins git HEAD. Make it safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (refuse-newer / backup-then-apply-older / no-op-equal / create-fresh), a backup before upgrade, an honest doctor + config_version, a verified clean install, and an upgrade/backup policy doc. The open-source-ship unlock. | **CLOSED (7/7)** | [phase-50-release-readiness](./phase-50-release-readiness/) |
| 51 | Public-Docs Hygiene: Phase 50 cut the release gate, but the public surface still reads like an internal artifact. User/operator-facing guides leak the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README is already clean. Strip that vocabulary from the deeper guides, rewrite phase-relative claims ("Phase 11 shipped X") into product-tense ("HoldSpeak supports X") so the meaning survives for a reader who never heard of a phase, keep legitimate product nouns (`actuator`) and named specs (`MIR-01`/`DIR-01`), and lock it with a doc-drift guard + a codified `DOCS_STYLE.md` rule so it cannot regress. Docs-and-test only, behavior-preserving. A cheap, release-facing follow-on. Net-new from a between-phases conversation ([backlog](./BACKLOG.md) candidate H). On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-51-public-docs-hygiene/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-51-public-docs-hygiene](./phase-51-public-docs-hygiene/) |
| 52 | Voice Command Macros on a carved dispatch seam: turn the dictation hotkey into a **voice command launcher**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through HoldSpeak's existing **actuator guarded executor** (Phase 37/38), reused and extended with new local connectors; a configured macro is auto-approved (the user owns the risk: configuring is consent), off by default, deterministic (the keyword selects which pre-configured action fires, the transcriber never composes a command), each macro bounded by its own permission manifest, every fire audited. Landed on a **dispatch seam carved out of the 2,341-line `web_runtime` god-object** (a scoped slice of backlog **E**) so a feature (backlog **B**) ships with a motivated refactor under one thesis. On user direction (re-envisioned from text macros to a command launcher; "B=E"). Scaffolded + [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-52-voice-macros](./phase-52-voice-macros/) |
| 53 | Activity Pre-Briefing: HoldSpeak already watches local activity (browser history + GitHub/Jira/calendar enrichment) but it only lives on the `/activity` ledger you go and inspect. Turn it into concrete, **source-cited, dismissible pre-briefing nudges** on the daily surfaces: "here is what you touched since your last meeting"; "you were looking at `owner/repo#123`, dictate a reply with it as context?". A small reader over the records that already exist (no new watcher), gated by the existing activity privacy toggle, read-only (it surfaces and offers, never acts), every nudge cites where it came from, and one action feeds the selected record into the dictation pipeline. On the daily-dictation north star. On user direction (picked backlog candidate **F**). Scaffolded + [`AGENT-BRIEF.md`](./phase-53-activity-prebriefing/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-53-activity-prebriefing](./phase-53-activity-prebriefing/) |
| 54 | Dictation Frontend Decomposition: pay down the cockpit's 6,101 coupled lines (`dictation.astro` 3,134 + `dictation-app.js` 2,967, grown five phases running) into section partials + single-concern behavior modules, **behavior-preserving** (existing page-content tests pass unmodified; every tab screenshot-verified before/after), locked by a density guard so it cannot silently regrow. Defines the codebase's frontend decomposition pattern (`history.astro` / `index.astro` are follow-up candidates). Frontend twin of Phases 26/31/32/34. From [backlog](./BACKLOG.md) candidate **D**, promoted from "ride along" to its own phase. Scaffolded + [`AGENT-BRIEF.md`](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md). | in-progress (2/6) | [phase-54-dictation-frontend-decomposition](./phase-54-dictation-frontend-decomposition/) |

(Status values: `planning`, `in-progress`, `done`, `paused`, `cancelled`.)

Phase 15 (cross-network reach) is gated on Phase 25 landing the web-runtime
auth + bind guard.

## Operating cadence

Per `pm/roadmap/roadmap-builder.md` §3, every shipping commit updates,
in the same commit:

1. The story file header (status flip).
2. The phase's `current-phase-status.md` story-status row + "Where we are".
3. This README's "Last updated" line.
4. Any project-canon doc touched by the story.

Per `pm/roadmap/PMO-CONTRACT.md`: the pre-commit hook gates every
commit on a fresh `.tmp/CONTRACT.md`.

## Project metadata

- **Slug:** `holdspeak`
- **Story ID prefix:** `HS` (e.g. `HS-0-01`, `HS-1-04`)
- **Greenfield?:** effectively yes — **pre-release** (HS-33-04): the `0.2.x` marker in `pyproject` is in-development, **not** a published tag, and HoldSpeak is not on PyPI. `scripts/install.sh` is the from-source install path. No committed external API surface beyond CLI flags + the `holdspeak` shell command; the standing posture is greenfield/aggressive (design freely — see memory `feedback_holdspeak_not_really_released`).

## Glossary

- **DIR-01** — Dictation Intent Routing, phase 1 of this roadmap. See `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md`.
- **MIR-01** — Multi-Intent Routing for meetings. Sibling phase, separate runtime, shares plugin contracts only.
- **Block** — a user-defined intent class with match examples and an injection template (DIR-01 §8).
- **Transducer** — DIR-01's new plugin kind: transcript-in, transcript-out stage.
