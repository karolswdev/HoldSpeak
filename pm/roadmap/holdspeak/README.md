# HoldSpeak — Roadmap

> **New here? Read [`HANDOVER.md`](./HANDOVER.md) first** — current state, where to
> pick up, and the repo conventions that bite (PMO commit gate, write-once
> evidence, no `Co-Authored-By`, metal-test exclusion).
>
> **Orchestrating a phase?** The multi-agent method — audit fleets, charter
> from evidence, Terra waves with serialized shipping, walks, honest ledgers —
> is canon at [`docs/internal/ORCHESTRATION.md`](../../../docs/internal/ORCHESTRATION.md).

**Newest update (2026-08-22): Phase 143 — The Intelligence Router — ACTIVE
(6/14 complete; Story 06 closeout tracked separately).**
The full execution charter is ready: one server-owned capability registry,
reusable immutable model profiles with hub-local bindings, sparse ordered
Assignments, frozen per-run route plans, and durable disposition-driven
fallback above the existing `InferenceRunner` waist. It generalizes the proven
meeting/speech plan pattern across Thoughts, writing, meetings, background,
agents, Workbenches, Recipes, and future tool turns. Owner settings separate
**Model Library** (what is available) from **Assignments** (which models do
which jobs), so adding a model never silently changes a job. The default UI
stays bounded as capabilities grow; ordered fallbacks are edited in one atomic
sheet and local-to-cloud expansion is explicit. Story 01 is done: three exact
generated ledgers now fail closed on new Python or Swift execution doors,
semantic AI callers, mutable routing authorities, browser selectors, and
retry/fallback branches. Stories 02 through 05 add the sealed capability
registry, reusable profile authority, assignments, and frozen route plans.
Story 06's durable controller substrate is merged, and Story 07 now proves the
first production adoption: Ask, Thought interview, intent classification, and
rewrite use atomic frozen evidence and controller-owned fallback, including a
saved local-to-cloud boundary proof. Fourteen recipe-like stories,
the repository census, architecture contract, owner experience, migration law,
kill criteria, and chaos/glass matrix are in the
[Phase 143 record](./phase-143-intelligence-router/current-phase-status.md).

**Previous update (2026-08-20): Phase 141 — From Thought to Work — ACTIVE
(6/9).** Raw custody, bounded resume continuity, the first owner-facing Develop
bridge, and the no-model completion/reopen path are done. The Chair now starts a local thought directly; every
ordinary Note—including Phase 140's first kept Note—can become the same
revisioned working Note without cloning or losing its byte-equal Original.
Inbox and Chair reopen unfinished work after reload. **Finish Thought** drains
the last accepted edit and completes immediately under the default YOLO posture:
no Save, confirmation, model, or setup. The same Note remains findable and can
explicitly Resume refining. The dedicated Thought Workbench keeps a document-like
Note beside a focused interview: **Ask AI** starts one durable turn, and explicit
**Add & ask next** atomically enriches the Note and reserves exactly one more.
When AI is unavailable, **Set up AI** opens Models directly. Models now offers
honest GGUF/llama.cpp local setup, three ready-to-fill OpenRouter Qwen presets,
and a first-class custom-provider path; the selected **Thoughts & notes** target
is the same target Workbench admits and executes. Context is explicit and visible:
new Thoughts begin with none, pinned Everyday context attaches in one compact
interaction, and only qualified refs cross HTTP/MCP before the server freezes
exact versioned leaves. The same compact picker can explicitly promote the
complete current set for future local create/adopt. That hub-local default
starts empty, never changes existing Thoughts or syncs, and a bad source skips
the whole set with a named receipt instead of blocking capture or invoking a
model. The next move is typed local outcomes.

Phase 140 — The First Sentence remains active at 5/6 with only its final
physical-device legs open. HS-140-01 through HS-140-05 are done.
A fresh owner gets one
quiet first-value Chair; the edited sentence copies truthfully or keeps under
one retry-safe note id, then opens through the normal-Chair note seam. A
transcript cannot prematurely complete onboarding. The later reveal has a
furnished six-drawer Desk, six editable starter notes, and explicitly
attachable Everyday context without invented owner facts or fake AI readiness.
Named capture failures stay in place with editable fallback and one exact
recovery action; retained audio is proven locally before Retry unlocks.
Every exit now furnishes before it reveals the normal Chair; deferred text is
explicitly saved under stable Note custody, and the six-drawer Floor survives
reload at both widths without horizontal overflow. The cold-walk glass cut now
removes internal roadmap objects from the ordinary Floor/List without deleting
Delivery, and the 393px furnished face is collision-free. Copy/Keep custody,
public first-run wording, local gates, and fresh counsel pass. The runner had no
physical microphone, so the real-mic and physical permission/no-speech legs
remain open rather than being replaced with synthetic proof.
The owner has also directed the next spine: a durable thought-refinement loop
that clarifies one question at a time, uses explicit context, and can freeze
typed/tool-backed proposals before existing policy and receipt machinery acts.
The audited design, connector truth, sequencing, and P0/P1 risk register are in
[`proposals/thought-refinement-spine.md`](./proposals/thought-refinement-spine.md);
Phase 141 is active at 6/9. Its raw-custody/CAS keystone is done: one atomic
aggregate preserves the original, owns a revisioned working Note, and closes
ordinary Note and paired-sync bypasses through a verifiable command ledger. The
resumable-thought contract, Develop bridge, immediate local completion, and
one-question refinement turn are done: owner-only bounded
load/list, mandatory cursors, a durable before-dispatch link for every physical
Ask attempt, and local capture/adoption/re-entry without a model.
Visible context is also done: empty by default, one-tap Everyday context,
versioned server-side hydration, named stale repair, and exact HTTP/MCP parity.
Its integrated default extension keeps Phase 141 at 6/9: an explicit complete
future set applies only to later local create/adopt; current removal, stopping
the future default, whole-set fail-open, and hub-local no-sync policy are named.
Next come typed local outcomes and a real GitHub actuator proof.
Jira/calendar writes remain truthful follow-ons.
The owner ordered a complexity cut. The proposed Dashboard Door is
cancelled: no TODO kanban, calendar aggregation, or new control center. A fresh
HoldSpeak will open on one obvious job—dictate one sentence—then reveal the
normal Chair after a useful result or an explicit Continue later. The phase
reuses existing capture, editable result, Copy, Keep as Note, retained-audio
recovery, and content-free first-value receipts. The reveal is furnished with
six useful drawers and editable, explicitly attachable Everyday context—never
fake personal facts or a pretend configured AI.
See the [current phase record](./phase-141-from-thought-to-work/current-phase-status.md).

**Previous update (2026-08-15): Phase 131 — One Admission Path — COMPLETE
(17/17).** The executable census closes at 100 sites, zero findings, zero blocking
families, and zero unregistered execution. The assembled real-model walk passes
all ten legs on `.43`: immutable revision dispatch, Ask/Agent,
Sequence/Workflow, Workbench work and memory, bounded scheduled work, finite
services, meeting and dictation sessions, fallback/indeterminate/sync/restart,
and the one-path fence. The web suite is green at 811 tests. The full inherited-
red backend output was read and name-diffed against HS-130-10; bounded current
regressions were repaired and the final ledger has zero Phase-131 regressions.
[`final-summary.md`](./phase-131-one-admission-path/final-summary.md) is the exit
record. PR #454 delivers the phase; Phase 132 remains separate follow-on work and
Swift remains held.

**Previous update (2026-08-08): Phase 129 — One Grammar — COMPLETE (11/11).**
The exit walk passes all 38 surfaces with pinned-foot, stationary-head,
no-overflow, height-cap, and console assertions green at desktop and mobile;
engine-hit desk-object openings, the in-world Note editor, Trust, Components,
and every live Go entry are in the evidence pack. The full web check chain is
green (779 tests). Backend triage records 96 pre-129 inherited failures for
Phase 130, fixes the one 129-caused delivery collector failure, and marks mesh
dispatch flaky/environmental; the owner may overrule this amendment at the
sitting.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-10 shipped.**
The token gate is green (23 remaining raw-value findings to zero; 27 findings at charter) with reviewed inlet and phone-sheet
radius tokens plus a current, per-file material allowlist. The live desk walk
proves zone, meeting, artifact, and workbench opens; its earlier double-click
failure was a canvas-walk artifact, now guarded by a title-addressed engine-hit
helper. Delivery names an old rail's missing capabilities/cursor-events protocol
as incompatible instead of exposing `DW exited 2`.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-09 shipped.**
Audit C's state dialects now use the shared grammar: `SurfaceState` legs,
compact named receipts, and folded RAW material. Runtime Guide is a compact
reference surface; the legacy WorkbenchCore item was moot because the core was
already deleted.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-06 shipped.**
The window is now the responsive viewport: Delivery, Intelligence, Roadmap,
and list content reflow through the named `surface` container at their own
window-body widths, never the browser width. The retired `desk-surface` alias
and anonymous container queries are gone; shell and sheet media rules remain
explicit exceptions.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-05 shipped.**
Speak now has one frame-owned foot: the shared slots carry readiness, landing
receipts, Review, and Export. The last sticky Speak rail is gone, seven dead
footer/style paths are deleted, and History, Live, Process, Project Memory,
and Delivery now use the shared receipt-slot grammar.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-08 shipped.**
The fixed InlineEditor lightbox is gone. Note, KB, Recipe, and Workflow editing
now stays in an open pullout or opens as a real origin window with standard
Escape and focus return; the existing autosave path is unchanged.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-03 shipped.**
The populated Intelligence Brief now keeps its fit-content card within the
working band: the 9,710px pathology becomes an 804px desktop window with a
scrollable body and reachable foot/grip. Its headline wraps at the pullout
edge, and dock-opening Brief no longer creates a phantom BACK step.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-04 shipped.**
Delivery and Tool Inspector frames now keep their heads fixed over one body
scroller; Constitutional Context and Delivery Dossier lost their duplicate
scroll paths. The Activity list was verified fixed by HS-129-01's host-body
repair, and unreachable legacy WorkbenchCore plus its orphan types/styles were
deleted after a repo-wide route/registry/import census.

**Previous update (2026-08-08): Phase 129 — One Grammar — HS-129-02 shipped.**
At ≤720px, every DeskWindowFrame sheet keeps its head and foot pinned while
its body is the sole scroll owner. The contract is covered by a mocked compact
viewport test and a 393×852 Playwright walk through Speak, Settings, Meetings,
and Intelligence.

**Previous update (2026-08-07): Phase 128 — Desk Intelligence — DONE (10/10).**
One Intelligence pullout brings Brief, Follow-Through, and Receipts together
on the Desk. Ten stories delivered its shell, three views, dock and palette
entry, WHY affordances, cross-links, attention badges, responsive sheet
behavior, and the walk covering every view, receipt search, type safety, and
intelligence services.

**Previous update (2026-08-07): Phase 127 — The Decision Receipt — DONE (10/10).**
Every consequential choice gets a permanent receipt: decision, rationale,
alternatives, owner, affected work, review date, and the conversation that
produced it. Schema v40→v42, DecisionReceiptService (create from meeting
or desk, append-only revisions, bidirectional work links, review queue,
supersession with retained evidence chains, ten-second FTS retrieval),
5 MCP tools, 1 MCP resource, local-first sync with LWW and tombstones.

**Previous update (2026-08-07): Phase 126 — The Monday Brief — DONE (9/9).**
The desk speaks first. At first open each day, the brief reconstructs the
operating picture: material changes from pipeline events, breakage with
repair paths, waiting work (overdue follow-through + high-priority loops),
and decisions requiring the owner. Deterministic composition — empty state
says "Nothing material changed," never invented content. Schema v40,
MondayBriefService with four collectors, MCP tools + resource, FastAPI
routes, and the walk proving the full pipeline.

**Previous update (2026-08-07): Phase 125 — The Follow-Through — DONE (10/10).**
Phase 124 shipped the observer: every service call recorded. Phase 125
made the desk follow through. Every meeting becomes a living execution
board: decisions bridge into commitments with owners and due dates,
aftercare enforces triage, and a unified board answers "who owes what?"
with provenance back to the sentence where it was promised. Observer
wired into production, board projection, decision commitments (schema
v39), aftercare triage, cadence loop collection, due/stall semantics,
write-through verbs, provenance on cards, MCP tools + resource + routes,
and the walk proving the full pipeline end to end.

**Previous update (2026-08-07): Phase 124 — The Observer — SHIPPED.**
Phase 123 completed the service pipeline: 33 services own every operation
in DeskOS, 41 MCP tools provide the full programmatic surface. Phase 124
added the observer — a `PipelineObserver` protocol with an `@observed`
decorator that records every public service method call (who, what, args,
result, timing, correlation) to a local append-only `pipeline_events`
SQLite table. An `EventQueryService` answers "what did the desk do?",
MCP resources expose the event stream, and the walk proves every service
is observed. No telemetry leaves the machine (Article III.3). PR #442.

**Previous update (2026-08-06): Phase 123 — The Pipeline — SHIPPED.**
Phase 122 built the skeleton: eight services and ten MCP tools. Phase 123
finished the boundary: 33 services, 41 MCP tools, 16 resources, route
handler bypass census 157 → 2. PR #441.

**Previous update (2026-08-06): Phase 122 — The Backbone — SHIPPED.**
DeskOS becomes programmable. Extract transport-neutral application
services from every route handler (workbenches and recipes are fat
controllers requiring substantive extraction; remaining services are
mechanical wrapping for pipeline consistency). Then plug two adapters:
thinned FastAPI routes and a new MCP server (10 day-one tools, Python
stdio sidecar). Walk harness (Python Playwright page objects, isolated
hub fixture, screenshot manifest) and desk doctor. Every operation in
the system flows through one pipeline regardless of caller.

**Previous update (2026-08-06): Phase 121 — The Fluency — CHARTERED.**
Kit-first resequence: story 01 builds five missing OS primitives
(SurfaceState action slot, SurfaceFooter with egress|receipt|verbs
slots, LedgerFilter with persistence, useCopyReceipt, useUndoReceipt);
story 02 migrates all 8 footer species + 10 footerless programs to
SurfaceFooter; stories 03-11 are adoption passes (pullout dead ends,
undo, keyboard/ARIA, windowcraft, universal verbs, command center
palette, live desk truth, error recovery, copy/export + attention).
The walk proves keyboard-first fluency. Held: escape consolidation,
onboarding, frecency, push notifications, lazy mounting, per-object
deep links.

**Previous update (2026-08-06): Phase 120 — The Reckoning — DONE (11/11).**
Four Opus scouts audited every UI surface. Terra verified. Ten Terra
builders implemented across three waves, ten Opus verifiers confirmed —
all passed first attempt. ~30 files modified, 1 deleted, 1 created.
Workbench agents reforged (inlet CSS, status hierarchy, sprites),
blank pullouts filled, chains readable, mic labels swept, constitutional
context rebuilt with desk primitives, editors unified on gadgets, emoji
replaced with sprites, dead ends closed, chrome bugs fixed, runtime
faces converged on Signal material. Owner walked it.

**Previous update (2026-08-03): Phase 116 — The Workbench — CHARTERED.**
The Workbench is a new desk primitive — a surface where an agent sits,
works through items, and produces receipts. One agent, one inference
target, one schedule. Nine stories: the contract (schema/API/kernel),
the surface (desk window), constitutional context (always-on briefing),
the composer (voice/text/grounding/drop), pre-built templates (TODO,
Triage, Meeting Prep, Delivery), skills (reusable procedural knowledge),
the conductor (scheduled runs), the morning brief (first autonomous
workbench), and the walk. Grounded in Hermes Agent research,
Delivery Workbench Phase 36, and the existing inference target system.

**Previous update (2026-08-03): Phase 115 — The Polish — DONE (8/8).**
73 violations remediated across ~30 Desk surfaces. Every surface on the
Desk now looks like it was born from the same OS.

**Previous update (2026-08-02): Phase 112 — Enough — CHARTERED.** The
owner's post-111 verdict, verbatim: "Enough with the
over-complication, enough with me not really even understanding what
HoldSpeak does and what HoldSpeak is." Six stories, nothing else:
one dial for endpoint+model (the InferenceTarget becomes the only
truth; the Prefs models module the only face), Speak performs the
flagship act (hold TALK in the room → text lands through the real
pipeline; today the flagship room only dry-runs into a void), the
architect's desk seed + reset-to-seed (ADRs / Meetings / Rules /
Decisions / Reference / Inbox; today a desk is append-only and the
clutter is dev residue), the plain-story docs, the fresh-HOME
sitting walk, and the owner's live rider — the open mic (one
browser mic grant on the Desk, continuous stream, VAD-segmented
utterances; push-to-talk byte-identical): the voice-first
environment. Phase 111's own sitting remains pending in parallel.

**Previous update (2026-07-31): Phase 110 CLOSED, Phase 111 SCAFFOLDED.**
Phase 110 replaced the macOS material model with Signal Workbench:
opaque surfaces, beveled depth, 2px corners, mono chrome font, solid
bars, rectangular gadgets, no backdrop-filter. Phase 111 is the
fast-follow surface refinement — agents diligently auditing every
dropdown, config pane, list view, conversation surface, and interactive
element to full coherence.

**Previous update (2026-07-30): Phase 108, The Locked Room, is CLOSED
7/7.** The transitional effect debt is **15 → 0** (and **38 → 0**
from the corrected baseline); raw desktop typing crosses a spawned,
warrant-validating child; terminal text and keys universally enter
`process.input@1`; CLI reads require authenticated principals; generic
executor liveness and the mandatory built-bundle browser gate landed. A
real marker landed in TextEdit with native and kernel success receipts.
The corrected assembled closeout passes 8/8 machine beats in one session;
the owner's verdict was "'s all's good, moyt."
[Final summary](./phase-108-the-locked-room/final-summary.md).

The long line below is the immediately preceding Phase 109 close
snapshot; its statement that Phase 108 was reserved is historical.

**Current snapshot (2026-08-18): Phase 140 — The First Sentence — ACTIVE
(5/6; physical-device legs open).** Simple arrival, furnished reveal,
Copy/Keep custody, both-width glass, public wording, local gates, and fresh
counsel pass. GitHub Actions was not watched. The runner had no physical mic,
so the real-mic and physical permission/no-speech legs remain open.

**Last updated:** 2026-08-24 — **Phase 143 (Intelligence Router) Story 08 IN PROGRESS** on `feat/hs143-08-meeting-adoption`: **Phase C and Phase D are structurally complete.** After the C1 checkpoint closed by owner authority (five counsel rounds, durable executor lease + epoch fencing landed; the loop-cap law was born there), C2 put installed meeting plugins on the routed queue with frozen ID/revision/schema members and made the owner's Disabled-plugins setting real in production, and C3 made Stop-handoff the reserve-inert primitive's first production adopter — unknown-disposition terminals stay reserved forever while a linked fresh local run auto-admits per the owner's ruling, and reserved handoffs are fenced from the recovery glass's generic Retry/Skip. Phase D slice 1 then put speech capture on the router: atomic routed capture admission for owner and wake sessions, routed transcription on frozen members, one bounded P=1 preload, and egress badges derived from every frozen route leg. Its three blocked rulings plus two code-review-stream findings went to Sol in ONE capped round (design amendments 7–11): speech is local-only with admission-time refusal (remote transport ledgered), cold-MLX wake works via a derived preload member (proven with a real unloaded transcriber, the faked-loaded test retired), the wake revision binds in the immutable parent snapshot, the two migration markers couple into one cutover switch, and faster-whisper's constructor load is a ratified local exception. Phase D slice 2 then cut the pre-session warm onto the router: the parentless startup warm freezes a capability-only owner transcription assignment and derives the SERVICE preload route in one transaction (mutable ModelConfig bytes select nothing), the MLX walker runs only frozen candidate/stage sequences, and warm reuse is deployment-revision + durable-receipt gated; a denied warm defers to the first lawful transcription, so capture never goes dark because warming was denied. Sol's capped pass on the whole phase then found and fixed two ordinary-path defects in the one permitted round — the default cold wake was dead on a migrated install (the pinned-on dictation pipeline collided with the closed wake policy; wake now lawfully authorizes its routed pipeline tail and the proof runs the default config end-to-end), and a deferred faster-whisper warm lied forever as warming (now settles to not_loaded) — closing Phase D RATIFIED-WITH-NOTES. Phase E (background adopters) then opened with a Sol-ratified design — only the Rails observer is true background SERVICE work; Cadence/decision/delivery are owner request-time drafts — and slice 1 shipped the Rails observer on the router (sealed SERVICE policy, batch-hash replay, event-only degradation with a real refusal receipt, sentinel migration), catching three real defects on the way: existing databases refused newly added parent kinds (reconcile now rebuilds on kind drift), SERVICE authority bases were being overwritten by the generic basis, and a stale shared spine rig. Sweep 6453 passed / zero branch-new — eleven inherited baseline failures now pass on this branch. GitHub Actions is not consulted (owner order); no push until the arc is ratified and the owner agrees. Next: Phase D slice 2, Phase E background adopters, Phase F cleanup.
— the kernel is at the entry points**. **HS-106-09 shipped truth-audited docs**:
backend architecture names the four caller calls, executor plane, journal/bus
rule, all six registered operation types, and a rendered lifecycle diagram;
Security leads with the cooperating-code-only boundary before any prevention
claim, names 36 of 40 ledgered sites outside the kernel, and points to RFC §5b
confinement as the stronger threshold; the User Guide and README carry the real
PR follow-through loop in owner vocabulary. Every claim is mapped to shipped
file and line. Mermaid rendered, focused guards passed 25/25, and the full suite
recorded 4,279 passed / 41 skipped / only the accepted voice-notes wording
failure. Earlier: **HS-106-08 shipped PR follow-through on real PR #387**: an exact registered worktree took a bounded agent through
`process.spawn@1` and child `process.input@1`; the agent's real Bash read became
a causally linked `tool.call` child with its own approval, claim, and receipt; a
real `.43` `inference.run@1` produced a durable review artifact; and the full
GitHub proposal remained readable before one approved `actuator.egress@1`
comment landed exactly once. Two denied proposals did not land. A live
credential yank retained the stale row, preserved local agent/draft verbs, and
refused only GitHub verbs by name. The 1440 and 393 walks show the exact row and
full proposal in place without a modal or mobile body overflow. The caller
surface remains exactly `read` / `submit` / `decide` / `events`, no second
consent path exists, and the protected kernel spine files are byte-unchanged.
Earlier: **HS-106-07 shipped bounded inference and the phase's PASS verdict**:
all three heterogeneous drivers share admission, principal, journal, and
receipt machinery with zero driver-specific broker conditionals. Earlier:
**HS-106-06 shipped actuator egress**: `actuator.egress@1` creates and links the
existing durable proposal; `ActuatorExecutor` remains the driver and closes the
kernel receipt from the native audit ref. Earlier: **HS-106-05 shipped terminal input**: `process.input@1` admits terminal
text once, exact-claims its existing native command, and closes the kernel
receipt from the unchanged Phase-94 node receipt. Earlier: **HS-106-04 shipped
the spine**:
`holdspeak/kernel/` now exposes
exactly four caller calls (`read`, `submit`, `decide`, `events`) plus the
separate `claim` / `receipt` / `reconcile` executor plane. The existing
Phase-104 gate is the one native decision record behind `decide`; there
is no rival approval surface. Authority resolves once in four ordered
layers and rides as an expiring, revocable, one-use warrant bound to the
immutable envelope hash, target, and placement. Lifecycle facts land in
a durable per-stream SHA-256 chain containing refs, hashes, and bounded
heads, while canonical domain records remain authoritative. A real
spawned hub proved refusal receipt → owner decision → node claim →
terminal receipt, agent decision refusal, immutable payload/target/
placement, and real SIGKILL restart with byte-equal cursor replay plus
an honest indeterminate recovery receipt. Density and tamper mutations
failed by name, then restored green; every broker module remains under
the unchanged 300-line cap with zero driver-specific conditionals.
Final kernel/gate/schema proof: 64 passed. The exact full suite recorded
4,245 passed / 41 skipped / 1 pre-existing unrelated UAT wording
failure, documented in the story. Earlier: **HS-106-02 and HS-106-03
landed both §8 prerequisites** — typed principals on loopback and the
40-site effect census as executable law.
**HS-106-01: Article XI is
in the Constitution.** The fourth council pass REFUSED the drafted
Article and was sustained on three counts, including a
process-topology definition error the first three passes all missed;
clauses 1-5 landed as the council rewrote them and the drafted clause
6 was deleted. Its further recommendation to defer ratification
entirely was overruled by the owner under Article X.1, who ratified
early behind a transitional migration provision — recorded as dissent
in the archive, the RFC council record, and the Constitution's own
amendment record. Clause 6 is executable, not rhetorical: the register
is the HS-106-03 effect ledger, no agent principal may reach a path it
names, and the clause expires with the register. Clause 2 is knowingly
false at ratification, which is precisely what clause 6 enumerates.
**HS-106-02 principal separation**. **HS-106-02 principal
separation**: loopback has lost all request-authority work. HTTP and
WebSocket derive typed owner, agent, and node principals from distinct
credentials; agent identity is minted at spawn / SessionStart, expires,
revokes at SessionEnd or factory kill, and rotates on respawn.
`decide`, posture, and delegation close at the edge by principal
right; gate and authority records no longer trust caller actor/session
labels. The owner's first load stays invisible-auth via a tokenized URL
captured and scrubbed by the Desk. Proven by a real `claude -p` process
against a real hub (proposal 200, decision 403 named `agent` +
`decide`), a 1440/393 screenshot walk, and 4,231 passing tests.
**HS-106-03** turned Article XI's migration debt register into
checked-in law and an executable fence — exactly 40 static effect
statements, 4 covered and 36 not, with additions and removals named by
file:line and family. Import origins survive from-imports, renamed
imports, module aliases, and callable aliases across all five families
(a review pass caught and closed the from-import evasion). The empty
broker field is already bounded by a 300-line module budget and a zero
driver-specific conditional census; mutations proved every fence fails
loudly, and no effect site was rerouted. The broker spine (HS-106-04)
is unblocked. Earlier: **Phase 106 chartered** from the owner's web-OS
charge, with Article XI's migration provision and council dissent on
record. Earlier: **Phase 104 (Borrowed Fire II)
CLOSED 8/8, all in one day**: the ledger, the fail-closed gate, the
gate under attack, PR receipts, session receipts, docs, the
seven-beat walk, and the sitting's own rider — **HS-104-08 the icon
family reforged** (the sitting's chartered rider):
crystals and avatar bitmojis are gone — knowledge is a bound tome,
an agent is a mechanical automaton head, and the whole 58-sprite
family regenerated in one Workbench-2.0-modern language via the
documented Pixellab recipe, states on disk, guard green, read on
the real desk at both densities. Earlier: **machine scope COMPLETE,
the sitting pending** — the HS-104-07 walk
passed live in one staged session (`seeded-desk-watched-hand`):
arm + honest readback, a real held call approved from the shade, a
real deny whose reason course-corrected the agent verbatim, expiry /
kill-mid-hold / inert-unarmed crown cases, PR receipts with a real
network yank, the three-tier receipt line with the price-row
absence rule, and the full sweep green. The phase flips CLOSED when
the owner drives the same seven beats and the verdict lands
(Article IX.4). Earlier the same day: **HS-104-06 docs** — the gate, PR receipts, and session receipts at
the real entry points (USER_GUIDE beside the steering material,
SECURITY boundary + egress row, ARCHITECTURE paragraph + rendered
mermaid, one README paragraph leading with off-by-default), every
claim truth-audited on the shipped tree. Earlier the same day:
**HS-104-05 session receipts** — one line, three tiers, every number
labeled: hub-record truths always; token figures only where the
ledger vouches (the gate hook's Stop leg reports the agent's own
transcript totals, cache figures separate); cost only with a price
row, absent otherwise; per-tool hold latency sample-floored. Proven
on a real gated claude session and a real steered pane (which
honestly renders NO token line). Earlier the same day:
**HS-104-04 PR receipts** — the candidate-Y deferral paid: registered
sources' PRs as honest receipt rows (one batched `gh` call, CI
conclusion not logs, observed-at always printed, attribution wearing
its epistemics: exact / name-match-only / unattributed), needs-you
ordering, local-only See-diff with an explicit-fetch offer, stale
rows retained and named on a real network yank. Earlier the same
day: **HS-104-03 the gate under attack** — the hostile checklist pinned as
tests that bite: restart / replay / TOCTOU over real HTTP against a
real spawned hub (SIGKILLed mid-hold and restarted), expiry race +
double decision on the injectable clock, unarmed latency budgeted,
redaction grep-censused; the load-bearing three proven by mutation
(guard out → named failure → guard back → green). Earlier the same
day: **HS-104-02 the tool-call gate** — a steered agent's risky call now
stops and asks the desk: fail-closed PreToolUse hook (inert unless
BOTH opt-ins; armed errors always deny by name, no timeout
auto-allow anywhere), proposals persisted as records never authority
(schema v25, one transition chokepoint, census-pinned), Approve/Deny
with an in-place reason on the shade, restart invalidation, redaction
to sha256 + 120 chars everywhere. Proven on real metal: two real
`claude -p` sessions — approve ran the held command, the deny reason
reached the agent verbatim and course-corrected it; hub-down /
expiry / kill-mid-hold all denied by name. Earlier the same day:
**HS-104-01 the capability ledger** — `holdspeak/agent_capabilities.py`
(4 adapters × 5 capabilities, hand-declared standings:
authoritative / inferred / unavailable), `GET /api/agents/capabilities`
+ contract schema validated against the live payload, the "Agent
capabilities" doctor census (red on a consumer the ledger cannot
back), and `require_capability` — the enforcement hook the gate
(HS-104-02) and session receipts (HS-104-05) must route through.
Earlier: **Phase 105 (Workbench) CLOSED 7/7 in
two days**: icon system (1:1 pixel handles, real state images, live
badges), drawers opening into remembering windows, Info/tooltypes on
everything, drop-onto composition with named verbs, the menu bar on
one verb registry, the Desk Grammar written as law, and a closeout of
sweep 4150/37/0 + a six-beat composite walk; the owner's sitting
accepted with the rider that Phase 104 + the kernel are the "actual
OS" program. Earlier: **Phase 105 (Workbench) 1/7:
HS-105-01 shipped the same evening** — the icon system: one uniform
cell (64px pixel art rendered 1:1, killing the fractional-upscale
mush), real dual-state images on disk for all 68 sprites, a drawer
for directories, badges only from named live routes, deterministic
clean-grid defaults, and the phone leading with the list at density;
gated by the owner on a real-art mockup ("I accept"), proven by a
live density walk that caught and fixed two defects, guarded by
`iconCell.test.ts` + `web/ICON-DISCIPLINE.md`. Earlier the same day:
**Phase 103 CLOSED (7/7)**: the
owner's second sitting accepted HS-103-07's chat-surface craft ("I
accept"); before it, the phase's documented pre-existing failure
family was paid on PR #367's repair commit and the machine chain
re-ran clean on merged main (pytest 4148/37 with zero unrelated
failures, web 318/318 + build + tokens gate) — full record in
evidence-story-06.md. The roadmap pointer moves to Phase 105.
Earlier the same day: **Phases 104 + 105 scaffolded; the
kernel RFC drafted.** Phase 105 (Workbench — the owner's "Workbench
2.0 on steroids" verdict made seven implementation-grade stories:
icon system, drop-onto, zone windows, Info-on-everything, menu bar,
docs, sitting-loop closeout) and Phase 104 (Borrowed Fire II — the
AgentGlass-derived tool-call gate, PR receipts paying the Phase-94
candidate-Y deferral, honest session receipts) are chartered;
activation order is 103 close → 105 → 104.
`docs/internal/PLAN_KERNEL_OPERATION_BROKER.md` (DRAFT) records the
kernel pivot — a three-model council (resident + Codex + gpt-5.6-sol,
opinions archived under `proposals/`) converged on an operation
broker (read/submit/decide/events + an executor plane) and a
proposed Article XI awaiting the owner's ratification. Standing
direction recorded in both charters: ALL iPad/HSM legs held — the
web desk is polished to the atom as the spec a Swift recreation
later builds from. Earlier: **Phase 102, The Refit, 6/7**:
HS-102-06 (Speech Settings) shipped — the worst offender per the
owner's screenshots ("an absolute joke — a cacophony of tiles,
panes, forms, form groups") is recomposed: the two raw wire dumps
(Pipeline readiness / Resolved delivery) become a composed
`SurfaceGroup` line with a live toggle and an inline "Create it" verb
on the missing-KB warning; Knowledge and Instructions lose their
orange Save buttons that had ALWAYS 400'd (a pre-existing wire
mismatch — `project-kb` needs `{kb: {<KEY>: value}}`, not
`{content}}`) in favor of a real facts list + `.hs/instructions.md`
edited correctly in place; the runtime knobs now embed the SAME
`RuntimeDestination` Settings already uses instead of a third
Backend/Runs-on/Latency-budget stack. A second live-caught defect: the
Learning digest was showing only its metadata wrapper while the real
counts sat hidden in `totals`. `EditInPlace` (shared kit) gained
select-on-focus so placeholder-shaped empty values get replaced by
typing, not appended after — every call site in the app benefits.
Proven live on a real seeded desk, both viewports: KB created, a real
fact added and persisted, Instructions saved correctly, the runtime
bay switched. New guard:
`test_dictation_core_speech_settings_never_regresses`. Earlier the
same day: **HS-102-05 (the selection mark yields to open) shipped** —
a click-grammar fix: opening a card (double-click, touch tap-open, the
context menu's Open, the a11y layer) now clears the selection ring +
"N selected · Ask AI" bar in the same frame the card appears, in all
four paths through `openPullout`, guarded so the Ask composer's held
selection (the HSM-16-04 carve-out) is never stripped mid-compose.
Proven live on a real seeded desk at 1440+393 (mark, then open — ring
and bar gone) plus the Ask carve-out (open Ask, double-click a
different object, Ask keeps its context); the `smoke()` walk leg grew
three assertions and passed live. Earlier: **HS-102-04
(the Meetings wings) shipped** — Artifacts recomposed from a
`Disclosure`+`SurfaceCode` dump to the SAME `SurfaceLibrary`
composition Blocks already uses (artifact body as the tile face via
`Material`); Outcomes rows sort needs-you first with a new `quiet`
treatment for settled meetings; Record leads with a "Record meeting"
verb above the existing drop well. Proven live with REAL synthesized
data: a transcript imported through the real wire, run through the
real plugin chain against the LAN model at `192.168.1.43` — 4
artifacts, correctly library-composed at both viewports; the
`meetingflow` walk leg grown and run live against populated data.
Earlier: **HS-102-03 (Ask AI) shipped** — the composer is
now one well (`desk-chat-well`, the same grammar `PersonaChat` and the
capability cards already use), Runs on/grounding/rails fold into the
well's foot as captions, and the printed answer renders through
`Material` instead of a raw-markdown `<pre>`; proven live end to end
including a real grounded ask against the LAN model — a correct
answer, `Material`-rendered, kept as a real desk artifact. Earlier:
**HS-102-02 (Live Meeting) shipped** — the
room's stat strip, Bookmark form, Intent routing preset, Preview-route
textarea, and Deferred-plugin-jobs dump (all visible at the FIRST
instant of a live meeting, before a word was spoken) fold behind a new
"Configure meeting" gear door; the working face is now one verb, one
quiet facts line, and the transcript as a `SurfaceStream`, with
Bookmark as an inline verb on the stream instead of a form section;
proven live at 1440+393, `/api/*` wire byte-identical. Earlier:
**HS-102-01 (Runs on) shipped** — destination create/edit is now
choice bays (Endpoint/This device/Paired device/Mesh node) expanding
in place on the switchboard, reusing the `RuntimeDestination` pattern
and closing a `SurfaceBay` kit gap, proven live at 1440+393 with
inline URL refusal, edit-in-place, make-default, and two-step delete;
wire byte-identical. Remaining: HS-102-07 closeout (the owner's
sitting). The design authority is the HS-101-02-approved
interior canon; each story works the round-9 way — hands first,
headed, both viewports. Earlier: **HS-101-04 round 9 (the reflex
round, PR #365) merged** — single-click selects / double-click opens,
cards open beside their object and coexist as real windows, growth
animates, right-click answers on the world, the note edits in place,
the ⌘K shelf's z-defect fixed; the owner's 101 sitting remains open in
parallel. Phase 101 itself stands at 3/4 (canon ratified and built,
PR #359; sweep 4122/0).

**Current phase:** [**Phase 141 — From Thought to Work**](./phase-141-from-thought-to-work/current-phase-status.md)
— **ACTIVE (6/9, 2026-08-20)**. Raw custody, resumable Thoughts, Develop,
Finish Thought, one explicit AI refinement turn, visible/default context, the
bounded Finish thoughts lane, and the dedicated Thought Workbench are shipped.
The Workbench uses the same application services through HTTP and MCP, keeps
model use explicit under the YOLO-first posture, and routes unavailable AI
straight to Models. Typed local outcomes are next.
Previous: [**Phase 139 — The Settings Reckoning**](./phase-139-the-settings-reckoning/current-phase-status.md)
— **COMPLETE (8/8, PR #465 merged 2026-08-18)**. Settings collapsed to seven
job-named tiles; the Models workbench and inline destination-key custody landed
in the merge follow-up. Previous: [**Phase 138 — The People Ledger**](./phase-138-the-people-ledger/current-phase-status.md) — **COMPLETE (8/8, 2026-08-17; fresh close counsel RATIFY-WITH-CONCERNS; owner sitting pending)**
— Encrypted relationship ledger for direct reports,
peers, and extended organizational relationships; grounding notes; manager
commitments in Follow-through; MCP capability. Previous: [**Phase 137 — The One Schema**](./phase-137-the-one-schema/current-phase-status.md)
— **COMPLETE (4/4, 2026-08-17; counsel RATIFY-WITH-CONCERNS; owner sitting pending)**. Owner mandate: kill the schema-migration
ceremony (a 1060-line chain + version pins + a gate that refuses the owner's
own DB) and replace it with one declarative, self-reconciling schema — no
versions, additive-only, edit the schema and it self-applies on open. Triggered
by the v63 fork (the owner's real DB drifted ahead of `main` after running an
unmerged branch). Previous: [**Phase 136 — Scheduled Recording**](./phase-136-scheduled-recording/current-phase-status.md)
— **COMPLETE (4/4, 2026-08-17; PR #462 merged; counsel RATIFY-WITH-CONCERNS; owner sitting pending)**. A focused standalone build off the
Dashboard Door reflection: the Chair can schedule a recording (one-shot
or recurring) that the hub starts on its own — via a visible
tap-to-cancel countdown that keeps the mic owner visible (Article
IV.3) — and auto-stops by duration. The Dashboard Door and the Narrow
Shell (the arc's sketched Phase 136) stay deferred; the Narrow Shell
renumbers when chartered. Previous: [**Phase 135 — The Comfy Chair**](./phase-135-the-comfy-chair/current-phase-status.md)
— **COMPLETE (15/15, 2026-08-16/17; PR #460 merged, owner sitting pending)**. The owner closed the consolidation era and
pointed the machine back at the functional side. Chartered from a six-pillar
functional-reality audit (six Opus auditors, file:line evidence archived in the
phase assets): every daily verb works when touched, every signal the hub
already emits lands on the desk, and every receipt and control tells the truth.
Fourteen stories: meeting stop/conflict safety, live action-item triage,
realtime frames consumed, one pipeline run per utterance, streaming-mic
honesty, write-failure receipts, Workbench editing repairs, Intelligence
truth, receipt model honesty, one placement dial, the Cadence reply route,
a green regression net, roadmap record truth, and the walk. Issue #450's
Wave 2 and the web-ownership slice move to the backlog.
Previous:
[**Phase 131 — One Admission Path**](./phase-131-one-admission-path/current-phase-status.md)
— **DONE (17/17, PR #454, 2026-08-15, merged `89259d4f`)**. Every actual model
invocation enters one runner, receives one kernel admission, executes one
immutable deployment revision, and ends in one terminal receipt; sessions admit
meeting/dictation/wake work with invocation children; enabled schedules carry
bounded delegation; the one-path fence closes at zero findings; proven on real
`.43` metal.
Previous:
[**Phase 130 — One Truth**](./phase-130-one-truth/current-phase-status.md)
— **DONE (11/11, PR #453, 2026-08-09)**. One resolver, deployment identity,
egress vocabulary, meeting policy, honest Ask placement, versioned settings,
collision-free secret slots, DecisionRecord naming, one-gesture Workbench, an
inherited-failure ledger, and a 19/19 live walk against the real `.43`. The
kernel-free half of issue #450 is merged at `0fc14aca`.
Previous:
[**Phase 129 — One Grammar**](./phase-129-one-grammar/current-phase-status.md)
— **DONE (11/11, PR #451, 2026-08-08)**. One window anatomy (head fixed,
body scrolls, foot fixed), one interaction grammar (the modal lightbox
dies, every input speakable, one state grammar), one CSS architecture
(four dead footer grammars deleted, container-query law enforced, tokens
gate green). The inherited 96-test backend ledger transfers to Phase 130.
Previous:
[**Phase 128 — Desk Intelligence**](./phase-128-desk-intelligence/current-phase-status.md)
— **DONE (10/10, PR #446)**. One Intelligence pullout: Brief,
Follow-Through, and Receipts views on the desk with dock badge, WHY
affordance, cross-links, and responsive sheet behavior.
Previous:
[**Phase 127 — The Decision Receipt**](./phase-127-the-decision-receipt/current-phase-status.md)
— **SHIPPED (10/10, PR #445)**. Every consequential choice gains a
compact, permanent receipt: decision, rationale, alternatives, owner,
affected work, review date, and conversation evidence. The phase unifies
existing origins without replacing them, then proves append-only history,
review, retrieval, MCP delivery, and local-first sync.
Previous:
[**Phase 124 — The Observer**](./phase-124-the-observer/current-phase-status.md)
— **SHIPPED (10/10, PR #442)**. Every public service method call recorded
to a local append-only `pipeline_events` table. Protocol, decorator,
SQLite backend, wiring to all 33 services, event query service, MCP
resources, doctor check, docs, and the walk.
Previous:
[**Phase 123 — The Pipeline**](./phase-123-the-pipeline/current-phase-status.md)
— **SHIPPED (13/13, PR #441)**. 33 services, 41 MCP tools, 16 resources,
route bypass census 157 → 2.
Previous:
[**Phase 116 — The Workbench**](./phase-116-the-workbench/current-phase-status.md)
— **CHARTERED (0/9, 2026-08-03)**. The agentic platform phase: the
Workbench primitive (agent + target + schedule + items), constitutional
context, skills, scheduled conductor, pre-built templates, and the
morning brief as the first autonomous workbench. Previous:
[**Phase 112 — Enough**](./phase-112-enough/current-phase-status.md)
— **CHARTERED (0/6, 2026-08-02)**. The owner's verdict made a phase:
one dial (endpoint+model collapses onto the InferenceTarget with the
Prefs models module as the only face), Speak speaks (the flagship
room performs the flagship act through the real delivery contract),
the architect's desk (a curated fresh seed — ADRs / Meetings / Rules
/ Decisions / Reference / Inbox — plus reset-to-seed with honest
tombstone semantics), the plain story (docs that say what HoldSpeak
IS in one paragraph), the fresh-HOME sitting walk, and the open mic
(the owner's live rider: one mic grant on the Desk, continuous
stream, VAD — the voice-first environment; PTT byte-identical).
Every story deletes more configuration surface than it adds.
Previous:
[**Phase 111 — The Refinement**](./phase-111-the-refinement/current-phase-status.md)
— **11/11 SHIPPED (chartered 2026-07-31, all eleven stories shipped by 2026-08-02); the walk exhibit is staged and THE CLOSED CLAIM AWAITS THE OWNER SITTING**. The
mandatory fast-follow to Phase 110: agents audit every program interior —
Settings, Speak, Meetings, Agents, Ask, delivery/process, system chrome,
the interactive-element kit, sprites — and RETHINK each to full Signal
Workbench coherence (Workbench 2.0 structural grammar, dark 2004-techy
render), closing with the refinement walk. Every room in the OS speaks
one language. Shipped so far (all proven live at 1440+393, full
suites green): 01 Settings — the Prefs rethink, the reusable gadget
kit, the sliding switch dead desk-wide. 02 Speak — the dictation deck
(TALK / LedMeter / STATE register), the journal a machine ledger,
toasts dead into the footer receipt bar. 03 Meetings — the archive
browser: SurfaceLedger catalog, the transcript in a sunken
SurfaceWell, stamped artifact receipts, recovery prose dead, the
1440 footer-overlap bug retired. 04 Agents — the crew console: one
SurfaceLedger crew board, persona detail a personnel record over a
SurfaceTraffic transmission log, steering re-rendered in gadget
grammar with the Phase-87 consent spine byte-untouched (steering.ts
zero diff; steering tests passed unedited). 05 Ask — the query
console: the thread is SurfaceTraffic, the grounding receipt finally
reaches the glass (GROUNDED ON N OF M + openable citations, one kit
species with Project Memory), all faults in-flow (the owner's live
error-overlap report structurally dead), zero naked HTML. 06
Delivery/process — instrumentation: the ~450-pin emoji flood dead
(census tokens + inverted NEEDS YOU cells), the process window a
zeroed instrument (kernel wire git-diff empty), PaneWell extracted
for story 11's xterm swap, and the owner's PR-387 duplicate rows
root-caused and FIXED on the wire (sweep on the read path; claims
adopt by session; 7 new tests). 07 System chrome — the biggest room:
one WorkMenu species (the z bug dead), the five verb lists collapsed
into ONE registry + keymap, THE FLOOR RIGHT-CLICK (NEW>/LAUNCH>),
the command-deck palette (Enter runs the top hit), the list view a
SurfaceLedger face, the sheet derived from the registry, one egress
species, naked debug behind RAW. 08 Interactive elements — the
two-dialect era ends: roving focus as kit law (every ledger
inherited arrows-walk in one change), the unarmed × extinct,
FoldGadget/PadGadget minted, the Wings pill reforged, six small
cores joined the kit, eleven legacy species deleted including the
OS's only modals (no-modals structurally true), ComponentsCore the
kit's living style guide. 09 Sprites — integer rendering ended the
fractional smear, the record orb sprite killed the last macOS
survivor, four orphans retired, the emoji era ended, the brand mark
and magnifier regenerated on the real desk; JUDGMENT.md filed for
the sitting. 11 The terminal pane — the owner's xterm.js ruling
real: raw ANSI peek (opt-in, hash gate intact), xterm as the one
PaneWell interior (lazy chunk, strictly a viewer — the armed
composer keeps the typing monopoly), true colors proven on a real
tmux pane with the read-only proof photographed; consent spine
untouched. Only the walk (10) remains. Owner
riders chartered the same day: HS-111-11 terminal pane (xterm.js),
07 desktop right-click + debug-RAW + command-deck palette, 08 roving
focus + arming deletes; the power-user usability doctrine PROPOSAL
(12 principles) filed at proposals/usability-doctrine.md awaiting
the sitting.

**Previous phase:**
[**Phase 110 — The Cohesion**](./phase-110-the-cohesion/current-phase-status.md)
— **CLOSED (7/7, 2026-07-31)**. The material model pivot: macOS Ventura
(frosted glass, traffic lights, 18px corners, Gaussian shadows) replaced with
Signal Workbench (opaque surfaces, beveled depth, 2px corners, rectangular
gadgets, mono chrome font, solid bars, no backdrop-filter). The foundation.

The line below is the Phase 108/109 close snapshot.

**Previous phase (newest):**
[**Phase 108 — The Locked Room**](./phase-108-the-locked-room/current-phase-status.md)
— **CLOSED (7/7, 2026-07-30)**. The register is empty, desktop effects
cross the spawned warrant executor, terminal input and CLI reads have
their audited doors, and the liveness/CI riders are green. The machine
closeout passes 8/8 and the owner accepted it. The older “Current phase”
line below is the Phase 109 close snapshot.

**Historical current-phase snapshot:** [**Phase 109 — The Long Memory**](./phase-109-the-long-memory/current-phase-status.md) — **CLOSED (8/8, 2026-07-30)**; chartered as: the second userland program — decisions as records with provenance and lifecycle, promotion to artifacts with causal lineage, long-horizon cited retrieval, the Project Memory window, and the process-window rider; Phase 108 (The Locked Room, §5b) reserved with its audited work list. Previous: [**Phase 107 — Close the Side Doors**](./phase-107-close-the-side-doors/current-phase-status.md) — **CLOSED (7/7, 2026-07-29)**; chartered as: the phase that moves the number Phase 106 could not. 36 effect sites still bypass the kernel as declared debt under Article XI clause 6; this phase migrates the four migratable families and names the rest. Honest ceiling stated at charter time: **4 → 30 of 40**, because all ten raw-desktop sites are the primitives in `typer.py` and close only under RFC §5b confinement — so clause 6 survives and does not self-repeal. Previous: [**Phase 106 — The Kernel**](./phase-106-the-kernel/current-phase-status.md), **CLOSED (10/10, 2026-07-29)** — the owner's sitting passed all eight beats (*"all passed, make progress"*). Four caller calls, six registered operation types, a hash-chained journal, and a spine that stayed **byte-unchanged** through four heterogeneous drivers plus a real product feature. The kill criterion PASSED. **Census delta: ZERO** (4 of 40 at start and close) — it built the desk and closed no doors, printed plainly rather than framed. [**Phase 106 — The Kernel**](./phase-106-the-kernel/current-phase-status.md) — **ACTIVE (3/10, chartered 2026-07-26)**: the RFC's strangler ladder as a phase — Article XI with the owner's migration provision, principal separation on loopback (the confused deputy), the 40-site effect census pinned as a test, the broker and hash-chained journal behind four calls (`read`/`submit`/`decide`/`events`), three HETEROGENEOUS driver slices (terminal input → actuator egress → inference runs) with the kill criterion applied for real at the third, and then the first §9 userland program: **PR follow-through** — see the PR that needs you, decide once, send a scoped agent at it, propose the comment, and get a receipt for every consequence. The fourth council pass (Sol) returned **do not ratify yet** with file-level evidence; its rewritten clauses 1-5 were adopted, the drafted clause 6 deleted, and the owner ruled to ratify early behind a temporary migration provision that names the unmigrated paths as declared debt and forbids agent access to them. Previous: [**Phase 104 — Borrowed Fire II**](./phase-104-borrowed-fire-ii/current-phase-status.md), **CLOSED (8/8, 2026-07-26)** — built, walked, ridden (HS-104-08, the icon reforge the sitting chartered), and accepted the same day ("Yup. Better. Let's close out the phase."). Previous: [Phase 105 — Workbench](./phase-105-workbench/current-phase-status.md), **CLOSED (7/7, 2026-07-26)** — the owner's acceptance carried the standing rider that 104 and the kernel are the "actual OS" program. Previous: [Phase 103 — Foundations & Borrowed Fire](./phase-103-foundations-and-borrowed-fire/current-phase-status.md) — **CLOSED (7/7, 2026-07-25)**.
— **IN PROGRESS (0/6)**. Chartered 2026-07-22 from a four-agent
research pass (3 independent Opus 4.8 analysts on
`ViuGiaLai/researchmind`, 1 independent Opus 4.8 skeptical audit of
HoldSpeak's own Desk-OS claim) the owner commissioned directly, debated,
and approved into six stories: session restoration (the one
load-bearing durability gap the independent audit found — the desk
remembers window geometry but not that a window was open), a
voice-guard coverage fix (three canon-violating dash/prose leaks the
audit caught live), and two skepticism-survived researchmind
carry-overs (per-claim grounding verification, endpoint health) plus a
UAT-harness fix so the flagship agent-steering feature is provable
end to end. [Phase 102 — The Refit](./phase-102-the-refit/current-phase-status.md)
remains open alongside at **IN PROGRESS (6/7, HS-102-07 closeout
next)** — its closeout needs the owner's own sitting (Article IX.4),
not a research pass. The
[Phase 101](./phase-101-the-native-innards/current-phase-status.md)
closeout sitting (HS-101-04) stays open alongside.

<details>
<summary>Prior roadmap status through Phase 90</summary>

**Last updated:** 2026-07-08 (**Phase 88 — The Rails-Aware Desk CLOSED 5/5, scaffolded and shipped the same day** — the rails are desk-native material. HS-88-05 closed it with a seven-beat walk against the real `dw`, `.43`, and a real tmux pane: ground an open story into an ask (control vs treatment) and a steer (the rail block lands in a real pane); receipt-not-scrape + bad-ref refusal; the observer journaling real rail motion on .43; the read-only census; the reach (a remote envelope named `@node`, then read stale + dropped); the journal grounds in turn. Docs shipped (USER_GUIDE rails-grounding + journal, SECURITY rails-as-material boundary, ARCHITECTURE paragraph). The remote-events worker daemon is the one deferred rider; B3/B4 handoff in the final summary. Earlier: **HS-88-04 the cross-machine reach** — a far node's worker POSTs `{node, ts, events}` to `POST /api/missioncontrol/rails/remote-events`; the observer merges the buffer each tick with the origin node STAMPED, honest liveness drops a quiet node's stream, the envelope carries EVENTS ONLY (a body-carrying event refused). Proven in-process (route → buffer → drain → journal, `@node` named); the pushing worker daemon is a deferred rider. Earlier: **HS-88-03 the ambient observer** — `holdspeak/rails_observer.py` (pure core: event diffing, batch summary, journal body) + `web_server._rails_observer_loop` (OFF BY DEFAULT, tails a bounded `dw events`, first obs a baseline, each new batch summarized by a RuntimeProfile model off the event loop into a `rails-journal` note; model-unavailable degrades honestly). READ-ONLY — a census forbids any rails-write path; `GET /api/missioncontrol/rails/journal` reads it. Proven live on .43: tailed 16 real rail events and journaled them accurately. Earlier: **HS-88-02 the rails picker** — `RailsPicker` (a `GroundingSection` sibling) flattens the belt's live projects (roadmap, current phase, its stories) into pickable rail objects, mounted beside `GroundingSection` in BOTH the ask panel and the Phase-87 steer composer; `buildGrounding(sel, rails)` merges desk objects + rails into ONE wire object so ask and steer send byte-identical grounding (parity pinned); the gauge is honest via `POST /api/missioncontrol/rails/size` (sizes only, the dw-named file, never content). Control-vs-treatment on .43 green. Earlier: **HS-88-01 rails hydrate as receipts** — `holdspeak/grounding_rails.py` resolves a `{repo, project, kind, id}` ref (phase/story/evidence/roadmap) through `dw context` (the CLI names the trace path, the read is contained), returns the SAME `GroundingBlock` the meeting path returns (`kind="rails:*"`, cap+cut); the ask AND steer routes fold `grounding.rails` in after the desk objects; a no-scrape census forbids parsing rail STATE from markdown. Proven live on this repo + control-vs-treatment on .43 (bare model guessed "GroundingBlock", grounded run answered `kind="rails:<kind>"`). Earlier the same day: **SCAFFOLDED (0/5)** from backlog candidate V / the owner's rails-aware direction: the rails become desk-native material — open phases/stories/evidence pickable in the grounding picker like a meeting, hydrated as RECEIPTS (`dw context` names the path, never a markdown scrape) into any run; plus the ambient dw observer, a local model journaling rail events read-only and off by default. Five implementation-grade stories in [phase-88-rails-aware-desk](./phase-88-rails-aware-desk/), building on the Phase-87 factored hydration seam. Earlier: **Phase 87 — The Steering Desk CLOSED 6/6, scaffolded and shipped the same day** — B2 at the owner's bar. HS-87-06 closed it with an eight-beat walk against one real tmux pane and the real .43 model: attach → refuse-unarmed → arm → steer → ground (control vs treatment) → classify → the crown cases live (recycled pane refused+revoked, TTL expiry refused+revoked, cross-surface disarm → unarmed) → the audit read back with every steer's row. Mechanical rules (chokepoint census, audit-completeness, desk locks over the pull-out tree) green; docs shipped (USER_GUIDE steer section, SECURITY consent boundary, ARCHITECTURE chokepoint paragraph); B3/B4 handoff in the final summary. Earlier: **HS-87-05 classify** — three triage verbs from the pull-out, all through existing write paths: keep-as-note (`POST /{key}/keep-note` mints a real desk note, lineage naming session/agent/ts, proven live openable), pin-to-story (a desk-side `manualPins` map in localStorage, a HOLLOW dashed ring on the conveyor that never disguises itself as the correlator's verdict and re-asserts if the registry changes), and flip-from-here (`flipTargetForStory` → the Phase-82 `proposeFlip`, the ProposalCard renders where it always does). Earlier: **HS-87-04 grounding** — desk objects ride into a steer. The Phase-83 ask hydration factored verbatim into `holdspeak/grounding.py` (`hydrate_refs` → `GroundingBlock`s; ask's `[MEETING:…]` headers a thin re-export so ask/recipes tests pass unmodified — one hydration truth); `compose_steer` fences each object with a `--- from <kind>: "<title>" ---` provenance header, caps context at 8 KB (over-cap refuses at compose time, `preview: true` returns the exact send text — executed == previewed), the composer mounts `GroundingSection` with the gauge. Control-vs-treatment PROVEN on .43: bare question → "I don't have access…", grounded steer → "Friday the 13th at 3:47pm, code-named BLUEBIRD", the exact composed text landing in a real pane. Earlier: **HS-87-03 the steer composer** — the desk now types into a live session. `coder_steering.deliver` is the ONE chokepoint: grant check → resolve the current pane → send to the VERIFIED `%N` (nothing re-resolves between check and keystroke) → audit row; delivery reuses `send_text_to_pane` verbatim. The `steering_audit` table (schema v12) records who/when/session/pane/shape for every attempt, delivered or refused — sha256 + first 120 chars, never the full steer. `POST /{key}/steer` (unarmed → typed 409, revoking refusal frames the disarm) + `GET /steering/audit`; the voice-first composer (MicButton, no-submit `⏎` toggle) mounts only while armed; the chokepoint census pins the call sites; steering routes carved into `coder_steering_routes.py` (Phase-79 budget). Live-proven: an armed steer lands in a real pane, the recycled-pane crown case refuses and revokes. Earlier: **HS-87-02 the arming grant** — consent with a countdown: in-memory grant store (monotonic clock, lazy sweep, fail-closed restart), `%N` pane identity pinned at arm and re-verified by `require_grant` (recycled/retargeted pane refused AND revoked — the live rig caught tmux 3.6 answering a dead target with rc 0 + empty expansion, now typed `pane_gone`), arm/disarm/grants routes with typed 409 refusals, hold-to-arm chip → countdown → one-tap disarm, armed rings on the pins, SECURITY.md consent model. Earlier: **HS-87-01 attach** — `coder_steering.peek_pane` (injectable runner, sha256 hash gate, ANSI strip, 200-line/64 KB caps), `GET /api/coders/{key}/peek` with the honest envelope (stale/awaiting/typed absences, never a 500), the SessionPullout on the desk (poll only-while-open, entry from the conveyor pin and the coder pull-out), the hub's one registry observer broadcasting `scope:"coder"` frames on awaiting transitions; proven live against a real tmux pane. Earlier the same day the phase was SCAFFOLDED from the owner's post-86 steering bar: the charter + six implementation-grade stories in [phase-87-steering-desk](./phase-87-steering-desk/); the RFC's B2 row expanded in place. Earlier: **Phase 86 — The Delivery Belt (read-only) CLOSED 5/5, scaffolded and shipped the same day** — the walk's shots caught this very story riding the belt; earlier the phase was SCAFFOLDED, from backlog candidate U / the [RFC](./proposals/delivery-belt.md): the AI-Headquarters floor — a projects registry on the hub, a belt per rails repo rendered from receipts (`dw state`/`sessions`/`events` + `gh` conclusions), `scope:"belt"` frames on the one bus, the `/belt` desk surface, closed by a live walk where a real story crosses the belt during its own shipping commit. The B0 substrate reconciliation shipped UPSTREAM the same day as delivery-workbench phase 16 ("the flagship tree", PR #2): the reader now parses this repo's 86 phases honestly — 397 spurious check errors → 31 real desyncs, consumed by HS-86-01. Earlier: **Phase 85 — The Mesh Edge CLOSED 5/5, scaffolded and shipped the same day.** HS-85-05 closed it with the live walk on the REAL hub: a second local process (`holdspeak mesh serve`, node `walk-edge`, its own HOME pointing at the .43 llama.cpp) served an agent chat, a meeting-intel reroute, and a dictation dry-run — badges reading `⇄ mesh · walk-edge`, the worker's own log proving every run executed in THAT process; killing the worker read offline everywhere and a forced run refused in 0.00s naming the node. The walk caught three shipping bugs the green unit suites could not: every LLM route ran the engine ON the event loop (a mesh run deadlocks the worker's claim polls — now `asyncio.to_thread` at all nine call sites, property-locked), `resolve_llm_capability` was mesh-blind (LLM plugins silently skipped while the reroute said executed=true), and the 14 builtin plugins speak `_chat_completion_text` — a de-facto second seam `MeshRelayIntel` now adapts onto the one relay wire. Plus: the v10→v11 upgrade silently dropped `profiles.node` (explicit ALTER TABLE guard), a worker recursion guard, and docs (MODELS.md mesh section, README, SECURITY egress row; guards green). The HSM follow-up — the Apple worker + per-device consent toggle on this proven wire — is recorded in the final summary.)
**Historical current-phase snapshot:** [**Phase 90 — The Factory**](./phase-90-the-factory/current-phase-status.md) — **CLOSED (3/3, 2026-07-08, scaffolded and shipped the same day)**: the fast follow to Phase 89. Phase 89 made an existing pane fully manipulable; this added the session LIFECYCLE on the same consent spine AND gave the whole capability its first on-glass surface. **HS-90-01: the factory** — `coder_factory.spawn/rename/kill`; `kill` reuses the STEER gate (armed + `%N` re-verified + drop the grant + audit), spawn/rename are name-validated (first char alnum, argv-safe, a bad name refuses before tmux) audited acts; the destructive verbs census-pinned; proven live (spawn → rename → arm → steer → kill). **HS-90-02: the manipulation surface** on the web desk — a KEY PALETTE (`^C`/`Esc`/arrows → `/keys`, armed-only), a `⧉ Panes` picker (attach to any `pane:%N`), a NODE CHIP (this Mac / a configured node via the relay, one `verbEndpoint` helper); the store node-aware end to end; built + desk tests 97/97 + screenshot. **HS-90-03: the factory on glass + the walk** — a `+ Spawn` row in the picker, a SESSION row (Rename + a two-step-confirm ⌫ Kill) in the armed pull-out; the walk passed 8/8 live through the desk's exact routes (spawn → picker → arm → `C-c` → steer → rename → kill → audit); docs shipped (USER_GUIDE / SECURITY / ARCHITECTURE). Suite 3514/0; voice guard green. You now spawn, drive (any key), rename, and kill a terminal from glass, all on the one consent spine. Next phase: not yet scaffolded — the iPad catching up to keys/panes/factory, cross-machine factory, and agent-into-a-spawned-pane are the recorded riders (candidates in [BACKLOG.md](./BACKLOG.md)). Previous: [**Phase 89 — Take the Terminal**](./phase-89-take-the-terminal/current-phase-status.md) — **CLOSED (4/4, 2026-07-08, scaffolded and shipped the same day)**: first-class agent manipulation, from the owner's direct ask ("we basically need first-class agent manipulation IN this framework"). Previous: [**Phase 89 — Take the Terminal**](./phase-89-take-the-terminal/current-phase-status.md) — **CLOSED (4/4, 2026-07-08, scaffolded and shipped the same day)**: first-class agent manipulation, from the owner's direct ask ("we basically need first-class agent manipulation IN this framework"). An honest audit found the Phase-87 steering channel real and safe but scoped three ways — text-only, registry-gated, local-only — and this phase removed all three on the SAME consent/audit chokepoint (more reach, never less consent). **HS-89-01: text-only → any key** — `send_keys_to_pane` + `coder_steering.deliver_keys` send real keys (allow-list refusing junk BY NAME) through the ONE chokepoint; `C-c` interrupted a real runaway, `BSpace` edited a line, `C-c` cancelled a typed line. **HS-89-02: registry-gated → any pane** — `list_panes` names every tmux pane, a `pane:%N` key steers ANY of them directly; proven by attaching to a HAND-started pane. **HS-89-03: local-only → any configured machine** — `coder_steering_relay` relays to a node's OWN steering routes, the node executing against its own tmux with its own consent + audit (the machine that types owns the record); proven TWO-PROCESS (the hub never touched tmux, yet a separate node process armed, stopped its runaway, landed `REMOTE_STEER_OK`; a killed node refused `node_offline` by name in 0.00s). **HS-89-04: the walk** passed 6/6 live (interrupt, edit, attach a hand-started pane, recycled-pane refuse+revoke for keys, cross-machine steer + quiet-node refuse, audit read-back); docs shipped (USER_GUIDE "Take Over A Session", SECURITY, ARCHITECTURE); final-summary records the B3 (factory) handoff. Suite 3499/0; both chokepoints census-pinned. Watch free, manipulate armed, `%N` re-verified per key, refuse+revoke, everything audited. Next phase: not yet scaffolded — B3 (the factory: spawn/kill/rename sessions) inherits this spine; the on-glass surface for keys/panes/nodes is the other rider (candidates in [BACKLOG.md](./BACKLOG.md)). Previous: [**Phase 88 — The Rails-Aware Desk**](./phase-88-rails-aware-desk/current-phase-status.md) — **CLOSED (5/5, 2026-07-08, scaffolded and shipped the same day)**: the rails as desk-native material — open phases/stories/evidence groundable into any run as receipts (CLI-mediated, never scraped), plus the ambient dw observer (a local model journaling rail events, read-only, off by default) with a cross-machine reach. Proven live end to end. Previous: [Phase 87 — The Steering Desk](./phase-87-steering-desk/current-phase-status.md) — **CLOSED (6/6, 2026-07-08, scaffolded and shipped the same day, PR #304)**: B2 at the owner's bar — the desk watches a live agent session, arms it under a consent grant, types into it through one audited chokepoint, carries desk objects in as grounded context, and triages onto the desk and the rails. Proven live end to end, "so robust, it will literally destroy our brains". Previous: [Phase 86 — The Delivery Belt (read-only)](./phase-86-delivery-belt/current-phase-status.md) — **CLOSED (5/5, 2026-07-07, scaffolded and shipped the same day, PR #303)**: the AI-Headquarters floor is live — the conveyor renders both rails repos from receipts (dw mission control + gh), station lights wear real gate refusals, evidence opens in place, scope:"belt" frames ride the one bus, and the closing story crossed its own belt live (walk-1..4 shots + an all-GET access log). Previous: [Phase 85 — The Mesh Edge](./phase-85-the-mesh-edge/current-phase-status.md) — **CLOSED (5/5, 2026-07-07 — scaffolded and shipped the same day)**: run where the node is. A meshNode profile relays a run through the hub to the node hosting the provider (pull worker + liveness born from its polling), refuses fast and by name when the node is offline, wears the honest `Mesh · <node>` badge, and was proven live end to end — the request moves; the model and the key never do. Next phase: not yet scaffolded (candidates in [BACKLOG.md](./BACKLOG.md); the Apple-side worker is the HSM track's follow-up).

</details>

**Previous phase:** **Phase 49 — Meeting Aftercare ("close the loop"): CLOSED (6/6)**, from [backlog](./BACKLOG.md) candidate A. The meeting result now closes its own loops, read-only + reuse, never acting without approval. The "Your next move" aftercare panel at `/history` shows what's still open (by owner), what was decided, and what changed since the last meeting (`compute_meeting_aftercare` + `GET /api/meetings/{id}/aftercare`, quiet when nothing's open/decided/changed); "show me the moment" jumps to the transcript segment that justifies a result (only on a real timestamp, focus-safe); a "File as issue" affordance turns an accepted action into a GitHub-issue actuator *proposal* through the existing propose -> approve -> execute flow (no new write primitive, off by default, human-approved, audited; an executor test proves an unapproved proposal never egresses); and "Draft follow-up" assembles a local markdown summary, preview + copy only, never sent. Capture/plugins/synthesis untouched; actuators off by default. Closed with before/after, a green dogfood, full suite green (2426/17), and a PR to `main` merged on green. See [final-summary](./phase-49-meeting-aftercare/final-summary.md). Prior: Phase 48 (Visible Learning Loop) CLOSED, PR #30.
**Status:** Plugin rollout complete (Phases 16 → 27 → 28 → 29; 14 real plugins). Remaining open work is largely hardware-gated (Phases 24, 25 HS-25-07).

**Backlog (candidate next phases):** [`BACKLOG.md`](./BACKLOG.md) — the living parking lot so post-Phase-48 ideas (meeting aftercare, voice macros, release-readiness, frontend density, activity pre-briefing, decision-point privacy) are not lost. Shipped as focused phases in sequence, not one mega-phase.

## Vision

HoldSpeak is a local, private, hold-to-talk voice typing tool for macOS
and Linux. The current product is a working voice typer with a web
runtime, meeting mode, transcription via MLX-Whisper / faster-whisper,
deferred meeting intelligence, and an emerging intelligent-typing layer
for project-aware local writing.

This roadmap exists because the project is graduating from "useful
script" to a real product. The current chapter is **local intelligent
typing**: utterances can flow through ordered, user-configurable stages
between Whisper and the keyboard, optionally using target-profile
detection, project context, agent hooks, and OpenAI-compatible local or
LAN LLM endpoints before text is injected.

## Source canon

Phase content must be grounded in these. If a phase disagrees with
canon, canon wins.

- `README.md` — public install + usage surface.
- `docs/SECURITY.md` — security & privacy posture: data classes, trust boundaries, egress points, encryption-at-rest stance (HS-25-03).
- `docs/USER_GUIDE.md` — user-facing product workflows.
- `docs/internal/PLAN_ARCHITECT_PLUGIN_SYSTEM.md` — parent RFC for the plugin system.
- `docs/internal/PLAN_PHASE_MULTI_INTENT_ROUTING.md` — sibling phase: meeting-side multi-intent routing (MIR-01).
- `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md` — DIR-01 spec for the dictation pipeline.
- `docs/internal/PLAN_PHASE_WEB_FLAGSHIP_RUNTIME.md` — web-first runtime migration.
- `docs/internal/PLAN_ACTIVITY_ASSISTED_ENRICHMENT.md` — local activity assisted enrichment design.
- `aipi-lite/` — first-class AIPI-Lite firmware, bridge, docs, tests, and imported device-side roadmap.
- `pyproject.toml` — package contract; the source of truth for installable extras.

## Phase index

| Phase | Goal (one line) | Status | Folder |
|---|---|---|---|
| 0 | Project + roadmap setup; packaging hardening | done | [phase-0-setup](./phase-0-setup/) |
| 1 | DIR-01: Dictation intent routing + transcript enrichment pipeline | done | [phase-1-dictation-intent-routing](./phase-1-dictation-intent-routing/) |
| 2 | MIR-01: Meeting-side multi-intent routing + artifact synthesis | done | [phase-2-multi-intent-routing](./phase-2-multi-intent-routing/) |
| 3 | DIR-01 loop closure: project context, llama_cpp leg, runtime counters, cold-start cap | done | [phase-3-dictation-loop-closure](./phase-3-dictation-loop-closure/) |
| 4 | WFS-01 extended: audit web flagship + interactive config for blocks / project KB / dictation runtime / dry-run preview | done | [phase-4-web-flagship-runtime](./phase-4-web-flagship-runtime/) |
| 5 | Usability powerhouse: reduce setup/dogfood friction and make web workflows faster | done | [phase-5-usability-powerhouse](./phase-5-usability-powerhouse/) |
| 6 | Action follow-through: make meeting action items and artifacts reviewable and traceable | done | [phase-6-action-follow-through](./phase-6-action-follow-through/) |
| 7 | Local handoff exports: make reviewed meeting work portable as local Markdown/JSON outputs | done | [phase-7-local-handoff-exports](./phase-7-local-handoff-exports/) |
| 8 | Local activity intelligence: mine Safari/Firefox history metadata into a private default-on work-context ledger | done | [phase-8-local-activity-intelligence](./phase-8-local-activity-intelligence/) |
| 9 | Assisted activity enrichment: add opt-in local connectors, annotations, and meeting candidates | done | [phase-9-assisted-activity-enrichment](./phase-9-assisted-activity-enrichment/) |
| 10 | Web design system & character pass: tokens, components, identity, route rebuilds, refreshed designer handoff | done | [phase-10-web-design-system](./phase-10-web-design-system/) |
| 11 | Local connector ecosystem: reusable manifests, fixtures, first-party connector packs, and developer docs | done | [phase-11-local-connector-ecosystem](./phase-11-local-connector-ecosystem/) |
| 12 | Workbench voice: replatform tokens + components on a Workbench-evoking palette + pixel font; dashboard polish; designer handoff refresh | done | [phase-12-workbench-voice](./phase-12-workbench-voice/) |
| 13 | Connector runtime + pipelines + meeting context: turn the framework on, chain packs, surface pre-meeting briefings + cross-meeting summaries | done | [phase-13-connector-runtime-and-context](./phase-13-connector-runtime-and-context/) |
| 14 | AIPI-Lite devices: remote audio ingest substrate — `AudioSource` Protocol, `RemoteAudioRecorder`, `/api/devices/audio` WebSocket, per-device speaker labels in transcripts; same-LAN scope (cross-network is phase 15) | done | [phase-14-aipi-lite-devices](./phase-14-aipi-lite-devices/) |
| 15 | Out-and-about: cross-network reach for AIPI-Lite — tunneling (Tailscale / Cloudflare Tunnel / WireGuard candidate evaluation), TLS, per-device PSKs, paired with the AIPI-Lite firmware's portable WiFi (multi-SSID + captive portal + Improv-WiFi) on the device-side roadmap | not-started | [phase-15-out-and-about](./phase-15-out-and-about/) |
| 16 | First real synthesizer: replace `mermaid_architecture`'s `DeterministicPlugin` stub with a real LLM-backed plugin, wire the LLM capability gate, render `mermaid` artifacts as inline SVG in the web view, reality-check the plugin RFC | done | [phase-16-first-real-plugin](./phase-16-first-real-plugin/) |
| 17 | Device Initiative: device → server upstream frames (`device_health` + `query` w/ `last_segment` case) lighting up AIPI-Lite phase 4's `blocked` bridge stories; minimal web UI rendering for device health. Sibling to HS-14, paired with AIPI-4 in the AIPI-Lite roadmap. | done | [phase-17-device-initiative](./phase-17-device-initiative/) |
| 18 | Intelligent Typing Copilot: project-aware local intelligent typing with target profiles, Claude/Codex hooks, optional external-agent summarization, `.hs` context conventions, OpenAI-compatible runtimes, and web cockpit support | done | [phase-18-intelligent-typing-copilot](./phase-18-intelligent-typing-copilot/) |
| 19 | Intelligent Typing Daily-Use Hardening: safe `.hs/.../*.md` project-doc suggestions, telemetry, target-profile overrides, and real endpoint dogfooding | done | [phase-19-intelligent-typing-hardening](./phase-19-intelligent-typing-hardening/) |
| 20 | AIPI Companion: same-LAN physical companion UX for agent-waiting status, voice replies, gestures, and debug visibility | done | [phase-20-aipi-companion](./phase-20-aipi-companion/) |
| 21 | AIPI-Lite First-Class Integration: import firmware and bridge source into HoldSpeak and define the unified developer workflow | done | [phase-21-aipi-lite-first-class](./phase-21-aipi-lite-first-class/) |
| 22 | AI PI Companion UX: state model, gestures, LCD cadence, bridge display wiring, and live hardware dogfood | done | [phase-22-ai-pi-companion-ux](./phase-22-ai-pi-companion-ux/) |
| 23 | AI PI Companion UX Polish: long-prompt display, multi-session identity, preview/browse, and target confidence | done | [phase-23-ai-pi-companion-ux-polish](./phase-23-ai-pi-companion-ux-polish/) |
| 24 | AI PI Companion Productization: web companion overview, stale-session controls, public companion docs/artwork, confidence affordances, and display update cadence | in-progress (3/6) | [phase-24-ai-pi-companion-productization](./phase-24-ai-pi-companion-productization/) |
| 25 | Trust & Hardening: no silent cloud egress, web-runtime auth + bind guard, threat-model/encryption-at-rest doc, LLM-runtime thread-safety, transcription timeout, config-knob audit. Prerequisite to Phase 15. | in-progress | [phase-25-trust-and-hardening](./phase-25-trust-and-hardening/) |
| 26 | Web Runtime Decomposition: break the `web_server.py` monolith into route modules + a shared context, behavior-preserving. Fast-follow to Phase 25. | done | [phase-26-web-runtime-decomposition](./phase-26-web-runtime-decomposition/) |
| 27 | Ubiquitous plugins + spoken-meeting e2e: flip the highest-value stub plugins to real (`action_owner_enforcer`, decisions/open-questions, requirements) re-using the Phase-16 pattern, and add a real `say`→Whisper→MIR→plugins→web spoken-meeting end-to-end harness with screenshots. Continuation of Phase 16. | done | [phase-27-ubiquitous-plugins-and-e2e](./phase-27-ubiquitous-plugins-and-e2e/) |
| 28 | Plugin rollout II: round out the core meeting types — a behavior-preserving synthesis renderer-registry refactor, then flip `adr_drafter` (architecture), `milestone_planner` (delivery), and `risk_heatmap` (risk register) to real, each with a structured web render + spoken-e2e coverage. Continuation of Phase 27. | done | [phase-28-plugin-rollout-ii](./phase-28-plugin-rollout-ii/) |
| 29 | Complete the plugin rollout + public docs: flip the last seven stubs (delivery/product, incident, comms) to real → fourteen real plugins / zero stubs, and document the plugin system on the public README. Continuation of Phase 28. | done | [phase-29-plugin-rollout-iii-and-docs](./phase-29-plugin-rollout-iii-and-docs/) |
| 30 | UI/UX overhaul: retire the Amiga Workbench aesthetic and ship "Signal" — a bold, distinctive, dark-first identity (Space Grotesk / Inter / JetBrains Mono, signature orange, real depth + motion) as a full UX/IA redesign of all five web routes, design system derived with the `ui-ux-pro-max` skill. | done | [phase-30-ui-ux-overhaul](./phase-30-ui-ux-overhaul/) |
| 31 | Database decomposition: break the 5,481-line `db.py` monolith into per-domain repositories behind a thin `Database` container, **delete** the `MeetingDatabase` god-class (greenfield), and **squash** the 18-version migration ladder to one canonical schema. Data-layer twin of Phase 26. | done | [phase-31-database-decomposition](./phase-31-database-decomposition/) |
| 32 | Foundation hardening & doc truth: class-ify `web_runtime.py`, invert the meeting→web-server coupling, **retire the TUI + menubar** (web is the sole interactive runtime), converge audio ownership, add an ungated CI core-path smoke test, extract a route error helper, and reconcile stale non-PMO docs. | done (7/7) | [phase-32-foundation-hardening](./phase-32-foundation-hardening/) |
| 33 | Documentation & open-source readiness: de-prescribe/refresh the model framing (current Qwen3.5 + a `MODELS.md` bring-your-own contract), add an **Apache-2.0 LICENSE** + `pyproject` metadata, reorganize `docs/` (user-facing vs internal/historical) with an index, an OSS-grade README pass + CHANGELOG/CONTRIBUTING, and a refreshed pixel-art asset set. | done | [phase-33-documentation-oss-readiness](./phase-33-documentation-oss-readiness/) |
| 34 | Structural decomposition II: sub-split the four files that now dominate the tree (5,373 lines combined) into navigable, single-responsibility packages — `web/routes/dictation.py` + `web/routes/activity.py` into route sub-packages, and `agent_context.py` + `intel.py` into modules — behavior-preserving (import paths + route table held identical). Twin of Phases 26/31/32; discharges Phase 26's deferred finer-split follow-up. | done | [phase-34-structural-decomposition-ii](./phase-34-structural-decomposition-ii/) |
| 35 | Plugin frontier: make the meeting-intel plugin system extensible by others — a public `docs/PLUGIN_AUTHORING.md`, a plugin-pack manifest + discovery loader (mirroring connector packs; first-party + user packs), per-project enable/disable, and the incident/comms spoken-e2e scenario. Behavior-preserving for the 14 built-ins; actuators deferred to Phase 37. Discharges Phase 29's "next frontier" handoff. | done | [phase-35-plugin-frontier](./phase-35-plugin-frontier/) |
| 36 | Meeting intelligence & experience: make HoldSpeak's meeting intelligence deliver on real, messy meetings end-to-end. **Intelligence** — fix the routing weakness where a brief intent in a digression is diluted below threshold and silently lost: segment the conversation + probe each segment for intent ("fish out" intent per segment), LLM-assisted with a lexical fallback. **Experience** — Signal "elevated" artifact cards (retire the basic chips), copy-as-Markdown, overflow-safe wide artifacts. Tied together by a dynamic/messy spoken-e2e with a before/after capture (7 → 13 artifact types). Artifact data shapes unchanged. On direct user feedback. | done | [phase-36-meeting-artifact-experience](./phase-36-meeting-artifact-experience/) |
| 37 | Actuators: turn on the plugin system's third kind — an actuator proposes an external side effect (file a ticket, post a message) behind a **preview → human approval → execute** flow with an audit trail (the RFC's open question #5; intersects the Phase-25 egress posture). The invariant: no external side effect without an explicit, audited, per-action human approval; executed == previewed. | done | [phase-37-actuators](./phase-37-actuators/) |
| 38 | Actuators II: make the proven-safe actuator mechanism *useful* — **real write connectors** (`gh issue create`, webhook POST) behind a per-connector **permission manifest** routed through the connector `PermissionGate`, and **live in-meeting proposals** (broadcast + a live approve/reject panel). The Phase-37 invariant is unchanged; every connector adds a narrower gate. On user direction. | done | [phase-38-actuators-ii](./phase-38-actuators-ii/) |
| 39 | Dictation Copilot Depth: make the daily-driver intelligent-typing loop **deeper and self-improving** — **multi-pass rewriting** (draft → critique → refine), **session correction memory** (the pipeline learns from corrections within a session), **model-assisted target detection** (LLM fallback below the heuristic threshold), a **project-doc suggestion quality gate** (dedup + recurrence suppression + consolidation), and **depth telemetry**. All opt-in; default byte-identical (the DIR-01 invariant). On user direction. | done | [phase-39-dictation-copilot-depth](./phase-39-dictation-copilot-depth/) |
| 40 | Configuration Cockpit & Persistent Memory: make the whole dictation copilot configurable, observable, and **memorable from the Web UI** — surface the Phase-39 knobs in `/api/settings` + a Signal-styled, readiness-driven **setup cockpit** (no hand-edited JSON/YAML), make correction memory **persistent** (DB-backed) with a curate UI, and render depth telemetry richly. UX + persistence; pipeline behavior unchanged. On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-40-config-cockpit/AGENT-BRIEF.md). | done | [phase-40-config-cockpit](./phase-40-config-cockpit/) |
| 41 | Runtime Presence Indicators: an **opt-in** (`HOLDSPEAK_DESKTOP_PRESENCE=1`), per-platform **native** desktop presence layer so a user dictating into another app sees what the copilot is doing — one `runtime_activity` contract → a websocket broadcast → a Signal `/presence` card, hosted natively (macOS non-activating `NSPanel`+`WKWebView` HUD + `NSStatusItem` glyph; Linux libnotify notification + tray + a GTK-WebKit overlay). Focus-safe; off by default; flag-unset byte-identical. Salvages codex PR #17's bones (rejects its Tk renderer). | done | [phase-41-runtime-presence](./phase-41-runtime-presence/) |
| 42 | First-Run Delight & Daily Confidence: make **arrival** stellar — a user goes from fresh clone to a **verified first dictation**, with visible privacy/trust state and **zero file editing**, in one guided local cockpit (TTFD dogfood: launch→/setup 1.13s). A setup-state contract (an adapter over the doctor) + `first_run` milestone, global settings completion, a welcome/setup route + CLI nudge, a guided real-app first-dictation, an ambient trust chip + panel, a runtime model assistant, and presence onboarding. On user direction. | done | [phase-42-first-run-delight](./phase-42-first-run-delight/) |
| 43 | World-Class Onboarding & First-Run UX: reimagine the first run as a full-screen `/welcome` **wizard** (Welcome · live Permissions · Model picker · a celebratory first-dictation reward · a one-click presence toggle · Done), make desktop presence a **config-backed UI toggle** (kill the env var as the path; live start/stop), and **redesign Settings** (sectioned + searchable + progressive). A world-class UX layer on the Phase-42 plumbing. On user feedback. | done | [phase-43-world-class-onboarding](./phase-43-world-class-onboarding/) |
| 44 | Daily-Surface Polish: carry the Phase-43 wizard's bar to the three daily-driver surfaces (dashboard `/`, dictation cockpit `/dictation`, history `/history`), behavior-preserving — one shared premium language (ambient glow, hero grammar, contained pill navs with a solid-accent active tab, elevated rounded surfaces, reduced-motion-safe depth). Per-surface page-content tests + a live before/after. On user direction. | done | [phase-44-daily-surface-polish](./phase-44-daily-surface-polish/) |
| 45 | Dictation Memory & the Moment of Truth: give the daily-driver dictation loop the memory the meeting side already has — a persistent, private, reviewable **dictation journal** (said → routed → typed → latency), an **in-the-moment "was that right? fix it here"** correction loop that teaches without leaving your flow, and **replay** (re-run a past utterance through the tuned pipeline so "it learned" becomes tangible). Local-first; journaling is a side-channel (typed output byte-identical); focus-safe; provable remotely via dry-run. On user direction. | done | [phase-45-dictation-memory](./phase-45-dictation-memory/) |
| 46 | Documentation Excellence & the 10-Second Hook: the product outgrew its docs (13 phases since the Phase-33 doc pass). Give the documentation the lift the product got — a **doc truth audit** (claims vs live code) → a **bold README reimagining** (10-second hook + cool-facts strip, every graphic kept, repetition cut, depth linked out, materially shorter) → a **docs voice & structure system** + an elevated journey-grouped index → a **visual lift** (real UI screenshots via the Playwright scripts) → a **coverage** sweep (feature → doc matrix; nothing shipped is undiscoverable; nothing overstated) → closeout. Honest not hype; graphics stay; docs-only. On user direction (README too low on cool facts, too large/repetitive; graphics stay; other docs need a lift). | done | [phase-46-documentation-lift](./phase-46-documentation-lift/) |
| 47 | Project Knowledge: Legible & Inviting: make the "teach the copilot about this project" capability understandable. Today `/dictation` shows two adjacent jargon-named tabs — **"Project KB"** (the `.holdspeak/project.yaml` `kb:` map → `{project.kb.*}` placeholders, default `kb-enricher` stage) and **"Project Context"** (the *separate* `.hs/` Markdown files, optional `project-rewriter` LLM stage) — with no explanation of what either is or how they relate (the Phase-46 docs pass even conflated them). Settle the concept + names → in-app explainers + inviting empty states → a guided setup flow (fresh repo → working, no file editing) → a dismissible discovery nudge → docs alignment → closeout. Behavior-preserving (presentation/onboarding, not a pipeline redesign); UX bar via `ui-ux-pro-max`. On user direction ("I struggle to understand the project KB; it's UI/UX too"). | CLOSED ✅ (6/6) | [phase-47-project-kb-legibility](./phase-47-project-kb-legibility/) |
| 48 | The Visible Learning Loop ("What HoldSpeak learned"): HoldSpeak already records every dictation (the journal), learns from corrections (the Jaccard correction memory), and can replay to prove it improved — but the learning is buried in two raw tabs. Make the loop **visible** (a windowed "What HoldSpeak learned" digest with honest counts), **trustworthy** (inline "learned from N similar" signals from the same matcher that nudges routing, quiet at N=0), and a **normal ritual** (one-tap right/wrong on a result, reusing the existing correct path) → docs telling the loop end to end as the open-source differentiator → closeout. Behavior-preserving (read-only aggregation; correcting reuses the existing write path); honesty over hype; focus-safe. On user direction ("I like the 'what I learned this week' view; let's make this open-source thing happen"). | CLOSED (5/5) | [phase-48-visible-learning-loop](./phase-48-visible-learning-loop/) |
| 49 | Meeting Aftercare ("close the loop"): the meeting side has 14 plugins, artifacts, proposals, and action review, but the result is display, not follow-through. Surface what is still open / decided / changed since last meeting, jump to the transcript moment that justifies a result, turn accepted actions into issues through the existing human-approved actuator flow, and draft a copyable follow-up. Read-only aggregation + reuse of the actuator system; never acts without approval. | CLOSED (6/6) | [phase-49-meeting-aftercare](./phase-49-meeting-aftercare/) |
| 50 | Release Readiness ("cut a real 0.x"): 49 phases of features, never cut as a real release. The DB is SCHEMA_VERSION=1 and the next bump silently wipes a user's data; the in-code version disagrees with the package version; doctor never checks the DB; the install pins git HEAD. Make it safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (refuse-newer / backup-then-apply-older / no-op-equal / create-fresh), a backup before upgrade, an honest doctor + config_version, a verified clean install, and an upgrade/backup policy doc. The open-source-ship unlock. | **CLOSED (7/7)** | [phase-50-release-readiness](./phase-50-release-readiness/) |
| 51 | Public-Docs Hygiene: Phase 50 cut the release gate, but the public surface still reads like an internal artifact. User/operator-facing guides leak the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README is already clean. Strip that vocabulary from the deeper guides, rewrite phase-relative claims ("Phase 11 shipped X") into product-tense ("HoldSpeak supports X") so the meaning survives for a reader who never heard of a phase, keep legitimate product nouns (`actuator`) and named specs (`MIR-01`/`DIR-01`), and lock it with a doc-drift guard + a codified `DOCS_STYLE.md` rule so it cannot regress. Docs-and-test only, behavior-preserving. A cheap, release-facing follow-on. Net-new from a between-phases conversation ([backlog](./BACKLOG.md) candidate H). On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-51-public-docs-hygiene/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-51-public-docs-hygiene](./phase-51-public-docs-hygiene/) |
| 52 | Voice Command Macros on a carved dispatch seam: turn the dictation hotkey into a **voice command launcher**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through HoldSpeak's existing **actuator guarded executor** (Phase 37/38), reused and extended with new local connectors; a configured macro is auto-approved (the user owns the risk: configuring is consent), off by default, deterministic (the keyword selects which pre-configured action fires, the transcriber never composes a command), each macro bounded by its own permission manifest, every fire audited. Landed on a **dispatch seam carved out of the 2,341-line `web_runtime` god-object** (a scoped slice of backlog **E**) so a feature (backlog **B**) ships with a motivated refactor under one thesis. On user direction (re-envisioned from text macros to a command launcher; "B=E"). Scaffolded + [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-52-voice-macros](./phase-52-voice-macros/) |
| 53 | Activity Pre-Briefing: HoldSpeak already watches local activity (browser history + GitHub/Jira/calendar enrichment) but it only lives on the `/activity` ledger you go and inspect. Turn it into concrete, **source-cited, dismissible pre-briefing nudges** on the daily surfaces: "here is what you touched since your last meeting"; "you were looking at `owner/repo#123`, dictate a reply with it as context?". A small reader over the records that already exist (no new watcher), gated by the existing activity privacy toggle, read-only (it surfaces and offers, never acts), every nudge cites where it came from, and one action feeds the selected record into the dictation pipeline. On the daily-dictation north star. On user direction (picked backlog candidate **F**). Scaffolded + [`AGENT-BRIEF.md`](./phase-53-activity-prebriefing/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-53-activity-prebriefing](./phase-53-activity-prebriefing/) |
| 54 | Dictation Frontend Decomposition: pay down the cockpit's 6,101 coupled lines (`dictation.astro` 3,134 + `dictation-app.js` 2,967, grown five phases running) into section partials + single-concern behavior modules, **behavior-preserving** (existing page-content tests pass unmodified; every tab screenshot-verified before/after), locked by a density guard so it cannot silently regrow. Defines the codebase's frontend decomposition pattern (`history.astro` / `index.astro` are follow-up candidates). Frontend twin of Phases 26/31/32/34. From [backlog](./BACKLOG.md) candidate **D**, promoted from "ride along" to its own phase. Scaffolded + [`AGENT-BRIEF.md`](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-54-dictation-frontend-decomposition](./phase-54-dictation-frontend-decomposition/) |
| 55 | Meeting Import ("bring your archive") + faceted history search: meeting intelligence is live-capture-only — productize the proven file→transcribe→meeting seam into an **import engine** (WAV native + ffmpeg-assisted compressed, ~30 s windowed transcription with real segment timestamps, the same deferred-intel enqueue as live), `POST /api/meetings/import` on a background thread + `holdspeak import`, an inviting `/history` upload affordance, and **server-side facets** (date range / speaker / tag / open-actions) composing with full-text search. Honest v1: one speaker label (no single-file diarization), audio not retained, ffmpeg stated as the compressed-format dependency. An imported meeting is a real meeting — one pipeline, indistinguishable downstream. From [backlog](./BACKLOG.md) candidate **I**, per the agreed sequence. Scaffolded + [`AGENT-BRIEF.md`](./phase-55-meeting-import/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-55-meeting-import](./phase-55-meeting-import/) |
| 56 | Qlippy, the Presence Enhancer: give the presence layer a **face** — an ambient mascot dock reflecting `runtime_activity` (the PixelLab pack: 14 sprite strips, body-emotes + composited glyphs), and a sliding, focus-safe, **one-at-a-time card** that makes the actuator-approval and learning moments actionable in place. Approve/Decline are a faster front door to the **same audited decision route** (the actuator invariant untouched); `learning_event` fires only with real reach; `aftercare_ready` only when non-empty. **Absorbs backlog G**: every actionable card answers what data is used / does anything leave this machine / what control do I have. Opt-in twice (`presence.enabled` AND `presence.mascot`, both default off); flag-unset byte-identical; the native macOS HUD grows to host the card with clickable, non-activating buttons. From [backlog](./BACKLOG.md) candidate **J** + the [RFC](./proposals/qlippy-presence-enhancer.md). Scaffolded + [`AGENT-BRIEF.md`](./phase-56-qlippy/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-56-qlippy](./phase-56-qlippy/) |
| 57 | Transcript Import ("bring your archive", part 2): most meeting tools export a transcript, not audio — let a user upload `.vtt`/`.srt`/`.txt` (web + CLI) and get a **real meeting** through the exact Phase-55 pipeline (factored persistence tail, same intel enqueue, same /history lifecycle), with **real cue timestamps and multi-speaker labels** when the file carries them (VTT voice tags / `Name:` prefixes) and honest synthetic ordering when it does not. The recording upload stays untouched (explicit user constraint). Net-new on user direction ([backlog](./BACKLOG.md) candidate **P**). Scaffolded + [`AGENT-BRIEF.md`](./phase-57-transcript-import/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-57-transcript-import](./phase-57-transcript-import/) |
| 58 | The Front Door: positioning + the user-facing docs, revised. Decide what HoldSpeak's story IS (a positioning canon: "one copilot, two modes", pitched to developers, with named honest comparisons — the user's explicit decisions) and revise the whole user-facing corpus to tell it: README as the pitch, why-ledes + canonical names + the humanizer voice across every guide, the em-dash cleanup the older corpus never had, and a drift guard so it cannot regress. The pitch stays as honest as the product. Net-new on user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-58-front-door/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-58-front-door](./phase-58-front-door/) |
| 59 | Speak Your Language: Whisper speaks ~99 languages and HoldSpeak exposes none of it — a `model.language` knob ("auto" default, byte-identical: today neither backend is ever passed a language) pinning transcription for dictation, meetings, AND import through the one shared Transcriber, plus **the spoken-symbol dictionary**: user-defined spoken→symbol entries ("tilde" → `~`, "arrow" → `→`) with the built-ins' attach semantics, merged user-wins over the hardcoded punctuation table. One thesis: the input layer adapts to you. Proven on real non-English speech at closeout. From [backlog](./BACKLOG.md) candidate **K** on user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-59-languages/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-59-languages](./phase-59-languages/) |
| 60 | The Wake Word: hands-free entry without betraying the trust posture — say the phrase, HoldSpeak **arms** visibly for a bounded window, the next sentence runs the normal dictation pipeline, and by default the result is **previewed, never typed** (direct typing is an explicit opt-in, the voice-commands consent model). openWakeWord (Apache-2.0, local ONNX; Porcupine ruled out on licensing) with a **measured** false-accept posture (the scaffold spike: wake 0.861 vs adversarial distractors ≤0.063 on TTS speech); the armed state unmissable via presence/Qlippy/cockpit; off by default and byte-identical when off; the only network moment is the explicit ~7 MB model download. From [backlog](./BACKLOG.md) candidate **O** on standing user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-60-wake-word/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-60-wake-word](./phase-60-wake-word/) |
| 61 | Send to Slack: the meeting loop closes locally (digest, follow-up draft) but the results only leave by clipboard — one outbound door through a Slack **incoming webhook** (one POST, no OAuth), riding the existing propose→approve→execute actuator flow on the Phase-38 host-gated webhook connector. Every send is a proposal whose **preview is the exact message**; the webhook URL is a credential (never in previews/broadcasts/responses); unconfigured is byte-identical (no buttons, 400). From [backlog](./BACKLOG.md) candidate **L**, scoped by the owner to one easy connector; **N (Windows) owner-rejected** in the same direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-61-send-to-slack/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-61-send-to-slack](./phase-61-send-to-slack/) |
| 62 | Quiet Trust: trust should be ambient, not narrated — the UI's privacy-reassurance prose ("nothing leaves your machine", on every Qlippy card and notification) replaced by one compact three-state **egress badge** (local · local+cloud · cloud, glyph + a word; the cloud state keeps the target label), a sweep of every reassurance tail in web/src (behavioral warnings stay), docs aligned, and every user-facing screenshot showing the old copy re-shot live. Opened on direct owner feedback; reverses the locked Phase-56 three-answers-verbatim card pattern. Scaffolded + [`AGENT-BRIEF.md`](./phase-62-quiet-trust/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-62-quiet-trust](./phase-62-quiet-trust/) |
| 63 | Backend Decomposition: the twin of Phase 54 — `web_runtime.py` (2,635 lines, regrown past its pre-Phase-52 size) and `meeting_session.py` (1,674) carve into single-concern mixin modules under `holdspeak/runtime/` and `holdspeak/meeting/`, behavior-preserving (verbatim moves; assertions byte-identical; the only test edits are monkeypatch target paths, censused at scaffold), locked by a backend density guard, proven by a live boot closeout. From [backlog](./BACKLOG.md) row **E** on owner direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-63-backend-decomposition/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-63-backend-decomposition](./phase-63-backend-decomposition/) |
| 64 | Docs Catch-Up: discovery, not coverage — the deep guides are current but the front door sells a Phase-55 product (zero README mentions of the wake word or Send to Slack; the index and Getting Started point at nothing newer). The two-modes tour absorbs the P59–P62 surface, the entry points gain pointers, one humanizer-grade coherence read smooths the solo-written seams, the locks stay green. Docs-only. Scaffolded + [`AGENT-BRIEF.md`](./phase-64-docs-catch-up/AGENT-BRIEF.md). | **CLOSED (3/3)** | [phase-64-docs-catch-up](./phase-64-docs-catch-up/) |
| 65 | The Launch: v0.3.0 reaches PyPI for real — the all-routes zero-page-error pre-flight, the release cut (version + a P51–P64 changelog + fresh-venv install proofs on macOS and .43), the announcement kit in canon voice (the owner posts), then tag → trusted publishing → verified from-PyPI install. The tag is the publish; cut only at closeout from the merged main. Scaffolded + [`AGENT-BRIEF.md`](./phase-65-the-launch/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-65-the-launch](./phase-65-the-launch/) |
| 66 | The Architecture Map: the contributor on-ramp the repo never had — `docs/ARCHITECTURE.md` with a system component map, the dictation and meeting pipelines, and the trust/egress boundary, all in Mermaid that renders on GitHub and is kept rendering by an mmdc-backed guard, every diagram traced against the shipped (post-decomposition) code. Verified gap: no system-overview doc, three Mermaid blocks in the whole corpus. Docs-only. Scaffolded + [`AGENT-BRIEF.md`](./phase-66-architecture-map/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-66-architecture-map](./phase-66-architecture-map/) |
| 67 | The Dogfood: a real, repeatable end-to-end exercise of the whole product on believable data — a `dogfood/` harness (3 mock repos with `.hs/` context + KB + completed-stage history, a `say`-pipeline scenario generator for meetings/dictation across voices, committed transcript fixtures, an isolated-`HOME` runner wired to `.43`) and a two-tier fillable `PROTOCOL.md` (plumbing + real-metal) covering every surface, plus an opt-in plumbing pytest. Scaffolded + [`AGENT-BRIEF.md`](./phase-67-the-dogfood/AGENT-BRIEF.md). | scaffolded | [phase-67-the-dogfood](./phase-67-the-dogfood/) |
| 68 | Convergence: the Design Map & Parity Plan — mapped the iPad's nine Signal craft patterns, audited two-way web↔iPad parity, and produced the ordered 12-story Phase-69 delivery backlog; the token foundation is byte-identical, so convergence is apply+port, not redesign. | **CLOSED (3/3)** | [phase-68-web-convergence](./phase-68-web-convergence/) |
| 69 | Web, Re-crafted: the delivery half of the web/iPad convergence — the Signal substrate broadened across the cockpit, premium sheets, Qlippy in the cockpit, the reactive waveform, the generation theater (proven on real `.43`), the full node canvas at `/workbench`, and `/companion`→the Agent Desk. PR #202. | **CLOSED (12/12)** | [phase-69-web-recrafted](./phase-69-web-recrafted/) |
| 70 | The Legible Product (Out-of-the-Box): reconcile the sprawled 16-page web surface to the "one copilot, two modes" story so a new user (and the owner) can say what it is and what to do first in one breath. Bold reorg to Home · Dictation · Meetings · Studio▾ · Settings; three first-run surfaces → one; power features tucked into a collapsed Studio tier; guiding empty states; naming + positioning locked. Owner-opened on "I'm confused about my own product." [`final-summary`](./phase-70-legible-product/final-summary.md). | **CLOSED (9/9)** | [phase-70-legible-product](./phase-70-legible-product/) |
| 71 | The Desk, as a World (the web diorama): port the iPad DeskOS 2.5D spatial diorama to the web `/desk` — warm lit atmosphere, hand-drawn floating sprite objects with detached shadows + glow, free drag-to-arrange, zones you file into and dive through, an in-world Qlippy — reusing the existing `/api/*` primitive data layer (a rendering change, not new plumbing). Only `/desk`; the two-mode cockpits stay clean. Owner-opened on "nowhere near the look and feel of the iOS app". [`final-summary`](./phase-71-desk-as-world/final-summary.md). | **CLOSED (8/8)** | [phase-71-desk-as-world](./phase-71-desk-as-world/) |
| 72 | One Spine (cross-surface cohesion): replace the prose seams holding the three surfaces together with machine-checked ones — a JSON-Schema'd primitive contract validated on hub/web/iPad, a generated API-surface manifest with per-route consumers, the "companion" triple overload untangled (`/api/coders/*` + `/api/desk/actuators/*`), one actuator lifecycle (the sentinel meeting row killed), shadow modules renamed, `routes/meetings.py` + `history.astro` decomposed, one live `/ws` bus, and iPad desk records embedding `Contracts` natively. The structural layer under the Equilibrium parity program (HSM 18–23), which keeps all feature gaps. Opened from the 2026-07-02 architectural analysis. HS-72-07 cut same day (superseded by the React migration decision); fourteen real bugs harvested; [`final-summary`](./phase-72-one-spine/final-summary.md). | **CLOSED (10/10)** | [phase-72-one-spine](./phase-72-one-spine/) |
| 73 | The Desk, Inhabited (on the React foundation): port the iPad desk's *inhabitation* to the web — the Desk becomes the front door (`/`), built as a React + Vite island (owner decisions 2026-07-02); instant create + edit-in-place (modals deleted), the in-world pull-out ("Open full" the one exit), zones as landmark trays, the Record orb, the agent rail, the Alpine-desk cutover behind a zero-loss verb inventory, mechanical no-prose/no-modal locks, and a zero-route-change closeout walk. Owner-opened on "a primitive copy, an uninviting mess". | **CLOSED (10/10)** | [phase-73-desk-inhabited](./phase-73-desk-inhabited/) |
| 74 | The Run Story: artifacts remember the run that made them (schema v6, scope=run frames) | done | [phase-74-run-story](./phase-74-run-story/) |
| 75 | Preview Before It Types: opt-in hold-key dictation preview on the arms-not-types seam | done | [phase-75-preview-before-type](./phase-75-preview-before-type/) |
| 76 | Documentation Sweep: the truth audit — every public doc re-verified against the shipped product | done | [phase-76-documentation-sweep](./phase-76-documentation-sweep/) |
| 77 | Loose Ends: schema v7, the runtime_queue frame, the HUD linger contract | done | [phase-77-loose-ends](./phase-77-loose-ends/) |
| 78 | Talk to the Desk: the transcribe route + hold-to-talk on every input | done | [phase-78-talk-to-the-desk](./phase-78-talk-to-the-desk/) |
| 79 | Backend Decomposition II: the Phase-63 discipline on the next three backend monoliths | done | [phase-79-backend-decomposition-ii](./phase-79-backend-decomposition-ii/) |
| 80 | Artifacts for the Archive: imported + re-processed meetings run the routed plugin chain | done | [phase-80-artifacts-for-the-archive](./phase-80-artifacts-for-the-archive/) |
| 81 | The Sweep and the Cut: the dogfood findings ledger closed; v0.4.0 cut to PyPI | done | [phase-81-the-sweep-and-the-cut](./phase-81-the-sweep-and-the-cut/) |
| 82 | Mission Control Conveyor: the desk's live work belt — sessions, documents, the approval leg | done | [phase-82-mission-control-conveyor](./phase-82-mission-control-conveyor/) |
| 83 | Web in Unison: the iPad's conversation pass on the web desk (grounding, threads, the models door) | done | [phase-83-web-in-unison](./phase-83-web-in-unison/) |
| 84 | One Runtime: every hub pipeline resolves "where intelligence runs" through the RuntimeProfile layer | done | [phase-84-one-runtime](./phase-84-one-runtime/) |
| 85 | The Mesh Edge: a run goes to the node that hosts the provider — hub relay queue, pull worker, liveness, honest refusal | done | [phase-85-the-mesh-edge](./phase-85-the-mesh-edge/) |
| 86 | The Delivery Belt (read-only): the AI-Headquarters floor — projects registry, belts rendered from receipts (`dw` mission-control + `gh`), `scope:"belt"` frames on the one bus, the `/belt` desk surface, closed by a live station-to-station walk | done | [phase-86-delivery-belt](./phase-86-delivery-belt/) |
| 87 | The Steering Desk (B2 at the owner's bar): attach to a live agent session from the desk (real pane view), armed voice-first steering with per-keystroke pane-identity verification and a full audit, desk objects riding into steers as hydrated context, classify verbs onto rails + primitives — the Telegram consent spine ported natively, contract-shaped for iPad/iPhone | scaffolded | [phase-87-steering-desk](./phase-87-steering-desk/) |
| 88 | The Rails-Aware Desk: rail receipts become groundable desk-native material; ambient observer and cross-machine reach | done | [phase-88-rails-aware-desk](./phase-88-rails-aware-desk/) |
| 89 | Take the Terminal: first-class, consent-gated manipulation of any pane on configured machines | done | [phase-89-take-the-terminal](./phase-89-take-the-terminal/) |
| 90 | The Factory: spawn, rename, drive, and kill terminal sessions from glass on the one consent spine | done | [phase-90-the-factory](./phase-90-the-factory/) |
| 91 | One React Surface: one Vite/React application carrying DeskOS craft across every Web route; owner/Swift parity close gate remains | in-progress | [phase-91-one-react-surface](./phase-91-one-react-surface/) |
| 92 | Make HoldSpeak feel like one powerful, understandable product by converging Web and native journeys on the Desk's shared objects, destinations, attention, authority, and retained results without rewriting the underlying systems. | planning | [phase-92-the-coalescence](./phase-92-the-coalescence/) |
| 93 | Effortless HoldSpeak: make the Desk a rich, learnable AI operating system through visible subtraction, coherent OS affordances, contextual power, real failure recovery, accessibility, and sustained owner use across Web and flagship Swift. | done | [phase-93-effortless-holdspeak](./phase-93-effortless-holdspeak/) |
| 94 | The Delivery Runtime: Delivery Workbench projects, Story progress, remote/local Coder sessions, terminals, evidence, and receipts as one robust platform primitive across workstation, iPad, and tailnet Web | done | [phase-94-delivery-runtime](./phase-94-delivery-runtime/) |
| 95 | The Desk OS: the desk becomes the one operating surface — WebGL stage, OS-grade windows and dock, every service surfaced through OS primitives, flat routes demoted to deep links | done | [phase-95-the-desk-os](./phase-95-the-desk-os/) |
| 96 | The Design System: the vendored design-system + ui-styling skills applied to the Desk OS — three-layer tokens with a validator lock, component state specs, one material grammar, the accessibility pass | done | [phase-96-the-design-system](./phase-96-the-design-system/) |
| 97 | The Window Grammar: windows land well, stack honestly, remember everything, move with intent, switch visibly, one shelf — the OS grammar as physics floors | done | [phase-97-the-window-grammar](./phase-97-the-window-grammar/) |
| 98 | Native Surfaces: the window interiors re-crafted in one desk idiom — the surface kit, container-driven reflow, honest rows, the page grammar retired from the desk | done | [phase-98-native-surfaces](./phase-98-native-surfaces/) |
| 99 | The OS Chrome: the ProzillaOS-studied skin — tonal depth ladder, a real title bar, skinned controls, custom scrollbars, a living dock, one menu vocabulary | done | [phase-99-the-os-chrome](./phase-99-the-os-chrome/) |
| 100 | The Application Layer: ground the philosophy/seams/use cases, judge the entire UI/UX against them, thesis + mockups, the owner's gate, then the build | done | [phase-100-the-application-layer](./phase-100-the-application-layer/) |
| 101 | The Native Innards: interior type scale, data as material, purpose-built compositions — mocked, owner-gated, then built (AGENT_BRIEF.md §5) | backlog | [phase-101-the-native-innards](./phase-101-the-native-innards/) |
| 103 | Foundations & Borrowed Fire: fix the desk's session-restoration gap, sweep the voice guard onto rendered UI, and land two skepticism-tested carry-overs from a four-agent research pass on `ViuGiaLai/researchmind` (grounding verification, endpoint health). Chartered from the owner's own research-debate commission, not a screenshot conviction. | in-progress (0/6) | [phase-103-foundations-and-borrowed-fire](./phase-103-foundations-and-borrowed-fire/) |
| 106 | Every consequential action in HoldSpeak enters through one named kernel — read/submit/decide/events — and the first tech-lead userland program rides on it. | done | [phase-106-the-kernel](./phase-106-the-kernel/) |
| 107 | Close the Side Doors: move the corrected effect census from 38 debt to 15 and give every remainder a machine closing condition. | done | [phase-107-close-the-side-doors](./phase-107-close-the-side-doors/) |
| 108 | The Locked Room: confine desktop effects behind broker warrants, close terminal/read side doors, empty the transitional register, and bound silent execution. | done | [phase-108-the-locked-room](./phase-108-the-locked-room/) |
| 109 | The Long Memory: decisions become records and cited artifacts; projects remember; kernel processes become visible. | done | [phase-109-the-long-memory](./phase-109-the-long-memory/) |
| 110 | The Cohesion: the material model pivot — macOS Ventura replaced with Signal Workbench (opaque surfaces, beveled depth, 2px corners, mono chrome, solid bars). | done | [phase-110-the-cohesion](./phase-110-the-cohesion/) |
| 111 | The Refinement: every program on the desk gets its interior rethought for Signal Workbench — Settings, Speak, Meetings, Agents, Ask, Delivery, system chrome, interactive controls, sprites. | in-progress | [phase-111-the-refinement](./phase-111-the-refinement/) |
| 138 | Ship an encrypted, local-only People relationship and 1:1 foundation whose explicit manager commitments appear in Follow-through without entering plaintext product stores. | done | [phase-138-the-people-ledger](./phase-138-the-people-ledger/) |
| 139 | The Settings Reckoning: cut the Settings face to seven job-named tiles, pin core defaults, move object settings home, and fold operator wiring behind RAW. | done | [phase-139-the-settings-reckoning](./phase-139-the-settings-reckoning/) |
| 140 | The First Sentence: one obvious first-value act on the Chair, a useful editable result, in-place recovery, then a furnished six-drawer reveal with explicitly attachable Everyday context. | in-progress (5/6) | [phase-140-the-first-sentence](./phase-140-the-first-sentence/) |
| 141 | From Thought to Work: preserve rough speech, develop it through one owner-triggered grounded question at a time, then freeze typed local or GitHub-backed outcomes on the existing authority and receipt spine. | in-progress (6/9) | [phase-141-from-thought-to-work](./phase-141-from-thought-to-work/) |
| 142 | Make AI capability, runtime, artifact, route, and readiness truth server-owned before adding acquisition or new execution paths. | active | [phase-142-inference-instrument](./phase-142-inference-instrument/) |
| 143 | Route every AI workload through one server-owned capability assignment to reusable model profiles, with ordered qualified fallbacks, frozen execution truth, and the Model Library + Assignments owner system. | in-progress (7/14) | [phase-143-intelligence-router](./phase-143-intelligence-router/) |

(Status values: `planning`, `in-progress`, `done`, `paused`, `cancelled`.)

Phase 15 (cross-network reach) is gated on Phase 25 landing the web-runtime
auth + bind guard.

## Operating cadence

Per `pm/roadmap/roadmap-builder.md` §3, every shipping commit updates,
in the same commit:

1. The story file header (status flip).
2. The phase's `current-phase-status.md` story-status row + "Where we are".
3. This README's "Last updated" line.
4. Any project-canon doc touched by the story.

Per `pm/roadmap/PMO-CONTRACT.md`: the pre-commit hook gates every
commit on a fresh `.tmp/CONTRACT.md`.

## Project metadata

- **Slug:** `holdspeak`
- **Story ID prefix:** `HS` (e.g. `HS-0-01`, `HS-1-04`)
- **Greenfield?:** effectively yes — **pre-release** (HS-33-04): the `0.2.x` marker in `pyproject` is in-development, **not** a published tag, and HoldSpeak is not on PyPI. `scripts/install.sh` is the from-source install path. No committed external API surface beyond CLI flags + the `holdspeak` shell command; the standing posture is greenfield/aggressive (design freely — see memory `feedback_holdspeak_not_really_released`).

## Glossary

- **DIR-01** — Dictation Intent Routing, phase 1 of this roadmap. See `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md`.
- **MIR-01** — Multi-Intent Routing for meetings. Sibling phase, separate runtime, shares plugin contracts only.
- **Block** — a user-defined intent class with match examples and an injection template (DIR-01 §8).
- **Transducer** — DIR-01's new plugin kind: transcript-in, transcript-out stage.
