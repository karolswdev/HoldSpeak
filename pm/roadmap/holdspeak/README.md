# HoldSpeak — Roadmap

> **New here? Read [`HANDOVER.md`](./HANDOVER.md) first** — current state, where to
> pick up, and the repo conventions that bite (PMO commit gate, write-once
> evidence, no `Co-Authored-By`, metal-test exclusion).

**Last updated:** 2026-06-30 (**Phase 69 — Web, Re-crafted: CLOSED (12/12)** — the web flagship reached the iPad's felt craft. All 12 delivery stories shipped through the PMO gate with screenshot proof (and a real-`.43`-metal proof for the generation theater): the Signal card primitive broadened across the cockpit, the materialize motion, the premium confirm sheet, Qlippy's dock into the cockpit, the reactive mic waveform (an additive `audio_level` frame), the generation theater (orb + constellation), the **full node canvas** (`/workbench`: pan/zoom/drag + type-validated wiring + inspector + palette), and `/companion`→**the Agent Desk**; plus the substrate flips (egress badge, gradient tokens, Queue HUD). Three real bugs fixed (the Astro-scope-on-JS-DOM activity gap, the canvas pan stealing palette/inspector clicks, the companion smoke test). Final suite **3044 passed, 37 skipped**; PR #202. See [`final-summary.md`](./phase-69-web-recrafted/final-summary.md). Earlier this push: the web/iPad convergence delivery, reconciled from the stale cadence. The cadence is being reconciled: the Phase-69 substrate (the `.signal-card` primitive, gradient/hairline tokens, `hs-materialize` motion, the egress badge, and the Queue HUD) already landed in the tree but rode in on a mixed mobile checkpoint commit (`f64d80d`) with no per-story evidence, and this README still pointed at Phase 67. Owner direction (2026-06-29): deliver the **full sweep in sequence** through the **full node canvas** + the `/companion`→Agent-Desk, for true two-way felt parity between the freshly-crafted iPad and the original web flagship. Prior: **Phase 67 — The Dogfood: scaffolded** — a `dogfood/` harness to exercise the whole product end to end on believable data: three mock repos (`ledgerline`/`questline`/`pylon-infra`) each with `.hs/` context, a `.holdspeak/project.yaml` KB, and completed-stage history (`STAGES.md`, ADRs, postmortems); a `make_fixtures.py` that renders 12 scenarios (6 meetings covering all 5 MIR profiles + balanced, 6 dictation sets incl. German + spoken-symbols + macros) through the macOS `say` voices into 16 kHz audio; committed `.vtt`/`.srt`/`.txt` transcript fixtures; an isolated-`HOME` runner (`dogfood/hs` + `setup.sh`) wired to `.43` intel so config/DB never touch the real `~`; and a fillable two-tier `dogfood/PROTOCOL.md` (~58 checks: plumbing + real-metal) with a per-run results template. Opt-in plumbing pytest (`HOLDSPEAK_DOGFOOD=1`, 20 green) keeps the fixtures from rotting. Phase stays open until a recorded run. Prior: **Phase 66 — The Architecture Map: CLOSED (4/4)** — `docs/ARCHITECTURE.md` is the runtime map the repo never had: six Mermaid diagrams (component map, dictation flow, learning loop, device path, meeting pipeline, trust boundary), all traced against the shipped post-decomposition code, all rendering on GitHub and kept rendering by an mmdc-backed guard (`tests/e2e/test_mermaid_renders.py`, proven both ways, CI-skippable, with a no-vacuous-pass canary); the trust boundary checked one-to-one against SECURITY's egress table. Wired into the docs index, the README, and CONTRIBUTING. Final suite **2779 passed, 17 skipped** (+2). See [`final-summary.md`](./phase-66-architecture-map/final-summary.md). Earlier same-day: **HS-66-03 done** — two more diagrams: the meeting pipeline (capture/import -> windowed transcribe -> routing -> plugin host -> artifacts -> aftercare -> the two approve-gated proposals) and the trust boundary (a "Your machine" box with all seven egress crossings labeled by their gate, checked one-to-one against SECURITY's egress table). Render guard green (6 blocks). Prior same-phase: **HS-66-02 done** — the dictation section gains three code-traced diagrams: the end-to-end flow (entries -> transcribe -> the voice-command branch -> the opt-in intent-router/project-rewriter/kb-enricher stages -> the wake preview-default fork -> type -> journal), the learning loop, and a sequence diagram of the ESP32-S3 device path with its agent-reply branch. Render guard green (4 blocks), flow eyeballed. Prior same-phase: **HS-66-01 done** — `docs/ARCHITECTURE.md` is born: the orienting overview + the top-level component diagram (traced against the shipped post-decomposition modules, rendered to PNG and eyeballed for legibility), plus the Mermaid render guard (`tests/e2e/test_mermaid_renders.py`, mmdc-backed, CI-skippable) proven both ways, and an "Understand the system" docs-index pointer. The voice guard caught HS-ID placeholders leaking into the user-facing doc (rewritten product-tense). Suite **2779 passed, 17 skipped**. Earlier same-day: scaffolded — opened on owner direction (a dedicated phase for pipeline architecture + Mermaid diagrams). Verified gap: no top-level architecture/system-overview doc (the two internal ARCHITECTURE_* docs are module-structure, not runtime data-flow), the docs index has no "how it works" entry, and the whole corpus has three Mermaid blocks. Four stories build `docs/ARCHITECTURE.md`: the system component map + a render guard (mmdc-backed, CI-skippable) → the dictation pipeline diagrammed → the meeting pipeline + the trust/egress boundary → closeout with render re-verify + index/README/CONTRIBUTING wiring. Every diagram traced against shipped code (the decomposition phases moved module names), rendering-verified, docs-only. Read [`AGENT-BRIEF.md`](./phase-66-architecture-map/AGENT-BRIEF.md) first. Prior: **Phase 65 — The Launch: CLOSED (4/4)** — **HoldSpeak v0.3.0 is LIVE on PyPI.** The roadmap's last strategic row is done. Pre-flight (the all-routes zero-page-error sweep that caught two dead-on-arrival page bugs) → the release cut (0.3.0, a P51–P64 changelog, fresh-venv install proofs on macOS + .43) → the announcement kit in canon voice → and the publish: PR #55 merged on green, `v0.3.0` tagged on the merge commit, the OIDC trusted-publishing workflow ran, PyPI serves 0.3.0, a from-PyPI `pip install holdspeak==0.3.0` verified (doctor 22 passed / 0 failed), the GitHub release live. The announcement drafts are the owner's to post. Final suite **2777 passed, 17 skipped**. See [`final-summary.md`](./phase-65-the-launch/final-summary.md). Earlier same-day: **HS-65-03 done** — the announcement kit: GitHub release notes + Show HN / lobste.rs / r/LocalLLaMA drafts in canon voice (one copilot two modes / developers / named honest comparisons / honest 0.x limits), posting left as the owner's button; the demo GIF took the documented honest fallback (the speak-and-type hero moment is a one-take manual capture, and a staged fake would break the honesty bar). Prior same-phase: **HS-65-02 done** — the release cut: pyproject → 0.3.0, the CHANGELOG [0.3.0] entry summarizing P51–P64 in canon voice, no stale version claims. The wheel built exactly as release.yml does it (194 `static/_built/` entries, twine PASSED) installs clean in a fresh venv to a working `holdspeak doctor` on macOS (20/0) AND real .43 Linux metal (22/0, faster-whisper resolving, real X11 hotkey + injection). Suite **2777 passed, 17 skipped**. Prior same-phase: **HS-65-01 done** — the all-routes zero-page-error pre-flight (11 page routes in Chromium + a coverage guard; CI-skip-clean) found and fixed TWO dead-on-arrival bugs the Phase-62 precedent predicted: `/activity`'s page JS had been dead since Phase 9/10 (a spurious leading `<script>` tag broke the `?raw`+`new Function` eval — the page was a static shell for ~55 phases; now fires all 5 of its API calls) and `/companion` threw on every load (an x-for reading `status.blockers` before first paint). Suite **2777 passed, 17 skipped**. Earlier same-day: scaffolded — opened on owner direction ("Let's launch!"): cut **v0.3.0** (PyPI is fourteen phases stale at 0.2.2) through the proven tag-triggered trusted-publishing workflow, behind a real pre-flight (the all-routes zero-page-error sweep the Phase-62 find demands), with fresh-venv install proofs on macOS + .43 and the announcement kit drafted in canon voice for the owner to post. The tag is the publish and is cut only at closeout from the merged main. Read [`AGENT-BRIEF.md`](./phase-65-the-launch/AGENT-BRIEF.md) first. Prior: **Phase 64 — Docs Catch-Up: CLOSED (3/3)** — opened and closed the same day; PR merged on green. Discovery was the gap, not coverage: the deep guides were already coherent (the per-phase docs stories held the line), but the entry points froze at Phase 58. The README's two-modes tour now carries the wake word / the spoken language setting / the spoken-symbol dictionary / Send to Slack; a Phase-62 sweep miss fixed (the Meet Qlippy paragraph still narrated the retired three-questions pattern); the index + Getting Started point forward (anchor-linked "Where To Go Next"); every embedded image audited, with ONE quiet-trust candidate flagged for the owner rather than scope-creeped (the journal tab's Phase-45 trust banner). Suite **2775 passed, 17 skipped** (docs-only). See [`final-summary.md`](./phase-64-docs-catch-up/final-summary.md). Earlier same-day: **HS-64-02 done** — the index blurbs name the new surface, Getting Started gains an anchor-linked "Where To Go Next", and the coherence read across the P59–P63 passages found the deep guides already coherent (zero retired-pattern or stale-version hits). Prior same-phase: **HS-64-01 done** — the README's two-modes tour absorbs the P59–P62 surface (wake word with the preview-default truth, the spoken language setting, the spoken-symbol dictionary, Send to Slack under the never-acts sentence; the local pillar notes the 99 languages), and a real find fixed: the Meet Qlippy paragraph still narrated the retired Phase-56 three-questions pattern (a Phase-62 sweep miss), now the badge posture. Guard 13 green. Earlier same-day: scaffolded — opened on owner direction ("Feels like a docs phase is on the books again, no?"). Measured: the README has ZERO mentions of the wake word and Send to Slack and its two-modes tour stops at the Phase-55 surface; the docs index and Getting Started point at nothing newer — while the deep guides are current (the per-phase docs stories did their jobs). Discovery is the gap, not coverage: three stories (the README catches up → the index + Getting Started + one humanizer-grade coherence read over the solo-written post-58 passages → closeout with an embedded-image audit), all under POSITIONING canon and the live voice guard. Read [`AGENT-BRIEF.md`](./phase-64-docs-catch-up/AGENT-BRIEF.md) first. Prior: **Phase 63 — Backend Decomposition: CLOSED (6/6)** — opened and closed the same day on owner direction ("E, puh-lease"); PR merged on green. The backend twin of Phase 54 landed: **web_runtime 2,635 → 555** over eight mixins in `holdspeak/runtime/`; **meeting_session 1,674 → a package** (lifecycle core 795, pure models out, four mixins); verbatim to ONE lost line per story (the class statements, rewritten as compositions); the censused patch-target policy held (13 sites, assertions byte-identical); the shape locked by the backend density guard + the twin architecture doc. **The closeout's live boot proof caught two pre-existing production bugs** no suite or dogfood had reached since Phase 60: live meeting start was BROKEN (HS-60-03's stray on_wake_type kwarg on the MeetingSession constructor, masked by a **kwargs fake — fixed + AST/signature-locked) and the transcriber's unlocked check-then-construct raced warmup vs first-use into the process-fatal cross-thread MLX death (fixed with a dedicated init lock + an 8-thread race lock). The full live trace: real run_web_runtime boot → a real meeting on the real recorder + real MLX Whisper → the carved save path → dry-run → zero cockpit page errors → clean SIGTERM (8/8). Final suite **2775 passed, 17 skipped** (+7); BACKLOG **E shipped**. See [`final-summary.md`](./phase-63-backend-decomposition/final-summary.md). Earlier same-day: **HS-63-05 done** — the shape is locked: 5 guard tests (runtime core ≤650, session core ≤850, every concern module ≤600, carve-don't-bump messages, proven both ways; routes/meetings.py recorded as the named watch item rather than silently bumped in) + `ARCHITECTURE_BACKEND_RUNTIME.md` (the backend twin doc: the concern map, the five rules incl. patch-targets-live-where-the-lookup-happens with this phase's two hard-way stories, the add-a-concern walkthrough) + the CONTRIBUTING pointer. Suite **2773 passed, 17 skipped** (+5). Prior same-phase: **HS-63-04 done** — the platform glue is out (meeting_glue 552 / routing_glue 450 / activity 264 / plugin_queue 171 / transcriber_state 144) and **web_runtime.py is 555 lines, boot/run/config only** (2,635 at phase open; meeting_session went 1,674 → 795 core). Verbatim: 0 body lines lost. The census proved itself the hard way: the first post-carve run ABORTED with a REAL MLX Whisper loading inside a unit test (the web_runtime.Transcriber patch missed its moved lookup) — the 8 censused retargets landed, assertions byte-identical, and every new module had its unused imports auto-trimmed so no patchable name exists where nothing calls it. Suite **2768 passed, 17 skipped**. Prior same-phase: **HS-63-03 done** — the feature glue is out of WebRuntime (dictation capture 413 / wake glue ~360 / device glue ~315; the core at **1,763**, was 2,635), verbatim-proven again to one line. The censused patch-target retargets landed (5 sites, 3 files) with an honest find: the first wake retarget passed two tests COINCIDENTALLY (the patched name lived in the wrong module's namespace; the real pipeline ran silently) — the third test failed loudly, the targets now point where the lookup actually happens, and the misleading unused imports are trimmed. Suite **2768 passed, 17 skipped**. Prior same-phase: **HS-63-02 done** — MeetingSession is now composed of four single-concern mixins (transcribe loop / intel analysis / persistence / mutations, all ≤285 lines) over a 795-line core (the original file was 1,674); the verbatim proof is ONE line (the only original body line lost is `class MeetingSession:` itself, rewritten as the composition); **zero test edits**, exactly as the scaffold census predicted. Suite **2768 passed, 17 skipped**. Prior same-phase: **HS-63-01 done** — `meeting_session` converted to a package (the brief's `holdspeak/meeting/` name was owned by MeetingRecorder; the conversion keeps the canonical import point instead), the five pure models carved to `models.py` with a body-line-diff verbatim proof (0 lines lost) and **zero test edits** across 38 importing files; a packaging trap (indented try-block optional imports silently swallowing ImportError) caught by 8 loud test failures and recorded for the remaining carves. Suite **2768 passed, 17 skipped**. Earlier same-day: scaffolded — backlog row **E** opened on owner direction ("E, puh-lease"): the backend twin of Phase 54. `web_runtime.py` is **2,635 lines** (regrown PAST its pre-Phase-52 size despite the dictation_runner carve) and `meeting_session.py` is **1,674** (models + recording + transcription + intel + persistence + mutations in one file). The cure is Phase 54's, translated to Python: verbatim-move mixins in `holdspeak/runtime/` + `holdspeak/meeting/`, thin assembly cores, a backend density guard, a live boot closeout. The test-edit policy is fixed at scaffold from a monkeypatch census: 11 patched globals, 6 stay with the core, 5 move with their methods — patch-target paths are the ONLY permitted test edits, assertions byte-identical. Six stories. Read [`AGENT-BRIEF.md`](./phase-63-backend-decomposition/AGENT-BRIEF.md) first. Prior: **Phase 62 — Quiet Trust: CLOSED (4/4)** — opened and closed the same day on direct owner feedback ("you can just literally have a symbol for local only or local plus cloud or just cloud"); PR merged on green. Trust is now ambient, not narrated: the Qlippy privacy paragraphs became one **egress badge** (⌂ Local / ⌂+☁ / ☁ + target, the destination surviving on the pill), nine files of reassurance tails were cut to their functional core (the TrustChip surfaces + one welcome pitch line + the behavioral warnings deliberately remain, once each), POSITIONING carries the standing voice rule (egress is a badge, not prose), and all seven user-facing screenshots were re-shot from live content-asserted runs incl. the native overlay on **real .43 metal**. Two pre-existing bugs fixed on the way: the welcome wizard's never-worked copy button (a Phase-43 escaped-quote Alpine SyntaxError on every /welcome load) and the dashboard guard lying for slack targets. Closeout proven on REAL broadcast-driven cards (17/17, zero page errors). Final suite **2768 passed, 17 skipped**. See [`final-summary.md`](./phase-62-quiet-trust/final-summary.md). Earlier same-day: **HS-62-03 done** — docs + the re-shot screenshots: the POSITIONING voice rule (egress is a badge, not prose) is canon; README/doc alts stopped quoting retired paragraphs; all seven user-facing screenshots re-shot from live, content-asserted runs, incl. the native overlay on **real .43 metal**; and the zero-page-error gate caught + fixed a **pre-existing Phase-43 bug** (the welcome wizard's escaped-quote @click HTML-truncated into a SyntaxError on every /welcome load — that copy button never worked; verified pre-existing on main in a clean worktree). Suite **2768 passed, 17 skipped**. Prior same-phase: **HS-62-02 done** — the sweep: nine files, every operational reassurance tail cut to its functional core (history/dashboard notes + guards + flashes, the wizard rail, the settings lead, the LocalPill tooltip, ContextSection); the per-target guard fix on the dashboard closed a real lie ('only records your decision' was untrue for slack since Phase 61); the deliberate remainder is the TrustChip surfaces + one welcome pitch line + the behavioral warnings, audited by grep in evidence. Locks updated in place; suite **2768 passed, 17 skipped**. Prior same-phase: **HS-62-01 done** — the egress badge: Qlippy's card shell renders one pill (⌂ Local / ⌂+☁ / ☁ + target) from a structured `egress` field, every card swapped its privacy paragraph for it (`privacyLine()` deleted; the wake card's not-typed state is three words), and the Phase-56 verbatim locks were REWRITTEN to pin the badge and refuse the retired prose forever. Live dogfood 11/11, zero page errors ('☁ slack' / '⌂ Local' styled, computed-style probed; no retired phrase in the card DOM). Suite **2768 passed, 17 skipped**. Earlier same-day: scaffolded — opened on direct owner feedback: the privacy-reassurance prose across the UI is "really cringey"; cards and notifications must not narrate "nothing leaves your machine" — *"you can just literally have a symbol for local only or local plus cloud or just cloud"*. One compact three-state **egress badge** replaces the privacy paragraphs on Qlippy cards (REVERSING the locked Phase-56 three-answers-verbatim decision); a sweep cuts every reassurance tail in web/src (behavioral warnings stay); docs align and every user-facing screenshot showing the old copy is re-shot live. Four stories: the badge → the sweep → docs+screenshots → closeout. Read [`AGENT-BRIEF.md`](./phase-62-quiet-trust/AGENT-BRIEF.md) first. Prior: **Phase 61 — Send to Slack: CLOSED (4/4)** — opened the day before on owner direction (backlog **L** scoped to one easy connector; **N (Windows) owner-rejected** in the same direction); PR merged on green. The aftercare digest + follow-up draft gained one outbound door through a Slack incoming webhook, riding propose→approve→execute on the Phase-38 host-gated connector: the preview IS the wire body (byte-equal, proven on a real receiver), **approving is the moment it posts** (the execute leg the repo never had — a scaffold ground-truth find was that NO production path ever executed an approved proposal), and **the credential rule is structural** (the URL never enters payloads/broadcasts/responses; the connector joins it in memory at execution time). The closeout ran with no mocks anywhere: real Chromium clicks → the real gated connector → a real urllib POST onto a real local incoming-webhook receiver, byte-equal; the live wrong-host probe refused before any socket opened; the off-proof 400 held; a guard-copy lie for slack targets found and fixed ("only records your decision" → the per-target truth). Final suite **2768 passed, 17 skipped** (+45); BACKLOG **L shipped**. See [`final-summary.md`](./phase-61-send-to-slack/final-summary.md). Earlier same-day: **HS-61-03 done** — docs: the Meeting Mode Guide's aftercare section gains "Send to Slack" with a real product screenshot and the plainly-stated truths (configured-only; the preview IS the message; **approving is the moment it posts**, explicitly contrasted with the state-only GitHub approval; the host gate; the URL-as-password rule; visible truncation) + the API-list row; SECURITY gains the double-opt-in egress row and the secrets entry; POSITIONING gains the canonical row ("Send to Slack", not "Slack integration"/"Slack export"); the voice guard's banned-names pattern covers the new synonyms, proven both ways. Doc slice 13 green; suite **2767 passed, 17 skipped**. Prior same-phase: **HS-61-02 done** — the surfaces: a green-edged "Send to Slack" pill on the aftercare digest + a second button beside Copy draft, both gated on a `slack_configured` bool the aftercare API now carries (never the URL — string-probed); the draft's privacy note flips honestly between "Preview and copy only" and the approval truth; the decision flash tells the execute-on-approve truth ("Approved — sent to Slack."); the settings field ships the honest copy (what is sent / only after approval / only this URL's host / stored locally, shown nowhere else). Live Chromium dogfood 11/11, zero page errors (visible off-proof; clicking records exactly one `proposed` proposal with NO egress; the credential nowhere on the record); 5 lock tests; 4 reviewed screenshots (a wrap-mid-word pill caught in the first screenshot and fixed). Suite **2767 passed, 17 skipped**. Prior same-phase: **HS-61-01 done** — the engine + route + **the execute leg the repo never had**: a scaffold ground-truth find was that NO production path ever executed an approved proposal (the `ActuatorExecutor` was host-injected in dogfoods only), so the decision route now executes approved `slack` proposals through the full executor guard stack (status gate, parity, audit, the `actuator_result` broadcast; other targets unchanged, locked). `slack_export.py` builds the exact mrkdwn message (visible truncation cap, never silent); the export route records a wire-safe proposal whose **preview is byte-equal to the stored body**; **the credential rule is structural** (the URL never enters the payload — the connector joins it in memory at execution time, so the proposals listing cannot leak it by construction); one shared URL rule (https with a host; loopback http only, for the real-receiver closeout) at config AND settings. +39 tests; suite **2762 passed, 17 skipped**. Earlier same-day: scaffolded — backlog candidate **L**, scoped by the owner to one easy connector ("Export Connectors are fine…, but let's just choose an easy one"); the pick is the Slack **incoming webhook** (one POST, no OAuth) because the Phase-38 gated webhook connector documents Slack incoming webhooks as its target case. In the same direction, **N (Windows) was rejected by the owner** and is recorded in BACKLOG as community-port-welcome. The aftercare digest + follow-up draft gain one outbound door riding the existing propose→approve→execute flow: every send is a proposal whose **preview IS the exact message**, approved per-action, POSTed by the host-gated connector; the webhook URL is treated as a credential (never in previews/broadcasts/responses); unconfigured is byte-identical. Seams verified at scaffold: `build_webhook_connector` (Phase 38), the file-issue route as the proposal template (Phase 49), Qlippy decision/result cards free on the existing broadcasts (Phase 56). Four stories: engine+route → surfaces → docs → the real-POST closeout. Read [`AGENT-BRIEF.md`](./phase-61-send-to-slack/AGENT-BRIEF.md) first. Prior: **Phase 60 — The Wake Word: CLOSED (6/6)** — opened and closed the same day; PR merged on green. Hands-free entry that keeps the trust posture: say the phrase, HoldSpeak arms visibly, the sentence runs the normal pipeline, and the result is **previewed, never typed** by default (Type it = a server-minted one-shot token; client text structurally ignored; the type action an explicit opt-in). Measured: **0 false accepts in 57 ordinary utterances** at the 0.5 default; the near-homophone class fires by nature and is mitigated by design. The live closeout (7/7, real detection → real Whisper verbatim → the real route) flushed out **two latent production crashes**: GGML's lldb auto-attach suspending all threads (→ GGML_NO_BACKTRACE at the package root) and cross-thread MLX transcription being process-fatal (→ all MLX work pinned to one dedicated thread — the Transcriber is now caller-agnostic for EVERY feature). Final suite **2723 passed, 17 skipped** (+40); BACKLOG **O shipped**. See [`final-summary.md`](./phase-60-wake-word/final-summary.md). Earlier same-day: **HS-60-05 (docs) done:** the User Guide leads with the safety fork and quotes the measured numbers verbatim (incl. the near-homophone reality); SECURITY gains the honestly-framed download row; POSITIONING gains the canonical rows; doc slice 82 green; suite 2723. Prior: **HS-60-04 (the false-accept measurement) done, with the phase's central honest finding:** near-homophones ("hey jarred": 0.995) and wake-word-containing sentences (0.996) are indistinguishable from real wakes — no threshold separates them; that is WHY preview-default exists. Ordinary speech (57 utterances, adversarial included): **0 false accepts** at 0.5, margin 0.196. The first run's honest FAIL drove the two-class split. Prior: **HS-60-03 (the armed UX + settings) done:** the armed state first-class on presence/Qlippy/every socket page; the sticky preview card with the safety copy; the one-shot Type-it route burning server-minted tokens and structurally ignoring client text (injection-asserted); the honest settings copy (egress note, false-detection warning, presence recommendation) + first-enable model download. 8 tests; 3 screenshots; suite **2723**. Prior: **HS-60-02 (arm, capture, the pipeline) done:** ArmedCapture (onset/silence-stop/cap/silent-disarm), the floor-respecting frame source, detect→arm→capture→the NORMAL pipeline (journal source `wake`), preview default with a one-shot burned token that NEVER touches the typing seam (locked), type as the explicit opt-in; lifecycle at boot/settings/shutdown. 10 tests; suite **2715**. Prior: **HS-60-01 (the engine seam + config) done:** the injectable `WakeWordListener` with the safety behaviors built in (refractory double-arm block; pause drains-never-scores; resume resets AND re-arms; observer-safe), the engine lazy-import-locked, `WakeWordConfig` (off/preview/0.5/8s) with strict settings 400s, the `[wakeword]` extra. 22 tests, none needing the engine; suite **2705**. Earlier: scaffolded — candidate **O** opened on standing direction; the spike already proves feasibility on this machine (openwakeword 0.6.0 installs on py3.13 arm64; wake 0.861 vs distractors ≤0.063 — wide margin at the 0.5 default) and the egress moment is identified (the ~7 MB model download from GitHub on first setup, to be stated in settings/docs/SECURITY). Six stories: the injectable engine seam → arm/capture/pipeline (preview default, NEVER types) → the armed UX + one-shot Type-it → the measured false-accept harness → docs → the real-metal closeout. Prior: **Phase 59 — Speak Your Language: CLOSED (4/4)** — opened and closed the same day; PR merged on green. The input layer adapts to you: `model.language` ("auto" default, byte-identical structurally) pins Whisper's 99 languages through the one shared Transcriber (dictation + meetings + import; all four construction sites source-locked; settings-boundary validation; the UI select registry-lockstep-locked) and the spoken-symbol dictionary merges user vocabulary over the built-ins (user wins, literal symbols, the settings editor). **A real find fixed**: per-table ordering broke cross-table phrases — the matcher is now one combined longest-first pass, with all 55 pre-existing processor tests passing unmodified as the byte-identical proof. **Proven on real metal**: real German speech through real Whisper pinned to `de`, near-verbatim; the dictionary through the real settings round-trip. +38 tests; final suite **2683 passed, 17 skipped**; BACKLOG **K shipped**, **O next** (with its misfire-safety conditions). See [`final-summary.md`](./phase-59-languages/final-summary.md). Earlier same-day: **HS-59-03 (docs) done:** the User Guide gains "Speak your language" (the honest short-utterance auto-detect note; one setting covers dictation/meetings/import) + the spoken-symbol dictionary beside the punctuation table (user-wins, all four attach modes, `std::vector`); Getting Started pointer; zero dashes; doc slice 81 green; suite 2683. Prior: **HS-59-02 (the spoken-symbol dictionary) done:** user spoken→symbol entries (attach semantics, user-wins merge, literal-symbol guarantee) over the built-in punctuation table; the Voice-typing editor; a real find fixed (per-table ordering let built-in "colon" eat a user's "double colon" — the pass is now one combined longest-first sweep, with all 55 pre-existing processor tests passing unmodified as the byte-identical proof); 20 tests; suite **2683**. Prior: **HS-59-01 (the language knob) done:** the vendored 99-language registry (pure, import-locked), `ModelConfig.language="auto"`, both backends threaded with a conditionally-built kwarg (auto is byte-identical structurally), all four construction sites source-locked, settings validation (a typo fails the write and changes nothing) + the Voice-section select set-equality-locked to the registry. 18 tests; suite **2663**. Earlier: scaffolded — candidate **K** opened on user direction ("K, then O"); seams verified: neither backend is ever passed a `language` (today IS auto-detect, so the "auto" default is byte-identical by construction), four Transcriber construction sites to thread, the punctuation table ready to merge user entries. Four stories: the language knob → the spoken-symbol dictionary → docs → a real-metal non-English closeout. Prior: **Phase 58 — The Front Door: CLOSED (6/6)** + two post-close follow-ups on user feedback (real product screenshots; the Meet Qlippy section (the README now showcases real product screenshots: the meeting artifacts under "The two modes", Qlippy's decision card in his paragraph). — opened and closed the same day; PR merged on green. HoldSpeak's story is now a decision, not an accumulation: the positioning canon (one copilot two modes / developers / named honest comparisons, the owner's fixed calls) binds every user-facing doc via CLAUDE.md; the README pitches both modes with **the comparison section the repo never had**; every guide opens with a why-lede in canonical names; **171 prose dashes removed hand-by-hand** (1 allowlisted verbatim UI quote survives); the voice guard locks dashes/AI-vocab/names both-ways; the Phase-51 pattern widened (the live HS-9-03 find). Verified through GitHub's own renderer + curl-checked assets. Final suite **2645 passed, 17 skipped**; BACKLOG **Q shipped**, **K next**. See [`final-summary.md`](./phase-58-front-door/final-summary.md). Earlier same-day: **HS-58-05 (the voice guard) done:** four tests lock dashes-zero (verbatim-UI-quote allowlist), the AI-vocab tells (tuned live to zero false positives), and canonical names over the user-facing corpus, proven both ways; the Phase-51 pattern widened to single-digit phases (the live HS-9-03 find); DOCS_STYLE.md documents the rules; suite **2645** (+4). Prior: **HS-58-04 (the developer + ops docs) done:** contributor-pitch ledes on PLUGIN_AUTHORING + CONNECTOR_DEVELOPMENT; 101 prose dashes removed across six files (example code exempt); no contract/protocol/schema fact altered; locks green; suite 2641. Prior: **HS-58-03 (the core guides) done:** all eight user-flow guides revised against the canon (why-ledes, canonical names, 70 dashes removed hand-by-hand; the one survivor is a verbatim UI quote the canon exempts). Bonus find: an `HS-9-03` vocab leak the Phase-51 guard's two-digit pattern misses (fixed; widening → HS-58-05). Locks green; suite 2641. Prior: **HS-58-02 (README, the front door) done:** the hero is the canon's one-liner; "The two modes" tour with equal billing + the post-48 surface (voice commands, pre-briefing, recording AND transcript import, aftercare, facets); the four pillars; **the named comparison section the repo never had** (both directions, date-stamped, closed by our own trade-offs); Qlippy as the delight beat; Contributing pitches building ON HoldSpeak; docs index hero aligned; zero dashes; plugin-count/link/image/vocab locks green; suite 2641. Prior: **HS-58-01 (the positioning canon) done:** `docs/internal/POSITIONING.md` fixes the story on the user's three decisions (one copilot two modes / developers / named honest comparisons): the one-liner, four pillars with shipped proof points, the named competitive frame (both directions, as-of-dated), the canonical feature-name table with banned synonyms, the voice rules; CLAUDE.md lists it as source canon. Earlier: scaffolded — corpus measured: vocab already clean, em-dashes pervasive pre-Phase-55 (PLUGIN_AUTHORING 82), no comparison section anywhere, README's feature story stops ~Phase 48. Prior: **Phase 57 — Transcript Import: CLOSED (5/5)** — opened and closed the same day; PR merged on green. Transcripts (`.vtt`/`.srt`/`.txt`) import as real meetings through the exact Phase-55 pipeline (one factored persistence tail; the recording path untouched and locked): real cue timestamps + real multi-speaker labels when the file carries them (the speaker facet works across imported transcripts for the first time), honest synthetic ordering for plain text, no model load ever (poisoned-factory-locked). **Proven on real metal**: a multi-speaker VTT through the real API → real intel on the `.43` endpoint (accurate summary + 2 extracted action items) → the speaker facet by a transcript-carried name → a real `say`→Whisper WAV in the same run. Three real finds: the untitled-import temp-stem title (latent Phase-55, audio too), the U+FFFD-passes-the-binary-gate hole, the 4-word name-rule overmatch. +39 tests; final suite **2641 passed, 17 skipped**; BACKLOG **P shipped**, **K next**. See [`final-summary.md`](./phase-57-transcript-import/final-summary.md). Earlier same-day: **HS-57-04 (docs) done:** the Meeting Mode Guide's import section became "Import an Existing Recording or Transcript" with an "Importing a transcript" subsection (the three formats + the no-model fast path, the speaker + timestamp honesty rules, the refusal posture, file-not-retained, the explicit recordings-unchanged statement); docs index touched; zero dashes in new text; doc-guard slice green; full suite **2641 passed, 17 skipped** (docs-only). Prior: **HS-57-03 (API + /history UI) done:** the route's worker branches by suffix (a transcript upload rides the exact recording lifecycle and **never constructs a transcriber** — a poisoned factory locks it in tests AND the live dogfood); the /history panel reads "Import a recording or transcript" with per-kind honest notes. **Two real bugs found + fixed**: untitled imports fell back to the temp file's stem (latent Phase-55, audio path included) and the binary-garbage gate counted U+FFFD as printable. Live browser dogfood 3/3 (a real multi-speaker VTT upload → file speakers + real cue starts + queued intel), zero page errors, 3 screenshots. +5 tests; full suite **2641 passed, 17 skipped**. Prior: **HS-57-02 (the engine path + CLI) done:** the Phase-55 persistence tail factored verbatim into `_persist_import` (audio byte-identical, its test file untouched); `import_transcript` persists honest segments through that one tail (real cue timestamps for VTT/SRT, synthetic ordering for TXT, file-carried speakers, mtime `started_at`); `ImportResult` truthful (0 windows + additive `speakers_found`); `validate_format` learns the trio; `holdspeak import notes.vtt` runs with **no model load** (the no-transcriber guarantee monkeypatch-asserted); one Phase-55 route-test fixture updated (.txt deliberately stopped being unsupported). 12 tests; full suite **2636 passed, 17 skipped**. Prior: **HS-57-01 (the transcript parsers) done:** `holdspeak/transcript_parse.py`, pure + greenfield — VTT (BOM/CRLF, hour-optional timings, voice tags + the continuity model, NOTE/STYLE skip, tag stripping, mislabeled-suffix rescue), SRT (comma times, `Name:` prefixes), TXT (a conservative ≤3-word name rule + structure-label blocklist; synthetic spacing); honesty flags (`has_real_timestamps` only for cue-timed formats, `speakers_found` only file-carried labels); zero-cue input refused actionably. 22 tests incl. a Teams/Zoom-shaped fixture + a purity lock; full suite **2624 passed, 17 skipped**. Earlier: scaffolded — opened on user direction right after Phase 56 closed: transcripts ("I often have transcripts, rarely do I have recordings") import as real meetings via the Phase-55 pipeline's factored persistence tail; a greenfield VTT/SRT/TXT parser module; the same route/UI lifecycle with honest per-kind notes; the recording path byte-identical (explicit user constraint). Five stories (parsers → engine+CLI → API/UI → docs → closeout with a real-VTT + real-`.43`-intel dogfood). Prior: **Phase 56 — Qlippy, the Presence Enhancer: CLOSED (7/7)** — opened and closed the same day; PR merged on green. The presence layer got a face: the double-gated Qlippy dock + the one-at-a-time card for exactly four moments (decision needed — the sticky marquee; result; learned-from-you under the honest-reach rule; meeting aftercare), with the G privacy answers verbatim on every actionable card (doc-locked). The closeout dogfood passed 9/9 on one live runtime with zero page errors: dock over the REAL socket, the queue holding a race ("+1"), **audit parity side by side** (card approve == dashboard approve, both approved/web-user), the off-proof byte-identical. The native HUD frame was **proven live on real Linux metal** (a real xdotool click on the real `.43` overlay recorded the audited decision; xwininfo logged 408x132 → 408x460 → 408x132), which flushed out **two latent Phase-41 production bugs** (the unpinned `Gdk` import; fork-from-threads deadlocking the GTK child → forkserver). The macOS live click waived by the user (locked screen; the proof script ships ready). Final suite **2602 passed, 17 skipped** (+34 this phase); BACKLOG **J shipped**, **G absorbed-shipped**, **K next**. See [`final-summary.md`](./phase-56-qlippy/final-summary.md). Earlier same-day: **HS-56-06 (the Qlippy docs) done:** the typing guide's Desktop Presence section gains "Qlippy, the mascot (optional)" (double opt-in, dock vs. card, exactly which moments card, the never-acts guarantee + the three privacy answers verbatim — LOCKED by a doc-drift test that also pins the markers in `qlippy-events.js`), two live-dogfood screenshots in doc assets, docs index linked, zero dashes in the new text, vocab guard green; full suite **2602 passed, 17 skipped**. Prior: **HS-56-05 (the native HUD frame) done, proven live on real Linux metal:** one pure `presence_panel_frame` policy drives both native renderers (passive IS the exact Phase-41 click-through geometry, locked by test; a card is 408×460 + pointer events, never focus), card presence read from the page itself (`#qlippy-card.is-in` probe). Going live on the `.43` Xorg box (user direction) found **two real Phase-41 production bugs** — the unpinned `Gdk` import (GTK4-shipping boxes explode) and fork-from-threads deadlocking the GTK child under a running server (→ `forkserver`). Live, no mocks: the real overlay grew for a real proposal's card (xwininfo: 408x132 → 408x460 → 408x132, same origin), a REAL xdotool click on Approve recorded the audited decision, the X11 active window never changed. The macOS live click waived by the user (locked screen); panel boot verified, glue unit-tested, proof script ships. 11 tests; full suite **2601 passed, 17 skipped**. Prior: **HS-56-04 (learning + aftercare cards) done:** the journal correct route broadcasts `learning_event` only under the honest-reach rule (`taught && similar > 0`; the wire's reach asserted equal to the route's — one matcher, one number), `MeetingSession.save()` broadcasts `aftercare_ready` only for a finished, DB-saved meeting with a non-empty digest (autosaves stay quiet), and the deferred intel queue gains a purely-observational, exploding-observer-safe `on_meeting_ready` hook the web drain route wires to the same broadcast. The `learned` 💡 card reports "matches N past dictations" (with the corrections-off honesty suffix, View digest) and the `present-note` aftercare card names the open count + top items (Open aftercare, 14-s auto-dismiss). Live dogfood 4/4 with zero page errors — a real correction and a real meeting wrap drove both cards; a refused teach stayed silent. 8 tests; full suite **2590 passed, 17 skipped**. Prior: **HS-56-03 (the actuator card; G absorbed) done:** the aftercare route broadcasts `actuator_proposed` (wire-safe), rejections broadcast `actuator_result`, the executor gains a purely-observational `on_result` hook, and the card's Approve/Decline send the byte-identical decision request the dashboard sends (decision-only — execution stays the guarded executor's job). The G privacy answers are verbatim-locked on every actionable card. Proven live with no mocks (a real proposal's real broadcast → the card → an audited approval, zero page errors). 4 tests; full suite **2582 passed, 17 skipped**. Prior: **HS-56-02 (the dock + the card shell) done:** Qlippy lives on `/presence` behind the double gate — the sprite-strip dock following the runtime state map (5-min sleep, the complete flourish), and the sliding card shell (`window.qlippyCard.present`) with the full Signal-settle motion spec, strict one-at-a-time FIFO + "+N" hint, pause-on-hover, aria-live announce, reduced-motion fades, and **no POST anywhere in the shell** (locked by test). Live dogfood 6/6 with zero page errors; 4 reviewed screenshots; full suite **2578 passed, 17 skipped**. Prior: **HS-56-01 (assets + the mascot gate) done:** the PixelLab pack vendored to `web/public/qlippy/` (14 sprite strips + 4 glyphs + provenance README, verified in the built bundle), `presence.mascot` (default off) round-tripping `/api/settings` with zero route changes and coercing older configs forward, and the `/settings` presence section's indented inert-when-off "Qlippy, the mascot" sub-toggle. 5 tests; full suite **2573 passed, 17 skipped**. Earlier: scaffolded — backlog candidate **J** (absorbing **G**, privacy at decision points), the third step of the agreed sequence. The presence layer gets a face: an ambient mascot dock reflecting `runtime_activity`, and a sliding, focus-safe, one-at-a-time card making the actuator-approval and learning moments actionable in place — Approve/Decline as a faster front door to the *same* audited decision route, never a new permission path; every actionable card answers what data / does anything leave / what control. Seam find at scaffold: `actuator_proposed` is **already broadcast** (Phase 38); the gaps are `actuator_result`, `learning_event`, `aftercare_ready`, and the aftercare-route proposal broadcast. Seven stories (assets+gate → dock+card shell → actuator card → learning/aftercare cards → native HUD frame → docs → closeout); opt-in twice (presence + mascot, both default off), flag-unset byte-identical. RFC: [`proposals/qlippy-presence-enhancer.md`](./proposals/qlippy-presence-enhancer.md). Read [`AGENT-BRIEF.md`](./phase-56-qlippy/AGENT-BRIEF.md) first. Prior: **Phase 55 — Meeting Import + Faceted History Search: CLOSED (6/6)** — opened and closed the same day, the second step of the agreed sequence **54 → I → J → K**. The archive is part of the product: an import engine (WAV native / ffmpeg-assisted, windowed real-timestamp transcription, the live path's intel conditions, audio not retained), `POST /api/meetings/import` on a background thread + `holdspeak import`, an inviting `/history` upload flow with a live importing lifecycle, server-side facets (date/speaker/tag/open-actions) composing with full-text search, and honest docs. **Proven on real metal**: real `say` speech → real MLX Whisper transcribed both utterances verbatim across two windows → real intel on the `.43` Qwen endpoint reached `ready` with a correct summary → facets included/excluded it; the first run proved the honest-failure path too. Three real finds: the missing `DELETE /api/meetings/{id}`, the broken search-result status pill (nested `intel_status`), and a machine-config backend quirk. +23 tests; final suite **2568 passed, 17 skipped**. BACKLOG candidate **I** flipped to shipped; PR merged on green. See [`final-summary.md`](./phase-55-meeting-import/final-summary.md). Earlier same-day: **HS-55-05 (docs) done:** "Import an Existing Recording" + "Find Meetings in Your Archive" sections in the Meeting Mode Guide (product-tense, the three honest truths in plain prose, humanizer findings fixed, zero dashes in the new sections), docs-index + root-README touches; doc guards green; full suite **2568 passed, 17 skipped**. Prior: **HS-55-04 (faceted history search) done:** server-side `date_from`/`date_to`/`speaker`/`tag`/`has_open_actions` filtering in SQL over the whole archive, composing with full-text `search`, + the `/history` filter row fed by `GET /api/meetings/facets`; one query builder drives every meetings fetch so filters survive search, refresh, and the import poll. A real pre-existing bug fixed along the way: search results returned nested `intel_status` objects that broke the status pill. 6 new tests + a live Playwright pass; full suite **2568 passed, 17 skipped**. Prior: **HS-55-03 (the /history import UI) done:** an "Import a recording" opener + accent-edged panel (drag/drop target, metadata fields, the honest notes verbatim), a pulsing "Importing…" pill with live window progress on the meeting card, in-place resolution via a quiet import-only poll, and a Remove affordance on failed imports — proven live by a real browser upload (Playwright, zero page errors, 3 screenshots). 4 page-content locks; full suite **2562 passed, 17 skipped**. Prior: **HS-55-02 (the import API + CLI) done:** `POST /api/meetings/import` (multipart; `python-multipart` added) refuses bad formats up front, saves a visible importing-state row immediately, returns 202, and transcribes on a daemon thread with progress riding the meeting row; failures mark `import_failed` honestly and are removable via the **new** `DELETE /api/meetings/{id}` (the repo's `delete_meeting` had no HTTP route at all); plus `holdspeak import <file>` with per-window progress. 4 route tests incl. responsiveness-under-import; full suite **2558 passed, 17 skipped**. Prior: **HS-55-01 (the import engine) done:** `holdspeak/meeting_import.py` turns a recording into a real meeting — stdlib-`wave` PCM decode (scipy is dev-only), ffmpeg-assisted compressed formats with an honest refusal when absent, resample/downmix to the transcriber contract, ~30 s windowed transcription with real segment timestamps, `started_at` from file mtime, the live path's exact intel-enqueue conditions. 8 unit tests + a downstream-parity integration test; full suite **2554 passed, 17 skipped**. Earlier: scaffolded — backlog candidate **I**, the next step of the agreed sequence **54 → I → J → K**. Meeting intelligence is live-capture-only today; the file→transcribe→meeting recipe is already proven by the spoken-e2e harness, and Phase 55 productizes it: an import engine (WAV native, ffmpeg-assisted compressed formats, windowed transcription with real timestamps, the same intel enqueue as live), `POST /api/meetings/import` + `holdspeak import`, an inviting `/history` upload affordance, and server-side facets (date/speaker/tag/open-actions) because import makes archives big enough to need them. Honest v1 limits stated up front: one speaker label (no single-file diarization dependency), audio not retained, ffmpeg required for compressed formats. Six stories (engine → API/CLI → UI → facets → docs → closeout with a real-Whisper dogfood). Read [`AGENT-BRIEF.md`](./phase-55-meeting-import/AGENT-BRIEF.md) first. Prior: **Phase 54 — Dictation Frontend Decomposition: CLOSED (6/6)** — opened and closed the same day. The 6,101-line dictation frontend is now a 252-line page + fourteen components and a 19-line script entry + twelve behavior modules (largest file 576, **5.4× smaller**), locked by a density guard, fully behavior-preserving — and the seam caught **two real latent bugs**: a hoisting-masked duplicate `escapeAttr`, and JS-rendered UI (block cards, template cards, readiness cards, the JS editor's controls) that had shipped **silently unstyled** because Astro's attribute-scoped selectors never match runtime DOM (fixed; proven with computed-style probes). Three zero-pageerror Playwright dogfoods (4/4 nudge persistence, 16/16 all-tabs + write paths, 6/6 seeded-activity nudges + pin); final suite **2545 passed, 17 skipped** (+5 = the guard); the pattern documented in `docs/internal/ARCHITECTURE_WEB_FRONTEND.md` with `history.astro`/`index.astro` as named follow-ups. BACKLOG candidate **D** flipped to shipped; PR to `main` merged on green. See [`final-summary.md`](./phase-54-dictation-frontend-decomposition/final-summary.md). Earlier same-day: **HS-54-05 (docs) done:** `docs/internal/ARCHITECTURE_WEB_FRONTEND.md` records the frontend decomposition pattern as shipped (the seam, the registry, the scoping trap + `is:global` rule, the budgets, a six-step add-a-section walkthrough, `history.astro`/`index.astro` as follow-ups), linked from `CONTRIBUTING.md`; doc guards green; full suite **2545 passed, 17 skipped**. Prior: **HS-54-04 (the density guard) done:** `tests/unit/test_frontend_density_guard.py` locks the shipped shape (page ≤300 / entry ≤50 / components + modules ≤600 each; largest shipped 576) with carve-don't-bump failure messages, proven both ways; full suite **2545 passed, 17 skipped**. Prior: **HS-54-03 (section partials) done:** the 3,131-line `dictation.astro` carved into a **252-line composition** of fourteen components (eleven section partials carrying their own styles + three markup-less shared-style components imported first to preserve the cascade order, verified in the built bundle). The carve exposed and fixed a **real latent visual bug**: Astro's attribute-scoped styles never match JS-rendered DOM, so block cards, template cards, readiness cards, and the JS-rendered editor's controls had been silently unstyled — proven with before/after computed-style probes. Assertions byte-identical; slice 158 passed; full suite **2540 passed, 17 skipped**; all-tabs dogfood 16/16, zero page errors; nine-tab screenshot sweep committed. Prior: **HS-54-02 (behavior modules) done:** the 2,907-line `dictation-app.js` carved into a 19-line entry + twelve single-concern ES modules under `scripts/dictation/` (largest 576 lines, all under the ~600 budget), code moved verbatim, the module graph kept acyclic by a section-loader registry in `core.js`. **Zero test files changed**; slice 158 passed; full suite **2540 passed, 17 skipped**; `npm run build` clean. Proven live by an all-tabs Playwright dogfood: nine tabs activate + populate, dry-run → moment-of-truth → journal → correction add/delete all work, **zero uncaught page errors** (16/16 PASS). Prior: **HS-54-01 (the module seam) done:** the `?raw` + `new Function()` loader was a Phase-10 migration shim with nothing depending on the eval; the dictation page now loads its behavior as a real bundled ES module, with the first carved module (`scripts/dictation/discovery-nudge.js`) proven live by a 4/4 Playwright dogfood (show / dismiss-persists / return-on-clear / global-off-persists). Module scope immediately caught a latent duplicate `escapeAttr` declaration the eval had masked via hoisting (dead alias deleted, live implementation kept). Client chunks ship un-minified via a `configEnvironment` hook (Astro 6 hardcodes client `minify: true`) — the shim always shipped full un-minified source anyway. Every page-content assertion byte-identical (one helper learned the carved layout). Full suite **2540 passed, 17 skipped**; `npm run build` clean; 0 `_built/` tracked. Earlier: scaffolded — backlog candidate **D** promoted to its own phase after a post-53 strategic review: the dictation cockpit is now **6,101 coupled lines** (`dictation.astro` 3,134 + `dictation-app.js` 2,967) and the density invariant lost five phases running (P40/P45/P47/P48/P53). Six stories (module seam → behavior modules → section partials → density guard → docs → closeout), behavior-preserving (tests unmodified, screenshot-verified per tab); defines the frontend decomposition pattern (the twin of the 26/31/32/34 backend lineage). The same review refreshed [`BACKLOG.md`](./BACKLOG.md): new candidates **I** (meeting import + faceted history search), **J** (Qlippy presence enhancer, absorbing **G**; RFC at [`proposals/qlippy-presence-enhancer.md`](./proposals/qlippy-presence-enhancer.md)), **K** (languages + spoken-symbol dictionary), parked **L–O**; agreed sequence **54 → I → J → K**, hardware day closes 25/24 then 15 opens. Also fixed the stale Phase-53 index row (was "scaffolded", is CLOSED). Prior: **Phase 53 — Activity Pre-Briefing: CLOSED (7/7)** — closed the loop for real. Chasing a real-metal proof exposed that "Dictate with this" did not actually feed the model: the live dictation path never read the pin and the project-rewriter never consumed activity records. HS-53-07 wired both ends — a process-local one-shot selection pin (`dictation_selection.py`) set by `POST /api/activity/nudges/select` and consumed by `run_dictation_pipeline`, which now passes `selected_record_id` to `build_activity_context`; the project-rewriter names the selected record in both the draft + refine prompts. Proven on the `.43` Qwen3.5-9B-Q6 endpoint: the same generic dictation, run with vs. without a selected `github_issue`, yields a generic control task off the ledger `.hs` context vs. a treatment task "Implement the `--since` flag ... as requested in HoldSpeak#412" — the selection demonstrably changes the model output (`dogfood-real-llm-transcript.txt`, RESULT: PASS). Byte-identical default when no pin. +17 tests; full suite **2540 passed, 17 skipped**; `npm run build` clean; 0 `_built/` tracked. Two dogfoods green (engine math + real metal). See [`final-summary.md`](./phase-53-activity-prebriefing/final-summary.md); PR to `main` merged on green; BACKLOG candidate F flipped to shipped.)
**Current phase:** **Phase 69 — Web, Re-crafted: CLOSED (12/12)** ([final-summary](./phase-69-web-recrafted/final-summary.md), [status](./phase-69-web-recrafted/current-phase-status.md), [parity map](./phase-68-web-convergence/parity-map.md)) — the web flagship reached the iPad's felt craft. The delivery half of the web/iPad **convergence**, all 12 stories shipped (the Signal substrate broadened; premium sheets; Qlippy in the cockpit; the reactive waveform; the generation theater proven on real `.43` metal; the **full node canvas** at `/workbench`; `/companion`→**the Agent Desk**), PR #202. The original delivery half of the web/iPad **convergence**: bring the iPad's shipped "Signal" craft onto the web cockpit, substrate-first, per the Phase-68 parity map. The substrate landed (the `.signal-card` primitive + gradient/hairline tokens + `hs-materialize` motion + the egress badge + the Queue HUD), though it rode in on a mixed mobile checkpoint commit (`f64d80d`) so the cadence is reconciled here; **7 web-craft stories remain** (premium sheets, Qlippy dock→cockpit, reactive waveform, generation theater, the node-canvas epic, `/companion`→Agent-Desk). Owner direction (2026-06-29): the **full sweep in sequence** through the **full node canvas**, for true two-way felt parity. Prior: **Phase 68 — Convergence: the Design Map & Parity Plan: CLOSED (3/3)** ([parity map](./phase-68-web-convergence/parity-map.md)) — mapped the iPad's nine Signal craft patterns, audited two-way parity (web←craft / iPad←breadth), and produced the ordered 12-story Phase-69 delivery backlog; the foundation is byte-identical (the web already runs the same Signal tokens), so convergence is apply+port, not a redesign. Prior: **Phase 67 — The Dogfood: scaffolded** ([AGENT-BRIEF](./phase-67-the-dogfood/AGENT-BRIEF.md), [status](./phase-67-the-dogfood/current-phase-status.md)) — the `dogfood/` harness (3 mock repos with `.hs/`+KB+completed-stage history, a `say`-pipeline scenario generator, committed transcript fixtures, an isolated-`HOME` runner wired to `.43`) and the fillable two-tier `PROTOCOL.md` are built, with an opt-in plumbing pytest (`tests/e2e/test_dogfood_plumbing_e2e.py`, 20 green). The phase stays open until a full run is recorded under `dogfood/results/`. Prior: **Phase 66 — The Architecture Map: CLOSED (4/4)** ([final-summary](./phase-66-architecture-map/final-summary.md)): `docs/ARCHITECTURE.md` with six guard-protected Mermaid diagrams (component map, both pipelines, the learning loop, the device path, the trust boundary), traced against the shipped code and wired into the index/README/CONTRIBUTING. The strongest remaining backlog threads are the journal trust banner (Phase-64 quiet-trust question), the `web/routes/meetings.py` decomposition watch item, and optionally teaching the Astro docs site to render Mermaid. Prior: **Phase 65 — The Launch: CLOSED (4/4)** ([final-summary](./phase-65-the-launch/final-summary.md)): **HoldSpeak v0.3.0 is published on PyPI and verified from there**; the GitHub release is live; the [announcement kit](./phase-65-the-launch/announcement-kit.md) is the owner's to post. The pre-flight caught and fixed two dead-on-arrival page bugs on the way out. Open non-blocking threads: the journal trust banner (the Phase-64 quiet-trust question) and the `web/routes/meetings.py` decomposition watch item. Prior: **Phase 64 — Docs Catch-Up: CLOSED (3/3)** ([final-summary](./phase-64-docs-catch-up/final-summary.md)): the entry points caught up to six phases of shipped surface; one open owner question (the journal trust banner: quiet-trust treatment or keep). The strongest remaining strategic row is the launch moment (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 63 — Backend Decomposition: CLOSED (6/6)** ([final-summary](./phase-63-backend-decomposition/final-summary.md), [status](./phase-63-backend-decomposition/current-phase-status.md)): backlog row **E** shipped (web_runtime 555 / meeting_session a package; guard-locked; two pre-existing production bugs in the live meeting path found + fixed at closeout). Named watch item: `web/routes/meetings.py`. The strongest remaining strategic row is the launch moment (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 62 — Quiet Trust: CLOSED (4/4)** ([final-summary](./phase-62-quiet-trust/final-summary.md), [status](./phase-62-quiet-trust/current-phase-status.md)): the privacy-narration prose on cards and notifications replaced by the compact three-state egress badge, the reassurance tails swept out of web/src, every affected screenshot re-shot live (native overlay on real .43 metal), two pre-existing bugs fixed. The strongest remaining backlog rows are the launch moment and M-leftovers (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 61 — Send to Slack: CLOSED (4/4)** ([final-summary](./phase-61-send-to-slack/final-summary.md), [status](./phase-61-send-to-slack/current-phase-status.md)): backlog candidate **L** shipped (scoped by the owner to the Slack incoming webhook; Notion/Docs stay parked), proven live against a real local receiver with the wire body byte-equal to the preview. **N (Windows) stays owner-rejected.** The strongest remaining backlog rows are the launch moment and M-leftovers (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 60 — The Wake Word: CLOSED (6/6)** ([final-summary](./phase-60-wake-word/final-summary.md), [status](./phase-60-wake-word/current-phase-status.md)): backlog candidate **O** shipped with all four safety conditions held, proven live, two latent production crashes fixed along the way. The standing sequence ("K, then O") is complete; the strongest remaining backlog rows are **N** (Windows), **L** (export connectors), and the launch moment (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 59 — Speak Your Language: CLOSED (4/4)** ([final-summary](./phase-59-languages/final-summary.md)). Prior: **Phase 58 — The Front Door (positioning + the user-facing docs, revised): CLOSED (6/6)** ([final-summary](./phase-58-front-door/final-summary.md), [status](./phase-58-front-door/current-phase-status.md)): backlog candidate **Q** shipped — the positioning canon, the README pitch with named honest comparisons, every guide in one voice, the voice guard. Next up per the agreed sequence: **K — languages + spoken-symbol dictionary** (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 57 — Transcript Import: CLOSED (5/5)** ([final-summary](./phase-57-transcript-import/final-summary.md)); **K — languages** remains queued (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 56 — Qlippy, the Presence Enhancer: CLOSED (7/7)** ([final-summary](./phase-56-qlippy/final-summary.md), [status](./phase-56-qlippy/current-phase-status.md), [RFC](./proposals/qlippy-presence-enhancer.md)): backlog candidate **J** shipped, **G** absorbed-shipped, per the agreed sequence **54 → I → J → K**. Next up per that sequence: **K — languages + spoken-symbol dictionary** (see [BACKLOG](./BACKLOG.md)). Prior: **Phase 55 — Meeting Import ("bring your archive") + faceted history search: CLOSED (6/6)** ([final-summary](./phase-55-meeting-import/final-summary.md)). from [backlog](./BACKLOG.md) candidate **D** (frontend density paydown), promoted to its own phase on user direction (the agreed post-53 sequence **54 → I → J → K**). Decompose the 6,101-line dictation frontend into section partials + behavior modules, behavior-preserving (the existing page-content tests pass unmodified; every tab screenshot-verified), locked by a density guard so it cannot regrow. Six stories (seam → modules → partials → guard → docs → closeout). Read [`AGENT-BRIEF.md`](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md) first. Prior: **Phase 53 — Activity Pre-Briefing: CLOSED (7/7)** ([final-summary](./phase-53-activity-prebriefing/final-summary.md)) from [backlog](./BACKLOG.md) candidate **F** (local activity as pre-briefing fuel), picked by the user as the next phase. HoldSpeak already records source-cited local activity; this turns it into concrete, dismissible pre-briefing nudges on the daily surfaces ("what you touched since your last meeting"; "dictate a reply with this issue as context"), gated by the existing activity privacy toggle, read-only (surfaces and offers, never acts), every nudge cites its source, one action feeds the record into dictation. Six stories (engine -> API -> dictate-with-context -> nudge UI -> docs -> closeout). Read [`AGENT-BRIEF.md`](./phase-53-activity-prebriefing/AGENT-BRIEF.md) first. Prior: **Phase 52 — Voice Command Macros on a carved dispatch seam: CLOSED (7/7)** ([final-summary](./phase-52-voice-macros/final-summary.md)) from [backlog](./BACKLOG.md) candidate **B** (a voice command launcher) with a scoped slice of **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) on a dedicated `/commands` board, and it fires when they speak the keyword while dictating, through the reused actuator guarded execution, on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Off by default and byte-identical when off; the user owns the risk (configuring is consent, no per-fire prompt) and the blast radius is bounded (deterministic match + a per-macro manifest). The board was designed UI-first and screenshot-verified; full suite 2499 passed; PR to `main` merged on green. The dogfood fired a real shell command end to end. **No follow-on required.** Earlier this phase: **HS-52-06 (docs) done:** `docs/VOICE_COMMANDS.md` (linked in the docs index under Dictate) documents the feature honestly in product-tense; the Phase-51 roadmap-vocabulary guard now scans this Phase-52 doc and passes, `humanizer` run, no em/en dashes. **HS-52-05 (the Voice Commands board) done:** the centerpiece. A dedicated `/commands` route with a card-per-command grid (per-kind color edges/badges, a live "what fires" preview per card, the honest shell danger treatment, a per-card Test button via `POST /api/commands/test`, a per-kind adaptive editor with match hint + conflict warning, an inviting empty state). Built UI-first to the design doc and screenshot-verified (4 PNGs; a screenshot pass caught a real `[hidden]`-override CSS bug). **HS-52-04 (dispatch wiring) done:** the feature works end to end (Python side, off by default): `dictation_runner.dispatch_voice_command` sits at the top of the carved seam, matches the whole utterance deterministically, fires the bounded connector and types nothing, surfacing "command: <keyword>" as a runtime activity; `web_runtime` returns early on a fire (byte-identical when off / no match). The non-meeting persistence wrinkle was resolved honestly: the actuator table is meeting-FK'd, so a fire reuses the guarded execution but is audited via the activity broadcast + log, not that table. **HS-52-03 (local action connectors) done:** `plugins/voice_macro_connector.py` reuses the Phase-38 gated-connector framework: `open_url`/`launch_app`/`shell` run bounded subprocesses behind a per-macro manifest (a connector built for one command refuses any other before egress), `type_text` types via an injected writer; `allow_actuators=False` blocks before the connector runs. **HS-52-02 (macro model + config) done:** `VoiceMacro` = keyword + a deterministic action (`open_url` / `launch_app` / `shell` / `type_text` + one payload), with a single-source `preview()` and a normalized whole-utterance `matches()`; `MacrosConfig` (off by default) round-trips config-version-safe through `/api/settings` with a clean 400 on a malformed macro. **HS-52-01 (scoped-E carve) done:** the dictation orchestration carved into `holdspeak/dictation_runner.py`, byte-identical. Full suite 2480 passed. Designed UI-first as a dedicated `/commands` board. from [backlog](./BACKLOG.md) candidate **B** (re-envisioned by the user as a voice command launcher) paired with a scoped slice of candidate **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through the existing actuator guarded executor (Phase 37/38), reused with new local connectors; configured macros are auto-approved (the user owns the risk), off by default, deterministic and bounded (the keyword selects a pre-configured action; the transcriber never composes a command), every fire audited. Landed on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Seven stories (carve -> model+config -> connectors -> dispatch -> editor UI -> docs -> closeout). Read [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md) first. Prior: **Phase 51 — Public-Docs Hygiene: CLOSED (5/5)** ([final-summary](./phase-51-public-docs-hygiene/final-summary.md)) from [backlog](./BACKLOG.md) candidate H, the cheap release-facing follow-on to the Phase-50 gate. The deeper user/operator guides leaked the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README was already clean. All five stories shipped: a classified leak inventory, a scrub of all 6 in-scope guides into product-tense (keeping product nouns `actuator` and named specs `MIR-01`/`DIR-01`), a case-insensitive doc-drift guard scoped to user-facing docs (`README` + non-recursive `docs/*.md`; internal/evidence/assets out), the rule codified in `DOCS_STYLE.md`, and a closeout dogfood proving the guard both ways. Docs-and-test only, behavior-preserving. Full suite green (2454 passed, 17 skipped), PR to `main` merged on green CI. **No follow-on required;** the strongest remaining backlog bet is **B** (voice macros), best paired with a scoped slice of **E** (the routing seam) under one thesis. Prior: **Phase 50 — Release Readiness ("cut a real 0.x"): CLOSED (7/7)** from [backlog](./BACKLOG.md) candidate C (the open-source-ship unlock). HS-50-01 (one true version), HS-50-02 (the safe-by-default schema policy, the heart of the phase), HS-50-03 (backup + restore), and HS-50-04 (doctor + config honesty) are done: `_ensure_schema` does create-fresh / no-op-equal / backup-then-apply-older / refuse-newer, `backup_database` takes a consistent SQLite snapshot before any destructive action, `holdspeak backup` / `holdspeak restore` give the user an on-demand snapshot and a safe reversal, `doctor` (and `/api/setup/status`) now report the real schema state honestly (newer DB flagged FAIL), and `Config` carries a `config_version` that coerces an older shape forward without dropping fields and flags a newer one. (The live schema is additive today, so the real holes closed are a newer DB run by an older build and the lack of a backup before any future migration.) HS-50-05 (verified clean-machine install) is done: a fresh-venv `uv pip install -e .` reaches `holdspeak doctor` exit 0 with only the expected optional warnings, version 0.2.1 resolved from real install metadata. All seven stories are done: one true version, the safe-by-default schema matrix, backup + restore, an honest doctor + config_version, a verified clean install, the upgrade/backup policy doc, and a green dogfood proving the matrix end to end. HoldSpeak is now safe to install, upgrade, and trust (the PyPI publish is a deliberate maintainer step once the gate is green). Make HoldSpeak safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (never silently destroy data on upgrade — refuse-newer, backup-then-apply-older, no-op-equal, create-fresh), a backup the user can run, a `doctor` honest about the DB/config it found, config that can evolve, a verified clean-machine install, and honest docs with an upgrade/backup policy. The next schema bump currently wipes an existing user's DB (`db/core.py:_ensure_schema`) with no backup — that is the bet. Behavior-preserving (no feature/capture/dictation/plugin/synthesis change). Read [`AGENT-BRIEF.md`](./phase-50-release-readiness/AGENT-BRIEF.md) first; HS-50-02 (the safe schema policy) is the heart, HS-50-01 (one true version) the cheapest first win. Prior: Phase 49 (Meeting Aftercare) CLOSED, PR #33.

**Previous phase:** **Phase 49 — Meeting Aftercare ("close the loop"): CLOSED (6/6)**, from [backlog](./BACKLOG.md) candidate A. The meeting result now closes its own loops, read-only + reuse, never acting without approval. The "Your next move" aftercare panel at `/history` shows what's still open (by owner), what was decided, and what changed since the last meeting (`compute_meeting_aftercare` + `GET /api/meetings/{id}/aftercare`, quiet when nothing's open/decided/changed); "show me the moment" jumps to the transcript segment that justifies a result (only on a real timestamp, focus-safe); a "File as issue" affordance turns an accepted action into a GitHub-issue actuator *proposal* through the existing propose -> approve -> execute flow (no new write primitive, off by default, human-approved, audited; an executor test proves an unapproved proposal never egresses); and "Draft follow-up" assembles a local markdown summary, preview + copy only, never sent. Capture/plugins/synthesis untouched; actuators off by default. Closed with before/after, a green dogfood, full suite green (2426/17), and a PR to `main` merged on green. See [final-summary](./phase-49-meeting-aftercare/final-summary.md). Prior: Phase 48 (Visible Learning Loop) CLOSED, PR #30.
**Status:** Plugin rollout complete (Phases 16 → 27 → 28 → 29; 14 real plugins). Remaining open work is largely hardware-gated (Phases 24, 25 HS-25-07).

**Backlog (candidate next phases):** [`BACKLOG.md`](./BACKLOG.md) — the living parking lot so post-Phase-48 ideas (meeting aftercare, voice macros, release-readiness, frontend density, activity pre-briefing, decision-point privacy) are not lost. Shipped as focused phases in sequence, not one mega-phase.

## Vision

HoldSpeak is a local, private, hold-to-talk voice typing tool for macOS
and Linux. The current product is a working voice typer with a web
runtime, meeting mode, transcription via MLX-Whisper / faster-whisper,
deferred meeting intelligence, and an emerging intelligent-typing layer
for project-aware local writing.

This roadmap exists because the project is graduating from "useful
script" to a real product. The current chapter is **local intelligent
typing**: utterances can flow through ordered, user-configurable stages
between Whisper and the keyboard, optionally using target-profile
detection, project context, agent hooks, and OpenAI-compatible local or
LAN LLM endpoints before text is injected.

## Source canon

Phase content must be grounded in these. If a phase disagrees with
canon, canon wins.

- `README.md` — public install + usage surface.
- `docs/SECURITY.md` — security & privacy posture: data classes, trust boundaries, egress points, encryption-at-rest stance (HS-25-03).
- `docs/USER_GUIDE.md` — user-facing product workflows.
- `docs/internal/PLAN_ARCHITECT_PLUGIN_SYSTEM.md` — parent RFC for the plugin system.
- `docs/internal/PLAN_PHASE_MULTI_INTENT_ROUTING.md` — sibling phase: meeting-side multi-intent routing (MIR-01).
- `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md` — DIR-01 spec for the dictation pipeline.
- `docs/internal/PLAN_PHASE_WEB_FLAGSHIP_RUNTIME.md` — web-first runtime migration.
- `docs/internal/PLAN_ACTIVITY_ASSISTED_ENRICHMENT.md` — local activity assisted enrichment design.
- `aipi-lite/` — first-class AIPI-Lite firmware, bridge, docs, tests, and imported device-side roadmap.
- `pyproject.toml` — package contract; the source of truth for installable extras.

## Phase index

| Phase | Goal (one line) | Status | Folder |
|---|---|---|---|
| 0 | Project + roadmap setup; packaging hardening | done | [phase-0-setup](./phase-0-setup/) |
| 1 | DIR-01: Dictation intent routing + transcript enrichment pipeline | done | [phase-1-dictation-intent-routing](./phase-1-dictation-intent-routing/) |
| 2 | MIR-01: Meeting-side multi-intent routing + artifact synthesis | done | [phase-2-multi-intent-routing](./phase-2-multi-intent-routing/) |
| 3 | DIR-01 loop closure: project context, llama_cpp leg, runtime counters, cold-start cap | done | [phase-3-dictation-loop-closure](./phase-3-dictation-loop-closure/) |
| 4 | WFS-01 extended: audit web flagship + interactive config for blocks / project KB / dictation runtime / dry-run preview | done | [phase-4-web-flagship-runtime](./phase-4-web-flagship-runtime/) |
| 5 | Usability powerhouse: reduce setup/dogfood friction and make web workflows faster | done | [phase-5-usability-powerhouse](./phase-5-usability-powerhouse/) |
| 6 | Action follow-through: make meeting action items and artifacts reviewable and traceable | done | [phase-6-action-follow-through](./phase-6-action-follow-through/) |
| 7 | Local handoff exports: make reviewed meeting work portable as local Markdown/JSON outputs | done | [phase-7-local-handoff-exports](./phase-7-local-handoff-exports/) |
| 8 | Local activity intelligence: mine Safari/Firefox history metadata into a private default-on work-context ledger | done | [phase-8-local-activity-intelligence](./phase-8-local-activity-intelligence/) |
| 9 | Assisted activity enrichment: add opt-in local connectors, annotations, and meeting candidates | done | [phase-9-assisted-activity-enrichment](./phase-9-assisted-activity-enrichment/) |
| 10 | Web design system & character pass: tokens, components, identity, route rebuilds, refreshed designer handoff | done | [phase-10-web-design-system](./phase-10-web-design-system/) |
| 11 | Local connector ecosystem: reusable manifests, fixtures, first-party connector packs, and developer docs | done | [phase-11-local-connector-ecosystem](./phase-11-local-connector-ecosystem/) |
| 12 | Workbench voice: replatform tokens + components on a Workbench-evoking palette + pixel font; dashboard polish; designer handoff refresh | done | [phase-12-workbench-voice](./phase-12-workbench-voice/) |
| 13 | Connector runtime + pipelines + meeting context: turn the framework on, chain packs, surface pre-meeting briefings + cross-meeting summaries | done | [phase-13-connector-runtime-and-context](./phase-13-connector-runtime-and-context/) |
| 14 | AIPI-Lite devices: remote audio ingest substrate — `AudioSource` Protocol, `RemoteAudioRecorder`, `/api/devices/audio` WebSocket, per-device speaker labels in transcripts; same-LAN scope (cross-network is phase 15) | done | [phase-14-aipi-lite-devices](./phase-14-aipi-lite-devices/) |
| 15 | Out-and-about: cross-network reach for AIPI-Lite — tunneling (Tailscale / Cloudflare Tunnel / WireGuard candidate evaluation), TLS, per-device PSKs, paired with the AIPI-Lite firmware's portable WiFi (multi-SSID + captive portal + Improv-WiFi) on the device-side roadmap | not-started | [phase-15-out-and-about](./phase-15-out-and-about/) |
| 16 | First real synthesizer: replace `mermaid_architecture`'s `DeterministicPlugin` stub with a real LLM-backed plugin, wire the LLM capability gate, render `mermaid` artifacts as inline SVG in the web view, reality-check the plugin RFC | done | [phase-16-first-real-plugin](./phase-16-first-real-plugin/) |
| 17 | Device Initiative: device → server upstream frames (`device_health` + `query` w/ `last_segment` case) lighting up AIPI-Lite phase 4's `blocked` bridge stories; minimal web UI rendering for device health. Sibling to HS-14, paired with AIPI-4 in the AIPI-Lite roadmap. | done | [phase-17-device-initiative](./phase-17-device-initiative/) |
| 18 | Intelligent Typing Copilot: project-aware local intelligent typing with target profiles, Claude/Codex hooks, optional external-agent summarization, `.hs` context conventions, OpenAI-compatible runtimes, and web cockpit support | done | [phase-18-intelligent-typing-copilot](./phase-18-intelligent-typing-copilot/) |
| 19 | Intelligent Typing Daily-Use Hardening: safe `.hs/.../*.md` project-doc suggestions, telemetry, target-profile overrides, and real endpoint dogfooding | done | [phase-19-intelligent-typing-hardening](./phase-19-intelligent-typing-hardening/) |
| 20 | AIPI Companion: same-LAN physical companion UX for agent-waiting status, voice replies, gestures, and debug visibility | done | [phase-20-aipi-companion](./phase-20-aipi-companion/) |
| 21 | AIPI-Lite First-Class Integration: import firmware and bridge source into HoldSpeak and define the unified developer workflow | done | [phase-21-aipi-lite-first-class](./phase-21-aipi-lite-first-class/) |
| 22 | AI PI Companion UX: state model, gestures, LCD cadence, bridge display wiring, and live hardware dogfood | done | [phase-22-ai-pi-companion-ux](./phase-22-ai-pi-companion-ux/) |
| 23 | AI PI Companion UX Polish: long-prompt display, multi-session identity, preview/browse, and target confidence | done | [phase-23-ai-pi-companion-ux-polish](./phase-23-ai-pi-companion-ux-polish/) |
| 24 | AI PI Companion Productization: web companion overview, stale-session controls, public companion docs/artwork, confidence affordances, and display update cadence | in-progress (3/6) | [phase-24-ai-pi-companion-productization](./phase-24-ai-pi-companion-productization/) |
| 25 | Trust & Hardening: no silent cloud egress, web-runtime auth + bind guard, threat-model/encryption-at-rest doc, LLM-runtime thread-safety, transcription timeout, config-knob audit. Prerequisite to Phase 15. | in-progress | [phase-25-trust-and-hardening](./phase-25-trust-and-hardening/) |
| 26 | Web Runtime Decomposition: break the `web_server.py` monolith into route modules + a shared context, behavior-preserving. Fast-follow to Phase 25. | done | [phase-26-web-runtime-decomposition](./phase-26-web-runtime-decomposition/) |
| 27 | Ubiquitous plugins + spoken-meeting e2e: flip the highest-value stub plugins to real (`action_owner_enforcer`, decisions/open-questions, requirements) re-using the Phase-16 pattern, and add a real `say`→Whisper→MIR→plugins→web spoken-meeting end-to-end harness with screenshots. Continuation of Phase 16. | done | [phase-27-ubiquitous-plugins-and-e2e](./phase-27-ubiquitous-plugins-and-e2e/) |
| 28 | Plugin rollout II: round out the core meeting types — a behavior-preserving synthesis renderer-registry refactor, then flip `adr_drafter` (architecture), `milestone_planner` (delivery), and `risk_heatmap` (risk register) to real, each with a structured web render + spoken-e2e coverage. Continuation of Phase 27. | done | [phase-28-plugin-rollout-ii](./phase-28-plugin-rollout-ii/) |
| 29 | Complete the plugin rollout + public docs: flip the last seven stubs (delivery/product, incident, comms) to real → fourteen real plugins / zero stubs, and document the plugin system on the public README. Continuation of Phase 28. | done | [phase-29-plugin-rollout-iii-and-docs](./phase-29-plugin-rollout-iii-and-docs/) |
| 30 | UI/UX overhaul: retire the Amiga Workbench aesthetic and ship "Signal" — a bold, distinctive, dark-first identity (Space Grotesk / Inter / JetBrains Mono, signature orange, real depth + motion) as a full UX/IA redesign of all five web routes, design system derived with the `ui-ux-pro-max` skill. | done | [phase-30-ui-ux-overhaul](./phase-30-ui-ux-overhaul/) |
| 31 | Database decomposition: break the 5,481-line `db.py` monolith into per-domain repositories behind a thin `Database` container, **delete** the `MeetingDatabase` god-class (greenfield), and **squash** the 18-version migration ladder to one canonical schema. Data-layer twin of Phase 26. | done | [phase-31-database-decomposition](./phase-31-database-decomposition/) |
| 32 | Foundation hardening & doc truth: class-ify `web_runtime.py`, invert the meeting→web-server coupling, **retire the TUI + menubar** (web is the sole interactive runtime), converge audio ownership, add an ungated CI core-path smoke test, extract a route error helper, and reconcile stale non-PMO docs. | done (7/7) | [phase-32-foundation-hardening](./phase-32-foundation-hardening/) |
| 33 | Documentation & open-source readiness: de-prescribe/refresh the model framing (current Qwen3.5 + a `MODELS.md` bring-your-own contract), add an **Apache-2.0 LICENSE** + `pyproject` metadata, reorganize `docs/` (user-facing vs internal/historical) with an index, an OSS-grade README pass + CHANGELOG/CONTRIBUTING, and a refreshed pixel-art asset set. | done | [phase-33-documentation-oss-readiness](./phase-33-documentation-oss-readiness/) |
| 34 | Structural decomposition II: sub-split the four files that now dominate the tree (5,373 lines combined) into navigable, single-responsibility packages — `web/routes/dictation.py` + `web/routes/activity.py` into route sub-packages, and `agent_context.py` + `intel.py` into modules — behavior-preserving (import paths + route table held identical). Twin of Phases 26/31/32; discharges Phase 26's deferred finer-split follow-up. | done | [phase-34-structural-decomposition-ii](./phase-34-structural-decomposition-ii/) |
| 35 | Plugin frontier: make the meeting-intel plugin system extensible by others — a public `docs/PLUGIN_AUTHORING.md`, a plugin-pack manifest + discovery loader (mirroring connector packs; first-party + user packs), per-project enable/disable, and the incident/comms spoken-e2e scenario. Behavior-preserving for the 14 built-ins; actuators deferred to Phase 37. Discharges Phase 29's "next frontier" handoff. | done | [phase-35-plugin-frontier](./phase-35-plugin-frontier/) |
| 36 | Meeting intelligence & experience: make HoldSpeak's meeting intelligence deliver on real, messy meetings end-to-end. **Intelligence** — fix the routing weakness where a brief intent in a digression is diluted below threshold and silently lost: segment the conversation + probe each segment for intent ("fish out" intent per segment), LLM-assisted with a lexical fallback. **Experience** — Signal "elevated" artifact cards (retire the basic chips), copy-as-Markdown, overflow-safe wide artifacts. Tied together by a dynamic/messy spoken-e2e with a before/after capture (7 → 13 artifact types). Artifact data shapes unchanged. On direct user feedback. | done | [phase-36-meeting-artifact-experience](./phase-36-meeting-artifact-experience/) |
| 37 | Actuators: turn on the plugin system's third kind — an actuator proposes an external side effect (file a ticket, post a message) behind a **preview → human approval → execute** flow with an audit trail (the RFC's open question #5; intersects the Phase-25 egress posture). The invariant: no external side effect without an explicit, audited, per-action human approval; executed == previewed. | done | [phase-37-actuators](./phase-37-actuators/) |
| 38 | Actuators II: make the proven-safe actuator mechanism *useful* — **real write connectors** (`gh issue create`, webhook POST) behind a per-connector **permission manifest** routed through the connector `PermissionGate`, and **live in-meeting proposals** (broadcast + a live approve/reject panel). The Phase-37 invariant is unchanged; every connector adds a narrower gate. On user direction. | done | [phase-38-actuators-ii](./phase-38-actuators-ii/) |
| 39 | Dictation Copilot Depth: make the daily-driver intelligent-typing loop **deeper and self-improving** — **multi-pass rewriting** (draft → critique → refine), **session correction memory** (the pipeline learns from corrections within a session), **model-assisted target detection** (LLM fallback below the heuristic threshold), a **project-doc suggestion quality gate** (dedup + recurrence suppression + consolidation), and **depth telemetry**. All opt-in; default byte-identical (the DIR-01 invariant). On user direction. | done | [phase-39-dictation-copilot-depth](./phase-39-dictation-copilot-depth/) |
| 40 | Configuration Cockpit & Persistent Memory: make the whole dictation copilot configurable, observable, and **memorable from the Web UI** — surface the Phase-39 knobs in `/api/settings` + a Signal-styled, readiness-driven **setup cockpit** (no hand-edited JSON/YAML), make correction memory **persistent** (DB-backed) with a curate UI, and render depth telemetry richly. UX + persistence; pipeline behavior unchanged. On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-40-config-cockpit/AGENT-BRIEF.md). | done | [phase-40-config-cockpit](./phase-40-config-cockpit/) |
| 41 | Runtime Presence Indicators: an **opt-in** (`HOLDSPEAK_DESKTOP_PRESENCE=1`), per-platform **native** desktop presence layer so a user dictating into another app sees what the copilot is doing — one `runtime_activity` contract → a websocket broadcast → a Signal `/presence` card, hosted natively (macOS non-activating `NSPanel`+`WKWebView` HUD + `NSStatusItem` glyph; Linux libnotify notification + tray + a GTK-WebKit overlay). Focus-safe; off by default; flag-unset byte-identical. Salvages codex PR #17's bones (rejects its Tk renderer). | done | [phase-41-runtime-presence](./phase-41-runtime-presence/) |
| 42 | First-Run Delight & Daily Confidence: make **arrival** stellar — a user goes from fresh clone to a **verified first dictation**, with visible privacy/trust state and **zero file editing**, in one guided local cockpit (TTFD dogfood: launch→/setup 1.13s). A setup-state contract (an adapter over the doctor) + `first_run` milestone, global settings completion, a welcome/setup route + CLI nudge, a guided real-app first-dictation, an ambient trust chip + panel, a runtime model assistant, and presence onboarding. On user direction. | done | [phase-42-first-run-delight](./phase-42-first-run-delight/) |
| 43 | World-Class Onboarding & First-Run UX: reimagine the first run as a full-screen `/welcome` **wizard** (Welcome · live Permissions · Model picker · a celebratory first-dictation reward · a one-click presence toggle · Done), make desktop presence a **config-backed UI toggle** (kill the env var as the path; live start/stop), and **redesign Settings** (sectioned + searchable + progressive). A world-class UX layer on the Phase-42 plumbing. On user feedback. | done | [phase-43-world-class-onboarding](./phase-43-world-class-onboarding/) |
| 44 | Daily-Surface Polish: carry the Phase-43 wizard's bar to the three daily-driver surfaces (dashboard `/`, dictation cockpit `/dictation`, history `/history`), behavior-preserving — one shared premium language (ambient glow, hero grammar, contained pill navs with a solid-accent active tab, elevated rounded surfaces, reduced-motion-safe depth). Per-surface page-content tests + a live before/after. On user direction. | done | [phase-44-daily-surface-polish](./phase-44-daily-surface-polish/) |
| 45 | Dictation Memory & the Moment of Truth: give the daily-driver dictation loop the memory the meeting side already has — a persistent, private, reviewable **dictation journal** (said → routed → typed → latency), an **in-the-moment "was that right? fix it here"** correction loop that teaches without leaving your flow, and **replay** (re-run a past utterance through the tuned pipeline so "it learned" becomes tangible). Local-first; journaling is a side-channel (typed output byte-identical); focus-safe; provable remotely via dry-run. On user direction. | done | [phase-45-dictation-memory](./phase-45-dictation-memory/) |
| 46 | Documentation Excellence & the 10-Second Hook: the product outgrew its docs (13 phases since the Phase-33 doc pass). Give the documentation the lift the product got — a **doc truth audit** (claims vs live code) → a **bold README reimagining** (10-second hook + cool-facts strip, every graphic kept, repetition cut, depth linked out, materially shorter) → a **docs voice & structure system** + an elevated journey-grouped index → a **visual lift** (real UI screenshots via the Playwright scripts) → a **coverage** sweep (feature → doc matrix; nothing shipped is undiscoverable; nothing overstated) → closeout. Honest not hype; graphics stay; docs-only. On user direction (README too low on cool facts, too large/repetitive; graphics stay; other docs need a lift). | done | [phase-46-documentation-lift](./phase-46-documentation-lift/) |
| 47 | Project Knowledge: Legible & Inviting: make the "teach the copilot about this project" capability understandable. Today `/dictation` shows two adjacent jargon-named tabs — **"Project KB"** (the `.holdspeak/project.yaml` `kb:` map → `{project.kb.*}` placeholders, default `kb-enricher` stage) and **"Project Context"** (the *separate* `.hs/` Markdown files, optional `project-rewriter` LLM stage) — with no explanation of what either is or how they relate (the Phase-46 docs pass even conflated them). Settle the concept + names → in-app explainers + inviting empty states → a guided setup flow (fresh repo → working, no file editing) → a dismissible discovery nudge → docs alignment → closeout. Behavior-preserving (presentation/onboarding, not a pipeline redesign); UX bar via `ui-ux-pro-max`. On user direction ("I struggle to understand the project KB; it's UI/UX too"). | CLOSED ✅ (6/6) | [phase-47-project-kb-legibility](./phase-47-project-kb-legibility/) |
| 48 | The Visible Learning Loop ("What HoldSpeak learned"): HoldSpeak already records every dictation (the journal), learns from corrections (the Jaccard correction memory), and can replay to prove it improved — but the learning is buried in two raw tabs. Make the loop **visible** (a windowed "What HoldSpeak learned" digest with honest counts), **trustworthy** (inline "learned from N similar" signals from the same matcher that nudges routing, quiet at N=0), and a **normal ritual** (one-tap right/wrong on a result, reusing the existing correct path) → docs telling the loop end to end as the open-source differentiator → closeout. Behavior-preserving (read-only aggregation; correcting reuses the existing write path); honesty over hype; focus-safe. On user direction ("I like the 'what I learned this week' view; let's make this open-source thing happen"). | CLOSED (5/5) | [phase-48-visible-learning-loop](./phase-48-visible-learning-loop/) |
| 49 | Meeting Aftercare ("close the loop"): the meeting side has 14 plugins, artifacts, proposals, and action review, but the result is display, not follow-through. Surface what is still open / decided / changed since last meeting, jump to the transcript moment that justifies a result, turn accepted actions into issues through the existing human-approved actuator flow, and draft a copyable follow-up. Read-only aggregation + reuse of the actuator system; never acts without approval. | CLOSED (6/6) | [phase-49-meeting-aftercare](./phase-49-meeting-aftercare/) |
| 50 | Release Readiness ("cut a real 0.x"): 49 phases of features, never cut as a real release. The DB is SCHEMA_VERSION=1 and the next bump silently wipes a user's data; the in-code version disagrees with the package version; doctor never checks the DB; the install pins git HEAD. Make it safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (refuse-newer / backup-then-apply-older / no-op-equal / create-fresh), a backup before upgrade, an honest doctor + config_version, a verified clean install, and an upgrade/backup policy doc. The open-source-ship unlock. | **CLOSED (7/7)** | [phase-50-release-readiness](./phase-50-release-readiness/) |
| 51 | Public-Docs Hygiene: Phase 50 cut the release gate, but the public surface still reads like an internal artifact. User/operator-facing guides leak the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README is already clean. Strip that vocabulary from the deeper guides, rewrite phase-relative claims ("Phase 11 shipped X") into product-tense ("HoldSpeak supports X") so the meaning survives for a reader who never heard of a phase, keep legitimate product nouns (`actuator`) and named specs (`MIR-01`/`DIR-01`), and lock it with a doc-drift guard + a codified `DOCS_STYLE.md` rule so it cannot regress. Docs-and-test only, behavior-preserving. A cheap, release-facing follow-on. Net-new from a between-phases conversation ([backlog](./BACKLOG.md) candidate H). On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-51-public-docs-hygiene/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-51-public-docs-hygiene](./phase-51-public-docs-hygiene/) |
| 52 | Voice Command Macros on a carved dispatch seam: turn the dictation hotkey into a **voice command launcher**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through HoldSpeak's existing **actuator guarded executor** (Phase 37/38), reused and extended with new local connectors; a configured macro is auto-approved (the user owns the risk: configuring is consent), off by default, deterministic (the keyword selects which pre-configured action fires, the transcriber never composes a command), each macro bounded by its own permission manifest, every fire audited. Landed on a **dispatch seam carved out of the 2,341-line `web_runtime` god-object** (a scoped slice of backlog **E**) so a feature (backlog **B**) ships with a motivated refactor under one thesis. On user direction (re-envisioned from text macros to a command launcher; "B=E"). Scaffolded + [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-52-voice-macros](./phase-52-voice-macros/) |
| 53 | Activity Pre-Briefing: HoldSpeak already watches local activity (browser history + GitHub/Jira/calendar enrichment) but it only lives on the `/activity` ledger you go and inspect. Turn it into concrete, **source-cited, dismissible pre-briefing nudges** on the daily surfaces: "here is what you touched since your last meeting"; "you were looking at `owner/repo#123`, dictate a reply with it as context?". A small reader over the records that already exist (no new watcher), gated by the existing activity privacy toggle, read-only (it surfaces and offers, never acts), every nudge cites where it came from, and one action feeds the selected record into the dictation pipeline. On the daily-dictation north star. On user direction (picked backlog candidate **F**). Scaffolded + [`AGENT-BRIEF.md`](./phase-53-activity-prebriefing/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-53-activity-prebriefing](./phase-53-activity-prebriefing/) |
| 54 | Dictation Frontend Decomposition: pay down the cockpit's 6,101 coupled lines (`dictation.astro` 3,134 + `dictation-app.js` 2,967, grown five phases running) into section partials + single-concern behavior modules, **behavior-preserving** (existing page-content tests pass unmodified; every tab screenshot-verified before/after), locked by a density guard so it cannot silently regrow. Defines the codebase's frontend decomposition pattern (`history.astro` / `index.astro` are follow-up candidates). Frontend twin of Phases 26/31/32/34. From [backlog](./BACKLOG.md) candidate **D**, promoted from "ride along" to its own phase. Scaffolded + [`AGENT-BRIEF.md`](./phase-54-dictation-frontend-decomposition/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-54-dictation-frontend-decomposition](./phase-54-dictation-frontend-decomposition/) |
| 55 | Meeting Import ("bring your archive") + faceted history search: meeting intelligence is live-capture-only — productize the proven file→transcribe→meeting seam into an **import engine** (WAV native + ffmpeg-assisted compressed, ~30 s windowed transcription with real segment timestamps, the same deferred-intel enqueue as live), `POST /api/meetings/import` on a background thread + `holdspeak import`, an inviting `/history` upload affordance, and **server-side facets** (date range / speaker / tag / open-actions) composing with full-text search. Honest v1: one speaker label (no single-file diarization), audio not retained, ffmpeg stated as the compressed-format dependency. An imported meeting is a real meeting — one pipeline, indistinguishable downstream. From [backlog](./BACKLOG.md) candidate **I**, per the agreed sequence. Scaffolded + [`AGENT-BRIEF.md`](./phase-55-meeting-import/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-55-meeting-import](./phase-55-meeting-import/) |
| 56 | Qlippy, the Presence Enhancer: give the presence layer a **face** — an ambient mascot dock reflecting `runtime_activity` (the PixelLab pack: 14 sprite strips, body-emotes + composited glyphs), and a sliding, focus-safe, **one-at-a-time card** that makes the actuator-approval and learning moments actionable in place. Approve/Decline are a faster front door to the **same audited decision route** (the actuator invariant untouched); `learning_event` fires only with real reach; `aftercare_ready` only when non-empty. **Absorbs backlog G**: every actionable card answers what data is used / does anything leave this machine / what control do I have. Opt-in twice (`presence.enabled` AND `presence.mascot`, both default off); flag-unset byte-identical; the native macOS HUD grows to host the card with clickable, non-activating buttons. From [backlog](./BACKLOG.md) candidate **J** + the [RFC](./proposals/qlippy-presence-enhancer.md). Scaffolded + [`AGENT-BRIEF.md`](./phase-56-qlippy/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-56-qlippy](./phase-56-qlippy/) |
| 57 | Transcript Import ("bring your archive", part 2): most meeting tools export a transcript, not audio — let a user upload `.vtt`/`.srt`/`.txt` (web + CLI) and get a **real meeting** through the exact Phase-55 pipeline (factored persistence tail, same intel enqueue, same /history lifecycle), with **real cue timestamps and multi-speaker labels** when the file carries them (VTT voice tags / `Name:` prefixes) and honest synthetic ordering when it does not. The recording upload stays untouched (explicit user constraint). Net-new on user direction ([backlog](./BACKLOG.md) candidate **P**). Scaffolded + [`AGENT-BRIEF.md`](./phase-57-transcript-import/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-57-transcript-import](./phase-57-transcript-import/) |
| 58 | The Front Door: positioning + the user-facing docs, revised. Decide what HoldSpeak's story IS (a positioning canon: "one copilot, two modes", pitched to developers, with named honest comparisons — the user's explicit decisions) and revise the whole user-facing corpus to tell it: README as the pitch, why-ledes + canonical names + the humanizer voice across every guide, the em-dash cleanup the older corpus never had, and a drift guard so it cannot regress. The pitch stays as honest as the product. Net-new on user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-58-front-door/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-58-front-door](./phase-58-front-door/) |
| 59 | Speak Your Language: Whisper speaks ~99 languages and HoldSpeak exposes none of it — a `model.language` knob ("auto" default, byte-identical: today neither backend is ever passed a language) pinning transcription for dictation, meetings, AND import through the one shared Transcriber, plus **the spoken-symbol dictionary**: user-defined spoken→symbol entries ("tilde" → `~`, "arrow" → `→`) with the built-ins' attach semantics, merged user-wins over the hardcoded punctuation table. One thesis: the input layer adapts to you. Proven on real non-English speech at closeout. From [backlog](./BACKLOG.md) candidate **K** on user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-59-languages/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-59-languages](./phase-59-languages/) |
| 60 | The Wake Word: hands-free entry without betraying the trust posture — say the phrase, HoldSpeak **arms** visibly for a bounded window, the next sentence runs the normal dictation pipeline, and by default the result is **previewed, never typed** (direct typing is an explicit opt-in, the voice-commands consent model). openWakeWord (Apache-2.0, local ONNX; Porcupine ruled out on licensing) with a **measured** false-accept posture (the scaffold spike: wake 0.861 vs adversarial distractors ≤0.063 on TTS speech); the armed state unmissable via presence/Qlippy/cockpit; off by default and byte-identical when off; the only network moment is the explicit ~7 MB model download. From [backlog](./BACKLOG.md) candidate **O** on standing user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-60-wake-word/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-60-wake-word](./phase-60-wake-word/) |
| 61 | Send to Slack: the meeting loop closes locally (digest, follow-up draft) but the results only leave by clipboard — one outbound door through a Slack **incoming webhook** (one POST, no OAuth), riding the existing propose→approve→execute actuator flow on the Phase-38 host-gated webhook connector. Every send is a proposal whose **preview is the exact message**; the webhook URL is a credential (never in previews/broadcasts/responses); unconfigured is byte-identical (no buttons, 400). From [backlog](./BACKLOG.md) candidate **L**, scoped by the owner to one easy connector; **N (Windows) owner-rejected** in the same direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-61-send-to-slack/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-61-send-to-slack](./phase-61-send-to-slack/) |
| 62 | Quiet Trust: trust should be ambient, not narrated — the UI's privacy-reassurance prose ("nothing leaves your machine", on every Qlippy card and notification) replaced by one compact three-state **egress badge** (local · local+cloud · cloud, glyph + a word; the cloud state keeps the target label), a sweep of every reassurance tail in web/src (behavioral warnings stay), docs aligned, and every user-facing screenshot showing the old copy re-shot live. Opened on direct owner feedback; reverses the locked Phase-56 three-answers-verbatim card pattern. Scaffolded + [`AGENT-BRIEF.md`](./phase-62-quiet-trust/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-62-quiet-trust](./phase-62-quiet-trust/) |
| 63 | Backend Decomposition: the twin of Phase 54 — `web_runtime.py` (2,635 lines, regrown past its pre-Phase-52 size) and `meeting_session.py` (1,674) carve into single-concern mixin modules under `holdspeak/runtime/` and `holdspeak/meeting/`, behavior-preserving (verbatim moves; assertions byte-identical; the only test edits are monkeypatch target paths, censused at scaffold), locked by a backend density guard, proven by a live boot closeout. From [backlog](./BACKLOG.md) row **E** on owner direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-63-backend-decomposition/AGENT-BRIEF.md). | **CLOSED (6/6)** | [phase-63-backend-decomposition](./phase-63-backend-decomposition/) |
| 64 | Docs Catch-Up: discovery, not coverage — the deep guides are current but the front door sells a Phase-55 product (zero README mentions of the wake word or Send to Slack; the index and Getting Started point at nothing newer). The two-modes tour absorbs the P59–P62 surface, the entry points gain pointers, one humanizer-grade coherence read smooths the solo-written seams, the locks stay green. Docs-only. Scaffolded + [`AGENT-BRIEF.md`](./phase-64-docs-catch-up/AGENT-BRIEF.md). | **CLOSED (3/3)** | [phase-64-docs-catch-up](./phase-64-docs-catch-up/) |
| 65 | The Launch: v0.3.0 reaches PyPI for real — the all-routes zero-page-error pre-flight, the release cut (version + a P51–P64 changelog + fresh-venv install proofs on macOS and .43), the announcement kit in canon voice (the owner posts), then tag → trusted publishing → verified from-PyPI install. The tag is the publish; cut only at closeout from the merged main. Scaffolded + [`AGENT-BRIEF.md`](./phase-65-the-launch/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-65-the-launch](./phase-65-the-launch/) |
| 66 | The Architecture Map: the contributor on-ramp the repo never had — `docs/ARCHITECTURE.md` with a system component map, the dictation and meeting pipelines, and the trust/egress boundary, all in Mermaid that renders on GitHub and is kept rendering by an mmdc-backed guard, every diagram traced against the shipped (post-decomposition) code. Verified gap: no system-overview doc, three Mermaid blocks in the whole corpus. Docs-only. Scaffolded + [`AGENT-BRIEF.md`](./phase-66-architecture-map/AGENT-BRIEF.md). | **CLOSED (4/4)** | [phase-66-architecture-map](./phase-66-architecture-map/) |
| 67 | The Dogfood: a real, repeatable end-to-end exercise of the whole product on believable data — a `dogfood/` harness (3 mock repos with `.hs/` context + KB + completed-stage history, a `say`-pipeline scenario generator for meetings/dictation across voices, committed transcript fixtures, an isolated-`HOME` runner wired to `.43`) and a two-tier fillable `PROTOCOL.md` (plumbing + real-metal) covering every surface, plus an opt-in plumbing pytest. Scaffolded + [`AGENT-BRIEF.md`](./phase-67-the-dogfood/AGENT-BRIEF.md). | scaffolded | [phase-67-the-dogfood](./phase-67-the-dogfood/) |

(Status values: `planning`, `in-progress`, `done`, `paused`, `cancelled`.)

Phase 15 (cross-network reach) is gated on Phase 25 landing the web-runtime
auth + bind guard.

## Operating cadence

Per `pm/roadmap/roadmap-builder.md` §3, every shipping commit updates,
in the same commit:

1. The story file header (status flip).
2. The phase's `current-phase-status.md` story-status row + "Where we are".
3. This README's "Last updated" line.
4. Any project-canon doc touched by the story.

Per `pm/roadmap/PMO-CONTRACT.md`: the pre-commit hook gates every
commit on a fresh `.tmp/CONTRACT.md`.

## Project metadata

- **Slug:** `holdspeak`
- **Story ID prefix:** `HS` (e.g. `HS-0-01`, `HS-1-04`)
- **Greenfield?:** effectively yes — **pre-release** (HS-33-04): the `0.2.x` marker in `pyproject` is in-development, **not** a published tag, and HoldSpeak is not on PyPI. `scripts/install.sh` is the from-source install path. No committed external API surface beyond CLI flags + the `holdspeak` shell command; the standing posture is greenfield/aggressive (design freely — see memory `feedback_holdspeak_not_really_released`).

## Glossary

- **DIR-01** — Dictation Intent Routing, phase 1 of this roadmap. See `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md`.
- **MIR-01** — Multi-Intent Routing for meetings. Sibling phase, separate runtime, shares plugin contracts only.
- **Block** — a user-defined intent class with match examples and an injection template (DIR-01 §8).
- **Transducer** — DIR-01's new plugin kind: transcript-in, transcript-out stage.
