# HoldSpeak — Roadmap

> **New here? Read [`HANDOVER.md`](./HANDOVER.md) first** — current state, where to
> pick up, and the repo conventions that bite (PMO commit gate, write-once
> evidence, no `Co-Authored-By`, metal-test exclusion).

**Last updated:** 2026-06-08 (**Phase 52 — Voice Command Macros on a carved dispatch seam: CLOSED (7/7)** from [backlog](./BACKLOG.md) candidate B (a voice command launcher) + a scoped slice of E. **HS-52-07 (closeout) done:** the dogfood (`dogfood.py` + `dogfood-transcript.txt`, RESULT: PASS) proves the feature both ways (off-by-default byte-identical, a configured shell macro fires a real `echo` into a temp file, type_text types via the writer); full suite green (2499 passed, 17 skipped), `npm run build` clean, 0 `_built/` tracked, `final-summary.md` written, BACKLOG candidate B flipped to shipped (E slice noted), PR to `main` merged on green. **HS-52-06 (docs) done:** the Voice Commands user guide. `docs/VOICE_COMMANDS.md` (linked under Dictate in the docs index) explains the action kinds, the deterministic match, the you-own-the-risk model, and the off-by-default switch honestly and in product-tense. The Phase-51 roadmap-vocabulary guard now scans this Phase-52 doc and passes; `humanizer` run (one tailing-negation fixed); no em/en dashes; doc guards green (75 passed). **HS-52-05 (the Voice Commands board) done:** the centerpiece surface. A dedicated `/commands` route (`web/src/pages/commands.astro` + `commands-app.js`, nav entry under Configure): a card-per-command grid with per-kind color edges/badges, a live `VoiceMacroAction.preview()` line per card, the honest shell danger treatment (red-framed command + "⚠ runs code"), a per-card Test button (`POST /api/commands/test`, egress runs on the host), a per-kind adaptive editor (match hint + conflict warning), and an inviting empty state with starters. Built UI-first to `design-voice-commands-board.md` and screenshot-verified (4 PNGs in the phase `screenshots/`; a screenshot pass caught a real `[hidden]`-override CSS bug, exactly why the rule is screenshot-verify). Full suite 2499 passed (+4); 0 `_built/` tracked. **HS-52-04 (dispatch wiring) done:** the feature works end to end on the Python side (off by default). `dictation_runner.dispatch_voice_command` is the dispatch decision at the top of the carved seam: deterministic whole-utterance match, fires the bounded connector and types nothing, surfaces "command: <keyword>" as a runtime activity; `web_runtime._transcribe_and_type` returns early on a fire and is inert (byte-identical) when off or on no match. Resolved the non-meeting persistence wrinkle: the actuator table is meeting-FK'd (`actuator_proposals.meeting_id REFERENCES meetings(id)`), so a voice fire reuses the guarded execution (connector + gate + bounded manifest) but is audited via the activity broadcast + log, not that table (a persistent fire-log deferred). Full suite 2495 passed (+7). **HS-52-03 (local action connectors) done:** `plugins/voice_macro_connector.py` reuses the Phase-38 gated-connector framework (no framework change): `open_url`/`launch_app`/`shell` run bounded subprocesses through `build_gated_connector` + a per-macro `WriteConnectorManifest`, `type_text` is a plain local connector. Safety proven: a connector built for one command refuses a different one before any side effect (bounded blast radius), and the capability off (`allow_actuators=False`) blocks before the connector runs. Full suite 2488 passed (+8). **HS-52-02 (macro model + config) done:** `VoiceMacro` (keyword + a deterministic action: `open_url` / `launch_app` / `shell` / `type_text` + one payload) with a single-source `preview()` and a normalized whole-utterance `matches()`; `MacrosConfig` (off by default) nested on `DictationConfig`, round-tripping config-version-safe through `/api/settings` with a clean 400 on a malformed macro. Designed UI-first so the schema is exactly what the `/commands` board shows. Full suite 2480 passed (+20 new tests). **HS-52-01 (scoped-E carve) done:** the dictation orchestration carved out of the 2,341-line `web_runtime.py` (now 2,255) into a standalone unit-testable `holdspeak/dictation_runner.py`; the old inline method is a thin delegate. Byte-identical: full suite green (2460 passed, 17 skipped; +6 is the new unit tests). Named `dictation_runner` to stay distinct from the DIR-01 LLM runtime. Phase re-scaffolded after a vision correction and designed UI-first (the dedicated `/commands` board). The user re-envisioned candidate **B** from deterministic text-macros into a voice command launcher: a spoken keyword fires a real system action (open URL, launch app, run shell command, type snippet), run through the reused actuator guarded executor (Phase 37/38) with new local connectors, auto-approved (the user owns the risk), off by default, deterministic and bounded, audited. Paired with a scoped slice of **E** (carve the dispatch seam out of the 2,341-line `web_runtime`). Seven stories (carve -> model+config -> connectors -> dispatch -> editor UI -> docs -> closeout); AGENT-BRIEF + current-phase-status rebuilt, actuator + dictation seams verified against the live tree. Prior close: **Phase 51 — Public-Docs Hygiene: CLOSED (5/5)** from [backlog](./BACKLOG.md) candidate H, the cheap release-facing follow-on to Phase 50. **HS-51-05 (closeout) done:** the dogfood (`dogfood.sh` + `dogfood-transcript.txt`, RESULT: PASS) proves the guard end to end (green on clean tree, in-scope grep empty, red on a planted "phase 99", green again after revert with the tree clean); full suite green (2454 passed, 17 skipped; +3 over Phase 50 is the new guard tests), 0 `_built/` tracked, `final-summary.md` written, BACKLOG candidate H flipped to shipped, PR to `main` merged on green. **HS-51-04 (DOCS_STYLE rule) done:** a new "Product-tense, not roadmap vocabulary (guard-enforced)" section in `docs/internal/DOCS_STYLE.md` states the banned set, the kept set (`MIR-01`/`DIR-01`/`WFS-01`), the exempt corpus, and the guard that enforces it, with no daylight from HS-51-03; `humanizer` run over it; its example tokens (`Phase 14`, `HS-17-05`) are internal so the guard ignores them (file still 8 passed). **HS-51-03 (guard) done:** three case-insensitive tests in `tests/unit/test_doc_drift_guard.py` lock the clean state (a scanning guard over README + non-recursive `docs/*.md` with internal/evidence/assets out, a non-vacuity sanity test, and a pattern-narrowness test proving `MIR-01`/`DIR-01` never trip); proven red on a planted lowercase "phase 99" then green after revert, file -> 8 passed. **HS-51-02 (scrub) done:** all 6 in-scope user guides (`DEVICE_PROTOCOL`, `CONNECTOR_DEVELOPMENT`, `SECURITY`, `RELEASING`, `INTELLIGENT_TYPING_GUIDE`, `PLUGIN_AUTHORING`) plus the optional asset readme are rewritten into product-tense; the in-scope leak grep (case-insensitive) is empty, doc guards green (75 passed), `MIR-01`/`DIR-01` intact, `humanizer` run over all 15 rewrites (no AI tells). A case-insensitive re-grep before scrubbing caught 5 lowercase-only leaks the inventory grep missed, so the guard must be case-insensitive. **HS-51-01 (leak inventory) done:** `leak-inventory.md` maps 20 banned roadmap-vocabulary hits across 6 in-scope user guides, each with a product-tense rewrite, plus the fixed scope (IN = README + `docs/*.md`; OUT = internal/evidence/assets/PMO corpus) and the guard pattern + allowlist; the scan found more than the scaffold-time list (`SECURITY.md`, `PLUGIN_AUTHORING.md`, an asset readme also leak; the root README is clean). Scaffolded on user direction, a between-phases mini-phase. Now that Phase 50 cut the release gate, the deeper user/operator guides still read like an internal artifact (they leak `Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap"); the root README is already clean. The phase strips that roadmap vocabulary, rewrites phase-relative claims into product-tense, and locks the clean state with a doc-drift guard (scoped to user-facing docs, never the internal corpus) plus a codified `DOCS_STYLE.md` rule. Five stories (inventory -> scrub -> guard -> DOCS_STYLE policy -> closeout); AGENT-BRIEF + current-phase-status written; BACKLOG candidate H added. Docs-and-test only, behavior-preserving. Prior close: **Phase 50 — Release Readiness ("cut a real 0.x"): CLOSED (7/7)** from [backlog](./BACKLOG.md) candidate C, the open-source-ship unlock. **HS-50-07 (closeout) done:** a green dogfood (`dogfood-transcript.txt`, RESULT: PASS) proves the safety matrix end to end (one true version, fresh-create, no-op-equal, older -> backup-then-apply with data intact, newer -> refused untouched, doctor honest, newer config kept + flagged); full suite green (2451 passed, 17 skipped), `npm run build` clean, 0 `_built/` tracked, `final-summary.md` written, BACKLOG candidate C flipped to shipped, PR to `main` merged on green. **HS-50-06 (docs) done:** new `docs/RELEASING.md` states the version/upgrade/backup policy (the four-way schema matrix in plain words, `holdspeak backup`/`restore`, what `doctor` reports) plus a maintainer release checklist; README gained an "Upgrading and your data" subsection and GETTING_STARTED a backup pointer; doc guards green, no em/en dashes. **HS-50-05 (verified clean-machine install) done:** a fresh-venv `uv pip install -e .` with a clean temp HOME reaches `holdspeak doctor` exit 0 (only the expected optional llama.cpp-model + system-audio warnings), version 0.2.1 resolved from real install metadata, the new Database/Config checks pass; `install.sh` pins a tag via `HOLDSPEAK_REF`; transcript captured. **HS-50-04 (doctor + config honesty) done:** `doctor` gained a read-only Database check (current=PASS, older=WARN, newer=FAIL, unreadable=WARN) that also flows into `/api/setup/status`; `Config` carries a `config_version` that coerces an older/unversioned shape forward without dropping fields and keeps + flags a newer one. **HS-50-03 (backup + restore) done:** `backup_database` now takes a consistent snapshot via SQLite `Connection.backup`; new `holdspeak backup` and `holdspeak restore` CLI commands let a user snapshot before upgrading and put a backup back, snapshotting the current DB first so a restore can never be the step that loses data. **HS-50-02 (safe-by-default schema policy) done** (the heart of the phase): `_ensure_schema` implements the four-way matrix (create-fresh / no-op-equal / backup-then-apply-older / refuse-newer); `SchemaVersionError` refuses a newer-than-known DB untouched. The live `SCHEMA_SQL` is additive today, so the closed holes are a newer DB run by an older build and the absence of a backup before a future migration. **HS-50-01 (one true version) done:** `__version__` resolves from package metadata, killing the `0.1.0`/`0.2.1` split; surfaced in `doctor` and `/api/setup/status`; `install.sh` pins a tag via `HOLDSPEAK_REF`. Next: HS-50-04, doctor + config honesty. The bet: 49 phases of features but never a real release — the next `SCHEMA_VERSION` bump silently wipes a user's DB (`db/core.py:_ensure_schema`) with no backup, the in-code version disagrees with the package version, `doctor` never checks the DB, and the install pins git HEAD. Phase 50 makes it safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (refuse-newer / backup-then-apply-older / no-op-equal / create-fresh), a backup before upgrade, an honest doctor + `config_version`, a verified clean install, and an upgrade/backup policy doc. Behavior-preserving. The [`AGENT-BRIEF`](./phase-50-release-readiness/AGENT-BRIEF.md) maps the version/db-core/doctor/config/install/docs seams; HS-50-02 (safe schema policy) is the heart, HS-50-01 (one true version) the cheapest first win. Prior: **Phase 49 — Meeting Aftercare ("close the loop"): CLOSED (6/6)** from [backlog](./BACKLOG.md) candidate A (user-favored). The meeting result now closes its own loops: a "Your next move" panel (open by owner / decided / changed), a focus-safe "show me the moment" transcript jump, an accepted action filed as a human-approved actuator proposal through the existing flow (no new write primitive), and a local preview-only follow-up draft. Read-only + reuse; capture/plugins/synthesis untouched; actuators off by default; never acts without approval. **HS-49-06 (closeout) done:** before/after captured (artifact-only view vs the aftercare surface), a green end-to-end dogfood (`dogfood-transcript.txt`), full suite green (2426 passed, 17 skipped), `npm run build` clean, 0 `_built/` tracked, `final-summary.md` written, BACKLOG candidate A flipped to shipped, PR to `main` merged on green. **HS-49-05 (docs) done:** the Meeting Mode guide gained a "Meeting Aftercare (close the loop)" section telling the flow end to end with three real shipped-UI screenshots and the honest posture (off-by-default actuators, preview-only drafts, real diffs/provenance); doc guards 65/2. **HS-49-04 (the follow-up draft) done:** `build_followup_draft(digest)` assembles a local markdown follow-up (decisions + open items by owner + the since-last delta) from the HS-49-01 aggregation, served read-only by `GET /api/meetings/{id}/followup-draft` and surfaced as a "Draft follow-up" preview + Copy in the aftercare panel head. Preview + copy only, never sent, no connector; honest at empty (one plain line, no padding); deterministic, no LLM. 6 new tests + the 479-passing relevant sweep; screenshot. **HS-49-03 (close the loop) done:** a "File as issue" affordance on accepted aftercare actions records a GitHub-issue actuator *proposal* through the existing propose -> approve -> execute flow — a shared `build_github_issue_proposal` feeds the same payload the existing connector consumes, so there is no new write primitive; `POST /api/meetings/{id}/aftercare/file-issue` only records a `proposed` proposal (idempotent per action), and the existing read/decision endpoints + `ActuatorExecutor` carry it the rest of the way. Off by default, human-approved, audited; an executor test proves a proposed/unapproved proposal never egresses (proposed -> approved -> executed only when enabled + allow-listed + approved). 5 new tests + the 523-passing relevant sweep; before/after screenshots. **HS-49-02 (transcript provenance) done:** `resolve_provenance_segment` threads a resolved jump target into the aftercare payload, and a focus-safe "show me the moment" affordance on open items, decisions, and action-item cards reveals + flashes the transcript segment that justifies a result — shown only when a real `source_timestamp` resolves to a real segment (no fake 0:00), reveal + flash never `.focus()`. Read-only, no schema churn; 5 new tests + the 509-passing relevant sweep; before/after screenshots. **HS-49-01 (the aftercare digest) done:** a read-only `compute_meeting_aftercare` + `GET /api/meetings/{id}/aftercare` aggregates what's still open (by owner), what was decided (the `decisions` artifact), and a real since-last-meeting diff (new decisions / new actions / closed actions vs the chronologically prior meeting), surfaced as a "Your next move" panel above the meeting's artifacts and quiet when nothing's open/decided/changed (`is_empty`). No new writes; capture/plugins/synthesis untouched; 9 new tests (unit + API) + the 477-passing relevant sweep; screenshot captured; 0 `_built/` tracked. Next: HS-49-03 (accepted actions -> issues). The phase makes the meeting result close its own loops: surface what is still open / decided / changed since last meeting, jump to the transcript moment, turn accepted actions into issues through the existing human-approved actuator flow, and draft a copyable follow-up. Read-only aggregation + reuse of the P37/P38 actuator system; never acts without approval; honest diffs (quiet at no-change). The brief maps the meetings/action-items/artifacts/actuator/history-UI seams; HS-49-01 (the aftercare digest) is the entry point. Prior: **Phase 48 — The Visible Learning Loop: CLOSED (5/5)** ([final-summary](./phase-48-visible-learning-loop/final-summary.md)). The local speech-to-work loop is now visible (a windowed "What HoldSpeak learned" digest), trustworthy (inline "learned from N similar" counts from the same Jaccard matcher that nudges routing), and a one-tap right/wrong ritual on the dry-run result + journal entries; docs tell it end to end as the local-first differentiator with an honest limits note. Read-only + behavior-preserving (routing byte-identical when corrections are off); honest at N=0; proven through real voice by an opt-in `say` -> Whisper -> digest e2e and a green dogfood; before/after captured; full suite 2401/18; 0 `_built/` tracked; PR to `main` merged on green. **HS-48-04 (docs) done:** the Intelligent Typing guide §12 now tells the loop as one five-step story (dictate, correct in one tap, learn, see the digest, replay) with real screenshots and an honest "how the learning works, and its limits" note (Jaccard, local, off by default); the README + docs index frame it as the local-first differentiator and the README gained the digest screenshot. Doc guards 65/2; full suite 2401/18; 0 `_built/` tracked. **HS-48-03 (the correction ritual) done:** correcting is now one tap on the dry-run result and every journal entry — a shared inline `correctionRitual` / `wireFixit`, "Right" a calm client-only acknowledgement, "Wrong" the existing correct path pre-scoped (block/target in one tap). Reuses `POST /journal/{id}/correct` (no new write primitive) and stays focus-safe (zero `.focus()`). Full suite 2401/18; 0 `_built/` tracked. **HS-48-02 (inline trust signals) done:** a calm "learned from N similar" chip now rides the dry-run result, journal entries, and the Memory list, and the post-correction toast states real coverage. All counts come from one shared helper (`reach_for_gist` / `best_correction_signal`, reusing the router's `best_match_in`); quiet at N=0, honest when corrections are off or a teach was secret-filtered. Full suite 2395/18; 0 `_built/` tracked. **HS-48-01 (the learning digest) done:** a read-only `GET /api/dictation/learning-digest?window=week|all` aggregates the journal + corrections into honest, windowed counts (corrections made, dictations corrected, by-kind/block/target, and a real "learned from N similar" per correction from the same Jaccard matcher that nudges routing); a Signal-styled "What HoldSpeak learned" hero on the Memory tab renders it (week/all toggle, stat cards, breakdown chips, per-correction reach, teaching empty state). Read-only, behavior-preserving, quiet at N=0. Proven through real voice end to end by an opt-in `say` -> Whisper -> dry-run -> correct -> digest e2e. Full suite 2385/18; 0 `_built/` tracked. Prior: **Phase 47 — Project Knowledge: Legible & Inviting: CLOSED ✅ (6/6)** ([final-summary](./phase-47-project-kb-legibility/final-summary.md)) — the overloaded "project KB" is now one settled model (**facts + context**) with clear names, in-app explainers + teaching empty states, a guided setup (a confirm-gated `.hs/` starter set and a copiable, repo-aware "draft my `.hs/` with your coding agent" prompt), an ambient focus-safe discovery nudge, and aligned docs; pipeline behavior unchanged (one additive, safe-by-default starter block); before/after captured, dogfood green, full suite 2372/17, doc guards 8/8, 0 `_built/`; PR to `main` merged on green. Prior: **HS-47-05 (docs alignment):** the Intelligent Typing guide's "§5. Set Up Project Knowledge" covers Facts (the `project.yaml` KB, worked example, `kb-enricher`, no LLM) and Context (the `.hs/` files, optional `project-rewriter`); the index, `DICTATION_COPILOT.md`, `USER_GUIDE.md`, and the `dictation-runtime` web doc reconciled. Prior: **HS-47-04 (discovery nudge) shipped:** an ambient, dismissible, focus-safe `#kn-nudge` bar above the `/dictation` tabs shows only when a detected project has no knowledge (no facts, no `.hs/`) and "Set it up" routes into the guided flow; durable per-project + global dismissal (localStorage); readiness gained an additive `project_context` existence signal. Prior: **HS-47-03 (guided setup) shipped:** a guided panel on the Context surface takes a fresh repo to working dictation with no file editing — a confirm-gated `.hs/` starter set and a copiable, repo-aware "draft my `.hs/` with your coding agent" prompt (Claude/Codex draft on your machine, you review in the tab); a new additive `project_facts_context` starter block consumes a fact so it is demonstrable, proven by `scripts/dogfood_project_knowledge.py` (zero hand-editing, no mic). Prior: **HS-47-02 (explainer + empty states) shipped:** both `/dictation` knowledge surfaces now teach — a what/why/worked-example explainer (Facts shows a `{project.kb.stack}` substitution; Context shows a rewrite being shaped) plus an inviting empty state with a one-click starter; static markup toggled by JS to keep scoped CSS, screenshot-verified, focus-safe. Prior: **HS-47-01 (concept & naming) shipped:** the `/dictation` "Project KB" tab is renamed **"Project Facts"** under one settled model, **project knowledge = Facts (the `project.yaml` KB, stamped in verbatim) + Context (the `.hs/` files, read by the optional rewrite stage)**; tab headers, ledes, the readiness card, and the `DOCS_STYLE.md` glossary all state the relationship; on-disk names + pipeline behavior unchanged. Prior: **Phase 46 — Documentation Excellence & the 10-Second Hook: CLOSED ✅ (6/6)** ([final-summary](./phase-46-documentation-lift/final-summary.md)) — the docs lift landed: README **205 → 157** (a 10-second hook + a cool-facts strip, every pixellab graphic kept, a real app screenshot), a uniform `## See also` footer (13/13) governed by `docs/internal/DOCS_STYLE.md` + a journey-map index, **real UI screenshots** from a repeatable `scripts/screenshot_docs.py`, a **feature → doc coverage matrix** (no orphans), and a **truth audit** that fixed 4 drift findings + pins the plugin count + image refs with guards. Docs-only (full suite 2365/17; a 2-test Playwright flake passed in isolation); 0 `_built/`. PR to `main` open. The project-KB *product/UX* legibility is teed up as **Phase 47**. **Phase 47 — Project Knowledge: Legible & Inviting scaffolded** (PLANNING 0/6) — the project-KB legibility work split out of Phase 46 on user direction: `/dictation` overloads "project KB" across two adjacent jargon-named tabs (the `project.yaml` `kb:` map vs the `.hs/` context — the Phase-46 docs pass even conflated them), so settle the concept + names → explainers + inviting empty states → a guided setup flow → a discovery nudge → docs alignment → closeout; behavior-preserving. **HS-46-05 — Coverage & discoverability: DONE.** A **feature → doc matrix** (in the audit doc) maps 16 user-facing capabilities → guide home → README hook → index journey: **no orphan features, no orphan docs**. Gaps closed: the index now names **actuators**, and the user-reported **"project KB" confusion** is fixed in the docs (plain definition on first use + a `kb-enricher` gloss + a `DOCS_STYLE.md` glossary entry); its deeper **product/UX legibility is teed up as new Phase 47 — "Project KB: legible & inviting"**. No stale residue. Guards 8 passed; full suite 2365/17. Next: HS-46-06 closeout + PR. — **HS-46-04 — Visual lift: DONE.** A repeatable `scripts/screenshot_docs.py` (boots a real server over seeded state — no mic/LLM — and writes `welcome.png` / `journal.png` / `history.png` to `docs/assets/screenshots/`) + three real UI shots embedded with alt+caption: the journal in the **README** ("See it learn"), the welcome wizard in **Getting Started**, the `/history` artifact cards in **Meeting Mode** (Intelligent Typing already carried 9 from Phases 40/41/45). A new **image-ref guard** validates `<img src>` + markdown images across the README + docs. Pixellab art kept (additive); `npm run build` ✓; 0 `_built/` tracked. Guards 8 passed; full suite 2365/17. Next: HS-46-05 coverage matrix → HS-46-06 closeout. — **HS-46-03 — Docs voice & structure + elevated index: DONE.** A style guide (`docs/internal/DOCS_STYLE.md`: voice + page skeleton + privacy callout + cross-link/anchor rules), a **uniform `## See also` footer across all 13 docs** (renamed the two strays, added the missing ones), and `docs/README.md` rebuilt as a **journey map** (Start here · Dictate · Meet · Extend · Operate & Trust) in lockstep with the README's "Where to go next". Voice+structure, not a rewrite (reference docs kept their depth); a code-fence link-guard catch fixed honestly. Guards 7 passed; full suite 2365/17. Next: HS-46-04 visual lift + HS-46-05 coverage matrix. — **HS-46-02 — The README, reimagined: DONE.** Spec sheet → product pitch: a 10-second hook ("Hold a key. Speak. It types — anywhere. 100% local. And it learns you.") + a "Why it's different" cool-facts strip (the journal/replay "it learns you" story above the fold), every pixellab graphic kept, the 52-line plugin table + 22-line AIPI prose cut to teasers+links, raw config linked out, pre-release stated once — **205 → 152 lines** (26%), honest per the HS-46-01 audit. Guards 7 passed; full suite 2365/17. Next: HS-46-03 voice/structure + HS-46-04 visual lift. — **HS-46-01 — Doc truth audit & drift fix: DONE.** `docs/internal/DOC_AUDIT_2026-06.md` inventories all 18 live docs vs a code-verified facts table; 4 drift findings fixed across 6 docs — presence is config-backed not env-var-only, a phantom `intel_cloud_timeout_seconds` config key removed, the missing `pipeline` connector kind added, the stale device-protocol `Listening...`/`Thinking...` pushbacks removed — plus a new guard pinning the README plugin count to the registry; the deep-link anchors an auto-pass flagged were verified *correct* and left alone. Suite 2365 passed, 17 skipped. Next: HS-46-02 the README reimagining. — **Opened Phase 46 — Documentation Excellence & the 10-Second Hook** — scaffolded on user direction after a grounded look at the docs: the README (205 lines) sells like a spec sheet — no 10-second hook, the best stories buried, repetitive ("What it does" / "Intelligence Pipeline" / "Meeting plugins" overlap; AIPI-Lite two long paragraphs; pre-release stated twice) — and the loved pixellab graphics never show the *actual app*; the 13 guides (4,677 lines) drifted across 13 phases and lack one voice. 6 stories: a doc truth audit → a bold README reimagining (10-second hook + cool-facts strip, every graphic kept, repetition cut, depth linked, ~130–150 lines) → a docs voice/structure system + an elevated index → a visual lift (real UI screenshots) → a feature → doc coverage matrix → closeout. Honest not hype; graphics stay; docs-only. Locked (user): bold README; real screenshots; truth/voice pass on all docs. HS-46-01 is the entry point. **Phase 45 — Dictation Memory & the Moment of Truth: CLOSED ✅ (6/6)** — the daily-driver dictation loop now has the soul the meeting side already had: **remember → review → correct-in-the-moment → replay**, all local-first, side-channel (typed output byte-identical when off), focus-safe, and mic-free-provable. A `dictation_journal` table + repository + a side-channel recorder (both the live + dry-run paths; secret-redacted; retention-capped; `journal_enabled` toggle; proven by a true e2e against `.43`) → a **Journal** tab on `/dictation` (said→typed timeline, per-utterance latency strip, search/filters, copy, delete/clear, local-only trust) → the dry-run panel's *"Was that right? → Fix it → Taught ✓"* (records a correction keyed on the transcript, nudges future routing, flips `corrected`) → **↻ Replay** (re-run a stored utterance through the current pipeline; before → after; correct → replay → routing changes). Docs in `INTELLIGENT_TYPING_GUIDE.md` §12. The no-mic dogfood `scripts/dogfood_journal.py` runs the full loop green (`JOURNAL DOGFOOD OK`). Also fixed a pre-existing dry-run ReferenceError that left browser results blank. Suite 2363/17; 0 `_built/`. Branch `phase-45-dictation-memory` — PR to `main` opened, merge when CI green. **HS-45-05 DONE: docs** — `docs/INTELLIGENT_TYPING_GUIDE.md` §12 "Dictation journal, corrections & replay" documents what's recorded, the local-only privacy posture (secret-filter · retention · wipe · `journal_enabled` toggle · side-channel), the in-moment fix loop, and replay, with three real screenshots; cross-linked from the root README + docs index; doc-drift + dangling-link guards green. **HS-45-04 DONE: replay** — `POST /api/dictation/journal/{id}/replay` re-runs a stored utterance's transcript through the current pipeline (dry-run, no typing, no new row, original untouched, in its original project context); a per-entry ↻ Replay action on the Journal renders a before → after diff. The "it learned" payoff, proven offline: correct an utterance's target → replay → the routed target flips to the corrected profile. Re-insert is preview + copy (focus-safe); OS-typing re-insert deferred (no web→typer seam; focus-steal risk). 5 integration tests incl. the correction→replay nudge + a live `replay_before_after.png`; route-table guard 34→35; suite 2363/17. **HS-45-03 DONE: the moment of truth** — after a dry-run, the result panel surfaces *"Was that right? → Fix it"* → teach a correction → *"Taught ✓"*, via `POST /api/dictation/journal/{id}/correct` (records a correction keyed on the entry transcript + flips `corrected` + nudges future routing). Focus-safe (no `autofocus`/`.focus()`), byte-identical when not correcting, provable with no mic; 7 integration tests incl. the nudge proof + a live `moment_of_truth.png`; also fixed a pre-existing dry-run ReferenceError that left browser results blank. Suite 2358/17. **HS-45-02 DONE: the Journal review timeline** — a Journal tab on `/dictation` over `GET/DELETE /api/dictation/journal`: an elevated said→typed timeline with source chips, block/target badges, a **per-utterance latency strip** (the black box, opened), search + source/warning/corrected filters, copy, per-entry delete + clear, a first-class local-only trust statement, a warm empty state, and the Phase-44 premium bar. 8 integration tests + a live `journal_timeline.png`; route-table guard 30→34; suite 2351/17; 0 `_built/`. **HS-45-01 DONE: the dictation journal persistence spine** — `dictation_journal` table + `DictationJournalRepository` + a side-channel `DictationJournalRecorder` wired into both the live dictation + dry-run paths [best-effort, secret-redacted, retention-capped, config toggle default ON]; journal-off ⇒ typed output byte-identical. Proven by 13 unit + 2 integration tests **and a true end-to-end run against the live `.43` endpoint** [real pipeline → real LLM → real DB row: a 446-char ramble journaled with routing/target/per-stage latency]. Suite 2343/17. **Opened Phase 45 — Dictation Memory & the Moment of Truth** — scaffolded on user direction after a deep look at the experience: the daily-driver dictation loop is a *black box* (no journal, no in-the-moment correction, no replay, opaque latency) while the meeting side is rich and reviewable. The phase gives dictation the soul meetings already have — a persistent private **dictation journal**, an **in-the-moment correct-and-teach** loop, **replay**, and a transparent per-utterance latency view — all local-first, side-channel (typed output byte-identical), focus-safe, and provable remotely via dry-run. 6 stories; HS-45-01 (the journal persistence spine) is the entry point. **Phase 44 — Daily-Surface Polish: CLOSED ✅ (4/4) + MERGED** (PR #22) — carried the Phase-43 wizard's bar to the three daily-driver surfaces, the dashboard · dictation cockpit · history, behavior-preserving; one shared premium language [ambient glow, hero grammar, solid-accent pill navs, elevated rounded surfaces, reduced-motion-safe depth]; per-surface page-content tests + a live before/after; suite 2328/16; 0 `_built/`. PR to `main` pending.) (2026-06-05: Opened **Phase 31 — Database Decomposition** and **Phase 32 — Foundation Hardening & Doc Truth** off a comprehensive engineering review. Phase 31 splits the 5,481-line `db.py` god-object into per-domain repositories behind a thin `Database` container — and, taking the **greenfield/aggressive** posture (one user, one dev DB, destructive-OK), **deletes** the `MeetingDatabase` god-class and **squashes** the 18-version migration ladder to one canonical schema rather than preserving compat. Phase 32 finishes the runtime decomposition Phase 26 left standing (class-ify `web_runtime.py`, invert meeting→web), converges the three audio-ownership models, adds the missing **ungated** CI core-path smoke test, extracts a route error helper, and **deletes** stale **non-PMO** docs (HANDOVER's dead "11 stubs" line; PLAN "branch feature/…" headers for branches that don't exist). The PMO historical record is kept verbatim by design. **HS-31-01 shipped same day:** `db.py` → `holdspeak/db/` package (`models`/`base`/`meetings`/`core`), the meetings domain extracted to `MeetingRepository`, 53 production + 112 test call sites moved to `db.meetings.*`, `MeetingDatabase` shed 5481→4311 lines; full public surface re-exported (no caller import changes); suite green at 2062 passed / 14 skipped, db package ruff-clean. **HS-31-02:** `IntelRepository` extracted (deferred-intel queue), 19 production + 32 test call sites → `db.intel.*`, container back-reference pattern established for cross-domain calls, `core.py` 4311→3934; suite green at 2062, db package ruff-clean. **HS-31-03:** last three repos extracted (PluginArtifact/Project/Activity) via AST extraction; `core.py` is now a 1224-line container (−78% from 5481) and the `MeetingDatabase` god-class was renamed to `Database` (zero refs in code); ~469 call sites moved; suite green at 2062. **HS-31-04:** squashed the 18-version migration ladder to one canonical schema (`SCHEMA_VERSION` 18→1), `core.py` 1224→666, fresh-build `sqlite_master` proven identical to the pre-squash v18 schema via a committed snapshot test; dev DB dropped & recreated; suite green at 2063. **HS-31-05 closeout — Phase 31 CLOSED, 5/5:** `db.py` 5481 → a `Database` container + 5 repositories (package total 5190, *less* than the original single file); `final-summary.md` written. **Phase 31 merged to `main` (PR #7) and Phase 32 opened for work. HS-32-01 shipped:** the 1,702-line `run_web_runtime()` god-function (10 `nonlocal` vars, ~55 closures) is now a `WebRuntime` class — state on `self`, closures as methods, `run()` owning the lifecycle, and a thin `run_web_runtime()` shim preserving the entry point; behavior-preserving, suite green at 2063/14, `web_runtime.py` ruff-clean. **HS-32-02 shipped:** `MeetingSession` is now web-free — it no longer imports/owns/starts/stops or broadcasts to a `MeetingWebServer`; it **emits** live events via an injected `on_broadcast` callback and `WebRuntime` observes (forwarding `intel_token`/`meeting_updated`, previously dead in the flagship). Per **user decision**, the embedded per-meeting web server was **dropped** (not relocated); `config.meeting.web_enabled` is now vestigial (flagged for HS-32-06). Suite green at 2066/14 (+3 headless tests). **HS-32-07 shipped (user directive, "kill the TUI"):** the Textual TUI (`tui/` + `controller.py`) and the macOS menu-bar app (`menubar.py`) are **deleted** — the `tui`/`menubar` subcommands, the `--no-tui` alias, the `textual`/`rumps` deps, the TUI/menubar-only tests, and the dead TUI/menubar docs all gone (~13k lines removed; live docs updated). The **web runtime is now the sole interactive runtime**; sequenced before HS-32-03 so the audio convergence has one home. Suite green at 1939/14. **HS-32-03 shipped:** one audio-ownership model — `VoiceTypingSession` gained source-less `acquire`/`release`, and the meeting now holds the **shared** floor (hotkey/device `begin()` is rejected mid-meeting and a meeting can't start while either records; redundant ad-hoc guards removed — the arbiter is the single decision point). New `TestAudioFloorArbitration` (incl. a concurrency mutual-exclusion test). Suite green at 1946/14. **HS-32-04 shipped:** the **ungated CI core-path smoke test** — a committed 16 kHz WAV (generated once via `say`) is run through the real `Transcriber("tiny")` → text processing → the injection seam, asserting the produced text contains the spoken phrase. It runs on the macOS integration job (mlx-whisper core dep) on every push — off the never-in-CI `metal`/`spoken_e2e` markers — and skips where no backend exists. Mutation check shown (silence → red). Suite green at 1948/14. **HS-32-05 shipped:** one route 500-response helper — `error_500(exc, logger, detail)` in `runtime_support.py` replaces the canonical `log.error + JSONResponse(500)` block at **48** call sites (activity/projects/meetings/system), so an error-contract change is a one-line edit; behavior byte-identical. Chose a helper function over the deferred decorator default (handlers have nested try/except + specific non-500 handling a whole-handler decorator would swallow). Suite green at 1952/14. **HS-32-06 shipped — Phase 32 CLOSED (7/7):** the doc-truth sweep + drift guard. Fixed the false `DeterministicPlugin` "stub" markers (the worst rot), a dead `Branch:` header, and retired-TUI mentions; removed the vestigial `config.meeting.web_enabled`; refreshed the stale `HANDOVER.md`; committed `test_doc_drift_guard.py` (verified it catches reintroduced stub-rot). Suite green at 1954/14; `final-summary.md` written. **Phase 32 merged to `main` via PR #8 (all CI green, incl. the new real-Whisper smoke test on the macOS runner).** **Opened Phase 33 — Documentation & Open-Source Readiness** (2026-06-03): de-prescribe + refresh the model framing (Qwen2.5/Mistral refs are 2–3 generations stale; current is Qwen3.5/3.6 — frame as suggestions + a `MODELS.md` contract, keep GGUF), Apache-2.0 LICENSE + `pyproject` metadata, `docs/` reorg + index, an OSS-grade README pass, and a refreshed pixel-art asset set (pixellab MCP). HS-33-01 (model framing) first. **HS-33-01 shipped (2026-06-03):** de-prescribed the user-facing model strings (missing-model errors, doctor/guidance fixes, the web dictation-runtime docs page → "any GGUF/MLX chat model / OpenAI-compatible endpoint"), bumped the example/default models to the **Qwen3.5** family (dictation `llama_cpp`/`mlx`/`openai_compatible`; intel realtime + `DEFAULT_INTEL_MODEL_PATH` → Qwen3.5-9B, matching `.43`), added **`docs/MODELS.md`** (the bring-your-own contract: GGUF in-process · MLX on Apple · any OpenAI-compatible endpoint) and clarified the intel `cloud` provider as "any OpenAI-compatible endpoint" (key optional for self-hosted). Web bundle rebuilt; pinned test assertions updated in the same commit; suite green at 1953/15, touched files ruff-clean. **HS-33-02 shipped (2026-06-03):** added the **Apache-2.0 `LICENSE`** (user decision) + a complete `pyproject` `[project]` metadata block — SPDX `license = "Apache-2.0"` + `license-files`, `authors`, 11 `keywords`, 13 `classifiers` (Python 3.10–3.13, macOS/Linux, speech/utilities, Beta), and `[project.urls]` (Homepage/Repository/Issues/Documentation → `github.com/karolswdev/HoldSpeak`); `uv build` clean, `Metadata-Version: 2.4` + `License-Expression` verified in the wheel. Suite green 1953/15. **HS-33-03 shipped (2026-06-03):** reorganized `docs/` — `git mv`'d 13 internal/historical docs into **`docs/internal/`** (the `PLAN_*` specs + `CROSS_PLATFORM_*`/`LINUX_PORT_*`/`RELEASE_HARDENING_CHECKLIST`), kept 11 user-facing guides in `docs/`, added a **`docs/README.md`** index (Start-here user journey + reference + an `internal/` pointer), repointed every **live** inbound link (CLAUDE.md + the live roadmap README source-canon, root README, ~12 source docstrings, `release_gate.py`, the flagship-audit test, internal cross-refs), and added a doc link-check (`test_no_live_doc_has_a_dangling_relative_link`). Frozen history (`docs/evidence/` + `pm/` phase folders) kept verbatim. Suite green 1953/15. **HS-33-04 shipped (2026-06-03):** OSS-grade README pass — license/CI/Python/platform **badges**, an honest **pre-release status** banner (not on PyPI; install from source), a clean-clone **quickstart** (install-script one-liner + from-clone `uv pip install -e .`, both → `doctor && holdspeak`) pointing at `docs/MODELS.md`, docs-index/Models/Contributing links, and the stray `MIT` line corrected to **Apache-2.0**; added **`CHANGELOG.md`** (Keep a Changelog, everything honestly "Unreleased") + a minimal **`CONTRIBUTING.md`** (uv setup, git hooks, test command, commit contract). All README links verified; suite green 1954/15. **HS-33-05 shipped (2026-06-03):** generated the HoldSpeak **brand mark** (held-key + rising soundwaves, orange `#FF6B35`) via the pixellab MCP and composed a **1280×640 social/OG card** + a 256px app icon + a refreshed 180px `apple-touch-icon` (PixelLab makes ≤400px sprites → the card is composed from the mark + workflow icons on the Signal palette via committed `compose_og_card.py`); audited the existing spot-art (coherent, no regen); wired the mark into the README header; provenance recorded in `docs/assets/pixellab/README.md`. Suite green 1954/15. **HS-33-06 — Phase 33 CLOSED ✅ (6/6) (2026-06-03):** the closeout — link-check sweep (root docs + `docs/**` resolve), doc-truth re-verify (drift guard green; reconciled the last "v0.2.0 released" contradiction → pre-release), the OSS checklist (LICENSE / pyproject metadata / README badges / docs index / assets / CHANGELOG / CONTRIBUTING all ✅), and `final-summary.md`. Suite green 1954/15. **Phase 33 is the local branch `phase-33/hs-33-01-model-framing` (6 story commits) — open a PR to `main`; manual follow-up: set `social-card.png` as the GitHub social preview.** **Phase 33 merged to `main` via PR #9 (all CI green); Phase 34 — Structural Decomposition II opened (2026-06-03):** the twin of 26/31/32 — sub-split the four files that dominate the tree (5,373 lines combined: `web/routes/dictation.py`, `web/routes/activity.py`, `agent_context.py`, `intel.py`) into navigable packages, behavior-preserving (import paths + route table held identical). HS-34-01 (dictation routes) first. **HS-34-01 shipped (2026-06-03):** `web/routes/dictation.py` (1,607L) → a `routes/dictation/` sub-package (intents / agent / project_docs / blocks / kb / pipeline + a ctx-free `_helpers.py`), composed behind a stable `build_dictation_router(ctx)`; the shared project-doc-suggestion store threaded explicitly; route table byte-identical (26 routes, hash unchanged) and locked by a committed `test_dictation_routes_split.py`. Suite green 1954/15; package ruff/F821 clean. **HS-34-02 shipped (2026-06-03):** `web/routes/activity.py` (1,319L / 38 handlers) → a `routes/activity/` sub-package (ledger / rules / enrichment / candidates / plugin_jobs) behind a stable `build_activity_router(ctx)`; every payload shaper is single-group so no shared state to thread; route table byte-identical (38 routes, hash `d4332051064ff059`) and locked by `test_activity_routes_split.py`. Suite green 1956/15; package ruff/F821 clean. **HS-34-03 shipped (2026-06-03):** `agent_context.py` (1,381L / 46 funcs) → an `agent_context/` package (`_common` ← `models` ← `hs_context`/`hooks` ← `sessions`) with a full re-export `__init__` (63 names), via decorator-aware AST extraction; the two monkeypatch hazards preserved with tests unchanged (`AGENT_CONTEXT_FILE` read through the package; `shutil` re-exposed). Committed `test_agent_context_package.py` guards the surface. Suite green 1958/15; package ruff/F821 clean. **HS-34-04 shipped (2026-06-03):** `intel.py` (1,066L) → an `intel/` package (`models` ← `parsing`/`providers` ← `engine`) with a full re-export `__init__`; the optional-dep import head (`OpenAI`/`Llama`) stays in `__init__` as the patch target and `providers`/`engine` read it via the package, so the egress-invariant/cloud/plugin-host patches all resolve with tests unchanged. Committed `test_intel_package.py`. Suite green 1962/15; package ruff/F821 clean. **HS-34-05 — Phase 34 CLOSED ✅ (5/5) (2026-06-03):** closeout — route-table invariants re-verified (dictation `0a0b26562cf25a36`/26, activity `d4332051064ff059`/38, both unchanged), all four trees ruff+F821 clean, `final-summary.md` written. The four files (5,373 lines) are now four packages (24 modules; largest single file 547, was 1,607). Suite green 1966/15. **Phase 34 is the local branch `phase-34/hs-34-01-dictation-routes-split` (open + 5 story commits) — open a PR to `main`.** **Phase 34 merged to `main` via PR #10 (all CI green); Phase 35 — Plugin Frontier opened (2026-06-03):** make the meeting-intel plugin system extensible by others — public `docs/PLUGIN_AUTHORING.md`, a plugin-pack manifest + discovery loader (mirroring connector packs), per-project enable/disable, and the incident/comms spoken-e2e scenario; the 14 built-ins stay behavior-identical and **actuators are deferred to Phase 36** (the host's dormant `actuator` gate + the RFC's open question #5). HS-35-01 (authoring guide) first. **HS-35-01 shipped (2026-06-03):** `docs/PLUGIN_AUTHORING.md` — the public authoring guide (mirrors `docs/CONNECTOR_DEVELOPMENT.md`): the `HostPlugin` protocol (`id`/`version`/`kind`/`execution_mode`/`required_capabilities` + `run(context) -> dict` and the context dict), the reference run pattern (JSON-only prompt → `build_configured_meeting_intel()._chat_completion_text` → parse/validate → success/failure shapes with `confidence_hint`), the `llm` capability gate (`resolve_llm_capability` → `enabled_capabilities` → `blocked` skip), registering a synthesis renderer (`_ARTIFACT_TYPE_BY_PLUGIN` + `_ARTIFACT_RENDERERS`), joining a chain (`PROFILE_PLUGIN_BASE_CHAINS`/`_INTENT_PLUGIN_CHAIN` + the routing-ripple warning), the testing pattern (the `intel_call` injection seam + the capability-gate test), and the "shipped" bar; wired into `docs/README.md` + the README plugin section. Doc drift-guard + link-check green; suite 1966 passed / 15 skipped. HS-35-02 (plugin packs) next. **HS-35-02 shipped (2026-06-03):** the plugin-pack system mirroring connector packs — `holdspeak/plugin_sdk.py` (`PluginManifest` + `validate_manifest`, collecting every error as a stable-`code` `ManifestError`; `actuator` kind rejected as deferred) + `holdspeak/plugin_pack_loader.py` (first-party + `~/.holdspeak/plugin_packs/` user-pack discovery via `HOLDSPEAK_USER_PLUGIN_PACKS_DIR`, requiring `MANIFEST` + a `create_plugin()` factory; `build_registry`/`register_discovered_plugins`/`load_and_register_plugin_packs` register pack plugins on the host with structured `DiscoveryError`s, never crashing). `holdspeak/plugin_packs/` is the first-party slot (empty `ALL_PACKS` — the 14 built-ins stay hardcoded). `WebRuntime` discovers + registers packs after the built-ins, defensively. Committed fixture pack proves load→register→dispatch; new `test_plugin_sdk.py`/`test_plugin_pack_loader.py` cover validation/discovery/malformed/collisions + the built-ins-unchanged regression. Scope: chain hints declared/validated but live router wiring deferred to HS-35-03 — `router.py` untouched, routing byte-identical. `docs/PLUGIN_AUTHORING.md` gained a "Plugin packs" section. Suite 1995 passed / 15 skipped; new modules ruff+F821 clean. HS-35-03 (per-project enable/disable) next. **HS-35-03 shipped (2026-06-03):** per-project plugin enable/disable — a flat `MeetingConfig.disabled_plugins: list[str]` knob (normalized + validated; unknown ids a no-op) + a dispatch gate in `plugins/dispatch.py` that drops disabled ids from the *executed* set and records each as a new `skipped` `PluginRun` status (distinct from capability-`blocked` and ran-and-failed `error`; synthesis ignores it). `router.py` untouched → the *built* chain + `test_intent_dispatch.py` unchanged. Threads `dispatch_window`/`dispatch_windows` → `process_meeting_state` → `MeetingSession` (ctor + `stop()`) → `WebRuntime` (passes `config.meeting.disabled_plugins`); `skipped` persists via `record_plugin_run` and surfaces on `GET /api/meetings/{id}/plugin-runs`. New `test_plugin_disable.py` (12 tests: pure `partition_chain`/`normalize_disabled_plugins`, skip-not-execute, unknown-id no-op, default byte-identical, config validation). Default (empty list) byte-identical. Suite 2007 passed / 15 skipped; authored files ruff clean. `docs/PLUGIN_AUTHORING.md` gained a "Disabling a plugin per project" note. HS-35-04 (incident-retro spoken-e2e) next. **HS-35-04 shipped (2026-06-04):** a second opt-in spoken-meeting e2e (`test_spoken_incident_retro_end_to_end`) covering the previously-uncovered **incident + comms** chains (`incident_timeline`/`runbook_delta`/`risk_heatmap`/`stakeholder_update_drafter`/`decision_announcement_drafter`) — `say` → Whisper → real `.43` PluginHost → synthesis → `MeetingWebServer` → Playwright, structural assertions, opt-in/module-skip identical to the existing scenario. **Verified for real against `.43` (Qwen3.5-9B-Q6): 1 passed in 24.14s, all five artifacts rendered** (committed screenshot `phase-35.../evidence/spoken_incident_artifacts.png`, 1280×3094). Default suite green 2007/15 (module skips cleanly); file ruff-clean. Surfaced + flagged for closeout: a stale HS-32-06-retired `meeting.web_enabled` key in the user config tripped `Config.load()`'s broad `except: return cls()`, silently discarding the *whole* config (the `.43` endpoint ignored on every load) — a latent foundation bug worth a small hardening fix. HS-35-05 (closeout + final-summary) next. **HS-35-05 — Phase 35 CLOSED ✅ (5/5) (2026-06-04):** closeout — routing invariants re-verified (`test_intent_dispatch`/`test_intent_router` 54 passed; the 14 built-ins register + route identically, packs + the disable gate layer around them), doc drift-guard + live-doc link-check green, plugin-frontier modules (`plugin_sdk`/`plugin_pack_loader`/`plugin_packs/`) ruff+F821 clean, suite 2007/15. `final-summary.md` written; README phase row → done. The plugin system is now externalizable end-to-end (public authoring guide · discoverable packs · per-project enable/disable · incident+comms e2e). **Actuators teed up as Phase 36** (the host's `actuator` kind stays blocked; this phase is its groundwork). Carried follow-up: the `Config.load()` silent total-fallback on an unknown config key (foundation-hardening, not yet scheduled). **Phase 35 merged to `main` via PR #11 (all CI green).** **Config-hardening follow-up shipped + merged via PR #12 (2026-06-04):** `Config.load()` now filters unknown/legacy keys per sub-config (the HS-32-06-retired `meeting.web_enabled` no longer discards the whole config) and logs the last-resort fallback instead of swallowing it; suite 2009/15. **Opened Phase 36 — Meeting Artifact Experience (2026-06-04):** a frontend/UX phase on direct user feedback that the meeting-artifact rendering is too basic, has no copy affordance, and overflows horizontally (the risk table). Make the output a first-class deliverable — Signal **"elevated" artifact cards** (retire the basic chips), **copy-as-Markdown** per artifact, and **overflow-safe** wide artifacts. Presentation only (plugin contract + artifact data unchanged). Direction chosen by the user: **"Elevated cards"**; **Actuators renumbered to Phase 37**. HS-36-01 (card shell + overflow fix) first. **Phase 36 scope expanded same-day (user):** added a **dynamic/messy multi-topic spoken-e2e** (HS-36-04) and — crucially — grew the phase from presentation-only to also **fix the intent-extraction weakness** that meeting exposes (HS-36-05): MIR-01's fixed-90s-window + lexical-keyword scoring dilutes a brief-but-clear intent below the 0.6 threshold so it's silently lost; the fix is **segment-aware per-segment intent probing** (LLM-assisted, lexical fallback, union-aggregated). The phase now has two tracks — intelligence (04→05) + experience (01–03) — and its **headline deliverable is a before/after** of the same messy meeting (old routing sparse → new routing rich), closeout bumped to HS-36-06. Title broadened to "Meeting Intelligence & Experience" (folder slug kept). HS-36-01 still leads (most visible UI win). **HS-36-04 shipped (2026-06-04):** the dynamic/messy multi-topic spoken-e2e (`test_spoken_dynamic_meeting_end_to_end`), driving the **real** MIR routing path, verified on `.43` — it **caught the weakness live**: across 5 windows the routing activated only `['architecture', 'product']` and silently dropped the meeting's clear incident ("fell over… rolled it back…" — no `rollback`/`incident` keyword match), risk, and comms (`announce` scored ≈0.22 < 0.6). BEFORE = 7 artifact types with none of incident_timeline/risk_register/runbook_delta/stakeholder_update/decision_announcement; `evidence/dynamic_meeting_before.png` captured (the BEFORE half of the headline). Default suite green 2007/15. Next: HS-36-05 (segment-aware per-segment intent probing) fishes the dropped intents out + captures the AFTER. **HS-36-05 shipped (2026-06-04) — the intelligence fix + the before/after:** an additive, gated **per-segment LLM intent probe** (`plugins/segment_probe.py`; merged element-wise `max` into `scoring.score_window`, threaded through `process_meeting_state` + `MeetingSession`/`WebRuntime`, off by default via `intent_segment_probe_enabled`) reads each routing window by *meaning, not keywords*. On the **same** messy meeting via the **same** real routing path: BEFORE = `['architecture','product']` / 7 artifact types (incident+comms silently lost); **AFTER = `['architecture','comms','delivery','incident','product']` / 13 types**, with incident_timeline / runbook_delta / stakeholder_update / decision_announcement fished out (`evidence/dynamic_meeting_before.png` vs `_after.png`). Default `probe=None` path is byte-identical so the routing tests are unchanged; 13 new probe unit tests incl. the natural-language-incident regression; suite green 2020/15. Intelligence track done; experience track (HS-36-01 cards + overflow, HS-36-02 copy, HS-36-03 polish) + HS-36-06 closeout remain. **HS-36-01 shipped (2026-06-04):** the flat `.segment` artifact rendering → Signal **elevated artifact cards** (type-colored accent edge + type icon + bold title + type chip + meta + collapse toggle + overflow-safe body; full-width `.artifact-stack`), and the **risk-table horizontal overflow is fixed** (`.table-scroll` + min-width + cell wrap). `web/src/pages/history.astro` + `history-app.js` (`artifactIcon`/`artifactAccent`); bundle rebuilt + committed. Selectors preserved (incident spoken-e2e green, all artifact selectors resolve inside the new card body); suite 2020/15; new-look screenshot `phase-36.../evidence/artifact_cards_new_look.png`. Experience track next: HS-36-02 (copy-as-Markdown) → HS-36-03 (per-type polish) → HS-36-06 closeout. **HS-36-02 shipped (2026-06-04):** copy-as-Markdown — each elevated card got a **"Copy"** button (the HS-36-01 header slot) + the Artifacts section a **"Copy all"** control; both serialize the artifact `structured_json` to clean Markdown from the *data* (collapsed cards still copy) and write to the clipboard reusing the `CommandPreview` pattern (copied-state + graceful `Press ⌘C` fallback). Pure per-type serializers in `history-app.js` (`artifactMarkdown`/`allArtifactsMarkdown`): tabular → Markdown table with `|` escaped + empty cells `—`, timeline → ordered list, sectioned types → `###` headings, diagram → a mermaid fence, fallback → `body_markdown`. Verified by a Node serializer harness over the real module; bundle rebuilt (helper present in the gitignored `_built`, not committed); selectors untouched; suite 2020/15. Experience track next: HS-36-03 (per-type body polish) → HS-36-06 closeout. **HS-36-03 shipped (2026-06-04):** per-artifact-type body polish — a Signal pass over **every** artifact body inside the card shell (CSS-only in `history.astro`, no markup/selector change). Root-caused the flatness: the HS-27–29 bodies referenced **non-existent** tokens (`--color-danger-soft`/`--color-warning`/`--color-success*`/`--font-weight-bold`/`--text-default`) and rendered via off-palette `rgba/hex` fallbacks — all migrated to the real Signal status tokens. Plus designed bodies: the **incident timeline** is now a real rail (vertical line + ringed node dots + red time chips), **requirements** got typed badges, **risk/runbook/scope/signal** chips are bordered typed badges, **decisions/stakeholder** got accent markers, **action/adr/milestone/announcement** records are left-accented sub-cards, **dependency** is node chips + accent arrow. Verified by a Playwright render of all 13 types over the real tokens+CSS (`evidence/artifact_bodies_polished.png`) + a 420px no-overflow check (`scrollWidth == clientWidth`); selectors preserved; suite 2020/15. Experience track complete; **HS-36-06 closeout** (re-captures the before/after in the new cards + final-summary) is the only story left. **HS-36-06 — Phase 36 CLOSED ✅ (6/6) (2026-06-04):** the closeout re-ran the spoken-e2e on real `.43` to re-capture the headline **before/after in the new polished cards** — same messy meeting, **BEFORE** `['architecture','product']` / 7 artifact types (23 cards) vs **AFTER** `['architecture','comms','delivery','incident','product']` / **13** types (51 cards), incident+comms fished out per segment (`evidence/dynamic_meeting_before.png` vs `_after.png`); dynamic before/after + incident-retro `3 passed in 245.99s`, all artifact selectors resolving inside the new card body. Bundle rebuilt (gitignored `_built` — 0 files tracked); suite 2020/15; routing invariants byte-identical (`segment_probe=None` default). `final-summary.md` written; README phase row → done; HANDOVER refreshed. **Phase 36 is the local branch `phase-36/hs-36-01-artifact-card-shell` (phase open + 6 story commits) — open a PR to `main`. Phase 37 — Actuators is the teed-up successor.** **Phase 36 merged to `main` via PR #13 (all 6 CI checks green). Phase 37 — Actuators scaffolded (2026-06-04):** 6 linear stories laid out grounded in the parent RFC (open question #5) + the Phase-25 egress posture — HS-37-01 actuator contract (`ActuatorProposal`) + unblock the kind (proposal-only, gated) → 02 proposal persistence + lifecycle/audit → 03 approval surface (preview → approve/reject, no execution on view) → 04 guarded executor + audit + governance gate (payload parity, Phase-25-gated egress) → 05 one reference actuator end-to-end (incl. "no approval ⇒ no action") → 06 closeout. The safety invariant: **no external side effect without an explicit, audited, per-action human approval; executed == previewed.** The seam already exists (`plugin_sdk` rejects the `actuator` kind; `PluginHost(allow_actuators=False)` blocks it). Plan: `phase-37-actuators/current-phase-status.md`. Not started — HS-37-01 is the entry point. **HS-37-01 shipped (2026-06-04):** the plugin system's third kind is now *proposable* — a new `holdspeak/plugins/actuators.py` defines `ActuatorProposal` (target/action/preview/payload/reversible/required_capabilities) with `from_run_output` validation; `plugin_sdk` accepts `kind: actuator` + the `actuator` capability (deferred rejection removed; the old rejection test flipped to acceptance in lockstep); the host runs an actuator to a **`proposed`** result (the proposal on `output`) and **never an inline side effect** (a malformed proposal is a plain `error`). Proposing is safe + opts in via the off-by-default `actuator` capability; `allow_actuators` is retained, reserved for gating *execution* (HS-37-04). Default path byte-identical (no actuator registered; routing tests green); suite 2040/15; modules ruff+F821 clean. Next: HS-37-02 (proposal persistence + lifecycle). **HS-37-02 shipped (2026-06-04):** a durable home for proposals — a new `holdspeak/db/actuators.py` `ActuatorRepository` (`db.actuators`) with `actuator_proposals` (idempotent on `idempotency_key`) + `actuator_proposal_audit` (a row per transition). Lifecycle as an explicit map — `proposed → {approved, rejected}`, `approved → {executed, failed}`, `failed → {approved}` retry, `executed`/`rejected` terminal — illegal transitions raise. The pipeline persists a proposal for any `proposed` run (`record_actuator_proposal`; dormant until an actuator is dispatched in HS-37-05); `proposed` added to `PLUGIN_RUN_STATUSES` in lockstep. Canonical schema snapshot regenerated (+2 tables/+3 indexes); 13 new repo tests; suite 2052/15; db package ruff-clean. Next: HS-37-03 (approval surface). **HS-37-03 shipped (2026-06-04):** the approval surface — `GET /api/meetings/{id}/proposals` (a pure DB read) + `POST …/proposals/{id}/decision` (`approved`|`rejected` → `transition_proposal`, records `decided_by` + audit; illegal transition → 400; unknown/other-meeting → 404), which **never executes** (HS-37-04 owns that). A "Proposed actions" Signal card stack in the meeting modal (`history.astro` + `history-app.js`): typed status pill + reversibility, a `CommandPreview`-style preview of `action → target` + the human preview + the exact machine payload, and Approve/Reject controls (guard: "nothing runs without your approval"); decided rows update in place, rejected is terminal/quieted. 7-case API test; `evidence/approval_surface.png`; bundle rebuilt; suite 2059/15. Next: HS-37-04 (guarded executor + audit + governance gate). **Phase 37 grew to 7 stories (2026-06-04, user ask):** documentation was promoted from a closeout footnote to a **dedicated story (HS-37-06 — Actuator documentation)** that updates all relevant project docs (a `docs/PLUGIN_AUTHORING.md` Actuators section + the public README + a doc-truth reconciliation so no live doc still calls actuators deferred); the closeout renumbered to HS-37-07 and now verifies the docs rather than writing them. **HS-37-04 shipped (2026-06-04):** the guarded executor — `holdspeak/plugins/actuator_executor.py` `ActuatorExecutor.execute(proposal_id)` runs an *approved* proposal through a guard stack: (1) status (only `approved`), (2) **policy gate** (`MeetingConfig.allow_actuators` master switch + an optional per-project `allowed_actuators` allow-list; a refusal raises with no state change), (3) **payload parity** (a wrong approved-hash aborts to `failed`, no outbound call — TOCTOU guard), (4) egress via an **injected** connector (this module never opens a socket; HS-37-05 supplies the Phase-25-gated one), (5) **audit** (`executed`/`failed` via `transition_proposal`, payload hash in the audit detail). Default-safe config (off + empty allow-list ⇒ nothing runs). 14 executor/config tests (all with a spy connector → no real outbound call); suite 2073/15; modules ruff+F821 clean. Next: HS-37-05 (reference actuator end-to-end). **HS-37-05 shipped (2026-06-04):** the first concrete actuator proves the whole loop — `holdspeak/plugins/builtin/followup_ticket_actuator.py` `FollowupTicketActuator` (`kind=actuator`, cap `["actuator"]`) proposes a follow-up ticket for the first **unowned** action item (faithful preview + Markdown payload; never reaches out), `build_outbox_connector` writes it to a local **outbox file** (the egress point, CI-safe), and `register_followup_actuator` registers it opt-in (**not** in `register_builtin_plugins`). The gh/jira default was dropped (the existing `github_cli` pack is read-only by Phase-25 policy + real `gh issue create` needs creds/makes real tickets); a local outbox is a real, observable, reversible side effect, with gh/jira/webhook as future connectors on the same executor contract. 7 tests: faithful proposal, nothing→error, capability-off→blocked, the **full loop** (propose→approve→execute→a real file on disk→audit) + the negatives (no-approval/gate-off/not-allow-listed→no file), and `ACTUATOR_ID not in register_builtin_plugins`. Suite 2080/15; module ruff+F821 clean. Next: HS-37-06 (actuator documentation). **HS-37-06 shipped (2026-06-04) — the dedicated docs story (user ask):** `docs/PLUGIN_AUTHORING.md` gains a full **Actuators** section (the propose → approve → execute flow, the `ActuatorProposal` field table, the lifecycle + the three gates [capability / human approval / `MeetingConfig`], the `ActuatorExecutor` parity+audit, and a worked example on `followup_ticket_actuator` linked to the real modules + the e2e test), and its four stale claims are reconciled; the public `README.md` plugin section gains an actuators paragraph (third kind, approval-gated, off by default, the reference actuator). `docs/evidence/**` frozen; MIR-01's "disabled by default" left (still true). Doc drift-guard + link-check green; suite 2080/15. Next: HS-37-07 (closeout + final-summary). **HS-37-07 — Phase 37 CLOSED ✅ (7/7) (2026-06-04):** the closeout — an **egress-posture review** (no path egresses without a registered+capability-enabled actuator, an `approved` proposal, the `allow_actuators`+allow-list gate, payload parity, and an audit row; the negative proven by the reference-actuator tests) leads `final-summary.md`; demo `evidence/actuator_lifecycle.png` (awaiting-approval → executed-with-audit-trail → rejected). Full suite **2080/15**; actuator stack **55** tests; routing invariants **18** (byte-identical default); doc-guards green; bundle rebuilt (gitignored `_built` — 0 tracked). **Phase 37 is the local branch `phase-37/hs-37-01-actuator-contract` (open + 7 story commits + the docs-story roadmap commit) — open a PR to `main`.** **Phase 37 merged to `main` via PR #14 (all 6 CI checks green). Phase 38 — Actuators II opened + scaffolded (2026-06-04):** on user direction (chosen over Release/First-Run, Dogfood/Reliability, UX-consolidation), build on the proven-safe actuator machinery — 6 stories: HS-38-01 gated **write-connector framework + permission manifest** (routes every outbound call through `connector_runtime.PermissionGate`) → 02 **GitHub** write connector (`gh issue create`, `shell:exec`) → 03 **webhook** write connector (HTTP POST to an allow-listed host, `network:outbound`; covers Slack/Teams) → 04 **live in-meeting proposals** (broadcast via `WebRuntime.server.broadcast` + a live approve/reject panel) → 05 **documentation** (dedicated docs story) → 06 closeout. The Phase-37 invariant is unchanged; every connector adds a *narrower* gate; actuators stay off + unregistered by default (routing byte-identical) and the default suite makes no real outbound call (injected runners/clients). Plan: `phase-38-actuators-ii/current-phase-status.md`. HS-38-01 is the entry point. **HS-38-01 shipped (2026-06-04):** the gated write-connector framework + permission manifest — `holdspeak/plugins/gated_connector.py`: `WriteConnectorManifest` declares one egress permission (`shell:exec`/`network:outbound`) + a concrete allow-list (argv prefixes / hosts; empty admits nothing), and `build_gated_connector(plan, interpret, …)` enforces **plan → allow-check → gate → interpret** per proposal — a refused op raises `ConnectorOperationRefused` *before* any egress, an admitted op routes through the existing `connector_runtime.PermissionGate` (reused, not a second egress primitive). The Phase-37 `ActuatorExecutor` is unchanged; nothing is registered (routing byte-identical); the default suite makes no real outbound call (injected runner/opener/spy gate). 12 new tests; suite **2092 passed, 15 skipped**; ruff + F821 clean. Next: HS-38-02 (GitHub `gh issue create` connector — the `shell:exec` reference on this framework). **HS-38-02 shipped (2026-06-04):** the GitHub write connector — `holdspeak/plugins/builtin/github_issue_actuator.py`: `GithubIssueActuator` proposes a GitHub issue for the first unowned action item (payload `repo`/`title`/`body`; pure `run()`), and `build_github_issue_connector(runner=…)` runs **`gh issue create` and only that** via `build_gated_connector` (`shell:exec` through `PermissionGate`, manifest-allow-listed). The argv is built from the stored payload and run without a shell (a hostile payload value stays an inert argument — no injection); a non-zero `gh` exit raises → `failed` + audit; the created issue URL is returned. A host-side gated connector the executor injects (not a discovered pack); opt-in + off by default. 12 new tests (full loop with an injected runner — no real `gh`); suite **2104 passed, 15 skipped**; ruff + F821 clean. Next: HS-38-03 (webhook connector — the `network:outbound` reference: HTTP POST to an allow-listed host). **HS-38-03 shipped (2026-06-04):** the webhook write connector — `holdspeak/plugins/builtin/webhook_post_actuator.py`: `WebhookPostActuator` proposes an HTTP POST `{url, body}` (url from `context["webhook_url"]`; pure `run()`), and `build_webhook_connector(allowed_hosts=…, client=…)` POSTs **only to an allow-listed host** via `build_gated_connector` (`network:outbound` through `PermissionGate.open_outbound_socket`). An off-list host is refused before egress (the HTTP client is never invoked); a non-2xx / transport error raises → `failed` + audit; the status is returned. The host-granularity HS-38-01 deferral is **resolved** — `MeetingConfig.webhook_allowed_hosts` (default-empty ⇒ nothing posts). Both gate kinds (`shell:exec` + `network:outbound`) are now proven on the framework. 13 new tests (full loop with an injected client — no real HTTP); suite **2117 passed, 15 skipped**; ruff + F821 clean. Next: HS-38-04 (live in-meeting proposals — broadcast `actuator_proposed` + a live approve/reject panel). **HS-38-04 shipped (2026-06-04):** live in-meeting proposals — `process_meeting_state` gained an `on_proposal` callback (best-effort), `MeetingSession._emit_actuator_proposal` emits a **read-only** `actuator_proposed` broadcast (id + lifecycle + preview only — never the egress `payload`/`result`/`error`), and `WebRuntime` already forwards it. The dashboard (`index.astro` + `dashboard-app.js`) shows a Signal "Pending actions" panel with Approve/Reject reusing the Phase-37 decision endpoint — a surface, not a new execution path; nothing runs without approval. `record_actuator_proposal` now returns the record. Default off + byte-identical (no actuator chained ⇒ no broadcast). 6 new backend tests; bundle rebuilt (gitignored); suite **2123 passed, 15 skipped**; ruff clean. Next: HS-38-05 (Actuators II documentation — the dedicated docs story). **HS-38-05 shipped (2026-06-04):** the dedicated docs story — `docs/PLUGIN_AUTHORING.md` gains a **Write connectors (the permission manifest)** subsection (`WriteConnectorManifest`, the `shell:exec`/`network:outbound` gate-mapping table, `build_gated_connector`'s plan→allow-check→gate→interpret, and both reference connectors `gh issue create` + webhook POST as worked examples) and a **Live proposals** subsection (the read-only `actuator_proposed` broadcast + the live approve/reject panel). Stale "local-only / post-meeting-only" framing reconciled (executor connector paragraph → `build_gated_connector`; outbox = "the simplest connector"; reference table + README actuator paragraph updated; webhook host allow-list documented). Doc drift-guard + link-check green; suite **2123 passed, 15 skipped** (docs-only). Next: HS-38-06 (closeout — egress-posture review + demo + final-summary). **HS-38-06 — Phase 38 CLOSED ✅ (6/6) (2026-06-04):** the closeout. The **extended egress-posture review** leads `final-summary.md` — no write path reaches an external system without a registered+capability-enabled actuator, an `approved` proposal, the `allow_actuators`+allow-list gate, payload parity, **the connector's permission manifest + `PermissionGate`** (new this phase), and an audit row; the negatives (execute-before-approval / a refused `gh repo delete` op / an off-list webhook host) proven to egress nothing. Demos committed: `evidence/actuator_write_loop.md` (propose→approve→execute→audit, exactly one `gh` call) + `evidence/live_pending_actions.png` (the live "Pending actions" panel). Routing invariants green (**38**); actuator stack **91**; full suite **2123 passed, 15 skipped**; bundle rebuilt (gitignored — 0 tracked); doc-guards green. `final-summary.md` written; README phase row → done; HANDOVER refreshed. **Phase 38 is the local branch `phase-38/hs-38-01-write-connector-framework` (scaffold + 6 story commits) — open a PR to `main`.** **Phase 38 merged to `main` via PR #15; Phase 39 — Dictation Copilot Depth opened + scaffolded (2026-06-05):** on user direction (feature work; the "Dictation Copilot depth" track over Actuators III and Artifact→action bridges), make the daily-driver intelligent-typing loop deeper and self-improving — 7 stories: HS-39-01 **multi-pass rewriting** (`rewrite_passes`, draft→critique→refine, latency-budget-aware) → 02 **session correction memory** (a bounded in-process store nudges the router + threshold; no DB) → 03 **model-assisted target detection** (opt-in LLM fallback below the heuristic confidence threshold; override still wins) → 04 **project-doc suggestion quality gate** (dedup vs the existing `.hs` doc + dismissal-recurrence suppression + consolidation) → 05 **depth telemetry** (per-stage p50/p95 + budget guidance + multi-pass/correction visibility on `/api/dictation/readiness`) → 06 **documentation** (dedicated docs story) → 07 **closeout** (real `.43` dogfood + before/after + final-summary). Grounded in the live `holdspeak/plugins/dictation/` package + DIR-01 spec; the DIR-01 invariant is unchanged (off by default; always typeable; pipeline-disabled byte-identical; no real LLM/network call in the default suite). Plan: `phase-39-dictation-copilot-depth/current-phase-status.md`. **HS-39-01 done 2026-06-05:** multi-pass rewriting — `DictationPipelineConfig.rewrite_passes` (default 1, validated 1–5); `ProjectRewriter` runs a draft→critique→refine loop (`_default_refine_prompt_builder`) gated on the projected `max_total_latency_ms` budget (an extra pass is skipped before it would breach it) and **fails open to the best successful draft** on a refine failure/empty/over-budget result so multi-pass never regresses below single-pass; per-pass timing+counts on `StageResult.metadata` (surfaced verbatim by the dry-run); wired through `assembly.build_pipeline`. Default (`rewrite_passes=1`) byte-identical; suite 2132/15 (+9: 5 rewriter + 4 config); touched files ruff-clean. **HS-39-02 done 2026-06-05:** correction memory (session learning) — a new `holdspeak/plugins/dictation/corrections.py` `CorrectionStore` (bounded ring, thread-safe, gist-only + secret-rejected) owned by `MeetingWebServer` and shared with the live `WebRuntime`; `POST/GET /api/dictation/corrections` capture+list; a deterministic post-classify nudge in `IntentRouter` (reinforce/redirect to a similar recent correction's known block) + `target_profile.apply_target_correction` (manual override always wins); all gated behind default-off `corrections_enabled` and threaded through `assembly.build_pipeline` + the dry-run helper + the live path. Empty/flag-off ⇒ byte-identical; suite 2157/15 (+25); ruff-clean. **HS-39-03 done 2026-06-05:** model-assisted target detection — `target_profile.apply_model_assisted_target` re-classifies a sub-threshold heuristic via the runtime `rewrite` seam (enum-validated at parse; degrades to the heuristic on any failure → never raises), behind default-off `target_detect_llm_enabled` + `target_detect_llm_below` (0.8); a manual override and a user correction both outrank it; `BuildResult.runtime` exposes the loaded runtime, wired into the dry-run helper + the live path. Flag-off ⇒ detection byte-identical; suite 2167/15 (+10); ruff-clean. **HS-39-08 done 2026-06-05 (user-requested, phase grew 7→8):** the first **real-endpoint** proof — a `ledgerline` fixture project (`.hs/` context + `src/ledgerline/**` code), `scripts/dictation_enrichment_demo.py` (real `run_enrichment` + a Signal before→after `render`), and a gated `tests/e2e/test_dictation_enrichment_e2e.py` that auto-skips with no endpoint and **passed live against `.43`** (Qwen3.5-9B-Q6, 15.07s): a 446-char ramble → a ~2.4k-char project-grounded coding task (real `idempotency_keys`/`ledger_entries`/double-entry/`src/ledgerline/**` + acceptance criteria) plus a `.hs/memory/*` suggestion (capture in `evidence/dictation_enrichment_demo.txt`). HS-39-01/02/03 were fakes-only; this is the first time Phase-39 code touched a real LLM. Hosted CI skips it (needs a LAN/self-hosted endpoint); hermetic suite 2167/16. **HS-39-09 done 2026-06-05 (user-requested, phase grew 8→9):** the dictation copilot **showcase** — the demo now fires **all four** depth features in one real `.43` run (intent-router + correction nudge ② · kb-enricher injection ④ · multi-pass rewrite ① · model-assisted target ③), with a "Features that fired" panel (incl. the honest case where the LLM classify fails and the correction rescues routing); the e2e asserts each (live, 18.77s). New public **`docs/DICTATION_COPILOT.md`** — real before/after + a feature table + **two Mermaid diagrams** (pipeline flow + target-detection decision) + config + how to run; linked from the docs index, the Intelligent Typing guide, and the root README. Doc-drift + link-check green. **HS-39-04 done 2026-06-05:** project-doc suggestion quality gate — pure helpers (`suggestion_signature` / `suggestion_already_covered` / `consolidate_suggestions`); the rewriter **dedups** a suggestion against the existing target `.hs/*.md` (`already_covered`), and a session `dismissed_suggestion_signatures` set + the dismiss route + `_store_project_doc_suggestion` stop a **dismissed** near-dupe recurring (the dry-run's `suggestion_status` surfaces the reason). Conservative 0.85 Jaccard (prefer false-surface over false-suppress); consolidation a tested helper (UI wiring deferred); no DB/auto-write. Suite 2177/16 (+10); ruff-clean. **HS-39-05 done 2026-06-05:** pipeline depth telemetry — a session `DictationTelemetryStore` (bounded ring on the server, fed via the pipeline `on_run` hook from the dry-run + live paths, since the per-instance ring resets each build) + a pure `build_depth_readiness`; `GET /api/dictation/readiness` gained a `depth` block (per-stage **p50/p95**, **budget guidance** at p95 ≥ 66% of `max_total_latency_ms`, multi-pass `rewrite_pass_ms`, correction-store state). Verified live on `.43` (3 runs → intent-router p50 3821ms, project-rewriter p50 3553ms). Suite 2186/16 (+9); ruff-clean. **HS-39-06 done 2026-06-05:** the dedicated docs story — `docs/INTELLIGENT_TYPING_GUIDE.md` §10 "Copilot Depth" documents every new knob (`rewrite_passes` / `corrections_enabled` / `target_detect_llm_enabled` / `_below`) with defaults + JSON example + opt-in posture, plus the suggestion quality gate + the `depth` readiness block; §8 reconciled for dedup/recurrence; knob names checked against `config.py`. Doc drift-guard + link-check green. **HS-39-07 — Phase 39 CLOSED ✅ (9/9) 2026-06-05:** the closeout — `final-summary.md`; a real **before/after** on `.43` (Phase-18/19 single-pass vs Phase-39 multi-pass: the depth pass *tightens* the task 1483→1430 chars, `evidence/before_after.md`); invariant re-verified (pipeline-disabled byte-identical; default suite makes no real LLM/network call); full suite **2186 passed, 16 skipped**; README phase row → done. Branch `phase-39/hs-39-01-multi-pass-rewriting` — push + open a PR to `main`.** **Phase 39 merged to `main` via PR #16 (all CI green); Phase 40 — Configuration Cockpit & Persistent Memory opened + scaffolded (2026-06-05):** on user direction ("nobody wants to frig around with files and settings"), make the whole dictation copilot configurable, observable, and memorable from the Web UI — 6 stories: HS-40-01 **settings-API knobs** (surface the four Phase-39 pipeline knobs in `/api/settings` GET/PUT) → 02 **persistent correction memory** (a `dictation_corrections` table + `DictationCorrectionRepository`; the store loads/persists, keeping the in-memory ring) → 03 **the Copilot Setup cockpit** (a Signal-styled, readiness-driven UI for every knob — no JSON editing) → 04 **memory + telemetry UI** (curate persistent corrections + render the depth p50/p95) → 05 **documentation** → 06 closeout. UX + persistence; the dictation pipeline behavior is unchanged (off-by-default invariant holds). Scaffolded with an `AGENT-BRIEF.md` (the seam map + PMO/UI rules for an executing agent); branch `phase-40/hs-40-01-settings-api-knobs` — **not started**, HS-40-01 is the entry point.** **HS-40-01 done (2026-06-05):** re-verifying the seam showed the brief was stale — the four Phase-39 knobs (`rewrite_passes`/`corrections_enabled`/`target_detect_llm_enabled`/`target_detect_llm_below`) **already** round-tripped + 4xx'd through `PUT /api/settings` (it deep-merges over `current.to_dict()` then constructs `DictationPipelineConfig(**pipeline_data)`, whose `__post_init__` is the single source of truth for the 1–5 / 0–1 bounds; `_coerce` is only on `load()`, not PUT). Real gaps closed: a **clean type-error** for non-numeric payloads (explicit `int()`/`float()` coercion mirroring the `max_total_latency_ms` block, so no raw `TypeError` leaks) + the **missing test coverage** (`TestSettingsPipelineDepthKnobs`, 12 tests: GET-includes-defaults, full PUT→GET→reload round-trip, partial-PUT-preserves-unsent, out-of-range/non-numeric → 400, rejected-PUT-doesn't-persist). The 1–5 / 0–1 bounds stay enforced by `__post_init__` (not re-implemented). Suite **2198 passed, 16 skipped** (+12). Next: HS-40-02 (persistent correction memory).** **HS-40-02 done (2026-06-05):** correction memory is now DB-backed. Added a `dictation_corrections` table + `idx_dictation_corrections_recent` to `SCHEMA_SQL` and a `DictationCorrectionRepository` (`db/corrections.py`, mirroring `db/actuators.py`) registered as `db.dictation_corrections`, and **regenerated the canonical schema snapshot** (the trap). `CorrectionStore` gained optional persistence (`repository=…`): it loads the recent set on construction (replayed oldest-first so `sequence`/recency holds) and writes through on `record`, with the in-memory ring still the fast nudge path; both DB calls are defensive so durability never blocks live typing. The repo is injected by the **live `WebRuntime`** (`_dictation_corrections_repo()` → `get_database().dictation_corrections`), **not** `MeetingWebServer.__init__` — the server uses the `get_database()` singleton, so an eager open there would force every server-constructing test onto the real `~/.local/share/holdspeak/holdspeak.db` and break `test_corrections_empty_by_default`; bare servers stay in-memory + byte-identical. Off-by-default invariant holds (persistence is additive; `corrections_enabled` still gates the routing nudge, which is byte-identical when off). 12 new tests (repo round-trip/recency/validation/delete+clear/fresh-container; store write-through/secret-not-persisted/load-on-construct/survive-restart/cap; web GET-reflects-persisted + POST-writes-through). Suite **2210 passed, 16 skipped** (+12). Next: HS-40-03 (the Signal cockpit UI).** **HS-40-03 done (2026-06-05):** the Signal **Copilot depth** cockpit. The `/dictation` runtime tab gained a `.depth-group` that sets every Phase-39 knob from the UI — a **segmented** rewrite-passes control (1–5, live `N×` badge + per-value descriptor), real **toggle switches** for `corrections_enabled` + `target_detect_llm_enabled`, and a **reveal-on-toggle** `target_detect_llm_below` slider (live value badge) — all wired to `GET`/`PUT /api/settings` with inline validation (1–5 / 0–1 before submit), a live depth summary in the meta banner, and a **"Save & test in dry-run"** jump. Rich Signal (tokens-only CSS for the segmented control / switch / animated reveal). **Also fixed a pre-existing `activateSection` bug** — every non-default tab (runtime/readiness/KB/hooks/dry-run) rendered **blank** because the `.view` `hidden` attribute was never cleared (`.view[hidden]{display:none}` outranks the base rule); proven by a HEAD screenshot, fixed by driving `hidden` itself. Full UI round-trip verified via Playwright (set in UI → Save → reload → controls + on-disk config both reflect it) + a page-surface test for the four control ids. Bundle rebuilt; only `web/src` committed (no `_built/`). Screenshots in `evidence/`. Suite **2211 passed, 16 skipped** (+1). Next: HS-40-04 (memory + telemetry UI).** **HS-40-04 done (2026-06-05):** a Signal **Memory** tab on `/dictation`. The **curation** panel lists the now-persistent corrections as elevated cards (kind chip · gist · → value · relative time · `×` remove), with an add form, a Forget-all clear, and an in-context `corrections_enabled` toggle that PUTs `/api/settings`. The **depth-telemetry** panel renders the readiness `depth` block — runs/budget/corrections stat tiles, per-stage **p50/p95** rows with a proportional bar (red at ≥66% of budget), the budget-guidance warnings, and the last **multi-pass** rewrite chips (labelled "this session"; in-memory). Backend: the API was GET/POST only, so added `DELETE /api/dictation/corrections/{id}` (remove one) + `DELETE /api/dictation/corrections` (clear all, ring + durable) and `CorrectionStore.list_for_display`/`remove`/durable-`clear`; GET now carries each item's durable `id`+`created_at` so the UI can address rows. The dictation route-table lock was updated 28→30. Playwright-verified (seeded 3 corrections + 3 telemetry runs → 3 cards / 3 stage rows / 2 pass chips / the red 75%-of-budget guidance; `×` removed a card 3→2). Bundle rebuilt; only `web/src` committed. Screenshot in `evidence/memory_panel.png`. Suite **2221 passed, 16 skipped** (+10). Next: HS-40-05 (documentation).** **HS-40-05 done (2026-06-05):** the docs now lead with the web cockpit. `docs/INTELLIGENT_TYPING_GUIDE.md` §10 + `docs/DICTATION_COPILOT.md` "Turn it on" present `/dictation → Runtime → Copilot depth` + the Memory tab as the primary setup path (with the cockpit + memory-panel screenshots under `docs/assets/cockpit/`), and the `config.json` block is demoted to an "Advanced / headless" fallback. Also a **doc-truth fix**: the correction-memory wording said "in-memory, never persisted" — corrected to **DB-backed, persists across restarts** (HS-40-02), with the `/dictation → Memory` curate/clear walkthrough + the depth-telemetry panel documented. Every documented knob cross-checked against `config.py`. Doc drift-guard + link-check green. Next: HS-40-06 (closeout — dogfood + final-summary + PR/merge).** **HS-40-06 — Phase 40 CLOSED ✅ (6/6) (2026-06-05):** the closeout. A **UI-only dogfood** (`scripts/dogfood_cockpit.py`) configured the copilot **entirely in the web UI** (pipeline on · 3 rewrite passes · corrections on · infer-target on), recorded a correction in the Memory tab, then booted a **fresh server over the same config + DB** (a simulated restart) — **both the config and the correction persisted** (`evidence/dogfood_post_restart.png`: the Memory tab post-restart shows `learning: on · remembered: 1` + the persisted card; telemetry correctly reset, it's session-scoped). Invariant re-verified (off-by-default byte-identical — `test_intent_dispatch`/`test_intent_router` + the no-repo `CorrectionStore` path, 25 green; the canonical schema snapshot matches a fresh build). Bundle rebuilt; **0** `holdspeak/static/_built/` files tracked. Full suite **2221 passed, 16 skipped** (2186/16 at phase open; +35). `final-summary.md` written; HANDOVER + README refreshed. **Phase 40 is the local branch `phase-40/hs-40-01-settings-api-knobs` (scaffold + 6 story commits) — open a PR to `main` and merge when CI is green.**). **Phase 40 merged to `main` via PR #18 (all CI green); Phase 41 — Runtime Presence Indicators opened + scaffolded (2026-06-05):** on user direction (a real desktop presence indicator so you can see what the copilot is doing while dictating), build an **opt-in**, per-platform **native** presence layer behind one `runtime_activity` contract + a pluggable renderer seam. Salvages the good bones of codex PR #17 (contract / Protocol / Signal web card) and **rejects its Tk renderer** (can't meet the Signal bar; risks stealing dictation focus; reintroduces the Phase-32-retired desktop UI). User-locked: the rich HUD is a **frameless webview of the Signal card** (local `/presence` route, live over the existing WS — one design); **native on both OSes** (macOS `NSStatusItem` + non-activating `NSPanel`; Linux D-Bus notification + StatusNotifierItem tray + GTK-WebKit overlay where allowed); focus-invariant + Wayland-GNOME/KDE reality designed in; off by default, native deps optional. 7 stories: contract → web card → renderer seam + `/presence` → macOS → Linux → docs → closeout. **HS-41-01 done:** the pure `runtime_activity` contract + tracker + window policy ported clean (no Tk/deps). **HS-41-02 done:** the lifecycle→activity mapping + the `runtime_activity` websocket broadcast + a live Signal presence card on the dashboard (zero deps). **HS-41-03 done:** the desktop seam (`PresenceRenderer` Protocol + `DesktopPresenceHost` + Wayland-aware `detect_presence_platform` + flag-gated host builder, re-wired into `web_runtime`) + the `/presence` HUD page + a framework-free WS driver rendering the Signal card live (the native-webview content). **HS-41-04 done:** the macOS renderer — a focus-safe non-activating `NSPanel` hosting a `WKWebView` of `/presence` (native rounding/shadow) + an `NSStatusItem` glyph (PyObjC `presence` extra), proven focus-safe with live native screenshots; plus a toolchain fix (venv → uv-managed CPython 3.13.11, off the broken Homebrew bottle). **HS-41-05 done:** the Linux renderer — an in-place-updating libnotify notification (coalesced) + a StatusNotifierItem tray glyph (PyGObject), focus-safe + portable across X11/Wayland/GNOME/KDE; logic fully unit-tested + live-verified on `.43`/GNOME. **HS-41-08 done:** the Linux floating GTK-WebKit overlay (the macOS HUD's twin) — a non-focus GTK3 POPUP hosting a WebKit2 webview of `/presence`, **live-captured on `.43`/X11** (same Signal card as macOS). Both renderers + the overlay proven on real hardware. Suite **2261/16**. **HS-41-06 done:** the user-facing presence docs — `docs/INTELLIGENT_TYPING_GUIDE.md` §11 "Desktop Presence" (the `HOLDSPEAK_DESKTOP_PRESENCE=1` enable + the `.[presence]` extra + the Linux freedesktop typelibs; an activity-state table cross-checked against `runtime_activity`; the macOS HUD + menu-bar glyph and the Linux in-place notification + GTK-WebKit overlay surfaces, **each with a real screenshot from that OS** under `docs/assets/presence/`; the honest Wayland-GNOME/KDE tray-only caveat + the GNOME AppIndicator wart; the never-takes-focus invariant), cross-linked from the root README ("What it does" + "Where to go next"), the docs index, and Getting Started. Doc drift-guard + link-check (it resolves embedded image paths, so the four screenshots are proven on disk) green: 3 passed; suite unchanged (docs-only). **HS-41-07 — Phase 41 CLOSED ✅ (8/8):** the closeout — the macOS focus-safety smoke re-ran live (`frontmost before: Terminal | after: Terminal | focus_stolen: False` — `SMOKE PASSED`, fresh HUD capture under `evidence/closeout_macos/`); Linux Tier-1 (notification + tray) + Tier-2 (GTK-WebKit overlay) stand live-verified on `.43`; flag-off byte-identity re-asserted (presence + runtime-activity units 37 passed; `build_desktop_presence_host` returns `None` with the flag unset); full suite **2261 passed, 16 skipped**; **0** `holdspeak/static/_built/` files tracked; `final-summary.md` written; README row → done + pointer advanced; HANDOVER refreshed; **codex PR #17 closed as superseded**. HoldSpeak now has an opt-in, native, focus-safe **desktop presence** layer on both macOS and Linux. **Phase 41 merged to `main` via PR #19 (all 6 CI checks green); codex PR #17 closed as superseded. Phase 42 — First-Run Delight & Daily Confidence opened + scaffolded (2026-06-06):** on user direction ("the onboarding experience HAS GOT TO be stellar"), the product depth is now ahead of the product **entrance** — make **arrival** stellar. The promise: fresh clone → a **verified first dictation**, visible privacy/trust state, **zero file editing**, in one guided local cockpit — held to a measured **time-to-first-dictation (TTFD)** headline on both OSes. Built off a **repo-verified proposal** ([`PROPOSAL_PHASE_42_first_run.md`](./PROPOSAL_PHASE_42_first_run.md) — every load-bearing claim checked against live code before scaffolding). 8 stories, resequenced so the interim-shell debt (the `AppLayout` "consolidating / History → Settings" drawer) is retired **before** the new surfaces land: HS-42-01 setup-state contract (`GET /api/setup/status`, an **adapter** over `collect_doctor_checks()`/readiness/egress/presence + a durable `first_run` milestone) → 02 global settings completion → 03 welcome/setup route + CLI nudge → 04 guided **real-app** first-dictation → 05 ambient **trust chip** + panel → 06 runtime model assistant → 07 presence onboarding → 08 closeout. Locked (user): all eight in one phase; trust is ambient; `first_run` = a durable milestone; reuse-don't-duplicate (a doctor-FAIL→section drift test). Invariant: all optional features off ⇒ local-first + byte-identical. Plan: `phase-42-first-run-delight/current-phase-status.md`. **HS-42-01 done (2026-06-06):** the backend spine — `holdspeak/setup_status.py::build_setup_status` composes one `GET /api/setup/status` snapshot as an **adapter** over the existing sources (`collect_doctor_checks()` 1:1 → sections, with a drift test that every `FAIL` surfaces; `intel_egress_posture()` + config → the trust block; `detect_presence_platform()` → presence) + a single `primary_action` + `overall` verdict, **cheap** by construction (doctor gained `skip_network=True` so the cloud preflight is a neutral "not run", not a 4s HTTP probe; the CLI keeps the live preflight). The durable `first_run` is a new `milestones` table + `MilestoneRepository` (`db.milestones`) that survives a restart (canonical schema snapshot regenerated). +22 tests (composition / drift / route shape / persist-across-restart / cheapness); suite **2283 passed, 16 skipped**; ruff-clean. **HS-42-02 done (2026-06-06):** the interim "consolidating / History → Settings" drawer (the `AppLayout` shell debt) is **retired** into a real, shell-level **`/settings`** page — a Signal-styled surface (Appearance/Core/Cloud-intel cards, a "Local · 127.0.0.1" chip, inline validation, a sticky save bar) backed by `GET`/`PUT /api/settings` via a dedicated `settingsApp()`; the TopNav gear links to it, the History "Settings" tab is removed (a read-only `loadSettings` stays for the intel-alert getters), and the server `/settings` route serves the built page. **Save round-trip proven live** (set→Save→reload→disk all `37`) + a screenshot; +3 tests, 3 settings-as-tab tests updated in lockstep; suite **2286 passed, 16 skipped**. **HS-42-03 done (2026-06-06):** the first real first-run surface — a Signal **`/setup`** welcome page (brand-mark hero + a dynamic headline + a 20/22 progress bar + one bright **primary action** + a "Needs attention" list with fixes + a first-dictation CTA + a "Ready" grid of all-passing checks + Privacy/Presence summary cards), driven entirely by `/api/setup/status`; a **`/` first-run guard** that redirects to `/setup` only when `first_run|blocked` (a healthy returning user — even with WARNs — keeps the dashboard, honoring "never nag"); and a **CLI launch nudge** (`→ First-run setup: open …/setup — N things need attention`). Restrained PixelLab (one brand mark). Proven live (Playwright redirect both ways + stdout capture) + a screenshot; +5 tests; suite **2291 passed, 16 skipped**. **HS-42-04 done (2026-06-06):** the magic-moment proof — a guided `/setup` first-dictation panel (3 numbered steps + a readiness mini-row + **live `runtime_activity` WS feedback** that flips to a green "✓ It worked — text landed in your app" + the transcript on a real `dictation_typed`/`dictation_delivered`) + an honest fallback ladder; and the durable **`FIRST_DICTATION_SUCCESS` milestone** set at the two real dictation-success points in `_transcribe_and_type` (so `first_run` flips false and `/` stops nagging). Proven by 3 milestone-wiring tests (fake transcriber+typer through the real path) + a live Playwright magic-moment (broadcast → success banner + transcript) + 3 screenshots; suite **2294 passed, 16 skipped**. The real-mic leg is the HS-42-08 dogfood. **HS-42-05 done (2026-06-06):** the local-first posture is now **ambient** — a persistent shell **TrustChip** (replaced the static LocalPill default in the TopNav) shows the single highest-priority posture from `/api/setup/status` `trust{}` (**Local only · Configured endpoint · Writes need approval · Needs attention**) and opens a right-side **Trust & Privacy panel** answering "what can leave this machine?" (web bind/auth · transcript egress + the configured endpoint · actuators (approval-gated) · webhook allow-list · presence), with a Settings link. Pure exported `trust-view.js` (`trustPosture`/`trustRows`) verified by a **Node harness (5 posture cases)** + a **live two-config Playwright** check (Local only vs Configured endpoint) + 2 screenshots; +2 committed shell tests (3 settings/identity tests updated in lockstep); suite **2296 passed, 16 skipped**. **HS-42-06 done (2026-06-06):** a guided **model-setup assistant** on `/setup` — the four backend choices (**Basic voice typing · Local Apple Silicon (MLX) · Local GGUF (llama.cpp) · OpenAI-compatible endpoint**) each with what it affects/needs + a **copyable install command**, a one-click **Test my runtime** (`holdspeak/setup_runtime.py::probe_runtime` → `resolve_backend` + a model-path-exists check for local backends; a **time-boxed, injectable `{base_url}/models` preflight** for OpenAI-compatible), and an **Advanced runtime settings** link into the existing `/dictation` cockpit (the assistant guides, doesn't replace it). `POST /api/setup/runtime-test`. 8 tests (injected seams — no real network call / no model load) + a live screenshot; suite **2304 passed, 16 skipped**. **HS-42-07 done (2026-06-06):** presence onboarding on `/setup` — a guided step driven by the `/api/setup/status` `presence{}` block: a status pill (On · Available · off · Not on this platform), an **honest per-platform tier** line (macOS HUD + menu-bar glyph · Linux X11/wlroots HUD + tray + notification · Wayland GNOME/KDE tray + notification only), the **focus invariant** in one line, **copyable enable commands** (`HOLDSPEAK_DESKTOP_PRESENCE=1` + the `.[presence]` extra + the Linux typelibs), a **faithful inline HUD preview**, and Presence-docs / Preview-the-live-HUD links. 2 tests + a screenshot; suite **2306 passed, 16 skipped**. **HS-42-08 — Phase 42 CLOSED ✅ (8/8):** the closeout — the **TTFD dogfood** (`scripts/dogfood_first_run.py` → `DOGFOOD OK`: launch→/setup **1.13s**, first-dictation confirmed via the live magic-moment, returning-user → dashboard (no nag), **zero file edits**; `evidence/first_run_dogfood.txt`), **Getting Started** rewritten to lead with the guided `/setup` path (hand-edited config demoted), `final-summary.md`, doc-guards + link-check green, full suite **2306 passed, 16 skipped**, **0** `_built/` tracked. A user now goes fresh-clone → guided `/setup` → a verified first dictation with **no file editing**, with the privacy posture visible the whole way. Branch `phase-42-first-run-delight` — open a PR to `main` + merge when CI green.
**Current phase:** **Phase 52 — Voice Command Macros on a carved dispatch seam: CLOSED (7/7)** ([final-summary](./phase-52-voice-macros/final-summary.md)) from [backlog](./BACKLOG.md) candidate **B** (a voice command launcher) with a scoped slice of **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) on a dedicated `/commands` board, and it fires when they speak the keyword while dictating, through the reused actuator guarded execution, on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Off by default and byte-identical when off; the user owns the risk (configuring is consent, no per-fire prompt) and the blast radius is bounded (deterministic match + a per-macro manifest). The board was designed UI-first and screenshot-verified; full suite 2499 passed; PR to `main` merged on green. The dogfood fired a real shell command end to end. **No follow-on required.** Earlier this phase: **HS-52-06 (docs) done:** `docs/VOICE_COMMANDS.md` (linked in the docs index under Dictate) documents the feature honestly in product-tense; the Phase-51 roadmap-vocabulary guard now scans this Phase-52 doc and passes, `humanizer` run, no em/en dashes. **HS-52-05 (the Voice Commands board) done:** the centerpiece. A dedicated `/commands` route with a card-per-command grid (per-kind color edges/badges, a live "what fires" preview per card, the honest shell danger treatment, a per-card Test button via `POST /api/commands/test`, a per-kind adaptive editor with match hint + conflict warning, an inviting empty state). Built UI-first to the design doc and screenshot-verified (4 PNGs; a screenshot pass caught a real `[hidden]`-override CSS bug). **HS-52-04 (dispatch wiring) done:** the feature works end to end (Python side, off by default): `dictation_runner.dispatch_voice_command` sits at the top of the carved seam, matches the whole utterance deterministically, fires the bounded connector and types nothing, surfacing "command: <keyword>" as a runtime activity; `web_runtime` returns early on a fire (byte-identical when off / no match). The non-meeting persistence wrinkle was resolved honestly: the actuator table is meeting-FK'd, so a fire reuses the guarded execution but is audited via the activity broadcast + log, not that table. **HS-52-03 (local action connectors) done:** `plugins/voice_macro_connector.py` reuses the Phase-38 gated-connector framework: `open_url`/`launch_app`/`shell` run bounded subprocesses behind a per-macro manifest (a connector built for one command refuses any other before egress), `type_text` types via an injected writer; `allow_actuators=False` blocks before the connector runs. **HS-52-02 (macro model + config) done:** `VoiceMacro` = keyword + a deterministic action (`open_url` / `launch_app` / `shell` / `type_text` + one payload), with a single-source `preview()` and a normalized whole-utterance `matches()`; `MacrosConfig` (off by default) round-trips config-version-safe through `/api/settings` with a clean 400 on a malformed macro. **HS-52-01 (scoped-E carve) done:** the dictation orchestration carved into `holdspeak/dictation_runner.py`, byte-identical. Full suite 2480 passed. Designed UI-first as a dedicated `/commands` board. from [backlog](./BACKLOG.md) candidate **B** (re-envisioned by the user as a voice command launcher) paired with a scoped slice of candidate **E**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through the existing actuator guarded executor (Phase 37/38), reused with new local connectors; configured macros are auto-approved (the user owns the risk), off by default, deterministic and bounded (the keyword selects a pre-configured action; the transcriber never composes a command), every fire audited. Landed on a dispatch seam carved out of the 2,341-line `web_runtime` god-object. Seven stories (carve -> model+config -> connectors -> dispatch -> editor UI -> docs -> closeout). Read [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md) first. Prior: **Phase 51 — Public-Docs Hygiene: CLOSED (5/5)** ([final-summary](./phase-51-public-docs-hygiene/final-summary.md)) from [backlog](./BACKLOG.md) candidate H, the cheap release-facing follow-on to the Phase-50 gate. The deeper user/operator guides leaked the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README was already clean. All five stories shipped: a classified leak inventory, a scrub of all 6 in-scope guides into product-tense (keeping product nouns `actuator` and named specs `MIR-01`/`DIR-01`), a case-insensitive doc-drift guard scoped to user-facing docs (`README` + non-recursive `docs/*.md`; internal/evidence/assets out), the rule codified in `DOCS_STYLE.md`, and a closeout dogfood proving the guard both ways. Docs-and-test only, behavior-preserving. Full suite green (2454 passed, 17 skipped), PR to `main` merged on green CI. **No follow-on required;** the strongest remaining backlog bet is **B** (voice macros), best paired with a scoped slice of **E** (the routing seam) under one thesis. Prior: **Phase 50 — Release Readiness ("cut a real 0.x"): CLOSED (7/7)** from [backlog](./BACKLOG.md) candidate C (the open-source-ship unlock). HS-50-01 (one true version), HS-50-02 (the safe-by-default schema policy, the heart of the phase), HS-50-03 (backup + restore), and HS-50-04 (doctor + config honesty) are done: `_ensure_schema` does create-fresh / no-op-equal / backup-then-apply-older / refuse-newer, `backup_database` takes a consistent SQLite snapshot before any destructive action, `holdspeak backup` / `holdspeak restore` give the user an on-demand snapshot and a safe reversal, `doctor` (and `/api/setup/status`) now report the real schema state honestly (newer DB flagged FAIL), and `Config` carries a `config_version` that coerces an older shape forward without dropping fields and flags a newer one. (The live schema is additive today, so the real holes closed are a newer DB run by an older build and the lack of a backup before any future migration.) HS-50-05 (verified clean-machine install) is done: a fresh-venv `uv pip install -e .` reaches `holdspeak doctor` exit 0 with only the expected optional warnings, version 0.2.1 resolved from real install metadata. All seven stories are done: one true version, the safe-by-default schema matrix, backup + restore, an honest doctor + config_version, a verified clean install, the upgrade/backup policy doc, and a green dogfood proving the matrix end to end. HoldSpeak is now safe to install, upgrade, and trust (the PyPI publish is a deliberate maintainer step once the gate is green). Make HoldSpeak safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (never silently destroy data on upgrade — refuse-newer, backup-then-apply-older, no-op-equal, create-fresh), a backup the user can run, a `doctor` honest about the DB/config it found, config that can evolve, a verified clean-machine install, and honest docs with an upgrade/backup policy. The next schema bump currently wipes an existing user's DB (`db/core.py:_ensure_schema`) with no backup — that is the bet. Behavior-preserving (no feature/capture/dictation/plugin/synthesis change). Read [`AGENT-BRIEF.md`](./phase-50-release-readiness/AGENT-BRIEF.md) first; HS-50-02 (the safe schema policy) is the heart, HS-50-01 (one true version) the cheapest first win. Prior: Phase 49 (Meeting Aftercare) CLOSED, PR #33.

**Previous phase:** **Phase 49 — Meeting Aftercare ("close the loop"): CLOSED (6/6)**, from [backlog](./BACKLOG.md) candidate A. The meeting result now closes its own loops, read-only + reuse, never acting without approval. The "Your next move" aftercare panel at `/history` shows what's still open (by owner), what was decided, and what changed since the last meeting (`compute_meeting_aftercare` + `GET /api/meetings/{id}/aftercare`, quiet when nothing's open/decided/changed); "show me the moment" jumps to the transcript segment that justifies a result (only on a real timestamp, focus-safe); a "File as issue" affordance turns an accepted action into a GitHub-issue actuator *proposal* through the existing propose -> approve -> execute flow (no new write primitive, off by default, human-approved, audited; an executor test proves an unapproved proposal never egresses); and "Draft follow-up" assembles a local markdown summary, preview + copy only, never sent. Capture/plugins/synthesis untouched; actuators off by default. Closed with before/after, a green dogfood, full suite green (2426/17), and a PR to `main` merged on green. See [final-summary](./phase-49-meeting-aftercare/final-summary.md). Prior: Phase 48 (Visible Learning Loop) CLOSED, PR #30.
**Status:** Plugin rollout complete (Phases 16 → 27 → 28 → 29; 14 real plugins). Remaining open work is largely hardware-gated (Phases 24, 25 HS-25-07).

**Backlog (candidate next phases):** [`BACKLOG.md`](./BACKLOG.md) — the living parking lot so post-Phase-48 ideas (meeting aftercare, voice macros, release-readiness, frontend density, activity pre-briefing, decision-point privacy) are not lost. Shipped as focused phases in sequence, not one mega-phase.

## Vision

HoldSpeak is a local, private, hold-to-talk voice typing tool for macOS
and Linux. The current product is a working voice typer with a web
runtime, meeting mode, transcription via MLX-Whisper / faster-whisper,
deferred meeting intelligence, and an emerging intelligent-typing layer
for project-aware local writing.

This roadmap exists because the project is graduating from "useful
script" to a real product. The current chapter is **local intelligent
typing**: utterances can flow through ordered, user-configurable stages
between Whisper and the keyboard, optionally using target-profile
detection, project context, agent hooks, and OpenAI-compatible local or
LAN LLM endpoints before text is injected.

## Source canon

Phase content must be grounded in these. If a phase disagrees with
canon, canon wins.

- `README.md` — public install + usage surface.
- `docs/SECURITY.md` — security & privacy posture: data classes, trust boundaries, egress points, encryption-at-rest stance (HS-25-03).
- `docs/USER_GUIDE.md` — user-facing product workflows.
- `docs/internal/PLAN_ARCHITECT_PLUGIN_SYSTEM.md` — parent RFC for the plugin system.
- `docs/internal/PLAN_PHASE_MULTI_INTENT_ROUTING.md` — sibling phase: meeting-side multi-intent routing (MIR-01).
- `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md` — DIR-01 spec for the dictation pipeline.
- `docs/internal/PLAN_PHASE_WEB_FLAGSHIP_RUNTIME.md` — web-first runtime migration.
- `docs/internal/PLAN_ACTIVITY_ASSISTED_ENRICHMENT.md` — local activity assisted enrichment design.
- `aipi-lite/` — first-class AIPI-Lite firmware, bridge, docs, tests, and imported device-side roadmap.
- `pyproject.toml` — package contract; the source of truth for installable extras.

## Phase index

| Phase | Goal (one line) | Status | Folder |
|---|---|---|---|
| 0 | Project + roadmap setup; packaging hardening | done | [phase-0-setup](./phase-0-setup/) |
| 1 | DIR-01: Dictation intent routing + transcript enrichment pipeline | done | [phase-1-dictation-intent-routing](./phase-1-dictation-intent-routing/) |
| 2 | MIR-01: Meeting-side multi-intent routing + artifact synthesis | done | [phase-2-multi-intent-routing](./phase-2-multi-intent-routing/) |
| 3 | DIR-01 loop closure: project context, llama_cpp leg, runtime counters, cold-start cap | done | [phase-3-dictation-loop-closure](./phase-3-dictation-loop-closure/) |
| 4 | WFS-01 extended: audit web flagship + interactive config for blocks / project KB / dictation runtime / dry-run preview | done | [phase-4-web-flagship-runtime](./phase-4-web-flagship-runtime/) |
| 5 | Usability powerhouse: reduce setup/dogfood friction and make web workflows faster | done | [phase-5-usability-powerhouse](./phase-5-usability-powerhouse/) |
| 6 | Action follow-through: make meeting action items and artifacts reviewable and traceable | done | [phase-6-action-follow-through](./phase-6-action-follow-through/) |
| 7 | Local handoff exports: make reviewed meeting work portable as local Markdown/JSON outputs | done | [phase-7-local-handoff-exports](./phase-7-local-handoff-exports/) |
| 8 | Local activity intelligence: mine Safari/Firefox history metadata into a private default-on work-context ledger | done | [phase-8-local-activity-intelligence](./phase-8-local-activity-intelligence/) |
| 9 | Assisted activity enrichment: add opt-in local connectors, annotations, and meeting candidates | done | [phase-9-assisted-activity-enrichment](./phase-9-assisted-activity-enrichment/) |
| 10 | Web design system & character pass: tokens, components, identity, route rebuilds, refreshed designer handoff | done | [phase-10-web-design-system](./phase-10-web-design-system/) |
| 11 | Local connector ecosystem: reusable manifests, fixtures, first-party connector packs, and developer docs | done | [phase-11-local-connector-ecosystem](./phase-11-local-connector-ecosystem/) |
| 12 | Workbench voice: replatform tokens + components on a Workbench-evoking palette + pixel font; dashboard polish; designer handoff refresh | done | [phase-12-workbench-voice](./phase-12-workbench-voice/) |
| 13 | Connector runtime + pipelines + meeting context: turn the framework on, chain packs, surface pre-meeting briefings + cross-meeting summaries | done | [phase-13-connector-runtime-and-context](./phase-13-connector-runtime-and-context/) |
| 14 | AIPI-Lite devices: remote audio ingest substrate — `AudioSource` Protocol, `RemoteAudioRecorder`, `/api/devices/audio` WebSocket, per-device speaker labels in transcripts; same-LAN scope (cross-network is phase 15) | done | [phase-14-aipi-lite-devices](./phase-14-aipi-lite-devices/) |
| 15 | Out-and-about: cross-network reach for AIPI-Lite — tunneling (Tailscale / Cloudflare Tunnel / WireGuard candidate evaluation), TLS, per-device PSKs, paired with the AIPI-Lite firmware's portable WiFi (multi-SSID + captive portal + Improv-WiFi) on the device-side roadmap | not-started | [phase-15-out-and-about](./phase-15-out-and-about/) |
| 16 | First real synthesizer: replace `mermaid_architecture`'s `DeterministicPlugin` stub with a real LLM-backed plugin, wire the LLM capability gate, render `mermaid` artifacts as inline SVG in the web view, reality-check the plugin RFC | done | [phase-16-first-real-plugin](./phase-16-first-real-plugin/) |
| 17 | Device Initiative: device → server upstream frames (`device_health` + `query` w/ `last_segment` case) lighting up AIPI-Lite phase 4's `blocked` bridge stories; minimal web UI rendering for device health. Sibling to HS-14, paired with AIPI-4 in the AIPI-Lite roadmap. | done | [phase-17-device-initiative](./phase-17-device-initiative/) |
| 18 | Intelligent Typing Copilot: project-aware local intelligent typing with target profiles, Claude/Codex hooks, optional external-agent summarization, `.hs` context conventions, OpenAI-compatible runtimes, and web cockpit support | done | [phase-18-intelligent-typing-copilot](./phase-18-intelligent-typing-copilot/) |
| 19 | Intelligent Typing Daily-Use Hardening: safe `.hs/.../*.md` project-doc suggestions, telemetry, target-profile overrides, and real endpoint dogfooding | done | [phase-19-intelligent-typing-hardening](./phase-19-intelligent-typing-hardening/) |
| 20 | AIPI Companion: same-LAN physical companion UX for agent-waiting status, voice replies, gestures, and debug visibility | done | [phase-20-aipi-companion](./phase-20-aipi-companion/) |
| 21 | AIPI-Lite First-Class Integration: import firmware and bridge source into HoldSpeak and define the unified developer workflow | done | [phase-21-aipi-lite-first-class](./phase-21-aipi-lite-first-class/) |
| 22 | AI PI Companion UX: state model, gestures, LCD cadence, bridge display wiring, and live hardware dogfood | done | [phase-22-ai-pi-companion-ux](./phase-22-ai-pi-companion-ux/) |
| 23 | AI PI Companion UX Polish: long-prompt display, multi-session identity, preview/browse, and target confidence | done | [phase-23-ai-pi-companion-ux-polish](./phase-23-ai-pi-companion-ux-polish/) |
| 24 | AI PI Companion Productization: web companion overview, stale-session controls, public companion docs/artwork, confidence affordances, and display update cadence | in-progress (3/6) | [phase-24-ai-pi-companion-productization](./phase-24-ai-pi-companion-productization/) |
| 25 | Trust & Hardening: no silent cloud egress, web-runtime auth + bind guard, threat-model/encryption-at-rest doc, LLM-runtime thread-safety, transcription timeout, config-knob audit. Prerequisite to Phase 15. | in-progress | [phase-25-trust-and-hardening](./phase-25-trust-and-hardening/) |
| 26 | Web Runtime Decomposition: break the `web_server.py` monolith into route modules + a shared context, behavior-preserving. Fast-follow to Phase 25. | done | [phase-26-web-runtime-decomposition](./phase-26-web-runtime-decomposition/) |
| 27 | Ubiquitous plugins + spoken-meeting e2e: flip the highest-value stub plugins to real (`action_owner_enforcer`, decisions/open-questions, requirements) re-using the Phase-16 pattern, and add a real `say`→Whisper→MIR→plugins→web spoken-meeting end-to-end harness with screenshots. Continuation of Phase 16. | done | [phase-27-ubiquitous-plugins-and-e2e](./phase-27-ubiquitous-plugins-and-e2e/) |
| 28 | Plugin rollout II: round out the core meeting types — a behavior-preserving synthesis renderer-registry refactor, then flip `adr_drafter` (architecture), `milestone_planner` (delivery), and `risk_heatmap` (risk register) to real, each with a structured web render + spoken-e2e coverage. Continuation of Phase 27. | done | [phase-28-plugin-rollout-ii](./phase-28-plugin-rollout-ii/) |
| 29 | Complete the plugin rollout + public docs: flip the last seven stubs (delivery/product, incident, comms) to real → fourteen real plugins / zero stubs, and document the plugin system on the public README. Continuation of Phase 28. | done | [phase-29-plugin-rollout-iii-and-docs](./phase-29-plugin-rollout-iii-and-docs/) |
| 30 | UI/UX overhaul: retire the Amiga Workbench aesthetic and ship "Signal" — a bold, distinctive, dark-first identity (Space Grotesk / Inter / JetBrains Mono, signature orange, real depth + motion) as a full UX/IA redesign of all five web routes, design system derived with the `ui-ux-pro-max` skill. | done | [phase-30-ui-ux-overhaul](./phase-30-ui-ux-overhaul/) |
| 31 | Database decomposition: break the 5,481-line `db.py` monolith into per-domain repositories behind a thin `Database` container, **delete** the `MeetingDatabase` god-class (greenfield), and **squash** the 18-version migration ladder to one canonical schema. Data-layer twin of Phase 26. | done | [phase-31-database-decomposition](./phase-31-database-decomposition/) |
| 32 | Foundation hardening & doc truth: class-ify `web_runtime.py`, invert the meeting→web-server coupling, **retire the TUI + menubar** (web is the sole interactive runtime), converge audio ownership, add an ungated CI core-path smoke test, extract a route error helper, and reconcile stale non-PMO docs. | done (7/7) | [phase-32-foundation-hardening](./phase-32-foundation-hardening/) |
| 33 | Documentation & open-source readiness: de-prescribe/refresh the model framing (current Qwen3.5 + a `MODELS.md` bring-your-own contract), add an **Apache-2.0 LICENSE** + `pyproject` metadata, reorganize `docs/` (user-facing vs internal/historical) with an index, an OSS-grade README pass + CHANGELOG/CONTRIBUTING, and a refreshed pixel-art asset set. | done | [phase-33-documentation-oss-readiness](./phase-33-documentation-oss-readiness/) |
| 34 | Structural decomposition II: sub-split the four files that now dominate the tree (5,373 lines combined) into navigable, single-responsibility packages — `web/routes/dictation.py` + `web/routes/activity.py` into route sub-packages, and `agent_context.py` + `intel.py` into modules — behavior-preserving (import paths + route table held identical). Twin of Phases 26/31/32; discharges Phase 26's deferred finer-split follow-up. | done | [phase-34-structural-decomposition-ii](./phase-34-structural-decomposition-ii/) |
| 35 | Plugin frontier: make the meeting-intel plugin system extensible by others — a public `docs/PLUGIN_AUTHORING.md`, a plugin-pack manifest + discovery loader (mirroring connector packs; first-party + user packs), per-project enable/disable, and the incident/comms spoken-e2e scenario. Behavior-preserving for the 14 built-ins; actuators deferred to Phase 37. Discharges Phase 29's "next frontier" handoff. | done | [phase-35-plugin-frontier](./phase-35-plugin-frontier/) |
| 36 | Meeting intelligence & experience: make HoldSpeak's meeting intelligence deliver on real, messy meetings end-to-end. **Intelligence** — fix the routing weakness where a brief intent in a digression is diluted below threshold and silently lost: segment the conversation + probe each segment for intent ("fish out" intent per segment), LLM-assisted with a lexical fallback. **Experience** — Signal "elevated" artifact cards (retire the basic chips), copy-as-Markdown, overflow-safe wide artifacts. Tied together by a dynamic/messy spoken-e2e with a before/after capture (7 → 13 artifact types). Artifact data shapes unchanged. On direct user feedback. | done | [phase-36-meeting-artifact-experience](./phase-36-meeting-artifact-experience/) |
| 37 | Actuators: turn on the plugin system's third kind — an actuator proposes an external side effect (file a ticket, post a message) behind a **preview → human approval → execute** flow with an audit trail (the RFC's open question #5; intersects the Phase-25 egress posture). The invariant: no external side effect without an explicit, audited, per-action human approval; executed == previewed. | done | [phase-37-actuators](./phase-37-actuators/) |
| 38 | Actuators II: make the proven-safe actuator mechanism *useful* — **real write connectors** (`gh issue create`, webhook POST) behind a per-connector **permission manifest** routed through the connector `PermissionGate`, and **live in-meeting proposals** (broadcast + a live approve/reject panel). The Phase-37 invariant is unchanged; every connector adds a narrower gate. On user direction. | done | [phase-38-actuators-ii](./phase-38-actuators-ii/) |
| 39 | Dictation Copilot Depth: make the daily-driver intelligent-typing loop **deeper and self-improving** — **multi-pass rewriting** (draft → critique → refine), **session correction memory** (the pipeline learns from corrections within a session), **model-assisted target detection** (LLM fallback below the heuristic threshold), a **project-doc suggestion quality gate** (dedup + recurrence suppression + consolidation), and **depth telemetry**. All opt-in; default byte-identical (the DIR-01 invariant). On user direction. | done | [phase-39-dictation-copilot-depth](./phase-39-dictation-copilot-depth/) |
| 40 | Configuration Cockpit & Persistent Memory: make the whole dictation copilot configurable, observable, and **memorable from the Web UI** — surface the Phase-39 knobs in `/api/settings` + a Signal-styled, readiness-driven **setup cockpit** (no hand-edited JSON/YAML), make correction memory **persistent** (DB-backed) with a curate UI, and render depth telemetry richly. UX + persistence; pipeline behavior unchanged. On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-40-config-cockpit/AGENT-BRIEF.md). | done | [phase-40-config-cockpit](./phase-40-config-cockpit/) |
| 41 | Runtime Presence Indicators: an **opt-in** (`HOLDSPEAK_DESKTOP_PRESENCE=1`), per-platform **native** desktop presence layer so a user dictating into another app sees what the copilot is doing — one `runtime_activity` contract → a websocket broadcast → a Signal `/presence` card, hosted natively (macOS non-activating `NSPanel`+`WKWebView` HUD + `NSStatusItem` glyph; Linux libnotify notification + tray + a GTK-WebKit overlay). Focus-safe; off by default; flag-unset byte-identical. Salvages codex PR #17's bones (rejects its Tk renderer). | done | [phase-41-runtime-presence](./phase-41-runtime-presence/) |
| 42 | First-Run Delight & Daily Confidence: make **arrival** stellar — a user goes from fresh clone to a **verified first dictation**, with visible privacy/trust state and **zero file editing**, in one guided local cockpit (TTFD dogfood: launch→/setup 1.13s). A setup-state contract (an adapter over the doctor) + `first_run` milestone, global settings completion, a welcome/setup route + CLI nudge, a guided real-app first-dictation, an ambient trust chip + panel, a runtime model assistant, and presence onboarding. On user direction. | done | [phase-42-first-run-delight](./phase-42-first-run-delight/) |
| 43 | World-Class Onboarding & First-Run UX: reimagine the first run as a full-screen `/welcome` **wizard** (Welcome · live Permissions · Model picker · a celebratory first-dictation reward · a one-click presence toggle · Done), make desktop presence a **config-backed UI toggle** (kill the env var as the path; live start/stop), and **redesign Settings** (sectioned + searchable + progressive). A world-class UX layer on the Phase-42 plumbing. On user feedback. | done | [phase-43-world-class-onboarding](./phase-43-world-class-onboarding/) |
| 44 | Daily-Surface Polish: carry the Phase-43 wizard's bar to the three daily-driver surfaces (dashboard `/`, dictation cockpit `/dictation`, history `/history`), behavior-preserving — one shared premium language (ambient glow, hero grammar, contained pill navs with a solid-accent active tab, elevated rounded surfaces, reduced-motion-safe depth). Per-surface page-content tests + a live before/after. On user direction. | done | [phase-44-daily-surface-polish](./phase-44-daily-surface-polish/) |
| 45 | Dictation Memory & the Moment of Truth: give the daily-driver dictation loop the memory the meeting side already has — a persistent, private, reviewable **dictation journal** (said → routed → typed → latency), an **in-the-moment "was that right? fix it here"** correction loop that teaches without leaving your flow, and **replay** (re-run a past utterance through the tuned pipeline so "it learned" becomes tangible). Local-first; journaling is a side-channel (typed output byte-identical); focus-safe; provable remotely via dry-run. On user direction. | done | [phase-45-dictation-memory](./phase-45-dictation-memory/) |
| 46 | Documentation Excellence & the 10-Second Hook: the product outgrew its docs (13 phases since the Phase-33 doc pass). Give the documentation the lift the product got — a **doc truth audit** (claims vs live code) → a **bold README reimagining** (10-second hook + cool-facts strip, every graphic kept, repetition cut, depth linked out, materially shorter) → a **docs voice & structure system** + an elevated journey-grouped index → a **visual lift** (real UI screenshots via the Playwright scripts) → a **coverage** sweep (feature → doc matrix; nothing shipped is undiscoverable; nothing overstated) → closeout. Honest not hype; graphics stay; docs-only. On user direction (README too low on cool facts, too large/repetitive; graphics stay; other docs need a lift). | done | [phase-46-documentation-lift](./phase-46-documentation-lift/) |
| 47 | Project Knowledge: Legible & Inviting: make the "teach the copilot about this project" capability understandable. Today `/dictation` shows two adjacent jargon-named tabs — **"Project KB"** (the `.holdspeak/project.yaml` `kb:` map → `{project.kb.*}` placeholders, default `kb-enricher` stage) and **"Project Context"** (the *separate* `.hs/` Markdown files, optional `project-rewriter` LLM stage) — with no explanation of what either is or how they relate (the Phase-46 docs pass even conflated them). Settle the concept + names → in-app explainers + inviting empty states → a guided setup flow (fresh repo → working, no file editing) → a dismissible discovery nudge → docs alignment → closeout. Behavior-preserving (presentation/onboarding, not a pipeline redesign); UX bar via `ui-ux-pro-max`. On user direction ("I struggle to understand the project KB; it's UI/UX too"). | CLOSED ✅ (6/6) | [phase-47-project-kb-legibility](./phase-47-project-kb-legibility/) |
| 48 | The Visible Learning Loop ("What HoldSpeak learned"): HoldSpeak already records every dictation (the journal), learns from corrections (the Jaccard correction memory), and can replay to prove it improved — but the learning is buried in two raw tabs. Make the loop **visible** (a windowed "What HoldSpeak learned" digest with honest counts), **trustworthy** (inline "learned from N similar" signals from the same matcher that nudges routing, quiet at N=0), and a **normal ritual** (one-tap right/wrong on a result, reusing the existing correct path) → docs telling the loop end to end as the open-source differentiator → closeout. Behavior-preserving (read-only aggregation; correcting reuses the existing write path); honesty over hype; focus-safe. On user direction ("I like the 'what I learned this week' view; let's make this open-source thing happen"). | CLOSED (5/5) | [phase-48-visible-learning-loop](./phase-48-visible-learning-loop/) |
| 49 | Meeting Aftercare ("close the loop"): the meeting side has 14 plugins, artifacts, proposals, and action review, but the result is display, not follow-through. Surface what is still open / decided / changed since last meeting, jump to the transcript moment that justifies a result, turn accepted actions into issues through the existing human-approved actuator flow, and draft a copyable follow-up. Read-only aggregation + reuse of the actuator system; never acts without approval. | CLOSED (6/6) | [phase-49-meeting-aftercare](./phase-49-meeting-aftercare/) |
| 50 | Release Readiness ("cut a real 0.x"): 49 phases of features, never cut as a real release. The DB is SCHEMA_VERSION=1 and the next bump silently wipes a user's data; the in-code version disagrees with the package version; doctor never checks the DB; the install pins git HEAD. Make it safe to install, upgrade, and trust: one true version, a safe-by-default schema policy (refuse-newer / backup-then-apply-older / no-op-equal / create-fresh), a backup before upgrade, an honest doctor + config_version, a verified clean install, and an upgrade/backup policy doc. The open-source-ship unlock. | **CLOSED (7/7)** | [phase-50-release-readiness](./phase-50-release-readiness/) |
| 51 | Public-Docs Hygiene: Phase 50 cut the release gate, but the public surface still reads like an internal artifact. User/operator-facing guides leak the project's own roadmap vocabulary (`Phase NN`, `HS-NN-NN`, `PMO`, "closeout", "the current roadmap") while the root README is already clean. Strip that vocabulary from the deeper guides, rewrite phase-relative claims ("Phase 11 shipped X") into product-tense ("HoldSpeak supports X") so the meaning survives for a reader who never heard of a phase, keep legitimate product nouns (`actuator`) and named specs (`MIR-01`/`DIR-01`), and lock it with a doc-drift guard + a codified `DOCS_STYLE.md` rule so it cannot regress. Docs-and-test only, behavior-preserving. A cheap, release-facing follow-on. Net-new from a between-phases conversation ([backlog](./BACKLOG.md) candidate H). On user direction. Scaffolded + [`AGENT-BRIEF.md`](./phase-51-public-docs-hygiene/AGENT-BRIEF.md). | **CLOSED (5/5)** | [phase-51-public-docs-hygiene](./phase-51-public-docs-hygiene/) |
| 52 | Voice Command Macros on a carved dispatch seam: turn the dictation hotkey into a **voice command launcher**. A user maps a spoken keyword to a real system action (open a URL, launch an app, run a shell command, type a snippet) in the web UI; speaking the keyword fires the action instead of typing. Actions run through HoldSpeak's existing **actuator guarded executor** (Phase 37/38), reused and extended with new local connectors; a configured macro is auto-approved (the user owns the risk: configuring is consent), off by default, deterministic (the keyword selects which pre-configured action fires, the transcriber never composes a command), each macro bounded by its own permission manifest, every fire audited. Landed on a **dispatch seam carved out of the 2,341-line `web_runtime` god-object** (a scoped slice of backlog **E**) so a feature (backlog **B**) ships with a motivated refactor under one thesis. On user direction (re-envisioned from text macros to a command launcher; "B=E"). Scaffolded + [`AGENT-BRIEF.md`](./phase-52-voice-macros/AGENT-BRIEF.md). | **CLOSED (7/7)** | [phase-52-voice-macros](./phase-52-voice-macros/) |

(Status values: `planning`, `in-progress`, `done`, `paused`, `cancelled`.)

Phase 15 (cross-network reach) is gated on Phase 25 landing the web-runtime
auth + bind guard.

## Operating cadence

Per `pm/roadmap/roadmap-builder.md` §3, every shipping commit updates,
in the same commit:

1. The story file header (status flip).
2. The phase's `current-phase-status.md` story-status row + "Where we are".
3. This README's "Last updated" line.
4. Any project-canon doc touched by the story.

Per `pm/roadmap/PMO-CONTRACT.md`: the pre-commit hook gates every
commit on a fresh `.tmp/CONTRACT.md`.

## Project metadata

- **Slug:** `holdspeak`
- **Story ID prefix:** `HS` (e.g. `HS-0-01`, `HS-1-04`)
- **Greenfield?:** effectively yes — **pre-release** (HS-33-04): the `0.2.x` marker in `pyproject` is in-development, **not** a published tag, and HoldSpeak is not on PyPI. `scripts/install.sh` is the from-source install path. No committed external API surface beyond CLI flags + the `holdspeak` shell command; the standing posture is greenfield/aggressive (design freely — see memory `feedback_holdspeak_not_really_released`).

## Glossary

- **DIR-01** — Dictation Intent Routing, phase 1 of this roadmap. See `docs/internal/PLAN_PHASE_DICTATION_INTENT_ROUTING.md`.
- **MIR-01** — Multi-Intent Routing for meetings. Sibling phase, separate runtime, shares plugin contracts only.
- **Block** — a user-defined intent class with match examples and an injection template (DIR-01 §8).
- **Transducer** — DIR-01's new plugin kind: transcript-in, transcript-out stage.
