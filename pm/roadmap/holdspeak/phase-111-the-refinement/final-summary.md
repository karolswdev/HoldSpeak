# Phase 111 — The Refinement — final summary

**Status at writing:** 10/11 shipped; HS-111-10 (the walk, this
phase's exit proof) staged for the owner's sitting. The CLOSED claim
belongs to the owner.

## The charter

Phase 110 replaced the material model at the chrome level; every
program still had its web-app interior. The owner's condition,
2026-07-31: every program gets its interior RETHOUGHT — not
token-tweaked — to feel native to Signal Workbench. The measuring
question for every room: **"If this OS shipped on a CD-ROM in 2004
with this dark techy aesthetic, what would this program look
like?"** The bar at exit: no surface on the desk reads SaaS; the
whole OS answers with one voice.

## The eleven stories

| # | Story | One-line verdict |
|---|-------|------------------|
| 01 | Settings | The JSON mirror in a SaaS sidebar became the OS's own Prefs program — icon-grid drawer, authored module roster, footer receipt bar — and MINTED THE GADGET KIT the whole phase built on. |
| 02 | Speak | The web form in a void became the dictation deck: TALK momentary key, real-RMS LedMeter, STATE register, the journal a machine ledger, every toast dead into the receipt bar. |
| 03 | Meetings | The card feed became the tape catalog: SurfaceLedger history, the transcript always visible in SurfaceWell, artifacts stamped as receipts, a real 1440 overlap bug retired by construction. |
| 04 | Agents | Three stapled eras became the crew console: ONE blocked-first board, persona detail a personnel record over SurfaceTraffic, steering in gadget grammar — with the Phase-87 consent spine byte-untouched. |
| 05 | Ask | The chat app wearing 110 paint became the query console: SurfaceTraffic thread, the HS-109-04 grounding receipt finally RENDERED (GROUNDED ON N OF M + CitationChips), every fault in-flow. |
| 06 | Delivery + process | The ~450 emoji-pin flood (the phase's largest violation) became census instrumentation; the owner's PR-387 duplicate rows root-caused live as two wire bugs and fixed; ONE PaneWell seam extracted. |
| 07 | System chrome | Five parallel verb lists became ONE registry; the portaled WorkMenu killed the menubar z bug; the floor right-click EXISTS; the palette became a command deck; the list view a SurfaceLedger face. |
| 08 | Interactive elements | The two-dialect era ended: roving focus and arming deletes became kit LAW, FoldGadget/PadGadget unified their species, eleven legacy species deleted dead — the no-modals rule is structurally true. |
| 09 | Sprites | The real disease was fractional scaling, not bad art: integer rendering everywhere, the last macOS survivor (CSS gloss orb) dead, the emoji era ended, the brand mark reborn — judged on the real desk at 4x. |
| 11 | Terminal pane | The owner's xterm.js ruling real: raw ANSI peek (stripped default, hash gate intact), xterm the ONE PaneWell interior, strictly a viewer — proven read-only on a real tmux pane, byte-identical. |
| 10 | The walk | This document's proof: 79+ live shots at both viewports, every room judged, three violations fixed on the spot, the mic sweep debt burned, the rest chartered by name (below). |

## The kit the phase minted (one language, in code)

Surface species: SurfaceLedger (+useRovingRows law), SurfaceWell,
SurfaceTraffic, SurfaceStream (kept), PaneWell (with the XtermPane
interior), WorkMenu, wings as sunken beveled tabs, CitationChips,
EgressChip (the ONE egress species), the footer receipt/refusal bar.
Gadget species: CheckGadget, CycleGadget, MxRadio, StringGadget
(mic by construction; `inputRef` added in 10), PadGadget (multiline,
mic by construction), StepperGadget, PropGadget, GadgetTable (arming
deletes by default), SecretRow, FoldGadget (the ONE disclosure),
LedMeter, LampGadget, TransportKey/TransportRow. Deleted dead in 08:
Switch, Tabs, StatusPill, InlineMessage, Dialog, ConfirmAction,
Skeleton, EmptyState, ChoiceCard, Disclosure, and the sliding-switch
species desk-wide.

## Owner rulings absorbed mid-phase

- The live error-tooltip overlap during 05 ("mixed-era joke") →
  faults are in-flow, machine-verified operable in refusal state.
- The same-day charters after 05: the terminal pane (xterm.js,
  "a lot more utility-oriented"), the desktop right-click, the
  debug-behind-RAW law, the command-deck palette, and the
  commissioned power-user usability review.
- The doctrine riders on 08: roving focus is kit law; destructive
  verbs arm.
- The PR-387 duplicate-rows report → two wire bugs found and fixed
  (liveness-by-gaze; idempotence-key defeat).
- The list view ("absolute ass") → SurfaceLedger face with window
  discipline.

## The walk's verdict (HS-111-10)

Every surface at 1440x900 AND 393x852 on the live hub; read-only
(one reversible create-and-bin via the floor NEW> menu). Fixed in
the walk: the translucent Speak footer receipt bar (content bled
through the sticky bar — now the well tint rides an opaque surface),
the meetings-detail fact hard-clip (facts wrap, never clip
mid-token), the `← ALL` chrome-position stamp over the list census
at 393, and the full chrome-input mic sweep (AttentionDrawer
search + kind cycle, InfoWindow rename, SystemShade deny reason, the
⌘K palette query — all kit species with the speak-to-fill mic; the
zone-rename mic press no longer commits-and-unmounts). Honest gaps,
not faked: no printed Ask turn (.43 down — the refusal leg is the
captured proof; the printed-turn-with-receipt shot is still owed),
and no live coder session existed (the xterm well proven on the
delivery terminal, the same PaneWell seam).

## The census (mechanical, greps in evidence-story-10)

- `backdrop-filter`: 26 textual hits, **0 live blurs** (19 `none`,
  7 vars pinned to `none` in tokens.css).
- `border-left` violations: **0**.
- Emoji-as-icons in shipped tsx: **0** rendered pictographs.
- Retired-species JSX usages: **0** outside tests.
- `InlineMessage`: **0** usages (4 comment mentions).
- Naked `input/select/textarea` outside kit internals: every
  remaining site is chartered by name or already carries the mic
  (file list in evidence).

## Held for the owner's sitting

1. **The usability doctrine PROPOSAL** (12 principles,
   `pm/roadmap/holdspeak/proposals/usability-doctrine.md`) —
   awaiting ratification; its P0s are already law in code (07/08).
2. **The sprite-face grammar decision** (09, JUDGMENT.md) —
   sprite-faced chat/handshake windows would be a registry-wide
   window-glyph grammar change; not minted unilaterally.
3. **The banked brand-mark alternate** (09 assets) — the spark-mark
   variant, kept for the owner's eye.
4. **The TrustWindow lede** — two sentences of prose above the
   boundary facts vs the no-privacy-novels rule; copy-token ruling.
5. **The owed shot**: a printed Ask turn WITH the grounding receipt,
   the moment .43 is back.

## Chartered debt handoff (named, none silent)

New from the walk: DeliveryListSection (HS-94 semantic table under
List view + CompanionCore) → SurfaceLedger refit; mobile spatial
label collisions at 393 (default arrival is list — not a nit, a
layout job); SystemShade row grammar; the PM timeline wire answering
NOT FOUND for a real zero-meeting project. Carried forward by name:
palette deep-settings + meeting content search (07), InlineEditor's
native cluster (08), composer→PadGadget migrations (mics already
present), the config-hermeticity gap (the live hub's YOLO posture
leaks into one unhermetic aftercare test — the suite's one
documented deselect), multi-select, the full-undo story (doctrine
§3), the Settings holds from 01 (audio-device-list endpoint,
per-section defaults source, delivery keys under `/api/settings`).

## Suites at exit

4228 python (3465 unit + 763 integration, 1 documented deselect,
3 environment skips) + 444 web (77 files), all green on this walk's
final state.
