# Evidence - HS-100-10

- **Story:** HS-100-10 - B6: Settings and the arrival
- **Status:** done
- **Date:** 2026-07-19

## Proof

### Captured run — 2026-07-19T15:07:28Z

- **Command:** `sh -c uv run python scripts/judgment_census.py && uv run pytest -q tests/unit -k 'product or vocabulary or no_exit or copy or drift' 2>&1 | tail -1`
- **Cwd:** .
- **Exit code:** 0
- **Index-tree:** 0f889a8dc64009e05e313858ef579edb248466ef

```text
census: 14 surface keys (incl. aliases), 28 components
census: every surface and component is judged — zero omissions
54 passed, 3158 deselected in 3.26s
```

## Summary of proof

- **Studio is dead**: registry row and StudioCore deleted; the room
  menu shrank; /studio survives only as a demoted deep link landing on
  Settings. **The Runtime guide is absorbed**: Settings gained wings
  (Settings | Guide) hosting RuntimeDocsCore; the old surface key
  rides a registry alias so every old dispatch and deep link lands.
- **The arrival is two starts and one trust line** (EmptyDesk
  rewritten; headline prose deleted per Article VII; the trust line
  derives from the real egress badge). FirstWords left the legacy
  idiom (`signal-eyebrow` gone).
- **The vocabulary is ONE canon end-to-end**: the pre-Constitution
  product-language registry (docs/product-language.json) was the
  split-brain's engine — it PRESCRIBED "Persona" and BANNED "Agent".
  Amended: Agent/Agents are the product terms, Personas? joined the
  banned pattern, Agent left the guarded/ambiguous lists; the copy
  engine gained path-scoped `terms` exceptions so the Apple surfaces'
  rename is staged honestly for the HSM follow-up (registry names the
  reason). README + docs + backend user-facing error strings swept.
  "Meetings & intel" → "Meetings & intelligence". The web vocabulary
  guard's allowlist is now EMPTY and locked ("any entry is a defect").
- **The full unit suite is green: 3212 passed** (nine pins across
  product-language/copy retargeted to the ratified canon with intent
  preserved). Judgment census re-derived against the pruned registry:
  zero omissions (captured). Config walk leg green end-to-end on the
  staged hub (walk arrivals/fetches are now token-aware everywhere).
- **POSITIONING amended** per Article I.4: the Desk IS the front door;
  Speak/Agents replace the Dictation/Studio bullets; first-run is the
  Desk's two starts + one trust line. Screenshots:
  assets/hs-100-10-settings-*.png (Settings + Guide wing, 1440/393).
