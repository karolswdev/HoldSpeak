# HS-100-07 — B3: Speak

- **Project:** holdspeak
- **Phase:** 100
- **Status:** backlog
- **Depends on:** HS-100-06
- **Unblocks:** HS-100-08

## Problem

The flagship job wears nine config tabs and opens on shouting
diagnostics (trace B). Thesis §1.1: the loop IS the front face.

## Scope

- In:
  - merge `design/inset-groups-spike` onto the phase branch first —
    the material lands with the interiors, per the gate;
  - DictationCore rebuilt: opens on the speak loop (mic, utterance,
    result, Right/Wrong, correction in place); ONE readiness status
    line that only expands when something is wrong; wings = Journal,
    Blocks; Memory/Knowledge/Runtime/Hooks/Nudges behind one gear
    door as grouped setting rows;
  - the window head carries the wing segments (max three) — no tab wall.
- Out: pipeline/wire changes (contracts stay byte-identical).

## Acceptance criteria

- [ ] Opens on the job, not diagnostics; 9 tabs are gone.
- [ ] Flow budget pinned: arrival → correction taught ≤ 6 clicks, 1
      window (extends the HS-100-02 trace as a test).
- [ ] Live screenshots at 1440 and 393 against a staged hub.

## Test plan

- vitest for the core; the flow-budget walk; the affected integration
  census pins.

## Evidence required

- Suite output; the walk trace; the screenshots.
