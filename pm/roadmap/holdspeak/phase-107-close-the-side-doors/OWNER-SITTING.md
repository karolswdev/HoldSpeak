# Phase 107 — The Owner's Sitting

**Phase:** Close the Side Doors. **Machine staging:** complete —
all six functional beats pass in every session; the two latency
beats are noise-bound (analysis in
[evidence-story-07](./evidence-story-07.md)) and carry question 3
below. **The census delta, plain: 38 debt → 18 debt** (audited
40/2/38 at the Phase 106 baseline → 21/3/18 now).

## The one command

```bash
uv run python scripts/phase107_closeout_beats.py
```

Run it from the repo root with the LAN model box (`192.168.1.43`)
up. It stages all eight beats in order, prints a labeled PASS/FAIL
per beat with the receipts read back from the journal, and cleans
up after itself (including the beat-8 fence probe). Rerunnable.

## The eight beats you drive

1. **Hold-key dictation, timed** — the script replays the fixture
   WAV through the full path; your physical hold supplies the human
   segment the fixture cannot. Expect ~930 ms release-to-landed
   against a 926.3 ms baseline; see question 3 before judging the
   beat.
2. **A typed act read back** — `desktop.type_text` receipt fetched
   from the journal: hash + byte count + `direct_gesture`, never
   your text.
3. **Dictation → agent pane** through `process.input` into a real
   tmux pane.
4. **A Cadence reply** landing in a real pane through the real
   route, loop closed.
5. **A subprocess three ways** — success; exit 7 receipted as the
   child's status, not the operation's failure; a mid-flight kill
   receipted `indeterminate` and not retried.
6. **Egress with the destination in the receipt**, and a refusal
   where the socket is never called.
7. **Transcription timed** — unchanged from baseline (your hold-key
   path gained no ceremony).
8. **The fence catches a new unlisted effect site by name**, then
   goes green when it is removed.

## The three questions held for you

1. **N10-N12 (transcription's model calls):** the charter and
   HS-107-04 ratified them clause-5-exempt computation (RFC §12);
   the independent auditor reads Article XI clause 1 as naming the
   model invocation itself consequential. The register currently
   carries the CONSERVATIVE status (bypass debt). Re-ratify the
   exemption, or charter admit-and-receipt off the parse path.
2. **Wake fires vs arms:** configured `wake.action=type` fires a
   typed act; Article IV.2 says wake surfaces arm rather than fire.
   The migration preserved existing behaviour and flagged the
   tension rather than resolving it.
3. **The ~25 ms admission price:** interleaved A/B shows kernel
   admission costs ~25 ms per dictated act (type segment 163 →
   190 ms; ~2.7% of the ~930 ms path) — the price of every typed
   act leaving a receipt. Accept and re-pin the baseline, or
   charter an optimization story (durable-record work off the
   pre-landing path). No story after HS-107-02 added cost; the
   beat-1 "failures" are cross-session noise around this constant.

## What the phase leaves behind

Register: **21 total / 3 covered / 18 debt**, every remaining row
with a machine-asserted closing condition — A01-A10 await §5b
confinement (BACKLOG candidate Y), T01/T02 await universal kernel
routing, C02/C03/C05 await mandatory principals, N10-N12 await your
ruling. Clause 6 remains in force; its sunset is unmet. The kernel
spine is byte-unchanged across the whole phase.

**Your verdict is recorded verbatim in the final summary; riders are
chartered, not absorbed.**
