# Phase 112 — Enough — final summary (the sitting brief)

Chartered and built 2026-08-02, one day. The owner's verdict made
six stories; all six shipped and were proven on a genuinely fresh
HOME the same day. The exhibit set is
[assets/hs-112-05/](./assets/hs-112-05/) — 21 shots, 1440 + 393.

## What the walk proved (machine scope)

A fresh HOME booted the hub on :8123 with `first_run: true` and zero
directories. Then, in order:

1. **Seed the desk** — the first-boot face carries "Seed the desk";
   one press seeded six drawers (ADRs, Decisions, Inbox, Meetings,
   Reference, Rules) with the ADR template and Working rules filed
   (shots 01–05; the seeded floor is shot 03).
2. **Set the dial** — the Prefs Models module; the LAN `.43`
   destination added and dictation pointed at it; PROBE pressed
   against the real endpoint (shots 06, 07, 21).
3. **Hold the key** — TALK held in a real (headless, fake-device)
   browser with synthesized speech; real Whisper transcribed it and
   the delivery LANDED with the release-to-landed receipt
   (`LANDED 1335 MS`, shots 08–10).
4. **The open mic** — the latch opened one grant; the chrome lamp
   read MIC OPEN; an ambient utterance segmented by the VAD landed
   with no key touched (`LANDED 12099 MS`, shots 11–13); unlatching
   read MIC CLOSED.
5. **Refusals by name, idempotent** — aim AGENT with none awaiting:
   422 `no_awaiting_agent`, terminal; the same `delivery_id`
   replayed `deduplicated: true` (evidence-story-05 capture; shot 20).
6. **Reset to seed** — a stray note planted, the Desk module's
   armed two-press ritual fired, receipt `TOMBSTONED 11 · SEEDED …`,
   the stray gone and the seed back (shots 14–16).
7. **Real metal** — `.43` was up; an Ask through the dial's
   destination returned a real model answer attributed to
   `walk-43` (`provider: cloud, profile_id: walk-43`).
8. **393 pass** — desk, Models, Speak (shots 17–19).

## Honest notes on the record

- Two TALK legs ran with aim FOCUSED APP from the headless browser,
  so the kernel desktop-typing path fired for real on the walk
  machine and typed short transcribed phrases into the frontmost
  window. That is the flagship act landing on real metal — and a
  walk-discipline error (the aim should have been THIS FIELD for
  headless passes). Recorded, not hidden.
- Whisper mis-heard the synthesized voice in places; transcripts
  landed verbatim as heard. The pipeline was honest; the fixture
  voice is the variable.
- Shot 20 photographs the deck after the aim-cycle click missed
  (the wire-level refusal + replay is the captured proof of that
  leg); the UI refusal rendering rides the owner's sitting.
- The owner's live hub (:55793, real data) runs the same merged
  build; the owner's own play is the second walk.

## Debt chartered by name (rides BACKLOG / next phases)

- Two audio stacks (browser vs host) and the twin preview-token
  stores; the companion tap-toggle still labeled "Hold to speak".
- `journal_source` is 2-valued; the transcribe leg has no
  `delivery_id`; macro dispatch bypasses `deliver()`.
- VAD tuning is code-level by deliberate ruling (a dial would add
  configuration surface); no per-device calibration.
- The legacy key env does not carry into migrated targets
  (per-destination key rule) — a real-OpenAI user re-enters once.
- `intel_provider` (local/cloud/auto) is still config-only.
- Docs drift named by the 04 pass: WEB_DESK.md omits the mic lamp;
  welcome.png depicts a dead surface.
- Cleanup owed on the dev machine: stale hub processes on ports
  8400/8817/8797/55798.

## Held for the owner

- The sitting itself: drive the three moves on the real desk, judge
  the seeded floor's composition, and rule on the FOCUSED APP
  default aim (should the deck default to THIS FIELD?).
- The Phase-111 printed-Ask-turn IOU: `.43` is back; the wire leg
  is proven here — the Ask-room shot rides the sitting.
