# HS-102-07 — Closeout (the owner's sitting)

- **Project:** holdspeak
- **Phase:** 102
- **Status:** backlog
- **Depends on:** HS-102-01, HS-102-02, HS-102-03, HS-102-04, HS-102-05, HS-102-06
- **Unblocks:** —

## Problem

The phase closes on the owner's felt verdict over the six refit
surfaces, not on a green sweep alone (Article IX.4).

## Scope

- In: the assembled chain (all walk legs incl. the grown smoke /
  meetingflow / geometry assertions, chrome + storm HEADED, full
  pytest with the metal exclusion, web vitest, token gate,
  vocabulary + interior-canon guards), a staged sitting from merged
  main, the verdict recorded verbatim.
- Out: new features.

## Acceptance criteria

- [ ] Machine proof green AND the owner's sitting verdict recorded.
- [ ] Each of the six stories' "Design direction" section (added
      2026-07-21) is checked against the SHIPPED surface, not just its
      acceptance criteria — a story can pass its own checklist while
      quietly drifting from the cited component (a hand-rolled
      lookalike instead of the reused `Surface.tsx` kit piece). Name
      any drift found; it blocks the sitting's pass verdict.
- [ ] No new kit fork: grep the diff for a second implementation of
      any component named in a Design direction section
      (`SurfaceBay`, `SurfaceLibrary`, `EditInPlace`,
      `RuntimeDestination`, `SurfaceGroup`/`SurfaceSettingRow`,
      `Material`, `SurfaceStream`) — one owner per shape, per the
      canon's own rule 2.

## Test plan

- The full chain; the sitting.

## Evidence required

- The chain output; the verdict.
