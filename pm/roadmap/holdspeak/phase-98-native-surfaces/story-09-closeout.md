# HS-98-09 — Closeout: the native walk

- **Project:** holdspeak
- **Phase:** 98
- **Status:** done
- **Depends on:** HS-98-01..08
- **Unblocks:** —

## Problem

Per-story proofs are not the phase proof. The claim "one visual
product" is only true if every window, opened on the real hub, wears
the idiom at both viewports — and someone looked.

## Scope

- In:
  - a `surfaces` walk leg: open EVERY registered surface on the
    production bundle, assert zero forbidden page classes in the live
    DOM, shoot each window at 1440 and the sheet form at 393;
  - the assembled walk chain (all Phase 95 legs + the Phase 97 grammar
    chain + surfaces) green with zero failed API responses;
  - the storm re-run within the Phase 95 envelope (the idiom must not
    cost frame time);
  - axe pass on open windows;
  - UAT: campaign scenario updated so the owner's next sitting sees
    the native surfaces; conductor phase-count pin bumped; ledger
    regenerated;
  - phase close: final-summary, README, memory of record.
- Out:
  - new features.

## Acceptance criteria

- [ ] Surfaces leg green: every surface opened, zero page classes in
      live DOM, shots at 1440/393 LOOKED AT and named in evidence.
- [ ] Assembled chain green, zero failed API responses; storm within
      envelope; axe clean.
- [ ] `npm run check` + full python sweep green; ledger regenerated;
      campaign row updated.
- [ ] final-summary.md records riders and the Living World handoff.

## Test plan

- The full walk chain + surfaces leg on the production bundle against
  a staged real hub; storm; axe; `npm run check`; full sweep.

## Evidence required

- Walk output, shot inventory, storm numbers, sweep output.
