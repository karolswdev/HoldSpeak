# Phase 167 - The Room in Use: final summary

**Exit, verbatim:** the owner's first real project lives in a Room on
his real desk — created, connected, watched, stewarded and written up
in one attended walk on his real GitHub and his real Jira site — and
his word is that he will use it on a Tuesday. **MET on the
orchestrator's real-desk leg** (`HS167_WALK_DB=real`, 1440, 1 passed
in 78s; the DB backed up first; his desk held ZERO projects before —
the first project it ever held was created, connected to
karolswdev/HoldSpeak and karolsaneapple.atlassian.net/KAN, activated
with real baselines, walked through a real change per source into
Review, a manual steward run with the calls receipt + the cadence set
on the face + the trigger + the same-watermark second run + ONE door
item, an update drafted and published, then ARCHIVED). THE OWNER'S
WORDS, verbatim: the design canvas — "PASS — build it."; the whole-Room
gallery — "PASS"; the attended walk — "Walk it later — close on the dry
run." (his attended leg LEDGERED PENDING; his desk is set up for it).

## The eight stories

1. **The settled design** - the drift audit (every hand-rolled block
   anchored), the design on the library (D0 spine, D1-D8 per face, D9
   species as a props table, D10 laws), counsel RATIFY-W-C with 18
   findings all paid (the wings law: TIMELINE/DECISIONS/SEARCH/ASK
   never retire; no fabricated health chip), an atlas of the library's
   resolved values, sixteen mockup artboards read at true size through
   three bounce rounds, the canvas ratified.
2. **The debts** - Jira scope persisted on the setup answers; the acli
   calls count receipted on the OBSERVE step; a cross-process acli file
   lock with a typed timeout; the cadence write wire on the policy PUT
   + MCP twin; a desk-wide steward trigger route + tool through a new
   conductor accessor. Three orchestrator catches paid in-round (a
   submit inside a React updater; a trigger route lying about project
   scope; a swallowed scheduler error).
3. **The library reform** - SurfaceIdentity, ledger `trailing` +
   `wrap`, verbs `active`, ScrollHint (one species, axis prop) in the
   barrel with contract.md; the ONE glass infrastructure for the eight
   rigs; the worker's build-first helper was theater (trusted any
   marker) → rewritten to mtime + a cross-process lock.
4. **The faces I** - the Room, the interview, the activation review,
   the GitHub wizard; three rounds; the ROOT CAUSE of every overprint
   was a species-level fixed `6ch` grid column → the `room` ledger
   template; the Review-verb-hides law restored.
5. **The faces II + the beauty pass** - Review, Update, Steward (the
   run a six-phase plan with counts and receipts; the policy a gadget
   sheet with the cadence wired; the conflict hashes folded); four
   rounds; a BUILD RACE caught (a marker newer than stale chunks) →
   the helper compares the oldest chunk; then the whole-Room beauty
   pass over all eight faces (chip geometry, dense strip/stream, the
   mold consistent) — the owner: "PASS".
6. **The Tuesday walk** - the live runner (real HOME, both DB modes,
   every step asserted on the wire and the glass at both widths, no
   conditional skips); one product defect found live (the Jira
   blockers template compiled `status in ("Blocked")` and Jira refused
   whole evaluations on boards without it); the real-desk leg run and
   archived; the owner's word recorded.
7. **The docs** - MCP_SIDECAR.md generated from the registry with a
   drift guard (five stale counts corrected); the new surface
   documented; the Rooms guide with fourteen shots and the Tuesday
   walk.
8. **The close** - this document.

## Gates (real numbers)

- Full CI-style suite (isolated HOME, `-n auto`, two halves): **21 failed
  / 7897 passed / 6 skipped in 12:00** + **3 failed / 1323 passed / 57
  skipped in 8:32** → 24 failed / 9220 passed. Sweep vs main's baseline at
  the branch base (24 names @ run 33784026156 on 31c072f5): **8 baseline
  + 15 branch-new paid at root + 1 proven flake** (the 143 model-library
  glass under load; x2 green alone). The fifteen: the canonical schema
  snapshot (the new `watch_evaluations.metadata_json` column — regenerated);
  the palette pin renamed honestly 44 → 45 and the project-family count 34
  → 35 (`project.steward.trigger`); the thread tool gate's classification
  census (the trigger classified `effect_proposal`, as run_steward); an
  ordered command-tool pin; and NINE MCP driver/commands failures with ONE
  root cause — a local `from … import get_database` inside one branch of
  the project dispatcher made the name local to the whole function
  (UnboundLocalError on every other tool path) — hoisted. On the settled
  tree: the fifteen + the close fixes + the censuses **81 passed**; the
  flake x2 passed.
- Web: vitest **789/789** (project-room + surface); baseline
  **zero branch-new**; token gate **clean**.
- Glass: the eight Rooms rigs **46 passed + 1 honest skip** (gh auth absent
  in the isolated HOME) on the honest build-first infrastructure; the live
  runner **1 passed** isolated at 1440 (82s) after the close fixes; the
  real-desk leg **1 passed** (78s).
- Counsel: **RATIFY-WITH-CONDITIONS** — 2 M + 5 S + 5 N; ALL M and S paid
  in-round with 53 tests: M-1 the thread-local calls count could leak a
  failed Jira fetch into the next watch's receipt (cleared on entry and in
  the per-watch except); M-2 ARCHIVE DID NOT STOP THE WATCHES — an
  inherited gap the real-desk walk exercised first: the archived project's
  two watches kept evaluating against the owner's real GitHub and Jira on
  the tick (mitigated on his desk within minutes: paused + unattended off,
  rows kept; then paid at the root — archive pauses the project's watches
  and disables unattended in the same transaction, restore never
  auto-resumes, list_due_watches excludes archived projects, the runner's
  finally does it before archiving); S-3 MCP configure_steward gained the
  cadence field (the one-source helper); S-7 the acli lock's fd leak on
  reentrancy (a per-thread depth counter); S-5/S-6 the last inline px in
  JiraWizard and the Room; S-4 the guide's false wings claim.

## Debts

- New, carried: the owner's attended walk (his word: later); the
  interview's suggestions still open a provider wizard that owns the
  body (by design) — no cross-face ScrollHint on the wizard well;
  `computeVerticalScrollHint` survives as a thin re-export in
  steward/model.ts (callers use the barrel); six raw `.egress-badge`
  sites outside the barrel (the EgressBadge already delegates; the
  class remains); the thread-local `_fetch_meta` carrying calls from
  the snapshot adapter to evaluate_core (a hidden coupling — a
  returned metadata shape would be honest); the Jira blockers fix
  removed `blocked_statuses` from the template's query defaults (rule
  matching unchanged; a board WITH a Blocked status now fetches wider
  — validate statuses against discovery in V1); the setup controller's
  jira_scope round-trip has python coverage only (no vitest); the
  product-copy guard's pre-existing violations (baseline on main);
  the second Jira account still fixture-only (166's debt).
- Counsel's N: MetricStrip/SurfaceStream composed by one face (by design —
  the Room's rail); raw `<button>`s with class styling survive in five
  faces (Load more, Use this repo, Save/Cancel/Edit, receipt refs, claim
  refs — a design-level call whether they become Button); three empty-state
  hints are sentences ("No updates yet. Draft the first one." etc.); the
  "Or type a repository path" label; `_ensure_build` does not notice a
  pure source deletion.
- Carried from before (re-listed): 166's counsel N (diff_snapshots'
  else-branch; the broad enrichment except; find_run_by_watermark's
  scan; the transcript's counts_match; the ActivationReview jira label
  test); 165's remaining N; 164 N-1..N-5; 163 S-4/N-1/N-3; 160
  N-5/N-1/N-2; 158 S-1/N-1/N-3; 159 seeding walls; 161 N-1.

## The arc

The SRS V0 slices P0..P7 were all on main before this phase; 167 made
them ONE Room on the library and put the first real project on the
owner's desk. Post-arc menu (his word required): the attended walk;
Gate B partner feedback; MCP-008 remote; the model-era collapse; 155
The Crew; the debt ledger.
