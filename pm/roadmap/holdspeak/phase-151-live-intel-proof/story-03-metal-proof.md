# HS-151-03 — The headless metal proof (control vs treatment)

- **Project:** holdspeak
- **Phase:** 151
- **Status:** ready
- **Depends on:** HS-151-01, HS-151-02
- **Unblocks:** HS-151-05, HS-151-06
- **Owner:** unassigned

## Problem

The loop has never run for real. Prove it headlessly end-to-end on
real metal: real speech WAV → the REAL import door → real
mlx-whisper → the real intel queue → the REAL .43 resident server
(the pinned one — the harshest honest environment) → action_items
with REAL model-emitted owners → UNASSIGNED lane → triage → map
gesture → chips/staleness → brief person sections.

## Scope

### In

1. The proof rig (orchestrator-runnable, phase assets): isolated
   HOME + the 149 keystore seam; wire via scripts/wire_metal_intel;
   import a real multi-speaker WAV (dogfood/_audio/meeting-*.wav;
   if the owner posts the YouTube 1:1 audio, prefer it) through
   POST /api/meetings/import; run the PRODUCTION intel queue
   worker path; wait honestly (the 35B is slow — detached-run law
   if needed).
2. CONTROL leg: same WAV, fresh HOME, intel disabled — assert zero
   action_items, zero intel_snapshots, no meeting cards, empty
   brief People.
3. TREATMENT assertions (shape, never exact strings): intel_status
   ready; ≥1 action_items row with review_state=pending; owners
   grounded in the transcript (the rig checks each non-reserved
   owner string appears in the segments text); the Door shows them
   UNASSIGNED; accept + map one real owner via the REAL gestures
   (API); chips + staleness appear; the brief People section
   carries the mapped person. Frames at each beat.
4. sync/push appears NOWHERE in this rig (the charter ban).
5. Record what the real model actually emitted (owners verbatim)
   into the evidence — the messy-reality record the risk register
   asks for.

### Out

- The live-mic leg (story 06); any prompt/dispatch changes.

## Acceptance criteria

1. Rig green ×2 + stamped capture; control-vs-treatment artifacts
   in evidence; frames.
2. Every owner asserted grounded; zero seeded intel anywhere.
3. The 35B timing recorded (the Tuesday-latency truth).

## Test plan

The rig ×2 + dw capture; focused suites for anything touched.
