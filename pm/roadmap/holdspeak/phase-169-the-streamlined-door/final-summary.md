# Phase 169 — The Streamlined Door: final summary (DRAFT, closes with 07)

## What shipped

- **The design, ratified by the owner** ("word", 2026-09-04) after two
  rounds on the canvas: the one-screen Door and the Room as four
  questions (assets/settled-design-streamlined-door.md; eleven
  artboards; canvas aa41070b-…). Counsel RATIFY-W-C paid before he saw it.
- **The Door (02):** one screen — the outcome line that becomes the
  name; source rows from the connections wire with an in-world picker,
  default Watches as token toggles, the count as the test (the same
  compile the Watch evaluates), Adjust as a well, Connect for a tool
  not signed in, `Create Project` as one service call. Five clicks on
  a connected desk (168: 17 face steps).
- **The wire for the four questions (04):** needs-you from real Watch
  entities; sources grouped per scope with zero-omitted tokens,
  checked/next-check times, host, state and a plain reason; health
  with its inputs and reason; the server-side read marker; decisions
  via the meeting link; target; `branch_ci` via `gh run list`; the
  native meeting template retired from suggestions; MCP parity.
- **The Room (03):** one display-step headline; NEEDS YOU · SOURCES ·
  SINCE YOU LOOKED · DECISIONS & COMMITMENTS as captions over rows; the
  ask well pinned at the foot with the model's host; wings ROOM ·
  HISTORY; the window 800 wide; 393 stacks.
- **The docs (06):** the guide's two sections rewritten claim by claim
  against the code with the rig's shots.
- **The walk (05):** the runner (face-driven, both widths, the window
  shot per step, the wire timed); the isolated leg 5 clicks / 11.7 s
  door-to-Room; the real leg on the owner's desk; his verdict (below).

## Found live, paid in the phase

- Persisted watch snapshots key entities by id with snake_case fields
  (the derivation had read the source's return shape) — the owner's
  real project showed no tokens until the unwrap.
- The Jira host chip carried the whole `site|email` connection ref.
- `branch_ci` entities had no id; `normalize_snapshot` rejected them —
  every default CI watch would have failed on the real desk.
- The door stayed open behind the Room after Create.
- The History phrase leaked raw field names (`name, source,
  watches_activated`).
- A STALE `holdspeak` app process (alive since 2026-08-31 on pre-169
  code) kept evaluating the owner's watches with the old dispatcher
  hours after the hub was fixed — restarted; the law: check every
  `holdspeak` process's age before a real leg.
- The room-css container query named `surface-window`; the desk's
  container is `surface` — three rounds of 393 failures.

## The debt ledger (carried to the next sitting)

1. The per-source `Adjust` well in the Room: the route
   `PUT /api/watches/{id}/rules` exists; the Door's AdjustWell must be
   extracted into a shared component; the verb is withheld until then.
2. The steward's settings under the source rows (today a section-level
   `Steward` verb opens the 167 posture); `Review N` likewise.
3. The 167 wings' faces (TIMELINE / DECISIONS / SEARCH / ASK) and the
   setup/ folder: unused, to park.
4. The door window should hug its content and grow with the picker
   (DeskWindow `fitContent` is not exposed to surface windows and does
   not re-measure); default 580 today.
5. MCP twins for the two door routes (`door/count`, `door`).
6. `next_evaluation_at` is null on the owner's existing watches, so
   `NEXT CHECK` is omitted for them until the scheduler stamps it.
7. A PR the owner authored with failing checks is a source token, not
   a needs-you row (the rule: needs-you only when the PR awaits him) —
   ask him on the walk whether his own red PRs should call him.
8. The native `meeting` Watch on his existing project: `CAN'T CHECK ·
   Remove` — his call; the adapter remains unbuilt.
9. From 168: the name/outcome/purpose derivation at finalize (the door
   sets name = outcome[:80], outcome = the line, purpose = the line);
   the Room stats row / footer token debts are gone with the old Room.

## Gates (filled at the close)

- Counsel on the build diff (2026-09-05): RATIFY-W-C — S-1 History
  counters of zero, S-2 the since-read group head without its source,
  S-3 `Fix` absent on cant_check rows (withheld with Adjust); N-1 the
  old setup still reachable from FirstWords — all paid in the same
  sitting except S-3 (documented). Hunts clean: one derivation for
  the count; every verb a Button; no sentences; egress at every point
  of decision; CHECKED/READ/headline honest; the schema additive; the
  walk's finally lawful; the tests assert what they claim.
- Full suite / sweep / web baseline / rigs — pending 07.
