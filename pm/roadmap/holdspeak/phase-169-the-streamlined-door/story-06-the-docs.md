# HS-169-06 - The docs (the guide's New Project + Project Room sections re-shot; MCP_SIDECAR regenerated; the design doc canonized)

- **Project:** holdspeak
- **Phase:** 169
- **Status:** done
- **Depends on:** HS-169-02, HS-169-03
- **Unblocks:** HS-169-07
- **Owner:** unassigned

## Problem

Three sentences in 168's guide described things the face did not do. The new door and Room need a guide that matches the pixels.

## Scope

- **In:** docs/USER_GUIDE.md New Project + Project Room re-written to the built faces with shots from the rig; docs/MCP_SIDECAR.md regenerated (never hand-edited counts); the settled design linked from DESIGN_SYSTEM.md's mockup roster; the doc-drift guard green.
- **Out:** marketing copy.

## Acceptance criteria

- [ ] Every claim in the guide's two sections checked against the code (a checklist in the evidence).
- [ ] Shots in the guide are the rig's, at 1440.
- [ ] Doc-drift guard + roadmap-vocabulary guard green.

## Test plan

`uv run pytest -q tests/unit/test_doc_drift_guard.py tests/unit/test_product_copy.py` (the inherited product-copy failure named); the guide read end to end.

## Delivered (2026-09-05)

- docs/USER_GUIDE.md: "New Project" (the one-screen door — the
  outcome line that becomes the name; source rows and what a pick
  does; the default Watches per tool and Adjust; the count as the
  check; Connect for a tool not signed in; Create and the blank
  project) and "Project Room" (the four questions and what feeds each;
  the ask well; HISTORY; the footers) rewritten with a claim → file:line
  checklist for every factual sentence (in the worker's report,
  mirrored in evidence); the 168 "Connect your tools" section checked
  clean; the guide's project-rooms images swapped for the rig's shots
  (eight new PNGs under docs/assets/project-rooms/; the old ones
  parked on disk, unreferenced).
- docs/MCP_SIDECAR.md regenerated by scripts/gen_mcp_sidecar_doc.py —
  zero diff (already current after 04).
- docs/internal/DESIGN_SYSTEM.md mockup roster: the phase-169 line
  (settled design + canvas).
- Guards: doc-drift, vocabulary, the MCP sidecar doc guard green; the
  product-copy guard's one failure is inherited from main in files this
  story did not touch.
