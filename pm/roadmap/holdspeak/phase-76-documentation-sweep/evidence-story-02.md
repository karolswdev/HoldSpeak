# Evidence — HS-76-02 — README: the Desk era front door

- **Shipped:** 2026-07-02
- **Commit:** this commit (branch `phase-76-documentation-sweep`)

## What changed (each fix maps to a ledger row)

- **The Desk section** (new, directly after the two-modes proof): what
  the front door IS (the world the two modes' output lives in), the
  fresh real-hub screenshot (`docs/assets/screenshots/desk.png`, 2x),
  and the closed loops (the orb records and the meeting lands; a rail
  ask persists as an artifact with lineage; the egress badge as the one
  trust answer). Links to WEB_DESK.md. The POSITIONING voice held (no
  dashes, no privacy narration; the badge line names the mechanism).
- **The preview conflation fixed**: the Dictate cell now separates the
  wake-word default from the optional every-dictation preview mode.
- **Quickstart** names where you land ("the browser opens on the Desk").
- **Where to go next** gains the Desk row first among the surface rows.
- Deliberately NOT added: a run-born-artifacts bullet under "Why it's
  different" — the Desk section carries that story where a reader can
  see it; the four differentiators stay four.

## Verification artifacts

- Doc guards **85 passed, 2 skipped** on the new prose, first try.
- The screenshot is from a REAL seeded hub (shot in 349646d, after the
  PreviewCard fix), not a mock.

## Acceptance criteria — re-checked

- [x] README presents the Desk as the main surface with a real current
      screenshot; the tour reads desk-first without breaking the voice.
- [x] Preview + run-story capabilities appear where they belong.
