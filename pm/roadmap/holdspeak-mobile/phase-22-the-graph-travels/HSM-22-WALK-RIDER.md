# HSM-22 — the graph rider (two checks for the staged couch session)

Ride-alongs for the session that runs the 18/19/21/23 walks — same hub, same
pairing, ~3 extra minutes. Both checks were already proven live (Chromium + the
connected simulator, see the per-story evidence); the rider verifies the same
truths on glass.

**G1 — authored here, runnable there (22-01/22-02/22-04).** On the iPad, open the
Workbench, wire Transcription → Decisions (the seeded patch is fine), tap **Save**
(the green Saved beat). Back on the desk, sync, then drag any note onto that
workflow object. EXPECT: the runs-on sheet offers **your desktop**; picking it
prints a card titled with the workflow's name wearing the **Cloud · your desktop**
badge, its body the hub's real graph output (the hub ran YOUR canvas's graph).
CONTROL: a prompt-only saved Ask still runs locally with no sheet.

**G2 — the web authors the same wire (22-03).** On the web desk, tap
**+ Workflow**, add a step or two in the in-world editor, then Run from its
pull-out. EXPECT: real output; on a graph with control flow (authored on the iPad
v2 canvas someday), the amber ⚠ warning line above the output names the honest
refusal.

## The trace

```
G1 iPad graph → hub run:  PASS/FAIL — <workflow name / card seen, badge>
G2 web author + run:      PASS/FAIL — <steps built / output seen>
```

PASS×2 closes HSM-22-04 and the phase — and with it the whole Equilibrium build.
