# Phase 2 — The Graph: final summary

**Closed 2026-09-23** (stories 01–07 done; the owner's ruling on the council's D1–D4 is the one thing still owed, and it is his).

## What the owner can do now that he could not before

- Read one page and know which of his eleven first-use jobs work, fail or are unverified, with the evidence a click away: `docs/internal/philo/graph/COUNCIL.md`, the decision page.
- See the whole platform as one graph — every edge, interface, connection, action and state, both brains' observations side by side — in `docs/generated/graph.json`, regenerated by `scripts/philo_graph_reference.py` and drift-checked in CI like the other Philo inventories.
- Run any first-use case against a real hub in a fresh HOME, one command, and get a verdict that cannot be earned by a diff: `scripts/graph_walk.py run --case <id>` (73 cases in `docs/internal/philo/graph/atlas.json`).

## What the graph found (both brains, on real hubs)

- **Broken on every desk:** recording a decision answers a server error (`del ctx` before its use); the Thought face never says when the words were kept; the Rhythm face shows UTC; a failed brief load is drawn as "No brief yet".
- **Broken by source, unobserved live:** the arrival never learns a summary landed; a retrying summary reads as "Nothing needs you"; two first-value recoveries open New Project; receipt Opens with the wrong source identity; a dismissed card can never return; the Concierge download never finishes.
- **Works:** Continue later and typing at the gate; the planned host beside the verb; a sweep receipt opening Rhythm; the empty same-day brief surviving reloads and regenerations by id; typed thoughts reaching the store. Two of the owner's sitting defects no longer reproduce.
- **Unverified:** the summary itself (the rig's lane runtime lacked the model client; a reported, unretained diagnostic showed the LAN engine summarising in 1.46 s); recording by microphone; voice typing into another app; anything the clock decides.

## The two brains

Every artefact was checked by the other brain: the charter (two rounds), the selection, story 01 (four rounds — three DO-NOT-RATIFY, each with reproduced false passes, all paid with negative controls), the council (Astra's DO-NOT-RATIFY of the first decision page, six of seven dissents resolved for Astra, one left to the owner). Static and live passes were sealed until all four existed.

## What must be built — Phase 3 The Meeting Loop (drafted, pending the owner)

A1 record the decision · A2 see and find the summary · A3 read the dated brief with that decision · A4 the thought's receipt (open dissent D3: Astra three, Muad'Dib four). The deferred list is explicit and priced in the council record.

## Numbers (diagnostics, not exits)

Static: 1,780 + 4,850 nodes. Live: 120 + 96 runs, all 71 applicable cases, every face case at 1440 and 393. Findings: 47, all dispositioned. Fences added this phase: 17 schema, 46 atlas, 62 calibration, 12 generator, 3 doc-claims rows. Merged: #601, #603, #604, #605, #607, #608, #609 (+#610 pending counsel).

## What Phase 2 did not do

It did not measure a working day. It did not judge a summary on real material. It did not walk the 225 MCP tools, the 29 CLI commands or the connectors. It did not fix the product: nothing was fixed mid-sweep, by the owner's ruling.
