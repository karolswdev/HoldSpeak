# Phase 7 — The Desk on the Contract — final summary

**CLOSED 2026-09-26 on the owner's word** <!-- owner word pending -->: rehearsed, owner-reviewed; not a sitting.

**Status: four of four stories merged or merging (01 #657 `9800a603`, 02 #659 `f6695090`, 03 #663 `4044072a`, 04 PR #661 on `feat/philo-7-04-cold-context`); exits 2, 3 and 6 MET; exits 1, 4 and 5 MET WITH QUALIFICATION (the table below).** The desk slice (notes, zones, knowledge bases, zone and knowledge membership, the remaining decision operations) runs on the one declared contract. Every write that files or decides makes one kernel operation and one terminal receipt, through HTTP and MCP. An agent writes only under a grant that the owner makes on the agent's credential row, and each such write has a receipt that names the grant. A cold-context Claude session (R6) did the owner's four jobs from the catalogue alone, with zero repository reads in the retained log, in 94.8 s. The owner reviewed the rerun's shots: "Reviewed — it's right". No sitting is claimed. The owner has not used this on his own desk.

## What he can do now that he could not on 2026-09-24

- **File a note into a zone and find it again**, from the Desk or from an MCP client. On main before this phase, every MCP zone call except `desk.list` failed (`create_directorie`), so "make a zone" over MCP never worked (`evidence-story-01.md:19`).
- **Put a decision on his review list with its reason.** A client that knows nothing of the repository records the title, the decision and the context; the Desk shows the context at 1440 and 393 (`evidence-story-04.md` "The closing run"; `assets/story-04-shots/final/20260926T015522Z-file-and-find/shots/owner-decision/`).
- **Make the brief**, and see the new decision on it (`brief.latest` `source_ref = decision:decision_32cacbceb68d`, `checks/story-04-built-astra-role-r1.md` "The job").
- **Let an agent file for him, and see what it did.** On the Remote Access credential row he presses `Allow filing` (or `Stop filing`). The chip reads `FILING ALLOWED` or `FILING STOPPED`. Every agent write under the grant has a receipt that names it: `authority_basis = desk-delegation:<id>:<terms_sha256>`, `delegator_kind = owner` (`evidence-story-02.md` "The face, as rendered"; `evidence-story-04.md` AGENT leg).
- **See refusals as receipts.** An agent without a grant is refused at once with a receipt (`desk_delegation_required`); nothing waits. A malformed attempt at a filing or deciding write leaves a refusal receipt that names the rule, and the response carries `operation_id` and `receipt` (`checks/story-02-round-two-muaddib.md` findings 2–3).
- **Rename a zone or a knowledge base without losing a filing.** A rename that raced a move or a new member reversed it on main; now a rename writes the name only (`story-01…md` rename-repair box; `docs/internal/philo/phase-7/notes-and-directories/red-main-behaviour.txt`).
- **Read the delete receipt on the Floor.** After Delete, "Removed … Undo" and then "Removal committed" show in the viewport at both widths, for 6 s (PR #665 `6ac73757`; `checks/delete-receipt-astra-role-r1.md` F1). Before, the receipt drew at y = −12.

### Measured against the Seven Tenets

| Tenet | How this phase meets it | Where it falls short |
|---|---|---|
| 1 No over-engineering for safety | One explicit descriptor row per (kind, verb), a module and not a framework (`current-phase-status.md` "Settled between the brains"). The Astra-role check refused to duplicate story 02's unit fences as atlas cases (`checks/story-03-built-astra-role-r1.md` ruling C1). | The story 04 driver is 1883 lines plus 677 lines of tests: "proportionate … but it is heavy" (`checks/story-04-built-astra-role-r1.md` UNKNOWN). |
| 2 Not even pre-alpha | Every proof is a rehearsal on an isolated hub. No record calls it a sitting. | He has still not used the product. |
| 3 Help and accelerate | Phase 5's client needed 201 s and 25 repository reads to find one decision job (`pm/roadmap/holdspeak/BACKLOG.md:1216`). The cold Claude session did four jobs in 94.8 s with zero reads. | The tool names did most of the discovery; the effect of the new words is confounded (the ToolSearch qualification, below). |
| 4 ASD-STE100 | The owner ratified the grant words (set A). The check caught "Add to a decision" where the text replaces the reason; it now reads "Change a decision … the new text replaces the old" (C2). | Not certified against the full STE dictionary. |
| 5 Component framework | The grant verb is the library `Button`; the chip and the receipt are library species; the 12 px floor was repaired in the library, desk-wide (`assets/story-02-canvas/README.md:56-75`). | The 11 px and 10 px rules outside the Settings window stay (the ledger). |
| 6 Workbench 2.0+ | The grant lives on the existing credential row: no new screen, panel or toggle. | The Workbench footer now lingers 6 s after a delete (the ledger). |
| 7 A Senior Architect with reports | His jobs, in his words: file, find, decide with a reason, the brief; and a bounded grant for the agents that work for him. | Four Floor defects stand between him and a smooth Tuesday (the ledger, first four rows). |

## The closing run (story 04)

`assets/story-04-shots/final/20260926T015522Z-file-and-find/`, the capture of 2026-09-26T01:55:22Z in `evidence-story-04.md`. Isolated hub; engine `none` (the jobs call no model). The claim, as R6 reworded R3: "a cold-context Claude session (an empty scratch dir, a scratch HOME with only the auth file, no user or project instructions, `--strict-mcp-config` with the one holdspeak server, no preamble), ZERO repository reads in the retained log, discovery from the catalogue alone". It is observational, not a filesystem sandbox.

| Leg | Turn | Tools (`docs/internal/philo/phase-7/file-and-find/rehearsal.md:122-127`) | Time | Outcome |
|---|---|---|---|---|
| OWNER (sidecar) | file | ToolSearch ×2, `desk_list` ×2, `zone_file` | 9.2 s | `zone.file` `succeeded`, actor the owner |
| OWNER | find | `zone_list_members` | 4.9 s | the note in its zone |
| OWNER | decide | ToolSearch, `desk_create` (title, `proposed`, `context_markdown`, `decision_markdown`) | 8.0 s | `decision.create` `succeeded`; the context reads back through `decision.read` |
| OWNER | brief | ToolSearch, `monday_brief_generate` | 8.0 s | the brief row `decision:decision_32cacbceb68d` |
| AGENT (`/api/mcp`, DESK bearer) | file, no grant | ToolSearch ×2, `desk_list` ×2, `zone_file` | 16.2 s | refused `desk_delegation_required`, actor `claude-desk-agent`; not filed; zero `awaiting_decision` |
| AGENT | file, with grant | the same | 8.5 s | `succeeded`, `desk-delegation:deskdeleg_87740697…:sha256:…`, `delegator_kind=owner` |

The zero-read fence: 0 findings in all six sessions (the capture of 2026-09-26T06:36:07Z). It is RED on the Phase 5 Codex logs (25 + 3, `docs/internal/philo/phase-7/file-and-find/red-zero-read-phase5.txt`), on a real Claude session told to read a repository file (`red-claude-read/`), and, after the Astra-role check's C1, on a read of `holdspeak://desk/constitution` (`red-constitution-resource.txt`). The first closing run bounced ("the decision is thin") and is kept under `assets/story-04-shots/attempts/20260926T014230Z-file-and-find/`.

## Both brains, per story

Who checked. Codex Astra (`gpt-6-astra`) checked the charter r1–r3, the lifecycle beat r1–r3, the canvas r1–r2, story 01 r1–r2 and story 02 r1–r2. Codex Astra's quota ran out at 15:38 MDT on 2026-09-25 (`docs/internal/project-rooms/HANDOVER-MUADDIB-XXIX.md:50`). The owner then ruled (a) "Build on; Astra checks on return" and (b) "You ARE Astra. You spawn Astras, which run on Opus 5.5". Story 03, story 04 and the receipt fix were checked by "Astra role (Opus 5.5 stand-in)" agents (the check headers). **The independence cost:** from that point the builder and the checker came from the same model family (Claude, Opus 5.5). The checks reproduced every claim from real runs, but no second model family looked at story 03, story 04 or the receipt fix. Codex Astra's check on return was not run for them.

| Item | Built by | Checked by | Verdicts per round | Merged |
|---|---|---|---|---|
| Charter | Muad'Dib | Codex Astra | r1 RATIFY-WITH-CONDITIONS (hidden filing effects; receipt-free domain refusal; `submit` is not the whole path) → r2 RATIFY-WITH-CONDITIONS (the rename races; held-write semantics; D4 exclusion) → r3 RATIFY-WITH-CONDITIONS (adapter refusals before `invoke`; the grant lifecycle); all paid or carried in r4 (`checks/charter-astra-r1.md` … `-r3.md`) | #650 `8482a2f1`; ratified #655 `dcf3eaeb` |
| Lifecycle design beat | Muad'Dib | Codex Astra | r1 RATIFY-WITH-CONDITIONS (a two-step terminal write loses its receipt) → r2 RATIFY-WITH-CONDITIONS (full terminal inventory; strict conflicts; commit boundary) → r3 RATIFY-WITH-CONDITIONS (explicit `BEGIN IMMEDIATE`; one-connection callback; the reject field); paid in round four (`design/grant-lifecycle-beat.md:3`) | #656 `cc8ff6a3` |
| Grant canvas | Muad'Dib | Codex Astra | r1 BOUNCE (the receipt control under the window grip, 6.61 × 10 px at 393; remote OFF hid a LIVE grant; 10 px species; posed expiry) → r2 RATIFY-WITH-CONDITIONS, supporting set B; the owner ratified set A (`checks/story-02-canvas-astra-r1.md`, `-r2.md`; `assets/story-02-canvas/README.md:3`) | #658 `c806c380` |
| 01 Notes and directories on the contract | the Fedaykin lane (Opus 5.5) for Muad'Dib | Codex Astra | r1 RATIFY-WITH-CONDITIONS (the rig dropped arguments on read/delete projections; `kb.create` admission missed the mapping `member_ids`), paid in round two (`checks/story-01-built-astra-r1.md`; `checks/story-01-round-two-muaddib.md`) → r2 RATIFY-WITH-CONDITIONS (no file in `checks/`; recorded at `HANDOVER-MUADDIB-XXIX.md:10` and in the #657 title; its carried item is story 02's note, `story-02…md:84`) | #657 `9800a603` |
| 02 Membership and decisions under Article XI | the Fedaykin lane (Opus 5.5) for Muad'Dib | Codex Astra | r1 BOUNCE (P0: a grant body could name another agent; refusals escaping admission; receipts the caller could not read; the 393 scroll edge; API reference drift) → round two paid → r2 RATIFY-WITH-CONDITIONS (the unknown-operation fence ran with no principal: a false negative) → round three paid (authenticated boundary fences; mb1–mb4b reds) (`checks/story-02-built-astra-r1.md`, `-r2.md`; `checks/story-02-round-two-muaddib.md`). The owner reviewed the built face: "Reviewed — merge it" (`HANDOVER-MUADDIB-XXIX.md:13`) | #659 `f6695090` |
| 03 The atlas for the desk | the Fedaykin lane (Opus 5.5), after Astra's Codex lane died on its quota | Muad'Dib counsel (Opus 5.5 counsel lane), then Astra role (Opus 5.5 stand-in) | Muad'Dib: RATIFY-WITH-CONDITIONS (F2 a vacuous delete identity; F3 an actor claim false for three receipt cases), paid in round two (`checks/story-03-built-muaddib.md`; `checks/story-03-round-two-muaddib.md`). Astra role: RATIFY-WITH-CONDITIONS (K1–K3 three branch-new reds nobody had reported; ruled C1 the narrower reading and C2 the new atlas file), paid in round three (`checks/story-03-built-astra-role-r1.md`) | #663 `4044072a` |
| 04 File it and find it without the repo | the Fedaykin lane (Opus 5.5) for Muad'Dib (the roles swapped: the lane was Astra's by the charter) | Astra role (Opus 5.5 stand-in) | RATIFY-WITH-CONDITIONS (C1 the constitution resource was a hole in the zero-read fence; C2 "Add to a decision"; C3 stale records; C4 the boxes to R6; C5 the author header), all paid (`checks/story-04-built-astra-role-r1.md`; `evidence-story-04.md` "The check on built, paid"). The owner: first review "the decision is thin", then "Reviewed — it's right" | PR #661 (open at this summary) |
| The delete receipt fix (story 03 follow-up) | Muad'Dib's lane | Astra role (Opus 5.5 stand-in) | RATIFY-WITH-CONDITIONS on merge; conditions are BACKLOG rows (F7 two deletes in one window; F3 the Workbench footer linger) (`checks/delete-receipt-astra-role-r1.md`). The owner ratified the seat | #665 `6ac73757` |
| The Floor diagnostic | Muad'Dib (read-only, through the real hub) | none recorded | two VERIFIED defects and S1 (`docs/internal/philo/phase-7/floor-diag/FINDING.md`) | #667 `3d3f1748` |

Also merged during the phase: the Phase 5 driver's resource-catalogue reconciliation (#653 `02a862f2`, Codex Astra RATIFY-WITH-CONDITIONS); three inherited Web Quality reds (#654 `4d427d9c`); the MCP tool count 229 in the docs (#662 `6cab3c1d`); the evidence scratch guard's reader allow-list (#666 `d524846b`).

## Fences that failed pre-fix

- **01:** `docs/internal/philo/phase-7/notes-and-directories/red-main-behaviour.txt` — the new test files on a `git archive` copy of main `02a862f2`: 65 failed, 70 passed. Counted reds: the two rename interleavings (`the rename tombstoned the member added during it: ['note:a']`; `… parent_id='p1'`), 17 zone MCP rows (`create_directorie`), 16 discovery (`'file a note into a zone' is named by []`), 1 shelf alignment, 4 invoke recordings. `red-main-residual.txt`: `RESIDUAL FENCE RED: 27 problem(s)`. `red-mutations.txt`: m1–m9. Round two: 9 rig-carriage reds on round one (`checks/story-01-round-two-muaddib.md`).
- **02:** `docs/internal/philo/phase-7/article-xi/red-main-behaviour.txt` — 31 behavioural fences, 31 failed on main (zero operations and zero receipts; the agent's unguarded filing wrote; the grant route 404). `red-main-residual.txt` 9 problems. `red-mutations.txt` 27 kernel/service and 2 face mutations, all red. `red-face.txt` six glass fences red on main's face. Round two `red-round-two.txt`, `red-scroll-edge.txt`; round three `red-mutations-round-two.txt` (mb1–mb4b). The Phase 5 characterization at `tests/unit/test_philo5_the_loop.py:618` went red on both transports at once, `(2, 2, 10) == (0, 0, 0)`, as it said it would (`story-02…md:132`).
- **03:** `docs/internal/philo/phase-7/atlas/reds/rig-carriage-base.txt` — 5 red on the base rig 1.3.0. `atlas-mutations.md` + `atlas-mutations-run.log` — 27/27 cases fail under one mutation each (they mutate the expectation, not the product; F4). Round two `round-two-*`. Round three: K1 `Extra items in the left set: 'focus'`, K2 `assert not ['tests/unit/test_philo7_atlas.py:30']`, K3 `API reference drift` (`checks/story-03-round-two-muaddib.md` "Round three").
- **04:** `docs/internal/philo/phase-7/file-and-find/red-zero-read-phase5.txt` (28 findings), `red-claude-read/` (1 finding), `red-constitution-resource.txt` (0 findings at `123675c7`, 1 after), `red-account-scan.txt` (the leak fence red on nine files before redaction), `red-discovery-reason-main.txt` (`'the reason for a decision' is named by []` on main).
- **The receipt fix (#665):** the glass fence on main `2 failed` (`'Removed': not in the viewport … 'y': -12`), on the branch `2 passed` (`checks/delete-receipt-astra-role-r1.md` F1).

## The exits

| # | Exit (short) | Verdict | Evidence |
|---|---|---|---|
| 1 | Every slice operation reachable by route, MCP and the rig's `op` step, one contract, one live `PrimitiveService` in the hub; durable across restart | **MET WITH QUALIFICATION** | `evidence-story-01.md` "Integration" (`test_every_slice_operation_is_bound_to_the_hubs_one_instance`, `test_durable_state_survives_a_new_hub_over_the_same_database`); `evidence-story-02.md` "Transports over `invoke`". Qualification: `decision.status` is HTTP only — over MCP the same mutation is `decision.update`, and the rig blocks `decision.status` by name (`story-02…md:124`); `delegation.grant`/`revoke` are HTTP-only owner operations by design; `desk.get` reads still carry the id only, and the rig blocks a read with extra arguments by name (`story-02…md:134`). |
| 2 | The duplicated paths retired; the residual set shrank by the enumerated identities; the fence red on a copy of main | **MET** | 320 → 293 → 284: all 36 paid (33 MCP; the 3 HTTP PAID because the fallback left the routes). `evidence-story-01.md` "Measurements"; `evidence-story-02.md` "Measurements"; `notes-and-directories/red-main-residual.txt` (27), `article-xi/red-main-residual.txt` (9). |
| 3 | The tool roster and `docs/generated/operations.json` regenerated and drift-guarded | **MET** | `evidence-story-01.md` "Generated"; `evidence-story-02.md` "Measurements"; every `scripts/philo_*.py --check` exits 0 at story 03's round three and story 04's C2. The docs tool count lagged 229 on main after #659 until #662. |
| 4 | Named pairs agree on durable outcome and refusals between `op` and browser; new browser cases at 1440 and 393; real and replayed runs separate | **MET WITH QUALIFICATION** | `evidence-story-03.md`: 18/18 pair-widths equivalent in both folders; 8 of 9 face cases pass at both widths. `case.p7.decision_delete.gone` FAILED at both widths in story 03 (the receipt drew at y = −12); #665 fixed the face, and the atlas case re-run on main `6ac73757` (with #665) PASSES at 1440 and 393 (`assets/closing/delete-rerun-after-665/`, `--engine none`, 2026-09-26) — so 9 of 9 face cases now pass. Qualification: The supersede action is not on the face. The "replay" folder is `--engine none`, not a provider-fixture replay, and no case calls a model. "Refusals equivalent" means "neither side refused" on the face legs. |
| 5 | The closing use from a cold-context client; OWNER and AGENT legs; a reopened Desk read at 1440 and 393; receipts read back; rehearsed, owner-reviewed shots; never a sitting | **MET WITH QUALIFICATION** | `evidence-story-04.md`; `docs/internal/philo/phase-7/file-and-find/rehearsal.md`; the owner's "Reviewed — it's right". Qualification: the exit names Astra and Codex; R6 amended D4, so the client is a cold-context Claude session on Opus 5.5 driven by the lane's driver, not Astra through Codex. The check on it is the Astra-role check (same model family). "Find it" proved "find it again" from the session's memory, not "find it cold". Tools were chosen mostly by name; the effect of the new words is confounded (`rehearsal.md:149`). |
| 6 | The agent path; every refusal class with its receipt and the protocol boundary with none; the grant lifecycle as the beat says; the grant verb and chip as ratified, fenced as rendered at 1440 and 393 | **MET** | `evidence-story-02.md` "The lifecycle fences", "Through the real hub", "The REAL restart", "The face, as rendered"; `checks/story-02-round-two-muaddib.md` rounds two and three; the owner's "Reviewed — merge it". One disclosed gap outside the real path: the stdio in-process `handle_message` (tests only) writes no class-4 receipt for non-object arguments; `/api/mcp` does (`evidence-story-02.md` "Not done"). |

The exit boxes in `current-phase-status.md` stay unflipped; the flip is the orchestrator's.

## The owner's rulings, in order

1. **2026-09-24 night — D1–D4** (`current-phase-status.md` Authority): D1 the next slice is desk notes; D2 repairs first (Phase 6 before Phase 7); D3 writes that FILE or DECIDE get a kernel operation and a receipt, plain edits do not; D4 the closing use is Codex without repository access.
2. **2026-09-25 — R1–R4** on the r2 draft: R1 bounded delegation for agent writes, no HELD state; R2 contract refusals leave receipts; R3 D4 is the observational claim, not a sandbox; R4 all admissions and exemptions confirmed.
3. **2026-09-25, at ratification — "Ratify, build it", with R5:** `decision.delete` IS in the grant ("Put it in the grant"), over Astra r3's reading (#655).
4. **2026-09-25 — the canvas:** set A (`Allow filing` / `FILING ALLOWED`) over both brains' set B; the caption `AGENTS · N ACTIVE CREDENTIALS`; the verb `Revoke credential`; the order and the footer receipt Button; the desk-wide 12 px library repair ("Ratify all") (`assets/story-02-canvas/README.md:3`).
5. **2026-09-25 — the built face of story 02:** "Reviewed — merge it" (`HANDOVER-MUADDIB-XXIX.md:13`).
6. **2026-09-25, 15:38 MDT onward — the interim rulings:** "Build on; Astra checks on return" (`HANDOVER-MUADDIB-XXIX.md:50`); then "You ARE Astra. You spawn Astras, which run on Opus 5.5" (as the orchestrator relays it; the check headers read "owner ruling 2026-09-25").
7. **2026-09-25 evening — R6 "Claude closes it":** D4 amended; the closing client is a cold-context Claude session on Opus 5.5 (`claude -p --model claude-opus-5-5`); no Codex rerun owed.
8. **2026-09-25 evening — story 04, first review:** "the decision is thin — a decision put on my review list should carry its context, not just a title."
9. **2026-09-25 — the delete receipt seat ratified** (#665).
10. **2026-09-25/26 — "rewrite the branch":** story 04's branch rebuilt from main with the clean tree only (`evidence-story-04.md` "Account redaction").
11. **2026-09-26 — story 04, second review:** "Reviewed — it's right".

## The numbers

- **Residual identities:** 320 → 293 (story 01: MCP 256 → 232, HTTP 64 → 61) → 284 (story 02: MCP 232 → 223, HTTP 61). The honest range the charter set was 284–287; the phase landed at 284.
- **Public MCP tools:** 228 → 229 (`kernel.receipt`, story 02), reported apart from the residual set.
- **Atlas cases:** 121 → 148 (`atlas.json` 85 and `atlas-phase3.json` 36 unchanged; `atlas-phase7.json` 27: 9 face, 18 `.op`).
- **Closing run:** 94.8 s, both legs, six sessions, zero repository reads.

## THE LEDGER: what he still cannot do, and the debts (ranked by owner cost)

1. **A second unnamed zone fails 409 on every face.** New Zone always names the zone "New zone"; names must be unique; after one Chair or list create, every further New Zone fails until he finds and renames the first on the spatial Floor (`floor-diag/FINDING.md` S1; BACKLOG "PHILO-7-03 follow-ups"). ASSIGNED: Muad'Dib, the Floor lane.
2. **No rename from the Chair or the list.** The rename field lives only in the spatial `WorldStage`; the Chair and the list make the zone and show nothing, and the stale field appears later on another face (`floor-diag/FINDING.md` Finding 1). ASSIGNED: the Floor lane.
3. **The list-view Delete does nothing.** The menu shows an enabled Delete; the only listener is in `WorldStage`; the decision stays, with no word (`floor-diag/FINDING.md` Finding 2, 1440 and 393). ASSIGNED: the Floor lane.
4. **Two deletes in one undo window keep the second.** A 404, B 200: the face said removed, the server kept it. On main too (`checks/delete-receipt-astra-role-r1.md` F7; BACKLOG "The Floor delete receipt follow-ups"). ASSIGNED: the Floor lane.
5. **The six non-desk two-step terminal writes** (admission refusal, native failure, reject, claim refusal, `recover_invalidated`, the reaper) leave `refused` with `receipt = NULL` when interrupted; story 02 paid only the desk paths (BACKLOG "PHILO-7-02 lifecycle beat follow-ups"). A kernel story after Phase 7.
6. **DESK palette = ALL.** A DESK credential narrows nothing and reads back as `ALL` (`holdspeak/mcp/palettes.py:22-32`; BACKLOG "PHILO-7-02 canvas follow-ups"; the Astra-role check's MISSED 1 on story 04). UNASSIGNED.
7. **Filing a tombstoned Thought's note over HTTP answers 500** (with its receipt now); MCP answers the named conflict (BACKLOG "PHILO-7-02 round two follow-ups"). UNASSIGNED; small.
8. **The find turn proved "find again", not "find cold".** The session used the note id from its own first turn; no rehearsal shows a cold client searching the Desk for a note (`checks/story-04-built-astra-role-r1.md` MISSED 4). Named in the evidence; no BACKLOG row yet.
9. **The ToolSearch confound.** Claude chose tools by name from the deferred list; the new words were decisive only for the placement of `context_markdown`, and that is confounded with the changed owner words; no A/B run exists (`rehearsal.md:149`).
10. ~~The atlas delete case is not re-run after #665~~ — PAID at the close: re-run on main `6ac73757`, PASS at 1440 and 393 (`assets/closing/delete-rerun-after-665/`).
11. **The 11 px and 10 px rules not in the Settings window** (`.surface-token` without `data-chip`, the provenance chip, `.desk-wing-door`, other rules in `surface.css` and `gadgets.css`) have not been rendered or repaired (`assets/story-02-canvas/README.md:73`). The library's next pass.
12. **The Workbench footer linger.** A delete hides the "N ITEMS · last run" line and any Copy receipt for 6 s (BACKLOG "The Floor delete receipt follow-ups"). The next Workbench sitting.
13. **Face notes from the story 04 check:** the decision window shows an empty CONSEQUENCES heading; the brief reports the people sections as unavailable on a fresh hub (`evidence-story-04.md` "Not done"). BACKLOG row filed at the close ("PHILO-7-04 follow-ups") yet.
14. **The privacy note (report, not fixed).** The owner's address is his git identity on main's commits (14 of main's last 20, `checks/story-04-built-astra-role-r1.md` F4). The retained runs are redacted at capture, and a leak fence guards them (`evidence-story-04.md` "Account redaction"). On main, ten tracked files still hold the address: one e2e test (`tests/e2e/test_hs174_door_confluence_glass.py`), one vitest (`web/src/features/project-room/door/__tests__/DoorCore.test.tsx`), `pyproject.toml`, three mockups, and four roadmap documents (a `git grep -l` on `origin/main` at the time of this summary). Not edited here.

## Laws learned

- **A boundary fence runs with an authenticated principal.** With `principal=None` the refusal path writes nothing by construction, and a wrong-journaling mutation stays green (story 02 r2; `HANDOVER-MUADDIB-XXIX.md` law 5).
- **Counting receipts in the database does not prove the caller can read one.** Every refusal response of an admitted operation carries `operation_id` and `receipt` (story 02 r1 MISSED 2).
- **A merge brings reds that a scoped set does not run.** Story 03's three branch-new reds were in the older rig fence, the evidence guard and the generators; neither the lane nor the first counsel ran them (story 03 Astra-role MISSED 1).
- **The owner overrules both brains, and that is the record.** Set A over set B; the reason stands beside the ruling, not re-argued (`HANDOVER-MUADDIB-XXIX.md` law 7).
- **Label who checked.** When the other brain is out, the stand-in is named "Astra role (Opus 5.5 stand-in)", never "Astra", and the same-family cost is stated.
- **A resource that serves a repository file is a repository read.** A zero-read fence that trusts every server resource has a hole (story 04 C1).
- **A retained client session can carry the account identity.** Redact at capture, fence the leak, and read the commit header as well as the file content (story 04 C5; `evidence-story-04.md` "Account redaction").
