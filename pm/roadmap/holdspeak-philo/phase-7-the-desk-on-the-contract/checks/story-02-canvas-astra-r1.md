VERDICT: BOUNCE — return this canvas for correction before presenting it for owner ratification.

FINDINGS:

1. **The receipt’s inspect control is not usable at both widths.** At 1440, `elementFromPoint` and real pointer events hit `.desk-window-grip` at the `ℹ` control’s center and corners. At 393, the control measures **6.61 × 10 px**; points 20 px above and below hit the footer. This is the proposed route to the receipt after the last row disappears. The canvas places it at the far end of `SurfaceFooter.verbs`, beside the resize corner. Evidence: `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:303`, [pointer proof](/var/folders/q7/5dzz5g2116b3lq8rhg7hwjrr0000gn/T/astra-grant-hit-proof-0cuw97ev/hit-proof.json), `web/src/desk/components/window-chrome.css:143`. **Fails Tenets 3 and 5; UX-CANON C.**

2. **Remote OFF still hides a LIVE grant when its credential remains.** `credentials = enabled ? wire.credentials : []` removes every credential row while OFF. The beat’s `delegations` contains only identities **without** credentials, so it cannot supply the missing grant. Board 5b has no credentials and therefore misses this branch. The beat explicitly changes the ledger condition to “any credential OR any delegation row.” Evidence: `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:202`, `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/design/grant-lifecycle-beat.md:155`. **Fails Tenet 3.** Keep credential-backed grants visible while OFF; add that board.

3. **The 10 px defect belongs to the library, but this canvas owns the dependency.** It cannot become the ratified geometry while violating the 12 px floor. The debt also extends beyond the four listed selectors: the proposed footer receipt’s words and timestamp are 10 px at `web/src/desk/surface/patterns/provenance.css:64`. The capture scans only the Remote access group, excluding that footer, so “every sub-12 px text” is overstated (`pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/shoot.py:32`). Repair the shared species and recapture; the present fit of B does not establish its fit at compliant sizes. **Fails Tenets 5 and 6; UX-CANON C.**

4. **The expiry boards show expected appearances, not a time-aware derivation.** `G()` always sets `expires_at: null`; boards 4 and 5c pass `G("EXPIRED")`. They do not represent the claimed stored-LIVE row with a past timestamp. Evidence: `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:324`, `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:350`, `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:391`. Reading `grant.state` in JSX is appropriate **if it is the server’s effective state**. Settle `credentials[].delegation` as that projection, using the same `by_identity(now)` rule as orphan rows; distinguish it from storage state. Also add **EXPIRED credential + LIVE grant**, explicitly required by `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/design/grant-lifecycle-beat.md:147`. **The current claims fail Tenet 3 and Article VI.**

5. **The fence sketch needs the other disappearing-row paths.** Board 6 already covers both refused **Allow** and refused **Stop**; those are not missing. What is missing is a refused Stop whose reread removes the orphan row—for example, another owner request already revoked it. “Refusal on THAT row” then has nowhere to render. Also fence the credential’s **Revoke**, whose grant receipt has `credential_revoked`, separately from Stop’s `owner_revoked`. Preserve the attempted act, identity, named refusal and operation receipt outside the removable ledger. Evidence: `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/README.md:78`, `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/design/grant-lifecycle-beat.md:131`. The receipt well currently demonstrates success only (`pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/harness/main.tsx:281`). **Fails Tenet 3 and Article V.2 if carried into the build.**

6. **Words and order: support B, with two clarifications.** “Filing” alone does not adequately name the decision authority, including `decision.delete`; B matches the `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/design/grant-lifecycle-beat.md:14`. Support **AGENTS**, credential status first, grant chip before credential facts, and grant verb before credential revocation. Recommend **Revoke credential** for that last verb, and identify any ACTIVE count explicitly as credentials. This lets a Senior Architect distinguish authority from authentication. Use **CANNOT ALLOW / CANNOT STOP**: `docs/internal/DOCS_STYLE.md:20` requires expanded contractions; the older ChairHome example supplies the composition, not an exemption from Tenet 4. **A’s incomplete scope and the contractions fail Tenets 3/4.**

7. **DESK → ALL is real inherited producer debt.** I reran the producer in scratch space with an isolated HOME: **one test collected, one passed**; issue returned DESK, read returned ALL, and both resolved sets contained 228 tools. Evidence: [run output](/var/folders/q7/5dzz5g2116b3lq8rhg7hwjrr0000gn/T/astra-grant-producer-4io7e0lr/run.txt), `holdspeak/web/routes/mcp_http.py:209`. File it in `pm/roadmap/holdspeak/BACKLOG.md`, owned at the credential issue/store/read boundary in `mcp_http.py` and `principals.py`. Preserve the issued name; merely reversing the map’s order transfers the mislabelling to ALL. Fence readback for both names. This demonstrates label loss, not additional authority. **Fails Tenet 3 and Article VI.**

8. **The review page discloses the unbuilt producer, but omits material qualifications.** `docs/internal/philo/phase-7/grant-canvas/index.html:58` is honest about that boundary. Its board captions nevertheless say “from the time-aware rule” and “ℹ opens” without the README’s explicit statement that no click drives a transition. Bring the assumed credential field, fixture refusals, static receipt states, omitted Settings content and known typography defects onto the review page or a clearly linked limitations section. Evidence: `docs/internal/philo/phase-7/grant-canvas/index.html:59`, `pm/roadmap/holdspeak-philo/phase-7-the-desk-on-the-contract/assets/story-02-canvas/README.md:62`. **The incomplete presentation fails Tenet 3 and Article VI.**

CONDITIONS:

1. Correct the receipt target and typography through the library; recapture both widths and prove pointer ownership, including the footer.
2. Add OFF-with-retained-credential, expired-credential-with-LIVE-grant, and refused-Stop-after-row-disappearance boards. Specify the effective-state wire field and label the expiry snapshots accurately.
3. Update the fence sketch for those transitions and credential Revoke. Require real producers, matching operation/receipt identity, rendered inspection after disappearance, and demonstrated behavioral or mutation reds. Change ratified words and their fences together.
4. Update the owner review page with those qualifications and record the palette defect in the debt ledger.

The component composition, lack of prose/modals/zero counters, and proposed single-screen approach are supportable.

MISSED:

1. Highest owner cost: the receipt survives in the DOM but its inspect control is obstructed or too small.
2. Next: switching remote OFF conceals authority attached to a surviving credential.
3. Next: a failed Stop can remove the very row assigned to explain the failure.
4. No owner-session row is required: this producer mints `PrincipalKind.AGENT` credentials (`holdspeak/principals.py:156`). An owner-issued agent credential remains an agent; the owner session itself is not this ledger’s object.

TUESDAY: The owner can understand B’s grant action, but cannot reliably inspect its receipt or find every surviving grant while remote access is OFF.

UNKNOWN: Grant execution, durable receipts, real clock transitions and actual atlas cases remain unverified because the producer is unbuilt. This was a canvas check: all 38 shots reviewed, targeted browser probes run, credential producer reproduced; no owner-device walk or full STE dictionary certification claimed. The worktree remained unchanged.