# PHILO-5-04 retained proof

**REHEARSED; OWNER REVIEW PENDING.** These are isolated rig records. No live
sitting or owner review is claimed. The LAN model is real; the input meeting
is the repository's synthetic architect fixture.

- `final/20260925T001407Z-his-words-real/`: completed 326.090-second run.
  Four Codex turns, full MCP server transcript, client events and rollouts,
  effective config, lock/DB-path proof, SQLite backup, hub log, readbacks,
  and fourteen PNGs. Absolute `.tmp` shot paths in raw observations are
  the original capture locations; use the same relative `shots/` paths here.
- `attempts/`: four stopped runs, original prompts and errors; see its README.
- `receipt-red/` and `receipt-green/`: same existing run-4 brief, fresh reads
  before/after the receipt fix. No second Generate in either probe.
- `carried/`: actual atlas cases, including S4 saved content at both widths
  and missing-decision refusal parity **FAIL, 2 HTTP rows versus 1 MCP row**.
- `verification/`: exact command output, collections, mutation reds, build,
  final structural audit and reproducer scripts. A failed output remains a
  failed output; later passing output does not replace it.

[Rehearsal narrative and shot table](../../../../../../docs/internal/philo/phase-5/his-words/rehearsal.md).
[Lane report](../../story-04-lane-report.md).
