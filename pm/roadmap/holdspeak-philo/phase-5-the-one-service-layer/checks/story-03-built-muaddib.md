# Check — Muad'Dib, 2026-09-24, counsel on built: PHILO-5-03 (PR #636 @ 4232ac5d)

Written by Muad'Dib's Opus 5.5 counsel worker (read-only; one op case and one browser case re-run headless in a fresh HOME; the skew, headless-page and op-removed mutations reproduced; scoped tests 191 passed). Conditions C1–C3 paid by Muad'Dib in this commit.

VERDICT: RATIFY-WITH-CONDITIONS

FINDINGS:
1. `op` reaches the registry INSIDE the owning hub: `Hub.mcp` POSTs JSON-RPC to `{hub.url}/api/mcp` with the hub's own owner token (`graph_walk.py:1530-1552`); `_op_call` refuses without that transport (`:659-661`); `OP_MCP_PROJECTIONS` (`:519-539`) is a name map only; inside the hub `desk.*` → `ops()` → `for_runtime` → `invoke` (`mcp/tools.py:590-610,793-810`); no `holdspeak.operations` import in the rig or `philo5_pairs.py` (the rig imports `holdspeak.*` only in the `_serve` subprocess = the hub); every one of the 24 op observations has its `db_path` under its run HOME.
2. Headless works: Playwright imported only when not headless (`:4519-4556`); `snapshot(None, …)` no `page.evaluate` (`:720-738`); restart detail reads `op`/`op_reads` (`:3095-3129`); a re-run of `case.a1.decision_face_create.opens_and_reopens.op` headless: PASS settled, 2.727 s, `shots: []`, durable `decision.read` unchanged after the `invalid decision status: bogus` refusal; `CAL-op-*` six OK; the retained S3 op restart: `same_db_path: true`, new PID, meeting/summary/receipt kept.
3. All 18 named pairs present (+2 replayed): `pairs.json` 20/20 pass; atlas.json 81 cases / 7 op; atlas-phase3.json 31 / 11 op + 4 replay variants; the comparator reads durable state (summary: title/`transcription_status`/`summary_state`, `philo5_pairs.py:1006-1034`; decision: title/status/markdown/context `:1037-1045`), never envelopes or model text; face verdicts browser-only (`faces.json` 20 pass / 13 None; S4 no face verdict at either width); real / replayed / none in separate folders with `provenance.engine_mode`; op durations: 18 base pairs min 2.635 s, median 2.735 s, max 13.628 s (no-engine 13 runs: 2.635/2.709/4.859 s; real-engine 5 runs: 10.453/12.211/13.628 s).
4. Astra's amendments are lawful narrowings: breakage under a process-local `TZ` (the machine clock untouched; both transports under the same condition; the same HTTP 404 seed; the exact `(text, detail)` cause set + source_ref→id mapping still compared; op-side breakage production disclosed as not established; caveat: `_evening_zone` always picks hour ≈20 — the evening path only); Thought: MCP `thought.create` refuses empty raw text (a real constraint), the final saved body compared exactly (`:1506-1510`) plus id/raw_sha/revisions/time/cursor per run (`:1424-1505`).
5. The ledger items do not contradict story 02: the shelf refusal is the MCP tool schema enum (`tools.py:459`, validated `:767`) — transport-level validation, recorded `refusal_origin: transport_schema`, excluded from exit 1; but it IS contract drift (the descriptor says it will not pre-empt, `operations.py:421-431`). The missing-decision fallback (`routes/decisions.py:58-61`: registry NotFound → the lifecycle service) makes ONE missing HTTP read produce TWO broke rows — a real owner-visible defect, correctly ledgered for 04. The UNKNOWNs are honest residue (missing-decision parity was never a named pair; S4 at 393 carries `face_verdict: None`).
6. Fences reproduced: `op` removed → `CAL-op-success` BLOCKED ("step kind 'op' is not in the rig's vocabulary"); a headless snapshot at a face selector → FAIL (UNRESOLVED); skew on `decision.read` title → comparator exit 1 (`decision.title`, `durable_unchanged`, `refusal.durable_projection` fail); scoped 191 passed; `--check` 0; validate OK; `dw check` ok. `verify_fences.py` not re-run (writes into tracked assets).
7. Evidence law holds; no product source touched; merge-base OK.
8. Two record defects (→ C1, C2): (a) replayed runs PASS `receipt_lan_identity` (`host: 192.168.1.43`) because the replay seam (`graph_walk.py:1754-1773`) sits below the product receipt — that check proves route/host identity only, never an engine contact; (b) 31 of the 40 selected observations ran against OLDER atlas bytes (`provenance.atlas.sha256`), every observation `dirty: true` against c4d46498 with no rig-source hash; a spot check found representational differences only — not a full proof.
9. Tenet 1: one rig, but `philo5_pairs.py` is 1,704 lines with 16 hand-written families chosen by case-id substring — a second judging layer beside the atlas predicate language; lawful (cross-transport equivalence cannot be a single-case predicate), expensive to extend, brittle to renames.

CONDITIONS (paid here):
- C1. The lane report and evidence say what `receipt_lan_identity` proves (route/host identity on the receipt) and that real-engine status rests on `engine_mode`, the absent replay hash and the `engine_identity` probe.
- C2. The evidence states that 31/40 selected observations ran against earlier atlas bytes on a dirty tree and that no case-level equality check (executed steps vs committed case) exists in `verify_pairs.py`.
- C3. The ledgered items are carried into story 04's file as WORK: SAVED shown on an `import_failed` VTT import; S4's blank reading view at 393; the two broke rows for one cause; the shelf schema drift + a schema-alignment fence; plus a structural fence that the rig never imports `holdspeak.operations` in-process (MISSED 5) and the op observations' forced `viewport: 1440` label (MISSED 4) ledgered.

MISSED (ranked by owner cost): 1. SAVED on the Arrival while durable state is `import_failed` (the face lies; `current-phase-status.md:251` Notes only). 2. S4 at 393: after Done the reading view is blank; cause unverified. 3. "2 things broke" for one missing read (`decisions.py:58-61`). 4. Op observations forced to `viewport: 1440` and a `-1440` run id. 5. No structural fence against an in-process `holdspeak.operations` import in the rig.

TUESDAY: Nothing on his screen changes; he gains confidence that a page and an agent land the same durable state for the whole loop; the no-engine headless path takes ~2.7 s. His real exposure is the three defects this lane found and did not fix — they land in story 04 as work.

UNKNOWN: real-engine op runs not re-run (Astra's retained S2–S5 only); `verify_fences.py`/`verify_pairs.py` not re-run (they write tracked assets); no rig-source hash in the observations; Astra's worktree reflog shows a `reset: moving to HEAD` (kind unknown; tree clean now); whether the product receipt should record the replay seam is undecided.
