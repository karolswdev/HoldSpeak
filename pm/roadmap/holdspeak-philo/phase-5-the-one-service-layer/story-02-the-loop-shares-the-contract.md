# PHILO-5-02 - The loop shares the contract

- **Project:** holdspeak-philo
- **Phase:** 5
- **Status:** done
- **Depends on:** PHILO-5-01
- **Unblocks:** PHILO-5-03
- **Owner:** Muad'Dib (Opus 5.5); Astra checks on built
- **Council tag:** Astra r2 findings 3, 4, 5; the owner's D1

## Problem

The rest of the loop has the same two hand-wired paths, and three of them diverge in behaviour. MCP builds a clockless brief service (`holdspeak/mcp/tools.py:685`; HTTP supplies the clock at `routes/monday_brief.py:97`) and an unwired summary service (`holdspeak/mcp/tools.py:879`, losing the `runtime_queue` notification at `holdspeak/services/meeting_intel_service.py:78`). HTTP summary `_svc()` prefers the factory over the composed instance (`holdspeak/web/routes/meetings/intel.py:16`, `holdspeak/web_server.py:932`). MCP resources construct their own brief, meeting and Thought services (`holdspeak/mcp/resources.py:504,523,566`). Import has no MCP tool and carries config, transcriber and tmp-file custody (`holdspeak/web/routes/meeting_import.py:52,90`). Shelf write/read has no MCP tool at all.

## Scope

- **In:** the corrected inventory (phase status, "The pilot inventory") minus the decision rows: `meeting.import` with the MCP import intake (its client contract settled between the brains BEFORE the worker brief; a raw `tmp_path`, configuration object or transcriber factory is not a client contract); `meeting.list` / `meeting.read` (the summary's planning, completion and terminal read — no separate summary-read implementation); `meeting.summary.run` with the `runtime_queue` callback; `brief.generate` / `brief.latest` with the producer clock (preserving `monday_brief.generate`'s `generate=true`); `brief.shelf.write` / `brief.shelf.read`; `thought.create` / `thought.save` / `thought.read` / `thought.workbench.read` / `thought.list`; the MCP resource bindings (`holdspeak://meetings/{id}`, `holdspeak://briefs/latest`, `holdspeak://thoughts/…`); the HTTP summary factory-vs-instance; gaps A–F closed.
- **Out:** everything the phase status lists as out; already-open brief refresh (a fresh/reopened Desk read is accepted); any face change.

## Acceptance criteria

- [x] Every inventory row above reaches one declaration and the hub's one live instance by HTTP and by MCP (tool or resource), object identity asserted in-hub; the duplicate MCP definitions retired; the residual set shrinks by exactly these entries. — 13 new descriptors (`holdspeak/operations.py:247-545`), bound once to the hub's instances (`holdspeak/runtime/composition.py:496,525`); HTTP `web/routes/meetings/crud.py:72,104`, `meetings/intel.py:99`, `meeting_import.py:94`, `monday_brief.py:115,126,134,146`, `primitives/thoughts.py:65,83,141,157,203`; MCP `mcp/tools.py:727,878,880,993,1075,1081,1084,1086`, `mcp/families/thought.py:244,334`; resources `mcp/resources.py:529,542,548,565,586`. Identity + every call through `invoke`: `tests/unit/test_philo5_the_loop.py:191,223`. The hand-written MCP branches are deleted, not shadowed. Residual 327 -> 320: exactly 7 MCP identities paid (`residual-set.json` `paid`, story PHILO-5-02); one HTTP identity MOVED, not paid (`moved`: the brief router's construction now runs only for a partially wired context, kept in the route for the Phase 4 route tests' seams). Public tools 225 -> 228 (`public_tools_added`), a separate measurement.
- [x] Gap A: an MCP brief generate uses the hub's producer clock (same producer day as HTTP). Gap B + F: an MCP summary run fires the `runtime_queue` notification (the fence names `runtime_queue`, not `desk_changed`). Gap C: HTTP summary uses the composed instance, not a factory. Gap D: MCP resources read through the hub's instances. Gap E: import keeps its multipart/file custody in the transport. — A: `test_philo5_the_loop.py:306` (MCP day 2026-10-13 -> 2026-10-14 on the hub clock; base dated it by the wall clock); B+F: `:354,:370`; C: `web_server.py:933` (factory removed), `meetings/intel.py:17` (instance first), fence `:203`; D: `mcp/resources.py:433`, fence `:223`; E: HTTP multipart unchanged, MCP intake copies the caller's file into hub custody (`mcp/tools.py:686`), the shared post-custody call takes `held` inputs (`operations.py:577-614`), fences `:392,:420,:445,:672`.
- [x] A4's KEPT is working-copy save: revision checks, workspace cursor and `working_note.last_modified` preserved (`holdspeak/web/routes/primitives/thoughts.py:191`). — `thought.save` binds `update_working` (`operations.py:446`), never `complete`; fence through both transports `test_philo5_the_loop.py:461` (revision conflict code equal on both; `workspace_cursor_conflict` with the fresh workbench; `last_modified` moves; state unchanged).
- [x] Phase 4 invariants preserved: Generate remains reachable in every ratified state; pending, failure and success receipts survive the rendered transition. — no face change; `cd web && npx vitest run src/desk/chair`: 15 files, 89 tests passed (evidence).
- [x] Phase 4 invariants preserved: decision rows lead; recency comes from persisted `created_at`, survives reload, and respects the existing three-row cap. — through MCP: `test_philo5_the_loop.py:513` (MCP-created decisions, MCP-generated brief newest first, same order on MCP and HTTP reload); the three-row cap is the face's (vitest chair green); `test_philo4_02_*` green.
- [x] Phase 4 invariants preserved: one source failure appearing in two briefs produces distinct brief-scoped item IDs, while old rows and shelf state remain unchanged. — through MCP (the failure's real producer is now an MCP shelf write): `test_philo5_the_loop.py:533`; `test_philo4_03_breakage_ids.py` green unchanged.
- [x] Phase 4 invariants preserved: `ALL n HANDLED` counts the nonempty Arrival item set with valid Ack/Defer states, excludes THIS WEEK, and cannot appear while a hidden raw-ID row remains unhandled. — MCP Ack/Defer write the hub's shelf, which HTTP `/api/brief/shelf` and `/api/brief/latest` (what the face reads) return: `test_philo5_the_loop.py:581`; the rendered handled-line fences in `src/desk/chair` green; `test_philo4_04_*` green.
- [x] Compatibility: names, arguments, defaults, envelopes, resource URIs, refusals and palette membership preserved; existing service-owned admissions and receipts preserved with no double admission; any missing admission named, not certified. — the compatibility table `tests/unit/test_philo5_loop_compat.py` (58 rows + shapes): 58 passed on a `git archive 6e707ff3` copy and 58 passed built, identical row outcomes (`docs/internal/philo/phase-5/the-loop/compat-base.txt`, `compat-built.txt`); story 01's `test_philo5_compat.py` green; palette refusal through dispatch for the new tools `test_philo5_the_loop.py:657`; the new tools classified and outside CHAT_PALETTE `:643`. No kernel call added or removed. The decision admission: named and ruled "no lawful kernel place yet" with the fence of the same absence on both transports (`:617`; phase status "Discovered missing admissions").
- [x] Fence law: every behavioural fence is red pre-fix through the real producers (real services, the real hub, the real lock and config paths — no test double that lies about the field the check reads); new structural invariants are proved by deliberate mutations that turn the fence red. An import failure or an unavailable symbol is not the required red. — base reds (the same file on the base copy): gap A `AssertionError: 2026-09-24T14:...` (wall clock), gap B `assert 'runtime_queue' in []`, gap C the hub's factory present, the invoke recording `[]` (`the-loop/red-base-loop-fences.txt`); eleven deliberate mutations M1-M11 each red (`the-loop/red-mutations.txt`); the residual fence red against base (`the-loop/red-base-residual.txt`). The new import/shelf tools' base reds are "Unknown tool" and are NOT claimed as the required red; their invariants are proved by M2/M3 instead.

## Effort (council-style estimate, not a promise)

4–5 days (Astra r2)

## Test plan

- **Unit:** per-operation identity and behaviour fences (clock, `runtime_queue`, factory-vs-instance, resources, save revision) red pre-fix through the real services; the four Phase 4 invariant fences re-run; compatibility and residual guards.
- **Integration:** the loop by HTTP and by MCP against one isolated hub, durable state read back across a restart.
- **Manual / device:** none (proof mode: rehearsed; story 04).

## Notes

- 2026-09-24 — chartered from draft r3 + Astra r2 (`checks/charter-astra-r2.md`, findings 3, 4, 5 and the story-02 amendment).
- 2026-09-24 — BUILT on `feat/philo-5-02-the-loop` (Muad'Dib lane, Opus 5.5). The MCP import intake as settled by Muad'Dib: `meeting.import {path, title?, occurred_at?}` -> `{meeting_id, transcription_status}`; refusals `path_not_absolute`, `path_not_readable`, `unsupported_type`, `file_empty`. `monday_brief.shelf` also takes `state: null` (clear), because the face's Ack/Defer toggle sends null; the brief named only the two states. The decision admission: recorded "no lawful kernel place yet" (phase status). Astra checks on built.
