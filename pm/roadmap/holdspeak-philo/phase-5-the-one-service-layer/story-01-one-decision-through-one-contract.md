# PHILO-5-01 - One decision through one contract

- **Project:** holdspeak-philo
- **Phase:** 5
- **Status:** done
- **Depends on:** -
- **Unblocks:** PHILO-5-02, PHILO-5-04 (the Codex path)
- **Owner:** Muad'Dib (Opus 5.5); Astra checks on built
- **Council tag:** Astra r2 findings 1, 2, 6, 7; the owner's D2 and D3

## Problem

Decisions reach HTTP through `POST/PUT/GET /api/decisions` (`holdspeak/web/routes/primitives/decisions.py:35`) and MCP through the generic `desk.create/update/list/get` with `kind="decisions"` (`holdspeak/mcp/tools.py:554,689`): two hand-wired paths to one job. No application-operation contract exists; the one descriptor in production is a narrow model-tool projection (`services/tool_capability_service.py:219`). The stdio MCP entry still has a standalone mode that composes services locally and claims the owner lock (`holdspeak/mcp/server.py:459`), which D2 retires. The Codex path of D3 is broken on two seams: the rig claims the lock without a port (`scripts/graph_walk.py:1494`), which `discover_hub()` rejects (`holdspeak/mcp/server.py:119`); the rig's token is not persisted to `meeting.web_auth_token` (`scripts/graph_walk.py:1517`, `holdspeak/mcp/server.py:144`, `holdspeak/web_server.py:322`). `scripts/astra` forwards `-C` but no `-c` overrides (`scripts/astra:20,50`).

## Scope

- **In:** `decision.create` / `decision.update` / `decision.read` / `decision.list` through one descriptor-module contract bound at hub composition, reached by the HTTP routes and by the MCP decision path; the export `docs/generated/operations.json`; the compatibility guards and the identity-based residual baseline/fence (`(transport, entry point, discriminator)`, pinned to `4b4f8d94`); the complete-roster doc check (generated + legacy); RETIRING the production standalone MCP entry (`mcp/server.py:459`) with its docs and tests; the Codex path: the rig publishes its loopback endpoint through the existing lock producer and persists the matching token through the isolated config path; the wrapper's narrow repeated `-c` passthrough; the concrete Codex configuration (lane executable + cwd absolute, proxy HOME = hub HOME, proxy mode only, default DB path under that HOME, Codex auth kept apart).
- **Out:** everything the phase status lists as out; the other pilot operations (story 02); the generic `desk.*` tools for other kinds; the rig's `op` step (story 03).

## Acceptance criteria

- [x] The contract is a small explicit descriptor module (name, version, args schema incl. empty objects, description, result and refusal shapes, completion/read identity, transport exposure) bound to the hub's one live decision service at composition; no decorator/plugin framework; the model-tool descriptor and kernel `OperationSpec` keep their roles. — `holdspeak/operations.py:57` (descriptor), `:228` (the catalogue: four decision operations; `decision.list` takes the empty object or a `limit`), `:300` (`bind`), `:262` (`invoke`); bound at `holdspeak/runtime/composition.py:479`; `tool_capability_service.py` and `kernel/model.py` untouched.
- [x] HTTP and MCP reach the same declaration and the same live instance within one hub (object identity asserted in-hub); durable state compared across restart. — `tests/unit/test_philo5_one_decision.py` `TestOneInstanceInTheHub` on the REAL `MeetingWebServer` app (identity; every HTTP and `/api/mcp` decision call recorded by the one registry's `invoke`), `test_durable_state_survives_a_new_hub_over_the_same_database`, and the Codex rig restart (`evidence-story-01.md`). The hub's HTTP reads are served by the project-decisions router (`web_server.py:1158` before `:1233`); its desk branches now call the contract (`web/routes/decisions.py:53,58`).
- [x] The duplicated MCP decision path is retired; `desk.*` stays for its other kinds with unchanged names, arguments, defaults, envelopes, refusals and palette membership; palette refusal tested through dispatch (`holdspeak/mcp/tools.py:1045`) and catalogue filtering. — `mcp/tools.py:561,567,573,579`; `test_palette_refusal_through_dispatch_writes_nothing`, `test_palette_refusal_through_the_http_jsonrpc_handler_is_mcp_005`, `test_published_desk_tool_schemas_are_unchanged_and_admit_every_contract_payload`, `test_other_kinds_still_take_the_generic_path`. Round one claimed "one changed refusal"; Astra's check on built proved that false (typed fields refused `title=123`, `decided_at=20260924` and `tags="one"`, which main accepted; `?limit=501` answered 500 rows). Round two: the create/update fields are type-permissive (`holdspeak/operations.py:128`), `decision.list` takes the caller's `limit` (`:206`, `web/routes/decisions.py:53`, `services/primitive_service.py:155`), and a `decision_id` inside update data stays refused (`operations.py:284`). The compatibility table `tests/unit/test_philo5_compat.py` (11 cases) is green on main, red on round one (9 of 11), green on round two. What changed, stated in full: (a) the refusal TEXT for an unknown field in `desk.create kind=decisions` data (`Invalid arguments for decision.create: ...`, was a Python `TypeError` text) and for a `decision_id` in update data (HTTP answers 400, was 500); (b) one deliberate narrowing by the settled Effects position: an authority field (`owner`, `actor`, ... `operations.py:41`) in `decision.update` data was accepted and ignored on main and is now refused (`authority_in_arguments`); on create it was already refused (a `TypeError`). Same `isError` envelope.
- [x] `docs/generated/operations.json` is generated and drift-guarded; the complete tool roster (generated + legacy, `gen_mcp_sidecar_doc.py:34`, `test_mcp_sidecar_doc_drift.py:41`) regenerated and drift-guarded. — `scripts/gen_operations_json.py`, `test_operations_export_matches_the_catalogue`; roster regenerated (225 tools, unchanged), drift guard green in the captured run.
- [x] The residual fence lists identities with reasons; a new residual identity FAILS (red proved on a copy of main); paid entries leave in the same commit; the public tool count and the residual implementation set are reported as separate measurements. — `docs/internal/philo/phase-5/residual-set.json` (334 -> 327; 7 paid), `scripts/residual_census.py`; red on the main copy (7 NEW identities) and mutation m4; public tools 225 -> 225.
- [x] The standalone MCP entry is gone from production (`mcp/server.py:459`), with its docs and tests updated; `holdspeak-mcp` is proxy-only. — `serve()` at `mcp/server.py:458`; `docs/MCP_SIDECAR.md`, `docs/SECURITY.md`, `runtime_lock.py`; fences red on main: the sidecar with the old variable set creates no DB and claims no lock; `composition.current()` improvises no root.
- [x] The Codex path, against an isolated HOME and never the desk: the rig hub's port is published through the existing lock producer and its token persisted through the isolated config path; from a FRESH Codex session with the wrapper's `-c` passthrough, the proxy discovers the hub, authenticates, performs one real decision write, and rediscovers the hub after a restart. The effective Codex configuration (HOME, lock, DB path) is retained as evidence. — `scripts/graph_walk.py:1484,1517`, `scripts/astra:21,25,54`; `docs/internal/philo/phase-5/one-decision/codex/` (session `01a0d4cc-8b58-78a1-b823-8976291fae32`, decision `decision_32e0c6ba0eea`, restart pid 57030 -> 57993 on port 59823, `mcp_errors: []`); Python fence `tests/unit/test_philo5_codex_seams.py`.
- [x] Fence law: every behavioural fence is red pre-fix through the real producers (real services, the real hub, the real lock and config paths — no test double that lies about the field the check reads); new structural invariants are proved by deliberate mutations that turn the fence red. An import failure or an unavailable symbol is not the required red. — `docs/internal/philo/phase-5/one-decision/red-main-behaviour.txt` (4 assertion reds on a `git archive origin/main` copy), `red-main-residual.txt`, `red-mutations.txt` (m1-m6).

## Effort (council-style estimate, not a promise)

3–4 days (Astra r2)

## Test plan

- **Unit:** the contract binding and identity fence; the residual fence (red on a copy of main); compatibility and palette-through-dispatch guards; the lock-publishes-port and token-persisted fences against the real lock producer and config path.
- **Integration:** the Codex → stdio proxy → isolated rig hub run (discovery, auth, one decision write, restart rediscovery), transcript and effective configuration retained under this phase's assets.
- **Manual / device:** none (proof mode: rehearsed; story 04).

## Notes

- 2026-09-24 — chartered from draft r3 + Astra r2 (`checks/charter-astra-r2.md`, findings 1, 2, 6, 7 and the story-01 amendment).
- 2026-09-24 — the owner ratified the charter; build starts (Muad'Dib lane).
- 2026-09-24 — built on `feat/philo-5-01-one-decision`. Found: in the hub the project-decisions router answers the decision GETs before the primitives router; paid by routing its desk branches through the contract. Found, not paid (out of scope): the desk's Info-window rename sends `name` for a decision, which the update ignores (`web/src/desk/components/InfoWindow.tsx:31`); the contract preserves that behaviour.
- 2026-09-24 — round two after Astra's check on built (`checks/story-01-built-astra.md`, BOUNCE): paid finding 1 (the HTTP list limit passes through the contract; 501 rows at `?limit=501`, as on main) and finding 2 (previously accepted inputs preserved; the compatibility table; the "one changed refusal" claim corrected above). Finding 8's two inherited debts got homes: the Info-window rename bug -> `pm/roadmap/holdspeak/BACKLOG.md` + the phase's "Decisions deferred"; the admission gap (a real Codex decision write made zero kernel operations and zero receipts) -> the phase status "Discovered missing admissions", for story 02's effects acceptance. Not fixed here.
- 2026-09-24 — Astra r2 on built: RATIFY-WITH-CONDITIONS, paid (`checks/story-01-built-astra.md`). Authority ruling: the narrowing stands (eight reserved fields accepted-and-ignored on main → refused; `principal` already failed on main — a refusal-shape change only).
