# PHILO-6-02 - The brief's truth (one time, honest counts, human words)

- **Project:** holdspeak-philo
- **Phase:** 6
- **Status:** done
- **Depends on:** -
- **Unblocks:** exit 3 (one time format, counts that agree with their labels, no raw ids)
- **Owner:** Muad'Dib (Opus 5.5); Astra checks
- **Closure finding:** BACKLOG "PHILO-5-04 follow-ups", ledger rows 5 and 6; Astra's check of the drafts, finding 2 rows 2 and 4, MISSED 2
- **Canvas:** (a) and (c) none; (b) a SMALL CANVAS AMENDMENT only if a count label or a count meaning changes

## Problem

On every fresh Arrival read the brief tells two stories (the PHILO-5-04 receipt seam made it visible; `final/20260925T001407Z-his-words-real/shots/brief/1440.png`):

- **(a) Two time formats.** The head says `GENERATED SEP 25 18:19` (`holdspeak/web/routes/monday_brief.py:47-58`, a 24-hour label built on the server). The receipt says `6:19 PM` (`web/src/desk/chair/briefEgress.tsx:47-50`, `toLocaleTimeString`). One brief, two times.
- **(b) Two counts.** The head says `5 THINGS WAITING`; the receipt says `Brief ready · 6 items`. The receipt counts every row in every section (`briefEgress.tsx:43-46`). The Arrival counts decisions, changed, broke and waiting rows that are not on the shelf and are not raw ids (`web/src/desk/chair/ChairHome.tsx:875-886`); THIS WEEK is excluded. The two counts have DIFFERENT lawful meanings (Astra, finding 2): the generated snapshot vs what is unhandled now. They differ after Ack/Defer. The fix is not "one count".
- **(c) Raw words.** The producer stores a pipeline item with the text `MeetingIntelService.run_intelligence` (`holdspeak/services/monday_brief_service.py:500`). The receipt counts it; the Arrival filters it as a raw id (`ChairHome.tsx:886`). The same raw wording is in breakage titles, `{service}.{method} failed` (`monday_brief_service.py:597`). If only the item is fixed, a failure stays hidden (Astra, MISSED 2). Whether `2 more` renders the raw name is unwalked.

## Scope

- **In:** (a) one time format on the brief through an existing idiom (canvas-free); (b) both counts keep their meanings and the historical snapshot, and the labels say what each counts; (c) human producer wording for the pipeline item and the breakage titles, naming the recorded action accurately (a summary REQUEST is not a completion); the fences; the rig case at 1440 and 393.
- **Out:** a redesign of the BRIEF section; a new row kind; the headline sentence (`MondayBriefService._compose`, a Phase 4 ledgered debt); everything the phase status lists as out.

## Acceptance criteria

- [x] (a) One time format for the brief's generated time across the head and the receipt, through an existing idiom (no new format invented). The stored `generated_at` is unchanged. Fence: the rendered head and receipt of one brief carry the same time string; red pre-fix (`18:19` vs `6:19 PM`). **Decision: ONE clock** — the producer's `generated_at` in the viewer's zone, the Brief view's existing stamp (`web/src/desk/pullouts/views/BriefView.tsx:117,125`), on the caption (`web/src/desk/chair/ChairHome.tsx:166`) and the receipt (`web/src/desk/chair/briefEgress.tsx:51`). Fence across offsets (hub `Etc/GMT+11`, browser `Europe/Warsaw`): red on `origin/main` (`expected '8:19 PM' to be 'SEP 25 07:19'`), green on the branch (`SEP 25 20:19` on both); `docs/internal/philo/phase-6/brief/`.
- [x] (b) The head count and the receipt count each keep their lawful meaning: the receipt counts the generated snapshot, the head counts the Arrival rows with an Ack/Defer (THIS WEEK excluded, the Phase 4 ask-2 ruling). The historical snapshot is not rewritten. The receipt and the head keep DISTINCT, accurately labelled counts (the generated snapshot vs the currently unhandled Arrival set; THIS WEEK excluded from the Arrival) — never conflated. IF a label or a meaning changes: a SMALL CANVAS AMENDMENT (both widths, exact ASD-STE100 strings) is ratified by the owner BEFORE build; if nothing changes, the story records "no canvas needed" with the reason. **No canvas needed:** no label and no meaning changed (`ChairHome.tsx:2096` `BRIEF · N THINGS WAITING`; `briefEgress.tsx:43-46` `Brief ready · N items`); the unexplained gap was the raw-id filter (`ChairHome.tsx:893`) hiding raw producer rows from the head while the receipt counted them, which (c) removes (`evidence-story-02.md` "(b)").
- [x] (b) Consistent-count fence: one brief from the REAL producer (`_compose`) with a raw pipeline item, a THIS WEEK item and one shelved row; each rendered count equals what its label claims. Red pre-fix (5 vs 6 unexplained). (`tests/unit/test_philo6_02_brief_truth.py` + `web/src/desk/chair/__tests__/briefTruth.philo602.test.tsx`; red `expected 'BRIEF · 1 THING WAITING…' to contain 'BRIEF · 3 THINGS WAITING'`; green.)
- [x] (c) The pipeline brief item (`monday_brief_service.py:500`) and the breakage titles (`:597`) carry human wording that names the recorded action accurately; no `Service.method` text reaches a stored brief item. A summary request is worded as a request, not a completion. (`holdspeak/services/monday_brief_service.py:86,476,560,669`: `Summary requested: <title>`, `Summary did not start: <title>`, a fallback with no raw names.)
- [x] (c) Fence: an item minted through the real producer (the real `@observe_service` failure and the real intel run) has no raw `Service.method` text; red pre-fix. Words and the ACTIVE atlas fences change together. (Red on `origin/main`: `assert ['MeetingInte...gence failed'] == []`; the atlas s5 breakage check and op capture re-worded with `tests/unit/test_philo_graph_atlas.py:622`.)
- [x] **Round 2 — the words follow the recorded OUTCOME and never use implementation vocabulary** (Astra's check on built, findings 1 and 5). (1) A failed operation makes NO Changed row; its one line is the Broke row (`holdspeak/services/monday_brief_service.py:723-724`: the group's last event is the operation's outcome — the outermost observed call emits last, a retry follows its failed attempt). Why omit rather than reword: a failed call changed nothing, and the Broke collector already tells it once; a second, failure-worded Changed row would make two rows of one cause. The audit covered every success line the collector writes: `Decision recorded`, `Summary requested` (a REFUSED request now makes only `Summary did not start`), `Brief triage saved` (never reachable in Changed: `shelve` carries no change marker), the generic `<Object> <past>` line. (2) The generic fallback is `<Object> did not <verb>`: the object from the method's noun, else the service's enumerated object (`_SERVICE_OBJECTS`, every `@observe_service` class, fenced complete), never a class name; the explicit table covers every observed producer of the Phase 5/6 runs (`:89-146`); `Primitive: get decision did not complete` is now `Decision did not load`. Fences (real producers: `create_from_desk` on a missing decision, the refused summary request, the failed triage, a lifecycle transition, the desk decision read): `tests/unit/test_philo6_02_round2_outcome_words.py` + `web/src/desk/chair/__tests__/briefOutcome.philo602r2.test.tsx` (the Arrival and the Brief view), red on a `git archive f6c0c0d1` copy (`assert ['Decision re...cture review'] == []` … `first extra item: 'Decision recorded'`; the rendered `expected 'BRIEF · 9 THINGS WAITING…' not to match /Decision recorded|Summary requested|…/`), green on the branch (`docs/internal/philo/phase-6/brief/round-2/`). Words and the atlas selectors changed together (the op variant of s5 pins row 0 after a words check: both reads of one missing decision now say `Decision did not load`; `tests/unit/test_philo_graph_atlas.py`).
- [x] **Round 3 — a brief line states only what the RECORD proves** (Astra's round-two check, findings 1, 2 and 4; Tenets 3/4/7, Article VI). (1) **The outermost call is the outcome.** The observer stores CALL-START time (`holdspeak/services/observer.py:113`), so a nested call sorts AFTER its parent; round two's "last event" was an inner call. A correlated operation's outcome is now the call that STARTED first (`holdspeak/services/monday_brief_service.py:263-272`, `_outermost`, used at `:742`); on a start-time tie the enclosing call holds the HIGHEST insertion id (it emits in `finally`, after the calls it encloses) — deliberately not "lowest id", which picks the child. The grouping takes every call of the correlation, so a non-change parent is never replaced by its inner call. An uncorrelated group (a failed call and its retries, siblings) keeps its last attempt (`:740`). (2) **No inferred outcomes.** A Changed line is written only from the table below (`:97-166`); a successful call outside it writes NOTHING (`:765`); a failed call outside it is `<Object> did not complete` with the service's enumerated object (`:255-260`). The frozen-clock positive control is deleted (`tests/unit/test_philo6_02_round2_outcome_words.py`); the formatter-with-`"{}"` fence is narrowed to the Broke lines' spelling shape and says so.

  | Recorded call | Changed line | Written only when the record shows | DB state the line names (fence) |
  |---|---|---|---|
  | `MeetingIntelService.run_intelligence` | `Summary requested: <meeting title>` | success (success returns `state: queued` only) | an intel job for the meeting (`rig_db.intel.get_intel_job`) |
  | `DecisionRecordService.create` | `Decision recorded` | success (one INSERT) | the `decision_records` row of the returned id |
  | `DecisionRecordService.create_from_desk` | `Decision recorded: <desk decision title>` | success AND a successful `create` in the same operation (`_PROVEN_BY`, `:149`); an existing record returns without one: no line | one `decision_records` row with `source_type = desk`, `source_id` = the desk decision |
  | `DecisionRecordService.create_from_meeting` | `Decision recorded` | same rule as above | one `decision_records` row for the meeting decision |
  | `FollowThroughService.complete`, result `verb = done` | `Follow-up completed: <task>` | the RESULT's `verb` (`_RESULT_WORDS`, `:159`); `replayed: true` writes nothing | `action_items.status = done` |
  | … `verb = delegate` | `Follow-up delegated: <task>` | the result's `verb` | `action_items.owner` = the named owner; status unchanged |
  | … `verb = reopen` | `Follow-up reopened: <task>` | the result's `verb` | `action_items.status = open` |
  | … `verb = due` | `Follow-up date set: <task>` | the result's `verb` | `action_items.due` = the day |
  | … `verb = dismiss` / `snooze` | none | no row names them | — |
  | reads (`DecisionLifecycleService.get_decision`, `PrimitiveService.get_decision`, `PrimitiveService.get_note`, `MondayBriefService.get_latest`, `DecisionRecordService.due_for_review`, `FollowThroughService.people_store_state` (store READINESS), `ProfileService.list_inference_targets` (execution DESTINATIONS)) | none | — | Broke only: `Decision did not load`, `Note did not load`, `Brief did not load`, `Decision reviews did not load`, `People status did not load`, `Destinations did not load` |
  | `MondayBriefService.generate` (may return the CACHED brief) | none | — | Broke only: `Brief did not complete` |
  | reconciliations (`ReactionService.refresh_due_watches`, `ReactionService.process_pending`, `ProjectService.recover_ask_tasks_on_startup`, `GateService.invalidate_held_on_startup`) | none | — | Broke only: `Watch check did not complete`, `Watch events did not complete`, `Project task check did not complete`, `Approval check did not complete` |
  | writes outside the Changed collector (`PrimitiveService.create_decision`, `MondayBriefService.shelve`, `SetupService.set_onboarding_disposition`) | none | — | Broke only: `Decision did not save`, `Brief triage did not save`, `Setup did not change` |
  | any other call | none | — | Broke only: `<Object> did not complete` |

  Fences (REAL producers, observer clock ADVANCING 1 ms per read, never frozen): `tests/unit/test_philo6_02_round3_record_truth.py` — the real `create_from_desk` asserts parent start < child start and child insert id < parent insert id in the DB, then `Decision recorded: Keep retrieval local`; the missing-decision failure stores only `Decision did not record`; delegate / reopen / date store no `completed` line (red on a `git archive 27ee9559` copy: `['Follow-up completed', 'Follow-up completed', 'Follow-up completed']`), done stores `Follow-up completed: <task>`; the coverage test runs the PRODUCER for every row that can write a Changed line (`changed_line_rows()`, `:169`) and asserts the DB change it names. Red 13 failed / green 21 passed: `docs/internal/philo/phase-6/brief/round-3/`. Not red on the round-two state (honest): the cached-brief and reconciliation fences (neither method carries a change marker, so round two wrote no line either) and the missing-decision failure.
- [x] Walked: whether `2 more` (the fold) renders a raw name, before and after, at 1440 and 393. The walk inspects the fold's DESTINATION (the Brief view): `case.philo602.brief_more.destination` FAIL on `origin/main` at both widths (`'Service.' PRESENT`), PASS on the branch at both widths (`assets/story-02-shots/before-origin-main/`, `after-branch/`).
- [x] **SCOPE AMENDMENT (2026-09-24 night, round 2; `docs/internal/ORCHESTRATION.md:69` — amended visibly in this story and in the phase status "Decisions made"; the owner may overrule at the sitting).** The criterion as chartered — "Rig: the Phase 5 rehearsal brief (`scripts/philo5_his_words.py`) at 1440 and 393; the shots show one time format and counts that agree with their labels" — is NOT met in this story. It moves to the phase's exit 3 (the closing chain), which re-runs the same driver. The box is checked for the AMENDMENT, not for the rehearsal. Why: the driver failures are INHERITED, not caused by this story. Astra ran the unchanged driver on isolated archives (`checks/lane-a-built-astra.md` finding 6): on main `66205729` Codex chose `door.add_item` (a follow-through task) and the driver failed on the missing `desk.create`; on the branch `f6c0c0d1` MCP created `decision_d3dc1cebef9e` and the driver then failed resource-list reconciliation. Homes: the tool selection/discoverability is the existing BACKLOG "PHILO-5-04 follow-ups" MCP discoverability row (Phase 7 story 01/04); the resource-list reconciliation is BACKLOG "PHILO-6 follow-ups" row 2 (Muad'Dib, before exit 3). The one time and the agreeing counts at 1440 and 393 are shown by the fold-destination walk (`after-branch/`).

## Effort (council-style estimate, not a promise)

1–1.5 days, plus the small canvas if (b) needs one

## Test plan

- **Unit:** pytest on the producer wording (real producer, `@observe_service`); vitest on the rendered head and receipt (time and counts), red pre-fix.
- **Integration:** the rehearsal brief and `case.closure.chain.s5_next_day_brief_has_it` at 1440 and 393 (exit 3), `--out` under this phase's assets.
- **Manual / device:** rehearsed, owner-reviewed shots (exit 4).

## Notes

- 2026-09-24 — chartered from XXVIII r2 + Astra's check (finding 2 rows 2 and 4: count meanings differ, a label or meaning change needs a small canvas; raw breakage wording at `:597` covered, MISSED 2).
- 2026-09-24 — Astra's charter check: (a) the counts are never conflated — the receipt never "counts what the Arrival shows"; distinct labelled counts, the conditional canvas amendment stands; (b) TIMEZONE: `routes/monday_brief.py:52` formats the timestamp's own offset while `briefEgress.tsx:47` converts through browser-local time — matching 12/24-hour formatting alone does not match the times; the story decides ONE clock (recommended: the producer's timestamp rendered in the browser's local zone on both lines) and its fence exercises DIFFERENT producer/browser offsets; (c) the "2 more" walk inspects its DESTINATION (`ChairHome.tsx:2127` opens the Brief view), not only the Arrival's overflow label.
- 2026-09-24 night — built in lane A (Opus 5.5): one clock (the Brief view's local stamp on caption and receipt), no canvas needed for the counts, human producer words; fences red on `origin/main`, green on the branch; the fold-destination walk FAIL before / PASS after at 1440 and 393. The Phase 5 rehearsal BLOCKED three times before the brief stage (Codex decision turn); that box stays open and is carried to exit 3. Evidence: `evidence-story-02.md`.
- 2026-09-24 night — ROUND 2 after Astra's check on built (BOUNCE; `checks/lane-a-built-astra.md`): outcome-dependent words (a failed operation makes only its Broke row), the implementation-free fallback with the enumerated objects, the atlas selectors with the words; the rehearsal box replaced by an explicit SCOPE AMENDMENT (carried to exit 3; inherited driver defects homed in BACKLOG); `evidence-story-02.md:60` corrected (the two runs made follow-through tasks through `door.add_item`); the Brief view's raw details and 393 overflow assigned (BACKLOG "PHILO-6 follow-ups" row 4). Reds and greens: `docs/internal/philo/phase-6/brief/round-2/`.
- 2026-09-24 night — ROUND 3 after Astra's round-two check (BOUNCE; `checks/lane-a-built-astra.md` "Round two"): the outcome is the OUTERMOST call (earliest start; tie = highest insertion id, the enclosing call emits last); a Changed line only where the record proves it (the table above: per-method rows, the follow-up line from the recorded `verb`, `create_from_*` only with its inner `create`); reads, reconciliations and cached results tell only their failure; an unknown success writes nothing. Wording choice: `Follow-up date set` (not "dated": ASD-STE100 has no verb "date"; the face verb is `Set a date`). Compat pins re-pointed from fallback words to table rows (`tests/unit/test_monday_brief_service.py`, `test_hs171_aggregate_notify.py`, `test_walk_monday_brief_126.py`, `test_brief_collectors.py`). The changed S5 `.op` case is assigned to exit 3's closing run (phase status). Evidence: `evidence-story-02.md` "Round 3".
- 2026-09-24 night — Astra r3: RATIFY-WITH-CONDITIONS (`checks/lane-a-built-astra.md` round three): the three r2 blockers repaired and reproduced; the tie deviation RATIFIED; double-Broke and the unknown-success fence gap → BACKLOG (PHILO-6 lane A follow-ups); the changed S5 `.op` case and the rehearsal stay owed to exit 3.
