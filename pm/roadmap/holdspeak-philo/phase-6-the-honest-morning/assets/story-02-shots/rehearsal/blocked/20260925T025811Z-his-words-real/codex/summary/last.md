The summary is saved and ready to view on **Architecture review** (`b5582b96`; status: `ready`).

It covers the ledger migration, summary retrieval after a restart, and recorded provider replies for isolated tests. It inherits the transcript’s accuracy limits.